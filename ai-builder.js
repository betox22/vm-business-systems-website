(() => {
  // src/ai-builder/config.js
  function resolveApiBaseUrl() {
    const params = new URLSearchParams(window.location.search);
    const apiFromQuery = params.get("api");
    if (apiFromQuery) {
      localStorage.setItem("lumaApiBaseUrl", apiFromQuery);
      return apiFromQuery.replace(/\/$/, "");
    }
    if (window.LUMA_API_BASE_URL) {
      localStorage.removeItem("lumaApiBaseUrl");
      return String(window.LUMA_API_BASE_URL).replace(/\/$/, "");
    }
    if (window.location.hostname.endsWith("trycloudflare.com")) return window.location.origin;
    const savedApi = localStorage.getItem("lumaApiBaseUrl");
    if (savedApi) return savedApi.replace(/\/$/, "");
    return "http://127.0.0.1:8010";
  }
  var API_BASE_URL = resolveApiBaseUrl();
  var API_URL = `${API_BASE_URL}/ai/website-builder`;
  var INTAKE_ASSISTANT_URL = `${API_BASE_URL}/api/ai/intake-assistant`;
  var LUMA_AGENT_URL = `${API_BASE_URL}/api/luma/chat`;
  var LYRA_EDIT_URL = `${API_BASE_URL}/api/luma/edit`;
  var CLIENT_REQUESTS_URL = `${API_BASE_URL}/client-requests`;
  var CLIENT_INTAKE_SESSION_URL = `${API_BASE_URL}/api/client/intake-session`;
  var CLIENT_AUTH_ME_URL = `${API_BASE_URL}/api/client/auth/me`;
  var CLIENT_AUTH_SESSION_URL = `${API_BASE_URL}/api/client/auth/session`;
  var CLIENT_AUTH_LOGOUT_URL = `${API_BASE_URL}/api/client/auth/logout`;
  var CLIENT_PROJECTS_URL = `${API_BASE_URL}/api/client/projects`;
  var ASSET_UPLOAD_URL = `${API_BASE_URL}/api/admin/assets/upload`;
  var SUPABASE_PROJECT_URL = "https://rzdidqclbvnqqlcaueoh.supabase.co";
  var SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ6ZGlkcWNsYnZucXFsY2F1ZW9oIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgxOTY3NzUsImV4cCI6MjA5Mzc3Mjc3NX0.R6gl2jmRRaXDzOzh_QdsAlzdzvdSyfp0muCEJGnJku0";
  var SUPABASE_AUTH_URL = "https://rzdidqclbvnqqlcaueoh.supabase.co/auth/v1/authorize";
  var SUPPORTED_LANGUAGES = ["en", "es", "fr", "pt"];
  var ASSISTANT_AVATAR_FALLBACK = "/assets/nixie_idle.png";
  var ASSISTANT_AVATARS = {
    neutral: "/assets/nixie_idle.png",
    happy: "/assets/nixie_success.png",
    thinking: "/assets/nixie_thinking.png",
    listening: "/assets/nixie_listening.png",
    speaking: "/assets/nixie_speaking.png",
    building: "/assets/nixie_building.png",
    optimize: "/assets/nixie_optimize.png",
    alert: "/assets/nixie_alert.png",
    success: "/assets/nixie_success.png"
  };
  var GUIDED_DRAFT_STORAGE_KEY = "lumaGuidedDraft";
  var GENERATED_SITE_STORAGE_KEY = "lumaGeneratedSite";
  var CLIENT_INTAKE_SESSION_STORAGE_KEY = "lumaClientIntakeSession";
  var CLIENT_WORKSPACE_IDLE_LOCK_MS = 5 * 60 * 1e3;

  // src/ai-builder/i18n.js
  var I18N = {
    en: {
      languageLabel: "Website language",
      quickForm: "Quick Form",
      guidedSetup: "Let LYRA help me",
      assistantSubtitle: "AI website assistant",
      onlineStatus: "Online",
      assistantIntro: "Hi, I\u2019m LYRA. I\u2019ll help you create your website or store step by step. You can type or use your voice.",
      intentDetected: "Great. I detected this direction:",
      selectedTemplate: "Selected template",
      catalogType: "Catalog type",
      currentInfo: "A few details collected so far.",
      livePreviewKicker: "Build workspace",
      livePreviewTitle: "Business model and site architecture",
      livePreviewText: "Your answers define the template family, catalog logic, page structure and conversion path before the editable draft is generated.",
      finalReviewTitle: "Review details",
      generateMyWebsite: "Generate my website",
      keepChatting: "Keep chatting",
      thinking: "LYRA is thinking...",
      myPages: "My pages",
      openProject: "Open",
      emptyProjects: "You do not have saved pages yet \u2014 generate your first one with LYRA.",
      loadProjectsError: "I could not load your saved pages. Sign in again and try once more.",
      startNewProject: "New page",
      startNewProjectConfirm: "Start a new page? This clears the current client draft from this browser.",
      micButton: "Use voice",
      voiceOutputOn: "Voice on",
      voiceOutputOff: "Voice off",
      voiceUnsupported: "Voice input is not supported in this browser.",
      voiceListening: "Listening... speak now.",
      voiceReady: "Voice transcript ready. Review it and press Send.",
      summaryHelper: "Make any changes before generating your first draft.",
      editDetails: "Edit details",
      reviewDetails: "Review details",
      selectedLanguage: "Selected language",
      businessName: "Client/business name",
      businessDescription: "Business/design context",
      industry: "Industry",
      location: "Location",
      servicesProducts: "Services/products",
      targetAudience: "Target audience",
      preferredTone: "Preferred tone/style",
      preferredColors: "Colors or logo",
      contactInfo: "Contact info",
      desiredDomain: "Desired domain",
      checkDomain: "Check domain availability",
      checkingDomain: "Checking domain options...",
      domainCheckEmpty: "Write a domain or business name first.",
      domainExact: "Real registrar availability",
      domainSuggestions: "Preliminary suggestions",
      logoUrl: "Logo URL optional",
      uploadLogo: "Upload logo optional",
      photoUrls: "Photo URLs optional",
      uploadPhotos: "Upload photos optional",
      assetPromptTitle: "Logo and photos",
      assetPromptText: "Upload them here and LYRA can use them for the draft.",
      salesMode: "Sales flow",
      hasLogoPhotos: "Logo/photos",
      websiteIntent: "Website type",
      reply: "Reply",
      pageTitle: "AI Website Builder",
      liveSummary: "Live summary",
      generateButton: "Generate Website with AI",
      reviewGenerate: "Generate my website",
      switchToManualForm: "Switch to manual form",
      backToChat: "Back to chat",
      send: "Send",
      skip: "Skip",
      decideStyle: "Style: Let AI decide",
      decideColors: "Colors: Let AI decide",
      decideAudience: "Audience: Let AI decide",
      decideSections: "Sections: Let AI decide",
      serverKeyNotice: "You can switch back to chat any time.",
      assistantServerNotice: "Tap Generate to create the full editable website, or keep chatting to refine the plan.",
      replyPlaceholder: "Type your answer...",
      photoUrlsPlaceholder: "One image URL per line",
      salesModePlaceholder: "online sales, quotes, or both",
      accountGateKicker: "Protected workspace",
      accountGateTitle: "Start by saving your progress",
      accountGateText: "Sign in so LYRA can save every answer, restore drafts, and keep you from repeating the same information.",
      continueGoogle: "Continue with Google",
      continueApple: "Continue with Apple",
      continueEmail: "Continue with email",
      saveEmailContinue: "Enter my workspace",
      authOrEmail: "or continue with email",
      comingSoon: "Coming soon",
      sendMagicLink: "Email me a sign-in link",
      continueDemo: "Keep viewing demo",
      sendingAssistant: "LYRA is reviewing your answer...",
      summaryUpdated: "Details updated.",
      devFallbackMissingKey: "LYRA is using a simplified response while the AI service finishes connecting.",
      localFallback: "Answer saved. Continuing with the setup.",
      localFallbackMessage: "Got it. I saved that detail and will keep going.",
      savingRequest: "Saving completed intake as client_request...",
      requestNotSaved: "Saved in this browser session. You can still generate your website draft.",
      generatedOpenAI: "Generated with OpenAI through the backend.",
      generating: "Generating...",
      generatingLong: "LYRA is designing your website. This can take about a minute.",
      generateError: "Could not generate",
      reviewStep: "Review",
      step: "Step",
      of: "of",
      railStepBusiness: "Your business",
      railStepProducts: "Products/services",
      railStepLocation: "Location",
      railStepSales: "How you sell",
      railStepStyle: "Style and audience",
      railStepColors: "Colors and brand",
      railStepReview: "Review",
      skipMessage: "Skip",
      letAiDecide: "Let AI decide",
      newClientWebsite: "New client website",
      generalBusiness: "General business",
      featuredOffer: "Featured offer",
      mainService: "Main service",
      contactRequest: "Contact request"
    },
    es: {
      languageLabel: "Idioma del sitio",
      quickForm: "Formulario rapido",
      guidedSetup: "Que LYRA me ayude",
      assistantSubtitle: "Asistente web con IA",
      onlineStatus: "En linea",
      assistantIntro: "Hola, soy LYRA. Te ayudare a crear tu pagina o tienda paso a paso. Puedes escribir o usar tu voz.",
      intentDetected: "Perfecto. Detecte esta direccion:",
      selectedTemplate: "Template seleccionado",
      catalogType: "Tipo de catalogo",
      currentInfo: "Algunos detalles recopilados hasta ahora.",
      livePreviewKicker: "Workspace de construccion",
      livePreviewTitle: "Modelo de negocio y arquitectura del sitio",
      livePreviewText: "Tus respuestas definen la familia de plantilla, logica de catalogo, estructura de paginas y flujo de conversion antes de generar el borrador editable.",
      finalReviewTitle: "Revisar detalles",
      generateMyWebsite: "Generar mi pagina",
      keepChatting: "Seguir conversando",
      thinking: "LYRA esta pensando...",
      myPages: "Mis p\xE1ginas",
      openProject: "Abrir",
      emptyProjects: "A\xFAn no tienes p\xE1ginas guardadas \u2014 genera tu primera con LYRA.",
      loadProjectsError: "No pude cargar tus p\xE1ginas guardadas. Inicia sesi\xF3n otra vez e intenta de nuevo.",
      startNewProject: "Nueva p\xE1gina",
      startNewProjectConfirm: "\xBFIniciar una p\xE1gina nueva? Esto borra el borrador actual de este navegador.",
      micButton: "Usar voz",
      voiceOutputOn: "Voz activada",
      voiceOutputOff: "Voz apagada",
      voiceUnsupported: "La entrada por voz no esta disponible en este navegador.",
      voiceListening: "Escuchando... habla ahora.",
      voiceReady: "Transcripcion lista. Revisala y presiona Enviar.",
      summaryHelper: "Haz cualquier ajuste antes de generar tu primer borrador.",
      editDetails: "Editar detalles",
      reviewDetails: "Revisar resumen",
      selectedLanguage: "Idioma seleccionado",
      businessName: "Nombre del negocio",
      businessDescription: "Contexto del negocio",
      industry: "Industria",
      location: "Ubicacion",
      servicesProducts: "Productos/servicios",
      targetAudience: "Audiencia objetivo",
      preferredTone: "Tono/estilo preferido",
      preferredColors: "Colores o logo",
      contactInfo: "Informacion de contacto",
      desiredDomain: "Dominio deseado",
      checkDomain: "Verificar disponibilidad",
      checkingDomain: "Buscando opciones de dominio...",
      domainCheckEmpty: "Escribe primero un dominio o nombre de negocio.",
      domainExact: "Disponibilidad real del registrador",
      domainSuggestions: "Sugerencias preliminares",
      logoUrl: "URL del logo opcional",
      uploadLogo: "Subir logo opcional",
      photoUrls: "URLs de fotos opcionales",
      uploadPhotos: "Subir fotos opcional",
      assetPromptTitle: "Logo y fotos",
      assetPromptText: "S\xFAbelos aqu\xED y LYRA los usar\xE1 para dise\xF1ar la primera versi\xF3n.",
      salesMode: "Flujo de venta",
      hasLogoPhotos: "Logo/fotos",
      websiteIntent: "Tipo de sitio",
      reply: "Respuesta",
      pageTitle: "Constructor de paginas con IA",
      liveSummary: "Resumen en vivo",
      generateButton: "Generar sitio con IA",
      reviewGenerate: "Generar mi pagina",
      switchToManualForm: "Usar formulario manual",
      backToChat: "Volver al chat",
      send: "Enviar",
      skip: "Saltar",
      decideStyle: "Estilo: que IA decida",
      decideColors: "Colores: que IA decida",
      decideAudience: "Audiencia: que IA decida",
      decideSections: "Secciones: que IA decida",
      serverKeyNotice: "Puedes volver al chat cuando quieras.",
      assistantServerNotice: "Toca Generar para crear la p\xE1gina editable completa, o sigue conversando para ajustar el plan.",
      replyPlaceholder: "Escribe tu respuesta...",
      photoUrlsPlaceholder: "Una URL de imagen por linea",
      salesModePlaceholder: "ventas online, cotizaciones, o ambos",
      accountGateKicker: "Workspace protegido",
      accountGateTitle: "Empieza guardando tu progreso",
      accountGateText: "Inicia sesi\xF3n para que LYRA guarde cada respuesta, recupere tus borradores y no tengas que repetir la misma informaci\xF3n.",
      continueGoogle: "Continuar con Google",
      continueApple: "Continuar con Apple",
      continueEmail: "Continuar con email",
      saveEmailContinue: "Entrar a mi workspace",
      authOrEmail: "o contin\xFAa con email",
      comingSoon: "Pr\xF3ximamente",
      sendMagicLink: "Enviarme un enlace de acceso",
      continueDemo: "Seguir viendo demo",
      sendingAssistant: "LYRA esta revisando tu respuesta...",
      summaryUpdated: "Detalles actualizados.",
      devFallbackMissingKey: "LYRA est\xE1 usando una respuesta simplificada mientras el servicio de IA termina de conectar.",
      localFallback: "Respuesta guardada. Seguimos con la configuraci\xF3n.",
      localFallbackMessage: "Listo. Guard\xE9 ese detalle y seguimos avanzando.",
      savingRequest: "Guardando intake completado como client_request...",
      requestNotSaved: "Guardado en esta sesi\xF3n del navegador. Igual puedes generar tu primera versi\xF3n.",
      generatedOpenAI: "Generado con OpenAI desde el backend.",
      generating: "Generando...",
      generatingLong: "LYRA esta disenando tu pagina. Esto puede tardar cerca de un minuto.",
      generateError: "No se pudo generar",
      reviewStep: "Revision",
      step: "Paso",
      of: "de",
      railStepBusiness: "Tu negocio",
      railStepProducts: "Productos/servicios",
      railStepLocation: "Ubicacion",
      railStepSales: "Como vendes",
      railStepStyle: "Estilo y audiencia",
      railStepColors: "Colores y marca",
      railStepReview: "Revision",
      skipMessage: "Saltar",
      letAiDecide: "Que IA decida",
      newClientWebsite: "Nuevo sitio de cliente",
      generalBusiness: "Negocio general",
      featuredOffer: "Oferta destacada",
      mainService: "Servicio principal",
      contactRequest: "Solicitud de contacto"
    },
    fr: {
      languageLabel: "Langue du site",
      quickForm: "Formulaire rapide",
      guidedSetup: "LYRA m'accompagne",
      assistantSubtitle: "Assistant de cr\xE9ation de site",
      onlineStatus: "En ligne",
      assistantIntro: "Bonjour, je suis LYRA. Je vais vous aider \xE0 cr\xE9er votre site ou boutique \xE9tape par \xE9tape. Vous pouvez \xE9crire ou utiliser votre voix.",
      intentDetected: "Tr\xE8s bien. J'ai d\xE9tect\xE9 cette direction :",
      selectedTemplate: "Template s\xE9lectionn\xE9",
      catalogType: "Type de catalogue",
      currentInfo: "Quelques d\xE9tails collect\xE9s jusqu'ici.",
      livePreviewKicker: "Workspace de creation",
      livePreviewTitle: "Modele business et architecture du site",
      livePreviewText: "Vos reponses definissent la famille de template, la logique catalogue, la structure des pages et le parcours de conversion.",
      finalReviewTitle: "V\xE9rifier les d\xE9tails",
      generateMyWebsite: "G\xE9n\xE9rer mon site",
      keepChatting: "Continuer la discussion",
      thinking: "LYRA r\xE9fl\xE9chit...",
      myPages: "Mes pages",
      openProject: "Ouvrir",
      emptyProjects: "Vous n'avez pas encore de pages enregistr\xE9es \u2014 g\xE9n\xE9rez votre premi\xE8re avec LYRA.",
      loadProjectsError: "Impossible de charger vos pages enregistr\xE9es. Connectez-vous \xE0 nouveau puis r\xE9essayez.",
      startNewProject: "Nouvelle page",
      startNewProjectConfirm: "Commencer une nouvelle page ? Cela efface le brouillon actuel de ce navigateur.",
      micButton: "Utiliser la voix",
      voiceOutputOn: "Voix activ\xE9e",
      voiceOutputOff: "Voix d\xE9sactiv\xE9e",
      voiceUnsupported: "La saisie vocale n'est pas prise en charge dans ce navigateur.",
      voiceListening: "\xC9coute en cours... parlez maintenant.",
      voiceReady: "Transcription pr\xEAte. V\xE9rifiez-la puis appuyez sur Envoyer.",
      summaryHelper: "Apportez vos modifications avant de g\xE9n\xE9rer votre premier brouillon.",
      editDetails: "Modifier les details",
      reviewDetails: "V\xE9rifier les d\xE9tails",
      selectedLanguage: "Langue s\xE9lectionn\xE9e",
      businessName: "Nom de l'entreprise",
      businessDescription: "Description de l'entreprise",
      industry: "Secteur",
      location: "Emplacement",
      servicesProducts: "Produits/services",
      targetAudience: "Public cible",
      preferredTone: "Ton/style pr\xE9f\xE9r\xE9",
      preferredColors: "Couleurs pr\xE9f\xE9r\xE9es",
      contactInfo: "Coordonn\xE9es",
      desiredDomain: "Domaine souhait\xE9",
      checkDomain: "V\xE9rifier le domaine",
      checkingDomain: "Recherche d'options de domaine...",
      domainCheckEmpty: "\xC9crivez d'abord un domaine ou un nom d'entreprise.",
      domainExact: "Disponibilit\xE9 r\xE9elle du registraire",
      domainSuggestions: "Suggestions pr\xE9liminaires",
      logoUrl: "URL du logo optionnelle",
      uploadLogo: "Importer un logo optionnel",
      photoUrls: "URLs de photos optionnelles",
      uploadPhotos: "Importer des photos optionnelles",
      assetPromptTitle: "Logo et photos",
      assetPromptText: "Importez-les ici et LYRA les utilisera pour le brouillon.",
      salesMode: "Parcours de vente",
      hasLogoPhotos: "Logo/photos",
      websiteIntent: "Type de site",
      reply: "R\xE9ponse",
      pageTitle: "G\xE9n\xE9rateur de site IA",
      liveSummary: "R\xE9sum\xE9 en direct",
      generateButton: "G\xE9n\xE9rer le site avec l'IA",
      reviewGenerate: "G\xE9n\xE9rer mon site",
      switchToManualForm: "Utiliser le formulaire manuel",
      backToChat: "Retour au chat",
      send: "Envoyer",
      skip: "Passer",
      decideStyle: "Style : laisser l'IA d\xE9cider",
      decideColors: "Couleurs : laisser l'IA d\xE9cider",
      decideAudience: "Audience : laisser l'IA d\xE9cider",
      decideSections: "Sections : laisser l'IA d\xE9cider",
      serverKeyNotice: "Vous pouvez revenir au chat \xE0 tout moment.",
      assistantServerNotice: "Touchez G\xE9n\xE9rer pour cr\xE9er le site modifiable complet, ou continuez \xE0 discuter pour affiner le plan.",
      replyPlaceholder: "\xC9crivez votre r\xE9ponse...",
      photoUrlsPlaceholder: "Une URL d'image par ligne",
      salesModePlaceholder: "vente en ligne, devis, ou les deux",
      accountGateKicker: "Espace prot\xE9g\xE9",
      accountGateTitle: "Commencez en sauvegardant votre progression",
      accountGateText: "Connectez-vous pour que LYRA sauvegarde chaque r\xE9ponse, retrouve vos brouillons et \xE9vite de r\xE9p\xE9ter les m\xEAmes informations.",
      continueGoogle: "Continuer avec Google",
      continueApple: "Continuer avec Apple",
      continueEmail: "Continuer avec email",
      saveEmailContinue: "Entrer dans mon espace",
      authOrEmail: "ou continuer avec email",
      comingSoon: "Bient\xF4t",
      sendMagicLink: "M'envoyer un lien de connexion",
      continueDemo: "Continuer la d\xE9mo",
      sendingAssistant: "LYRA analyse votre r\xE9ponse...",
      summaryUpdated: "D\xE9tails mis \xE0 jour.",
      devFallbackMissingKey: "LYRA utilise une r\xE9ponse simplifi\xE9e pendant que le service IA se connecte.",
      localFallback: "R\xE9ponse enregistr\xE9e. Nous continuons la configuration.",
      localFallbackMessage: "J'ai enregistr\xE9 votre r\xE9ponse et nous continuons.",
      savingRequest: "Enregistrement de l'intake comme client_request...",
      requestNotSaved: "Enregistr\xE9 dans cette session. Vous pouvez quand m\xEAme g\xE9n\xE9rer le brouillon.",
      generatedOpenAI: "G\xE9n\xE9r\xE9 avec OpenAI via le backend.",
      generating: "G\xE9n\xE9ration...",
      generatingLong: "LYRA con\xE7oit votre site. Cela peut prendre environ une minute.",
      generateError: "Impossible de g\xE9n\xE9rer",
      reviewStep: "V\xE9rification",
      step: "\xC9tape",
      of: "sur",
      railStepBusiness: "Votre entreprise",
      railStepProducts: "Produits/services",
      railStepLocation: "Emplacement",
      railStepSales: "Comment vous vendez",
      railStepStyle: "Style et audience",
      railStepColors: "Couleurs et marque",
      railStepReview: "V\xE9rification",
      skipMessage: "Passer",
      letAiDecide: "Laisser l'IA d\xE9cider",
      newClientWebsite: "Nouveau site client",
      generalBusiness: "Entreprise g\xE9n\xE9rale",
      featuredOffer: "Offre phare",
      mainService: "Service principal",
      contactRequest: "Demande de contact"
    },
    pt: {
      languageLabel: "Idioma do site",
      quickForm: "Formulario rapido",
      guidedSetup: "LYRA me ajuda",
      assistantSubtitle: "Assistente de cria\xE7\xE3o de sites",
      onlineStatus: "Online",
      assistantIntro: "Ol\xE1, sou a LYRA. Vou ajudar voc\xEA a criar seu site ou loja passo a passo. Voc\xEA pode digitar ou usar a voz.",
      intentDetected: "\xD3timo. Detectei esta dire\xE7\xE3o:",
      selectedTemplate: "Template selecionado",
      catalogType: "Tipo de cat\xE1logo",
      currentInfo: "Alguns detalhes coletados at\xE9 agora.",
      livePreviewKicker: "Workspace de criacao",
      livePreviewTitle: "Modelo de negocio e arquitetura do site",
      livePreviewText: "Suas respostas definem familia de template, logica de catalogo, estrutura de paginas e fluxo de conversao.",
      finalReviewTitle: "Revisar detalhes",
      generateMyWebsite: "Gerar meu site",
      keepChatting: "Continuar conversando",
      thinking: "LYRA est\xE1 pensando...",
      myPages: "Minhas p\xE1ginas",
      openProject: "Abrir",
      emptyProjects: "Voc\xEA ainda n\xE3o tem p\xE1ginas salvas \u2014 gere a primeira com a LYRA.",
      loadProjectsError: "N\xE3o consegui carregar suas p\xE1ginas salvas. Entre novamente e tente outra vez.",
      startNewProject: "Nova p\xE1gina",
      startNewProjectConfirm: "Iniciar uma nova p\xE1gina? Isso limpa o rascunho atual deste navegador.",
      micButton: "Usar voz",
      voiceOutputOn: "Voz ativada",
      voiceOutputOff: "Voz desligada",
      voiceUnsupported: "Entrada por voz n\xE3o \xE9 compat\xEDvel com este navegador.",
      voiceListening: "Ouvindo... fale agora.",
      voiceReady: "Transcri\xE7\xE3o pronta. Revise e pressione Enviar.",
      summaryHelper: "Fa\xE7a os ajustes antes de gerar seu primeiro rascunho.",
      editDetails: "Editar detalhes",
      reviewDetails: "Revisar resumo",
      selectedLanguage: "Idioma selecionado",
      businessName: "Nome do neg\xF3cio",
      businessDescription: "Descri\xE7\xE3o do neg\xF3cio",
      industry: "Setor",
      location: "Localiza\xE7\xE3o",
      servicesProducts: "Produtos/servi\xE7os",
      targetAudience: "P\xFAblico-alvo",
      preferredTone: "Tom/estilo preferido",
      preferredColors: "Cores preferidas",
      contactInfo: "Informa\xE7\xF5es de contato",
      desiredDomain: "Dom\xEDnio desejado",
      checkDomain: "Verificar dom\xEDnio",
      checkingDomain: "Buscando op\xE7\xF5es de dom\xEDnio...",
      domainCheckEmpty: "Digite primeiro um dom\xEDnio ou nome do neg\xF3cio.",
      domainExact: "Disponibilidade real do registrador",
      domainSuggestions: "Sugest\xF5es preliminares",
      logoUrl: "URL do logo opcional",
      uploadLogo: "Enviar logo opcional",
      photoUrls: "URLs de fotos opcionais",
      uploadPhotos: "Enviar fotos opcionais",
      assetPromptTitle: "Logo e fotos",
      assetPromptText: "Envie aqui e a LYRA usar\xE1 no primeiro rascunho.",
      salesMode: "Fluxo de venda",
      hasLogoPhotos: "Logo/fotos",
      websiteIntent: "Tipo de site",
      reply: "Resposta",
      pageTitle: "Criador de sites com IA",
      liveSummary: "Resumo ao vivo",
      generateButton: "Gerar site com IA",
      reviewGenerate: "Gerar meu site",
      switchToManualForm: "Usar formulario manual",
      backToChat: "Voltar ao chat",
      send: "Enviar",
      skip: "Pular",
      decideStyle: "Estilo: deixar a IA decidir",
      decideColors: "Cores: deixar a IA decidir",
      decideAudience: "P\xFAblico: deixar a IA decidir",
      decideSections: "Se\xE7\xF5es: deixar a IA decidir",
      serverKeyNotice: "Voc\xEA pode voltar ao chat quando quiser.",
      assistantServerNotice: "Toque em Gerar para criar o site edit\xE1vel completo, ou continue conversando para ajustar o plano.",
      replyPlaceholder: "Digite sua resposta...",
      photoUrlsPlaceholder: "Uma URL de imagem por linha",
      salesModePlaceholder: "vendas online, or\xE7amentos, ou ambos",
      accountGateKicker: "Workspace protegido",
      accountGateTitle: "Comece salvando seu progresso",
      accountGateText: "Entre para a LYRA salvar cada resposta, recuperar seus rascunhos e evitar repetir as mesmas informa\xE7\xF5es.",
      continueGoogle: "Continuar com Google",
      continueApple: "Continuar com Apple",
      continueEmail: "Continuar com email",
      saveEmailContinue: "Entrar no meu workspace",
      authOrEmail: "ou continuar com email",
      comingSoon: "Em breve",
      sendMagicLink: "Enviar um link de acesso por email",
      continueDemo: "Continuar vendo demo",
      sendingAssistant: "LYRA est\xE1 revisando sua resposta...",
      summaryUpdated: "Detalhes atualizados.",
      devFallbackMissingKey: "A LYRA est\xE1 usando uma resposta simplificada enquanto o servi\xE7o de IA termina de conectar.",
      localFallback: "Resposta salva. Continuamos a configura\xE7\xE3o.",
      localFallbackMessage: "Salvei sua resposta e vamos continuar.",
      savingRequest: "Salvando intake conclu\xEDdo como client_request...",
      requestNotSaved: "Salvo nesta sess\xE3o. Voc\xEA ainda pode gerar o rascunho do site.",
      generatedOpenAI: "Gerado com OpenAI pelo backend.",
      generating: "Gerando...",
      generatingLong: "A LYRA est\xE1 criando seu site. Isso pode levar cerca de um minuto.",
      generateError: "N\xE3o foi poss\xEDvel gerar",
      reviewStep: "Revis\xE3o",
      step: "Etapa",
      of: "de",
      railStepBusiness: "Seu neg\xF3cio",
      railStepProducts: "Produtos/servi\xE7os",
      railStepLocation: "Localiza\xE7\xE3o",
      railStepSales: "Como voc\xEA vende",
      railStepStyle: "Estilo e p\xFAblico",
      railStepColors: "Cores e marca",
      railStepReview: "Revis\xE3o",
      skipMessage: "Pular",
      letAiDecide: "Deixar a IA decidir",
      newClientWebsite: "Novo site de cliente",
      generalBusiness: "Neg\xF3cio geral",
      featuredOffer: "Oferta em destaque",
      mainService: "Servi\xE7o principal",
      contactRequest: "Solicita\xE7\xE3o de contato"
    }
  };

  // src/ai-builder/templates.js
  var TEMPLATE_PREVIEW_CHOICES = [
    {
      templateId: "premium-product-store",
      name: "Premium Product",
      names: { en: "Premium Product", es: "Producto premium", fr: "Produit premium", pt: "Produto premium" },
      catalogType: "premium_editorial_catalog",
      image: "/templates-preview/carousel-assets/apple_2.png",
      description: "Premium product showcase with spacious hero, refined copy, and polished sections.",
      descriptions: {
        en: "Premium product showcase with spacious hero, refined copy, and polished sections.",
        es: "Presentacion premium de producto con hero amplio, copy refinado y secciones pulidas.",
        fr: "Presentation produit premium avec hero spacieux, texte raffine et sections soignees.",
        pt: "Apresentacao premium de produto com hero amplo, copy refinado e secoes polidas."
      }
    },
    {
      templateId: "luxury-high-ticket-pro",
      name: "Luxury High Ticket",
      names: { en: "Luxury High Ticket", es: "Lujo high ticket", fr: "Luxe high ticket", pt: "Luxo high ticket" },
      catalogType: "luxury_high_ticket_catalog",
      image: "/templates-preview/carousel-assets/professional_2.png",
      description: "Private showroom style for jewelry, watches, art, luxury cars, collectibles, and high-ticket pieces.",
      descriptions: {
        en: "Private showroom style for jewelry, watches, art, luxury cars, collectibles, and high-ticket pieces.",
        es: "Showroom privado para joyeria, relojes, arte, carros de lujo, coleccionables y piezas de alto valor.",
        fr: "Showroom prive pour bijoux, montres, art, voitures de luxe, objets de collection et pieces haut de gamme.",
        pt: "Showroom privado para joias, relogios, arte, carros de luxo, colecionaveis e pecas de alto valor."
      }
    },
    {
      templateId: "education-course-academy-pro",
      name: "Course Academy",
      names: { en: "Course Academy", es: "Academia de cursos", fr: "Academie de cours", pt: "Academia de cursos" },
      catalogType: "education_course_catalog",
      image: "/templates-preview/screenshots/education.png",
      description: "Premium academy style for courses, coaching programs, bootcamps, classes, and training centers.",
      descriptions: {
        en: "Premium academy style for courses, coaching programs, bootcamps, classes, and training centers.",
        es: "Estilo academia premium para cursos, programas de coaching, bootcamps, clases y centros de formacion.",
        fr: "Style academie premium pour cours, coaching, bootcamps, classes et centres de formation.",
        pt: "Estilo academia premium para cursos, coaching, bootcamps, aulas e centros de treinamento."
      }
    },
    {
      templateId: "medical-wellness-clinic-pro",
      name: "Clinic / Wellness",
      names: { en: "Clinic / Wellness", es: "Clinica / Wellness", fr: "Clinique / Wellness", pt: "Clinica / Wellness" },
      catalogType: "medical_wellness_service_catalog",
      image: "/templates-preview/carousel-assets/clinic_2.png",
      description: "Premium clinic style for medical, aesthetic, dental, wellness, therapy, and consultation-based businesses.",
      descriptions: {
        en: "Premium clinic style for medical, aesthetic, dental, wellness, therapy, and consultation-based businesses.",
        es: "Estilo clinica premium para negocios medicos, esteticos, dentales, wellness, terapia y consultas.",
        fr: "Style clinique premium pour activites medicales, esthetiques, dentaires, wellness, therapie et consultation.",
        pt: "Estilo clinica premium para negocios medicos, esteticos, dentarios, wellness, terapia e consultas."
      }
    },
    {
      templateId: "legal-professional-services-pro",
      name: "Legal / Professional",
      names: { en: "Legal / Professional", es: "Legal / Profesional", fr: "Juridique / Professionnel", pt: "Juridico / Profissional" },
      catalogType: "legal_professional_services_catalog",
      image: "/templates-preview/carousel-assets/professional_2.png",
      description: "Premium professional firm style for legal, accounting, tax, consulting, insurance, and B2B advisory.",
      descriptions: {
        en: "Premium professional firm style for legal, accounting, tax, consulting, insurance, and B2B advisory.",
        es: "Estilo firma profesional premium para abogados, contadores, impuestos, consultoria, seguros y asesoria B2B.",
        fr: "Style cabinet professionnel premium pour juridique, comptabilite, fiscalite, conseil, assurance et B2B.",
        pt: "Estilo firma profissional premium para juridico, contabilidade, impostos, consultoria, seguros e B2B."
      }
    },
    {
      templateId: "b2b-saas-enterprise-pro",
      name: "B2B SaaS / Enterprise",
      names: { en: "B2B SaaS / Enterprise", es: "B2B SaaS / Empresarial", fr: "B2B SaaS / Entreprise", pt: "B2B SaaS / Empresarial" },
      catalogType: "b2b_solution_catalog",
      image: "/templates-preview/carousel-assets/b2b_2.png",
      description: "Enterprise software style for SaaS, automation, IT services, integrations, dashboards, and B2B systems.",
      descriptions: {
        en: "Enterprise software style for SaaS, automation, IT services, integrations, dashboards, and B2B systems.",
        es: "Estilo software empresarial para SaaS, automatizacion, IT, integraciones, dashboards y sistemas B2B.",
        fr: "Style logiciel entreprise pour SaaS, automatisation, IT, integrations, dashboards et systemes B2B.",
        pt: "Estilo software empresarial para SaaS, automacao, IT, integracoes, dashboards e sistemas B2B."
      }
    },
    {
      templateId: "manufacturing-industrial-supplier-pro",
      name: "Industrial Supplier",
      names: { en: "Industrial Supplier", es: "Proveedor industrial", fr: "Fournisseur industriel", pt: "Fornecedor industrial" },
      catalogType: "industrial_supplier_catalog",
      image: "/templates-preview/carousel-assets/industrial_2.png",
      description: "Technical B2B supplier style for manufacturing, machinery, parts, tools, safety equipment, and bulk RFQs.",
      descriptions: {
        en: "Technical B2B supplier style for manufacturing, machinery, parts, tools, safety equipment, and bulk RFQs.",
        es: "Estilo proveedor B2B tecnico para manufactura, maquinaria, repuestos, herramientas, seguridad y cotizaciones por volumen.",
        fr: "Style fournisseur B2B technique pour fabrication, machines, pieces, outils, securite et demandes de devis.",
        pt: "Estilo fornecedor B2B tecnico para manufatura, maquinas, pecas, ferramentas, seguranca e cotacoes em volume."
      }
    },
    {
      templateId: "mega-retail-store",
      name: "Mega Retail Store",
      names: { en: "Mega Retail Store", es: "Mega tienda retail", fr: "Mega boutique retail", pt: "Mega loja retail" },
      catalogType: "single_vendor_dense_catalog",
      image: "/templates-preview/carousel-assets/amazon_2.jpg",
      description: "Large owned catalog store with search, categories, deals, filters, cart, checkout, and one-store inventory.",
      descriptions: {
        en: "Large owned catalog store with search, categories, deals, filters, cart, checkout, and one-store inventory.",
        es: "Tienda de catalogo grande propia, con busqueda, categorias, ofertas, filtros, carrito, checkout e inventario de una sola empresa.",
        fr: "Grande boutique catalogue avec recherche, categories, offres, filtres, panier, checkout et inventaire d'une seule entreprise.",
        pt: "Loja de catalogo grande propria, com busca, categorias, ofertas, filtros, carrinho, checkout e inventario de uma empresa."
      }
    },
    {
      templateId: "mega-marketplace",
      name: "Mega Marketplace",
      names: { en: "Mega Marketplace", es: "Mega marketplace", fr: "Mega marketplace", pt: "Mega marketplace" },
      catalogType: "dense_marketplace_catalog",
      image: "/templates-preview/carousel-assets/amazon_2.jpg",
      description: "Large catalog marketplace with search, categories, deals, filters, and dense products.",
      descriptions: {
        en: "Large catalog marketplace with search, categories, deals, filters, and dense products.",
        es: "Marketplace de catalogo grande con busqueda, categorias, ofertas, filtros y muchos productos.",
        fr: "Marketplace a grand catalogue avec recherche, categories, offres, filtres et nombreux produits.",
        pt: "Marketplace de catalogo grande com busca, categorias, ofertas, filtros e muitos produtos."
      }
    },
    {
      templateId: "listing-marketplace-pro",
      name: "Listing Marketplace",
      names: { en: "Listing Marketplace", es: "Marketplace de listados", fr: "Marketplace d'annonces", pt: "Marketplace de anuncios" },
      catalogType: "listing_marketplace_catalog",
      image: "/templates-preview/carousel-assets/ebay_2.png",
      description: "Seller listing marketplace with condition labels, offers, and comparison layout.",
      descriptions: {
        en: "Seller listing marketplace with condition labels, offers, and comparison layout.",
        es: "Marketplace de listados con vendedores, condicion, ofertas y comparacion.",
        fr: "Marketplace d'annonces avec vendeurs, etat, offres et comparaison.",
        pt: "Marketplace de anuncios com vendedores, condicao, ofertas e comparacao."
      }
    },
    {
      templateId: "fashion-drop-pro",
      name: "Fashion Drop",
      names: { en: "Fashion Drop", es: "Fashion drop", fr: "Drop mode", pt: "Fashion drop" },
      catalogType: "lookbook_collection_catalog",
      image: "/templates-preview/carousel-assets/apple_2.png",
      description: "Editorial boutique storefront with visual hero, collections, lookbook, and launch energy.",
      descriptions: {
        en: "Editorial boutique storefront with visual hero, collections, lookbook, and launch energy.",
        es: "Tienda boutique editorial con hero visual, colecciones, lookbook y energia de lanzamiento.",
        fr: "Boutique editoriale avec hero visuel, collections, lookbook et energie de lancement.",
        pt: "Loja boutique editorial com hero visual, colecoes, lookbook e energia de lancamento."
      }
    },
    {
      templateId: "corporate-company-pro",
      name: "Corporate Company",
      names: { en: "Corporate Company", es: "Pagina de empresa", fr: "Site entreprise", pt: "Site empresarial" },
      catalogType: "company_services_catalog",
      image: "/templates-preview/carousel-assets/services_2.png",
      description: "Professional company website with positioning, services, process, proof, and contact.",
      descriptions: {
        en: "Professional company website with positioning, services, process, proof, and contact.",
        es: "Pagina profesional de empresa con posicionamiento, servicios, proceso, prueba y contacto.",
        fr: "Site professionnel d'entreprise avec positionnement, services, processus, preuves et contact.",
        pt: "Site profissional de empresa com posicionamento, servicos, processo, prova e contato."
      }
    },
    {
      templateId: "lead-funnel-pro",
      name: "Lead Funnel",
      names: { en: "Lead Funnel", es: "Landing de conversion", fr: "Landing de conversion", pt: "Landing de conversao" },
      catalogType: "lead_funnel_offer_catalog",
      image: "/templates-preview/carousel-assets/services_2.png",
      description: "Focused landing page for one offer, benefits, proof, FAQ, and lead capture.",
      descriptions: {
        en: "Focused landing page for one offer, benefits, proof, FAQ, and lead capture.",
        es: "Landing enfocada en una oferta, beneficios, prueba, preguntas frecuentes y captacion de leads.",
        fr: "Landing centree sur une offre, benefices, preuves, FAQ et capture de leads.",
        pt: "Landing focada em uma oferta, beneficios, prova, FAQ e captacao de leads."
      }
    },
    {
      templateId: "restaurant-food-business",
      name: "Restaurant Menu",
      names: { en: "Restaurant Menu", es: "Menu restaurante", fr: "Menu restaurant", pt: "Menu restaurante" },
      catalogType: "restaurant_menu_catalog",
      image: "/templates-preview/carousel-assets/services_2.png",
      description: "Restaurant-first site with menu categories, signature dishes, specials, hours, and order CTA.",
      descriptions: {
        en: "Restaurant-first site with menu categories, signature dishes, specials, hours, and order CTA.",
        es: "Pagina para restaurante con categorias de menu, platos destacados, especiales, horarios y boton de pedido.",
        fr: "Site restaurant avec categories de menu, plats signature, offres, horaires et CTA de commande.",
        pt: "Site para restaurante com categorias de menu, pratos destaque, especiais, horarios e CTA de pedido."
      }
    },
    {
      templateId: "digital-products-store",
      name: "Digital Products",
      names: { en: "Digital Products", es: "Productos digitales", fr: "Produits digitaux", pt: "Produtos digitais" },
      catalogType: "digital_offer_catalog",
      image: "/templates-preview/carousel-assets/apple_2.png",
      description: "Creator/SaaS-style digital product site with bundles, modules, proof, and instant access CTA.",
      descriptions: {
        en: "Creator/SaaS-style digital product site with bundles, modules, proof, and instant access CTA.",
        es: "Sitio para productos digitales con bundles, modulos, prueba social y CTA de acceso inmediato.",
        fr: "Site pour produits digitaux avec bundles, modules, preuves et CTA d'acces immediat.",
        pt: "Site para produtos digitais com bundles, modulos, prova e CTA de acesso imediato."
      }
    },
    {
      templateId: "real-estate-listings-pro",
      name: "Real Estate / Listings",
      names: { en: "Real Estate / Listings", es: "Propiedades / listings", fr: "Immobilier / annonces", pt: "Imoveis / anuncios" },
      catalogType: "real_estate_listing_catalog",
      image: "/templates-preview/carousel-assets/ebay_2.png",
      description: "Search-first listing site with filters, location, price/spec cards, area panel, and inquiry CTAs.",
      descriptions: {
        en: "Search-first listing site with filters, location, price/spec cards, area panel, and inquiry CTAs.",
        es: "Sitio de listings con busqueda, filtros, ubicacion, cards con precio/especificaciones y CTA de consulta.",
        fr: "Site d'annonces avec recherche, filtres, localisation, cartes prix/specs et CTA de demande.",
        pt: "Site de anuncios com busca, filtros, localizacao, cards com preco/especificacoes e CTA de consulta."
      }
    },
    {
      templateId: "home-services-premium",
      name: "Local Services Premium",
      names: { en: "Local Services Premium", es: "Servicios locales premium", fr: "Services locaux premium", pt: "Servicos locais premium" },
      catalogType: "home_services_quote_catalog",
      image: "/templates-preview/carousel-assets/services_2.png",
      description: "Premium local service site with phone-first hero, service areas, proof, reviews, and quote flow.",
      descriptions: {
        en: "Premium local service site with phone-first hero, service areas, proof, reviews, and quote flow.",
        es: "Pagina premium para servicios locales con telefono visible, areas, prueba, resenas y cotizacion.",
        fr: "Site premium de services locaux avec telephone visible, zones, preuves, avis et devis.",
        pt: "Site premium para servicos locais com telefone visivel, areas, provas, avaliacoes e orcamento."
      }
    },
    {
      templateId: "local-services-pro-plus",
      name: "Local Services",
      names: { en: "Local Services", es: "Servicios locales", fr: "Services locaux", pt: "Servicos locais" },
      catalogType: "service_area_catalog",
      image: "/templates-preview/carousel-assets/services_2.png",
      description: "Quote-focused service site with service cards, trust, service area, and contact CTA.",
      descriptions: {
        en: "Quote-focused service site with service cards, trust, service area, and contact CTA.",
        es: "Pagina de servicios con cotizaciones, confianza, areas de servicio y CTA de contacto.",
        fr: "Site de services oriente devis, confiance, zone de service et CTA de contact.",
        pt: "Site de servicos focado em orcamentos, confianca, area de atendimento e CTA."
      }
    },
    {
      templateId: "booking-appointment-pro",
      name: "Booking",
      names: { en: "Booking", es: "Reservas", fr: "Reservations", pt: "Agendamentos" },
      catalogType: "booking_menu_catalog",
      image: "/templates-preview/carousel-assets/booking_2.jpg",
      description: "Appointment-first layout with service menu, availability, staff/process, and booking CTA.",
      descriptions: {
        en: "Appointment-first layout with service menu, availability, staff/process, and booking CTA.",
        es: "Layout enfocado en citas con menu de servicios, disponibilidad, equipo/proceso y CTA.",
        fr: "Layout centre sur les rendez-vous avec services, disponibilite, equipe/processus et CTA.",
        pt: "Layout focado em agendamentos com servicos, disponibilidade, equipe/processo e CTA."
      }
    }
  ];
  var TEMPLATE_ID_ALIASES = Object.freeze({
    "apple-premium-product": "premium-product-store",
    "premium-product-showcase": "premium-product-store",
    "luxury-product-store": "premium-product-store"
  });
  function normalizeTemplateId(templateId = "") {
    const id = String(templateId || "").trim();
    return TEMPLATE_ID_ALIASES[id] || id;
  }
  var TEMPLATE_REFERENCE_EXECUTION_STATUS = Object.freeze({
    status: "reference_only",
    sourceType: "reference_asset",
    runtimeEnabled: false,
    runtimePath: ""
  });
  var RUNTIME_SCHEMA_EXECUTION_STATUS = Object.freeze({
    status: "runtime_schema",
    sourceType: "runtime_template",
    runtimeEnabled: true,
    runtimePath: "",
    manifestPath: ""
  });
  var TEMPLATE_EXECUTION_STATUS = Object.freeze({
    "premium-product-store": Object.freeze({
      status: "runtime_backend_prototype",
      sourceType: "runtime_template",
      runtimeEnabled: true,
      runtimePath: "/templates/retail/premium-product-store/phase-5/visual-prototype/preview.html",
      manifestPath: "/templates/retail/premium-product-store/template.manifest.json",
      backendContractPath: "/templates/retail/premium-product-store/contracts/commerce-integration.json"
    }),
    "mega-retail-store": Object.freeze({
      status: "runtime_prototype",
      sourceType: "runtime_template",
      runtimeEnabled: true,
      runtimePath: "/templates/retail/mega-retail-store/phase-5/visual-prototype/preview.html",
      manifestPath: "/templates/retail/mega-retail-store/template.manifest.json"
    }),
    "mega-marketplace": Object.freeze({
      status: "runtime_prototype",
      sourceType: "runtime_template",
      runtimeEnabled: true,
      runtimePath: "/templates/marketplace/mega-marketplace/phase-5/visual-prototype/preview.html",
      manifestPath: "/templates/marketplace/mega-marketplace/template.manifest.json"
    }),
    "luxury-high-ticket-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "education-course-academy-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "medical-wellness-clinic-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "legal-professional-services-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "b2b-saas-enterprise-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "manufacturing-industrial-supplier-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "fashion-drop-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "corporate-company-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "lead-funnel-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "restaurant-food-business": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "digital-products-store": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "real-estate-listings-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "home-services-premium": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "local-services-pro-plus": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS }),
    "booking-appointment-pro": Object.freeze({ ...RUNTIME_SCHEMA_EXECUTION_STATUS })
  });
  function templateExecutionStatus(templateId) {
    const id = normalizeTemplateId(templateId);
    return {
      ...TEMPLATE_REFERENCE_EXECUTION_STATUS,
      templateId: id,
      ...TEMPLATE_EXECUTION_STATUS[id] || {}
    };
  }
  function isRuntimeTemplateId(templateId) {
    return Boolean(templateExecutionStatus(templateId).runtimeEnabled);
  }
  function withTemplateExecutionStatus(choice) {
    if (!choice) return null;
    const executionStatus = templateExecutionStatus(choice.templateId);
    return {
      ...choice,
      executionStatus,
      runtimeEnabled: executionStatus.runtimeEnabled,
      sourceType: executionStatus.sourceType
    };
  }
  function runtimeTemplateSelection(selection) {
    if (!selection || !isRuntimeTemplateId(selection.templateId)) return null;
    return {
      ...selection,
      executionStatus: templateExecutionStatus(selection.templateId)
    };
  }
  function stripReferenceTemplateRuntimePayload(payload = {}) {
    const next = { ...payload };
    delete next.templateId;
    delete next.templateIntent;
    delete next.catalogType;
    delete next.selectedTemplate;
    delete next.templateInstructions;
    delete next.templateExecution;
    delete next.sitePlan;
    delete next.sitePlanApproved;
    next.designStrategy = {
      ...next.designStrategy || {},
      diagnosis: null,
      selectedTemplateId: "",
      selectedCatalogType: "",
      templateExecutionPolicy: {
        status: "reference_selection_removed",
        reason: "Reference-only visual assets are isolated from local runtime fallback generation."
      }
    };
    return next;
  }
  var TEMPLATE_PRESETS = [
    {
      id: "editorial-boutique",
      name: "Editorial boutique",
      heroLayout: "image_left",
      productLayout: "masonry",
      density: "spacious"
    },
    {
      id: "operational-catalog",
      name: "Catalogo operativo",
      heroLayout: "compact_catalog",
      productLayout: "grid",
      density: "balanced"
    },
    {
      id: "bold-landing",
      name: "Landing bold",
      heroLayout: "centered_hero",
      productLayout: "boutique_grid",
      density: "compact"
    }
  ];
  var DEFAULT_BRAND = {
    logoUrl: "",
    primaryColor: "#0e7c66",
    secondaryColor: "#e3f3ee",
    accentColor: "#14b8a6",
    backgroundColor: "#f8fafc",
    surfaceColor: "#ffffff",
    textColor: "#101828",
    mutedTextColor: "#667085",
    borderColor: "#d9e2df",
    buttonColor: "#0e7c66",
    buttonTextColor: "#ffffff",
    styleDirection: "clean professional",
    fontPairing: { heading: "Inter", body: "Inter" },
    borderRadius: "10px",
    shadowStyle: "0 18px 48px rgba(16, 24, 40, 0.10)"
  };
  var DESIGN_QUALITY_RULES = [
    "Use the uploaded logo as the source of brand identity, not just as an image.",
    "Create only purposeful sections with a clear reason, conversion purpose, priority, content, and visual treatment.",
    "Strong hero, clear headline, clear CTA, consistent spacing, consistent typography, no filler, no repeated sections, no clutter, and accessible contrast.",
    "Avoid arbitrary gradients, childish styling, oversized random cards, and colors that do not come from the brand system.",
    "Choose one design direction and keep every section consistent with it.",
    "Never render sections that have no conversion purpose or meaningful content.",
    "Review and improve headline, CTA, section order, contrast, visual rhythm, mobile layout, and brand consistency before preview."
  ];

  // src/ai-builder/utils.js
  function escapeHtml(value) {
    return String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
  }
  function escapeAttribute(value) {
    return escapeHtml(value).replaceAll("`", "&#096;");
  }

  // src/ai-builder/variants.js
  function hashText(value) {
    let hash = 2166136261;
    for (const character of String(value || "")) {
      hash ^= character.charCodeAt(0);
      hash = Math.imul(hash, 16777619);
    }
    return hash >>> 0;
  }
  function pickVariantSeed(businessName = "", niche = "", offset = 0, count = 3) {
    const total = Math.max(1, Number(count) || 1);
    const normalizedOffset = Math.max(0, Number(offset) || 0);
    return (hashText(`${String(businessName).trim().toLowerCase()}|${String(niche).trim().toLowerCase()}`) + normalizedOffset) % total;
  }
  function isExplicitRedesignRequest(value = "") {
    return /\b(otro\s+diseno|otro\s+diseño|redisen[oa]|rediseñ[oa]|cambia\s+el\s+diseno|cambia\s+el\s+diseño|different\s+design|another\s+design|redesign)\b/i.test(String(value || ""));
  }

  // src/ai-builder/color-provenance.js
  var EXPLICIT_META_SOURCES = /* @__PURE__ */ new Set(["explicit", "explicit_user_choice"]);
  function normalizedColorValues(values) {
    const source = Array.isArray(values) ? values : values ? [values] : [];
    return source.map((value) => String(value || "").trim()).filter(Boolean);
  }
  function buildColorProvenance({
    preferredColors = [],
    logoPalette = [],
    localBrand = {},
    preferredColorMeta = {},
    structuredFormInput = false
  } = {}) {
    const explicitPreferred = structuredFormInput || EXPLICIT_META_SOURCES.has(preferredColorMeta?.source) ? normalizedColorValues(preferredColors) : [];
    const logoColors = normalizedColorValues(logoPalette);
    const localColors = normalizedColorValues([
      localBrand?.primaryColor,
      localBrand?.secondaryColor,
      localBrand?.accentColor
    ]);
    const entries = [];
    const seen = /* @__PURE__ */ new Set();
    const append = (colors, source) => {
      colors.forEach((color) => {
        const key = color.toLowerCase();
        if (seen.has(key)) return;
        seen.add(key);
        entries.push({ color, source });
      });
    };
    append(explicitPreferred, "explicit_client");
    append(logoColors, "logo_extracted");
    append(localColors, "local_suggestion");
    const anchor = entries[0] || null;
    return {
      anchorColor: anchor?.color || null,
      anchorSource: anchor?.source || "unknown",
      colors: entries
    };
  }

  // src/ai-builder/theme-policy.js
  var BACKEND_THEME_SOURCE = "backend_generated";
  function shouldPreserveBackendTheme(schema = {}) {
    return schema?.generation_metadata?.theme_source === BACKEND_THEME_SOURCE;
  }
  function applyAuthoritativeThemeToBrand(schema = {}, brand = {}) {
    if (!shouldPreserveBackendTheme(schema)) return brand;
    const colors = schema?.theme?.colors || {};
    const fonts = schema?.theme?.fonts || {};
    return {
      ...brand,
      primaryColor: colors.primary || brand.primaryColor,
      secondaryColor: colors.secondary || brand.secondaryColor,
      accentColor: colors.accent || brand.accentColor,
      backgroundColor: colors.background || brand.backgroundColor,
      surfaceColor: colors.surface || brand.surfaceColor,
      textColor: colors.text || brand.textColor,
      successColor: colors.success || brand.successColor,
      warningColor: colors.warning || brand.warningColor,
      errorColor: colors.error || brand.errorColor,
      infoColor: colors.info || brand.infoColor,
      mutedTextColor: colors.muted || brand.mutedTextColor,
      borderColor: colors.border || brand.borderColor,
      buttonColor: colors.button || colors.primary || brand.buttonColor,
      buttonTextColor: colors.buttonText || brand.buttonTextColor,
      fontPairing: {
        ...brand.fontPairing || {},
        ...fonts
      },
      paletteSource: BACKEND_THEME_SOURCE
    };
  }

  // src/ai-builder/auth-session-policy.js
  function decodeJwtPayload(token = "") {
    const segments = String(token || "").split(".");
    if (segments.length !== 3 || !segments[1]) return null;
    try {
      const base64 = segments[1].replace(/-/g, "+").replace(/_/g, "/");
      const padded = base64.padEnd(Math.ceil(base64.length / 4) * 4, "=");
      return JSON.parse(atob(padded));
    } catch {
      return null;
    }
  }
  function isUnexpiredAccessToken(token, nowSeconds = Date.now() / 1e3) {
    const payload = decodeJwtPayload(token);
    const expiresAt = Number(payload?.exp);
    return Number.isFinite(expiresAt) && expiresAt > nowSeconds;
  }
  function hasValidPersistedCredential({ accessTokens = [], previewTokens = [], clientEmails = [] } = {}, nowSeconds = Date.now() / 1e3) {
    if (clientEmails.some((email) => Boolean(String(email || "").trim()))) return true;
    if (previewTokens.some((token) => Boolean(String(token || "").trim()))) return true;
    return accessTokens.some((token) => isUnexpiredAccessToken(token, nowSeconds));
  }
  function generationAuthAction({ hasCredential, workspaceUnlocked }) {
    if (!hasCredential) return "prompt_auth";
    return workspaceUnlocked ? "continue" : "unlock_and_continue";
  }

  // src/ai-builder/renderers.js
  function arrayValue(value) {
    if (Array.isArray(value)) return value.filter(Boolean);
    if (!value) return [];
    return String(value).split(/[\n,]+/).map((item) => item.trim()).filter(Boolean);
  }
  function slugify(value) {
    return String(value || "default").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
  }
  function normalizeBrandForTheme(brandInput = {}) {
    const input = brandInput || {};
    const brand = { ...DEFAULT_BRAND, ...input };
    const colors = input.colors || {};
    brand.logoUrl = brand.logoUrl || input.logo_url || input.logo || "";
    brand.primaryColor = resolveColor(brand.primaryColor || colors.primary, DEFAULT_BRAND.primaryColor);
    brand.secondaryColor = resolveColor(brand.secondaryColor || colors.secondary, DEFAULT_BRAND.secondaryColor);
    brand.accentColor = resolveColor(brand.accentColor || colors.accent, brand.primaryColor);
    brand.backgroundColor = resolveColor(brand.backgroundColor || colors.background, DEFAULT_BRAND.backgroundColor);
    brand.surfaceColor = resolveColor(brand.surfaceColor || colors.surface, DEFAULT_BRAND.surfaceColor);
    brand.textColor = resolveColor(brand.textColor || colors.text, DEFAULT_BRAND.textColor);
    brand.successColor = resolveColor(brand.successColor || colors.success, "#15803D");
    brand.warningColor = resolveColor(brand.warningColor || colors.warning, "#B45309");
    brand.errorColor = resolveColor(brand.errorColor || colors.error, "#B91C1C");
    brand.infoColor = resolveColor(brand.infoColor || colors.info, "#1D4ED8");
    brand.mutedTextColor = resolveColor(brand.mutedTextColor || colors.muted, DEFAULT_BRAND.mutedTextColor);
    brand.borderColor = resolveColor(brand.borderColor || colors.border, mixColorsForTheme(brand.textColor, brand.backgroundColor, 0.14));
    brand.buttonColor = resolveColor(brand.buttonColor || colors.button || brand.primaryColor, brand.primaryColor);
    brand.buttonTextColor = resolveColor(brand.buttonTextColor || colors.buttonText || contrastColorForTheme(brand.buttonColor), contrastColorForTheme(brand.buttonColor));
    brand.fontPairing = typeof brand.fontPairing === "string" ? fontPairingFromStringForTheme(brand.fontPairing) : brand.fontPairing || DEFAULT_BRAND.fontPairing;
    brand.borderRadius = String(brand.borderRadius || DEFAULT_BRAND.borderRadius);
    brand.shadowStyle = brand.shadowStyle || DEFAULT_BRAND.shadowStyle;
    return brand;
  }
  function brandToThemeColorsForTheme(brand) {
    return {
      background: brand.backgroundColor,
      surface: brand.surfaceColor,
      primary: brand.primaryColor,
      secondary: brand.secondaryColor,
      accent: brand.accentColor,
      text: brand.textColor,
      success: brand.successColor,
      warning: brand.warningColor,
      error: brand.errorColor,
      info: brand.infoColor,
      muted: brand.mutedTextColor,
      border: brand.borderColor,
      button: brand.buttonColor,
      buttonText: brand.buttonTextColor
    };
  }
  function fontPairingFromStringForTheme(value) {
    const [heading, body] = String(value || "").split(/[+/|,]/).map((part) => part.trim()).filter(Boolean);
    return { heading: heading || "Inter", body: body || heading || "Inter" };
  }
  function hexToRgbForTheme(hex) {
    const clean = String(hex || "").replace("#", "");
    const full = clean.length === 3 ? clean.split("").map((char) => char + char).join("") : clean.padEnd(6, "0").slice(0, 6);
    const int = Number.parseInt(full, 16);
    if (Number.isNaN(int)) return [16, 24, 40];
    return [int >> 16 & 255, int >> 8 & 255, int & 255];
  }
  function rgbToHexForTheme(r, g, b) {
    return "#" + [r, g, b].map((value) => Math.max(0, Math.min(255, Math.round(value))).toString(16).padStart(2, "0")).join("");
  }
  function relativeLuminanceForTheme([r, g, b]) {
    const transform = (channel) => {
      const value = channel / 255;
      return value <= 0.03928 ? value / 12.92 : ((value + 0.055) / 1.055) ** 2.4;
    };
    return 0.2126 * transform(r) + 0.7152 * transform(g) + 0.0722 * transform(b);
  }
  function contrastColorForTheme(color) {
    const luminance = relativeLuminanceForTheme(hexToRgbForTheme(resolveColor(color, "#000000")));
    return luminance > 0.55 ? "#101828" : "#FFFFFF";
  }
  function mixColorsForTheme(a, b, amount = 0.5) {
    const first = hexToRgbForTheme(resolveColor(a, "#000000"));
    const second = hexToRgbForTheme(resolveColor(b, "#ffffff"));
    return rgbToHexForTheme(
      first[0] + (second[0] - first[0]) * amount,
      first[1] + (second[1] - first[1]) * amount,
      first[2] + (second[2] - first[2]) * amount
    );
  }
  function renderWebsite(schema, pageKey, context = {}) {
    const page = schema.pages.find((item) => item.page_key === pageKey) || schema.pages[0];
    const theme = schema.theme || {};
    ensureGoogleFontsLoaded(theme);
    schema.business = schema.business || {};
    if (!schema.business.selectedLanguage && !schema.business.selected_language && context.selectedLanguage) {
      schema.business.selectedLanguage = context.selectedLanguage;
    }
    schema.global_components = schema.global_components || {};
    const logo = schema.brand?.logoUrl || schema.global_components.logo_url;
    const layoutId = schema.layout_mode?.id || "standard";
    const templateId = schema.active_template?.id || schema.selected_template?.id || "standard";
    const commerceActions = isCommerceSite(schema) ? renderCommerceNavActions(schema) : "";
    return `<div class="rendered-site layout-${escapeAttribute(slugify(layoutId))} template-${escapeAttribute(slugify(templateId))}" style="${themeVars(theme, schema.brand)}">
    ${renderStudioFloatingCatalog(schema, context)}
    <div class="rendered-page-switcher">
      <span>${escapeHtml(schema.business.name || "Website")}</span>
      <div>${schema.pages.sort((a, b) => a.order - b.order).map((item) => `<a class="${item.page_key === page.page_key ? "active" : ""}" href="#" data-page-link="${escapeAttribute(item.page_key)}">${escapeHtml(item.title || item.page_key)}</a>`).join("")}</div>
    </div>
    <header class="rendered-nav ${schema.layout_mode?.navigation?.sticky_header ? "sticky" : ""}">
      <div class="rendered-nav-brand">${logo ? `<img src="${escapeAttribute(logo)}" alt="${escapeAttribute(schema.business.name)}">` : renderLogoMark(schema)}</div>
      <nav>${schema.navigation.map((item) => `<a class="${item.page_key === page.page_key ? "active" : ""}" href="#" data-page-link="${escapeAttribute(item.page_key)}">${escapeHtml(item.label)}</a>`).join("")}</nav>
      ${commerceActions}
    </header>
    ${page.sections.sort((a, b) => a.order - b.order).map((section) => renderSection(section, schema)).join("")}
    <footer class="rendered-footer">
      <div>${logo ? `<img src="${escapeAttribute(logo)}" alt="${escapeAttribute(schema.business.name)}">` : renderLogoMark(schema)}</div>
      <span>${escapeHtml(schema.global_components.footer_text || "")}</span>
    </footer>
  </div>`;
  }
  function isCommerceSite(schema = {}) {
    const templateId = schema.active_template?.id || schema.selected_template?.id || "";
    const catalogType = schema.catalog_model?.catalogType || schema.catalogModel?.catalogType || schema.layout_mode?.catalog_type || "";
    const salesMode = schema.business?.salesMode || schema.business?.sales_mode || schema.sales_mode || "";
    const intent = schema.business?.websiteIntent || schema.business?.website_intent || schema.business?.intent || "";
    const text = [templateId, catalogType, salesMode, intent, schema.business?.industry, schema.business?.description].join(" ").toLowerCase();
    return /marketplace|ecommerce|commerce|store|shop|retail|online_sales|sell|venta|tienda|productos/.test(text);
  }
  function commerceLabels(schema = {}) {
    const language = schema?.business?.selectedLanguage || schema?.business?.selected_language || schema?.selectedLanguage || schema?.selected_language || "en";
    const labels = {
      en: { account: "Account", cart: "Cart", addToCart: "Add to cart" },
      es: { account: "Cuenta", cart: "Carrito", addToCart: "Agregar al carrito" },
      fr: { account: "Compte", cart: "Panier", addToCart: "Ajouter au panier" },
      pt: { account: "Conta", cart: "Carrinho", addToCart: "Adicionar ao carrinho" }
    };
    return labels[language] || labels.en;
  }
  function renderCommerceNavActions(schema) {
    const labels = commerceLabels(schema);
    return `<div class="commerce-actions">
    <button class="commerce-action" type="button">${escapeHtml(labels.account)}</button>
    <button class="commerce-action cart-button" type="button">${escapeHtml(labels.cart)} <span>0</span></button>
  </div>`;
  }
  function renderStudioFloatingCatalog(schema, context = {}) {
    if (context.isClientPreviewMode) return "";
    const items = arrayValue(schema.catalog_items || schema.products_services).filter((item) => item.is_active !== false).slice(0, 3);
    if (!items.length) return "";
    return `<div class="studio-floating-catalog" aria-hidden="true">
    <span>Store preview</span>
    <div>${items.map((item) => `<article>
      ${renderCatalogImage(item)}
      <strong>${escapeHtml(item.name || "Product")}</strong>
      <small>${escapeHtml(item.price_label || "Precio editable")}</small>
    </article>`).join("")}</div>
  </div>`;
  }
  function renderSection(section, schema) {
    const renderers = {
      Hero: renderHero,
      PremiumHero: renderPremiumHero,
      ProductStory: renderProductStory,
      FeatureShowcase: renderFeatureShowcase,
      EditorialGallery: renderPortfolioGallery,
      PortfolioGallery: renderPortfolioGallery,
      VideoShowcase: renderVideoShowcase,
      CourseOffering: renderCourseOffering,
      SpecStrip: renderSpecStrip,
      FashionHero: renderFashionHero,
      FashionCollectionRail: renderFashionCollectionRail,
      FashionDropStory: renderFashionDropStory,
      FashionLookbook: renderFashionLookbook,
      FashionFitGuide: renderFashionFitGuide,
      CorporateHero: renderCorporateHero,
      CorporateServices: renderCorporateServices,
      CorporateProcess: renderCorporateProcess,
      CorporateProof: renderCorporateProof,
      RestaurantHero: renderRestaurantHero,
      RestaurantCategoryRail: renderRestaurantCategoryRail,
      RestaurantSignatureMenu: renderRestaurantSignatureMenu,
      RestaurantSpecials: renderRestaurantSpecials,
      RestaurantInfo: renderRestaurantInfo,
      RestaurantOrderPanel: renderRestaurantOrderPanel,
      DigitalHero: renderDigitalHero,
      DigitalBundle: renderDigitalBundle,
      DigitalModules: renderDigitalModules,
      DigitalProof: renderDigitalProof,
      DigitalAccessPanel: renderDigitalAccessPanel,
      LuxuryHero: renderLuxuryHero,
      LuxurySignature: renderLuxurySignature,
      LuxuryCollection: renderLuxuryCollection,
      LuxuryProvenance: renderLuxuryProvenance,
      LuxuryPrivateService: renderLuxuryPrivateService,
      LuxuryContact: renderLuxuryContact,
      AcademyHero: renderAcademyHero,
      AcademyLearningPath: renderAcademyLearningPath,
      AcademyPrograms: renderAcademyPrograms,
      AcademyOutcomes: renderAcademyOutcomes,
      AcademyInstructor: renderAcademyInstructor,
      AcademyEnroll: renderAcademyEnroll,
      ClinicHero: renderClinicHero,
      ClinicServices: renderClinicServices,
      ClinicTreatmentPath: renderClinicTreatmentPath,
      ClinicTrust: renderClinicTrust,
      ClinicResults: renderClinicResults,
      ClinicTeam: renderClinicTeam,
      ClinicBooking: renderClinicBooking,
      ProfessionalHero: renderProfessionalHero,
      ProfessionalPracticeAreas: renderProfessionalPracticeAreas,
      ProfessionalProcess: renderProfessionalProcess,
      ProfessionalProof: renderProfessionalProof,
      ProfessionalTeam: renderProfessionalTeam,
      ProfessionalFAQ: renderProfessionalFAQ,
      ProfessionalConsultation: renderProfessionalConsultation,
      EnterpriseHero: renderEnterpriseHero,
      EnterpriseSolutions: renderEnterpriseSolutions,
      EnterpriseUseCases: renderEnterpriseUseCases,
      EnterpriseIntegrations: renderEnterpriseIntegrations,
      EnterpriseProof: renderEnterpriseProof,
      EnterprisePricing: renderEnterprisePricing,
      EnterpriseDemo: renderEnterpriseDemo,
      IndustrialHero: renderIndustrialHero,
      IndustrialSpecCatalog: renderIndustrialSpecCatalog,
      IndustrialCapabilities: renderCapabilitiesEquipment,
      CapabilitiesEquipment: renderCapabilitiesEquipment,
      IndustrialCertifications: renderIndustrialCertifications,
      IndustrialSupplyChain: renderIndustrialSupplyChain,
      IndustrialQuotePanel: renderQuoteRequestForm,
      QuoteRequestForm: renderQuoteRequestForm,
      ListingHero: renderListingHero,
      ListingFilters: renderListingFilters,
      ListingFeatured: renderListingFeatured,
      ListingAreaPanel: renderListingAreaPanel,
      ListingTrust: renderListingTrust,
      ListingContact: renderListingContact,
      HomeServiceHero: renderHomeServiceHero,
      HomeServiceCategories: renderHomeServiceCategories,
      HomeServiceAreas: renderHomeServiceAreas,
      HomeServiceGallery: renderPortfolioGallery,
      HomeServiceTrust: renderHomeServiceTrust,
      HomeServiceQuote: renderHomeServiceQuote,
      BookingHero: renderBookingHero,
      BookingServices: renderBookingServices,
      BookingAvailability: renderBookingAvailability,
      BookingTeam: renderBookingTeam,
      BookingContact: renderBookingContact,
      FunnelHero: renderFunnelHero,
      FunnelBenefits: renderFunnelBenefits,
      FunnelOffer: renderFunnelOffer,
      FunnelProof: renderFunnelProof,
      FunnelFAQ: renderFunnelFAQ,
      MarketplaceHero: renderMarketplaceHero,
      CategoryRail: renderCategoryRail,
      DealRow: renderDealRow,
      TrustStrip: renderTrustStrip,
      ProductGrid: renderProductGrid,
      ServiceList: renderProductGrid,
      FeatureBand: renderFeatureBand,
      About: renderFeatureBand,
      Gallery: renderGallery,
      Testimonials: renderFeatureBand,
      Contact: renderContact,
      Footer: renderContact
    };
    return (renderers[section.type] || renderFeatureBand)(section, schema);
  }
  function marketplaceItems(schema) {
    return (schema.catalog_items || schema.products_services || []).filter((item) => item.is_active !== false && item.display_in_catalog !== false).sort((a, b) => Number(a.sort_order || 0) - Number(b.sort_order || 0));
  }
  function renderHero(section, schema) {
    const editable = section.editable || {};
    const heroItem = (schema.catalog_items || schema.products_services || []).find((item) => item.is_featured && item.image_url) || (schema.catalog_items || schema.products_services || []).find((item) => item.image_url);
    const image = editable.image_url || heroItem?.image_url || "";
    const variant = section.variant || section.settings?.layout || "split_showcase";
    const heroCopy = `<div class="rendered-hero-copy">
      ${schema.brand?.logoUrl ? `<span class="hero-brand-badge"><img src="${escapeAttribute(schema.brand.logoUrl)}" alt="">${escapeHtml(schema.business.name || "")}</span>` : ""}
      <span class="rendered-kicker">${escapeHtml(schema.business.industry || schema.business.location || "Featured")}</span>
      <h1>${escapeHtml(editable.headline || schema.business.name)}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business.description)}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || schema.theme.buttons.primary_label)}</a>
        ${editable.secondary_button || schema.theme.buttons.secondary_label ? `<a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || schema.theme.buttons.secondary_label)}</a>` : ""}
      </div>
    </div>`;
    const visual = `<div class="rendered-visual">
      ${image ? `<img src="${escapeAttribute(image)}" alt="">` : visualPlaceholder(schema)}
    </div>`;
    if (variant === "centered_bold") {
      return `<section class="rendered-hero hero-centered-bold ${sectionClass(section)}" ${sectionAttrs(section)}>
      <div class="rendered-hero-backdrop">${visual}</div>${heroCopy}
    </section>`;
    }
    return `<section class="rendered-hero hero-${escapeAttribute(slugify(variant))} ${sectionClass(section)}" ${sectionAttrs(section)}>${heroCopy}${visual}</section>`;
  }
  function renderPremiumHero(section, schema) {
    const editable = section.editable || {};
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url);
    const image = editable.image_url || heroItem?.image_url || "";
    const firstItem = items[0];
    const variant = section.variant || section.settings?.layout || "split_showcase";
    return `<section class="premium-hero premium-hero-${escapeAttribute(slugify(variant))} ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="premium-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || schema.business?.tone || "")}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || schema.theme?.buttons?.primary_label || "Explore")}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || schema.theme?.buttons?.secondary_label || "Learn more")}</a>
      </div>
    </div>
    <div class="premium-product-stage">
      ${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(schema.business?.name || "")}">` : premiumVisualPlaceholder(schema)}
    </div>
    ${firstItem ? `<div class="premium-hero-meta"><span>${escapeHtml(firstItem.name)}</span><strong>${escapeHtml(productPriceLabel(firstItem, schema))}</strong></div>` : ""}
  </section>`;
  }
  function renderProductStory(section, schema) {
    const editable = section.editable || {};
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    return `<section class="premium-story ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(schema.business?.tone || "")}</span>
      <h2>${escapeHtml(editable.title || editable.headline || "")}</h2>
      <p>${escapeHtml(editable.text || editable.subtitle || "")}</p>
    </div>
    <div class="premium-story-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : premiumVisualPlaceholder(schema)}</div>
  </section>`;
  }
  function renderFeatureShowcase(section, schema) {
    const editable = section.editable || {};
    const items = marketplaceItems(schema).slice(0, 3);
    return `<section class="premium-feature-showcase ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || "")}</span>
      <h2>${escapeHtml(editable.title || "")}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div class="premium-feature-grid">
      ${items.map((item, index) => `<article>
        <small>${escapeHtml(index === 0 ? catalogLocaleLabels(schema).signature : catalogLocaleLabels(schema).detail)}</small>
        <h3>${escapeHtml(item.name)}</h3>
        <p>${escapeHtml(item.description)}</p>
      </article>`).join("")}
    </div>
  </section>`;
  }
  function renderSpecStrip(section, schema) {
    const editable = section.editable || {};
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : catalogLocaleLabels(schema).premiumSpecs;
    return `<section class="premium-spec-strip ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <h2>${escapeHtml(editable.title || "")}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div>${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function premiumVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "P").slice(0, 2).toUpperCase();
    return `<div class="premium-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderFashionHero(section, schema) {
    const editable = section.editable || {};
    const items = marketplaceItems(schema);
    const image = editable.image_url || items.find((item) => item.image_url)?.image_url || "";
    return `<section class="fashion-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="fashion-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || catalogLocaleLabels(schema).newDrop || "New drop")}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || schema.theme?.buttons?.primary_label || "Shop")}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || catalogLocaleLabels(schema).lookbook || "Lookbook")}</a>
      </div>
    </div>
    <div class="fashion-hero-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : fashionVisualPlaceholder(schema)}</div>
    <div class="fashion-hero-strip">${items.slice(0, 3).map((item) => `<span>${escapeHtml(item.name)}</span>`).join("")}</div>
  </section>`;
  }
  function renderFashionCollectionRail(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const collections = fashionCollections(schema);
    return `<section class="fashion-collection-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.newDrop)}</span><h2>${escapeHtml(editable.title || labels.collections)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="fashion-collection-rail">${collections.map((collection, index) => `<article><small>0${index + 1}</small><strong>${escapeHtml(collection)}</strong><span>${escapeHtml(index % 2 ? labels.fit : labels.drop)}</span></article>`).join("")}</div>
  </section>`;
  }
  function renderFashionDropStory(section, schema) {
    const editable = section.editable || {};
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    return `<section class="fashion-drop-story ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="fashion-story-image">${image ? `<img src="${escapeAttribute(image)}" alt="">` : fashionVisualPlaceholder(schema)}</div>
    <div><span class="rendered-kicker">${escapeHtml(schema.business?.tone || "")}</span><h2>${escapeHtml(editable.title || "")}</h2><p>${escapeHtml(editable.text || "")}</p></div>
  </section>`;
  }
  function renderFashionLookbook(section, schema) {
    const editable = section.editable || {};
    return `<section class="fashion-lookbook-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).lookbook)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="fashion-lookbook-strip">${marketplaceItems(schema).slice(0, 5).map((item, index) => `<article class="${index === 1 ? "tall" : ""}">${renderCatalogImage(item)}<strong>${escapeHtml(item.name)}</strong></article>`).join("")}</div>
  </section>`;
  }
  function renderFashionFitGuide(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.fitGuideItems;
    return `<section class="fashion-fit-guide ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function fashionCollections(schema) {
    const fromItems = [...new Set(marketplaceItems(schema).map((item) => item.category).filter(Boolean))];
    const labels = catalogLocaleLabels(schema);
    return [.../* @__PURE__ */ new Set([...fromItems, ...labels.fashionCollections || []])].slice(0, 6);
  }
  function fashionVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "FD").slice(0, 2).toUpperCase();
    return `<div class="fashion-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderCorporateHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    return `<section class="corporate-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="corporate-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || labels.company)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestConsultation)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewServices)}</a>
      </div>
    </div>
    <div class="corporate-hero-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : corporateVisualPlaceholder(schema)}</div>
    <div class="corporate-hero-proof">${labels.corporateProofItems.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderCorporateServices(section, schema) {
    const editable = section.editable || {};
    return `<section class="corporate-services-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).services)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderCorporateServicesCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderCorporateProcess(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.corporateProcessItems;
    return `<section class="corporate-process-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.process)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="corporate-process-list">${items.map((item, index) => `<article><small>0${index + 1}</small><strong>${escapeHtml(item)}</strong></article>`).join("")}</div>
  </section>`;
  }
  function renderCorporateProof(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.corporateProofItems;
    return `<section class="corporate-proof-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function corporateVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "CO").slice(0, 2).toUpperCase();
    return `<div class="corporate-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderRestaurantHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url);
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="restaurant-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="restaurant-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || labels.menu)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.orderNow)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewMenu)}</a>
      </div>
      <div class="restaurant-proof-strip">${labels.restaurantProofItems.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="restaurant-stage">
      <div class="restaurant-stage-visual">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(schema.business?.name || "")}">` : restaurantVisualPlaceholder(schema)}</div>
      <article class="restaurant-mini-card">
        <small>${escapeHtml(labels.pickupDelivery)}</small>
        <strong>${escapeHtml(labels.orderNow)}</strong>
        <span>${escapeHtml(labels.quickOrderNote)}</span>
      </article>
    </div>
  </section>`;
  }
  function renderRestaurantCategoryRail(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const categories = restaurantMenuCategories(schema, editable.items);
    return `<section class="restaurant-categories-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(labels.menu)}</span>
      <h2>${escapeHtml(editable.title || labels.restaurantCategoriesTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div class="restaurant-category-rail">${categories.map((category, index) => `<article>
      <small>0${index + 1}</small>
      <strong>${escapeHtml(category)}</strong>
      <span>${escapeHtml(index % 2 ? labels.chefPick : labels.popularDish)}</span>
    </article>`).join("")}</div>
  </section>`;
  }
  function renderRestaurantSignatureMenu(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="restaurant-menu-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(labels.signatureMenu)}</span>
      <h2>${escapeHtml(editable.title || labels.restaurantSignatureTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    ${renderRestaurantMenuCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderRestaurantSpecials(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.restaurantSpecialItems;
    return `<section class="restaurant-specials-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(labels.specials)}</span>
      <h2>${escapeHtml(editable.title || labels.restaurantSpecialsTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div class="restaurant-specials-list">${items.map((item, index) => `<article>
      <small>${escapeHtml(index === 0 ? labels.chefPick : labels.specials)}</small>
      <strong>${escapeHtml(item)}</strong>
      <span>${escapeHtml(labels.editableMenuNote)}</span>
    </article>`).join("")}</div>
  </section>`;
  }
  function renderRestaurantInfo(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.restaurantInfoItems;
    return `<section class="restaurant-info-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(labels.hoursLocation)}</span>
      <h2>${escapeHtml(editable.title || labels.restaurantInfoTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div>${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderRestaurantOrderPanel(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="restaurant-order-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="restaurant-order-card">
      <span class="rendered-kicker">${escapeHtml(labels.orderNow)}</span>
      <h2>${escapeHtml(editable.title || labels.restaurantOrderTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.restaurantOrderText)}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(labels.orderNow)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(labels.contact)}</a>
      </div>
    </div>
  </section>`;
  }
  function restaurantMenuCategories(schema, fallback = []) {
    const fromItems = [...new Set(marketplaceItems(schema).map((item) => item.category).filter(Boolean))];
    const labels = catalogLocaleLabels(schema);
    return [.../* @__PURE__ */ new Set([...fromItems, ...Array.isArray(fallback) ? fallback : [], ...labels.restaurantCategories || []])].slice(0, 6);
  }
  function restaurantVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "RM").slice(0, 2).toUpperCase();
    return `<div class="restaurant-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderDigitalHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url);
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="digital-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="digital-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || labels.digitalProducts)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#" data-page-link="catalog">${escapeHtml(editable.primary_button || labels.getAccess)}</a>
        <a class="rendered-button secondary" href="#" data-page-link="contact">${escapeHtml(editable.secondary_button || labels.viewProducts)}</a>
      </div>
      <div class="digital-proof-strip">${labels.digitalProofItems.slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="digital-stage">
      <div class="digital-stage-visual">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(schema.business?.name || "")}">` : digitalVisualPlaceholder(schema)}</div>
      <article class="digital-access-card">
        <small>${escapeHtml(labels.instantAccess)}</small>
        <strong>${escapeHtml(labels.getAccess)}</strong>
        <span>${escapeHtml(labels.digitalAccessShort)}</span>
      </article>
    </div>
  </section>`;
  }
  function renderDigitalBundle(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="digital-bundle-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(labels.digitalProducts)}</span>
      <h2>${escapeHtml(editable.title || labels.digitalBundleTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    ${renderDigitalOfferCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderDigitalModules(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.digitalModuleItems;
    return `<section class="digital-modules-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.modules)}</span><h2>${escapeHtml(editable.title || labels.digitalModulesTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="digital-module-grid">${items.map((item, index) => `<article><small>0${index + 1}</small><strong>${escapeHtml(item)}</strong><span>${escapeHtml(index % 2 ? labels.downloadable : labels.bonus)}</span></article>`).join("")}</div>
  </section>`;
  }
  function renderDigitalProof(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.digitalProofItems;
    return `<section class="digital-proof-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || labels.digitalProofTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderDigitalAccessPanel(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="digital-access-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="digital-access-panel">
      <span class="rendered-kicker">${escapeHtml(labels.instantAccess)}</span>
      <h2>${escapeHtml(editable.title || labels.digitalAccessTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.digitalAccessText)}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#" data-page-link="catalog">${escapeHtml(labels.getAccess)}</a>
        <a class="rendered-button secondary" href="#" data-page-link="contact">${escapeHtml(labels.contact)}</a>
      </div>
    </div>
  </section>`;
  }
  function digitalVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "DP").slice(0, 2).toUpperCase();
    return `<div class="digital-visual-placeholder">
    <span>${escapeHtml(initials)}</span>
    <small>${escapeHtml(catalogLocaleLabels(schema).instantAccess)}</small>
  </div>`;
  }
  function renderLuxuryHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="luxury-pro-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="luxury-pro-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.limitedSelection)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestPrivateViewing)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewCollection)}</a>
      </div>
      <div class="luxury-proof-strip">
        ${(labels.luxuryProofItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
      </div>
    </div>
    <div class="luxury-pro-stage">
      <div class="luxury-hero-visual">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : luxuryVisualPlaceholder(schema)}</div>
      <div class="luxury-floating-card">
        <small>${escapeHtml(labels.signaturePiece)}</small>
        <strong>${escapeHtml(heroItem?.name || schema.business?.name || "")}</strong>
        <span>${escapeHtml(heroItem?.price_label || labels.priceOnRequest)}</span>
      </div>
    </div>
  </section>`;
  }
  function renderLuxurySignature(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const item = marketplaceItems(schema)[0];
    const image = editable.image_url || item?.image_url || "";
    const proofItems = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.luxuryProofItems;
    return `<section class="luxury-signature-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="luxury-signature-media">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(item?.name || "")}">` : luxuryVisualPlaceholder(schema)}</div>
    <div class="luxury-signature-copy">
      <span class="rendered-kicker">${escapeHtml(labels.authenticated)}</span>
      <h2>${escapeHtml(editable.title || labels.signaturePiece)}</h2>
      <p>${escapeHtml(editable.text || "")}</p>
      <div class="luxury-mini-proof">${proofItems.slice(0, 4).map((proof) => `<span>${escapeHtml(proof)}</span>`).join("")}</div>
    </div>
  </section>`;
  }
  function renderLuxuryCollection(section, schema) {
    const editable = section.editable || {};
    return `<section class="luxury-collection-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).collection)}</span>
      <h2>${escapeHtml(editable.title || "")}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    ${renderLuxuryHighTicketCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderLuxuryProvenance(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.luxuryProofItems;
    return `<section class="luxury-provenance-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(labels.provenance)}</span>
      <h2>${escapeHtml(editable.title || labels.luxuryProvenanceTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.luxuryProvenanceText)}</p>
    </div>
    <div class="luxury-provenance-grid">
      ${items.slice(0, 6).map((item) => `<article><span></span><strong>${escapeHtml(item)}</strong><p>${escapeHtml(labels.privateService)}</p></article>`).join("")}
    </div>
  </section>`;
  }
  function renderLuxuryPrivateService(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="luxury-private-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(labels.privateInquiry)}</span>
      <h2>${escapeHtml(editable.title || labels.luxuryPrivateTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.luxuryPrivateText)}</p>
    </div>
    <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestPrivateViewing)}</a>
  </section>`;
  }
  function renderLuxuryContact(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="luxury-contact-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="luxury-contact-card">
      <span class="rendered-kicker">${escapeHtml(labels.privateInquiry)}</span>
      <h2>${escapeHtml(editable.title || labels.luxuryContactTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.luxuryContactText)}</p>
      <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestPrivateViewing)}</a>
    </div>
  </section>`;
  }
  function luxuryVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "LX").slice(0, 2).toUpperCase();
    return `<div class="luxury-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).privateService)}</small></div>`;
  }
  function renderAcademyHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="academy-pro-hero ${sectionClass(section)}">
    <div class="academy-pro-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.courseAcademy)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.enrollNow)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewCurriculum)}</a>
      </div>
      <div class="academy-proof-strip">${(labels.learningOutcomeItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="academy-pro-stage">
      <div class="academy-dashboard-card">
        <div class="academy-dashboard-top"><span></span><span></span><span></span></div>
        <div class="academy-dashboard-main">
          <div class="academy-video-frame">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : academyVisualPlaceholder(schema)}</div>
          <div class="academy-progress-panel">
            <small>${escapeHtml(labels.curriculum)}</small>
            ${(labels.learningPathItems || []).slice(0, 4).map((item, index) => `<div><b>0${index + 1}</b><span>${escapeHtml(item)}</span></div>`).join("")}
          </div>
        </div>
      </div>
      <div class="academy-floating-card"><small>${escapeHtml(labels.featuredProgram)}</small><strong>${escapeHtml(heroItem?.name || labels.programsTitle)}</strong><span>${escapeHtml(heroItem?.shipping_label || labels.educationDurations?.[0] || "")}</span></div>
    </div>
  </section>`;
  }
  function renderAcademyLearningPath(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.learningPathItems;
    return `<section class="academy-path-section ${sectionClass(section)}">
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.curriculum)}</span><h2>${escapeHtml(editable.title || labels.learningPathTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="academy-path-grid">${items.slice(0, 6).map((item, index) => `<article><span>0${index + 1}</span><strong>${escapeHtml(item)}</strong><p>${escapeHtml(index % 2 ? labels.beginnerFriendly : labels.certificateReady)}</p></article>`).join("")}</div>
  </section>`;
  }
  function renderAcademyPrograms(section, schema) {
    const editable = section.editable || {};
    return `<section class="academy-programs-section ${sectionClass(section)}">
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).programs)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderEducationCourseCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderAcademyOutcomes(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.learningOutcomeItems;
    return `<section class="academy-outcomes-section ${sectionClass(section)}">
    <div><span class="rendered-kicker">${escapeHtml(labels.outcomes)}</span><h2>${escapeHtml(editable.title || labels.outcomesTitle)}</h2><p>${escapeHtml(editable.text || labels.outcomesText)}</p></div>
    <div class="academy-outcome-grid">${items.slice(0, 6).map((item) => `<article><span></span><strong>${escapeHtml(item)}</strong><p>${escapeHtml(labels.courseAcademy)}</p></article>`).join("")}</div>
  </section>`;
  }
  function renderAcademyInstructor(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.instructorTrustItems;
    return `<section class="academy-instructor-section ${sectionClass(section)}">
    <div class="academy-instructor-card">${editable.image_url ? `<img src="${escapeAttribute(editable.image_url)}" alt="${escapeAttribute(editable.title || "")}">` : academyVisualPlaceholder(schema)}</div>
    <div><span class="rendered-kicker">${escapeHtml(labels.instructor)}</span><h2>${escapeHtml(editable.title || labels.instructorTitle)}</h2><p>${escapeHtml(editable.text || labels.instructorText)}</p><div class="academy-mini-proof">${items.slice(0, 4).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></div>
  </section>`;
  }
  function renderAcademyEnroll(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="academy-enroll-section ${sectionClass(section)}">
    <div><span class="rendered-kicker">${escapeHtml(labels.enroll)}</span><h2>${escapeHtml(editable.title || labels.enrollmentTitle)}</h2><p>${escapeHtml(editable.text || labels.enrollmentText)}</p></div>
    <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.enrollNow)}</a>
  </section>`;
  }
  function academyVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "AC").slice(0, 2).toUpperCase();
    return `<div class="academy-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).courseAcademy)}</small></div>`;
  }
  function renderClinicHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="clinic-pro-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="clinic-pro-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.clinicCare)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.bookConsultation)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewTreatments)}</a>
      </div>
      <div class="clinic-proof-strip">${(labels.clinicTrustItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="clinic-pro-stage">
      <div class="clinic-appointment-card">
        <div class="clinic-appointment-top"><span>${escapeHtml(labels.bookConsultation)}</span><b>${escapeHtml(labels.consultationBased)}</b></div>
        <div class="clinic-appointment-media">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : clinicVisualPlaceholder(schema)}</div>
        <div class="clinic-slots">${(labels.clinicDurations || []).slice(0, 3).map((item, index) => `<span class="${index === 1 ? "active" : ""}">${escapeHtml(item)}</span>`).join("")}</div>
      </div>
      <div class="clinic-floating-card"><small>${escapeHtml(labels.popularTreatment)}</small><strong>${escapeHtml(heroItem?.name || labels.treatmentsTitle)}</strong><span>${escapeHtml(heroItem?.shipping_label || labels.clinicDurations?.[0] || "")}</span></div>
    </div>
  </section>`;
  }
  function renderClinicServices(section, schema) {
    const editable = section.editable || {};
    return `<section class="clinic-services-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).treatments)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderMedicalWellnessCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderClinicTreatmentPath(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.carePathItems;
    return `<section class="clinic-path-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.clinicCare)}</span><h2>${escapeHtml(editable.title || labels.carePathTitle)}</h2><p>${escapeHtml(editable.text || labels.carePathText)}</p></div>
    <div class="clinic-path-list">${items.slice(0, 6).map((item, index) => `<article><span>0${index + 1}</span><strong>${escapeHtml(item)}</strong><p>${escapeHtml(index % 2 ? labels.personalizedPlan : labels.specialistLed)}</p></article>`).join("")}</div>
  </section>`;
  }
  function renderClinicTrust(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.clinicTrustItems;
    return `<section class="clinic-trust-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.clinicCare)}</span><h2>${escapeHtml(editable.title || labels.clinicTrustTitle)}</h2><p>${escapeHtml(editable.text || labels.clinicTrustText)}</p></div>
    <div class="clinic-trust-grid">${items.slice(0, 6).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderClinicResults(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.resultItems;
    return `<section class="clinic-results-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="clinic-results-copy"><span class="rendered-kicker">${escapeHtml(labels.results)}</span><h2>${escapeHtml(editable.title || labels.resultsTitle)}</h2><p>${escapeHtml(editable.text || labels.resultsText)}</p></div>
    <div class="clinic-results-board">${items.slice(0, 4).map((item, index) => `<article><b>${index === 0 ? "01" : `0${index + 1}`}</b><strong>${escapeHtml(item)}</strong><span>${escapeHtml(index % 2 ? labels.clinicCare : labels.consultationBased)}</span></article>`).join("")}</div>
  </section>`;
  }
  function renderClinicTeam(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.clinicTeamItems;
    return `<section class="clinic-team-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="clinic-team-media">${editable.image_url ? `<img src="${escapeAttribute(editable.image_url)}" alt="${escapeAttribute(editable.title || "")}">` : clinicVisualPlaceholder(schema)}</div>
    <div><span class="rendered-kicker">${escapeHtml(labels.specialistLed)}</span><h2>${escapeHtml(editable.title || labels.clinicTeamTitle)}</h2><p>${escapeHtml(editable.text || labels.clinicTeamText)}</p><div class="clinic-mini-proof">${items.slice(0, 4).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></div>
  </section>`;
  }
  function renderClinicBooking(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="clinic-booking-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.bookConsultation)}</span><h2>${escapeHtml(editable.title || labels.clinicBookingTitle)}</h2><p>${escapeHtml(editable.text || labels.clinicBookingText)}</p></div>
    <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.bookConsultation)}</a>
  </section>`;
  }
  function clinicVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "CL").slice(0, 2).toUpperCase();
    return `<div class="clinic-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).clinicCare)}</small></div>`;
  }
  function renderProfessionalHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="professional-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="professional-hero-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.professionalFirm)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.scheduleConsultation)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewServices)}</a>
      </div>
      <div class="professional-proof-strip">${(labels.professionalProofItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="professional-stage">
      <div class="professional-case-card">
        <div class="professional-case-top"><span>${escapeHtml(labels.scheduleConsultation)}</span><b>${escapeHtml(labels.confidential)}</b></div>
        <div class="professional-case-visual">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : professionalVisualPlaceholder(schema)}</div>
        <div class="professional-document-list">${(labels.professionalProcessItems || []).slice(0, 4).map((item, index) => `<span><b>0${index + 1}</b>${escapeHtml(item)}</span>`).join("")}</div>
      </div>
      <div class="professional-floating-card"><small>${escapeHtml(labels.caseReview)}</small><strong>${escapeHtml(heroItem?.name || labels.practiceAreasTitle)}</strong><span>${escapeHtml(heroItem?.shipping_label || labels.professionalEngagements?.[0] || "")}</span></div>
    </div>
  </section>`;
  }
  function renderProfessionalPracticeAreas(section, schema) {
    const editable = section.editable || {};
    return `<section class="professional-services-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).services)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderLegalProfessionalCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderProfessionalProcess(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.professionalProcessItems;
    return `<section class="professional-process-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.process)}</span><h2>${escapeHtml(editable.title || labels.professionalProcessTitle)}</h2><p>${escapeHtml(editable.text || labels.professionalProcessText)}</p></div>
    <div class="professional-process-list">${items.slice(0, 6).map((item, index) => `<article><span>0${index + 1}</span><strong>${escapeHtml(item)}</strong><p>${escapeHtml(index % 2 ? labels.businessReady : labels.confidential)}</p></article>`).join("")}</div>
  </section>`;
  }
  function renderProfessionalProof(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.professionalProofItems;
    return `<section class="professional-proof-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || labels.professionalProofTitle)}</h2><p>${escapeHtml(editable.text || labels.professionalProofText)}</p></div>
    <div class="professional-proof-grid">${items.slice(0, 6).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderProfessionalTeam(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.professionalTeamItems;
    return `<section class="professional-team-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="professional-team-media">${editable.image_url ? `<img src="${escapeAttribute(editable.image_url)}" alt="${escapeAttribute(editable.title || "")}">` : professionalVisualPlaceholder(schema)}</div>
    <div><span class="rendered-kicker">${escapeHtml(labels.seniorAdvisor)}</span><h2>${escapeHtml(editable.title || labels.professionalTeamTitle)}</h2><p>${escapeHtml(editable.text || labels.professionalTeamText)}</p><div class="professional-mini-proof">${items.slice(0, 4).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></div>
  </section>`;
  }
  function renderProfessionalFAQ(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.professionalFaqItems;
    return `<section class="professional-faq-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.faq)}</span><h2>${escapeHtml(editable.title || labels.professionalFaqTitle)}</h2></div>
    <div class="professional-faq-list">${items.slice(0, 5).map((item) => `<article><strong>${escapeHtml(item)}</strong><p>${escapeHtml(labels.professionalConsultationText)}</p></article>`).join("")}</div>
  </section>`;
  }
  function renderProfessionalConsultation(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="professional-consultation-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.scheduleConsultation)}</span><h2>${escapeHtml(editable.title || labels.professionalConsultationTitle)}</h2><p>${escapeHtml(editable.text || labels.professionalConsultationText)}</p></div>
    <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.scheduleConsultation)}</a>
  </section>`;
  }
  function professionalVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "PF").slice(0, 2).toUpperCase();
    return `<div class="professional-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).professionalFirm)}</small></div>`;
  }
  function renderEnterpriseHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="enterprise-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="enterprise-hero-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.enterprisePlatform)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions"><a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestDemo)}</a><a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewSolutions)}</a></div>
      <div class="enterprise-metric-strip">${(labels.enterpriseProofItems || []).slice(0, 3).map((item, index) => `<span><b>${index === 0 ? "99%" : index === 1 ? "2x" : "24/7"}</b>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="enterprise-dashboard">
      <div class="enterprise-dashboard-top"><span></span><span></span><span></span><b>${escapeHtml(labels.integrations)}</b></div>
      <div class="enterprise-dashboard-main">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : enterpriseVisualPlaceholder(schema)}</div>
      <div class="enterprise-dashboard-grid">${(labels.enterpriseIntegrationItems || []).slice(0, 6).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
  </section>`;
  }
  function renderEnterpriseSolutions(section, schema) {
    const editable = section.editable || {};
    return `<section class="enterprise-solutions-section ${sectionClass(section)}" ${sectionAttrs(section)}><div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).solutions)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>${renderB2BSolutionCatalog(marketplaceItems(schema), schema)}</section>`;
  }
  function renderEnterpriseUseCases(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.enterpriseUseCaseItems;
    return `<section class="enterprise-use-cases-section ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.useCases)}</span><h2>${escapeHtml(editable.title || labels.enterpriseUseCasesTitle)}</h2><p>${escapeHtml(editable.text || labels.enterpriseUseCasesText)}</p></div><div class="enterprise-use-case-grid">${items.slice(0, 6).map((item, index) => `<article><b>0${index + 1}</b><strong>${escapeHtml(item)}</strong><span>${escapeHtml(index % 2 ? labels.integrationReady : labels.roiFocused)}</span></article>`).join("")}</div></section>`;
  }
  function renderEnterpriseIntegrations(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.enterpriseIntegrationItems;
    return `<section class="enterprise-integrations-section ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.integrations)}</span><h2>${escapeHtml(editable.title || labels.enterpriseIntegrationsTitle)}</h2><p>${escapeHtml(editable.text || labels.enterpriseIntegrationsText)}</p></div><div class="enterprise-integration-map">${items.slice(0, 8).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></section>`;
  }
  function renderEnterpriseProof(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.enterpriseProofItems;
    return `<section class="enterprise-proof-section ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || labels.enterpriseProofTitle)}</h2><p>${escapeHtml(editable.text || labels.enterpriseProofText)}</p></div><div class="enterprise-proof-grid">${items.slice(0, 6).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></section>`;
  }
  function renderEnterprisePricing(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="enterprise-pricing-section ${sectionClass(section)}" ${sectionAttrs(section)}><div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.customPlan)}</span><h2>${escapeHtml(editable.title || labels.enterprisePricingTitle)}</h2><p>${escapeHtml(editable.text || labels.enterprisePricingText)}</p></div>${renderB2BSolutionCatalog(marketplaceItems(schema).slice(0, 3), schema)}</section>`;
  }
  function renderEnterpriseDemo(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="enterprise-demo-section ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.requestDemo)}</span><h2>${escapeHtml(editable.title || labels.enterpriseDemoTitle)}</h2><p>${escapeHtml(editable.text || labels.enterpriseDemoText)}</p></div><a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestDemo)}</a></section>`;
  }
  function enterpriseVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "B2").slice(0, 2).toUpperCase();
    return `<div class="enterprise-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).enterprisePlatform)}</small></div>`;
  }
  function renderIndustrialHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const heroItem = items.find((item) => item.is_featured && item.image_url) || items.find((item) => item.image_url) || items[0];
    const image = editable.image_url || heroItem?.image_url || "";
    return `<section class="industrial-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="industrial-hero-copy">
      <span class="rendered-kicker">${escapeHtml(editable.badge || labels.industrialSupplier)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.requestQuote)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewSpecs)}</a>
      </div>
      <div class="industrial-proof-strip">${(labels.industrialCertificationItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="industrial-rfq-dashboard">
      <div class="industrial-dashboard-top"><span></span><span></span><span></span><b>${escapeHtml(labels.requestQuote)}</b></div>
      <div class="industrial-dashboard-body">
        <div class="industrial-product-visual">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(heroItem?.name || schema.business?.name || "")}">` : industrialVisualPlaceholder(schema)}</div>
        <div class="industrial-spec-table">
          ${(items.length ? items : [{ name: labels.industrialCatalogTitle }, { name: labels.certifications }, { name: labels.supplyChain }]).slice(0, 4).map((item, index) => `<div><b>${escapeHtml(item.specs?.sku || `IND-${String(index + 1).padStart(3, "0")}`)}</b><span>${escapeHtml(item.name)}</span><small>${escapeHtml(item.shipping_label || labels.industrialLeadTimes?.[index % (labels.industrialLeadTimes?.length || 1)] || "")}</small></div>`).join("")}
        </div>
      </div>
    </div>
  </section>`;
  }
  function renderIndustrialSpecCatalog(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="industrial-catalog-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="industrial-catalog-header">
      <div><span class="rendered-kicker">${escapeHtml(labels.viewSpecs)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
      <div class="industrial-catalog-stats"><span>${escapeHtml(labels.quoteRequired)}</span><span>${escapeHtml(labels.moqReady)}</span><span>${escapeHtml(labels.certified)}</span></div>
    </div>
    ${renderIndustrialSupplierCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderCapabilitiesEquipment(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.industrialCapabilityItems;
    return `<section class="industrial-capabilities-section capabilities-equipment ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.capabilities)}</span><h2>${escapeHtml(editable.title || labels.industrialCapabilitiesTitle)}</h2><p>${escapeHtml(editable.text || labels.industrialCapabilitiesText)}</p></div><div class="industrial-capability-board">${items.slice(0, 8).map((item, index) => {
      const entry = typeof item === "object" ? item : { title: item };
      return `<div><b>${escapeHtml(entry.icon || String(index + 1).padStart(2, "0"))}</b><strong>${escapeHtml(entry.title || entry.name || "")}</strong><span>${escapeHtml(entry.description || (index % 2 ? labels.bulkReady : labels.specReady))}</span></div>`;
    }).join("")}</div></section>`;
  }
  function renderIndustrialCertifications(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.industrialCertificationItems;
    return `<section class="industrial-certifications-section ${sectionClass(section)}" ${sectionAttrs(section)}><div><span class="rendered-kicker">${escapeHtml(labels.certifications)}</span><h2>${escapeHtml(editable.title || labels.industrialCertificationsTitle)}</h2><p>${escapeHtml(editable.text || labels.industrialCertificationsText)}</p></div><div class="industrial-doc-stack">${items.slice(0, 6).map((item, index) => `<article><b>DOC-${String(index + 1).padStart(2, "0")}</b><span>${escapeHtml(item)}</span><small>PDF / QA</small></article>`).join("")}</div></section>`;
  }
  function renderIndustrialSupplyChain(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.industrialSupplyItems;
    return `<section class="industrial-supply-section ${sectionClass(section)}" ${sectionAttrs(section)}><div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.supplyChain)}</span><h2>${escapeHtml(editable.title || labels.industrialSupplyTitle)}</h2><p>${escapeHtml(editable.text || labels.industrialSupplyText)}</p></div><div class="industrial-supply-line">${items.slice(0, 6).map((item, index) => `<article><small>STEP ${index + 1}</small><strong>${escapeHtml(item)}</strong></article>`).join("")}</div></section>`;
  }
  function renderQuoteRequestForm(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const language = schema.business?.selectedLanguage || "en";
    const defaults = language === "es" ? [{ name: "need", label: "Que necesitas" }, { name: "quantity", label: "Cantidad aproximada" }, { name: "target_date", label: "Fecha deseada", type: "date" }, { name: "notes", label: "Notas", type: "textarea" }] : [{ name: "need", label: "What do you need" }, { name: "quantity", label: "Approximate quantity" }, { name: "target_date", label: "Desired date", type: "date" }, { name: "notes", label: "Notes", type: "textarea" }];
    const fields = Array.isArray(editable.fields) && editable.fields.length ? editable.fields : defaults;
    return `<section class="industrial-quote-section quote-request-form ${sectionClass(section)}" ${sectionAttrs(section)}><div class="industrial-quote-card"><div><span class="rendered-kicker">${escapeHtml(labels.requestQuote)}</span><h2>${escapeHtml(editable.title || labels.industrialQuoteTitle)}</h2><p>${escapeHtml(editable.text || labels.industrialQuoteText)}</p></div><div class="industrial-rfq-fields">${fields.slice(0, 8).map((field, index) => {
      const entry = typeof field === "object" ? field : { label: field };
      const label = entry.label || entry.name || defaults[index]?.label || "Details";
      const control = entry.type === "textarea" ? `<textarea name="${escapeAttribute(entry.name || slugify(label))}" placeholder="${escapeAttribute(entry.placeholder || label)}" ${entry.required ? "required" : ""}></textarea>` : `<input type="${escapeAttribute(entry.type || "text")}" name="${escapeAttribute(entry.name || slugify(label))}" placeholder="${escapeAttribute(entry.placeholder || label)}" ${entry.required ? "required" : ""}>`;
      return `<label><span>${escapeHtml(label)}</span>${control}</label>`;
    }).join("")}</div><button class="rendered-button" type="button" data-open-lead>${escapeHtml(editable.primary_button || labels.requestQuote)}</button></div></section>`;
  }
  function industrialVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "IN").slice(0, 2).toUpperCase();
    return `<div class="industrial-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).industrialSupplier)}</small></div>`;
  }
  function renderListingHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema);
    const image = editable.image_url || items.find((item) => item.image_url)?.image_url || "";
    return `<section class="listing-pro-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="listing-pro-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.location || labels.areas)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <label class="listing-search-bar">
        <span>${escapeHtml(labels.search)}</span>
        <input readonly value="" placeholder="${escapeAttribute(editable.search_placeholder || labels.listingSearchPlaceholder)}">
        <button type="button">${escapeHtml(editable.primary_button || labels.searchListings)}</button>
      </label>
      <div class="listing-quick-filters">${listingCategories(schema).slice(0, 4).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
    </div>
    <div class="listing-pro-stage">
      <div class="listing-map-card">${image ? `<img src="${escapeAttribute(image)}" alt="${escapeAttribute(schema.business?.name || "")}">` : listingVisualPlaceholder(schema)}</div>
      <article class="listing-floating-card">
        <small>${escapeHtml(labels.featuredListing)}</small>
        <strong>${escapeHtml(items[0]?.name || labels.featuredListingsTitle)}</strong>
        <span>${escapeHtml(items[0]?.shipping_label || listingLocationForIndex(0, labels))}</span>
        <b>${escapeHtml(productPriceLabel(items[0] || { price_label: labels.listingPrice }, schema))}</b>
      </article>
    </div>
  </section>`;
  }
  function renderListingFilters(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const categories = Array.isArray(editable.items) && editable.items.length ? editable.items : listingCategories(schema);
    return `<section class="listing-filter-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.searchFilters)}</span><h2>${escapeHtml(editable.title || labels.listingFiltersTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="listing-filter-grid">${categories.slice(0, 6).map((item, index) => `<article><strong>${escapeHtml(item)}</strong><span>${escapeHtml(index % 2 ? labels.availableNow : labels.featuredListing)}</span></article>`).join("")}</div>
  </section>`;
  }
  function renderListingFeatured(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="listing-featured-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.listings)}</span><h2>${escapeHtml(editable.title || labels.featuredListingsTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderRealEstateListingCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderListingAreaPanel(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const locations = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.listingLocations;
    return `<section class="listing-area-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.areas)}</span><h2>${escapeHtml(editable.title || labels.listingAreaTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="listing-area-map">${locations.slice(0, 6).map((item, index) => `<span style="--x:${18 + index * 13 % 64}%;--y:${18 + index * 19 % 58}%">${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderListingTrust(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.listingTrustItems;
    return `<section class="listing-trust-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.trustTitle)}</span><h2>${escapeHtml(editable.title || labels.listingTrustTitle)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.slice(0, 6).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderListingContact(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="listing-contact-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="listing-contact-card">
      <span class="rendered-kicker">${escapeHtml(labels.inquireNow)}</span>
      <h2>${escapeHtml(editable.title || labels.listingContactTitle)}</h2>
      <p>${escapeHtml(editable.text || labels.listingContactText)}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(labels.inquireNow)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(labels.contact)}</a>
      </div>
    </div>
  </section>`;
  }
  function renderRealEstateListingCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-real-estate-listings">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="listing-image">${renderCatalogImage(item)}</div>
    <div class="listing-card-body">
      <div class="listing-card-top"><small>${escapeHtml(item.deal_label || labels.availableNow)}</small><span>${escapeHtml(item.category || labels.listings)}</span></div>
      <h3>${escapeHtml(item.name)}</h3>
      <p>${escapeHtml(item.description)}</p>
      <div class="listing-spec-row"><span>${escapeHtml(item.shipping_label || listingLocationForIndex(index, labels))}</span><span>${escapeHtml(index % 2 ? "3 bd" : "2 bd")}</span><span>${escapeHtml(index % 3 ? "2 ba" : "1 ba")}</span></div>
      <div class="listing-card-bottom"><strong>${escapeHtml(productPriceLabel(item, schema))}</strong><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.inquireNow)}</a></div>
    </div>
  </article>`).join("")}</div>`;
  }
  function listingCategories(schema) {
    const fromItems = [...new Set(marketplaceItems(schema).map((item) => item.category).filter(Boolean))];
    const labels = catalogLocaleLabels(schema);
    return [.../* @__PURE__ */ new Set([...fromItems, ...labels.listingCategories || []])];
  }
  function listingVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "LS").slice(0, 2).toUpperCase();
    return `<div class="listing-visual-placeholder"><span>${escapeHtml(initials)}</span><small>${escapeHtml(catalogLocaleLabels(schema).listings)}</small></div>`;
  }
  function renderHomeServiceHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    const phone = schema.contact?.phone || schema.contact?.whatsapp || labels.callNow;
    return `<section class="home-service-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="home-service-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.location || labels.localExperts)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="home-service-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.freeQuote)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.callNow)}</a>
      </div>
      <div class="home-service-phone"><strong>${escapeHtml(phone)}</strong><span>${escapeHtml(labels.fastResponse)}</span></div>
    </div>
    <div class="home-service-hero-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : homeServiceVisualPlaceholder(schema)}</div>
    <div class="home-service-proof">${labels.homeServiceTrustItems.slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderHomeServiceCategories(section, schema) {
    const editable = section.editable || {};
    return `<section class="home-service-categories ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).services)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderHomeServicesCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderHomeServiceAreas(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.serviceAreaItems;
    return `<section class="home-service-areas ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.serviceAreas)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="home-service-area-map">${items.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderPortfolioGallery(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = (Array.isArray(editable.items) && editable.items.length ? editable.items : marketplaceItems(schema)).slice(0, 8);
    const beforeAfter = section.settings?.layout === "before_after" || editable.before_after === true || items.some((item) => item.beforeImageUrl || item.afterImageUrl);
    return `<section class="home-service-gallery portfolio-gallery ${beforeAfter ? "is-before-after" : ""} ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.workProof)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="home-service-work-grid">${items.map((item) => `<article>
      ${beforeAfter && (item.beforeImageUrl || item.afterImageUrl) ? `<div class="portfolio-before-after"><figure>${renderResilientImage(item.beforeImageUrl || item.image_url, item.name, `${item.name || "Project"} before`)}<figcaption>${escapeHtml(labels.before)}</figcaption></figure><figure>${renderResilientImage(item.afterImageUrl || item.image_url, item.name, `${item.name || "Project"} after`)}<figcaption>${escapeHtml(labels.after)}</figcaption></figure></div>` : renderResilientImage(item.image_url || item.imageUrl, item.name || item.title, item.name || item.title)}
      ${renderImageAttribution(item)}
      <div><strong>${escapeHtml(item.name || item.title || "")}</strong>${item.description ? `<p>${escapeHtml(item.description)}</p>` : ""}${item.price_label || item.price ? `<b>${escapeHtml(item.price_label || item.price)}</b>` : ""}</div>
    </article>`).join("")}</div>
  </section>`;
  }
  function renderVideoShowcase(section) {
    const editable = section.editable || {};
    const videoUrl = editable.videoUrl || editable.video_url || editable.url || "";
    const embedUrl = safeVideoEmbedUrl(videoUrl);
    if (!embedUrl) return `<section class="video-showcase ${sectionClass(section)}" ${sectionAttrs(section)}><div class="section-heading"><h2>${escapeHtml(editable.title || "Video")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div><p class="video-unavailable">Video URL unavailable or unsupported.</p></section>`;
    return `<section class="video-showcase ${sectionClass(section)}" ${sectionAttrs(section)}><div class="section-heading"><h2>${escapeHtml(editable.title || "Video")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div><div class="video-embed"><iframe src="${escapeAttribute(embedUrl)}" title="${escapeAttribute(editable.title || "Video")}" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div></section>`;
  }
  function safeVideoEmbedUrl(value) {
    try {
      const url = new URL(String(value || ""));
      const host = url.hostname.toLowerCase();
      if (host === "youtu.be") {
        const id = url.pathname.split("/").filter(Boolean)[0];
        return /^[a-zA-Z0-9_-]{6,}$/.test(id || "") ? `https://www.youtube.com/embed/${id}` : "";
      }
      if (["youtube.com", "www.youtube.com"].includes(host)) {
        const id = url.searchParams.get("v") || (url.pathname.match(/^\/(?:embed|shorts)\/([a-zA-Z0-9_-]+)/) || [])[1];
        return /^[a-zA-Z0-9_-]{6,}$/.test(id || "") ? `https://www.youtube.com/embed/${id}` : "";
      }
      if (["vimeo.com", "www.vimeo.com", "player.vimeo.com"].includes(host)) {
        const id = (url.pathname.match(/\/(?:video\/)?(\d+)/) || [])[1];
        return id ? `https://player.vimeo.com/video/${id}` : "";
      }
    } catch (_error) {
      return "";
    }
    return "";
  }
  function renderHomeServiceTrust(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.homeServiceTrustItems;
    return `<section class="home-service-trust ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.map((item) => `<blockquote>${escapeHtml(item)}</blockquote>`).join("")}</div>
  </section>`;
  }
  function renderHomeServiceQuote(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const phone = schema.contact?.phone || schema.contact?.whatsapp || labels.callNow;
    return `<section class="home-service-quote ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><h2>${escapeHtml(editable.title || labels.freeQuote)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="home-service-quote-card">
      <strong>${escapeHtml(phone)}</strong>
      <span>${escapeHtml(labels.quoteExpectation)}</span>
      <a class="rendered-button" href="#">${escapeHtml(labels.freeQuote)}</a>
    </div>
  </section>`;
  }
  function homeServiceVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "LS").slice(0, 2).toUpperCase();
    return `<div class="home-service-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderBookingHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    return `<section class="booking-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="booking-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.location || labels.appointments)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.bookNow)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.viewServices)}</a>
      </div>
    </div>
    <div class="booking-stage">
      <div class="booking-stage-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : bookingVisualPlaceholder(schema)}</div>
      <div class="booking-mini-card"><strong>${escapeHtml(labels.nextAvailable)}</strong><span>${escapeHtml(labels.availabilityItems[0])}</span><button type="button">${escapeHtml(labels.bookNow)}</button></div>
    </div>
    <div class="booking-proof-strip">${labels.bookingProofItems.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderBookingServices(section, schema) {
    const editable = section.editable || {};
    return `<section class="booking-services-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(catalogLocaleLabels(schema).services)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    ${renderBookingMenuCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderBookingAvailability(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.availabilityItems;
    return `<section class="booking-availability-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.availability)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="booking-availability-grid">${items.map((item, index) => `<article><small>${escapeHtml(labels.slot)} 0${index + 1}</small><strong>${escapeHtml(item)}</strong><span>${escapeHtml(labels.confirmation)}</span></article>`).join("")}</div>
  </section>`;
  }
  function renderBookingTeam(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.bookingTeamItems;
    return `<section class="booking-team-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.team)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="booking-team-list">${items.map((item, index) => `<article><small>0${index + 1}</small><strong>${escapeHtml(item)}</strong></article>`).join("")}</div>
  </section>`;
  }
  function renderBookingContact(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="booking-contact-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><h2>${escapeHtml(editable.title || labels.bookNow)}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="booking-contact-card"><strong>${escapeHtml(labels.confirmation)}</strong><span>${escapeHtml(labels.bookingContactFallback)}</span><a class="rendered-button" href="#">${escapeHtml(labels.bookNow)}</a></div>
  </section>`;
  }
  function bookingVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "BK").slice(0, 2).toUpperCase();
    return `<div class="booking-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderFunnelHero(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    return `<section class="funnel-hero ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="funnel-hero-copy">
      <span class="rendered-kicker">${escapeHtml(schema.business?.industry || labels.offer)}</span>
      <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
      <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
      <div class="rendered-actions">
        <a class="rendered-button" href="#">${escapeHtml(editable.primary_button || labels.claimOffer)}</a>
        <a class="rendered-button secondary" href="#">${escapeHtml(editable.secondary_button || labels.seeProof)}</a>
      </div>
    </div>
    <div class="funnel-hero-card">
      <div class="funnel-hero-visual">${image ? `<img src="${escapeAttribute(image)}" alt="">` : funnelVisualPlaceholder(schema)}</div>
      <div class="funnel-mini-form">
        <strong>${escapeHtml(labels.nextStep)}</strong>
        <span>${escapeHtml(labels.quickRequest)}</span>
        <button type="button">${escapeHtml(labels.claimOffer)}</button>
      </div>
    </div>
    <div class="funnel-proof-strip">${(labels.funnelProofItems || []).slice(0, 3).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderFunnelBenefits(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.funnelBenefitsItems;
    return `<section class="funnel-benefits-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading"><span class="rendered-kicker">${escapeHtml(labels.benefits)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div class="funnel-benefit-grid">${items.map((item, index) => `<article><small>0${index + 1}</small><strong>${escapeHtml(item)}</strong></article>`).join("")}</div>
  </section>`;
  }
  function renderFunnelOffer(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    return `<section class="funnel-offer-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(labels.offer)}</span>
      <h2>${escapeHtml(editable.title || "")}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
      ${editable.guarantee ? `<strong>${escapeHtml(editable.guarantee)}</strong>` : ""}
    </div>
    ${renderLeadFunnelOfferCatalog(marketplaceItems(schema), schema)}
  </section>`;
  }
  function renderFunnelProof(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.funnelProofItems;
    return `<section class="funnel-proof-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.proof)}</span><h2>${escapeHtml(editable.title || "")}</h2>${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}</div>
    <div>${items.map((item) => `<blockquote>${escapeHtml(item)}</blockquote>`).join("")}</div>
  </section>`;
  }
  function renderFunnelFAQ(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = Array.isArray(editable.items) && editable.items.length ? editable.items : labels.funnelFaqItems;
    return `<section class="funnel-faq-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div><span class="rendered-kicker">${escapeHtml(labels.faq)}</span><h2>${escapeHtml(editable.title || "")}</h2></div>
    <div class="funnel-faq-list">${items.map((item, index) => `<article><strong>${escapeHtml(item.question || item)}</strong><p>${escapeHtml(item.answer || labels.faqAnswer)}</p></article>`).join("")}</div>
  </section>`;
  }
  function funnelVisualPlaceholder(schema) {
    const initials = String(schema.business?.name || "LF").slice(0, 2).toUpperCase();
    return `<div class="funnel-visual-placeholder"><span>${escapeHtml(initials)}</span></div>`;
  }
  function renderMarketplaceHero(section, schema) {
    const editable = section.editable || {};
    const items = marketplaceItems(schema);
    const labels = catalogLocaleLabels(schema);
    const topItems = items.slice(0, 4);
    const categories = marketplaceCategories(schema);
    const heroProducts = topItems.map((item, index) => `<article class="marketplace-hero-product-card" ${catalogSearchAttributes(item)}>
        ${renderCatalogImage(item)}
        <small>${escapeHtml(index % 2 ? labels.fastShip : labels.deal)}</small>
        <strong>${escapeHtml(item.name)}</strong>
        <span>${escapeHtml(productPriceLabel(item, schema))}</span>
      </article>`).join("");
    const variant = section.variant || section.settings?.layout || "split_showcase";
    return `<section class="marketplace-hero marketplace-hero-${escapeAttribute(slugify(variant))} ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="marketplace-search-panel">
      <div class="marketplace-logo-row">
        <strong>${escapeHtml(schema.business?.name || "Marketplace")}</strong>
        <span>${escapeHtml(editable.deal_badge || labels.deal)}</span>
      </div>
      <form class="marketplace-search-box" data-catalog-search-form>
        <input type="search" name="catalog-search" aria-label="${escapeAttribute(labels.search)}" placeholder="${escapeAttribute(editable.search_placeholder || labels.searchPlaceholder)}">
        <button type="submit">${escapeHtml(labels.searchButton)}</button>
      </form>
      <div class="marketplace-chip-row">${categories.slice(0, 6).map((category) => `<button type="button" data-catalog-category="${escapeAttribute(category)}">${escapeHtml(category)}</button>`).join("")}</div>
    </div>
    <div class="marketplace-deal-hero marketplace-commerce-home">
      <div class="marketplace-hero-copy">
        <small>${escapeHtml(editable.deal_badge || labels.deal)}</small>
        <h1>${escapeHtml(editable.headline || schema.business?.name || "")}</h1>
        <p>${escapeHtml(editable.subtitle || schema.business?.description || "")}</p>
        <div class="marketplace-mini-stat-row">
          <span>${escapeHtml(labels.secureCheckout)}</span>
          <span>${escapeHtml(labels.fastShip)}</span>
          <span>${escapeHtml(labels.support)}</span>
        </div>
        <div class="rendered-actions">
          <a class="rendered-button" href="#" data-page-link="catalog">${escapeHtml(editable.primary_button || schema.theme?.buttons?.primary_label || labels.shopNow)}</a>
          <a class="rendered-button secondary" href="#" data-page-link="catalog">${escapeHtml(editable.secondary_button || labels.categories)}</a>
        </div>
      </div>
      <aside class="marketplace-hero-products-panel">
        <div class="marketplace-hero-products-head">
          <b>${escapeHtml(editable.deal_title || labels.dealTitle)}</b>
          <span>${escapeHtml(labels.sortBy)}: ${escapeHtml(labels.featured)}</span>
        </div>
        <div class="marketplace-hero-products">${heroProducts}</div>
      </aside>
    </div>
  </section>`;
  }
  function renderCategoryRail(section, schema) {
    const editable = section.editable || {};
    const categories = marketplaceCategories(schema);
    return `<section class="marketplace-category-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(schema.business?.tone || "")}</span>
      <h2>${escapeHtml(editable.title || catalogLocaleLabels(schema).categories)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div class="marketplace-category-rail">${categories.map((category, index) => `<article>
      <span>${escapeHtml(category.slice(0, 2).toUpperCase())}</span>
      <strong>${escapeHtml(category)}</strong>
      <small>${escapeHtml(index % 2 ? catalogLocaleLabels(schema).fastShip : catalogLocaleLabels(schema).deal)}</small>
    </article>`).join("")}</div>
  </section>`;
  }
  function renderDealRow(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const items = marketplaceItems(schema).slice(0, 6);
    return `<section class="marketplace-deal-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(labels.deal)}</span>
      <h2>${escapeHtml(editable.title || labels.dealTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div class="marketplace-deal-row catalog-count-${Math.min(items.length, 9)}">${items.map((item, index) => renderCatalogCard(item, "market-card deal-card", index % 2 ? labels.fastShip : labels.deal, schema)).join("")}</div>
  </section>`;
  }
  function renderTrustStrip(section, schema) {
    const editable = section.editable || {};
    const labels = catalogLocaleLabels(schema);
    const trust = [labels.secureCheckout, labels.fastShip, labels.support, labels.easyReturns];
    return `<section class="marketplace-trust-strip ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <h2>${escapeHtml(editable.title || labels.trustTitle)}</h2>
      ${editable.text ? `<p>${escapeHtml(editable.text)}</p>` : ""}
    </div>
    <div>${trust.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div>
  </section>`;
  }
  function renderProductGrid(section, schema) {
    const editable = section.editable || {};
    const columns = Math.max(2, Math.min(Number(section.settings?.columns || 3), 4));
    const catalogItems = (schema.catalog_items || schema.products_services || []).filter((item) => item.is_active !== false && item.display_in_catalog !== false).sort((a, b) => Number(a.sort_order || 0) - Number(b.sort_order || 0));
    const catalogType = schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || "editorial_minimal_grid";
    const customCatalog = renderCatalogByType(catalogType, catalogItems, schema);
    return `<section class="rendered-section section-${escapeAttribute(slugify(section.settings?.layout || "grid"))} ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(schema.business.tone || "Selected")}</span>
      <h2>${escapeHtml(editable.title || editable.headline || "Products and services")}</h2>
      ${editable.text || editable.subtitle ? `<p>${escapeHtml(editable.text || editable.subtitle)}</p>` : ""}
    </div>
    ${customCatalog || `<div class="rendered-grid columns-${columns}">
      ${catalogItems.map(
      (item) => `<article class="rendered-card">
            ${renderCatalogImage(item)}
            <div>
              <h3>${escapeHtml(item.name)}</h3>
              <p>${escapeHtml(item.description)}</p>
              <strong>${escapeHtml(productPriceLabel(item, schema))}</strong>
              ${productStockBadge(item)}
              <br><a class="rendered-button" href="#">${escapeHtml(item.button_label)}</a>
            </div>
          </article>`
    ).join("")}
    </div>`}
  </section>`;
  }
  function renderCatalogByType(catalogType, items, schema) {
    const renderers = {
      premium_editorial_catalog: renderPremiumEditorialCatalog,
      single_vendor_dense_catalog: renderMarketplaceCatalog,
      dense_retail_catalog: renderMarketplaceCatalog,
      dense_marketplace_catalog: renderMarketplaceCatalog,
      listing_marketplace_catalog: renderClassifiedMarketplaceCatalog,
      real_estate_listing_catalog: renderRealEstateListingCatalog,
      editorial_minimal_grid: renderMinimalProductGrid,
      lookbook_collection_catalog: renderFashionLookbookCatalog,
      luxury_gallery_catalog: renderLuxuryGalleryCatalog,
      luxury_high_ticket_catalog: renderLuxuryHighTicketCatalog,
      education_course_catalog: renderEducationCourseCatalog,
      medical_wellness_service_catalog: renderMedicalWellnessCatalog,
      legal_professional_services_catalog: renderLegalProfessionalCatalog,
      b2b_solution_catalog: renderB2BSolutionCatalog,
      industrial_supplier_catalog: renderIndustrialSupplierCatalog,
      digital_offer_catalog: renderDigitalOfferCatalog,
      restaurant_menu_catalog: renderRestaurantMenuCatalog,
      menu_catalog: renderRestaurantMenuCatalog,
      online_ordering_catalog: renderRestaurantMenuCatalog,
      booking_menu_catalog: renderBookingMenuCatalog,
      service_area_catalog: renderLocalServiceCatalog,
      company_services_catalog: renderCorporateServicesCatalog,
      home_services_quote_catalog: renderHomeServicesCatalog,
      lead_funnel_offer_catalog: renderLeadFunnelOfferCatalog,
      practice_area_catalog: renderProfessionalServicesCatalog,
      project_gallery_catalog: renderBeforeAfterProjectCatalog,
      pricing_plan_catalog: renderPricingPlanCatalog,
      ticket_or_offer_catalog: renderEventTicketCatalog,
      service_package_catalog: renderPersonalBrandServicesCatalog
    };
    return (renderers[catalogType] || renderMinimalProductGrid)(items, schema, catalogType);
  }
  function renderMarketplaceCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    const groups = groupCatalogItemsByCategory(items, labels);
    return `<div class="catalog-shell catalog-marketplace">
    <aside>
      <strong>${labels.searchFilters}</strong>
      ${groups.map(([category]) => `<a href="#catalog-category-${escapeAttribute(slugify(category))}">${escapeHtml(category)}</a>`).join("")}
      <span>${labels.price}</span><span>${labels.rating}</span><span>${labels.delivery}</span>
    </aside>
    <div class="marketplace-catalog-main">
      <div class="marketplace-sort-bar"><b>${escapeHtml(labels.results)}</b><span>${escapeHtml(labels.sortBy)}: ${escapeHtml(labels.featured)}</span></div>
      <div class="marketplace-category-groups">${groups.map(([category, categoryItems]) => `<section id="catalog-category-${escapeAttribute(slugify(category))}" class="marketplace-category-group" data-catalog-group><div class="marketplace-category-heading"><h3>${escapeHtml(category)}</h3><span>${categoryItems.length}</span></div><div class="catalog-results">${categoryItems.map((item, index) => renderCatalogCard(item, "market-card", `${index % 3 === 0 ? labels.deal : labels.fastShip}`, schema)).join("")}</div></section>`).join("")}</div>
    </div>
  </div>${renderMarketplaceSubscribe(schema)}`;
  }
  function groupCatalogItemsByCategory(items, labels) {
    const groups = /* @__PURE__ */ new Map();
    arrayValue(items).forEach((item) => {
      const category = String(item.category || labels.featured || "Featured").trim();
      if (!groups.has(category)) groups.set(category, []);
      groups.get(category).push(item);
    });
    return [...groups.entries()];
  }
  function renderMarketplaceSubscribe(schema) {
    const copy = marketplaceSubscribeCopy(schema);
    return `<section class="marketplace-subscribe">
    <div>
      <span>${escapeHtml(copy.kicker)}</span>
      <h2>${escapeHtml(copy.title)}</h2>
      <p>${escapeHtml(copy.text)}</p>
    </div>
    <form>
      <input type="email" placeholder="${escapeAttribute(copy.placeholder)}">
      <button class="rendered-button" data-open-lead type="button">${escapeHtml(copy.button)}</button>
    </form>
  </section>`;
  }
  function marketplaceSubscribeCopy(schema) {
    const language = schema?.business?.selectedLanguage || schema?.business?.selected_language || schema?.selectedLanguage || schema?.selected_language || "en";
    const copies = {
      en: {
        kicker: "Subscriber deals",
        title: "Get drops, offers and rare finds first",
        text: "Customers can subscribe for new arrivals, limited deals and category updates.",
        placeholder: "Email address",
        button: "Subscribe"
      },
      es: {
        kicker: "Ofertas para suscriptores",
        title: "Recibe drops, ofertas y hallazgos primero",
        text: "Los clientes pueden suscribirse para novedades, ofertas limitadas y actualizaciones por categoria.",
        placeholder: "Correo electronico",
        button: "Suscribirme"
      },
      fr: {
        kicker: "Offres abonnees",
        title: "Recevez les nouveaut\xE9s et offres en premier",
        text: "Les clients peuvent recevoir nouveaut\xE9s, offres limit\xE9es et mises \xE0 jour par cat\xE9gorie.",
        placeholder: "Adresse email",
        button: "S'abonner"
      },
      pt: {
        kicker: "Ofertas para assinantes",
        title: "Receba novidades, ofertas e achados primeiro",
        text: "Clientes podem receber novidades, ofertas limitadas e atualizacoes por categoria.",
        placeholder: "Email",
        button: "Assinar"
      }
    };
    return copies[language] || copies.en;
  }
  function renderPremiumEditorialCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-premium-editorial">
    ${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
      <div class="premium-card-visual">${renderCatalogImage(item)}</div>
      <div>
        <small>${escapeHtml(index === 0 ? labels.flagship : labels.curated)}</small>
        <h3>${escapeHtml(item.name)}</h3>
        <p>${escapeHtml(item.description)}</p>
        <strong>${escapeHtml(productPriceLabel(item, schema))}</strong>
        <a class="rendered-button secondary" href="#">${escapeHtml(item.button_label || labels.view)}</a>
      </div>
    </article>`).join("")}
  </div>`;
  }
  function renderClassifiedMarketplaceCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-shell catalog-classified">
    ${items.map((item, index) => `<article class="listing-card">
      <div>${renderCatalogImage(item)}</div>
      <section><strong>${escapeHtml(item.name)}</strong><p>${escapeHtml(item.description)}</p><small>${labels.sellerVerified} \xB7 ${index % 2 ? labels.used : labels.newItem} \xB7 ${labels.localPickup}</small></section>
      <aside><b>${escapeHtml(item.price_label || labels.makeOffer)}</b><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.contactSeller)}</a></aside>
    </article>`).join("")}
  </div>`;
  }
  function renderMinimalProductGrid(items) {
    return `<div class="catalog-minimal">${items.map((item) => renderCatalogCard(item, "minimal-card", item.price_label, arguments[1])).join("")}</div>`;
  }
  function renderFashionLookbookCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-lookbook">${items.map((item, index) => `<article class="lookbook-card ${index === 0 ? "wide" : ""}">
    ${renderCatalogImage(item)}
    <span>${labels.newDrop}</span><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><b>${escapeHtml(item.price_label)}</b>
  </article>`).join("")}</div>`;
  }
  function renderCorporateServicesCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-corporate-services">${items.map((item, index) => `<article>
    <small>${escapeHtml(item.category || `${labels.capability} 0${index + 1}`)}</small>
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <a class="rendered-button secondary" href="#">${escapeHtml(item.button_label || labels.requestConsultation)}</a>
  </article>`).join("")}</div>`;
  }
  function renderHomeServicesCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-home-services">${items.map((item, index) => `<article>
    <small>${escapeHtml(item.category || `${labels.service} 0${index + 1}`)}</small>
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <div><span>${escapeHtml(labels.serviceAreaReady)}</span><span>${escapeHtml(labels.quoteOnly)}</span></div>
    <a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.freeQuote)}</a>
  </article>`).join("")}</div>`;
  }
  function renderLeadFunnelOfferCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-lead-offers">${items.slice(0, 3).map((item, index) => `<article class="${index === 1 ? "featured" : ""}">
    <small>${escapeHtml(index === 1 ? labels.bestValue : labels.offer)}</small>
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <ul>
      <li>${escapeHtml(labels.outcomeFocused)}</li>
      <li>${escapeHtml(labels.fastNextStep)}</li>
      <li>${escapeHtml(labels.editableOffer)}</li>
    </ul>
    <a class="rendered-button ${index === 1 ? "" : "secondary"}" href="#">${escapeHtml(item.button_label || labels.claimOffer)}</a>
  </article>`).join("")}</div>`;
  }
  function renderLuxuryGalleryCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-luxury">${items.map((item) => `<article>
    <div>${renderCatalogImage(item)}</div>
    <small>${labels.limitedSelection}</small><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><b>${escapeHtml(item.price_label)}</b>
  </article>`).join("")}</div>`;
  }
  function renderLuxuryHighTicketCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-luxury-high-ticket">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="luxury-card-top">${renderCatalogImage(item)}</div>
    <div class="luxury-card-bottom">
      <small>${escapeHtml(item.deal_label || (index % 2 ? labels.authenticated : labels.limitedPiece))}</small>
      <h3>${escapeHtml(item.name)}</h3>
      <p>${escapeHtml(item.description)}</p>
      <div><span>${escapeHtml(item.category || labels.collection)}</span><strong>${escapeHtml(productPriceLabel(item, schema) || labels.priceOnRequest)}</strong></div>
      <a class="rendered-button secondary" href="#">${escapeHtml(item.button_label || labels.requestPrivateViewing)}</a>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderEducationCourseCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-education-course">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="education-card-top">
      <small>${escapeHtml(item.deal_label || (index % 2 ? labels.beginnerFriendly : labels.certificateReady))}</small>
      <span>${escapeHtml(item.shipping_label || labels.educationDurations?.[index % (labels.educationDurations?.length || 1)] || "")}</span>
    </div>
    <div class="education-card-image">${renderCatalogImage(item)}</div>
    <div class="education-card-body">
      <small>${escapeHtml(item.category || labels.courseAcademy)}</small>
      <h3>${escapeHtml(item.name)}</h3>
      <p>${escapeHtml(item.description)}</p>
      <ul>
        <li>${escapeHtml(labels.structuredModules)}</li>
        <li>${escapeHtml(labels.practicalOutcome)}</li>
        <li>${escapeHtml(labels.flexibleAccess)}</li>
      </ul>
      <div><strong>${escapeHtml(productPriceLabel(item, schema) || labels.coursePrice)}</strong><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.enrollNow)}</a></div>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderMedicalWellnessCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-medical-wellness">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="clinic-card-top">
      <small>${escapeHtml(item.deal_label || (index % 2 ? labels.specialistLed : labels.popularTreatment))}</small>
      <span>${escapeHtml(item.shipping_label || labels.clinicDurations?.[index % (labels.clinicDurations?.length || 1)] || "")}</span>
    </div>
    <div class="clinic-card-visual">${renderCatalogImage(item)}</div>
    <div class="clinic-card-body">
      <small>${escapeHtml(item.category || labels.treatments)}</small>
      <h3>${escapeHtml(item.name)}</h3>
      <p>${escapeHtml(item.description)}</p>
      <ul>
        <li>${escapeHtml(labels.specialistLed)}</li>
        <li>${escapeHtml(labels.personalizedPlan)}</li>
        <li>${escapeHtml(labels.clinicCare)}</li>
      </ul>
      <div><strong>${escapeHtml(productPriceLabel(item, schema) || labels.consultationBased)}</strong><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.bookConsultation)}</a></div>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderLegalProfessionalCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-legal-professional">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="professional-card-top">
      <small>${escapeHtml(item.deal_label || (index % 2 ? labels.seniorAdvisor : labels.confidential))}</small>
      <span>${escapeHtml(item.shipping_label || labels.professionalEngagements?.[index % (labels.professionalEngagements?.length || 1)] || "")}</span>
    </div>
    <div class="professional-card-body">
      <small>${escapeHtml(item.category || labels.professionalFirm)}</small>
      <h3>${escapeHtml(item.name)}</h3>
      <p>${escapeHtml(item.description)}</p>
      <ul>
        <li>${escapeHtml(labels.caseReview)}</li>
        <li>${escapeHtml(labels.confidential)}</li>
        <li>${escapeHtml(labels.businessReady)}</li>
      </ul>
      <div><strong>${escapeHtml(productPriceLabel(item, schema) || labels.consultationBased)}</strong><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.scheduleConsultation)}</a></div>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderB2BSolutionCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-b2b-solutions">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="b2b-card-top"><small>${escapeHtml(item.deal_label || (index % 2 ? labels.integrationReady : labels.enterpriseReady))}</small><span>${escapeHtml(item.shipping_label || labels.enterpriseTimelines?.[index % (labels.enterpriseTimelines?.length || 1)] || "")}</span></div>
    <div class="b2b-card-body"><small>${escapeHtml(item.category || labels.solutions)}</small><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><ul><li>${escapeHtml(labels.roiFocused)}</li><li>${escapeHtml(labels.integrationReady)}</li><li>${escapeHtml(labels.enterpriseReady)}</li></ul><div><strong>${escapeHtml(productPriceLabel(item, schema) || labels.customPlan)}</strong><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.requestDemo)}</a></div></div>
  </article>`).join("")}</div>`;
  }
  function renderIndustrialSupplierCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-industrial-supplier">
    <div class="industrial-table-head"><span>SKU</span><span>${escapeHtml(labels.products || "Products")}</span><span>MOQ</span><span>${escapeHtml(labels.delivery)}</span><span>Material</span><span></span></div>
    ${items.map((item, index) => {
      const specs = item.specs || {};
      return `<article class="${index === 0 ? "featured" : ""}">
      <b>${escapeHtml(specs.sku || item.sku || `IND-${String(index + 1).padStart(3, "0")}`)}</b>
      <div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.category || labels.industrialSupplier)} \xB7 ${escapeHtml(item.deal_label || labels.certified)}</small><p>${escapeHtml(item.description)}</p></div>
      <span>${escapeHtml(specs.moq || (index % 2 ? "100+" : "25+"))}</span>
      <span>${escapeHtml(specs.lead_time || item.shipping_label || labels.industrialLeadTimes?.[index % (labels.industrialLeadTimes?.length || 1)] || "")}</span>
      <span>${escapeHtml(specs.material || labels.industrialMaterials?.[index % (labels.industrialMaterials?.length || 1)] || labels.specReady)}</span>
      <a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.requestQuote)}</a>
    </article>`;
    }).join("")}</div>`;
  }
  function renderDigitalOfferCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-digital-pro catalog-count-${Math.min(items.length, 9)}">${items.map((item) => `<article ${catalogSearchAttributes(item)}>
    <div class="digital-card-top">
      <small>${escapeHtml(item.category || labels.digitalProducts)}</small>
      <span>${escapeHtml(labels.instantAccess)}</span>
    </div>
    ${renderCatalogImage(item)}
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <ul><li>${escapeHtml(labels.downloadable)}</li><li>${escapeHtml(labels.bonus)}</li><li>${escapeHtml(labels.lifetime)}</li></ul>
    <div class="digital-card-bottom">
      <strong>${escapeHtml(productPriceLabel(item, schema))}</strong>
      <a class="rendered-button" href="#" data-page-link="contact">${escapeHtml(item.button_label || labels.getAccess)}</a>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderRestaurantMenuCatalog(items, schema) {
    const labels = catalogLocaleLabels(schema);
    return `<div class="catalog-restaurant-menu">${items.map((item, index) => `<article class="${index === 0 ? "featured" : ""}">
    <div class="restaurant-menu-card-top">
      <small>${escapeHtml(item.category || (index % 2 ? labels.chefPick : labels.popularDish))}</small>
      <span>${escapeHtml(index === 0 ? labels.signatureMenu : labels.menu)}</span>
    </div>
    ${renderCatalogImage(item)}
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <div class="restaurant-menu-card-bottom">
      <strong>${escapeHtml(productPriceLabel(item, schema) || labels.menuPrice)}</strong>
      <a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.orderNow)}</a>
    </div>
  </article>`).join("")}</div>`;
  }
  function renderBookingMenuCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-booking-pro">${items.map((item, index) => `<article class="${index === 1 ? "featured" : ""}">
    <small>${30 + index * 15} min</small>
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    <div><span>${escapeHtml(labels.staffPick)}</span><span>${escapeHtml(labels.confirmation)}</span></div>
    <b>${escapeHtml(item.price_label || labels.fromQuote)}</b><a class="rendered-button" href="#">${escapeHtml(item.button_label || labels.bookNow)}</a>
  </article>`).join("")}</div>`;
  }
  function renderLocalServiceCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-services">${items.map((item) => renderCatalogCard(item, "service-card-pro", labels.freeQuote, arguments[1])).join("")}</div>`;
  }
  function renderProfessionalServicesCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-practice">${items.map((item) => `<article><span>${labels.practiceArea}</span><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><a class="rendered-button secondary" href="#">${labels.scheduleConsultation}</a></article>`).join("")}</div>`;
  }
  function renderBeforeAfterProjectCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-projects">${items.map((item) => `<article><div><span>${labels.before}</span><span>${labels.after}</span></div><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><a class="rendered-button" href="#">${labels.viewProject}</a></article>`).join("")}</div>`;
  }
  function renderPricingPlanCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-pricing">${items.map((item, index) => `<article class="${index === 1 ? "featured" : ""}"><small>${labels.plan}</small><h3>${escapeHtml(item.name)}</h3><b>${escapeHtml(item.price_label || labels.custom)}</b><p>${escapeHtml(item.description)}</p><a class="rendered-button" href="#">${labels.start}</a></article>`).join("")}</div>`;
  }
  function renderEventTicketCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-tickets">${items.map((item) => `<article><span>${labels.ticketOffer}</span><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><b>${escapeHtml(item.price_label)}</b><a class="rendered-button" href="#">${labels.reserve}</a></article>`).join("")}</div>`;
  }
  function renderPersonalBrandServicesCatalog(items) {
    const labels = catalogLocaleLabels(arguments[1]);
    return `<div class="catalog-packages">${items.map((item) => `<article><small>${labels.package}</small><h3>${escapeHtml(item.name)}</h3><p>${escapeHtml(item.description)}</p><a class="rendered-button secondary" href="#">${labels.applyNow}</a></article>`).join("")}</div>`;
  }
  function renderCatalogCard(item, className, badge, schema) {
    const labels = catalogLocaleLabels(schema);
    const commerce = commerceLabels(schema);
    const isMarket = String(className || "").includes("market-card");
    return `<article class="${className}" ${catalogSearchAttributes(item)}>
    ${renderCatalogImage(item)}
    ${badge ? `<small>${escapeHtml(badge)}</small>` : ""}
    ${item.category ? `<small>${escapeHtml(item.category)}</small>` : ""}
    <h3>${escapeHtml(item.name)}</h3>
    <p>${escapeHtml(item.description)}</p>
    ${isMarket ? `<div class="market-meta"><span>${"\u2605".repeat(Math.max(1, Math.min(5, Math.round(Number(item.rating) || 4))))} ${escapeHtml(item.rating || "4.6")}</span><span>${escapeHtml(item.shipping_label || labels.fastShip)}</span></div>` : ""}
    <b>${escapeHtml(productPriceLabel(item, schema))}</b>
    ${productStockBadge(item)}
    <button class="rendered-button" type="button">${escapeHtml(isMarket ? commerce.addToCart : item.button_label || labels.view)}</button>
  </article>`;
  }
  function renderCatalogImage(item = {}) {
    return `${renderResilientImage(item.image_url || item.imageUrl, item.name || item.title, item.name || item.title)}${renderImageAttribution(item)}`;
  }
  function renderImageAttribution(item = {}) {
    const asset = item.image_asset || item.imageAsset || {};
    if (asset.source !== "unsplash_api") return "";
    const photographer = asset.photographer_name || "Unsplash contributor";
    const profileUrl = asset.photographer_profile_url || "https://unsplash.com";
    return `<a class="image-attribution" href="${escapeAttribute(profileUrl)}" target="_blank" rel="noopener noreferrer">Photo: ${escapeHtml(photographer)} / Unsplash</a>`;
  }
  function productPriceLabel(item = {}, schema = {}) {
    if (item.price_label && !/precio editable|price editable/i.test(item.price_label)) return item.price_label;
    const amount = item.price_amount ?? item.price_value;
    if (amount !== "" && amount !== null && amount !== void 0 && !Number.isNaN(Number(amount))) {
      return `${item.currency || "USD"} ${Number(amount).toLocaleString(void 0, { maximumFractionDigits: 2 })}`;
    }
    return item.price_type === "quote_only" ? catalogLocaleLabels(schema).request : "Precio editable";
  }
  function productStockBadge(item = {}) {
    if (!item.track_inventory) return "";
    const quantity = Number(item.inventory_quantity);
    if (Number.isNaN(quantity) || item.inventory_quantity === "") return `<span class="stock-badge stock-unknown">Stock editable</span>`;
    if (quantity <= 0) return `<span class="stock-badge stock-out">Out of stock</span>`;
    if (quantity <= 3) return `<span class="stock-badge stock-low">Only ${quantity} left</span>`;
    return `<span class="stock-badge stock-in">In stock</span>`;
  }
  function stableCatalogImageUrl(seed = "") {
    const text = String(seed || "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "");
    const fallbacks = [
      [/bracelet|pulsera|pearl|perla|charm/, "https://images.unsplash.com/photo-1611591437281-460bfbe1220a?auto=format&fit=crop&w=900&q=82"],
      [/earring|arete/, "https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?auto=format&fit=crop&w=900&q=82"],
      [/ring|anillo/, "https://images.unsplash.com/photo-1605100804763-247f67b3557e?auto=format&fit=crop&w=900&q=82"],
      [/necklace|collar|jewel|joya|bisuter|crystal|cristal|gift|regalo/, "https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?auto=format&fit=crop&w=900&q=82"],
      [/streetwear|jacket|chaqueta|fashion|moda|ropa|sneaker|denim|cap/, "https://images.unsplash.com/photo-1523398002811-999ca8dec234?auto=format&fit=crop&w=900&q=82"],
      [/usb|phone|gadget|tech|keyboard|lamp|projector|electronics/, "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=900&q=82"],
      [/truck|bumper|4x4|off-road|auto|car|automotive/, "https://images.unsplash.com/photo-1503376780353-7e6692767b70?auto=format&fit=crop&w=900&q=82"],
      [/coffee|espresso|brew|latte|cafe/, "https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=900&q=82"],
      [/restaurant|food|menu|pizza|dish|comida/, "https://images.unsplash.com/photo-1504674900247-0877df9cc836?auto=format&fit=crop&w=900&q=82"],
      [/spa-bath-towel|bath-towel|toalla|towel/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/handmade-soap|soap-bar|jabon|jabón|jabones|soap/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/bath-salts|sales-de-bano|sales de bano|sales de baño/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/bath-sponge|bano-sponge|natural-bath-sponge|esponja/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/body-oil|aceite-corporal|body oil/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/bath-bomb|bath bomb|bombas-de-bano|bombas de bano|bombas de baño/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/aromatic-candle|scented-candle|candle|candles|vela|velas/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/soap|jabon|jabón|jabones|bath|bath-bomb|bath bomb|bombas-de-bano|bombas de bano|bombas de baño|body-care|body care|candle|candles|vela|velas|beauty|skincare|cosmetic|belleza|makeup|spa/, "https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=82"],
      [/home|decor|furniture|mueble|hogar/, "https://images.unsplash.com/photo-1484154218962-a197022b5858?auto=format&fit=crop&w=900&q=82"]
    ];
    return (fallbacks.find(([pattern]) => pattern.test(text)) || [null, "https://images.unsplash.com/photo-1472851294608-062f824d29cc?auto=format&fit=crop&w=900&q=82"])[1];
  }
  function resolveCatalogImageUrl(url, fallbackText = "") {
    const raw = String(url || "").trim();
    if (!raw) return stableCatalogImageUrl(fallbackText);
    if (/images\.unsplash\.com\/featured\/600x600|source\.unsplash\.com|photo-1523275335684-37898b6baf30/i.test(raw)) {
      const query = raw.split("?").slice(1).join("?").replace(/[=&]/g, " ");
      return stableCatalogImageUrl(`${query} ${fallbackText}`);
    }
    return raw;
  }
  function renderResilientImage(url, alt = "", fallbackText = "") {
    const initials = String(fallbackText || alt || "Item").slice(0, 2).toUpperCase();
    const placeholder = `<div class="card-placeholder">${escapeHtml(initials)}</div>`;
    const resolvedUrl = resolveCatalogImageUrl(url, `${fallbackText} ${alt}`);
    if (!resolvedUrl) return placeholder;
    return `<div class="image-shell"><img src="${escapeAttribute(resolvedUrl)}" alt="${escapeAttribute(alt)}" loading="lazy" decoding="async" onerror="this.closest('.image-shell').classList.add('image-failed')">${placeholder}</div>`;
  }
  function catalogLocaleLabels(schema = {}) {
    const language = schema?.business?.selectedLanguage || schema?.business?.selected_language || schema?.selectedLanguage || schema?.selected_language || "en";
    const labels = {
      en: {
        searchFilters: "Search & filters",
        category: "Category",
        brand: "Brand",
        price: "Price",
        rating: "Rating",
        delivery: "Delivery",
        deal: "Deal",
        fastShip: "Fast ship",
        search: "Search",
        searchPlaceholder: "Search products, brands, or categories",
        searchButton: "Search",
        shopNow: "Shop now",
        categories: "Categories",
        dealTitle: "Top picks",
        dealText: "Featured products, deals, and fast shipping options.",
        results: "Results",
        sortBy: "Sort by",
        featured: "Featured",
        secureCheckout: "Secure checkout",
        support: "Support",
        easyReturns: "Easy returns",
        trustTitle: "Marketplace trust",
        signature: "Signature",
        detail: "Detail",
        curated: "Curated",
        flagship: "Flagship",
        premiumSpecs: ["Presentation", "Quality", "Support", "Delivery"],
        sellerVerified: "Seller verified",
        used: "Used",
        newItem: "New",
        localPickup: "Local pickup",
        makeOffer: "Make offer",
        contactSeller: "Contact seller",
        listings: "Listings",
        areas: "Areas",
        searchListings: "Search listings",
        viewListings: "View listings",
        inquireNow: "Inquire now",
        featuredListing: "Featured listing",
        newListing: "New listing",
        availableNow: "Available now",
        listingPrice: "Price on request",
        listingSearchPlaceholder: "Search by location, type, price or keyword",
        listingFiltersTitle: "Search with the right filters",
        listingFiltersText: "Help customers narrow options by category, location, price and availability.",
        featuredListingsTitle: "Featured listings",
        featuredListingsText: "Active listings with price, location, specs and inquiry CTAs.",
        listingAreaTitle: "Explore the best areas",
        listingAreaText: "Area cards and location notes make discovery feel local.",
        listingTrustTitle: "Confidence before the inquiry",
        listingTrustText: "Verified details, contact paths and updated availability.",
        listingContactTitle: "Ask about a listing",
        listingContactText: "Send listing, budget, location and preferred contact method.",
        listingCategories: ["Homes", "Rentals", "Commercial", "Land", "Cars", "Featured"],
        listingLocations: ["Downtown", "North area", "West district", "Near schools", "Waterfront", "Business zone"],
        listingTrustItems: ["Verified details", "Updated availability", "Clear pricing", "Local support", "Fast response", "Easy comparison"],
        newDrop: "New drop",
        limitedSelection: "Limited selection",
        collection: "Collection",
        provenance: "Provenance",
        privateInquiry: "Private inquiry",
        requestPrivateViewing: "Request private viewing",
        viewCollection: "View collection",
        priceOnRequest: "Price on request",
        limitedPiece: "Limited piece",
        authenticated: "Authenticated",
        privateService: "Private service",
        signaturePiece: "Signature piece",
        luxuryProvenanceTitle: "Provenance, authenticity and service",
        luxuryProvenanceText: "Confidence signals for high-ticket buyers before they inquire.",
        luxuryPrivateTitle: "Private appointment",
        luxuryPrivateText: "Request availability, preferred piece and contact method.",
        luxuryContactTitle: "Request a private consultation",
        luxuryContactText: "Send the piece, occasion, preferred timing and contact method.",
        luxuryProofItems: ["Verified provenance", "Limited availability", "Private advisor", "Insured handling", "Secure inquiry", "Concierge follow-up"],
        courseAcademy: "Course academy",
        programs: "Programs",
        curriculum: "Curriculum",
        enroll: "Enroll",
        enrollNow: "Enroll now",
        viewCurriculum: "View curriculum",
        coursePrice: "Enrollment price",
        featuredProgram: "Featured program",
        beginnerFriendly: "Beginner friendly",
        certificateReady: "Certificate-ready",
        outcomes: "Outcomes",
        instructor: "Instructor",
        outcomesTitle: "What students should be able to do",
        outcomesText: "Clear results, skills and confidence markers.",
        instructorTitle: "Expert guidance",
        instructorText: "Instructor credibility, method and support.",
        learningPathItems: ["Foundation", "Practice", "Apply", "Feedback", "Complete", "Next step"],
        learningOutcomeItems: ["Clear skill progression", "Practical assignments", "Editable curriculum", "Student-ready proof", "Support notes", "Launch-ready offer"],
        instructorTrustItems: ["Expert-led lessons", "Structured modules", "Support path", "Student outcomes"],
        educationDurations: ["4 weeks", "6 modules", "Live cohort", "Self-paced"],
        clinicCare: "Personalized care",
        treatments: "Treatments",
        bookConsultation: "Book consultation",
        viewTreatments: "View treatments",
        consultationBased: "Consultation-based",
        popularTreatment: "Popular treatment",
        specialistLed: "Specialist-led",
        personalizedPlan: "Personalized plan",
        treatmentsTitle: "Treatments and services",
        carePathTitle: "Clear care path",
        carePathText: "Consultation, plan, treatment and follow-up in one easy path.",
        carePathItems: ["Consultation", "Personal plan", "Treatment", "Progress check", "Aftercare", "Next visit"],
        clinicTrustTitle: "Trust and safety first",
        clinicTrustText: "Credentials, expectations and response details before booking.",
        clinicTrustItems: ["Specialist guidance", "Safety-first process", "Clear expectations", "Private consultation", "Aftercare notes", "Fast response"],
        resultsTitle: "Results with realistic expectations",
        resultsText: "Proof, transformation notes and follow-up details.",
        resultItems: ["Visible progress", "Before/after proof", "Personal plan", "Follow-up support"],
        clinicTeamTitle: "Specialists who guide the process",
        clinicTeamText: "Clinical standards, provider credibility and care philosophy.",
        clinicTeamItems: ["Credentialed team", "Consultation-first", "Patient education", "Care follow-up"],
        clinicBookingTitle: "Book a consultation or ask a question",
        clinicBookingText: "Send the preferred treatment, concern, schedule and contact method.",
        clinicDurations: ["30 min consult", "45 min session", "Personal plan", "Follow-up ready"],
        structuredModules: "Structured modules",
        practicalOutcome: "Practical outcome",
        flexibleAccess: "Flexible access",
        instantAccess: "Instant access",
        downloadable: "Downloadable content",
        bonus: "Bonus resources",
        lifetime: "Lifetime access",
        getAccess: "Get access",
        digitalProducts: "Digital products",
        viewProducts: "View products",
        modules: "Modules",
        digitalAccessShort: "Downloads, modules and support notes.",
        digitalBundleTitle: "Digital offers built to sell",
        digitalModulesTitle: "What customers get inside",
        digitalProofTitle: "Trust before checkout",
        digitalAccessTitle: "Get access and start immediately",
        digitalAccessText: "Customers know exactly what they receive, how access works and where to get support.",
        digitalModuleItems: ["Core training", "Downloadable resources", "Templates and tools", "Bonus material", "Access instructions", "Support notes"],
        digitalProofItems: ["Instant access", "Editable modules", "Clear license", "Support-ready", "Bundle value", "Simple checkout"],
        collections: "Collections",
        lookbook: "Lookbook",
        fit: "Fit guide",
        drop: "Drop",
        fitGuide: "Fit guide",
        fitGuideItems: ["Size and fit notes", "Styling suggestions", "Care details", "Shipping and returns"],
        fashionCollections: ["New arrivals", "Essentials", "Statement pieces", "Accessories", "Limited drop", "Best sellers"],
        jewelryCategories: ["Necklaces", "Bracelets", "Earrings", "Rings", "Gift sets", "Custom pieces"],
        company: "Company",
        services: "Services",
        process: "Process",
        proof: "Proof",
        capability: "Capability",
        requestConsultation: "Request consultation",
        viewServices: "View services",
        corporateProcessItems: ["Discovery", "Strategy", "Delivery", "Support"],
        corporateProofItems: ["Reliable delivery", "Clear communication", "Professional standards"],
        localExperts: "Local experts",
        callNow: "Call now",
        fastResponse: "Fast local response",
        serviceAreas: "Service areas",
        workProof: "Work proof",
        workSlug: "work",
        service: "Service",
        quoteOnly: "Quote only",
        serviceAreaReady: "Area-ready",
        quoteExpectation: "Tell us what you need and get a clear next step.",
        homeServiceHeadline: (name) => `${name} handles the job right the first time`,
        homeServiceSubheadline: (description) => description || "Trusted local service with clear communication, reliable scheduling, and quote-first service.",
        homeServiceCategoriesTitle: "Services built for real local needs",
        homeServiceCategoriesText: "Organize every service into clear quote-ready options that customers can understand quickly.",
        homeServiceAreasTitle: "Serving the areas that matter",
        homeServiceAreasText: "Show cities, neighborhoods, response expectations and emergency availability in one clean section.",
        serviceAreaItems: ["Nearby neighborhoods", "Same-day options", "Emergency calls", "Recurring service", "Licensed work", "Clear estimates"],
        beforeAfterTitle: "Real work, visible results",
        beforeAfterText: "Use job photos, before-and-after proof and finished projects to build trust before the first call.",
        homeServiceTrustTitle: "Why customers call first",
        homeServiceTrustText: "Trust signals, reviews and response expectations make the next step feel safe.",
        homeServiceTrustItems: ["Licensed and insured", "Clear estimates", "On-time arrivals", "Before/after proof", "Local references", "Clean follow-up"],
        homeServiceQuoteTitle: "Request a clear quote",
        homeServiceQuoteText: "Share the job details, preferred time and location. The business can respond by phone, WhatsApp or email.",
        appointments: "Appointments",
        availability: "Availability",
        team: "Team",
        teamSlug: "team",
        bookNow: "Book now",
        nextAvailable: "Next available",
        slot: "Slot",
        confirmation: "Confirmation-ready",
        staffPick: "Staff pick",
        bookingContactFallback: "Confirm the preferred service, time and contact method.",
        bookingHeadline: (name) => `Book ${name} without the back-and-forth`,
        bookingSubheadline: (description) => description || "A clear appointment page with services, availability, staff notes and a simple booking path.",
        bookingServicesTitle: "Choose the right appointment",
        bookingServicesText: "Show duration, service details and what the client should expect before booking.",
        availabilityTitle: "Availability that feels simple",
        availabilityText: "Make open windows, preparation notes and confirmation expectations clear.",
        availabilityItems: ["Today / tomorrow windows", "Morning appointments", "Afternoon appointments", "Weekend options", "Consultation calls", "Follow-up visits"],
        bookingTeamTitle: "A smoother visit from start to finish",
        bookingTeamText: "Explain the staff, process and preparation so clients know what happens next.",
        bookingTeamItems: ["Pick a service", "Choose a preferred time", "Receive confirmation", "Arrive prepared"],
        bookingProofItems: ["Clear durations", "Easy confirmation", "Service details upfront"],
        bookingContactTitle: "Reserve the next available time",
        bookingContactText: "Send the preferred service, day and contact method. The business can confirm by phone, WhatsApp or email.",
        menu: "Menu",
        specials: "Specials",
        hoursLocation: "Hours & location",
        orderNow: "Order now",
        viewMenu: "View menu",
        contact: "Contact",
        signatureMenu: "Signature menu",
        popularDish: "Popular dish",
        chefPick: "Chef pick",
        pickupDelivery: "Pickup / delivery",
        quickOrderNote: "Confirm by phone, WhatsApp or email.",
        editableMenuNote: "Editable from the catalog manager.",
        restaurantProofItems: ["Editable menu", "Featured combos", "Fast contact"],
        restaurantCategories: ["Starters", "Mains", "Drinks", "Desserts", "Specials", "Chef picks"],
        restaurantCategoriesTitle: "Explore the menu by category",
        restaurantSignatureTitle: "Signature dishes ready to order",
        restaurantSpecialsTitle: "Specials and combos",
        restaurantInfoTitle: "Hours, location and service options",
        restaurantOrderTitle: "Place an order or ask a question",
        restaurantOrderText: "Send the preferred dishes, pickup or delivery option, and contact method.",
        offer: "Offer",
        benefits: "Benefits",
        faq: "FAQ",
        claimOffer: "Start now",
        seeProof: "See proof",
        nextStep: "Next step",
        quickRequest: "Send a quick request and get a clear response.",
        bestValue: "Best value",
        outcomeFocused: "Outcome-focused",
        fastNextStep: "Fast next step",
        editableOffer: "Editable offer",
        faqAnswer: "This can be adjusted to match the business, offer, and customer objections.",
        funnelBenefitsItems: ["Clear promise", "Simple next step", "Qualified leads", "Editable sections", "Proof-first structure", "Fast launch"],
        funnelProofItems: ["Specific outcome", "Simple process", "Fast response", "Clear pricing conversation"],
        funnelFaqItems: [{ question: "What happens next?", answer: "The business follows up with the next step." }, { question: "Can it be customized?", answer: "Yes, the offer and page content are editable." }, { question: "Is this a store?", answer: "No, it is focused on leads." }],
        main: "Main",
        popular: "Popular",
        marketPrice: "Market price",
        fromQuote: "From quote",
        book: "Book",
        freeQuote: "Free quote",
        practiceArea: "Practice area",
        scheduleConsultation: "Schedule consultation",
        before: "Before",
        after: "After",
        viewProject: "View project",
        plan: "Plan",
        custom: "Custom",
        start: "Start",
        ticketOffer: "Ticket / offer",
        reserve: "Reserve",
        package: "Package",
        applyNow: "Apply now",
        view: "View",
        request: "Ask now"
      },
      es: {
        searchFilters: "Busqueda y filtros",
        category: "Categoria",
        brand: "Marca",
        price: "Precio",
        rating: "Calificacion",
        delivery: "Entrega",
        deal: "Oferta",
        fastShip: "Envio rapido",
        search: "Buscar",
        searchPlaceholder: "Buscar productos, marcas o categorias",
        searchButton: "Buscar",
        shopNow: "Comprar ahora",
        categories: "Categorias",
        dealTitle: "Productos destacados",
        dealText: "Productos destacados, ofertas y opciones de envio rapido.",
        results: "Resultados",
        sortBy: "Ordenar por",
        featured: "Destacados",
        secureCheckout: "Checkout seguro",
        support: "Soporte",
        easyReturns: "Devoluciones simples",
        trustTitle: "Confianza marketplace",
        signature: "Principal",
        detail: "Detalle",
        curated: "Curado",
        flagship: "Producto estrella",
        premiumSpecs: ["Presentacion", "Calidad", "Soporte", "Entrega"],
        sellerVerified: "Vendedor verificado",
        used: "Usado",
        newItem: "Nuevo",
        localPickup: "Retiro local",
        makeOffer: "Hacer oferta",
        contactSeller: "Contactar vendedor",
        listings: "Listings",
        areas: "Zonas",
        searchListings: "Buscar listings",
        viewListings: "Ver listings",
        inquireNow: "Consultar ahora",
        featuredListing: "Listing destacado",
        newListing: "Nuevo listing",
        availableNow: "Disponible",
        listingPrice: "Precio a consultar",
        listingSearchPlaceholder: "Buscar por ubicacion, tipo, precio o palabra clave",
        listingFiltersTitle: "Busca con filtros claros",
        listingFiltersText: "Ayuda al cliente a comparar por categoria, zona, precio y disponibilidad.",
        featuredListingsTitle: "Listings destacados",
        featuredListingsText: "Listings activos con precio, ubicacion, detalles y CTA de consulta.",
        listingAreaTitle: "Explora las mejores zonas",
        listingAreaText: "Tarjetas de zona y notas de ubicacion hacen la busqueda mas clara.",
        listingTrustTitle: "Confianza antes de consultar",
        listingTrustText: "Detalles verificados, contacto claro y disponibilidad actualizada.",
        listingContactTitle: "Pregunta por un listing",
        listingContactText: "Envia el listing, presupuesto, ubicacion y metodo de contacto.",
        listingCategories: ["Casas", "Alquileres", "Comercial", "Terrenos", "Autos", "Destacados"],
        listingLocations: ["Centro", "Zona norte", "Distrito oeste", "Cerca de escuelas", "Frente al agua", "Zona comercial"],
        listingTrustItems: ["Detalles verificados", "Disponibilidad actualizada", "Precios claros", "Soporte local", "Respuesta rapida", "Comparacion simple"],
        newDrop: "Nuevo drop",
        limitedSelection: "Seleccion limitada",
        collection: "Coleccion",
        provenance: "Provenance",
        privateInquiry: "Consulta privada",
        requestPrivateViewing: "Solicitar cita privada",
        viewCollection: "Ver coleccion",
        priceOnRequest: "Precio bajo consulta",
        limitedPiece: "Pieza limitada",
        authenticated: "Autenticada",
        privateService: "Servicio privado",
        signaturePiece: "Pieza protagonista",
        luxuryProvenanceTitle: "Provenance, autenticidad y servicio",
        luxuryProvenanceText: "Senales de confianza para compradores de alto valor antes de consultar.",
        luxuryPrivateTitle: "Cita privada",
        luxuryPrivateText: "Solicita disponibilidad, pieza preferida y metodo de contacto.",
        luxuryContactTitle: "Solicita una consulta privada",
        luxuryContactText: "Envia la pieza, ocasion, horario preferido y metodo de contacto.",
        luxuryProofItems: ["Provenance verificada", "Disponibilidad limitada", "Asesor privado", "Manejo asegurado", "Consulta segura", "Seguimiento concierge"],
        courseAcademy: "Academia de cursos",
        programs: "Programas",
        curriculum: "Curriculum",
        enroll: "Inscripcion",
        enrollNow: "Inscribirme",
        viewCurriculum: "Ver curriculum",
        coursePrice: "Precio de inscripcion",
        featuredProgram: "Programa destacado",
        beginnerFriendly: "Ideal para empezar",
        certificateReady: "Con certificado",
        outcomes: "Resultados",
        instructor: "Instructor",
        outcomesTitle: "Lo que el estudiante debe lograr",
        outcomesText: "Resultados, habilidades y senales claras de avance.",
        instructorTitle: "Guia experta",
        instructorText: "Credibilidad del instructor, metodo y soporte.",
        learningPathItems: ["Base", "Practica", "Aplicacion", "Soporte", "Resultado", "Siguiente paso"],
        learningOutcomeItems: ["Progreso claro", "Practicas accionables", "Curriculum editable", "Prueba para estudiantes", "Notas de soporte", "Oferta lista"],
        instructorTrustItems: ["Clases expertas", "Modulos estructurados", "Ruta de soporte", "Resultados"],
        educationDurations: ["4 semanas", "6 modulos", "Cohorte en vivo", "A tu ritmo"],
        clinicCare: "Cuidado personalizado",
        treatments: "Tratamientos",
        bookConsultation: "Agendar consulta",
        viewTreatments: "Ver tratamientos",
        consultationBased: "Segun consulta",
        popularTreatment: "Tratamiento popular",
        specialistLed: "Guiado por especialista",
        personalizedPlan: "Plan personalizado",
        treatmentsTitle: "Tratamientos y servicios",
        carePathTitle: "Ruta clara de atencion",
        carePathText: "Consulta, plan, tratamiento y seguimiento en un camino simple.",
        carePathItems: ["Consulta", "Plan personal", "Tratamiento", "Revision", "Cuidado posterior", "Proxima visita"],
        clinicTrustTitle: "Confianza y seguridad primero",
        clinicTrustText: "Credenciales, expectativas y respuesta antes de reservar.",
        clinicTrustItems: ["Guia especializada", "Proceso seguro", "Expectativas claras", "Consulta privada", "Cuidado posterior", "Respuesta rapida"],
        resultsTitle: "Resultados con expectativas reales",
        resultsText: "Pruebas, notas de transformacion y seguimiento.",
        resultItems: ["Progreso visible", "Antes/despues", "Plan personal", "Seguimiento"],
        clinicTeamTitle: "Especialistas que guian el proceso",
        clinicTeamText: "Estandares clinicos, credibilidad y filosofia de atencion.",
        clinicTeamItems: ["Equipo certificado", "Consulta primero", "Educacion al paciente", "Seguimiento"],
        clinicBookingTitle: "Agenda una consulta o pregunta",
        clinicBookingText: "Envia tratamiento, inquietud, horario y metodo de contacto.",
        clinicDurations: ["Consulta 30 min", "Sesion 45 min", "Plan personal", "Seguimiento"],
        structuredModules: "Modulos estructurados",
        practicalOutcome: "Resultado practico",
        flexibleAccess: "Acceso flexible",
        instantAccess: "Acceso inmediato",
        downloadable: "Contenido descargable",
        bonus: "Recursos extra",
        lifetime: "Acceso de por vida",
        getAccess: "Obtener acceso",
        digitalProducts: "Productos digitales",
        viewProducts: "Ver productos",
        modules: "Modulos",
        digitalAccessShort: "Descargas, modulos y notas de soporte.",
        digitalBundleTitle: "Ofertas digitales listas para vender",
        digitalModulesTitle: "Que recibe el cliente",
        digitalProofTitle: "Confianza antes del checkout",
        digitalAccessTitle: "Obten acceso y empieza de inmediato",
        digitalAccessText: "El cliente sabe exactamente que recibe, como entra y donde pide soporte.",
        digitalModuleItems: ["Entrenamiento principal", "Recursos descargables", "Plantillas y herramientas", "Material bonus", "Instrucciones de acceso", "Notas de soporte"],
        digitalProofItems: ["Acceso inmediato", "Modulos editables", "Licencia clara", "Soporte listo", "Valor del bundle", "Checkout simple"],
        collections: "Colecciones",
        lookbook: "Lookbook",
        fit: "Guia de tallas",
        drop: "Drop",
        fitGuide: "Guia de tallas",
        fitGuideItems: ["Notas de talla y ajuste", "Sugerencias de estilo", "Cuidados de la prenda", "Envios y devoluciones"],
        fashionCollections: ["Novedades", "Esenciales", "Piezas destacadas", "Accesorios", "Drop limitado", "Mas vendidos"],
        jewelryCategories: ["Collares", "Pulseras", "Aretes y zarcillos", "Anillos", "Sets de regalo", "Piezas personalizadas"],
        company: "Empresa",
        services: "Servicios",
        process: "Proceso",
        proof: "Prueba",
        capability: "Capacidad",
        requestConsultation: "Solicitar consulta",
        viewServices: "Ver servicios",
        corporateProcessItems: ["Diagnostico", "Estrategia", "Entrega", "Soporte"],
        corporateProofItems: ["Entrega confiable", "Comunicacion clara", "Estandares profesionales"],
        localExperts: "Expertos locales",
        callNow: "Llamar ahora",
        fastResponse: "Respuesta local rapida",
        serviceAreas: "Areas de servicio",
        workProof: "Trabajos",
        workSlug: "trabajos",
        service: "Servicio",
        quoteOnly: "Cotizacion",
        serviceAreaReady: "Disponible por zona",
        quoteExpectation: "Cuenta que necesitas y recibe el siguiente paso claro.",
        homeServiceHeadline: (name) => `${name} resuelve el trabajo bien desde el primer intento`,
        homeServiceSubheadline: (description) => description || "Servicio local confiable con comunicacion clara, agenda ordenada y cotizacion primero.",
        homeServiceCategoriesTitle: "Servicios para necesidades locales reales",
        homeServiceCategoriesText: "Organiza cada servicio en opciones claras para cotizar sin confundir al cliente.",
        homeServiceAreasTitle: "Atendemos las zonas importantes",
        homeServiceAreasText: "Muestra ciudades, vecindarios, tiempos de respuesta y disponibilidad de emergencia en una seccion clara.",
        serviceAreaItems: ["Zonas cercanas", "Opciones el mismo dia", "Emergencias", "Servicio recurrente", "Trabajo autorizado", "Estimados claros"],
        beforeAfterTitle: "Trabajo real, resultados visibles",
        beforeAfterText: "Usa fotos, antes/despues y proyectos terminados para generar confianza antes de la llamada.",
        homeServiceTrustTitle: "Por que llaman primero",
        homeServiceTrustText: "Senales de confianza, resenas y expectativas de respuesta hacen que el siguiente paso sea seguro.",
        homeServiceTrustItems: ["Licencia y seguro", "Estimados claros", "Llegadas puntuales", "Antes/despues", "Referencias locales", "Seguimiento limpio"],
        homeServiceQuoteTitle: "Solicita una cotizacion clara",
        homeServiceQuoteText: "Comparte el trabajo, horario preferido y ubicacion. El negocio puede responder por telefono, WhatsApp o email.",
        appointments: "Citas",
        availability: "Disponibilidad",
        team: "Equipo",
        teamSlug: "equipo",
        bookNow: "Reservar ahora",
        nextAvailable: "Proxima disponibilidad",
        slot: "Horario",
        confirmation: "Listo para confirmar",
        staffPick: "Recomendado",
        bookingContactFallback: "Confirma el servicio, horario y metodo de contacto preferido.",
        bookingHeadline: (name) => `Reserva en ${name} sin vueltas`,
        bookingSubheadline: (description) => description || "Pagina clara de citas con servicios, disponibilidad, notas del equipo y reserva simple.",
        bookingServicesTitle: "Elige la cita correcta",
        bookingServicesText: "Muestra duracion, detalles del servicio y que debe esperar el cliente antes de reservar.",
        availabilityTitle: "Disponibilidad facil de entender",
        availabilityText: "Muestra horarios, preparacion y expectativas de confirmacion de forma clara.",
        availabilityItems: ["Hoy / manana", "Citas en la manana", "Citas en la tarde", "Opciones fin de semana", "Consultas por llamada", "Visitas de seguimiento"],
        bookingTeamTitle: "Una visita mas simple de inicio a fin",
        bookingTeamText: "Explica equipo, proceso y preparacion para que el cliente sepa que sigue.",
        bookingTeamItems: ["Elige un servicio", "Selecciona horario preferido", "Recibe confirmacion", "Llega preparado"],
        bookingProofItems: ["Duraciones claras", "Confirmacion simple", "Detalles antes de reservar"],
        bookingContactTitle: "Reserva el proximo horario disponible",
        bookingContactText: "Envia servicio, dia y metodo de contacto preferido. El negocio puede confirmar por telefono, WhatsApp o email.",
        menu: "Menu",
        specials: "Especiales",
        hoursLocation: "Horarios y ubicacion",
        orderNow: "Ordenar ahora",
        viewMenu: "Ver menu",
        contact: "Contacto",
        signatureMenu: "Menu destacado",
        popularDish: "Popular",
        chefPick: "Chef recomienda",
        pickupDelivery: "Pickup / delivery",
        quickOrderNote: "Confirma por telefono, WhatsApp o email.",
        editableMenuNote: "Editable desde el catalog manager.",
        restaurantProofItems: ["Menu editable", "Combos destacados", "Contacto rapido"],
        restaurantCategories: ["Entradas", "Platos fuertes", "Bebidas", "Postres", "Especiales", "Favoritos"],
        restaurantCategoriesTitle: "Explora el menu por categoria",
        restaurantSignatureTitle: "Platos destacados listos para pedir",
        restaurantSpecialsTitle: "Especiales y combos",
        restaurantInfoTitle: "Horarios, ubicacion y opciones de servicio",
        restaurantOrderTitle: "Haz un pedido o pregunta",
        restaurantOrderText: "Envia platos preferidos, opcion de pickup o delivery y metodo de contacto.",
        offer: "Oferta",
        benefits: "Beneficios",
        faq: "Preguntas",
        claimOffer: "Empezar ahora",
        seeProof: "Ver prueba",
        nextStep: "Siguiente paso",
        quickRequest: "Envia una solicitud rapida y recibe una respuesta clara.",
        bestValue: "Mejor opcion",
        outcomeFocused: "Enfocado en resultado",
        fastNextStep: "Siguiente paso rapido",
        editableOffer: "Oferta editable",
        faqAnswer: "Esto se puede ajustar al negocio, la oferta y las dudas del cliente.",
        funnelBenefitsItems: ["Promesa clara", "Siguiente paso simple", "Leads calificados", "Secciones editables", "Estructura con prueba", "Lanzamiento rapido"],
        funnelProofItems: ["Resultado especifico", "Proceso simple", "Respuesta rapida", "Conversacion clara sobre precios"],
        funnelFaqItems: [{ question: "Que pasa despues?", answer: "El negocio responde con el siguiente paso." }, { question: "Se puede personalizar?", answer: "Si, la oferta y el contenido son editables." }, { question: "Esto es una tienda?", answer: "No, esta enfocado en leads." }],
        main: "Principal",
        popular: "Popular",
        marketPrice: "Precio de mercado",
        fromQuote: "Desde cotizacion",
        book: "Reservar",
        freeQuote: "Cotizacion gratis",
        practiceArea: "Area de practica",
        scheduleConsultation: "Agendar consulta",
        before: "Antes",
        after: "Despues",
        viewProject: "Ver proyecto",
        plan: "Plan",
        custom: "Personalizado",
        start: "Empezar",
        ticketOffer: "Ticket / oferta",
        reserve: "Reservar",
        package: "Paquete",
        applyNow: "Aplicar ahora",
        view: "Ver",
        request: "Consultar"
      },
      fr: {
        searchFilters: "Recherche et filtres",
        category: "Cat\xE9gorie",
        brand: "Marque",
        price: "Prix",
        rating: "Note",
        delivery: "Livraison",
        deal: "Offre",
        fastShip: "Livraison rapide",
        search: "Recherche",
        searchPlaceholder: "Rechercher produits, marques ou categories",
        searchButton: "Rechercher",
        shopNow: "Acheter",
        categories: "Categories",
        dealTitle: "Selections",
        dealText: "Produits mis en avant, offres et options de livraison rapide.",
        results: "Resultats",
        sortBy: "Trier par",
        featured: "Mis en avant",
        secureCheckout: "Paiement securise",
        support: "Support",
        easyReturns: "Retours simples",
        trustTitle: "Confiance marketplace",
        signature: "Signature",
        detail: "Detail",
        curated: "Soigne",
        flagship: "Produit phare",
        premiumSpecs: ["Presentation", "Qualite", "Support", "Livraison"],
        sellerVerified: "Vendeur v\xE9rifi\xE9",
        used: "Occasion",
        newItem: "Neuf",
        localPickup: "Retrait local",
        makeOffer: "Faire une offre",
        contactSeller: "Contacter le vendeur",
        listings: "Annonces",
        areas: "Zones",
        searchListings: "Rechercher",
        viewListings: "Voir les annonces",
        inquireNow: "Demander",
        featuredListing: "Annonce en avant",
        newListing: "Nouvelle annonce",
        availableNow: "Disponible",
        listingPrice: "Prix sur demande",
        listingSearchPlaceholder: "Rechercher par lieu, type, prix ou mot-cle",
        listingFiltersTitle: "Rechercher avec les bons filtres",
        listingFiltersText: "Aidez les clients a comparer par categorie, zone, prix et disponibilite.",
        featuredListingsTitle: "Annonces en avant",
        featuredListingsText: "Annonces actives avec prix, localisation, details et CTA.",
        listingAreaTitle: "Explorer les meilleures zones",
        listingAreaText: "Cartes de zones et notes locales pour clarifier la recherche.",
        listingTrustTitle: "Confiance avant la demande",
        listingTrustText: "Details verifies, contact clair et disponibilite mise a jour.",
        listingContactTitle: "Demander une annonce",
        listingContactText: "Envoyez l'annonce, le budget, la localisation et le contact prefere.",
        listingCategories: ["Maisons", "Locations", "Commercial", "Terrain", "Voitures", "En avant"],
        listingLocations: ["Centre", "Zone nord", "Quartier ouest", "Pres des ecoles", "Bord de l'eau", "Zone business"],
        listingTrustItems: ["Details verifies", "Disponibilite a jour", "Prix clairs", "Support local", "Reponse rapide", "Comparaison simple"],
        newDrop: "Nouvelle collection",
        limitedSelection: "S\xE9lection limit\xE9e",
        courseAcademy: "Academie de cours",
        programs: "Programmes",
        curriculum: "Programme",
        enroll: "Inscription",
        enrollNow: "S'inscrire",
        viewCurriculum: "Voir le programme",
        coursePrice: "Prix d'inscription",
        featuredProgram: "Programme phare",
        beginnerFriendly: "Pour debuter",
        certificateReady: "Certificat pret",
        outcomes: "Resultats",
        instructor: "Instructeur",
        outcomesTitle: "Ce que l'etudiant doit savoir faire",
        outcomesText: "Resultats, competences et progression claire.",
        instructorTitle: "Guidage expert",
        instructorText: "Credibilite, methode et support.",
        learningPathItems: ["Bases", "Pratique", "Application", "Support", "Resultat", "Suite"],
        learningOutcomeItems: ["Progression claire", "Exercices pratiques", "Programme modifiable", "Preuve etudiante", "Notes support", "Offre prete"],
        instructorTrustItems: ["Cours experts", "Modules structures", "Support", "Resultats"],
        educationDurations: ["4 semaines", "6 modules", "Cohorte live", "A son rythme"],
        clinicCare: "Soin personnalise",
        treatments: "Soins",
        bookConsultation: "Reserver une consultation",
        viewTreatments: "Voir les soins",
        consultationBased: "Selon consultation",
        popularTreatment: "Soin populaire",
        specialistLed: "Guide par specialiste",
        personalizedPlan: "Plan personnalise",
        treatmentsTitle: "Soins et services",
        carePathTitle: "Parcours de soin clair",
        carePathText: "Consultation, plan, soin et suivi dans un parcours simple.",
        carePathItems: ["Consultation", "Plan personnel", "Soin", "Suivi", "Apres-soin", "Prochaine visite"],
        clinicTrustTitle: "Confiance et securite",
        clinicTrustText: "Diplomes, attentes et reponse avant reservation.",
        clinicTrustItems: ["Guidage specialise", "Processus securise", "Attentes claires", "Consultation privee", "Apres-soin", "Reponse rapide"],
        resultsTitle: "Resultats realistes",
        resultsText: "Preuves, notes de transformation et suivi.",
        resultItems: ["Progres visible", "Avant/apres", "Plan personnel", "Suivi"],
        clinicTeamTitle: "Specialistes qui guident le processus",
        clinicTeamText: "Standards cliniques, credibilite et philosophie de soin.",
        clinicTeamItems: ["Equipe certifiee", "Consultation d'abord", "Education patient", "Suivi"],
        clinicBookingTitle: "Reserver ou poser une question",
        clinicBookingText: "Envoyez soin, besoin, horaire et contact.",
        clinicDurations: ["Consultation 30 min", "Session 45 min", "Plan personnel", "Suivi pret"],
        structuredModules: "Modules structures",
        practicalOutcome: "Resultat pratique",
        flexibleAccess: "Acces flexible",
        instantAccess: "Acc\xE8s imm\xE9diat",
        downloadable: "Contenu t\xE9l\xE9chargeable",
        bonus: "Ressources bonus",
        lifetime: "Acc\xE8s \xE0 vie",
        getAccess: "Obtenir l'acc\xE8s",
        digitalProducts: "Produits digitaux",
        viewProducts: "Voir les produits",
        modules: "Modules",
        digitalAccessShort: "Telechargements, modules et notes de support.",
        digitalBundleTitle: "Offres digitales pretes a vendre",
        digitalModulesTitle: "Ce que le client recoit",
        digitalProofTitle: "Confiance avant paiement",
        digitalAccessTitle: "Obtenir l'acces et commencer tout de suite",
        digitalAccessText: "Le client sait exactement ce qu'il recoit, comment acceder et ou demander du support.",
        digitalModuleItems: ["Formation principale", "Ressources telechargeables", "Modeles et outils", "Bonus", "Instructions d'acces", "Notes de support"],
        digitalProofItems: ["Acces immediat", "Modules modifiables", "Licence claire", "Support pret", "Valeur du bundle", "Paiement simple"],
        collections: "Collections",
        lookbook: "Lookbook",
        fit: "Guide des tailles",
        drop: "Drop",
        fitGuide: "Guide des tailles",
        fitGuideItems: ["Notes de taille", "Suggestions de style", "Conseils d'entretien", "Livraison et retours"],
        fashionCollections: ["Nouveautes", "Essentiels", "Pieces fortes", "Accessoires", "Drop limite", "Meilleures ventes"],
        jewelryCategories: ["Colliers", "Bracelets", "Boucles d'oreilles", "Bagues", "Coffrets cadeau", "Pieces personnalisees"],
        company: "Entreprise",
        services: "Services",
        process: "Processus",
        proof: "Preuve",
        capability: "Capacite",
        requestConsultation: "Demander une consultation",
        viewServices: "Voir les services",
        corporateProcessItems: ["Diagnostic", "Strategie", "Livraison", "Support"],
        corporateProofItems: ["Livraison fiable", "Communication claire", "Standards professionnels"],
        localExperts: "Experts locaux",
        callNow: "Appeler",
        fastResponse: "Reponse locale rapide",
        serviceAreas: "Zones desservies",
        workProof: "Realisations",
        workSlug: "realisations",
        service: "Service",
        quoteOnly: "Sur devis",
        serviceAreaReady: "Zone couverte",
        quoteExpectation: "Expliquez le besoin et recevez une prochaine etape claire.",
        homeServiceHeadline: (name) => `${name} realise le travail correctement des le depart`,
        homeServiceSubheadline: (description) => description || "Service local fiable avec communication claire, planning simple et devis avant intervention.",
        homeServiceCategoriesTitle: "Services pour des besoins locaux reels",
        homeServiceCategoriesText: "Organisez chaque service en options claires et faciles a demander.",
        homeServiceAreasTitle: "Nous couvrons les zones importantes",
        homeServiceAreasText: "Affichez les villes, quartiers, delais de reponse et options d'urgence.",
        serviceAreaItems: ["Quartiers proches", "Options le jour meme", "Urgences", "Service recurrent", "Travail autorise", "Devis clairs"],
        beforeAfterTitle: "Travail reel, resultats visibles",
        beforeAfterText: "Utilisez photos, avant/apres et projets termines pour creer la confiance.",
        homeServiceTrustTitle: "Pourquoi les clients appellent d'abord",
        homeServiceTrustText: "Avis, preuves et delais de reponse rendent la demande plus rassurante.",
        homeServiceTrustItems: ["Assure et autorise", "Devis clairs", "Arrivees ponctuelles", "Avant/apres", "References locales", "Suivi propre"],
        homeServiceQuoteTitle: "Demander un devis clair",
        homeServiceQuoteText: "Partagez le besoin, l'horaire prefere et la localisation. L'entreprise peut repondre par telephone, WhatsApp ou email.",
        appointments: "Rendez-vous",
        availability: "Disponibilite",
        team: "Equipe",
        teamSlug: "equipe",
        bookNow: "Reserver",
        nextAvailable: "Prochaine disponibilite",
        slot: "Creneau",
        confirmation: "Pret a confirmer",
        staffPick: "Recommande",
        bookingContactFallback: "Confirmez le service, le creneau et le mode de contact prefere.",
        bookingHeadline: (name) => `Reservez ${name} sans aller-retour`,
        bookingSubheadline: (description) => description || "Page de rendez-vous claire avec services, disponibilite, equipe et reservation simple.",
        bookingServicesTitle: "Choisissez le bon rendez-vous",
        bookingServicesText: "Affichez duree, details du service et attentes avant reservation.",
        availabilityTitle: "Disponibilite facile a comprendre",
        availabilityText: "Clarifiez les horaires, la preparation et la confirmation.",
        availabilityItems: ["Aujourd'hui / demain", "Matin", "Apres-midi", "Week-end", "Appels de consultation", "Suivis"],
        bookingTeamTitle: "Une visite plus fluide",
        bookingTeamText: "Expliquez l'equipe, le processus et la preparation.",
        bookingTeamItems: ["Choisir un service", "Choisir un creneau", "Recevoir confirmation", "Arriver prepare"],
        bookingProofItems: ["Durees claires", "Confirmation simple", "Details avant reservation"],
        bookingContactTitle: "Reserver le prochain creneau",
        bookingContactText: "Envoyez le service, le jour et le contact prefere. L'entreprise confirme par telephone, WhatsApp ou email.",
        menu: "Menu",
        specials: "Specialites",
        hoursLocation: "Horaires et adresse",
        orderNow: "Commander",
        viewMenu: "Voir le menu",
        contact: "Contact",
        signatureMenu: "Menu signature",
        popularDish: "Populaire",
        chefPick: "Choix du chef",
        pickupDelivery: "Retrait / livraison",
        quickOrderNote: "Confirmation par telephone, WhatsApp ou email.",
        editableMenuNote: "Modifiable depuis le gestionnaire de catalogue.",
        restaurantProofItems: ["Menu modifiable", "Menus en avant", "Contact rapide"],
        restaurantCategories: ["Entrees", "Plats", "Boissons", "Desserts", "Specialites", "Choix du chef"],
        restaurantCategoriesTitle: "Explorer le menu par categorie",
        restaurantSignatureTitle: "Plats signature prets a commander",
        restaurantSpecialsTitle: "Offres et menus",
        restaurantInfoTitle: "Horaires, adresse et options de service",
        restaurantOrderTitle: "Commander ou poser une question",
        restaurantOrderText: "Envoyez les plats souhaites, retrait ou livraison et le contact prefere.",
        offer: "Offre",
        benefits: "Benefices",
        faq: "FAQ",
        claimOffer: "Commencer",
        seeProof: "Voir les preuves",
        nextStep: "Prochaine etape",
        quickRequest: "Envoyez une demande rapide et recevez une reponse claire.",
        bestValue: "Meilleur choix",
        outcomeFocused: "Oriente resultat",
        fastNextStep: "Etape rapide",
        editableOffer: "Offre modifiable",
        faqAnswer: "Cela peut etre ajuste au business, a l'offre et aux objections client.",
        funnelBenefitsItems: ["Promesse claire", "Etape simple", "Leads qualifies", "Sections modifiables", "Structure avec preuves", "Lancement rapide"],
        funnelProofItems: ["Resultat specifique", "Processus simple", "Reponse rapide", "Prix clarifies"],
        funnelFaqItems: [{ question: "Que se passe-t-il ensuite ?", answer: "L'entreprise repond avec la prochaine etape." }, { question: "Peut-on personnaliser ?", answer: "Oui, l'offre et le contenu sont modifiables." }, { question: "Est-ce une boutique ?", answer: "Non, c'est centre sur les leads." }],
        main: "Principal",
        popular: "Populaire",
        marketPrice: "Prix du march\xE9",
        fromQuote: "Sur devis",
        book: "R\xE9server",
        freeQuote: "Devis gratuit",
        practiceArea: "Domaine d'expertise",
        scheduleConsultation: "Planifier une consultation",
        before: "Avant",
        after: "Apr\xE8s",
        viewProject: "Voir le projet",
        plan: "Offre",
        custom: "Sur mesure",
        start: "Commencer",
        ticketOffer: "Billet / offre",
        reserve: "R\xE9server",
        package: "Forfait",
        applyNow: "Postuler",
        view: "Voir",
        request: "Demander"
      },
      pt: {
        searchFilters: "Busca e filtros",
        category: "Categoria",
        brand: "Marca",
        price: "Pre\xE7o",
        rating: "Avalia\xE7\xE3o",
        delivery: "Entrega",
        deal: "Oferta",
        fastShip: "Envio r\xE1pido",
        search: "Buscar",
        searchPlaceholder: "Buscar produtos, marcas ou categorias",
        searchButton: "Buscar",
        shopNow: "Comprar agora",
        categories: "Categorias",
        dealTitle: "Destaques",
        dealText: "Produtos em destaque, ofertas e opcoes de entrega rapida.",
        results: "Resultados",
        sortBy: "Ordenar por",
        featured: "Destaques",
        secureCheckout: "Checkout seguro",
        support: "Suporte",
        easyReturns: "Devolucoes simples",
        trustTitle: "Confianca marketplace",
        signature: "Principal",
        detail: "Detalhe",
        curated: "Curado",
        flagship: "Produto principal",
        premiumSpecs: ["Apresentacao", "Qualidade", "Suporte", "Entrega"],
        sellerVerified: "Vendedor verificado",
        used: "Usado",
        newItem: "Novo",
        localPickup: "Retirada local",
        makeOffer: "Fazer oferta",
        contactSeller: "Contatar vendedor",
        listings: "Anuncios",
        areas: "Areas",
        searchListings: "Buscar anuncios",
        viewListings: "Ver anuncios",
        inquireNow: "Consultar agora",
        featuredListing: "Anuncio destaque",
        newListing: "Novo anuncio",
        availableNow: "Disponivel",
        listingPrice: "Preco sob consulta",
        listingSearchPlaceholder: "Buscar por localizacao, tipo, preco ou palavra-chave",
        listingFiltersTitle: "Busca com filtros claros",
        listingFiltersText: "Ajude o cliente a comparar por categoria, area, preco e disponibilidade.",
        featuredListingsTitle: "Anuncios em destaque",
        featuredListingsText: "Anuncios ativos com preco, localizacao, detalhes e CTA de consulta.",
        listingAreaTitle: "Explore as melhores areas",
        listingAreaText: "Cards de area e notas locais deixam a busca clara.",
        listingTrustTitle: "Confianca antes da consulta",
        listingTrustText: "Detalhes verificados, contato claro e disponibilidade atualizada.",
        listingContactTitle: "Perguntar sobre um anuncio",
        listingContactText: "Envie o anuncio, orcamento, localizacao e metodo de contato.",
        listingCategories: ["Casas", "Alugueis", "Comercial", "Terrenos", "Carros", "Destaques"],
        listingLocations: ["Centro", "Zona norte", "Distrito oeste", "Perto de escolas", "Frente a agua", "Zona comercial"],
        listingTrustItems: ["Detalhes verificados", "Disponibilidade atualizada", "Precos claros", "Suporte local", "Resposta rapida", "Comparacao simples"],
        newDrop: "Novo drop",
        limitedSelection: "Sele\xE7\xE3o limitada",
        courseAcademy: "Academia de cursos",
        programs: "Programas",
        curriculum: "Curriculo",
        enroll: "Inscricao",
        enrollNow: "Inscrever-se",
        viewCurriculum: "Ver curriculo",
        coursePrice: "Preco de inscricao",
        featuredProgram: "Programa destaque",
        beginnerFriendly: "Para iniciantes",
        certificateReady: "Com certificado",
        outcomes: "Resultados",
        instructor: "Instrutor",
        outcomesTitle: "O que o aluno deve conseguir fazer",
        outcomesText: "Resultados, habilidades e progresso claro.",
        instructorTitle: "Guia especialista",
        instructorText: "Credibilidade, metodo e suporte.",
        learningPathItems: ["Base", "Pratica", "Aplicacao", "Suporte", "Resultado", "Proximo passo"],
        learningOutcomeItems: ["Progressao clara", "Atividades praticas", "Curriculo editavel", "Prova para alunos", "Notas de suporte", "Oferta pronta"],
        instructorTrustItems: ["Aulas especialistas", "Modulos estruturados", "Suporte", "Resultados"],
        educationDurations: ["4 semanas", "6 modulos", "Turma ao vivo", "No seu ritmo"],
        clinicCare: "Cuidado personalizado",
        treatments: "Tratamentos",
        bookConsultation: "Agendar consulta",
        viewTreatments: "Ver tratamentos",
        consultationBased: "Sob consulta",
        popularTreatment: "Tratamento popular",
        specialistLed: "Com especialista",
        personalizedPlan: "Plano personalizado",
        treatmentsTitle: "Tratamentos e servicos",
        carePathTitle: "Caminho de cuidado claro",
        carePathText: "Consulta, plano, tratamento e acompanhamento em um caminho simples.",
        carePathItems: ["Consulta", "Plano pessoal", "Tratamento", "Revisao", "Pos-cuidado", "Proxima visita"],
        clinicTrustTitle: "Confianca e seguranca",
        clinicTrustText: "Credenciais, expectativas e resposta antes de agendar.",
        clinicTrustItems: ["Guia especializada", "Processo seguro", "Expectativas claras", "Consulta privada", "Pos-cuidado", "Resposta rapida"],
        resultsTitle: "Resultados realistas",
        resultsText: "Provas, notas de transformacao e acompanhamento.",
        resultItems: ["Progresso visivel", "Antes/depois", "Plano pessoal", "Acompanhamento"],
        clinicTeamTitle: "Especialistas que guiam o processo",
        clinicTeamText: "Padroes clinicos, credibilidade e filosofia de cuidado.",
        clinicTeamItems: ["Equipe certificada", "Consulta primeiro", "Educacao do paciente", "Acompanhamento"],
        clinicBookingTitle: "Agende uma consulta ou pergunte",
        clinicBookingText: "Envie tratamento, duvida, horario e contato.",
        clinicDurations: ["Consulta 30 min", "Sessao 45 min", "Plano pessoal", "Acompanhamento"],
        structuredModules: "Modulos estruturados",
        practicalOutcome: "Resultado pratico",
        flexibleAccess: "Acesso flexivel",
        instantAccess: "Acesso imediato",
        downloadable: "Conte\xFAdo baix\xE1vel",
        bonus: "Recursos b\xF4nus",
        lifetime: "Acesso vital\xEDcio",
        getAccess: "Obter acesso",
        digitalProducts: "Produtos digitais",
        viewProducts: "Ver produtos",
        modules: "Modulos",
        digitalAccessShort: "Downloads, modulos e notas de suporte.",
        digitalBundleTitle: "Ofertas digitais prontas para vender",
        digitalModulesTitle: "O que o cliente recebe",
        digitalProofTitle: "Confianca antes do checkout",
        digitalAccessTitle: "Obtenha acesso e comece imediatamente",
        digitalAccessText: "O cliente sabe exatamente o que recebe, como acessar e onde pedir suporte.",
        digitalModuleItems: ["Treinamento principal", "Recursos para download", "Templates e ferramentas", "Material bonus", "Instrucoes de acesso", "Notas de suporte"],
        digitalProofItems: ["Acesso imediato", "Modulos editaveis", "Licenca clara", "Suporte pronto", "Valor do bundle", "Checkout simples"],
        collections: "Colecoes",
        lookbook: "Lookbook",
        fit: "Guia de tamanhos",
        drop: "Drop",
        fitGuide: "Guia de tamanhos",
        fitGuideItems: ["Notas de tamanho e caimento", "Sugestoes de estilo", "Cuidados com a peca", "Envios e devolucoes"],
        fashionCollections: ["Novidades", "Essenciais", "Pecas destaque", "Acessorios", "Drop limitado", "Mais vendidos"],
        jewelryCategories: ["Colares", "Pulseiras", "Brincos", "Aneis", "Kits presente", "Pecas personalizadas"],
        company: "Empresa",
        services: "Servicos",
        process: "Processo",
        proof: "Prova",
        capability: "Capacidade",
        requestConsultation: "Solicitar consulta",
        viewServices: "Ver servicos",
        corporateProcessItems: ["Diagnostico", "Estrategia", "Entrega", "Suporte"],
        corporateProofItems: ["Entrega confiavel", "Comunicacao clara", "Padroes profissionais"],
        localExperts: "Especialistas locais",
        callNow: "Ligar agora",
        fastResponse: "Resposta local rapida",
        serviceAreas: "Areas atendidas",
        workProof: "Trabalhos",
        workSlug: "trabalhos",
        service: "Servico",
        quoteOnly: "Orcamento",
        serviceAreaReady: "Area atendida",
        quoteExpectation: "Conte o que precisa e receba o proximo passo claro.",
        homeServiceHeadline: (name) => `${name} resolve o servico certo desde o primeiro contato`,
        homeServiceSubheadline: (description) => description || "Servico local confiavel com comunicacao clara, agenda simples e orcamento primeiro.",
        homeServiceCategoriesTitle: "Servicos para necessidades locais reais",
        homeServiceCategoriesText: "Organize cada servico em opcoes claras para solicitar orcamento sem confusao.",
        homeServiceAreasTitle: "Atendemos as areas importantes",
        homeServiceAreasText: "Mostre cidades, bairros, expectativa de resposta e disponibilidade de emergencia.",
        serviceAreaItems: ["Bairros proximos", "Opcoes no mesmo dia", "Emergencias", "Servico recorrente", "Trabalho autorizado", "Estimativas claras"],
        beforeAfterTitle: "Trabalho real, resultado visivel",
        beforeAfterText: "Use fotos, antes/depois e projetos finalizados para gerar confianca antes da chamada.",
        homeServiceTrustTitle: "Por que os clientes ligam primeiro",
        homeServiceTrustText: "Provas, avaliacoes e expectativa de resposta deixam o proximo passo seguro.",
        homeServiceTrustItems: ["Licenciado e segurado", "Estimativas claras", "Pontualidade", "Antes/depois", "Referencias locais", "Acompanhamento claro"],
        homeServiceQuoteTitle: "Solicite um orcamento claro",
        homeServiceQuoteText: "Compartilhe o servico, horario preferido e localizacao. A empresa pode responder por telefone, WhatsApp ou email.",
        appointments: "Agendamentos",
        availability: "Disponibilidade",
        team: "Equipe",
        teamSlug: "equipe",
        bookNow: "Agendar agora",
        nextAvailable: "Proxima disponibilidade",
        slot: "Horario",
        confirmation: "Pronto para confirmar",
        staffPick: "Recomendado",
        bookingContactFallback: "Confirme o servico, horario e metodo de contato preferido.",
        bookingHeadline: (name) => `Agende em ${name} sem complicacao`,
        bookingSubheadline: (description) => description || "Pagina clara de agendamento com servicos, disponibilidade, equipe e reserva simples.",
        bookingServicesTitle: "Escolha o agendamento certo",
        bookingServicesText: "Mostre duracao, detalhes do servico e o que o cliente deve esperar.",
        availabilityTitle: "Disponibilidade facil de entender",
        availabilityText: "Mostre horarios, preparacao e expectativa de confirmacao.",
        availabilityItems: ["Hoje / amanha", "Horarios de manha", "Horarios a tarde", "Fim de semana", "Chamadas de consulta", "Retornos"],
        bookingTeamTitle: "Uma visita mais simples do inicio ao fim",
        bookingTeamText: "Explique equipe, processo e preparacao.",
        bookingTeamItems: ["Escolha um servico", "Escolha horario preferido", "Receba confirmacao", "Chegue preparado"],
        bookingProofItems: ["Duracoes claras", "Confirmacao simples", "Detalhes antes de agendar"],
        bookingContactTitle: "Reserve o proximo horario",
        bookingContactText: "Envie servico, dia e contato preferido. A empresa confirma por telefone, WhatsApp ou email.",
        menu: "Menu",
        specials: "Especiais",
        hoursLocation: "Horarios e localizacao",
        orderNow: "Pedir agora",
        viewMenu: "Ver menu",
        contact: "Contato",
        signatureMenu: "Menu destaque",
        popularDish: "Popular",
        chefPick: "Chef indica",
        pickupDelivery: "Pickup / delivery",
        quickOrderNote: "Confirme por telefone, WhatsApp ou email.",
        editableMenuNote: "Editavel no gerenciador de catalogo.",
        restaurantProofItems: ["Menu editavel", "Combos destaque", "Contato rapido"],
        restaurantCategories: ["Entradas", "Pratos principais", "Bebidas", "Sobremesas", "Especiais", "Chef indica"],
        restaurantCategoriesTitle: "Explore o menu por categoria",
        restaurantSignatureTitle: "Pratos destaque prontos para pedir",
        restaurantSpecialsTitle: "Especiais e combos",
        restaurantInfoTitle: "Horarios, localizacao e servico",
        restaurantOrderTitle: "Fazer pedido ou perguntar",
        restaurantOrderText: "Envie os pratos desejados, pickup ou delivery e metodo de contato.",
        offer: "Oferta",
        benefits: "Beneficios",
        faq: "FAQ",
        claimOffer: "Comecar agora",
        seeProof: "Ver prova",
        nextStep: "Proximo passo",
        quickRequest: "Envie uma solicitacao rapida e receba uma resposta clara.",
        bestValue: "Melhor opcao",
        outcomeFocused: "Foco em resultado",
        fastNextStep: "Proximo passo rapido",
        editableOffer: "Oferta editavel",
        faqAnswer: "Isto pode ser ajustado ao negocio, oferta e duvidas do cliente.",
        funnelBenefitsItems: ["Promessa clara", "Proximo passo simples", "Leads qualificados", "Secoes editaveis", "Estrutura com prova", "Lancamento rapido"],
        funnelProofItems: ["Resultado especifico", "Processo simples", "Resposta rapida", "Conversa clara sobre preco"],
        funnelFaqItems: [{ question: "O que acontece depois?", answer: "O negocio responde com o proximo passo." }, { question: "Pode personalizar?", answer: "Sim, a oferta e o conteudo sao editaveis." }, { question: "Isso e uma loja?", answer: "Nao, e focado em leads." }],
        main: "Principal",
        popular: "Popular",
        marketPrice: "Pre\xE7o de mercado",
        fromQuote: "Sob or\xE7amento",
        book: "Reservar",
        freeQuote: "Or\xE7amento gr\xE1tis",
        practiceArea: "\xC1rea de atua\xE7\xE3o",
        scheduleConsultation: "Agendar consulta",
        before: "Antes",
        after: "Depois",
        viewProject: "Ver projeto",
        plan: "Plano",
        custom: "Personalizado",
        start: "Come\xE7ar",
        ticketOffer: "Ingresso / oferta",
        reserve: "Reservar",
        package: "Pacote",
        applyNow: "Aplicar agora",
        view: "Ver",
        request: "Consultar"
      }
    };
    const professionalLabels = {
      en: {
        professionalFirm: "Professional firm",
        scheduleConsultation: "Schedule consultation",
        consultationBased: "Consultation-based",
        confidential: "Confidential",
        seniorAdvisor: "Senior advisor",
        caseReview: "Case review",
        businessReady: "Business-ready",
        practiceAreasTitle: "Practice areas and services",
        professionalProcessTitle: "A clear advisory process",
        professionalProcessText: "Review, strategy, action plan and follow-up.",
        professionalProcessItems: ["Initial review", "Document check", "Strategy call", "Action plan", "Follow-up", "Ongoing advisory"],
        professionalProofTitle: "Proof clients need before contact",
        professionalProofText: "Credentials, confidentiality and professional standards.",
        professionalProofItems: ["Confidential process", "Senior review", "Clear next steps", "Business-ready advice", "Document support", "Responsive contact"],
        professionalTeamTitle: "Advisors who handle the details",
        professionalTeamText: "Show the people and standards behind the service.",
        professionalTeamItems: ["Experienced advisors", "Private consultation", "Clear documentation", "Follow-through"],
        professionalFaqTitle: "Questions before consultation",
        professionalFaqItems: ["What should I prepare?", "How does the first consultation work?", "Can you review documents?", "Can this become ongoing advisory?"],
        professionalConsultationTitle: "Request a consultation or document review",
        professionalConsultationText: "Send the service needed, urgency, preferred schedule and contact method.",
        professionalEngagements: ["Initial review", "Document review", "Strategy call", "Ongoing advisory"]
      },
      es: {
        professionalFirm: "Firma profesional",
        scheduleConsultation: "Agendar consulta",
        consultationBased: "Segun consulta",
        confidential: "Confidencial",
        seniorAdvisor: "Asesor senior",
        caseReview: "Revision de caso",
        businessReady: "Listo para empresa",
        practiceAreasTitle: "Areas de practica y servicios",
        professionalProcessTitle: "Un proceso de asesoria claro",
        professionalProcessText: "Revision, estrategia, plan de accion y seguimiento.",
        professionalProcessItems: ["Revision inicial", "Chequeo documental", "Llamada estrategica", "Plan de accion", "Seguimiento", "Asesoria continua"],
        professionalProofTitle: "Confianza antes del contacto",
        professionalProofText: "Credenciales, confidencialidad y estandares profesionales.",
        professionalProofItems: ["Proceso confidencial", "Revision senior", "Pasos claros", "Asesoria empresarial", "Soporte documental", "Contacto rapido"],
        professionalTeamTitle: "Asesores que manejan los detalles",
        professionalTeamText: "Muestra las personas y estandares detras del servicio.",
        professionalTeamItems: ["Asesores expertos", "Consulta privada", "Documentacion clara", "Seguimiento"],
        professionalFaqTitle: "Preguntas antes de la consulta",
        professionalFaqItems: ["Que debo preparar?", "Como funciona la primera consulta?", "Pueden revisar documentos?", "Puede ser asesoria continua?"],
        professionalConsultationTitle: "Solicita una consulta o revision documental",
        professionalConsultationText: "Envia el servicio requerido, urgencia, horario y metodo de contacto.",
        professionalEngagements: ["Revision inicial", "Revision documental", "Llamada estrategica", "Asesoria continua"]
      },
      fr: {
        professionalFirm: "Cabinet professionnel",
        scheduleConsultation: "Reserver une consultation",
        consultationBased: "Selon consultation",
        confidential: "Confidentiel",
        seniorAdvisor: "Conseiller senior",
        caseReview: "Analyse du dossier",
        businessReady: "Pret pour entreprise",
        practiceAreasTitle: "Services et domaines d'expertise",
        professionalProcessTitle: "Un processus de conseil clair",
        professionalProcessText: "Analyse, strategie, plan d'action et suivi.",
        professionalProcessItems: ["Analyse initiale", "Verification documents", "Appel strategie", "Plan d'action", "Suivi", "Conseil continu"],
        professionalProofTitle: "Preuves avant contact",
        professionalProofText: "Credentials, confidentialite et standards professionnels.",
        professionalProofItems: ["Processus confidentiel", "Analyse senior", "Etapes claires", "Conseil entreprise", "Support documents", "Contact rapide"],
        professionalTeamTitle: "Des conseillers qui gerent les details",
        professionalTeamText: "Montrez les personnes et standards derriere le service.",
        professionalTeamItems: ["Conseillers experimentes", "Consultation privee", "Documentation claire", "Suivi"],
        professionalFaqTitle: "Questions avant la consultation",
        professionalFaqItems: ["Que dois-je preparer?", "Comment se passe la premiere consultation?", "Pouvez-vous analyser des documents?", "Accompagnement continu possible?"],
        professionalConsultationTitle: "Demander une consultation ou analyse",
        professionalConsultationText: "Envoyez le service requis, l'urgence, le moment prefere et le contact.",
        professionalEngagements: ["Analyse initiale", "Analyse documents", "Appel strategie", "Conseil continu"]
      },
      pt: {
        professionalFirm: "Firma profissional",
        scheduleConsultation: "Agendar consulta",
        consultationBased: "Sob consulta",
        confidential: "Confidencial",
        seniorAdvisor: "Consultor senior",
        caseReview: "Revisao do caso",
        businessReady: "Pronto para empresa",
        practiceAreasTitle: "Areas de atuacao e servicos",
        professionalProcessTitle: "Um processo de consultoria claro",
        professionalProcessText: "Revisao, estrategia, plano de acao e acompanhamento.",
        professionalProcessItems: ["Revisao inicial", "Analise documental", "Chamada estrategica", "Plano de acao", "Acompanhamento", "Consultoria continua"],
        professionalProofTitle: "Confianca antes do contato",
        professionalProofText: "Credenciais, confidencialidade e padroes profissionais.",
        professionalProofItems: ["Processo confidencial", "Revisao senior", "Passos claros", "Consultoria empresarial", "Suporte documental", "Contato rapido"],
        professionalTeamTitle: "Consultores que cuidam dos detalhes",
        professionalTeamText: "Mostre pessoas e padroes por tras do servico.",
        professionalTeamItems: ["Consultores experientes", "Consulta privada", "Documentacao clara", "Acompanhamento"],
        professionalFaqTitle: "Perguntas antes da consulta",
        professionalFaqItems: ["O que devo preparar?", "Como funciona a primeira consulta?", "Podem revisar documentos?", "Pode virar consultoria continua?"],
        professionalConsultationTitle: "Solicite consulta ou revisao documental",
        professionalConsultationText: "Envie o servico, urgencia, horario preferido e contato.",
        professionalEngagements: ["Revisao inicial", "Revisao documental", "Chamada estrategica", "Consultoria continua"]
      }
    };
    const enterpriseLabels = {
      en: { enterprisePlatform: "Enterprise platform", requestDemo: "Request demo", viewSolutions: "View solutions", solutions: "Solutions", useCases: "Use cases", integrations: "Integrations", customPlan: "Custom plan", enterpriseReady: "Enterprise-ready", integrationReady: "Integration-ready", roiFocused: "ROI-focused", enterpriseSolutionsTitle: "Solutions built for operational teams", enterpriseUseCasesTitle: "Use cases by workflow", enterpriseUseCasesText: "Show how the solution fits key business teams.", enterpriseUseCaseItems: ["Automate manual work", "Centralize reporting", "Connect business tools", "Improve team visibility", "Reduce operational delays", "Scale service delivery"], enterpriseIntegrationsTitle: "Integrations and rollout", enterpriseIntegrationsText: "APIs, CRM, ERP, payments, analytics and support workflows.", enterpriseIntegrationItems: ["CRM", "ERP", "Payments", "Analytics", "Support desk", "Custom API"], enterpriseProofTitle: "Business proof before the demo", enterpriseProofText: "Security, rollout, ROI and support signals.", enterpriseProofItems: ["Secure workflows", "Implementation support", "Clear ROI path", "Admin controls", "API-ready", "Team onboarding"], enterprisePricingTitle: "Packages that can start simple and scale", enterprisePricingText: "Starter, growth and enterprise packages.", enterpriseDemoTitle: "Book a demo or request a solution review", enterpriseDemoText: "Send team size, current tools, workflow problem and preferred contact method.", enterpriseTimelines: ["Demo first", "2 week setup", "API-ready", "Managed rollout"] },
      es: { enterprisePlatform: "Plataforma empresarial", requestDemo: "Solicitar demo", viewSolutions: "Ver soluciones", solutions: "Soluciones", useCases: "Casos de uso", integrations: "Integraciones", customPlan: "Plan personalizado", enterpriseReady: "Listo para empresa", integrationReady: "Listo para integrar", roiFocused: "Enfocado en ROI", enterpriseSolutionsTitle: "Soluciones para equipos operativos", enterpriseUseCasesTitle: "Casos de uso por flujo de trabajo", enterpriseUseCasesText: "Muestra como encaja la solucion en equipos clave.", enterpriseUseCaseItems: ["Automatizar trabajo manual", "Centralizar reportes", "Conectar herramientas", "Mejorar visibilidad", "Reducir demoras", "Escalar operaciones"], enterpriseIntegrationsTitle: "Integraciones e implementacion", enterpriseIntegrationsText: "APIs, CRM, ERP, pagos, analitica y soporte.", enterpriseIntegrationItems: ["CRM", "ERP", "Pagos", "Analitica", "Soporte", "API personalizada"], enterpriseProofTitle: "Pruebas antes de la demo", enterpriseProofText: "Seguridad, implementacion, ROI y soporte.", enterpriseProofItems: ["Flujos seguros", "Soporte de implementacion", "Ruta clara de ROI", "Controles admin", "API lista", "Onboarding"], enterprisePricingTitle: "Paquetes para empezar simple y escalar", enterprisePricingText: "Paquetes starter, growth y enterprise.", enterpriseDemoTitle: "Agenda una demo o revision de solucion", enterpriseDemoText: "Envia tamano del equipo, herramientas actuales, problema y contacto.", enterpriseTimelines: ["Demo primero", "Setup 2 semanas", "API lista", "Implementacion guiada"] },
      fr: { enterprisePlatform: "Plateforme entreprise", requestDemo: "Demander une demo", viewSolutions: "Voir les solutions", solutions: "Solutions", useCases: "Cas d'usage", integrations: "Integrations", customPlan: "Plan personnalise", enterpriseReady: "Pret entreprise", integrationReady: "Pret integration", roiFocused: "Oriente ROI", enterpriseSolutionsTitle: "Solutions pour equipes operationnelles", enterpriseUseCasesTitle: "Cas d'usage par workflow", enterpriseUseCasesText: "Montrez l'adaptation aux equipes cles.", enterpriseUseCaseItems: ["Automatiser le travail manuel", "Centraliser les rapports", "Connecter les outils", "Ameliorer la visibilite", "Reduire les delais", "Scaler les operations"], enterpriseIntegrationsTitle: "Integrations et deploiement", enterpriseIntegrationsText: "APIs, CRM, ERP, paiements, analytics et support.", enterpriseIntegrationItems: ["CRM", "ERP", "Paiements", "Analytics", "Support", "API custom"], enterpriseProofTitle: "Preuves avant la demo", enterpriseProofText: "Securite, deploiement, ROI et support.", enterpriseProofItems: ["Workflows securises", "Support deploiement", "ROI clair", "Controles admin", "API prete", "Onboarding"], enterprisePricingTitle: "Packages simples a scaler", enterprisePricingText: "Starter, growth et enterprise.", enterpriseDemoTitle: "Reserver une demo ou analyse solution", enterpriseDemoText: "Envoyez equipe, outils, probleme et contact.", enterpriseTimelines: ["Demo d'abord", "Setup 2 semaines", "API prete", "Deploiement guide"] },
      pt: { enterprisePlatform: "Plataforma empresarial", requestDemo: "Solicitar demo", viewSolutions: "Ver solucoes", solutions: "Solucoes", useCases: "Casos de uso", integrations: "Integracoes", customPlan: "Plano personalizado", enterpriseReady: "Pronto para empresa", integrationReady: "Pronto para integrar", roiFocused: "Focado em ROI", enterpriseSolutionsTitle: "Solucoes para equipes operacionais", enterpriseUseCasesTitle: "Casos de uso por workflow", enterpriseUseCasesText: "Mostre como a solucao se encaixa em equipes chave.", enterpriseUseCaseItems: ["Automatizar trabalho manual", "Centralizar relatorios", "Conectar ferramentas", "Melhorar visibilidade", "Reduzir atrasos", "Escalar operacoes"], enterpriseIntegrationsTitle: "Integracoes e implementacao", enterpriseIntegrationsText: "APIs, CRM, ERP, pagamentos, analytics e suporte.", enterpriseIntegrationItems: ["CRM", "ERP", "Pagamentos", "Analytics", "Suporte", "API custom"], enterpriseProofTitle: "Provas antes da demo", enterpriseProofText: "Seguranca, implementacao, ROI e suporte.", enterpriseProofItems: ["Workflows seguros", "Suporte de implementacao", "ROI claro", "Controles admin", "API pronta", "Onboarding"], enterprisePricingTitle: "Pacotes para comecar simples e escalar", enterprisePricingText: "Starter, growth e enterprise.", enterpriseDemoTitle: "Agende uma demo ou revisao da solucao", enterpriseDemoText: "Envie equipe, ferramentas, problema e contato.", enterpriseTimelines: ["Demo primeiro", "Setup 2 semanas", "API pronta", "Implementacao guiada"] }
    };
    const industrialLabels = {
      en: { industrialSupplier: "Industrial supplier", requestQuote: "Request quote", viewSpecs: "View specs", capabilities: "Capabilities", certifications: "Certifications", supplyChain: "Supply chain", quoteRequired: "Quote required", certified: "Certified", bulkReady: "Bulk-ready", specReady: "Spec-ready", moqReady: "MOQ-ready", industrialCapabilitiesTitle: "Capabilities buyers need to verify", industrialCapabilitiesText: "Custom specs, bulk ordering, quality control and B2B support.", industrialCapabilityItems: ["Custom specs", "Bulk ordering", "Quality control", "Fast sourcing", "Replacement parts", "B2B support"], industrialCertificationsTitle: "Certifications, quality and documentation", industrialCertificationsText: "Documentation, safety data, warranty and compliance notes.", industrialCertificationItems: ["ISO-ready docs", "Material specs", "Warranty notes", "Safety data", "Compliance support", "Batch tracking"], industrialSupplyTitle: "Supply chain and fulfillment", industrialSupplyText: "Lead times, MOQ planning, freight support and repeat orders.", industrialSupplyItems: ["Lead times", "MOQ planning", "Freight support", "Warehouse-ready", "Vendor sourcing", "Repeat orders"], industrialQuoteTitle: "Request a quote with specs", industrialQuoteText: "Send product, quantities, material, deadline and delivery needs.", industrialLeadTimes: ["Quote first", "Bulk order", "2-4 week lead", "Custom spec"], industrialMaterials: ["Steel", "Aluminum", "Composite", "Safety-rated"] },
      es: { industrialSupplier: "Proveedor industrial", requestQuote: "Solicitar cotizacion", viewSpecs: "Ver especificaciones", capabilities: "Capacidades", certifications: "Certificaciones", supplyChain: "Cadena de suministro", quoteRequired: "Cotizacion requerida", certified: "Certificado", bulkReady: "Listo por volumen", specReady: "Specs listas", industrialCapabilitiesTitle: "Capacidades que compras necesita validar", industrialCapabilitiesText: "Specs personalizadas, compras por volumen, control de calidad y soporte B2B.", industrialCapabilityItems: ["Specs personalizadas", "Ordenes por volumen", "Control de calidad", "Sourcing rapido", "Repuestos", "Soporte B2B"], industrialCertificationsTitle: "Certificaciones, calidad y documentacion", industrialCertificationsText: "Documentacion, datos de seguridad, garantia y cumplimiento.", industrialCertificationItems: ["Docs ISO", "Specs de material", "Garantia", "Datos de seguridad", "Soporte compliance", "Trazabilidad"], industrialSupplyTitle: "Suministro y fulfillment", industrialSupplyText: "Tiempos, MOQ, flete y ordenes recurrentes.", industrialSupplyItems: ["Tiempos de entrega", "Plan MOQ", "Soporte de flete", "Listo para almacen", "Sourcing", "Ordenes repetidas"], industrialQuoteTitle: "Solicita una cotizacion con especificaciones", industrialQuoteText: "Envia producto, cantidades, material, fecha limite y entrega.", industrialLeadTimes: ["Cotizar primero", "Orden por volumen", "2-4 semanas", "Spec personalizada"], industrialMaterials: ["Acero", "Aluminio", "Compuesto", "Certificado"] },
      fr: { industrialSupplier: "Fournisseur industriel", requestQuote: "Demander un devis", viewSpecs: "Voir les specs", capabilities: "Capacites", certifications: "Certifications", supplyChain: "Supply chain", quoteRequired: "Devis requis", certified: "Certifie", bulkReady: "Pret volume", specReady: "Specs pretes", industrialCapabilitiesTitle: "Capacites a verifier", industrialCapabilitiesText: "Specs custom, commandes volume, qualite et support B2B.", industrialCapabilityItems: ["Specs custom", "Commandes volume", "Controle qualite", "Sourcing rapide", "Pieces rechange", "Support B2B"], industrialCertificationsTitle: "Certifications, qualite et documentation", industrialCertificationsText: "Docs, securite, garantie et conformite.", industrialCertificationItems: ["Docs ISO", "Specs materiaux", "Garantie", "Donnees securite", "Support conformite", "Tracabilite"], industrialSupplyTitle: "Supply chain et fulfillment", industrialSupplyText: "Delais, MOQ, fret et commandes recurrentes.", industrialSupplyItems: ["Delais", "Plan MOQ", "Support fret", "Pret entrepot", "Sourcing", "Commandes repetees"], industrialQuoteTitle: "Demander un devis avec specs", industrialQuoteText: "Envoyez produit, quantites, materiau, delai et livraison.", industrialLeadTimes: ["Devis d'abord", "Commande volume", "2-4 semaines", "Spec custom"], industrialMaterials: ["Acier", "Aluminium", "Composite", "Certifie"] },
      pt: { industrialSupplier: "Fornecedor industrial", requestQuote: "Solicitar cotacao", viewSpecs: "Ver especificacoes", capabilities: "Capacidades", certifications: "Certificacoes", supplyChain: "Cadeia de suprimentos", quoteRequired: "Cotacao requerida", certified: "Certificado", bulkReady: "Pronto para volume", specReady: "Specs prontas", industrialCapabilitiesTitle: "Capacidades que compras precisa validar", industrialCapabilitiesText: "Specs customizadas, compras em volume, qualidade e suporte B2B.", industrialCapabilityItems: ["Specs customizadas", "Pedidos em volume", "Controle de qualidade", "Sourcing rapido", "Pecas reposicao", "Suporte B2B"], industrialCertificationsTitle: "Certificacoes, qualidade e documentacao", industrialCertificationsText: "Documentacao, seguranca, garantia e compliance.", industrialCertificationItems: ["Docs ISO", "Specs de material", "Garantia", "Dados seguranca", "Suporte compliance", "Rastreabilidade"], industrialSupplyTitle: "Suprimentos e fulfillment", industrialSupplyText: "Prazos, MOQ, frete e pedidos recorrentes.", industrialSupplyItems: ["Prazos", "Planejamento MOQ", "Suporte frete", "Pronto para estoque", "Sourcing", "Pedidos repetidos"], industrialQuoteTitle: "Solicite cotacao com especificacoes", industrialQuoteText: "Envie produto, quantidades, material, prazo e entrega.", industrialLeadTimes: ["Cotar primeiro", "Pedido volume", "2-4 semanas", "Spec custom"], industrialMaterials: ["Aco", "Aluminio", "Composto", "Certificado"] }
    };
    return { ...labels.en, ...labels[language] || {}, ...professionalLabels.en, ...professionalLabels[language] || {}, ...enterpriseLabels.en, ...enterpriseLabels[language] || {}, ...industrialLabels.en, ...industrialLabels[language] || {} };
  }
  function renderFeatureBand(section, schema) {
    const editable = section.editable || {};
    const variant = section.variant || section.settings?.layout || "feature_band";
    const image = editable.image_url || marketplaceItems(schema).find((item) => item.image_url)?.image_url || "";
    const heading = `<div class="section-heading">
      <span class="rendered-kicker">${escapeHtml(section.type)}</span>
      <h2>${escapeHtml(editable.title || editable.headline || section.type)}</h2>
      <p>${escapeHtml(editable.text || editable.subtitle || "")}</p>
    </div>`;
    if (variant === "card_grid") {
      const items = Array.isArray(editable.items) && editable.items.length ? editable.items : marketplaceItems(schema).slice(0, 3).map((item) => item.name);
      return `<section class="rendered-section feature-band feature-card-grid ${sectionClass(section)}" ${sectionAttrs(section)}>
      ${heading}<div class="feature-band-cards">${items.slice(0, 3).map((item) => `<article><strong>${escapeHtml(item.name || item)}</strong><span>${escapeHtml(item.description || editable.text || "")}</span></article>`).join("")}</div>
    </section>`;
    }
    const visual = image ? `<div class="feature-band-visual"><img src="${escapeAttribute(image)}" alt=""></div>` : "";
    return `<section class="rendered-section feature-band feature-${escapeAttribute(slugify(variant))} ${sectionClass(section)}" ${sectionAttrs(section)}>
    ${variant === "image_left" ? `${visual}${heading}` : `${heading}${visual}`}
  </section>`;
  }
  function renderGallery(section, schema) {
    const editable = section.editable || {};
    const images = Array.isArray(editable.images) && editable.images.length ? editable.images : marketplaceItems(schema).map((item) => item.image_url).filter(Boolean).slice(0, 3);
    return `<section class="rendered-section ${sectionClass(section)}" ${sectionAttrs(section)}>
    <h2>${escapeHtml(editable.title || "Gallery")}</h2>
    <div class="rendered-grid">${images.map((url) => `<article class="rendered-card"><img src="${escapeAttribute(url)}" alt=""></article>`).join("")}</div>
  </section>`;
  }
  function renderContact(section, schema) {
    const editable = section.editable || {};
    return `<section class="rendered-section contact-panel ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div>
      <span class="rendered-kicker">${escapeHtml(schema.business.location || "Contact")}</span>
      <h2>${escapeHtml(editable.title || "Contact")}</h2>
      <p>${escapeHtml(editable.text || "Reach out for more information.")}</p>
    </div>
    <div class="contact-list">${Object.entries(schema.contact || {}).filter(([, value]) => value).map(([key, value]) => `<p><strong>${escapeHtml(key)}</strong><span>${escapeHtml(value)}</span></p>`).join("")}</div>
  </section>`;
  }
  function visualPlaceholder(schema) {
    const initials = schema.business.name.split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]).join("");
    return `<div class="visual-placeholder">
    <span>${escapeHtml(initials || "AI")}</span>
    <small>${escapeHtml(schema.active_design_variant?.background_style || schema.business.industry || "Brand preview")}</small>
  </div>`;
  }
  function renderLogoMark(schema) {
    const initials = schema.business.name.split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]).join("");
    return `<div class="logo-lockup">
    <span>${escapeHtml(initials || "M")}</span>
    <div>
      <strong>${escapeHtml(schema.business.name)}</strong>
      <small>${escapeHtml(schema.active_design_variant?.logo_style || "brand mark")}</small>
    </div>
  </div>`;
  }
  function sectionClass(section) {
    const headingSize = slugify(section.settings?.heading_size || "medium");
    const spacing = slugify(section.settings?.spacing || "balanced");
    const container = slugify(section.settings?.container_width || "standard");
    const density = slugify(section.settings?.card_density || "comfortable");
    const gap = slugify(section.settings?.card_gap || "comfortable");
    const variant = slugify(section.variant || "default");
    return `heading-${headingSize} spacing-${spacing} container-${container} density-${density} gap-${gap} variant-${variant}`;
  }
  function sectionVars(section) {
    const styles = [];
    if (section.settings?.background) {
      styles.push(`--section-bg:${resolveColor(section.settings.background, section.settings.background)}`);
    }
    if (section.settings?.text_color) {
      styles.push(`--section-text:${resolveColor(section.settings.text_color, section.settings.text_color)}`);
    }
    return styles.length ? `style="${escapeAttribute(styles.join(";"))}"` : "";
  }
  function sectionAttrs(section) {
    const attrs = [
      `data-studio-section="${escapeAttribute(section.id || section.type || "section")}"`,
      `data-studio-section-type="${escapeAttribute(section.type || "Section")}"`,
      `data-studio-section-variant="${escapeAttribute(section.variant || "default")}"`
    ];
    const vars = sectionVars(section);
    if (vars) attrs.push(vars);
    return attrs.join(" ");
  }
  var FALLBACK_TYPE_SCALE = {
    "--type-caption-size": "clamp(0.5501rem, 0.5314rem + 0.0832vw, 0.5980rem)",
    "--type-small-size": "clamp(0.7492rem, 0.7305rem + 0.0832vw, 0.7971rem)",
    "--type-label-size": "clamp(0.8466rem, 0.8178rem + 0.1280vw, 0.9203rem)",
    "--type-body-size": "clamp(1.0094rem, 0.9886rem + 0.0924vw, 1.0625rem)",
    "--type-button-size": "clamp(1.0732rem, 1.0464rem + 0.1191vw, 1.1417rem)",
    "--type-h6-size": "clamp(1.2464rem, 1.1798rem + 0.2956vw, 1.4163rem)",
    "--type-h5-size": "clamp(1.4616rem, 1.3361rem + 0.5580vw, 1.7825rem)",
    "--type-h4-size": "clamp(1.7049rem, 1.4942rem + 0.9363vw, 2.2433rem)",
    "--type-h3-size": "clamp(2.0334rem, 1.6590rem + 1.6642vw, 2.9903rem)",
    "--type-h2-size": "clamp(2.6176rem, 1.9898rem + 2.7902vw, 4.2220rem)",
    "--type-h1-size": "clamp(3.7448rem, 2.5935rem + 5.1171vw, 6.6872rem)",
    "--type-h1-line-height": "1.05",
    "--type-h1-tracking": "-0.02em",
    "--type-h1-weight": "700",
    "--type-h2-line-height": "1.1",
    "--type-h2-tracking": "-0.02em",
    "--type-h2-weight": "700",
    "--type-h3-line-height": "1.15",
    "--type-h3-tracking": "-0.01em",
    "--type-h3-weight": "600",
    "--type-h4-line-height": "1.2",
    "--type-h4-tracking": "0em",
    "--type-h4-weight": "600",
    "--type-h5-line-height": "1.25",
    "--type-h5-tracking": "0em",
    "--type-h5-weight": "600",
    "--type-h6-line-height": "1.3",
    "--type-h6-tracking": "0em",
    "--type-h6-weight": "600",
    "--type-body-line-height": "1.65",
    "--type-body-weight": "400",
    "--type-small-line-height": "1.55",
    "--type-small-weight": "400",
    "--type-caption-line-height": "1.45",
    "--type-caption-weight": "400",
    "--type-label-line-height": "1.2",
    "--type-label-weight": "600",
    "--type-label-tracking": "0.08em",
    "--type-label-transform": "uppercase",
    "--type-button-line-height": "1.2",
    "--type-button-weight": "600",
    "--type-button-tracking": "0.01em"
  };
  var GOOGLE_FONT_WEIGHTS = { heading: "400;500;600;700;800", body: "400;500;600" };
  var lastLoadedFontPairKey = "";
  function googleFontFamilyParam(fontName, weights) {
    const family = String(fontName || "").trim();
    if (!family) return "";
    return `family=${encodeURIComponent(family).replace(/%20/g, "+")}:wght@${weights}`;
  }
  function renderCourseOffering(section, schema) {
    const editable = section.editable || {};
    const embedUrl = safeVideoEmbedUrl(editable.videoUrl || editable.video_url || "");
    const includes = arrayValue(editable.includes).slice(0, 8);
    const actionPage = editable.ctaMode === "purchase" ? "catalog" : "contact";
    return `<section class="course-offering ${sectionClass(section)}" ${sectionAttrs(section)}>
    <div class="course-offering-media">
      ${embedUrl ? `<div class="video-embed"><iframe src="${escapeAttribute(embedUrl)}" title="${escapeAttribute(editable.title || "Course")}" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div>` : `<div class="course-video-placeholder"><span>${escapeHtml(schema.business?.name || "LYRA")}</span><strong>${escapeHtml(editable.title || "Course")}</strong></div>`}
    </div>
    <div class="course-offering-copy">
      <span class="rendered-kicker">${escapeHtml(editable.audience || "Course")}</span>
      <h2>${escapeHtml(editable.title || "Course")}</h2>
      <p>${escapeHtml(editable.description || editable.text || "")}</p>
      ${includes.length ? `<ul>${includes.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>` : ""}
      <div class="course-offering-action">
        ${editable.priceLabel ? `<strong>${escapeHtml(editable.priceLabel)}</strong>` : ""}
        <a class="rendered-button" href="#" data-page-link="${actionPage}">${escapeHtml(editable.ctaLabel || "Learn more")}</a>
      </div>
    </div>
  </section>`;
  }
  function catalogSearchAttributes(item = {}) {
    const searchText = [item.name, item.category, item.description, item.sku].filter(Boolean).join(" ").toLowerCase();
    return `data-catalog-item data-catalog-search="${escapeAttribute(searchText)}" data-catalog-item-category="${escapeAttribute(String(item.category || "").toLowerCase())}"`;
  }
  function ensureGoogleFontsLoaded(theme = {}) {
    if (typeof document === "undefined") return;
    const heading = theme.fonts?.heading || "Inter";
    const body = theme.fonts?.body || "Inter";
    const pairKey = `${heading}|${body}`;
    if (pairKey === lastLoadedFontPairKey) return;
    lastLoadedFontPairKey = pairKey;
    if (!document.querySelector('link[data-lyra-font-preconnect="googleapis"]')) {
      const preconnect1 = document.createElement("link");
      preconnect1.rel = "preconnect";
      preconnect1.href = "https://fonts.googleapis.com";
      preconnect1.dataset.lyraFontPreconnect = "googleapis";
      document.head.appendChild(preconnect1);
      const preconnect2 = document.createElement("link");
      preconnect2.rel = "preconnect";
      preconnect2.href = "https://fonts.gstatic.com";
      preconnect2.crossOrigin = "anonymous";
      preconnect2.dataset.lyraFontPreconnect = "gstatic";
      document.head.appendChild(preconnect2);
    }
    const seen = /* @__PURE__ */ new Set();
    const families = [];
    [[heading, GOOGLE_FONT_WEIGHTS.heading], [body, GOOGLE_FONT_WEIGHTS.body]].forEach(([name, weights]) => {
      const key = name.toLowerCase();
      if (seen.has(key)) return;
      seen.add(key);
      const param = googleFontFamilyParam(name, weights);
      if (param) families.push(param);
    });
    if (!families.length) return;
    const href = `https://fonts.googleapis.com/css2?${families.join("&")}&display=swap`;
    let link = document.querySelector("link[data-lyra-fonts]");
    if (!link) {
      link = document.createElement("link");
      link.rel = "stylesheet";
      link.dataset.lyraFonts = "true";
      document.head.appendChild(link);
    }
    link.href = href;
  }
  function themeVars(theme = {}, brandInput = null) {
    theme.colors = theme.colors || {};
    theme.fonts = theme.fonts || {};
    const brand = normalizeBrandForTheme(brandInput || { colors: theme.colors, fontPairing: theme.fonts, borderRadius: `${theme.radius || 10}px`, shadowStyle: theme.shadow });
    const colors = brandToThemeColorsForTheme(brand);
    const typeScale = Object.keys(theme.fonts.scale || {}).length ? theme.fonts.scale : FALLBACK_TYPE_SCALE;
    const typeVars = Object.entries(typeScale).map(([key, value]) => `${key}:${value}`);
    return [
      `--brand-primary:${colors.primary}`,
      `--brand-secondary:${colors.secondary}`,
      `--brand-accent:${colors.accent}`,
      `--brand-background:${colors.background}`,
      `--brand-surface:${colors.surface}`,
      `--brand-text:${colors.text}`,
      `--brand-muted:${colors.muted}`,
      `--brand-border:${colors.border}`,
      `--brand-button:${colors.button}`,
      `--brand-button-text:${colors.buttonText}`,
      `--brand-radius:${brand.borderRadius}`,
      `--brand-shadow:${brand.shadowStyle}`,
      `--site-bg:${colors.background}`,
      `--site-surface:${colors.surface}`,
      `--site-primary:${colors.primary}`,
      `--site-secondary:${colors.secondary}`,
      `--site-accent:${colors.accent}`,
      `--site-text:${colors.text}`,
      `--site-success:${colors.success}`,
      `--site-warning:${colors.warning}`,
      `--site-error:${colors.error}`,
      `--site-info:${colors.info}`,
      `--site-muted:${colors.muted}`,
      `--site-border:${colors.border}`,
      `--site-button:${colors.button}`,
      `--site-button-text:${colors.buttonText}`,
      `--site-heading:${JSON.stringify(brand.fontPairing.heading || "Inter")}`,
      `--site-body:${JSON.stringify(brand.fontPairing.body || "Inter")}`,
      ...typeVars
    ].join(";");
  }
  function resolveColor(value, fallback) {
    const raw = String(value || "").trim();
    if (!raw) return fallback;
    if (/^#([0-9a-f]{3}|[0-9a-f]{6}|[0-9a-f]{8})$/i.test(raw)) return raw;
    if (/^(rgb|hsl)a?\(/i.test(raw)) return raw;
    const normalized = raw.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "");
    const palettes = [
      [["negro", "black", "oscuro", "noir"], "#111111"],
      [["dorado", "gold", "oro"], "#C89B3C"],
      [["marfil", "ivory", "crema", "cream"], "#F7F1E7"],
      [["beige", "arena", "sand"], "#E8D9C5"],
      [["verde profundo", "verde elegante", "emerald", "esmeralda"], "#0F5E46"],
      [["verde", "natural", "botanico", "organico"], "#2F6F4E"],
      [["azul confianza", "azul corporativo", "navy", "marino"], "#163B73"],
      [["azul", "blue"], "#2563EB"],
      [["rosa pastel", "pastel rosa", "rose", "rosado"], "#E8A7B8"],
      [["pastel", "soft"], "#F5D7E3"],
      [["rojo", "red"], "#B42318"],
      [["vino", "burgundy", "burdeos"], "#7A263A"],
      [["neon", "electrico"], "#39FF88"],
      [["morado", "purple", "lila"], "#6D4AFF"],
      [["minimalista", "minimal", "limpio"], "#F8FAFC"],
      [["lujo", "luxury", "premium"], "#14110F"]
    ];
    const match = palettes.find(([words]) => words.some((word) => normalized.includes(word)));
    return match ? match[1] : fallback;
  }

  // src/ai-builder/state.js
  function normalizeBrowserLanguage(input) {
    const normalized = String(input || "en").trim().toLowerCase();
    const base = normalized.split("-")[0];
    return SUPPORTED_LANGUAGES.includes(base) ? base : "en";
  }
  function detectBrowserLanguage() {
    const languages = navigator.languages && navigator.languages.length ? navigator.languages : [navigator.language];
    return normalizeBrowserLanguage(languages.find(Boolean) || "en");
  }
  function createEmptyGuidedState(language = detectBrowserLanguage()) {
    return {
      websiteIntent: "",
      businessName: "",
      businessDescription: "",
      industry: "",
      location: "",
      servicesProducts: [],
      targetAudience: "",
      preferredTone: "",
      preferredColors: [],
      contactInfo: {},
      logoUrl: "",
      photoUrls: [],
      videoUrls: [],
      logoPalette: [],
      colorProvenance: null,
      brand: null,
      selectedLanguage: language,
      hasLogo: false,
      hasPhotos: false,
      salesMode: "",
      salesFlow: "",
      hasLogoPhotos: "",
      aiGeneratedLogoRequested: false,
      logoPreference: "",
      sectionsPreference: "",
      desiredDomain: "",
      revisionMode: "",
      designVariantOffset: 0,
      requestedAdjustments: [],
      sitePlan: null,
      sitePlanApproved: false,
      fieldMeta: {},
      generatedSiteId: "",
      projectId: "",
      aiStudioPlan: null,
      designStrategy: null
    };
  }
  var initialSelectedLanguage = detectBrowserLanguage();
  var builderState = {
    selectedLanguage: initialSelectedLanguage,
    currentSchema: null,
    selectedPageKey: "home",
    selectedVariantId: "",
    selectedStudioSectionId: "",
    advancedInspectorOpen: false,
    currentSiteId: null,
    currentBusinessId: null,
    currentGenerationId: null,
    currentRequestId: null,
    currentCatalogItems: [],
    isGeneratingWebsite: false,
    hasBackendIntakeSignal: false,
    backendReadyToGenerate: false,
    backendMissingFields: [],
    guidedStep: "websiteIntent",
    // Backend-reported field (mapped) that the last assistant question was
    // actually about - see mapBackendSlotToGuidedField in index.js. Preferred
    // over guidedStep for attributing a reply to a field, since guidedStep is
    // a separate local sequence used only for the progress UI.
    lastAskedGuidedField: "",
    guidedHistory: [],
    assistantState: "neutral",
    assistantVoiceEnabled: localStorage.getItem("gnuDevAssistantVoice") === "on",
    forcedTemplateSelection: null,
    restoredGuidedDraftInfo: null,
    restoredDraftNoticeCard: null,
    restoredDraftNoticeShown: false,
    guidedBuildStatusCard: null,
    guidedCoachCard: null,
    liveSitePreviewCard: null,
    templateBoardLoading: false,
    templateBoardLoadingSignature: "",
    templateBoardLoadingTimer: null,
    guidedSendLocked: false,
    guidedLastSendAt: 0,
    clientIntakeSession: null,
    clientIntakeSyncTimer: null,
    clientIntakeSyncInFlight: false,
    clientIntakeLastSyncedSnapshot: "",
    clientAccountButton: null,
    clientProjectsButton: null,
    clientProjectsPanel: null,
    clientProjects: [],
    clientWorkspaceIdleTimer: null,
    clientWorkspaceUnlocked: false,
    clientAuthResumePromise: null,
    studioAuthRedirectCaptureComplete: false,
    guidedState: createEmptyGuidedState(initialSelectedLanguage),
    pendingServerIntakeGate: null,
    lastAssistantPromptSignature: "",
    builderAvatarAssistant: null
  };

  // src/ai-builder/dom.js
  var isLegacyBuilderPage = Boolean(document.querySelector("#intakeForm"));
  var form2 = document.querySelector("#intakeForm");
  var statusText = document.querySelector("#statusText");
  var storageStatus2 = document.querySelector("#storageStatus");
  var siteTitle2 = document.querySelector("#siteTitle");
  var editorMount = document.querySelector("#editorMount");
  var previewFrame2 = document.querySelector("#previewFrame");
  var studioSelectionToolbar = document.querySelector("#studioSelectionToolbar");
  var studioProgressList = document.querySelector("#studioProgressList");
  var studioAdjustButton = document.querySelector("#studioAdjustButton");
  var studioLumaMessage = document.querySelector(".studio-luma-message");
  var studioSuggestedList = document.querySelector("#studioSuggestedList");
  var studioRecentList = document.querySelector("#studioRecentList");
  var studioAuthGate = document.querySelector("#studioAuthGate");
  var studioAuthCloseButton = document.querySelector("#studioAuthCloseButton");
  var studioGoogleAuthButton = document.querySelector("#studioGoogleAuthButton");
  var studioAppleAuthButton = document.querySelector("#studioAppleAuthButton");
  var studioEmailAuthButton = document.querySelector("#studioEmailAuthButton");
  var studioEmailAuthForm = document.querySelector("#studioEmailAuthForm");
  var studioAuthEmail = document.querySelector("#studioAuthEmail");
  var studioAuthDemoButton = document.querySelector("#studioAuthDemoButton");
  var quickModeButton2 = document.querySelector("#quickModeButton");
  var guidedModeButton2 = document.querySelector("#guidedModeButton");
  var guidedPanel2 = document.querySelector("#guidedPanel");
  var guidedChatCard = document.querySelector(".guided-chat-card");
  var liveSitePreviewMount = document.querySelector("#liveSitePreviewMount");
  var guidedChat = document.querySelector("#guidedChat");
  var guidedReply2 = document.querySelector("#guidedReply");
  var guidedSendButton = document.querySelector("#guidedSendButton");
  var guidedStatusText = document.querySelector("#guidedStatusText");
  var guidedStepLabel = document.querySelector("#guidedStepLabel");
  var guidedGenerateButton = document.querySelector("#guidedGenerateButton");
  var guidedCloseButton = document.querySelector("#guidedCloseButton");
  var guidedMicButton = document.querySelector("#guidedMicButton");
  var assistantAudioToggle = document.querySelector("#assistantAudioToggle");
  var voiceStatusText = document.querySelector("#voiceStatusText");
  var guidedThinking = document.querySelector("#guidedThinking");
  var guidedProgressBar = document.querySelector("#guidedProgressBar");
  var reviewDetailsButton = document.querySelector("#reviewDetailsButton");
  var keepChattingButton = document.querySelector("#keepChattingButton");
  var currentInfoPreview = document.querySelector("#currentInfoPreview");
  var currentInfoMeta = document.querySelector("#currentInfoMeta");
  var quickChipRow = document.querySelector("#quickChipRow");
  var guidedLogoUpload = document.querySelector("#guidedLogoUpload");
  var guidedPhotoUpload = document.querySelector("#guidedPhotoUpload");
  var guidedLogoPreview = document.querySelector("#guidedLogoPreview");
  var guidedPhotoPreview = document.querySelector("#guidedPhotoPreview");
  var brandKitPanel = document.querySelector("#brandKitPanel");
  var guidedAssetPrompt = document.querySelector("#guidedAssetPrompt");
  var chatLogoUploadButton = document.querySelector("#chatLogoUploadButton");
  var chatPhotoUploadButton = document.querySelector("#chatPhotoUploadButton");
  var guidedBriefReview = document.querySelector("#guidedBriefReview");
  var summaryLogoUploadButton = document.querySelector("#summaryLogoUploadButton");
  var summaryPhotoUploadButton = document.querySelector("#summaryPhotoUploadButton");
  var editDetailsButton = document.querySelector("#editDetailsButton");
  var checkDomainButton = document.querySelector("#checkDomainButton");
  var domainCheckStatus = document.querySelector("#domainCheckStatus");
  var domainResults = document.querySelector("#domainResults");
  var languageSelector = document.querySelector("#languageSelector");
  var summaryLanguageSelector = document.querySelector("#summaryLanguageSelector");
  var isPublicClientSetup = document.body.dataset.context === "client-setup";
  var isEmbeddedClientSetup = new URLSearchParams(window.location.search).get("embedded") === "1";
  var switchManualFormButton = document.querySelector("#switchManualFormButton");
  var backToChatButton = document.querySelector("#backToChatButton");
  var submitDraftReviewButton = document.querySelector("#submitDraftReviewButton");
  var adjustWithLumaButton = document.querySelector("#adjustWithLumaButton");
  var startNewProjectButton = document.querySelector("#startNewProjectButton");
  var startNewGeneratedProjectButton = document.querySelector("#startNewGeneratedProjectButton");
  var guidedHeaderActions = document.querySelector(".guided-header-actions");
  var builderAvatarRoot = document.querySelector("#builderAvatarAssistant");
  var builderAvatarManager = window.AvatarStateManager ? new window.AvatarStateManager("idle") : null;

  // src/ai-builder/supabase-magic-link.js
  function readSupabaseAuthRedirect(locationLike = globalThis.location) {
    const hashParams = new URLSearchParams(String(locationLike?.hash || "").replace(/^#/, ""));
    const queryParams = new URLSearchParams(String(locationLike?.search || ""));
    return {
      accessToken: hashParams.get("access_token") || queryParams.get("access_token") || "",
      refreshToken: hashParams.get("refresh_token") || queryParams.get("refresh_token") || "",
      type: hashParams.get("type") || queryParams.get("type") || ""
    };
  }
  async function requestSupabaseMagicLink({
    email,
    redirectTo,
    projectUrl,
    anonKey,
    fetchImpl = globalThis.fetch
  }) {
    try {
      const response = await fetchImpl(`${String(projectUrl || "").replace(/\/$/, "")}/auth/v1/otp`, {
        method: "POST",
        headers: {
          apikey: anonKey,
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          email,
          options: {
            emailRedirectTo: redirectTo,
            shouldCreateUser: true
          }
        })
      });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        const message = payload?.msg || payload?.message || payload?.error_description || payload?.error || `Request failed (${response.status})`;
        return {
          ok: false,
          status: response.status,
          message: String(message),
          isRateLimited: response.status === 429 || /rate|wait|seconds|retry/i.test(String(message))
        };
      }
      return { ok: true, status: response.status, email };
    } catch (error) {
      return {
        ok: false,
        status: 0,
        message: error instanceof Error ? error.message : String(error || "Network request failed"),
        isRateLimited: false
      };
    }
  }
  function magicLinkFeedback(result, email, language = "en") {
    const copy = {
      en: {
        title: "Check your email",
        sent: `We sent a sign-in link to ${email}. Open it in this browser to continue.`,
        rate: "Please wait a moment before trying again."
      },
      es: {
        title: "Revisa tu correo",
        sent: `Te enviamos un enlace de acceso a ${email}. \xC1brelo en este navegador para continuar.`,
        rate: "Espera un momento antes de reintentar."
      },
      fr: {
        title: "Consultez votre email",
        sent: `Nous avons envoy\xE9 un lien de connexion \xE0 ${email}. Ouvrez-le dans ce navigateur pour continuer.`,
        rate: "Veuillez patienter un instant avant de r\xE9essayer."
      },
      pt: {
        title: "Confira seu email",
        sent: `Enviamos um link de acesso para ${email}. Abra-o neste navegador para continuar.`,
        rate: "Espere um momento antes de tentar novamente."
      }
    };
    const selected = copy[language] || copy.en;
    if (result?.ok) return { kind: "success", title: selected.title, message: selected.sent };
    return {
      kind: "error",
      title: "",
      message: result?.isRateLimited ? selected.rate : String(result?.message || "Unable to send the sign-in link.")
    };
  }

  // src/ai-builder/auth.js
  var CLIENT_INTAKE_AUTOSAVE_DELAY_MS = 15e3;
  var CLIENT_AUTH_SLOW_NOTICE_DELAY_MS = 9e3;
  var MAGIC_LINK_RESEND_COOLDOWN_SECONDS = 60;
  var clientAuthSlowNoticeTimer = null;
  var magicLinkCooldownTimer = null;
  var magicLinkCooldownEndsAt = 0;
  var magicLinkResendEmail = "";
  function magicLinkElements() {
    return {
      confirmation: studioAuthGate?.querySelector("[data-magic-link-confirmation]") || null,
      title: studioAuthGate?.querySelector("[data-magic-link-confirmation] strong") || null,
      message: studioAuthGate?.querySelector("[data-magic-link-message]") || null,
      formError: studioAuthGate?.querySelector("[data-magic-link-error]") || null,
      resendError: studioAuthGate?.querySelector("[data-magic-link-resend-error]") || null,
      resendButton: studioAuthGate?.querySelector("[data-magic-link-resend]") || null
    };
  }
  function setMagicLinkError(element, message = "") {
    if (!element) return;
    element.textContent = message;
    element.hidden = !message;
  }
  function resetMagicLinkView() {
    clearInterval(magicLinkCooldownTimer);
    magicLinkCooldownTimer = null;
    magicLinkCooldownEndsAt = 0;
    magicLinkResendEmail = "";
    const { confirmation, formError, resendError } = magicLinkElements();
    if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
    const submitButton = studioEmailAuthForm?.querySelector("button[type='submit']") || null;
    if (submitButton) {
      submitButton.disabled = false;
      submitButton.textContent = t("sendMagicLink");
    }
    if (confirmation) confirmation.hidden = true;
    setMagicLinkError(formError);
    setMagicLinkError(resendError);
  }
  function updateMagicLinkCooldown() {
    const { resendButton } = magicLinkElements();
    if (!resendButton) return;
    const seconds = Math.max(0, Math.ceil((magicLinkCooldownEndsAt - Date.now()) / 1e3));
    resendButton.disabled = seconds > 0;
    resendButton.textContent = seconds > 0 ? langText({
      en: `Resend link (${seconds}s)`,
      es: `Reenviar enlace (${seconds}s)`,
      fr: `Renvoyer le lien (${seconds}s)`,
      pt: `Reenviar link (${seconds}s)`
    }) : langText({ en: "Resend link", es: "Reenviar enlace", fr: "Renvoyer le lien", pt: "Reenviar link" });
    if (seconds === 0) {
      clearInterval(magicLinkCooldownTimer);
      magicLinkCooldownTimer = null;
    }
  }
  function startMagicLinkCooldown(email) {
    magicLinkResendEmail = email;
    magicLinkCooldownEndsAt = Date.now() + MAGIC_LINK_RESEND_COOLDOWN_SECONDS * 1e3;
    clearInterval(magicLinkCooldownTimer);
    updateMagicLinkCooldown();
    magicLinkCooldownTimer = setInterval(updateMagicLinkCooldown, 1e3);
  }
  function ensureMagicLinkResendHandler() {
    const { resendButton } = magicLinkElements();
    if (!resendButton || resendButton.dataset.magicLinkBound === "true") return;
    resendButton.dataset.magicLinkBound = "true";
    resendButton.addEventListener("click", () => {
      if (!magicLinkResendEmail || Date.now() < magicLinkCooldownEndsAt) return;
      sendMagicLink(magicLinkResendEmail, { resend: true });
    });
  }
  async function sendMagicLink(email, { resend = false } = {}) {
    const { confirmation, title, message, formError, resendError, resendButton } = magicLinkElements();
    const submitButton = studioEmailAuthForm?.querySelector("button[type='submit']") || null;
    const activeButton = resend ? resendButton : submitButton;
    const previousText = activeButton?.textContent || "";
    setMagicLinkError(resend ? resendError : formError);
    if (activeButton) {
      activeButton.disabled = true;
      activeButton.textContent = langText({ en: "Sending...", es: "Enviando...", fr: "Envoi...", pt: "Enviando..." });
    }
    const result = await requestSupabaseMagicLink({
      email,
      redirectTo: window.location.href,
      projectUrl: SUPABASE_PROJECT_URL,
      anonKey: SUPABASE_ANON_KEY
    });
    const feedback = magicLinkFeedback(result, email, builderState.selectedLanguage);
    if (result.ok) {
      if (studioEmailAuthForm) studioEmailAuthForm.hidden = true;
      if (confirmation) confirmation.hidden = false;
      if (title) title.textContent = feedback.title;
      if (message) message.textContent = feedback.message;
      setMagicLinkError(resendError);
      startMagicLinkCooldown(email);
      if (storageStatus) storageStatus.textContent = feedback.message;
      if (guidedStatusText) guidedStatusText.textContent = feedback.message;
      return true;
    }
    setMagicLinkError(resend ? resendError : formError, feedback.message);
    if (resend) startMagicLinkCooldown(email);
    if (storageStatus) storageStatus.textContent = feedback.message;
    if (guidedStatusText) guidedStatusText.textContent = feedback.message;
    if (activeButton && !resend) {
      activeButton.disabled = false;
      activeButton.textContent = previousText;
    }
    return false;
  }
  function authLoadingElements() {
    return {
      content: studioAuthGate?.querySelector("[data-studio-auth-content]") || null,
      loading: studioAuthGate?.querySelector("[data-studio-auth-loading]") || null,
      title: studioAuthGate?.querySelector("[data-studio-auth-loading-title]") || null,
      message: studioAuthGate?.querySelector("[data-studio-auth-loading-message]") || null
    };
  }
  function resetStudioAuthLoading() {
    clearTimeout(clientAuthSlowNoticeTimer);
    clientAuthSlowNoticeTimer = null;
    const { content, loading } = authLoadingElements();
    if (content) content.hidden = false;
    if (loading) loading.hidden = true;
    studioAuthGate?.removeAttribute("aria-busy");
  }
  function showStudioAuthLoading() {
    if (!studioAuthGate) return;
    const { content, loading, title, message } = authLoadingElements();
    clearTimeout(clientAuthSlowNoticeTimer);
    studioAuthGate.hidden = false;
    studioAuthGate.setAttribute("aria-busy", "true");
    document.body.classList.add("studio-auth-open", "client-auth-required");
    if (content) content.hidden = true;
    if (loading) loading.hidden = false;
    if (title) title.textContent = langText({
      en: "Connecting your session",
      es: "Conectando tu sesi\xF3n",
      fr: "Connexion de votre session",
      pt: "Conectando sua sess\xE3o"
    });
    if (message) message.textContent = langText({
      en: "We are securely validating your access.",
      es: "Estamos validando tu acceso seguro.",
      fr: "Nous validons votre acc\xE8s s\xE9curis\xE9.",
      pt: "Estamos validando seu acesso seguro."
    });
    clientAuthSlowNoticeTimer = setTimeout(() => {
      if (studioAuthGate?.hidden || loading?.hidden) return;
      if (message) message.textContent = langText({
        en: "This is taking a little longer than usual. Your session is still being checked.",
        es: "Esto est\xE1 tardando un poco m\xE1s de lo normal. Tu sesi\xF3n se sigue verificando.",
        fr: "Cela prend un peu plus de temps que pr\xE9vu. Votre session est toujours en cours de v\xE9rification.",
        pt: "Isso est\xE1 demorando um pouco mais que o normal. Sua sess\xE3o continua sendo verificada."
      });
    }, CLIENT_AUTH_SLOW_NOTICE_DELAY_MS);
  }
  function initClientIntakeSessionGate() {
    if (!isPublicClientSetup) return;
    ensureStudioAuthRedirectCaptured();
    if (storedClientAccessToken()) {
      showStudioAuthLoading();
      resumeClientSessionFromAuthToken();
      return;
    }
    if (!isClientWorkspaceUnlocked()) {
      openStudioAuthGate("start");
      if (studioAuthDemoButton) studioAuthDemoButton.hidden = true;
      if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
      if (studioAuthEmail) {
        studioAuthEmail.value = builderState.guidedState.contactInfo?.email || localStorage.getItem("lumaPendingClientEmail") || "";
        setTimeout(() => studioAuthEmail.focus(), 80);
      }
      if (guidedStatusText) {
        guidedStatusText.textContent = langText({
          en: "Sign in first so LYRA can protect and save this workspace.",
          es: "Inicia sesi\xF3n primero para que LYRA proteja y guarde este espacio.",
          fr: "Connectez-vous d'abord pour que LYRA prot\xE8ge et sauvegarde cet espace.",
          pt: "Entre primeiro para a LYRA proteger e salvar este espa\xE7o."
        });
      }
      return;
    }
    const stored = readClientIntakeSession();
    if (stored?.clientEmail) {
      builderState.clientIntakeSession = stored;
      hydrateClientIntakeSession(stored, { silent: true });
      syncClientIntakeSession({ immediate: true, reason: "resume" });
      return;
    }
    openStudioAuthGate("start");
    if (studioAuthDemoButton) studioAuthDemoButton.hidden = true;
    if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
    if (studioEmailAuthButton) studioEmailAuthButton.hidden = true;
    revealStudioAuthProviderButtons();
    if (studioAuthEmail) {
      studioAuthEmail.value = builderState.guidedState.contactInfo?.email || localStorage.getItem("lumaPendingClientEmail") || "";
      setTimeout(() => studioAuthEmail.focus(), 80);
    }
    if (guidedStatusText) {
      guidedStatusText.textContent = langText({
        en: "Create or resume your workspace first, so LYRA can save every answer.",
        es: "Crea o recupera tu espacio primero, as\xED LYRA guarda cada respuesta.",
        fr: "Cr\xE9ez ou reprenez votre espace pour que LYRA sauvegarde chaque r\xE9ponse.",
        pt: "Crie ou recupere seu espa\xE7o primeiro para a LYRA salvar cada resposta."
      });
    }
  }
  function isClientWorkspaceUnlocked() {
    return builderState.clientWorkspaceUnlocked;
  }
  function markClientWorkspaceUnlocked() {
    builderState.clientWorkspaceUnlocked = true;
    scheduleClientWorkspaceAutoLock();
  }
  function clearClientWorkspaceUnlock() {
    builderState.clientWorkspaceUnlocked = false;
    clearTimeout(builderState.clientWorkspaceIdleTimer);
  }
  function initClientWorkspaceSecurity() {
    if (!isPublicClientSetup) return;
    scheduleClientWorkspaceAutoLock();
    ["pointerdown", "keydown", "touchstart"].forEach((eventName) => {
      window.addEventListener(eventName, scheduleClientWorkspaceAutoLock, { passive: true });
    });
    window.addEventListener("pagehide", () => {
      clearClientWorkspaceUnlock();
    });
  }
  function scheduleClientWorkspaceAutoLock() {
    if (!isPublicClientSetup) return;
    clearTimeout(builderState.clientWorkspaceIdleTimer);
    if (!isClientWorkspaceUnlocked()) return;
    builderState.clientWorkspaceIdleTimer = setTimeout(() => {
      lockClientWorkspace("idle");
    }, CLIENT_WORKSPACE_IDLE_LOCK_MS);
  }
  function lockClientWorkspace(reason = "idle") {
    if (!isPublicClientSetup) return;
    clearClientWorkspaceUnlock();
    openStudioAuthGate("start");
    if (studioAuthDemoButton) studioAuthDemoButton.hidden = true;
    if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
    if (studioEmailAuthButton) studioEmailAuthButton.hidden = true;
    revealStudioAuthProviderButtons();
    if (guidedStatusText) {
      guidedStatusText.textContent = reason === "idle" ? langText({
        en: "Workspace locked after inactivity. Sign in again to continue.",
        es: "Espacio bloqueado por inactividad. Inicia sesi\xF3n otra vez para continuar.",
        fr: "Espace verrouill\xE9 apr\xE8s inactivit\xE9. Connectez-vous \xE0 nouveau pour continuer.",
        pt: "Espa\xE7o bloqueado por inatividade. Entre novamente para continuar."
      }) : langText({
        en: "Sign in again to continue.",
        es: "Inicia sesi\xF3n otra vez para continuar.",
        fr: "Connectez-vous \xE0 nouveau pour continuer.",
        pt: "Entre novamente para continuar."
      });
    }
  }
  function readClientIntakeSession() {
    try {
      const raw = localStorage.getItem(CLIENT_INTAKE_SESSION_STORAGE_KEY);
      return raw ? JSON.parse(raw) : null;
    } catch {
      localStorage.removeItem(CLIENT_INTAKE_SESSION_STORAGE_KEY);
      return null;
    }
  }
  function writeClientIntakeSession(session) {
    const cleanSession = {
      ...session || {},
      draft: sanitizeClientSessionDraft(session?.draft || {})
    };
    builderState.clientIntakeSession = {
      ...builderState.clientIntakeSession || {},
      ...cleanSession,
      savedAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    try {
      localStorage.setItem(CLIENT_INTAKE_SESSION_STORAGE_KEY, JSON.stringify(builderState.clientIntakeSession));
    } catch {
    }
    renderClientAccountControl();
  }
  function renderClientAccountControl() {
    if (!isPublicClientSetup || !guidedHeaderActions) return;
    if (!builderState.clientAccountButton) {
      builderState.clientAccountButton = document.createElement("button");
      builderState.clientAccountButton.id = "clientAccountButton";
      builderState.clientAccountButton.className = "secondary-action compact-action client-account-button";
      builderState.clientAccountButton.type = "button";
      builderState.clientAccountButton.addEventListener("click", switchClientAccount);
      guidedHeaderActions.insertBefore(builderState.clientAccountButton, guidedHeaderActions.firstChild);
    }
    if (!builderState.clientProjectsButton) {
      builderState.clientProjectsButton = document.createElement("button");
      builderState.clientProjectsButton.id = "clientProjectsButton";
      builderState.clientProjectsButton.className = "secondary-action compact-action client-projects-button";
      builderState.clientProjectsButton.type = "button";
      builderState.clientProjectsButton.addEventListener("click", openClientProjectsPanel);
      guidedHeaderActions.insertBefore(builderState.clientProjectsButton, builderState.clientAccountButton.nextSibling);
    }
    const session = builderState.clientIntakeSession || readClientIntakeSession();
    const email = session?.clientEmail || localStorage.getItem("lumaPendingClientEmail") || "";
    builderState.clientAccountButton.textContent = email ? langText({
      en: `Account: ${compactEmailLabel(email)}`,
      es: `Cuenta: ${compactEmailLabel(email)}`,
      fr: `Compte : ${compactEmailLabel(email)}`,
      pt: `Conta: ${compactEmailLabel(email)}`
    }) : langText({ en: "Sign in", es: "Iniciar sesi\xF3n", fr: "Connexion", pt: "Entrar" });
    builderState.clientProjectsButton.textContent = t("myPages");
    builderState.clientProjectsButton.hidden = !storedClientAccessToken();
  }
  function switchClientAccount() {
    const hasSession = Boolean(builderState.clientIntakeSession?.clientEmail || readClientIntakeSession()?.clientEmail || storedClientAccessToken());
    if (hasSession) {
      const ok = window.confirm(langText({
        en: "Switch account? This clears the current business draft from this browser -- the next workspace starts clean.",
        es: "\xBFCambiar cuenta? Esto borra el borrador de negocio actual de este navegador -- el siguiente workspace empieza limpio.",
        fr: "Changer de compte ? Cela efface le brouillon d'entreprise actuel de ce navigateur -- le prochain espace d\xE9marre \xE0 z\xE9ro.",
        pt: "Trocar de conta? Isso apaga o rascunho de neg\xF3cio atual deste navegador -- o pr\xF3ximo workspace come\xE7a limpo."
      }));
      if (!ok) return;
    }
    builderState.clientIntakeSession = null;
    localStorage.removeItem(CLIENT_INTAKE_SESSION_STORAGE_KEY);
    localStorage.removeItem("lumaClientAccessToken");
    localStorage.removeItem("lumaClientRefreshToken");
    localStorage.removeItem("lumaPendingClientEmail");
    sessionStorage.removeItem("lumaClientAccessToken");
    sessionStorage.removeItem("lumaClientRefreshToken");
    resetGuidedStateForNewAccount();
    applyGuidedStateToForm();
    renderGuidedSummary();
    clearClientWorkspaceUnlock();
    renderClientAccountControl();
    openStudioAuthGate("start");
    if (studioAuthDemoButton) studioAuthDemoButton.hidden = true;
    if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
    if (studioEmailAuthButton) studioEmailAuthButton.hidden = true;
    revealStudioAuthProviderButtons();
    if (studioAuthEmail) {
      studioAuthEmail.value = "";
      studioAuthEmail.focus();
    }
  }
  function captureClientAuthResetIntent() {
    if (!isPublicClientSetup) return;
    const params = new URLSearchParams(window.location.search);
    const shouldReset = ["test-login", "force-login", "logout"].some((key) => {
      const value = params.get(key);
      return value === "" || value === "1" || value === "true";
    });
    if (!shouldReset) return;
    builderState.clientIntakeSession = null;
    clearClientWorkspaceUnlock();
    localStorage.removeItem(GUIDED_DRAFT_STORAGE_KEY);
    localStorage.removeItem(GENERATED_SITE_STORAGE_KEY);
    localStorage.removeItem(CLIENT_INTAKE_SESSION_STORAGE_KEY);
    localStorage.removeItem("lumaPendingGeneratedSite");
    localStorage.removeItem("lumaPendingAuthAction");
    localStorage.removeItem("lumaClientAccessToken");
    localStorage.removeItem("lumaClientRefreshToken");
    sessionStorage.removeItem("lumaClientAccessToken");
    sessionStorage.removeItem("lumaClientRefreshToken");
    sessionStorage.removeItem("vm_portal_preview_token");
    if (params.has("logout")) {
      localStorage.removeItem("lumaPendingClientEmail");
    }
  }
  function clientAuthHeaders(extra = {}) {
    const token = storedClientAccessToken();
    return {
      ...extra,
      ...token ? { authorization: `Bearer ${token}` } : {}
    };
  }
  function handleExpiredClientAuth(reason = "unknown", detail = null) {
    console.error("Client OAuth session expired or rejected", {
      reason,
      detail,
      tokenPresent: Boolean(storedClientAccessToken())
    });
    localStorage.removeItem("lumaClientAccessToken");
    localStorage.removeItem("lumaClientRefreshToken");
    sessionStorage.removeItem("lumaClientAccessToken");
    sessionStorage.removeItem("lumaClientRefreshToken");
    fetch(CLIENT_AUTH_LOGOUT_URL, { method: "POST", credentials: "include" }).catch(() => {
    });
    closeClientProjectsPanel();
    renderClientAccountControl();
    openStudioAuthGate("start");
  }
  async function fetchClientAuthUser() {
    const token = storedClientAccessToken();
    if (!token) {
      console.error("Cannot validate client auth: no stored access token.");
      return null;
    }
    const response = await fetch(CLIENT_AUTH_ME_URL, {
      headers: clientAuthHeaders(),
      credentials: "include"
    });
    if (!response.ok) {
      const message = await readErrorMessage(response);
      console.error("Client auth /me validation failed", {
        status: response.status,
        message,
        tokenPresent: Boolean(token)
      });
      if (response.status === 401) {
        handleExpiredClientAuth("auth-me-401", { status: response.status, message });
      }
      throw new Error(message);
    }
    return response.json();
  }
  async function fetchClientProjects() {
    if (!storedClientAccessToken()) return [];
    const response = await fetchWithTimeout(CLIENT_PROJECTS_URL, {
      headers: clientAuthHeaders(),
      credentials: "include"
    }, 12e3);
    if (!response.ok) {
      if (response.status === 401) handleExpiredClientAuth();
      throw new Error(await readErrorMessage(response));
    }
    const data = await response.json();
    return Array.isArray(data.projects) ? data.projects : [];
  }
  function ensureClientProjectsPanel() {
    if (builderState.clientProjectsPanel) return builderState.clientProjectsPanel;
    builderState.clientProjectsPanel = document.createElement("section");
    builderState.clientProjectsPanel.className = "client-projects-panel";
    builderState.clientProjectsPanel.hidden = true;
    builderState.clientProjectsPanel.setAttribute("aria-label", "Mis p\xE1ginas");
    builderState.clientProjectsPanel.addEventListener("click", (event) => {
      if (event.target?.closest?.("[data-client-projects-close]")) {
        closeClientProjectsPanel();
        return;
      }
      const continueButton = event.target?.closest?.("[data-client-project-id]");
      if (continueButton) {
        loadClientProject(continueButton.dataset.clientProjectId).catch((error) => {
          console.warn("Could not open client project", error);
          if (storageStatus) storageStatus.textContent = t("loadProjectsError");
        });
        return;
      }
      if (event.target?.closest?.("[data-client-new-project]")) {
        startNewClientProject({ skipConfirm: true });
      }
    });
    document.body.appendChild(builderState.clientProjectsPanel);
    return builderState.clientProjectsPanel;
  }
  function renderClientProjectsLoading() {
    const panel = ensureClientProjectsPanel();
    panel.innerHTML = `
    <div class="client-projects-card">
      <div class="client-projects-head">
        <span>${escapeHtml(t("myPages"))}</span>
        <h2>${escapeHtml(langText({ en: "Loading your saved pages", es: "Cargando tus p\xE1ginas", fr: "Chargement de vos pages", pt: "Carregando suas p\xE1ginas" }))}</h2>
        <p>${escapeHtml(langText({ en: "LYRA is checking the projects saved under your account.", es: "LYRA est\xE1 revisando los proyectos guardados en tu cuenta.", fr: "LYRA v\xE9rifie les projets enregistr\xE9s sur votre compte.", pt: "LYRA est\xE1 verificando os projetos salvos na sua conta." }))}</p>
      </div>
      <div class="client-projects-skeleton"></div>
    </div>
  `;
    panel.hidden = false;
    document.body.classList.add("client-projects-open");
  }
  function renderClientProjectsPanel(projects = []) {
    const panel = ensureClientProjectsPanel();
    const emptyState = `
    <div class="client-projects-empty">
      <strong>${escapeHtml(t("myPages"))}</strong>
      <p>${escapeHtml(t("emptyProjects"))}</p>
    </div>
  `;
    const rows = projects.map((project) => `
    <article class="client-project-card">
      <div>
        <span>${escapeHtml(project.status || "draft")}</span>
        <h3>${escapeHtml(project.business_name || "Untitled page")}</h3>
        <p>${escapeHtml(project.template_name || "Generated website")} \xB7 ${escapeHtml(formatProjectUpdatedAt(project.updated_at))}</p>
        ${project.public_url ? `<small>${escapeHtml(project.public_url)}</small>` : ""}
      </div>
      <button type="button" data-client-project-id="${escapeAttribute(project.id)}">${escapeHtml(t("openProject"))}</button>
    </article>
  `).join("");
    panel.innerHTML = `
    <div class="client-projects-card">
      <button class="client-projects-close" type="button" data-client-projects-close aria-label="Close">\xD7</button>
      <div class="client-projects-head">
        <span>${escapeHtml(langText({ en: "Your workspace", es: "Tu espacio", fr: "Votre espace", pt: "Seu espa\xE7o" }))}</span>
        <h2>${escapeHtml(langText({ en: "Choose a page to continue", es: "Elige una p\xE1gina para continuar", fr: "Choisissez une page \xE0 continuer", pt: "Escolha uma p\xE1gina para continuar" }))}</h2>
        <p>${escapeHtml(langText({
      en: "Each page stays separate under your account.",
      es: "Cada p\xE1gina queda separada dentro de tu cuenta.",
      fr: "Chaque page reste s\xE9par\xE9e dans votre compte.",
      pt: "Cada p\xE1gina fica separada dentro da sua conta."
    }))}</p>
      </div>
      <div class="client-projects-list">${rows || emptyState}</div>
      <button class="client-new-project-button" type="button" data-client-new-project>${escapeHtml(t("startNewProject"))}</button>
    </div>
  `;
    panel.hidden = false;
    document.body.classList.add("client-projects-open");
  }
  function closeClientProjectsPanel() {
    if (builderState.clientProjectsPanel) builderState.clientProjectsPanel.hidden = true;
    document.body.classList.remove("client-projects-open");
  }
  async function openClientProjectsPanel() {
    if (!isPublicClientSetup) return;
    if (!storedClientAccessToken()) {
      openStudioAuthGate("start");
      return;
    }
    renderClientProjectsLoading();
    try {
      builderState.clientProjects = await fetchClientProjects();
      renderClientProjectsPanel(builderState.clientProjects);
    } catch (error) {
      console.warn("Could not load client projects", error);
      if (!storedClientAccessToken()) return;
      if (storageStatus) storageStatus.textContent = t("loadProjectsError");
      renderClientProjectsPanel([]);
    }
  }
  async function handleClientProjectsAfterAuth(user, session) {
    if (!isPublicClientSetup || !storedClientAccessToken()) return;
    try {
      builderState.clientProjects = await fetchClientProjects();
    } catch (error) {
      console.warn("Could not load client projects", error);
      return;
    }
    if (builderState.clientProjects.length > 1) {
      renderClientProjectsPanel(builderState.clientProjects);
      guidedStatusText.textContent = langText({
        en: "Choose which page you want to continue, or start a new one.",
        es: "Elige qu\xE9 p\xE1gina quieres continuar, o crea una nueva.",
        fr: "Choisissez la page \xE0 continuer, ou cr\xE9ez-en une nouvelle.",
        pt: "Escolha qual p\xE1gina deseja continuar, ou crie uma nova."
      });
      return;
    }
    if (builderState.clientProjects.length === 1 && !builderState.currentSchema) {
      await loadClientProject(builderState.clientProjects[0].id, { silent: true, session });
    }
  }
  async function loadClientProject(projectId, options = {}) {
    if (!projectId) return;
    if (storageStatus && !options.silent) {
      storageStatus.textContent = langText({
        en: "Opening saved page...",
        es: "Abriendo p\xE1gina guardada...",
        fr: "Ouverture de la page enregistr\xE9e...",
        pt: "Abrindo p\xE1gina salva..."
      });
    }
    const response = await fetchWithTimeout(`${CLIENT_PROJECTS_URL}/${encodeURIComponent(projectId)}`, {
      headers: clientAuthHeaders(),
      credentials: "include"
    }, 14e3);
    if (!response.ok) {
      if (response.status === 401) handleExpiredClientAuth();
      throw new Error(await readErrorMessage(response));
    }
    const data = await response.json();
    const schema = data.schema || {};
    builderState.currentSchema = prepareWebsiteConfig(schema, { brand: schema.brand || builderState.guidedState.brand || {} }, null);
    builderState.currentSiteId = data.generatedSiteId || data.site_id || projectId;
    builderState.currentBusinessId = data.business_id || builderState.currentBusinessId;
    builderState.currentGenerationId = null;
    builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
    builderState.selectedPageKey = builderState.currentSchema.pages?.[0]?.page_key || "home";
    builderState.selectedVariantId = builderState.currentSchema.design_variants?.[0]?.id || "";
    builderState.guidedState.businessName = builderState.currentSchema.business?.name || builderState.guidedState.businessName;
    builderState.guidedState.businessDescription = builderState.currentSchema.business?.description || builderState.guidedState.businessDescription;
    builderState.guidedState.industry = builderState.currentSchema.business?.industry || builderState.guidedState.industry;
    builderState.guidedState.preferredTone = builderState.currentSchema.business?.tone || builderState.guidedState.preferredTone;
    builderState.guidedState.logoUrl = builderState.currentSchema.brand?.logoUrl || builderState.guidedState.logoUrl;
    builderState.guidedState.logoPreference = builderState.currentSchema.brand?.logoPreference || builderState.guidedState.logoPreference;
    builderState.guidedState.generatedSiteId = builderState.currentSiteId;
    builderState.guidedState.projectId = builderState.currentSiteId;
    builderState.clientIntakeSession = {
      ...builderState.clientIntakeSession || options.session || {},
      generatedSiteId: builderState.currentSiteId,
      projectId: builderState.currentSiteId,
      clientEmail: builderState.clientIntakeSession?.clientEmail || options.session?.clientEmail || localStorage.getItem("lumaPendingClientEmail") || "",
      draft: guidedSessionDraftForApi()
    };
    writeClientIntakeSession(builderState.clientIntakeSession);
    closeClientProjectsPanel();
    saveGeneratedSite({
      schema: builderState.currentSchema,
      business_id: builderState.currentBusinessId,
      site_id: builderState.currentSiteId,
      generatedSiteId: builderState.currentSiteId,
      storage_status: data.storage_status || "stored",
      used_dev_mock: false
    });
    siteTitle.textContent = builderState.currentSchema.business?.name || "Generated site";
    storageStatus.textContent = storageLabel(data.storage_status || "stored", false);
    renderEditor();
    renderPreview();
    renderGuidedSummary();
    renderLiveSitePreview();
    if (!options.silent) showGeneratedClientPreview();
  }
  async function resumeClientSessionFromAuthToken() {
    if (builderState.clientAuthResumePromise) return builderState.clientAuthResumePromise;
    showStudioAuthLoading();
    builderState.clientAuthResumePromise = (async () => {
      try {
        const user = await fetchClientAuthUser();
        const email = user?.email || "";
        if (!email) throw new Error("Authenticated user email missing.");
        const name = user?.userMetadata?.full_name || user?.userMetadata?.name || builderState.guidedState.businessName || "";
        const session = await createOrResumeClientIntakeSession({
          email,
          name,
          reason: "oauth-resume"
        });
        if (storageStatus) {
          storageStatus.textContent = session.restored ? langText({
            en: "Session restored. LYRA will keep saving your progress.",
            es: "Sesi\xF3n recuperada. LYRA seguir\xE1 guardando tu progreso.",
            fr: "Session restaur\xE9e. LYRA continuera \xE0 sauvegarder votre progression.",
            pt: "Sess\xE3o recuperada. LYRA continuar\xE1 salvando seu progresso."
          }) : langText({
            en: "Session connected. LYRA will save every answer.",
            es: "Sesi\xF3n conectada. LYRA guardar\xE1 cada respuesta.",
            fr: "Session connect\xE9e. LYRA sauvegardera chaque r\xE9ponse.",
            pt: "Sess\xE3o conectada. LYRA salvar\xE1 cada resposta."
          });
        }
        markClientWorkspaceUnlocked();
        closeStudioAuthGate();
        await handleClientProjectsAfterAuth(user, session);
        return session;
      } catch (error) {
        console.error("Could not resume client OAuth session", {
          message: error?.message || String(error),
          tokenPresent: Boolean(storedClientAccessToken()),
          clientIntakeSessionPresent: Boolean(builderState.clientIntakeSession)
        }, error);
        openStudioAuthGate("start");
        if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
        if (storageStatus) {
          storageStatus.textContent = langText({
            en: "Could not restore the login session. Continue with email.",
            es: "No se pudo recuperar la sesi\xF3n. Contin\xFAa con email.",
            fr: "Impossible de restaurer la session. Continuez avec email.",
            pt: "N\xE3o foi poss\xEDvel recuperar a sess\xE3o. Continue com email."
          });
        }
        return null;
      } finally {
        builderState.clientAuthResumePromise = null;
      }
    })();
    return builderState.clientAuthResumePromise;
  }
  function hydrateClientIntakeSession(session, options = {}) {
    if (!session) return;
    builderState.currentRequestId = session.requestId || session.request_id || builderState.currentRequestId;
    builderState.currentSiteId = session.generatedSiteId || session.projectId || session.siteId || builderState.currentSiteId;
    if (session.restored) {
      builderState.restoredGuidedDraftInfo = builderState.restoredGuidedDraftInfo || {
        savedAt: "",
        completionPercent: guidedCompletionPercent(),
        missing: missingGuidedSteps(),
        source: "client_intake_session"
      };
    }
    const draft = sanitizeClientSessionDraft(session.draft || {});
    if (draft.selectedLanguage) setSelectedLanguage(draft.selectedLanguage);
    const normalizedDraft = {
      ...draft,
      servicesProducts: arrayValue2(draft.servicesProducts),
      preferredColors: arrayValue2(draft.preferredColors),
      photoUrls: arrayValue2(draft.photoUrls),
      videoUrls: arrayValue2(draft.videoUrls),
      contactInfo: { ...draft.contactInfo || {} }
    };
    normalizedDraft.generatedSiteId = session.generatedSiteId || session.projectId || normalizedDraft.generatedSiteId || "";
    normalizedDraft.projectId = session.projectId || session.generatedSiteId || normalizedDraft.projectId || "";
    if (session.clientEmail && !normalizedDraft.contactInfo.email) {
      normalizedDraft.contactInfo.email = session.clientEmail;
    }
    mergeGuidedUpdates(normalizedDraft);
    applyGuidedStateToForm();
    renderGuidedSummary();
    refreshQuickChips();
    renderLiveSitePreview();
    if (!options.silent && session.restored) {
      renderRestoredDraftNotice();
    }
  }
  async function createOrResumeClientIntakeSession({ email, name = "", reason = "start", immediateDraft = null, forceNew = false } = {}) {
    const cleanEmail = String(email || "").trim().toLowerCase();
    if (!cleanEmail) throw new Error("Email is required.");
    const storedSession = readClientIntakeSession();
    const lastKnownEmail = String(
      builderState.clientIntakeSession?.clientEmail || builderState.clientIntakeSession?.client_email || storedSession?.clientEmail || storedSession?.client_email || localStorage.getItem("lumaPendingClientEmail") || ""
    ).trim().toLowerCase();
    if (lastKnownEmail && lastKnownEmail !== cleanEmail) {
      resetGuidedStateForNewAccount({ preserveAuth: Boolean(storedClientAccessToken()) });
    }
    const draft = sanitizeClientSessionDraft(immediateDraft || guidedSessionDraftForApi());
    draft.contactInfo = {
      ...draft.contactInfo || {},
      email: draft.contactInfo?.email || cleanEmail,
      name: draft.contactInfo?.name || name || draft.businessName || ""
    };
    const response = await fetchWithTimeout(CLIENT_INTAKE_SESSION_URL, {
      method: "POST",
      headers: clientAuthHeaders({ "content-type": "application/json" }),
      credentials: "include",
      body: JSON.stringify({
        email: cleanEmail,
        name,
        selectedLanguage: builderState.selectedLanguage,
        requestId: forceNew ? null : builderState.currentRequestId,
        generatedSiteId: forceNew ? "" : builderState.currentSiteId || builderState.clientIntakeSession?.generatedSiteId || builderState.clientIntakeSession?.projectId || "",
        projectId: forceNew ? "" : builderState.currentSiteId || builderState.clientIntakeSession?.generatedSiteId || builderState.clientIntakeSession?.projectId || "",
        forceNew,
        draft
      })
    }, 18e3);
    if (!response.ok) throw new Error(await readErrorMessage(response));
    const session = await response.json();
    writeClientIntakeSession(session);
    localStorage.setItem("lumaPendingClientEmail", cleanEmail);
    hydrateClientIntakeSession(session, { silent: reason === "autosave" });
    return session;
  }
  function syncClientIntakeSession({ immediate = false, reason = "autosave" } = {}) {
    if (!isPublicClientSetup || !builderState.clientIntakeSession?.clientEmail) return;
    const currentSnapshot = JSON.stringify(guidedStateForApi());
    if (currentSnapshot === builderState.clientIntakeLastSyncedSnapshot) return;
    clearTimeout(builderState.clientIntakeSyncTimer);
    const run = async () => {
      if (builderState.clientIntakeSyncInFlight) return;
      builderState.clientIntakeSyncInFlight = true;
      try {
        const session = await createOrResumeClientIntakeSession({
          email: builderState.clientIntakeSession.clientEmail,
          name: builderState.guidedState.contactInfo?.name || builderState.guidedState.businessName || "",
          reason
        });
        if (session.requestId) builderState.currentRequestId = session.requestId;
        builderState.clientIntakeLastSyncedSnapshot = currentSnapshot;
      } catch (error) {
        console.warn("Client intake autosave failed", error);
        if (guidedStatusText) {
          guidedStatusText.textContent = langText({
            en: "Saved in this browser. Cloud sync will retry.",
            es: "Guardado en este navegador. La nube reintentar\xE1.",
            fr: "Enregistr\xE9 dans ce navigateur. La synchronisation r\xE9essaiera.",
            pt: "Salvo neste navegador. A nuvem tentar\xE1 novamente."
          });
        }
      } finally {
        builderState.clientIntakeSyncInFlight = false;
      }
    };
    if (immediate) {
      run();
    } else {
      builderState.clientIntakeSyncTimer = setTimeout(run, CLIENT_INTAKE_AUTOSAVE_DELAY_MS);
    }
  }
  function startNewClientProject(options = {}) {
    if (!isPublicClientSetup) return;
    const hasExistingWork = Boolean(builderState.currentSchema || builderState.guidedState.businessName || builderState.guidedState.businessDescription || builderState.guidedState.websiteIntent);
    if (hasExistingWork && !options.skipConfirm && !window.confirm(t("startNewProjectConfirm"))) return;
    const existingEmail = builderState.clientIntakeSession?.clientEmail || readClientIntakeSession()?.clientEmail || "";
    closeClientProjectsPanel();
    localStorage.removeItem(GUIDED_DRAFT_STORAGE_KEY);
    localStorage.removeItem(GENERATED_SITE_STORAGE_KEY);
    localStorage.removeItem("lumaPendingGeneratedSite");
    localStorage.removeItem("lumaPendingAuthAction");
    builderState.currentSchema = null;
    builderState.selectedPageKey = "home";
    builderState.selectedVariantId = "";
    builderState.selectedStudioSectionId = "";
    builderState.currentRequestId = null;
    builderState.currentSiteId = "";
    builderState.currentBusinessId = "";
    builderState.currentGenerationId = null;
    builderState.currentCatalogItems = [];
    builderState.forcedTemplateSelection = null;
    builderState.restoredGuidedDraftInfo = null;
    builderState.guidedStep = "websiteIntent";
    builderState.lastAskedGuidedField = "";
    builderState.hasBackendIntakeSignal = false;
    builderState.backendReadyToGenerate = false;
    builderState.backendMissingFields = [];
    builderState.guidedState = createEmptyGuidedState(builderState.selectedLanguage);
    if (existingEmail) builderState.guidedState.contactInfo.email = existingEmail;
    guidedAskedSteps.clear();
    builderState.lastAssistantPromptSignature = "";
    document.body.classList.remove(
      "generated-preview-open",
      "client-preview-mode",
      "draft-adjust-open",
      "review-details-open",
      "final-review-mode",
      "manual-form-open",
      "studio-auth-open"
    );
    syncLyraExperienceMode();
    guidedPanel?.classList.add("active");
    form?.classList.remove("active");
    previewFrame.innerHTML = "";
    applyGuidedStateToForm();
    renderGuidedSummary();
    refreshQuickChips();
    renderLiveSitePreview();
    resetAssistantConversation();
    guidedStatusText.textContent = langText({
      en: "New page started.",
      es: "Nueva p\xE1gina iniciada.",
      fr: "Nouvelle page commenc\xE9e.",
      pt: "Nova p\xE1gina iniciada."
    });
    if (existingEmail) {
      createOrResumeClientIntakeSession({
        email: existingEmail,
        name: "",
        reason: "new-project",
        forceNew: true
      }).catch((error) => console.warn("Could not create new client intake session", error));
    } else {
      initClientIntakeSessionGate();
    }
    guidedReply?.focus();
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function ensureStudioAuthRedirectCaptured() {
    if (builderState.studioAuthRedirectCaptureComplete) return;
    captureStudioAuthRedirect();
  }
  function establishServerSession(accessToken, refreshToken) {
    fetch(CLIENT_AUTH_SESSION_URL, {
      method: "POST",
      headers: { "content-type": "application/json" },
      credentials: "include",
      body: JSON.stringify({ access_token: accessToken, refresh_token: refreshToken || "" })
    }).catch((error) => {
      console.error("Could not establish httpOnly session cookie (non-fatal, header-based auth still works)", error);
    });
  }
  function captureStudioAuthRedirect() {
    if (builderState.studioAuthRedirectCaptureComplete) return;
    try {
      const { accessToken, refreshToken } = readSupabaseAuthRedirect(window.location);
      if (!accessToken) return;
      localStorage.setItem("lumaClientAccessToken", accessToken);
      if (refreshToken) localStorage.setItem("lumaClientRefreshToken", refreshToken);
      establishServerSession(accessToken, refreshToken);
      markClientWorkspaceUnlocked();
      restorePendingStudioAfterAuth();
      if (isPublicClientSetup) {
        setTimeout(() => resumeClientSessionFromAuthToken(), 0);
      }
      const cleanUrl = new URL(window.location.href);
      cleanUrl.hash = "";
      ["access_token", "refresh_token", "expires_in", "expires_at", "token_type", "type"].forEach((param) => cleanUrl.searchParams.delete(param));
      window.history.replaceState({}, "", cleanUrl);
    } finally {
      builderState.studioAuthRedirectCaptureComplete = true;
    }
  }
  function restorePendingStudioAfterAuth() {
    try {
      const raw = localStorage.getItem("lumaPendingGeneratedSite");
      if (!raw) return;
      const saved = JSON.parse(raw);
      if (!saved.schema) return;
      builderState.currentSchema = prepareWebsiteConfig(saved.schema, { brand: saved.schema.brand || builderState.guidedState.brand || {} }, null);
      builderState.currentSiteId = saved.siteId || builderState.currentSiteId;
      builderState.currentBusinessId = saved.businessId || builderState.currentBusinessId;
      builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
      builderState.selectedPageKey = saved.selectedPageKey || builderState.currentSchema.pages?.[0]?.page_key || "home";
      builderState.selectedVariantId = builderState.currentSchema.design_variants?.[0]?.id || builderState.selectedVariantId || "";
      siteTitle.textContent = builderState.currentSchema.business?.name || "Generated site";
      storageStatus.textContent = langText({
        en: "Account connected. Your draft was restored.",
        es: "Cuenta conectada. Tu borrador fue restaurado.",
        fr: "Compte connect\xE9. Votre brouillon a \xE9t\xE9 restaur\xE9.",
        pt: "Conta conectada. Seu rascunho foi restaurado."
      });
      renderEditor();
      renderPreview();
      showGeneratedClientPreview();
      continuePendingStudioAction();
    } catch {
      localStorage.removeItem("lumaPendingGeneratedSite");
    }
  }
  function guidedSessionDraftForApi() {
    const logoUrl = isCloudSafeUrl(builderState.guidedState.logoUrl) ? builderState.guidedState.logoUrl : "";
    const logoPreference = builderState.guidedState.logoPreference || (builderState.guidedState.aiGeneratedLogoRequested ? "generate_ai_logo" : "");
    const fieldMeta = { ...builderState.guidedState.fieldMeta || {} };
    if (logoPreference) {
      fieldMeta.logo = fieldMeta.logo || { source: "explicit", confidence: 1 };
      fieldMeta.logoPreference = fieldMeta.logoPreference || { source: "explicit", confidence: 1 };
    }
    return sanitizeClientSessionDraft({
      generatedSiteId: builderState.currentSiteId || builderState.clientIntakeSession?.generatedSiteId || builderState.clientIntakeSession?.projectId || builderState.guidedState.generatedSiteId || "",
      projectId: builderState.currentSiteId || builderState.clientIntakeSession?.projectId || builderState.clientIntakeSession?.generatedSiteId || builderState.guidedState.projectId || "",
      websiteIntent: builderState.guidedState.websiteIntent,
      businessName: builderState.guidedState.businessName,
      businessDescription: builderState.guidedState.businessDescription,
      industry: builderState.guidedState.industry,
      location: builderState.guidedState.location,
      servicesProducts: arrayValue2(builderState.guidedState.servicesProducts),
      targetAudience: builderState.guidedState.targetAudience,
      preferredTone: builderState.guidedState.preferredTone,
      preferredColors: arrayValue2(builderState.guidedState.preferredColors),
      contactInfo: builderState.guidedState.contactInfo || {},
      desiredDomain: builderState.guidedState.desiredDomain,
      logoUrl,
      photoUrls: arrayValue2(builderState.guidedState.photoUrls).filter(isCloudSafeUrl),
      videoUrls: arrayValue2(builderState.guidedState.videoUrls).filter(isCloudSafeUrl),
      logoPalette: arrayValue2(builderState.guidedState.logoPalette),
      colorProvenance: builderState.guidedState.colorProvenance,
      logoPreference,
      fieldMeta,
      selectedLanguage: builderState.selectedLanguage,
      hasLogo: Boolean(builderState.guidedState.hasLogo || builderState.guidedState.logoUrl),
      hasPhotos: Boolean(builderState.guidedState.hasPhotos || arrayValue2(builderState.guidedState.photoUrls).length || arrayValue2(builderState.guidedState.videoUrls).length),
      salesMode: builderState.guidedState.salesMode,
      hasLogoPhotos: builderState.guidedState.hasLogoPhotos,
      sectionsPreference: builderState.guidedState.sectionsPreference,
      selectedTemplateId: builderState.guidedState.selectedTemplateId || builderState.forcedTemplateSelection?.templateId || "",
      selectedTemplateName: builderState.guidedState.selectedTemplateName || builderState.forcedTemplateSelection?.name || "",
      catalogType: builderState.guidedState.catalogType || builderState.forcedTemplateSelection?.catalogType || "",
      websiteType: builderState.guidedState.websiteType || "",
      salesFlow: builderState.guidedState.salesFlow || "",
      designVariantOffset: Math.max(0, Number(builderState.guidedState.designVariantOffset) || 0)
    });
  }
  function sanitizeClientSessionDraft(raw = {}) {
    const source = raw && typeof raw === "object" ? raw : {};
    const trimmed = (value, limit = 1200) => String(value || "").trim().slice(0, limit);
    const cleanList = (value, limit = 20) => arrayValue2(value).map((item) => String(item || "").trim()).filter(Boolean).slice(0, limit);
    const contactInfo = source.contactInfo && typeof source.contactInfo === "object" ? source.contactInfo : {};
    const fieldMeta = source.fieldMeta && typeof source.fieldMeta === "object" ? source.fieldMeta : {};
    const colorProvenance = source.colorProvenance && typeof source.colorProvenance === "object" ? source.colorProvenance : null;
    return {
      generatedSiteId: trimmed(source.generatedSiteId || source.siteId || source.projectId, 180),
      projectId: trimmed(source.projectId || source.generatedSiteId || source.siteId, 180),
      websiteIntent: trimmed(source.websiteIntent),
      businessName: trimmed(source.businessName, 180),
      businessDescription: trimmed(source.businessDescription, 1600),
      industry: trimmed(source.industry, 220),
      location: trimmed(source.location, 220),
      servicesProducts: cleanList(source.servicesProducts),
      targetAudience: trimmed(source.targetAudience, 500),
      preferredTone: trimmed(source.preferredTone, 240),
      preferredColors: cleanList(source.preferredColors, 10),
      contactInfo: {
        name: trimmed(contactInfo.name, 180),
        email: trimmed(contactInfo.email, 220),
        phone: trimmed(contactInfo.phone, 80),
        whatsapp: trimmed(contactInfo.whatsapp, 80),
        instagram: trimmed(contactInfo.instagram, 140),
        website: trimmed(contactInfo.website, 220),
        notes: trimmed(contactInfo.notes, 800)
      },
      desiredDomain: trimmed(source.desiredDomain, 240),
      logoUrl: isCloudSafeUrl(source.logoUrl) ? trimmed(source.logoUrl, 1200) : "",
      photoUrls: cleanList(source.photoUrls).filter(isCloudSafeUrl),
      videoUrls: cleanList(source.videoUrls).filter(isCloudSafeUrl),
      logoPalette: cleanList(source.logoPalette, 12),
      colorProvenance: colorProvenance ? {
        anchorColor: trimmed(colorProvenance.anchorColor, 80) || null,
        anchorSource: trimmed(colorProvenance.anchorSource, 40) || "unknown",
        colors: arrayValue2(colorProvenance.colors).slice(0, 20).map((item) => ({
          color: trimmed(item?.color, 80),
          source: trimmed(item?.source, 40) || "unknown"
        })).filter((item) => item.color)
      } : null,
      fieldMeta,
      selectedLanguage: SUPPORTED_LANGUAGES.includes(source.selectedLanguage) ? source.selectedLanguage : builderState.selectedLanguage,
      hasLogo: Boolean(source.hasLogo || source.logoUrl),
      hasPhotos: Boolean(source.hasPhotos || cleanList(source.photoUrls).length || cleanList(source.videoUrls).length),
      salesMode: trimmed(source.salesMode, 160),
      hasLogoPhotos: trimmed(source.hasLogoPhotos, 180),
      logoPreference: trimmed(source.logoPreference, 180),
      sectionsPreference: trimmed(source.sectionsPreference, 600),
      selectedTemplateId: trimmed(source.selectedTemplateId, 180),
      selectedTemplateName: trimmed(source.selectedTemplateName, 180),
      catalogType: trimmed(source.catalogType, 180),
      websiteType: trimmed(source.websiteType, 180),
      salesFlow: trimmed(source.salesFlow, 180),
      designVariantOffset: Math.max(0, Number(source.designVariantOffset) || 0)
    };
  }
  function hasStudioAccountSession() {
    return hasValidPersistedCredential({
      clientEmails: [builderState.clientIntakeSession?.clientEmail],
      accessTokens: [
        localStorage.getItem("lumaClientAccessToken"),
        sessionStorage.getItem("lumaClientAccessToken")
      ],
      previewTokens: [
        localStorage.getItem("vm_portal_preview_token"),
        sessionStorage.getItem("vm_portal_preview_token")
      ]
    });
  }
  function revealStudioAuthProviderButtons() {
    const providerActions = studioGoogleAuthButton?.closest(".studio-auth-actions");
    if (providerActions) providerActions.hidden = false;
    if (studioGoogleAuthButton) studioGoogleAuthButton.hidden = false;
    if (studioAppleAuthButton) {
      studioAppleAuthButton.hidden = false;
      studioAppleAuthButton.disabled = true;
      studioAppleAuthButton.setAttribute("aria-disabled", "true");
    }
    ensureMagicLinkResendHandler();
  }
  function openStudioAuthGate(action = "continue") {
    if (!studioAuthGate) return;
    resetStudioAuthLoading();
    resetMagicLinkView();
    persistPendingStudioAccountAction(action);
    studioAuthGate.dataset.action = action;
    if (studioAuthCloseButton) studioAuthCloseButton.hidden = action === "start";
    studioAuthGate.hidden = false;
    document.body.classList.add("studio-auth-open");
    if (isPublicClientSetup && action === "start") {
      document.body.classList.add("client-auth-required");
    }
    if (isPublicClientSetup) {
      if (studioEmailAuthForm) studioEmailAuthForm.hidden = false;
      if (studioEmailAuthButton) studioEmailAuthButton.hidden = true;
      revealStudioAuthProviderButtons();
      if (studioAuthDemoButton) studioAuthDemoButton.hidden = true;
    }
    setAssistantState("success");
  }
  function closeStudioAuthGate() {
    if (!studioAuthGate) return;
    resetStudioAuthLoading();
    resetMagicLinkView();
    studioAuthGate.hidden = true;
    document.body.classList.remove("studio-auth-open");
    document.body.classList.remove("client-auth-required");
  }
  async function continueWithDemoSession() {
    const pendingAction = localStorage.getItem("lumaPendingAuthAction") || "";
    sessionStorage.setItem("vm_portal_preview_token", `demo-${Date.now()}`);
    closeStudioAuthGate();
    if (pendingAction === "generate") {
      localStorage.removeItem("lumaPendingAuthAction");
      guidedStatusText.textContent = langText({
        en: "Demo session active. Generating your editable draft...",
        es: "Sesion demo activa. Generando tu borrador editable...",
        fr: "Session demo active. G\xE9n\xE9ration du brouillon modifiable...",
        pt: "Sessao demo ativa. Gerando seu rascunho editavel..."
      });
      await reviewAndGenerateFromGuided();
    }
  }
  function continueWithStudioAuth(provider) {
    persistPendingStudioAccountAction(provider);
    const returnTo = encodeURIComponent(window.location.href);
    const supabaseProvider = provider === "apple" ? "apple" : "google";
    window.location.href = `${SUPABASE_AUTH_URL}?provider=${supabaseProvider}&redirect_to=${returnTo}`;
  }
  async function continueWithEmailAuth(event) {
    event.preventDefault();
    const email = studioAuthEmail?.value.trim();
    if (!isValidWorkspaceEmail(email)) {
      const message = langText({
        en: "Enter a complete email, for example test@gmail.com.",
        es: "Escribe un correo completo, por ejemplo test@gmail.com.",
        fr: "Entrez un email complet, par exemple test@gmail.com.",
        pt: "Digite um email completo, por exemplo test@gmail.com."
      });
      if (studioAuthEmail) {
        studioAuthEmail.setCustomValidity(message);
        studioAuthEmail.reportValidity();
        studioAuthEmail.addEventListener("input", () => studioAuthEmail.setCustomValidity(""), { once: true });
      }
      if (storageStatus) storageStatus.textContent = message;
      if (guidedStatusText) guidedStatusText.textContent = message;
      return;
    }
    persistPendingStudioAccountAction("email");
    localStorage.setItem("lumaPendingClientEmail", email);
    await sendMagicLink(email);
  }

  // src/ai-builder/chat.js
  function guidedQuestion(step) {
    return publicAssistantCopy(GUIDED_QUESTIONS[builderState.selectedLanguage]?.[step] || GUIDED_QUESTIONS.en[step] || GUIDED_QUESTIONS.en.review);
  }
  function ensureGuidedCoachCard() {
    if (builderState.guidedCoachCard || !guidedChatCard || !guidedChat) return builderState.guidedCoachCard;
    builderState.guidedCoachCard = document.createElement("section");
    builderState.guidedCoachCard.className = "luma-coach-card";
    builderState.guidedCoachCard.setAttribute("aria-live", "polite");
    guidedChatCard.insertBefore(builderState.guidedCoachCard, guidedChat);
    return builderState.guidedCoachCard;
  }
  function renderGuidedCoachCard() {
    builderState.guidedCoachCard?.remove();
    builderState.guidedCoachCard = null;
  }
  function initBuilderAvatarAssistant() {
    if (!builderAvatarRoot || !window.AvatarAssistant || !builderAvatarManager) return;
    builderState.builderAvatarAssistant = new window.AvatarAssistant({
      root: builderAvatarRoot,
      manager: builderAvatarManager,
      name: "LYRA",
      imageUrl: ASSISTANT_AVATAR_FALLBACK,
      imagePaths: {
        idle: ASSISTANT_AVATAR_FALLBACK,
        neutral: ASSISTANT_AVATARS.neutral,
        happy: ASSISTANT_AVATARS.happy,
        thinking: ASSISTANT_AVATARS.thinking,
        listening: ASSISTANT_AVATARS.listening,
        speaking: ASSISTANT_AVATARS.speaking,
        building: ASSISTANT_AVATARS.building,
        optimize: ASSISTANT_AVATARS.optimize,
        confused: ASSISTANT_AVATARS.thinking,
        success: ASSISTANT_AVATARS.success,
        alert: ASSISTANT_AVATARS.alert
      },
      compact: true,
      labels: {
        idle: t("assistantSubtitle"),
        listening: langText({ en: "I'm listening to your idea.", es: "Estoy escuchando tu idea.", fr: "J'\xE9coute votre id\xE9e.", pt: "Estou ouvindo sua ideia." }),
        thinking: t("thinking"),
        speaking: langText({ en: "Guiding you step by step.", es: "Te guio paso a paso.", fr: "Je vous guide \xE9tape par \xE9tape.", pt: "Vou guiar voc\xEA passo a passo." }),
        happy: langText({ en: "Ready to help.", es: "Listo para ayudarte.", fr: "Pr\xEAte \xE0 vous aider.", pt: "Pronta para ajudar." }),
        confused: langText({ en: "I need a little more context.", es: "Necesito un poco mas de contexto.", fr: "J'ai besoin d'un peu plus de contexte.", pt: "Preciso de um pouco mais de contexto." }),
        success: langText({ en: "Your draft is ready.", es: "Tu borrador esta listo.", fr: "Votre brouillon est pr\xEAt.", pt: "Seu rascunho est\xE1 pronto." })
      }
    });
    document.body.classList.add("avatar-assistant-ready");
  }
  function avatarStateFromAssistantState(state) {
    return {
      neutral: "idle",
      happy: "happy",
      thinking: "thinking",
      listening: "listening",
      speaking: "speaking",
      alert: "confused",
      success: "success"
    }[state] || "idle";
  }
  function normalizeAssistantState(state) {
    return ["neutral", "happy", "thinking", "listening", "speaking", "alert", "success"].includes(state) ? state : "neutral";
  }
  function toggleAssistantAudio() {
    builderState.assistantVoiceEnabled = !builderState.assistantVoiceEnabled;
    localStorage.setItem("gnuDevAssistantVoice", builderState.assistantVoiceEnabled ? "on" : "off");
    updateAssistantAudioToggle();
  }
  function updateAssistantAudioToggle() {
    assistantAudioToggle.textContent = builderState.assistantVoiceEnabled ? t("voiceOutputOn") : t("voiceOutputOff");
    assistantAudioToggle.setAttribute("aria-label", builderState.assistantVoiceEnabled ? t("voiceOutputOn") : t("voiceOutputOff"));
    assistantAudioToggle.dataset.enabled = builderState.assistantVoiceEnabled ? "true" : "false";
    assistantAudioToggle.classList.toggle("active", builderState.assistantVoiceEnabled);
  }
  function speakAssistantMessage(message) {
    if (!builderState.assistantVoiceEnabled || !("speechSynthesis" in window) || !message) return;
    window.speechSynthesis.cancel();
    const utterance = new SpeechSynthesisUtterance(message);
    utterance.lang = languageToSpeechLocale(builderState.selectedLanguage);
    utterance.rate = 0.96;
    utterance.pitch = 1.02;
    utterance.addEventListener("start", () => setAssistantState("speaking"));
    utterance.addEventListener("end", () => setAssistantState(builderState.guidedStep === "review" ? "success" : "happy"));
    utterance.addEventListener("error", () => setAssistantState("alert"));
    window.speechSynthesis.speak(utterance);
  }
  function initVoiceInput() {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    const supported = Boolean(SpeechRecognition);
    guidedMicButton.disabled = !supported;
    guidedMicButton.hidden = !supported;
    voiceStatusText.textContent = supported ? "" : t("voiceUnsupported");
  }
  function startVoiceInput() {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    if (!SpeechRecognition) {
      voiceStatusText.textContent = t("voiceUnsupported");
      return;
    }
    const recognition = new SpeechRecognition();
    recognition.lang = languageToSpeechLocale(builderState.selectedLanguage);
    recognition.interimResults = false;
    recognition.maxAlternatives = 1;
    voiceStatusText.textContent = t("voiceListening");
    guidedMicButton.classList.add("listening");
    setAssistantState("listening");
    recognition.addEventListener("result", (event) => {
      const transcript = event.results?.[0]?.[0]?.transcript || "";
      guidedReply2.value = transcript;
      voiceStatusText.textContent = t("voiceReady");
      setAssistantState("happy");
    });
    recognition.addEventListener("error", () => {
      voiceStatusText.textContent = t("voiceUnsupported");
      guidedMicButton.classList.remove("listening");
      setAssistantState("alert");
    });
    recognition.addEventListener("end", () => {
      guidedMicButton.classList.remove("listening");
      if (builderState.assistantState === "listening") setAssistantState("neutral");
    });
    recognition.start();
  }
  function initGuidedIntake() {
    importQuickFormToGuidedState();
    if (isPublicClientSetup && !isClientWorkspaceUnlocked()) {
      renderGuidedSummary();
      resetAssistantConversation();
      return;
    }
    restoreGuidedDraft();
    applyPromptFromQuery();
    restoreGeneratedSite();
    renderGuidedSummary();
    resetAssistantConversation();
  }
  function setIntakeMode(mode) {
    const guided = mode === "guided";
    quickModeButton.classList.toggle("active", !guided);
    guidedModeButton.classList.toggle("active", guided);
    form2.classList.toggle("active", !guided);
    guidedPanel2.classList.toggle("active", guided);
    document.body.classList.toggle("guided-modal-open", guided);
    document.body.classList.remove("review-details-open", "final-review-mode");
    if (!guided && "speechSynthesis" in window) window.speechSynthesis.cancel();
    if (!guided) setAssistantState("neutral");
    if (guided) {
      importQuickFormToGuidedState();
      setAssistantState("happy");
      renderGuidedSummary();
      refreshQuickChips();
    }
  }
  function switchBackToChat() {
    importQuickFormToGuidedState();
    document.body.classList.remove("manual-form-open");
    setIntakeMode("guided");
  }
  async function sendGuidedReply() {
    const message = guidedReply2.value.trim();
    if (!message) return;
    appendChatMessage("user", message);
    guidedReply2.value = "";
    if (shouldResetRestoredWorkspaceForMessage(message)) {
      const email = builderState.clientIntakeSession?.clientEmail || builderState.clientIntakeSession?.client_email || readClientIntakeSession()?.clientEmail || readClientIntakeSession()?.client_email || localStorage.getItem("lumaPendingClientEmail") || "";
      resetGuidedStateForNewAccount();
      if (email) {
        builderState.guidedState.contactInfo.email = email;
        localStorage.setItem("lumaPendingClientEmail", email);
        try {
          await createOrResumeClientIntakeSession({
            email,
            name: "",
            reason: "new-brief",
            forceNew: true,
            immediateDraft: guidedSessionDraftForApi()
          });
        } catch (error) {
          console.warn("Could not reset restored client intake session", error);
        }
      }
      appendChatMessage("assistant", langText({
        en: "I started a clean workspace for this new project.",
        es: "Empec\xE9 un espacio limpio para este nuevo proyecto.",
        fr: "J'ai lanc\xE9 un nouvel espace propre pour ce projet.",
        pt: "Comecei um espa\xE7o limpo para este novo projeto."
      }), "success");
    }
    const attributionStep = builderState.lastAskedGuidedField || builderState.guidedStep;
    const broadLocalUpdates = inferGuidedUpdatesFromAnyMessage(message, attributionStep);
    const stepUpdates = inferGuidedUpdates(attributionStep, message);
    const localContextUpdates = { ...broadLocalUpdates, ...stepUpdates };
    if (builderState.guidedStep === "websiteIntent" && !localContextUpdates.websiteIntent) {
      localContextUpdates.websiteIntent = extractWebsiteIntent(message) || message.slice(0, 180);
    }
    if (!localContextUpdates.businessDescription && !builderState.guidedState.businessDescription && isRichIntakeMessage(message)) {
      localContextUpdates.businessDescription = message;
    }
    Object.assign(localContextUpdates, completeGuidedBriefFromMessage(message, localContextUpdates));
    mergeGuidedUpdates(localContextUpdates);
    syncTemplateSelectionFromGuidedContext(message);
    const localStudioPlan = refreshAiStudioPlanFromContext(message);
    const isPreGenerationReview = builderState.guidedStep === "review" && !builderState.currentSchema;
    if (builderState.guidedStep === "review") {
      const adjustmentLabel = langText({
        en: "Client requested adjustments",
        es: "Ajustes pedidos por el cliente",
        fr: "Ajustements demand\xE9s par le client",
        pt: "Ajustes solicitados pelo cliente"
      });
      if (builderState.currentSchema) {
        builderState.guidedState.revisionMode = "targeted_edit";
      }
      builderState.guidedState.requestedAdjustments = [
        ...arrayValue2(builderState.guidedState.requestedAdjustments),
        `${adjustmentLabel}: ${message}`
      ];
      if (builderState.currentSchema) {
        await applyDraftAdjustmentFromChat(message, localContextUpdates);
        guidedStatusText.textContent = langText({
          en: "Draft updated.",
          es: "Borrador actualizado.",
          fr: "Brouillon mis \xE0 jour.",
          pt: "Rascunho atualizado."
        });
        renderGuidedSummary();
        refreshQuickChips();
        saveGuidedDraft();
        return;
      }
    }
    guidedStatusText.textContent = t("sendingAssistant");
    setThinking(true);
    try {
      const response = await fetch(LUMA_AGENT_URL, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({
          current: guidedStateForApi(),
          message,
          currentStep: builderState.guidedStep,
          current_step: builderState.guidedStep,
          history: builderState.guidedHistory,
          selectedTemplateId: builderState.forcedTemplateSelection?.templateId || "",
          sitePlan: builderState.guidedState.sitePlan || null,
          previousSchema: builderState.currentSchema || null
        })
      });
      if (!response.ok) {
        throw new Error(await readErrorMessage(response));
      }
      const result = await response.json();
      const assistantMessage = result.assistantMessage || result.message;
      const emotion = result.emotion || (result.readyToGenerate ? "success" : "speaking");
      const updatedFields = result.updatedFields || result.updates || {};
      builderState.guidedHistory.push({ role: "user", content: message });
      builderState.guidedHistory.push({ role: "assistant", content: assistantMessage });
      mergeGuidedUpdates(updatedFields);
      await applyLumaAgentDecision(result);
      const planAfterAgent = refreshAiStudioPlanFromContext(message);
      const serverNextStep = result.next_step || result.nextStep || "";
      const missingBackendFields = arrayValue2(result.missingImportantFields);
      builderState.hasBackendIntakeSignal = true;
      builderState.backendReadyToGenerate = Boolean(result.readyToGenerate);
      builderState.backendMissingFields = missingBackendFields;
      const backendMissingStep = mapBackendSlotToGuidedField(missingBackendFields[0]);
      builderState.guidedStep = result.readyToGenerate ? "review" : backendMissingStep || normalizeNextGuidedStep(serverNextStep || builderState.guidedStep);
      builderState.lastAskedGuidedField = result.readyToGenerate ? "" : backendMissingStep;
      const serverNextQuestion = result.nextQuestion || result.next_question;
      const nextQuestion = result.readyToGenerate ? "" : chooseNextQuestionText(serverNextQuestion, builderState.guidedStep);
      const usedDevFallback = Boolean(result.used_dev_fallback || result.usedDevFallback);
      const finalAssistantMessage = sanitizeAssistantTemplateClaim(assistantMessage, planAfterAgent);
      const publicAssistantMessage = composeAssistantReply(finalAssistantMessage, nextQuestion, usedDevFallback);
      appendUnderstandingCard({ updates: updatedFields, sourceMessage: message });
      appendChatMessage("assistant", publicAssistantMessage, usedDevFallback ? "alert" : emotion);
      guidedStatusText.textContent = usedDevFallback ? t("devFallbackMissingKey") : t("summaryUpdated");
    } catch (error) {
      const updates = localContextUpdates;
      mergeGuidedUpdates(updates);
      syncTemplateSelectionFromGuidedContext(message);
      refreshAiStudioPlanFromContext(message);
      builderState.guidedStep = isPreGenerationReview ? "review" : nextSmartGuidedStep(builderState.guidedStep);
      builderState.lastAskedGuidedField = "";
      console.warn("LYRA intake assistant request failed; continuing locally.", error);
      appendUnderstandingCard({ updates, sourceMessage: message });
      if (isPreGenerationReview) {
        appendChatMessage(
          "assistant",
          langText({
            en: "I could not reach LYRA, but I saved that detail in the plan. You can retry or add another change before generating.",
            es: "No pude comunicarme con LYRA, pero guard\xE9 ese detalle en el plan. Puedes reintentar o agregar otro cambio antes de generar.",
            fr: "Je n'ai pas pu joindre LYRA, mais j'ai enregistr\xE9 ce d\xE9tail dans le plan. Vous pouvez r\xE9essayer ou ajouter une autre modification avant de g\xE9n\xE9rer.",
            pt: "N\xE3o consegui acessar a LYRA, mas salvei esse detalhe no plano. Voc\xEA pode tentar novamente ou adicionar outra mudan\xE7a antes de gerar."
          }),
          "alert"
        );
      } else {
        appendChatMessage(
          "assistant",
          composeAssistantReply(
            t("localFallbackMessage"),
            guidedQuestion(builderState.guidedStep),
            true
          ),
          "speaking"
        );
      }
      guidedStatusText.textContent = t("localFallback");
    }
    setThinking(false);
    if (builderState.guidedStep === "review") {
      builderState.guidedState.sitePlan = buildSitePlan(builderState.forcedTemplateSelection);
      builderState.guidedState.sitePlan.aiStudioPlan = builderState.guidedState.aiStudioPlan || localStudioPlan;
    }
    renderGuidedSummary();
    refreshQuickChips();
  }
  function sanitizeAssistantTemplateClaim(message = "", plan = {}) {
    const text = String(message || "");
    const templateName = plan?.recommendedTemplateName || localizedTemplateName(templatePreviewMeta(plan?.recommendedTemplateId || ""));
    if (!templateName || !/selected .* as the base|use .* as the base|mapped .* as/i.test(text)) return text;
    return langText({
      en: `I mapped this to ${templateName}. I will use that proven structure as the base and adapt the copy, products, colors and flow professionally.`,
      es: `Lo mape\xE9 como ${templateName}. Usar\xE9 esa estructura probada como base y adaptar\xE9 textos, productos, colores y flujo de forma profesional.`,
      fr: `Je l'ai associ\xE9 \xE0 ${templateName}. J'utiliserai cette structure \xE9prouv\xE9e comme base et j'adapterai les textes, produits, couleurs et parcours.`,
      pt: `Mapeei isso como ${templateName}. Vou usar essa estrutura validada como base e adaptar textos, produtos, cores e fluxo profissionalmente.`
    });
  }
  function skipGuidedQuestion() {
    appendChatMessage("user", t("skipMessage"));
    guidedAskedSteps.set(builderState.guidedStep, 1);
    builderState.guidedStep = nextSmartGuidedStep(builderState.guidedStep);
    appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), "speaking");
    renderGuidedSummary();
  }
  function appendUnderstandingCard({ updates = {}, sourceMessage = "" } = {}) {
    if (isPublicClientSetup) return;
    const filledItems = understandingItems().filter((item) => item.status === "detected");
    const missingItems = understandingItems().filter((item) => item.status === "missing");
    const changedKeys = Object.keys(updates || {}).filter((key) => key in builderState.guidedState);
    if (filledItems.length < 2 && changedKeys.length < 2 && !isRichIntakeMessage(sourceMessage)) return;
    const card = document.createElement("div");
    card.className = "chat-understanding-card";
    const heading = document.createElement("strong");
    heading.textContent = langText({
      en: "This is what I understood",
      es: "Esto es lo que entend\xED",
      fr: "Voici ce que j'ai compris",
      pt: "Foi isso que entendi"
    });
    const grid = document.createElement("div");
    grid.className = "understanding-grid";
    understandingItems().filter((item) => item.status === "detected" || item.priority).slice(0, 8).forEach((item) => {
      const pill = document.createElement("span");
      pill.className = `understanding-pill ${item.status}`;
      pill.textContent = item.value ? `${item.label}: ${item.value}` : item.label;
      grid.appendChild(pill);
    });
    const footer = document.createElement("p");
    footer.textContent = missingItems.length ? langText({
      en: `Still useful to know: ${missingItems.slice(0, 3).map((item) => item.label).join(", ")}.`,
      es: `Todav\xEDa ser\xEDa \xFAtil saber: ${missingItems.slice(0, 3).map((item) => item.label).join(", ")}.`,
      fr: `Il serait encore utile de conna\xEEtre: ${missingItems.slice(0, 3).map((item) => item.label).join(", ")}.`,
      pt: `Ainda seria \xFAtil saber: ${missingItems.slice(0, 3).map((item) => item.label).join(", ")}.`
    }) : langText({
      en: "I have enough to prepare the first draft. You can review or generate.",
      es: "Ya tengo suficiente para preparar la primera versi\xF3n. Puedes revisar o generar.",
      fr: "J'ai assez d'informations pour pr\xE9parer le premier brouillon. Vous pouvez v\xE9rifier ou g\xE9n\xE9rer.",
      pt: "J\xE1 tenho o suficiente para preparar o primeiro rascunho. Voc\xEA pode revisar ou gerar."
    });
    card.append(heading, grid, footer);
    guidedChat.appendChild(card);
    guidedChat.scrollTop = guidedChat.scrollHeight;
  }
  function understandingItems() {
    return [
      understandingItem("businessName", t("businessName"), builderState.guidedState.businessName, true),
      understandingItem("websiteIntent", t("websiteIntent"), builderState.guidedState.websiteIntent || builderState.forcedTemplateSelection?.templateId, true),
      understandingItem("industry", t("industry"), builderState.guidedState.industry, true),
      understandingItem("location", t("location"), builderState.guidedState.location, false),
      understandingItem("servicesProducts", t("servicesProducts"), arrayValue2(builderState.guidedState.servicesProducts).slice(0, 3).join(", "), true),
      understandingItem("preferredTone", t("preferredTone"), builderState.guidedState.preferredTone, false),
      understandingItem("preferredColors", t("preferredColors"), arrayValue2(builderState.guidedState.preferredColors).slice(0, 4).join(", "), false),
      understandingItem("salesMode", t("salesMode"), builderState.guidedState.salesMode, false),
      understandingItem("contactInfo", t("contactInfo"), contactInfoCompactLabel(builderState.guidedState.contactInfo), true),
      understandingItem("hasLogoPhotos", t("hasLogoPhotos"), builderState.guidedState.hasLogoPhotos || (builderState.guidedState.hasLogo ? t("logoUrl") : ""), false)
    ];
  }
  function understandingItem(key, label, value, priority) {
    return {
      key,
      label,
      value,
      priority,
      status: value ? "detected" : "missing"
    };
  }
  function normalizeGuidedStepForCurrentState(step) {
    if (builderState.currentSchema) return "review";
    if (builderState.hasBackendIntakeSignal) {
      if (builderState.backendReadyToGenerate) return "review";
      const backendMissing = missingGuidedSteps();
      if (backendMissing.length) return backendMissing[0];
    }
    const normalized = normalizeNextGuidedStep(step);
    if (normalized !== "review") return normalized;
    const requiredMissing = REQUIRED_GUIDED_STEPS.filter((item) => !isGuidedStepAnswered(item));
    if (requiredMissing.length) return requiredMissing[0];
    if (!hasEnoughContextForTemplatePreview()) return nextSmartGuidedStep("websiteIntent");
    return "review";
  }
  function renderSitePlanInChatIfNeeded() {
    if (builderState.guidedStep !== "review" || !guidedChat) return;
    guidedChat.querySelectorAll(".site-plan-card, .luma-ready-card").forEach((card) => card.remove());
    guidedChat.appendChild(renderLumaReadyCard());
    guidedChat.scrollTop = guidedChat.scrollHeight;
  }
  function keepChatting() {
    document.body.classList.remove("review-details-open", "final-review-mode", "ready-chat-mode");
    const nextMissing = nextSmartGuidedStep(builderState.guidedStep, { allowReview: false });
    builderState.guidedStep = nextMissing || "review";
    const message = nextMissing ? langText({
      en: `We can keep going. The most useful missing detail is this:

${guidedQuestion(nextMissing)}`,
      es: `Podemos seguir. Lo m\xE1s \xFAtil que falta es esto:

${guidedQuestion(nextMissing)}`,
      fr: `Nous pouvons continuer. Le d\xE9tail manquant le plus utile est celui-ci :

${guidedQuestion(nextMissing)}`,
      pt: `Podemos continuar. O detalhe mais \xFAtil que falta \xE9 este:

${guidedQuestion(nextMissing)}`
    }) : langText({
      en: "What would you like to change or add? You can ask for style changes, upload a logo/photo, add products, change colors, or clarify any detail.",
      es: "\xBFQu\xE9 quieres cambiar o agregar? Puedes pedir cambios de estilo, subir logo/foto, agregar productos, cambiar colores o aclarar cualquier detalle.",
      fr: "Que voulez-vous modifier ou ajouter ? Vous pouvez demander un changement de style, importer un logo/photo, ajouter des produits, changer les couleurs ou clarifier un d\xE9tail.",
      pt: "O que voc\xEA quer mudar ou adicionar? Pode pedir mudan\xE7a de estilo, enviar logo/foto, adicionar produtos, mudar cores ou esclarecer qualquer detalhe."
    });
    appendChatMessage("assistant", message, "speaking");
    guidedReply2.focus();
    renderGuidedSummary();
    refreshQuickChips();
  }
  function renderGuidedStepRail(stepIndex) {
    const items = document.querySelectorAll("#guidedRailSteps .guided-rail-step");
    if (!items.length) return;
    items.forEach((item) => {
      const index = Number(item.dataset.stepIndex || 0);
      item.classList.toggle("active", index === stepIndex);
      item.classList.toggle("done", index < stepIndex);
    });
  }
  function appendChatMessage(role, message, emotion = "neutral") {
    const cleanMessage = role === "assistant" || role === "system" ? sanitizeAssistantMessage(message) : message;
    if ((role === "assistant" || role === "system") && shouldSuppressAssistantMessage(cleanMessage)) {
      return;
    }
    if ((role === "assistant" || role === "system") && cleanMessage) {
      trackAssistantPrompt(cleanMessage);
    }
    const bubble = document.createElement("div");
    const state = role === "user" ? "neutral" : normalizeAssistantState(emotion);
    bubble.className = `chat-message ${role} state-${state}`;
    if (role === "assistant" || role === "system") {
      setAssistantState(state);
      const avatar = document.createElement("img");
      avatar.className = "assistant-avatar tiny";
      avatar.src = ASSISTANT_AVATARS[state] || ASSISTANT_AVATAR_FALLBACK;
      avatar.onerror = () => {
        avatar.onerror = null;
        avatar.src = ASSISTANT_AVATAR_FALLBACK;
      };
      avatar.dataset.state = state;
      avatar.alt = "";
      const text = document.createElement("span");
      text.textContent = cleanMessage;
      bubble.append(avatar, text);
      speakAssistantMessage(cleanMessage);
    } else {
      bubble.textContent = cleanMessage;
    }
    guidedChat.appendChild(bubble);
    guidedChat.scrollTop = guidedChat.scrollHeight;
  }
  function shouldSuppressAssistantMessage(message) {
    const text = String(message || "").trim();
    if (!text) return true;
    if (/homeServiceHeadline is not a function/i.test(text)) {
      if (guidedStatusText) {
        guidedStatusText.textContent = langText({
          en: "Recovered a stale template error. You can generate again.",
          es: "Se corrigi\xF3 un error viejo de plantilla. Ya puedes generar otra vez.",
          fr: "Une ancienne erreur de template a \xE9t\xE9 corrig\xE9e. Vous pouvez reg\xE9n\xE9rer.",
          pt: "Um erro antigo de template foi corrigido. Voc\xEA pode gerar novamente."
        });
      }
      return true;
    }
    const recentMessages = [...guidedChat.querySelectorAll(".chat-message.assistant span, .chat-message.system span")].slice(-5).map((node) => node.textContent?.trim()).filter(Boolean);
    return recentMessages.includes(text);
  }
  function trackAssistantPrompt(message) {
    const signature = questionSignature(message);
    if (signature && signature === builderState.lastAssistantPromptSignature) return;
    builderState.lastAssistantPromptSignature = signature;
    if (!builderState.guidedStep || builderState.guidedStep === "review") return;
    if (message.includes(guidedQuestion(builderState.guidedStep)) || /[?¿]/.test(message)) {
      guidedAskedSteps.set(builderState.guidedStep, (guidedAskedSteps.get(builderState.guidedStep) || 0) + 1);
    }
  }
  function nextSmartGuidedStep(referenceStep = builderState.guidedStep, options = {}) {
    const { allowReview = true } = options;
    const requiredMissing = REQUIRED_GUIDED_STEPS.find((step) => !isGuidedStepAnswered(step));
    if (requiredMissing) return requiredMissing;
    const referenceIndex = SMART_GUIDED_STEP_PRIORITY.indexOf(referenceStep);
    const ordered = referenceIndex >= 0 ? [
      ...SMART_GUIDED_STEP_PRIORITY.slice(referenceIndex + 1),
      ...SMART_GUIDED_STEP_PRIORITY.slice(0, referenceIndex + 1)
    ] : SMART_GUIDED_STEP_PRIORITY;
    const optionalMissing = ordered.find((step) => {
      if (!OPTIONAL_GUIDED_STEPS.has(step)) return false;
      if (isGuidedStepAnswered(step)) return false;
      const askedCount = guidedAskedSteps.get(step) || 0;
      return askedCount < 1;
    });
    if (optionalMissing) return optionalMissing;
    return allowReview ? "review" : "";
  }
  function composeAssistantReply(message, nextQuestion, usedFallback = false) {
    const cleanMessage = sanitizeAssistantMessage(message || "");
    const cleanQuestion = sanitizeAssistantMessage(nextQuestion || "");
    const base = usedFallback && !cleanMessage ? t("localFallbackMessage") : cleanMessage;
    if (!cleanQuestion) return base || t("localFallbackMessage");
    if (!base || isDuplicateQuestion(base, cleanQuestion) || base.includes(cleanQuestion)) return cleanQuestion;
    return `${base}

${cleanQuestion}`;
  }
  function sanitizeAssistantMessage(message) {
    return String(message || "").replace(/Development fallback:?\s*/gi, "").replace(/Load failed\.?/gi, "").replace(/\s{2,}/g, " ").trim();
  }
  function nextGuidedStep(step) {
    const index = GUIDED_STEPS.indexOf(step);
    for (let i = Math.min((index < 0 ? 0 : index) + 1, GUIDED_STEPS.length - 1); i < GUIDED_STEPS.length; i += 1) {
      const candidate = GUIDED_STEPS[i];
      if (candidate === "review" || !isGuidedStepAnswered(candidate)) return candidate;
    }
    return "review";
  }
  function normalizeNextGuidedStep(step) {
    const candidate = GUIDED_STEPS.includes(step) ? step : nextGuidedStep(builderState.guidedStep);
    if (candidate === "review") return "review";
    return isGuidedStepAnswered(candidate) ? nextGuidedStep(candidate) : candidate;
  }
  function isGuidedStepAnswered(step) {
    if (step === "websiteIntent") return Boolean(builderState.guidedState.websiteIntent);
    if (step === "businessName") return Boolean(builderState.guidedState.businessName);
    if (step === "businessDescription") return Boolean(builderState.guidedState.businessDescription);
    if (step === "industry") return Boolean(builderState.guidedState.industry);
    if (step === "location") return Boolean(builderState.guidedState.location);
    if (step === "servicesProducts") return arrayValue2(builderState.guidedState.servicesProducts).length > 0;
    if (step === "targetAudience") return Boolean(builderState.guidedState.targetAudience);
    if (step === "preferredTone") return Boolean(builderState.guidedState.preferredTone);
    if (step === "preferredColors") {
      return arrayValue2(builderState.guidedState.preferredColors).length > 0 || arrayValue2(builderState.guidedState.logoPalette).length > 0;
    }
    if (step === "contactInfo") return Object.keys(builderState.guidedState.contactInfo || {}).length > 0;
    if (step === "salesMode") return Boolean(builderState.guidedState.salesMode);
    if (step === "hasLogoPhotos") {
      return Boolean(builderState.guidedState.hasLogoPhotos || builderState.guidedState.hasLogo || builderState.guidedState.hasPhotos || builderState.guidedState.aiGeneratedLogoRequested || builderState.guidedState.logoPreference);
    }
    if (step === "desiredDomain") return Boolean(builderState.guidedState.desiredDomain);
    return false;
  }
  function isRichIntakeMessage(text) {
    return text.length > 80 || /nombre|negocio|tienda|ubic|productos|servicios|contact|telefono|teléfono|whatsapp|instagram|colores|vende|ofrece|audiencia|clientes|location|products|services|email|phone|colors/i.test(text);
  }
  function ensureServerIntakeGate() {
    let gate = document.querySelector("#serverIntakeGate");
    if (gate) return gate;
    gate = document.createElement("div");
    gate.id = "serverIntakeGate";
    gate.className = "server-intake-gate";
    gate.innerHTML = `
    <p class="server-intake-question"></p>
    <textarea name="server_intake_reply" rows="2" placeholder="Type your answer..."></textarea>
  `;
    form2.prepend(gate);
    return gate;
  }
  async function applyDraftAdjustmentFromChat(message, localContextUpdates = {}) {
    const requestsDifferentDesign = isExplicitRedesignRequest(message);
    if (requestsDifferentDesign) {
      builderState.guidedState.designVariantOffset = Math.max(0, Number(builderState.guidedState.designVariantOffset) || 0) + 1;
    }
    guidedStatusText.textContent = langText({
      en: "Applying that to the draft...",
      es: "Aplicando eso al borrador...",
      fr: "Application au brouillon...",
      pt: "Aplicando isso ao rascunho..."
    });
    setThinking(true);
    builderAvatarManager?.setState("thinking", { source: "draft-adjustment" });
    try {
      applyGuidedStateToForm();
      syncTemplateSelectionFromGuidedContext(message);
      const payload = await collectPayload();
      const text = normalizeTemplateIntentText([
        message,
        payload.business_description,
        payload.industry,
        arrayValue2(payload.services_products).join(" "),
        payload.preferred_tone,
        arrayValue2(payload.preferred_colors).join(" ")
      ].join(" "));
      const templateSelection = await selectTemplateForPayload(payload);
      const explicitTemplateSwitch = explicitlyRequestsTemplateSwitch(text);
      const serverEdit = builderState.currentSchema && !explicitTemplateSwitch ? await requestLyraSchemaEdit(message, payload, localContextUpdates, templateSelection).catch((error) => {
        console.warn("Lyra server edit unavailable; falling back to local patch.", error);
        return null;
      }) : null;
      if (serverEdit?.patchedSchema) {
        builderState.currentSchema = prepareWebsiteConfig(serverEdit.patchedSchema, payload, templateSelection);
        if (briefRequestsCyberpunk(text)) builderState.currentSchema = applyCyberpunkVisualDirection(builderState.currentSchema);
        builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
        builderState.selectedPageKey = builderState.currentSchema.pages?.[0]?.page_key || "home";
        builderState.selectedVariantId = builderState.currentSchema.design_variants?.[0]?.id || builderState.selectedVariantId || "";
        saveGeneratedSite({
          business_id: builderState.currentBusinessId,
          site_id: builderState.currentSiteId,
          generation_id: builderState.currentGenerationId,
          storage_status: builderState.currentSiteId ? "ai_revision_preview" : "ai_revision_preview_unsaved",
          schema: builderState.currentSchema,
          used_dev_mock: false
        });
        renderEditor();
        renderPreview();
        showGeneratedClientPreview();
        document.body.classList.add("draft-adjust-open");
        guidedPanel2.classList.add("active");
        appendChatMessage(
          "assistant",
          serverEdit.patchSummary || draftAdjustmentReply(false, templateSelection),
          "success"
        );
        builderAvatarManager?.setState("success", { source: "draft-adjustment" });
        return;
      }
      const shouldRebuildFromTemplate = shouldRebuildDraftFromTemplate(message, payload, templateSelection);
      const nextSchema = shouldRebuildFromTemplate ? buildInstantTemplateSchema(payload, templateSelection) : applyTargetedSchemaPatch(builderState.currentSchema, message, payload, localContextUpdates, templateSelection);
      const mergedSchema = templateSelection ? mergeTemplateSelectionIntoSchema(nextSchema, templateSelection) : nextSchema;
      builderState.currentSchema = prepareWebsiteConfig(mergedSchema, payload, templateSelection);
      if (briefRequestsCyberpunk(text)) builderState.currentSchema = applyCyberpunkVisualDirection(builderState.currentSchema);
      builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
      builderState.selectedPageKey = builderState.currentSchema.pages?.[0]?.page_key || "home";
      builderState.selectedVariantId = builderState.currentSchema.design_variants?.[0]?.id || builderState.selectedVariantId || "";
      saveGeneratedSite({
        business_id: builderState.currentBusinessId,
        site_id: builderState.currentSiteId,
        generation_id: builderState.currentGenerationId,
        storage_status: builderState.currentSiteId ? "local_revision_preview" : "local_revision_preview_unsaved",
        schema: builderState.currentSchema,
        used_dev_mock: false
      });
      renderEditor();
      renderPreview();
      showGeneratedClientPreview();
      document.body.classList.add("draft-adjust-open");
      guidedPanel2.classList.add("active");
      appendChatMessage(
        "assistant",
        requestsDifferentDesign ? langText({
          en: "I kept your content and changed the visual composition: a different hero and section rhythm are now applied.",
          es: "Mantuve tu contenido y cambi\xE9 la composici\xF3n visual: ya apliqu\xE9 otro hero y otro ritmo de secciones.",
          fr: "J'ai conserv\xE9 votre contenu et modifi\xE9 la composition visuelle : un autre hero et un autre rythme de sections sont appliqu\xE9s.",
          pt: "Mantive seu conte\xFAdo e alterei a composi\xE7\xE3o visual: outro hero e outro ritmo de se\xE7\xF5es j\xE1 foram aplicados."
        }) : draftAdjustmentReply(shouldRebuildFromTemplate, templateSelection),
        "success"
      );
      builderAvatarManager?.setState("success", { source: "draft-adjustment" });
    } catch (error) {
      console.warn("Draft adjustment could not be applied locally.", error);
      appendChatMessage(
        "assistant",
        langText({
          en: "I understood the change, but I could not apply it visually in this pass. I saved it as a specific revision instruction so the next generation changes only that.",
          es: "Entend\xED el cambio, pero no pude aplicarlo visualmente en este pase. Lo dej\xE9 guardado como instrucci\xF3n espec\xEDfica para que la pr\xF3xima generaci\xF3n cambie s\xF3lo eso.",
          fr: "J'ai compris le changement, mais je n'ai pas pu l'appliquer visuellement maintenant. Je l'ai gard\xE9 comme instruction pr\xE9cise pour la prochaine g\xE9n\xE9ration.",
          pt: "Entendi a mudan\xE7a, mas n\xE3o consegui aplicar visualmente agora. Deixei como instru\xE7\xE3o espec\xEDfica para a pr\xF3xima gera\xE7\xE3o mudar s\xF3 isso."
        }),
        "alert"
      );
      builderAvatarManager?.setState("alert", { source: "draft-adjustment" });
    } finally {
      setThinking(false);
    }
  }
  function closeDraftAdjustmentChat() {
    document.body.classList.remove("draft-adjust-open", "guided-modal-open", "review-details-open", "final-review-mode");
    guidedPanel2.classList.remove("active");
    document.body.classList.add("generated-preview-open", "client-preview-mode");
    syncLyraExperienceMode();
    storageStatus2.textContent = langText({
      en: "Draft preview is still open.",
      es: "El preview del borrador sigue abierto.",
      fr: "L'apercu du brouillon reste ouvert.",
      pt: "O preview do rascunho continua aberto."
    });
  }
  function assistantVisibleCopy(value) {
    return String(value ?? "").replace(/\bLuma\b/g, "LYRA").replace(/\bGNU Dev\b/g, "LYRA").replace(/gnu-dev-assistant/g, "nixie_idle");
  }
  function publicAssistantCopy(value) {
    return isPublicClientSetup ? assistantVisibleCopy(value) : value;
  }

  // src/ai-builder/section-policy.js
  var REUSABLE_PLANNER_TYPES = /* @__PURE__ */ new Set([
    "QuoteRequestForm",
    "CapabilitiesEquipment",
    "PortfolioGallery",
    "VideoShowcase",
    "CourseOffering"
  ]);
  function keepEssentialSections(sections, maxSections) {
    const priority = { Hero: 0, ProductGrid: 1, ServiceList: 2, FeatureBand: 3, Testimonials: 4, About: 5, Gallery: 6, Contact: 7, Footer: 8 };
    const limit = Math.max(3, maxSections);
    const sorted = [...sections].sort((a, b) => (priority[a.type] ?? 9) - (priority[b.type] ?? 9));
    const selected = sorted.slice(0, limit);
    const preserve = (section) => {
      if (!section || selected.includes(section)) return;
      if (selected.length < limit) {
        selected.push(section);
        return;
      }
      let replaceIndex = -1;
      for (let index = selected.length - 1; index >= 0; index -= 1) {
        const candidate = selected[index];
        if (candidate.type !== "Contact" && !REUSABLE_PLANNER_TYPES.has(candidate.type)) {
          replaceIndex = index;
          break;
        }
      }
      if (replaceIndex >= 0) selected[replaceIndex] = section;
    };
    sections.filter((section) => REUSABLE_PLANNER_TYPES.has(section.type)).forEach(preserve);
    preserve(sections.find((section) => section.type === "Contact"));
    return selected;
  }

  // src/ai-builder/editor.js
  function ensureLiveSitePreviewCard() {
    if (!builderState.liveSitePreviewCard) {
      builderState.liveSitePreviewCard = document.createElement("section");
      builderState.liveSitePreviewCard.className = "live-site-preview-card";
      builderState.liveSitePreviewCard.setAttribute("aria-label", "Live website draft preview");
    }
    if (liveSitePreviewMount && builderState.liveSitePreviewCard.parentElement !== liveSitePreviewMount) {
      liveSitePreviewMount.appendChild(builderState.liveSitePreviewCard);
    } else if (!liveSitePreviewMount && guidedChatCard && guidedChat && builderState.liveSitePreviewCard.parentElement !== guidedChatCard) {
      guidedChatCard.insertBefore(builderState.liveSitePreviewCard, guidedChat);
    }
    return builderState.liveSitePreviewCard;
  }
  function renderAiContractPreview(card) {
    const renderer = window.KreatonTemplateRenderer;
    const plan = builderState.guidedState.sitePlan || {};
    const pages = Array.isArray(plan.pages) ? plan.pages : [];
    const homePage = pages.find((page) => /home|inicio/i.test(`${page.pageId || page.pageKey || page.title || ""}`)) || pages[0];
    const hasAiBlocks = Boolean(
      renderer?.buildPageHTML && homePage && Array.isArray(homePage.sections) && homePage.sections.some((section) => section?.componentType)
    );
    if (!hasAiBlocks) return false;
    const selection = livePreviewTemplateSelection();
    const pageHtml = renderer.buildPageHTML(homePage, {
      templateId: plan.templateId || plan.recommendedTemplateId || selection?.templateId || "",
      catalogType: plan.catalogType || selection?.catalogType || "",
      catalogItems: Array.isArray(plan.catalogItems) ? plan.catalogItems : [],
      designTokens: plan.designTokens || plan.colors || {}
    });
    if (!pageHtml) return false;
    card.classList.add("live-render-card-host");
    card.innerHTML = `
    <div class="live-template-preview-shell ai-contract-preview-shell canvas-fade-in">
      ${pageHtml}
    </div>
  `;
    return true;
  }
  function renderNeutralLiveWorkspace() {
    const offerItems = meaningfulOfferItems(builderState.guidedState.servicesProducts);
    const hasInitialGoal = Boolean(builderState.guidedState.websiteIntent);
    const hasOffer = offerItems.length > 0 || Boolean(builderState.guidedState.businessDescription);
    const hasDirection = hasInitialGoal && hasOffer;
    const steps = [
      {
        title: langText({ en: "Business model", es: "Modelo del negocio", fr: "Modele business", pt: "Modelo do negocio" }),
        text: builderState.guidedState.websiteIntent || langText({ en: "Website, store, marketplace, catalog, booking, leads or company site", es: "Web, tienda, marketplace, catalogo, reservas, captacion o empresa", fr: "Site, boutique, marketplace, catalogue, reservation, leads ou entreprise", pt: "Site, loja, marketplace, catalogo, reservas, leads ou empresa" }),
        active: hasInitialGoal
      },
      {
        title: langText({ en: "Offer intelligence", es: "Inteligencia de oferta", fr: "Intelligence de l'offre", pt: "Inteligencia da oferta" }),
        text: offerItems.join(", ") || builderState.guidedState.businessDescription || langText({ en: "LYRA is waiting for product, service or catalog context", es: "LYRA espera contexto de productos, servicios o catalogo", fr: "LYRA attend le contexte des produits, services ou catalogue", pt: "LYRA aguarda contexto de produtos, servicos ou catalogo" }),
        active: hasOffer
      },
      {
        title: langText({ en: "Template decision", es: "Decision de plantilla", fr: "Decision template", pt: "Decisao de template" }),
        text: hasDirection ? langText({
          en: "KREATON will match the request against the template library before showing a draft.",
          es: "KREATON cruzara la solicitud con la biblioteca de plantillas antes de mostrar un borrador.",
          fr: "KREATON compare la demande avec la bibliotheque de templates avant d'afficher un brouillon.",
          pt: "KREATON compara o pedido com a biblioteca de templates antes de mostrar um rascunho."
        }) : langText({
          en: "No random preview. The visual base appears only when the business context is clear.",
          es: "Sin preview random. La base visual aparece solo cuando el contexto del negocio esta claro.",
          fr: "Pas de preview aleatoire. La base visuelle apparait quand le contexte est clair.",
          pt: "Sem preview aleatorio. A base visual aparece quando o contexto fica claro."
        }),
        active: hasDirection
      }
    ];
    return `
    <div class="neutral-live-workspace">
      <div class="neutral-live-orbit" aria-hidden="true">
        <span></span><span></span><span></span>
      </div>
      <section class="neutral-live-copy">
        <span>${escapeHtml(langText({ en: "KREATON AI Studio", es: "KREATON AI Studio", fr: "KREATON AI Studio", pt: "KREATON AI Studio" }))}</span>
        <h2>${escapeHtml(langText({ en: "LYRA is mapping the business before designing", es: "LYRA esta entendiendo el negocio antes de dise\xF1ar", fr: "LYRA cartographie le business avant de designer", pt: "LYRA mapeia o negocio antes de desenhar" }))}</h2>
        <p>${escapeHtml(langText({
      en: "Describe the business naturally. LYRA extracts the model, catalog depth, audience, style and sales flow, then chooses the right template family.",
      es: "Describe el negocio de forma natural. LYRA extrae modelo, profundidad de catalogo, publico, estilo y flujo de venta, luego elige la familia de plantilla correcta.",
      fr: "Decrivez l'activite naturellement. LYRA extrait le modele, le catalogue, l'audience, le style et le parcours de vente, puis choisit la bonne famille de template.",
      pt: "Descreva o negocio naturalmente. LYRA extrai modelo, profundidade do catalogo, publico, estilo e venda, depois escolhe a familia de template correta."
    }))}</p>
      </section>
      <div class="neutral-live-steps">
        ${steps.map((step, index) => `
          <article class="${step.active ? "active" : ""}">
            <b>${index + 1}</b>
            <div>
              <strong>${escapeHtml(step.title)}</strong>
              <span>${escapeHtml(step.text)}</span>
            </div>
          </article>
        `).join("")}
      </div>
    </div>
  `;
  }
  function livePreviewPayload() {
    const offerItems = meaningfulOfferItems(builderState.guidedState.servicesProducts);
    return {
      business_name: builderState.guidedState.businessName || langText({ en: "Your business", es: "Tu negocio", fr: "Votre entreprise", pt: "Seu neg\xF3cio" }),
      business_description: builderState.guidedState.businessDescription || builderState.guidedState.websiteIntent || "",
      industry: builderState.guidedState.industry || inferCommerceIndustry(builderState.guidedState),
      location: builderState.guidedState.location || "",
      services_products: offerItems.length ? offerItems : livePreviewFallbackItems(),
      target_audience: builderState.guidedState.targetAudience || "",
      preferred_tone: builderState.guidedState.preferredTone || "",
      preferred_colors: arrayValue2(builderState.guidedState.preferredColors),
      contact_info: builderState.guidedState.contactInfo || {},
      selectedLanguage: builderState.selectedLanguage,
      salesMode: builderState.guidedState.salesMode || builderState.guidedState.websiteIntent || "",
      logoPalette: builderState.guidedState.logoPalette || [],
      assets: [
        ...builderState.guidedState.logoUrl ? [{ asset_type: "logo", url: builderState.guidedState.logoUrl }] : [],
        ...arrayValue2(builderState.guidedState.photoUrls).map((url) => ({ asset_type: "photo", url }))
      ],
      brand: builderState.guidedState.brand
    };
  }
  function livePreviewPublicCopy(profile, items = []) {
    const name = builderState.guidedState.businessName || langText({
      en: "Your business",
      es: "Tu negocio",
      fr: "Votre entreprise",
      pt: "Seu negocio"
    });
    const productFocus = publicProductFocus(items, builderState.guidedState.industry);
    const templates = {
      marketplace: {
        headline: langText({
          en: `${name} makes discovery feel simple`,
          es: `${name} convierte descubrir productos en algo simple`,
          fr: `${name} rend la decouverte simple`,
          pt: `${name} torna a descoberta simples`
        }),
        subtitle: langText({
          en: `A search-first shopping experience for ${productFocus}, built with clear categories, featured finds, and a fast path to buy or ask.`,
          es: `Una experiencia de compra con busqueda clara para ${productFocus}, con categorias ordenadas, destacados y una ruta rapida para comprar o preguntar.`,
          fr: `Une experience d'achat claire pour ${productFocus}, avec categories, selections et parcours rapide vers l'achat ou la demande.`,
          pt: `Uma experiencia de compra clara para ${productFocus}, com categorias, destaques e caminho rapido para comprar ou perguntar.`
        }),
        supportingLine: langText({
          en: "LYRA is shaping this as a catalog with search, categories, featured offers and editable product sections.",
          es: "LYRA lo esta armando como catalogo con busqueda, categorias, ofertas destacadas y secciones editables.",
          fr: "LYRA le structure comme catalogue avec recherche, categories, offres et sections modifiables.",
          pt: "A LYRA esta estruturando como catalogo com busca, categorias, ofertas e secoes editaveis."
        })
      },
      fashion: {
        headline: langText({
          en: `${name} gets a collection-led storefront`,
          es: `${name} se presenta como una tienda visual por colecciones`,
          fr: `${name} devient une boutique visuelle par collections`,
          pt: `${name} vira uma loja visual por colecoes`
        }),
        subtitle: langText({
          en: `An editorial storefront for ${productFocus}, with drops, looks, product cards and a clean path to shop.`,
          es: `Una tienda editorial para ${productFocus}, con drops, looks, tarjetas de producto y una ruta clara para comprar.`,
          fr: `Une boutique editoriale pour ${productFocus}, avec drops, looks, fiches produit et achat clair.`,
          pt: `Uma loja editorial para ${productFocus}, com drops, looks, cards de produto e compra clara.`
        }),
        supportingLine: langText({
          en: "The client notes guide the visual direction; the page copy is rewritten as customer-facing retail copy.",
          es: "Las notas del cliente guian la direccion visual; el texto se reescribe como copy comercial para compradores.",
          fr: "Les notes client guident la direction visuelle; le texte devient un copy commercial.",
          pt: "As notas do cliente guiam a direcao visual; o texto vira copy comercial."
        })
      },
      restaurant: {
        headline: langText({
          en: `${name} gets a menu that sells`,
          es: `${name} obtiene un menu pensado para vender`,
          fr: `${name} obtient un menu pense pour vendre`,
          pt: `${name} ganha um menu feito para vender`
        }),
        subtitle: langText({
          en: "A warm food experience with signature items, specials, hours, location and quick ordering.",
          es: "Una experiencia calida con platos destacados, especiales, horarios, ubicacion y pedidos rapidos.",
          fr: "Une experience chaleureuse avec plats signature, offres, horaires, adresse et commande rapide.",
          pt: "Uma experiencia acolhedora com destaques, especiais, horarios, localizacao e pedidos rapidos."
        }),
        supportingLine: langText({
          en: "LYRA is turning the intake into a menu structure, not pasting internal notes onto the page.",
          es: "LYRA convierte la informacion en estructura de menu, no pega notas internas en la pagina.",
          fr: "LYRA transforme les infos en structure de menu, sans coller les notes internes.",
          pt: "A LYRA transforma as informacoes em menu, sem colar notas internas."
        })
      },
      service: {
        headline: langText({
          en: `${name} gets a trust-first website`,
          es: `${name} obtiene una pagina enfocada en confianza`,
          fr: `${name} obtient un site axe confiance`,
          pt: `${name} ganha um site focado em confianca`
        }),
        subtitle: langText({
          en: `A clear service page for ${productFocus}, with proof, process, contact options and quote-ready sections.`,
          es: `Una pagina clara para ${productFocus}, con pruebas, proceso, contacto y secciones listas para cotizar.`,
          fr: `Une page claire pour ${productFocus}, avec preuves, processus, contact et demande de devis.`,
          pt: `Uma pagina clara para ${productFocus}, com provas, processo, contato e orcamento.`
        }),
        supportingLine: langText({
          en: "LYRA is translating the brief into positioning, proof and conversion sections.",
          es: "LYRA traduce el brief en posicionamiento, prueba y secciones de conversion.",
          fr: "LYRA traduit le brief en positionnement, preuves et conversion.",
          pt: "A LYRA traduz o briefing em posicionamento, prova e conversao."
        })
      },
      premium: {
        headline: langText({
          en: `${name} gets a polished brand showcase`,
          es: `${name} obtiene una presentacion de marca pulida`,
          fr: `${name} obtient une presentation de marque soignee`,
          pt: `${name} ganha uma apresentacao de marca polida`
        }),
        subtitle: langText({
          en: `A premium presentation for ${productFocus}, with refined messaging, strong visuals and editable story sections.`,
          es: `Una presentacion premium para ${productFocus}, con mensaje refinado, visuales fuertes y secciones editables.`,
          fr: `Une presentation premium pour ${productFocus}, avec message raffine, visuels forts et sections modifiables.`,
          pt: `Uma apresentacao premium para ${productFocus}, com mensagem refinada, visual forte e secoes editaveis.`
        }),
        supportingLine: langText({
          en: "The preview uses designer-written copy inferred from the intake, not the raw answer.",
          es: "El preview usa copy escrito como disenador a partir del intake, no la respuesta cruda.",
          fr: "L'apercu utilise un copy deduit du brief, pas la reponse brute.",
          pt: "O preview usa copy criado a partir do briefing, nao a resposta bruta."
        })
      }
    };
    return templates[profile.kind] || templates.premium;
  }
  function livePreviewNavItems(profile) {
    const sets = {
      marketplace: ["Shop", "Categories", "Deals", "Cart"],
      listings: ["Search", "Listings", "Sellers", "Contact"],
      fashion: ["New drop", "Collections", "Lookbook", "Fit"],
      restaurant: ["Menu", "Specials", "Order", "Location"],
      service: ["Services", "Process", "Proof", "Contact"],
      premium: ["Overview", "Features", "Story", "Buy"]
    };
    return (sets[profile.kind] || sets.premium).map((item) => translatePreviewNav(item));
  }
  function translatePreviewNav(item) {
    const dictionary = {
      es: { Shop: "Tienda", Categories: "Categorias", Deals: "Ofertas", Cart: "Carrito", Search: "Buscar", Listings: "Listados", Sellers: "Vendedores", Contact: "Contacto", "New drop": "Nuevo drop", Collections: "Colecciones", Lookbook: "Lookbook", Fit: "Tallas", Menu: "Menu", Specials: "Especiales", Order: "Pedir", Location: "Ubicacion", Services: "Servicios", Process: "Proceso", Proof: "Prueba", Overview: "Inicio", Features: "Detalles", Story: "Historia", Buy: "Comprar" },
      fr: { Shop: "Boutique", Categories: "Categories", Deals: "Offres", Cart: "Panier", Search: "Recherche", Listings: "Annonces", Sellers: "Vendeurs", Contact: "Contact", "New drop": "Nouveautes", Collections: "Collections", Lookbook: "Lookbook", Fit: "Tailles", Menu: "Menu", Specials: "Specials", Order: "Commander", Location: "Adresse", Services: "Services", Process: "Processus", Proof: "Preuves", Overview: "Accueil", Features: "Details", Story: "Histoire", Buy: "Acheter" },
      pt: { Shop: "Loja", Categories: "Categorias", Deals: "Ofertas", Cart: "Carrinho", Search: "Busca", Listings: "Anuncios", Sellers: "Vendedores", Contact: "Contato", "New drop": "Novo drop", Collections: "Colecoes", Lookbook: "Lookbook", Fit: "Tamanhos", Menu: "Menu", Specials: "Especiais", Order: "Pedir", Location: "Localizacao", Services: "Servicos", Process: "Processo", Proof: "Prova", Overview: "Inicio", Features: "Detalhes", Story: "Historia", Buy: "Comprar" }
    };
    return dictionary[builderState.selectedLanguage]?.[item] || item;
  }
  function livePreviewItemMicrocopy(profile, index) {
    const copy = {
      marketplace: [
        langText({ en: "Prepared for category browsing and quick discovery.", es: "Preparado para categorias y busqueda rapida.", fr: "Pret pour categories et decouverte rapide.", pt: "Pronto para categorias e descoberta rapida." }),
        langText({ en: "Deal-ready product block.", es: "Bloque listo para ofertas.", fr: "Bloc pret pour offres.", pt: "Bloco pronto para ofertas." }),
        langText({ en: "Filterable and editable.", es: "Filtrable y editable.", fr: "Filtrable et modifiable.", pt: "Filtravel e editavel." })
      ],
      fashion: [
        langText({ en: "Visual collection card.", es: "Card visual de coleccion.", fr: "Carte collection visuelle.", pt: "Card visual de colecao." }),
        langText({ en: "Lookbook-ready section.", es: "Seccion lista para lookbook.", fr: "Section lookbook.", pt: "Secao pronta para lookbook." }),
        langText({ en: "Drop and fit details.", es: "Drop y detalles de talla.", fr: "Drop et tailles.", pt: "Drop e tamanhos." })
      ],
      restaurant: [
        langText({ en: "Menu item with order path.", es: "Producto de menu con pedido.", fr: "Plat avec commande.", pt: "Item de menu com pedido." }),
        langText({ en: "Specials and combos.", es: "Especiales y combos.", fr: "Specials et menus.", pt: "Especiais e combos." }),
        langText({ en: "Pickup or delivery CTA.", es: "CTA para pickup o delivery.", fr: "CTA retrait/livraison.", pt: "CTA retirada/entrega." })
      ],
      service: [
        langText({ en: "Service benefit and proof.", es: "Beneficio y prueba.", fr: "Benefice et preuve.", pt: "Beneficio e prova." }),
        langText({ en: "Process step.", es: "Paso del proceso.", fr: "Etape processus.", pt: "Etapa do processo." }),
        langText({ en: "Quote-ready contact.", es: "Contacto listo para cotizar.", fr: "Contact pret pour devis.", pt: "Contato pronto para orcamento." })
      ]
    };
    const list = copy[profile.kind] || [
      langText({ en: "Editable section preview.", es: "Seccion editable.", fr: "Section modifiable.", pt: "Secao editavel." }),
      langText({ en: "Premium detail block.", es: "Bloque premium.", fr: "Bloc premium.", pt: "Bloco premium." }),
      langText({ en: "Conversion-ready CTA.", es: "CTA listo para convertir.", fr: "CTA pret a convertir.", pt: "CTA pronto para converter." })
    ];
    return list[index % list.length];
  }
  function livePreviewFallbackItems() {
    return [
      langText({ en: "Main offer", es: "Oferta principal", fr: "Offre principale", pt: "Oferta principal" }),
      langText({ en: "Proof section", es: "Prueba/confianza", fr: "Preuve/confiance", pt: "Prova/confian\xE7a" }),
      langText({ en: "Contact CTA", es: "Boton de contacto", fr: "CTA contact", pt: "CTA contato" })
    ];
  }
  function shouldShowAssetPrompt() {
    const typed = guidedReply2?.value || "";
    if (wantsAiGeneratedLogo(typed) || wantsAiGeneratedLogo(builderState.guidedState.hasLogoPhotos)) return false;
    return document.body.classList.contains("review-details-open") || builderState.guidedStep === "hasLogoPhotos" || wantsToUploadAssets(typed) || Boolean((builderState.guidedState.hasLogo || builderState.guidedState.hasPhotos) && !wantsAiGeneratedLogo(builderState.guidedState.hasLogoPhotos));
  }
  function updateAssetPromptVisibility() {
    guidedAssetPrompt?.classList.toggle("active", shouldShowAssetPrompt());
  }
  function wantsToUploadAssets(value) {
    const text = String(value || "").toLowerCase();
    if (wantsAiGeneratedLogo(text)) return false;
    return /subir|cargar|adjuntar|upload|attach|tengo logo|tengo fotos|logo listo|fotos listas|photo ready|photos ready|use my logo|usar mi logo/.test(text);
  }
  function renderAssetPreviews() {
    guidedLogoPreview.src = builderState.guidedState.logoUrl || "";
    guidedLogoPreview.classList.toggle("active", Boolean(builderState.guidedState.logoUrl));
    guidedPhotoPreview.innerHTML = arrayValue2(builderState.guidedState.photoUrls).slice(0, 4).map((url) => `<img src="${escapeAttribute(url)}" alt="">`).join("");
    renderBrandKit();
  }
  function renderBrandKit(brand = builderState.guidedState.brand) {
    if (!brandKitPanel) return;
    const normalizedBrand = normalizeBrand(brand || (builderState.guidedState.logoUrl ? { logoUrl: builderState.guidedState.logoUrl } : null));
    brandKitPanel.hidden = !normalizedBrand.logoUrl && !builderState.guidedState.logoPalette.length;
    if (brandKitPanel.hidden) {
      brandKitPanel.innerHTML = "";
      return;
    }
    const swatches = brandPalette(normalizedBrand).map((color) => `<span class="brand-swatch" style="--swatch:${escapeAttribute(color)}" title="${escapeAttribute(color)}"></span>`).join("");
    brandKitPanel.innerHTML = `
    <div class="brand-kit-head">
      ${normalizedBrand.logoUrl ? `<img src="${escapeAttribute(normalizedBrand.logoUrl)}" alt="">` : ""}
      <div>
        <strong>Brand Kit</strong>
        <span>${escapeHtml(normalizedBrand.styleDirection)}</span>
      </div>
    </div>
    <div class="brand-kit-swatches">${swatches}</div>
    <div class="brand-kit-meta">
      <span>${escapeHtml(fontPairingLabel(normalizedBrand.fontPairing))}</span>
      <span>Radius ${escapeHtml(normalizedBrand.borderRadius)}</span>
      <button class="brand-kit-button" type="button">Button preview</button>
    </div>
  `;
  }
  function renderGuidedBriefReview() {
    if (!guidedBriefReview) return;
    syncTemplateSelectionFromGuidedContext();
    const offer = meaningfulOfferItems(builderState.guidedState.servicesProducts).slice(0, 3).join(", ");
    const summary = [
      builderState.guidedState.businessName,
      builderState.guidedState.industry,
      builderState.guidedState.location,
      builderState.guidedState.salesFlow || builderState.guidedState.salesMode,
      offer
    ].filter(Boolean);
    guidedBriefReview.innerHTML = `
    <section class="business-review-summary">
      <strong>${escapeHtml(langText({ en: "What we understand about your business", es: "Esto entendimos de tu negocio", fr: "Ce que nous avons compris de votre entreprise", pt: "O que entendemos sobre seu neg\xF3cio" }))}</strong>
      <p>${escapeHtml(summary.join(" \xB7 ") || langText({ en: "Add a few details and LYRA will prepare your draft.", es: "Agrega algunos detalles y LYRA preparar\xE1 tu borrador.", fr: "Ajoutez quelques d\xE9tails et LYRA pr\xE9parera votre brouillon.", pt: "Adicione alguns detalhes e a LYRA preparar\xE1 seu rascunho." }))}</p>
    </section>
  `;
  }
  function renderLumaReadyCard() {
    const offer = meaningfulOfferItems(builderState.guidedState.servicesProducts).slice(0, 3).join(", ");
    const summary = [
      builderState.guidedState.businessName,
      builderState.guidedState.industry,
      builderState.guidedState.location,
      builderState.guidedState.salesFlow || builderState.guidedState.salesMode,
      offer
    ].filter(Boolean);
    const card = document.createElement("section");
    card.className = "luma-ready-card";
    card.innerHTML = `
    <div class="luma-ready-head">
      <strong>${escapeHtml(langText({ en: "This is what I understand about your business", es: "Esto entendimos de tu negocio", fr: "Voici ce que j'ai compris de votre entreprise", pt: "Isto \xE9 o que entendi sobre seu neg\xF3cio" }))}</strong>
    </div>
    <p>${escapeHtml(summary.join(" \xB7 "))}</p>
    <div class="luma-ready-actions">
      <button type="button" data-chat-generate>${escapeHtml(langText({ en: "Generate my website", es: "Generar mi web", fr: "G\xE9n\xE9rer mon site", pt: "Gerar meu site" }))}</button>
      <button type="button" data-chat-review>${escapeHtml(langText({ en: "Correct something", es: "Corregir algo", fr: "Corriger quelque chose", pt: "Corrigir algo" }))}</button>
    </div>
  `;
    card.querySelector("[data-chat-generate]")?.addEventListener("click", (event) => handleGuidedGenerateButton(event));
    card.querySelector("[data-chat-review]")?.addEventListener("click", openReviewDetails);
    return card;
  }
  function renderSelectedDomainState() {
    if (!domainCheckStatus || !builderState.guidedState.desiredDomain || domainResults?.children.length) return;
    domainCheckStatus.textContent = langText({
      en: "You can check availability before generating.",
      es: "Puedes verificar disponibilidad antes de generar.",
      fr: "Vous pouvez v\xE9rifier la disponibilit\xE9 avant de g\xE9n\xE9rer.",
      pt: "Voc\xEA pode verificar a disponibilidade antes de gerar."
    });
  }
  async function checkDesiredDomainOptions() {
    syncGuidedStateFromSummary();
    const query = builderState.guidedState.desiredDomain || builderState.guidedState.businessName || "";
    if (!query.trim()) {
      domainCheckStatus.textContent = t("domainCheckEmpty");
      domainResults.innerHTML = "";
      return;
    }
    checkDomainButton.disabled = true;
    domainCheckStatus.textContent = t("checkingDomain");
    domainResults.innerHTML = "";
    try {
      const response = await fetch(`${API_BASE_URL}/public/domain-search?q=${encodeURIComponent(query)}`);
      if (!response.ok) throw new Error(await readErrorMessage(response));
      renderDomainResults(await response.json());
    } catch (error) {
      domainCheckStatus.textContent = `${langText({
        en: "Could not check domain",
        es: "No se pudo verificar",
        fr: "Impossible de v\xE9rifier le domaine",
        pt: "N\xE3o foi poss\xEDvel verificar o dom\xEDnio"
      })}: ${shortError(error.message)}`;
    } finally {
      checkDomainButton.disabled = false;
    }
  }
  function renderDomainResults(result) {
    const rows = result.results || [];
    domainCheckStatus.textContent = result.exact_availability || result.exactAvailability ? t("domainExact") : t("domainSuggestions");
    if (!rows.length) {
      domainResults.innerHTML = `<p class="mini-note">${langText({ en: "No options found.", es: "No encontramos opciones.", fr: "Aucune option trouv\xE9e.", pt: "Nenhuma op\xE7\xE3o encontrada." })}</p>`;
      return;
    }
    domainResults.innerHTML = rows.slice(0, 6).map((item) => {
      const selectable = item.status !== "not_available";
      return `<button class="domain-choice ${selectable ? "selectable" : "blocked"}" data-domain-choice="${escapeAttribute(item.domain)}" type="button" ${selectable ? "" : "disabled"}>
      <strong>${escapeHtml(item.domain)}</strong>
      <span>${escapeHtml(domainStatusLabel(item))}</span>
    </button>`;
    }).join("");
    domainResults.querySelectorAll("[data-domain-choice]").forEach((button) => {
      button.addEventListener("click", () => {
        builderState.guidedState.desiredDomain = button.dataset.domainChoice;
        renderGuidedSummary();
        domainCheckStatus.textContent = langText({
          en: `Selected domain: ${button.dataset.domainChoice}`,
          es: `Dominio seleccionado: ${button.dataset.domainChoice}`,
          fr: `Domaine s\xE9lectionn\xE9 : ${button.dataset.domainChoice}`,
          pt: `Dom\xEDnio selecionado: ${button.dataset.domainChoice}`
        });
      });
    });
  }
  function domainStatusLabel(item) {
    const status = item.status || "";
    const labels = {
      available_included: { en: "Available in package", es: "Disponible en el paquete", fr: "Disponible dans le forfait", pt: "Dispon\xEDvel no pacote" },
      available_requires_review: { en: "Available, needs review", es: "Disponible, requiere revision", fr: "Disponible, v\xE9rification requise", pt: "Dispon\xEDvel, precisa de revis\xE3o" },
      not_available: { en: "Not available", es: "No disponible", fr: "Non disponible", pt: "N\xE3o dispon\xEDvel" },
      needs_registrar_check: { en: "Suggestion, confirm before payment", es: "Sugerencia, confirmar antes de pagar", fr: "Suggestion, confirmer avant paiement", pt: "Sugest\xE3o, confirmar antes do pagamento" }
    };
    return labels[status] ? langText(labels[status]) : status || langText({ en: "Suggested option", es: "Opcion sugerida", fr: "Option sugg\xE9r\xE9e", pt: "Op\xE7\xE3o sugerida" });
  }
  function compactCollectedPreview() {
    const templateMeta = templatePreviewMeta(builderState.forcedTemplateSelection?.templateId || builderState.guidedState.sitePlan?.templateId || "");
    const humanTemplate = localizedTemplateName(templateMeta);
    const parts = [
      builderState.guidedState.businessName,
      humanTemplate,
      builderState.guidedState.industry,
      builderState.guidedState.location,
      arrayValue2(builderState.guidedState.servicesProducts).slice(0, 2).join(", ")
    ].filter(Boolean);
    return parts.length ? parts.join(" \xB7 ") : langText({
      en: "LYRA is collecting the essentials.",
      es: "LYRA est\xE1 reuniendo lo esencial.",
      fr: "LYRA collecte l'essentiel.",
      pt: "A LYRA est\xE1 reunindo o essencial."
    });
  }
  async function createDomainOrderIfNeeded(payload, result) {
    const requestedDomain = payload.desiredDomain || payload.desired_domain || "";
    if (!requestedDomain.trim() || !result.business_id) return;
    try {
      const search = await fetch(`${API_BASE_URL}/public/domain-search?q=${encodeURIComponent(requestedDomain)}`);
      const searchResult = search.ok ? await search.json() : { results: [] };
      const selectedResult = (searchResult.results || []).find((item) => item.domain === requestedDomain.trim().toLowerCase()) || (searchResult.results || [])[0] || {};
      const orderPayload = {
        businessId: result.business_id,
        siteId: result.site_id,
        clientRequestId: builderState.currentRequestId,
        requestedDomain,
        ownerEmail: payload.contact_info?.email || payload.contact_info?.contact || "",
        ownerName: payload.contact_info?.name || payload.business_name || "",
        packageCode: "starter",
        selectedResult
      };
      const response = await fetch(`${API_BASE_URL}/domain-orders`, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify(orderPayload)
      });
      if (!response.ok) throw new Error(await readErrorMessage(response));
      const saved = await response.json();
      if (saved.storage_status === "stored") {
        guidedStatusText.textContent = langText({
          en: "Website generated. Domain saved for review/payment before registration.",
          es: "Pagina generada. Dominio guardado para revisar/pagar antes de registrarlo.",
          fr: "Site g\xE9n\xE9r\xE9. Domaine enregistr\xE9 pour v\xE9rification/paiement avant l'enregistrement.",
          pt: "Site gerado. Dom\xEDnio salvo para revis\xE3o/pagamento antes do registro."
        });
      }
    } catch (error) {
      console.warn("Domain order could not be saved", error);
      guidedStatusText.textContent = langText({
        en: "Website generated. Could not save the domain order; you can do it from admin.",
        es: "Pagina generada. No se pudo guardar la orden de dominio; puedes hacerlo desde admin.",
        fr: "Site g\xE9n\xE9r\xE9. Impossible d'enregistrer la commande de domaine; vous pouvez le faire depuis l'admin.",
        pt: "Site gerado. N\xE3o foi poss\xEDvel salvar o pedido de dom\xEDnio; voc\xEA pode fazer isso no admin."
      });
    }
  }
  function ensurePurposefulSections(schema, strategy, options = {}) {
    const layoutStrategy = strategy.layoutStrategy || {};
    const business = schema.business || {};
    const hasCatalog = arrayValue2(schema.catalog_items || schema.products_services).length > 0;
    return {
      ...schema,
      pages: arrayValue2(schema.pages).map((page) => {
        const seen = /* @__PURE__ */ new Set();
        let sections = arrayValue2(page.sections).filter((section) => hasSectionPurpose(section, hasCatalog, page.page_key === "home")).filter((section) => {
          const key = `${section.type}:${normalizeGenericText(section.editable?.title || section.editable?.headline || "")}`;
          if (seen.has(key)) return false;
          seen.add(key);
          return true;
        });
        if (!sections.some((section) => section.type === "Hero") && page.page_key === "home") {
          sections.unshift({
            type: "Hero",
            editable: {
              headline: professionalHeadline(schema, strategy.businessContext),
              subtitle: business.description || strategy.businessContext?.offer || "",
              primary_button: primaryCtaForStrategy(strategy)
            },
            settings: {}
          });
        }
        if (page.page_key === "home" && !options.reviewOnly) {
          const maxSections = layoutStrategy.spacingScale?.maxHomeSections || 6;
          sections = keepEssentialSections(sections, maxSections);
        }
        return { ...page, sections };
      })
    };
  }
  function hasSectionPurpose(section, hasCatalog, isHomePage) {
    if (!section) return false;
    if (section.type === "Hero" || section.type === "Contact" || section.type === "Footer") return true;
    if ((section.type === "ProductGrid" || section.type === "ServiceList") && hasCatalog) return true;
    if (section.type === "Gallery") return arrayValue2(section.editable?.images).length > 0;
    const editable = section.editable || {};
    const text = [editable.title, editable.headline, editable.text, editable.subtitle].filter(Boolean).join(" ");
    if (!text.trim()) return false;
    if (!isHomePage) return true;
    return !/lorem ipsum|placeholder|random section|insert text/i.test(text);
  }
  function normalizeSectionSettings(section, index, scale, direction) {
    const settings = { ...section.settings || {} };
    if (settings.background && /gradient|random|rainbow/i.test(settings.background)) delete settings.background;
    settings.spacing = section.type === "Hero" ? "spacious" : settings.spacing || scale.sectionPadding || "balanced";
    settings.heading_size = section.type === "Hero" ? "large" : settings.heading_size || (direction === "marketplace" ? "small" : "medium");
    settings.container_width = settings.container_width || (section.type === "Hero" ? scale.containerWidth || "standard" : scale.containerWidth || "standard");
    settings.card_gap = settings.card_gap || scale.cardGap || "comfortable";
    settings.card_density = settings.card_density || scale.cardDensity || "comfortable";
    settings.mobile_stack = true;
    if (section.type === "Hero") settings.layout = settings.layout || scale.heroLayout || "split_showcase";
    if (section.type === "ProductGrid" || section.type === "ServiceList") {
      settings.columns = Number(settings.columns || (direction === "marketplace" ? 4 : direction === "luxury" || direction === "ecommerce_premium" ? 3 : 3));
    }
    return settings;
  }
  function improveSectionCopy(section, schema, strategy) {
    const editable = { ...section.editable || {} };
    if (section.type === "Hero") {
      if (isWeakHeadline(editable.headline, schema.business?.name)) editable.headline = professionalHeadline(schema, strategy.businessContext);
      if (!editable.subtitle || isGenericText(editable.subtitle)) editable.subtitle = professionalSubtitle(schema, strategy.businessContext);
      if (!editable.primary_button || isWeakCta(editable.primary_button)) editable.primary_button = primaryCtaForStrategy(strategy);
      if (isWeakCta(editable.secondary_button)) editable.secondary_button = "View offerings";
    }
    if ((section.type === "ProductGrid" || section.type === "ServiceList") && isGenericText(editable.title || editable.headline)) {
      editable.title = strategy.designDirection === "restaurant" ? "Menu highlights" : "What you can get here";
    }
    if (section.type === "Contact") {
      editable.title = editable.title && !isGenericText(editable.title) ? editable.title : "Ready for the next step?";
      editable.text = editable.text && !isGenericText(editable.text) ? editable.text : "Send a message and get a clear answer about availability, pricing, or fit.";
    }
    return editable;
  }
  function reviewWebsiteConfigBeforeRender(schema, strategy) {
    const improvements = [];
    const reviewed = {
      ...schema,
      global_components: {
        ...schema.global_components || {},
        footer_text: schema.global_components?.footer_text || `${schema.business?.name || "Brand"} - ${strategy.businessContext?.mainConversionGoal || "clear next steps"}`
      }
    };
    reviewed.pages = arrayValue2(reviewed.pages).map((page) => ({
      ...page,
      sections: arrayValue2(page.sections).map((section) => {
        if (section.type !== "Hero") return section;
        const editable = { ...section.editable || {} };
        if (isWeakHeadline(editable.headline, reviewed.business?.name)) {
          editable.headline = professionalHeadline(reviewed, strategy.businessContext);
          improvements.push("Strengthened hero headline");
        }
        if (!editable.primary_button || isWeakCta(editable.primary_button)) {
          editable.primary_button = primaryCtaForStrategy(strategy);
          improvements.push("Clarified primary CTA");
        }
        return { ...section, editable };
      })
    }));
    reviewed.design_review = {
      reviewedAt: (/* @__PURE__ */ new Date()).toISOString(),
      focus: ["headline", "CTA", "section order", "contrast", "visual rhythm", "mobile layout", "brand consistency"],
      improvements
    };
    return reviewed;
  }
  function addSectionPurpose(section, index, strategy) {
    const typePurpose = {
      Hero: "State the offer, establish brand credibility, and drive the primary CTA.",
      ProductGrid: "Help visitors compare offers and choose a next action.",
      ServiceList: "Clarify services and reduce decision friction.",
      FeatureBand: "Explain differentiators that support the conversion goal.",
      About: "Build trust with context and brand story.",
      Testimonials: "Provide proof and reduce risk.",
      Gallery: "Show real visual evidence.",
      Contact: "Convert interest into a request.",
      Footer: "Close with brand recall and contact routes."
    };
    return {
      ...section,
      reason: section.reason || typePurpose[section.type] || "Support the page narrative with useful information.",
      conversion_purpose: section.conversion_purpose || (index === 0 ? strategy.conversionGoal : typePurpose[section.type] || strategy.businessGoal),
      priority: section.priority || (index === 0 ? "high" : index < 3 ? "medium" : "supporting"),
      visual_treatment: section.visual_treatment || `${strategy.visualDirection}; spacing ${section.settings?.spacing || "balanced"}`
    };
  }
  function requireStudioAccount(event, action, callback) {
    if (event?.preventDefault) event.preventDefault();
    if (!builderState.currentSchema) {
      callback?.();
      return;
    }
    if (hasStudioAccountSession()) {
      callback?.();
      return;
    }
    openStudioAuthGate(action);
  }
  function studioInspector() {
    const selectedSection = selectedStudioSection();
    const title = selectedSection ? `${selectedSection.type} inspector` : "Page inspector";
    return `<div class="editor-group studio-inspector">
    <div class="studio-inspector-head">
      <div>
        <h3>${escapeHtml(title)}</h3>
        <p>${escapeHtml(selectedSection ? "Edit the selected section from the canvas." : "Select a section on the canvas to edit it directly.")}</p>
      </div>
      <button id="toggleAdvancedInspector" class="small-button" type="button">${builderState.advancedInspectorOpen ? "Hide advanced" : "Advanced controls"}</button>
    </div>
    ${selectedSection ? sectionQuickInspector(selectedSection) : pageQuickInspector()}
  </div>`;
  }
  function sectionQuickInspector(section) {
    const page = selectedPage();
    const index = page.sections.findIndex((item) => (item.id || item.type) === (section.id || section.type));
    const basePath = `pages.${pageIndex()}.sections.${index}`;
    const editable = section.editable || {};
    if (section.type === "Hero") {
      return `<div class="studio-inspector-grid">
      ${textareaField("Headline", `${basePath}.editable.headline`, editable.headline || "", "data-section-path")}
      ${textareaField("Subtitle", `${basePath}.editable.subtitle`, editable.subtitle || "", "data-section-path")}
      ${inputField("Primary CTA", `${basePath}.editable.primary_button`, editable.primary_button || "", "data-section-path")}
      ${inputField("Image URL", `${basePath}.editable.image_url`, editable.image_url || "", "data-section-path")}
      ${selectField("Layout", `${basePath}.settings.layout`, section.settings?.layout || "split_showcase", ["split_showcase", "image_left", "centered_hero", "minimal_statement"], "data-section-path")}
    </div>`;
    }
    if (section.type === "ProductGrid" || section.type === "ServiceList") {
      return `<div class="studio-inspector-grid">
      ${inputField("Title", `${basePath}.editable.title`, editable.title || editable.headline || "", "data-section-path")}
      ${textareaField("Intro text", `${basePath}.editable.text`, editable.text || editable.subtitle || "", "data-section-path")}
      ${selectField("Columns", `${basePath}.settings.columns`, section.settings?.columns || 3, ["2", "3", "4"], "data-section-path")}
      ${selectField("Card density", `${basePath}.settings.card_density`, section.settings?.card_density || "comfortable", ["compact", "comfortable", "airy"], "data-section-path")}
      <button class="small-button" type="button" data-studio-add-product>Add product</button>
    </div>`;
    }
    if (section.type === "Contact") {
      return `<div class="studio-inspector-grid">
      ${inputField("Title", `${basePath}.editable.title`, editable.title || "", "data-section-path")}
      ${textareaField("Text", `${basePath}.editable.text`, editable.text || "", "data-section-path")}
      ${inputField("Email", "contact.email", builderState.currentSchema.contact?.email || "")}
      ${inputField("Phone", "contact.phone", builderState.currentSchema.contact?.phone || "")}
      ${inputField("WhatsApp", "contact.whatsapp", builderState.currentSchema.contact?.whatsapp || "")}
    </div>`;
    }
    return `<div class="studio-inspector-grid">
    ${inputField("Title", `${basePath}.editable.title`, editable.title || editable.headline || "", "data-section-path")}
    ${textareaField("Text", `${basePath}.editable.text`, editable.text || editable.subtitle || "", "data-section-path")}
    ${selectField("Spacing", `${basePath}.settings.spacing`, section.settings?.spacing || "balanced", ["compact", "balanced", "spacious"], "data-section-path")}
  </div>`;
  }
  function selectedStudioSection() {
    const page = selectedPage();
    return arrayValue2(page?.sections).find((section) => (section.id || section.type) === builderState.selectedStudioSectionId) || null;
  }
  function handleStudioSelectionAction(action) {
    const page = selectedPage();
    const index = arrayValue2(page.sections).findIndex((section) => (section.id || section.type) === builderState.selectedStudioSectionId);
    if (index < 0) return;
    if (action === "edit") {
      renderEditor();
      selectStudioSection(builderState.selectedStudioSectionId);
      return;
    }
    if (action === "up" || action === "down") {
      const next = action === "up" ? index - 1 : index + 1;
      if (next >= 0 && next < page.sections.length) {
        [page.sections[index], page.sections[next]] = [page.sections[next], page.sections[index]];
        resequence(page.sections);
      }
    } else if (action === "duplicate") {
      const copy = structuredClone(page.sections[index]);
      copy.id = `${slugify2(copy.type)}_${Date.now()}`;
      copy.order = index + 2;
      page.sections.splice(index + 1, 0, copy);
      builderState.selectedStudioSectionId = copy.id;
      resequence(page.sections);
    } else if (action === "delete") {
      page.sections.splice(index, 1);
      builderState.selectedStudioSectionId = "";
      studioSelectionToolbar.hidden = true;
      resequence(page.sections);
    } else if (action === "luma") {
      adjustGeneratedDraftWithLuma();
      return;
    }
    renderEditor();
    renderPreview();
    if (builderState.selectedStudioSectionId) window.setTimeout(() => selectStudioSection(builderState.selectedStudioSectionId), 50);
  }
  function renderStudioProgress() {
    if (!studioProgressList) {
      renderStudioLyraInsights();
      return;
    }
    if (!builderState.currentSchema) {
      studioProgressList.innerHTML = studioProgressItems([
        ["pending", "Home page"],
        ["pending", "Brand system"],
        ["pending", "Online shop"],
        ["pending", "Contact page"]
      ]);
      renderStudioLyraInsights();
      return;
    }
    const pages = arrayValue2(builderState.currentSchema.pages);
    const sections = pages.flatMap((page) => arrayValue2(page.sections));
    const catalogItems = arrayValue2(builderState.currentSchema.catalog_items || builderState.currentSchema.products_services);
    const items = [
      [pages.some((page) => page.page_key === "home") || sections.some((section) => section.type === "Hero") ? "done" : "active", "Home page"],
      [builderState.currentSchema.brand?.primaryColor || builderState.currentSchema.theme?.colors?.primary ? "done" : "active", "Brand system"],
      [catalogItems.length || sections.some((section) => /ProductGrid|ServiceList/.test(section.type)) ? "done" : "active", "Online shop"],
      [sections.some((section) => section.type === "Contact") || builderState.currentSchema.contact?.email || builderState.currentSchema.contact?.phone ? "done" : "pending", "Contact page"]
    ];
    studioProgressList.innerHTML = studioProgressItems(items);
    renderStudioLyraInsights();
  }
  function setStudioProgressPhase(phase) {
    if (!studioProgressList) {
      renderStudioLyraInsights();
      return;
    }
    const steps = [
      ["understanding", "Understanding business"],
      ["brand", "Building brand kit"],
      ["homepage", "Creating homepage"],
      ["shop", "Creating shop"],
      ["mobile", "Optimizing mobile"],
      ["ready", "Ready"]
    ];
    const activeIndex = Math.max(0, steps.findIndex(([key]) => key === phase));
    studioProgressList.innerHTML = studioProgressItems(steps.map(([key, label], index) => [
      phase === "ready" || index < activeIndex ? "done" : index === activeIndex ? "active" : "pending",
      label
    ]));
    renderStudioLyraInsights();
  }
  function studioProgressItems(items) {
    const icon = { done: "<span></span>", active: "<i></i>", pending: "<em></em>" };
    return items.map(([state, label]) => `<div data-progress-state="${escapeAttribute(state)}">${icon[state] || icon.pending}<strong>${escapeHtml(label)}</strong></div>`).join("");
  }
  function renderStudioLyraInsights() {
    if (studioSuggestedList) studioSuggestedList.innerHTML = studioInsightItems(studioSuggestedImprovements());
    if (studioRecentList) studioRecentList.innerHTML = studioInsightItems(studioRecentChanges());
  }
  function studioInsightItems(items) {
    const cleaned = arrayValue2(items).map((item) => String(item || "").trim()).filter(Boolean).slice(0, 4);
    const fallback = ["Sin cambios recientes."];
    return (cleaned.length ? cleaned : fallback).map((item) => `<li>${escapeHtml(item)}</li>`).join("");
  }
  function studioSuggestedImprovements() {
    if (!builderState.currentSchema) {
      return [
        "Elegir una plantilla base.",
        "Completar marca y productos.",
        "Generar el primer borrador."
      ];
    }
    const pages = arrayValue2(builderState.currentSchema.pages);
    const sections = pages.flatMap((page) => arrayValue2(page.sections));
    const catalogItems = arrayValue2(builderState.currentSchema.catalog_items || builderState.currentSchema.products_services);
    const suggestions = [];
    if (!builderState.currentSchema.brand?.logoUrl && !builderState.currentSchema.brand?.logo_url) suggestions.push("Subir logo o confirmar identidad visual.");
    if (!catalogItems.length && !sections.some((section) => /ProductGrid|ServiceList/.test(section.type))) suggestions.push("Agregar productos o servicios editables.");
    if (!sections.some((section) => section.type === "Contact") && !builderState.currentSchema.contact?.email && !builderState.currentSchema.contact?.phone) suggestions.push("Completar contacto visible para clientes.");
    if (!sections.some((section) => /Testimonials|Gallery|Proof|Trust/i.test(section.type))) suggestions.push("Agregar prueba visual o testimonios.");
    if (builderState.selectedStudioSectionId) suggestions.unshift("Pulir la secci\xF3n seleccionada con Lyra.");
    if (!suggestions.length) {
      suggestions.push("Revisar versi\xF3n m\xF3vil antes de publicar.");
      suggestions.push("Guardar cambios y enviar a revisi\xF3n.");
      suggestions.push("Probar CTA principal desde el preview.");
    }
    return suggestions;
  }
  function studioRecentChanges() {
    if (!builderState.currentSchema) return ["Sin cambios recientes."];
    const changes = [];
    arrayValue2(builderState.currentSchema.revision_history).slice(-3).reverse().forEach((item) => {
      const request = cleanShortText(item.request || item.message || "", 88);
      if (request) changes.push(`Ajuste aplicado: ${request}`);
    });
    arrayValue2(builderState.currentSchema.design_review?.improvements).slice(-2).forEach((item) => {
      const mapped = {
        "Strengthened hero headline": "Titular principal reforzado.",
        "Clarified primary CTA": "CTA principal aclarado."
      }[item] || item;
      if (mapped) changes.push(mapped);
    });
    const templateName = builderState.currentSchema.selected_template?.name || builderState.currentSchema.active_template?.name || localizedTemplateName(templatePreviewMeta(builderState.currentSchema.layout_mode?.template_id || ""));
    if (templateName) changes.push(`Base activa: ${templateName}.`);
    if (!changes.length) changes.push("Borrador generado y listo para revisar.");
    return changes;
  }
  function addStudioSection(type = "FeatureBand") {
    if (!builderState.currentSchema) return;
    const page = builderState.currentSchema.pages.find((item) => item.page_key === builderState.selectedPageKey) || builderState.currentSchema.pages[0];
    if (!page) return;
    page.sections = arrayValue2(page.sections);
    page.sections.push(createSectionByType(type, page.sections.length + 1));
    builderState.currentSchema = prepareWebsiteConfig(builderState.currentSchema, { brand: builderState.currentSchema.brand || builderState.guidedState.brand || {} }, null);
    builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
    renderEditor();
    renderPreview();
  }
  function createSectionByType(type, order) {
    const base = {
      id: `${slugify2(type)}_${Date.now()}`,
      type,
      order,
      editable: {},
      settings: { spacing: "balanced", container_width: "standard", card_density: "comfortable" }
    };
    if (type === "ProductGrid") {
      base.editable = { title: "Productos destacados", text: "Una selecci\xF3n editable del cat\xE1logo.", images: [] };
      base.settings = { ...base.settings, layout: "featured", columns: 3 };
    } else if (type === "Hero") {
      base.editable = {
        headline: builderState.currentSchema?.business?.name || "Nueva propuesta principal",
        subtitle: builderState.currentSchema?.business?.description || "Explica en una frase clara por qu\xE9 vale la pena seguir.",
        primary_button: "Ver productos",
        secondary_button: "Contactar",
        image_url: "",
        images: []
      };
      base.settings = { ...base.settings, layout: "split_showcase", spacing: "spacious", heading_size: "large" };
    } else if (type === "Gallery") {
      base.editable = { title: "Galer\xEDa", images: [] };
    } else if (type === "Testimonials") {
      base.editable = { title: "Clientes que conf\xEDan", text: "Agrega pruebas, comentarios o resultados reales." };
    } else if (type === "Contact") {
      base.editable = { title: "Hablemos", text: "Escr\xEDbenos para m\xE1s informaci\xF3n." };
    } else {
      base.editable = { title: "Nueva secci\xF3n", text: "Edita este bloque para explicar una raz\xF3n clara para comprar o contactar." };
    }
    return base;
  }
  function selectStudioSection(sectionId) {
    builderState.selectedStudioSectionId = sectionId || "";
    previewFrame2.querySelectorAll("[data-studio-section].is-selected").forEach((element2) => element2.classList.remove("is-selected"));
    const element = previewFrame2.querySelector(`[data-studio-section="${cssEscape(sectionId)}"]`);
    element?.classList.add("is-selected");
    if (studioSelectionToolbar) studioSelectionToolbar.hidden = !element;
    element?.scrollIntoView({ block: "center", behavior: "smooth" });
    const editorSection = editorMount.querySelector(`[data-editor-section="${cssEscape(sectionId)}"]`);
    editorSection?.scrollIntoView({ block: "start", behavior: "smooth" });
    editorSection?.classList.add("editor-focus-flash");
    window.setTimeout(() => editorSection?.classList.remove("editor-focus-flash"), 900);
    const type = element?.dataset.studioSectionType || "section";
    if (studioLumaMessage) {
      studioLumaMessage.textContent = langText({
        en: `I selected the ${type}. Tell me what to improve, or adjust its controls on the left.`,
        es: `Seleccion\xE9 ${type}. Dime qu\xE9 quieres mejorar o ajusta sus controles a la izquierda.`,
        fr: `J'ai s\xE9lectionn\xE9 ${type}. Dites-moi quoi am\xE9liorer ou ajustez ses contr\xF4les \xE0 gauche.`,
        pt: `Selecionei ${type}. Diga o que quer melhorar ou ajuste os controles \xE0 esquerda.`
      });
    }
    setAssistantState("listening");
    renderEditor();
  }
  function brandKitEditor(brandInput, score) {
    const brand = normalizeBrand(brandInput);
    const swatches = brandPalette(brand).map((color) => `<span class="brand-swatch" style="--swatch:${escapeAttribute(color)}" title="${escapeAttribute(color)}"></span>`).join("");
    const total = score?.total ? `${score.total}/100` : "Not scored yet";
    return `<div class="editor-group brand-kit-editor">
    <h3>Brand Kit</h3>
    <div class="brand-kit-head">
      ${brand.logoUrl ? `<img src="${escapeAttribute(brand.logoUrl)}" alt="">` : ""}
      <div>
        <strong>${escapeHtml(brand.styleDirection)}</strong>
        <span>Design score: ${escapeHtml(total)}</span>
      </div>
    </div>
    <div class="brand-kit-swatches">${swatches}</div>
    <div class="brand-kit-meta">
      <span>${escapeHtml(fontPairingLabel(brand.fontPairing))}</span>
      <span>${escapeHtml(brand.borderRadius)}</span>
      <button class="brand-kit-button" type="button">Button preview</button>
    </div>
  </div>`;
  }
  function renderWebsite2(schema, pageKey) {
    return renderWebsite(schema, pageKey, {
      selectedLanguage: builderState.selectedLanguage,
      isClientPreviewMode: Boolean(document.body?.classList?.contains("client-preview-mode"))
    });
  }
  function sectionEditor(section, index) {
    const editable = section.editable || {};
    const basePath = `pages.${pageIndex()}.sections.${index}`;
    const settings = section.settings || {};
    return `<div class="editor-group" data-editor-section="${escapeAttribute(section.id || section.type)}">
    <h4>${index + 1}. ${section.type}</h4>
    <div class="control-grid">
      ${selectField("Layout", `${basePath}.settings.layout`, settings.layout || "", [
      "image_right",
      "image_left",
      "centered_hero",
      "compact_catalog",
      "grid",
      "masonry",
      "boutique_grid",
      "simple",
      "trust_first"
    ])}
      ${selectField("Heading size", `${basePath}.settings.heading_size`, settings.heading_size || "medium", ["small", "medium", "large", "huge"])}
      ${selectField("Spacing", `${basePath}.settings.spacing`, settings.spacing || "balanced", ["compact", "balanced", "spacious"])}
      ${inputField("Columns", `${basePath}.settings.columns`, settings.columns || 3, "data-section-path")}
      ${inputField("Background", `${basePath}.settings.background`, settings.background || "", "data-section-path")}
      ${inputField("Text color", `${basePath}.settings.text_color`, settings.text_color || "", "data-section-path")}
    </div>
    ${Object.entries(editable).map(
      ([key, value]) => typeof value === "string" ? textareaField(key, `${basePath}.editable.${key}`, value, "data-section-path") : ""
    ).join("")}
    <div class="row-actions">
      <button class="small-button" data-remove-section="${escapeAttribute(section.id)}" type="button">Remove section</button>
    </div>
  </div>`;
  }
  async function uploadAssetOrFallback(file, assetType, label) {
    try {
      const uploaded = await uploadAssetFile(file, assetType, label);
      if (uploaded?.url) return uploaded.url;
    } catch (error) {
      console.warn("Asset upload failed, using development fallback.", error);
      if (storageStatus2) {
        storageStatus2.textContent = "Asset storage unavailable. Using development image fallback.";
      }
    }
    if (assetType === "video") return "";
    return fileToOptimizedDataUrl(file, assetType);
  }
  async function uploadAssetFile(file, assetType, label) {
    if (assetType === "video" && file.size > 12 * 1024 * 1024) {
      throw new Error("Video file is too large for direct upload.");
    }
    const dataUrl = await fileToOptimizedDataUrl(file, assetType);
    const response = await fetch(ASSET_UPLOAD_URL, {
      method: "POST",
      headers: adminHeaders({ "Content-Type": "application/json" }),
      body: JSON.stringify({
        businessId: builderState.currentBusinessId,
        siteId: builderState.currentSiteId,
        assetType,
        fileName: file.name || `${assetType}.png`,
        contentType: file.type || "image/png",
        dataUrl,
        label
      })
    });
    if (!response.ok) {
      const details = await response.text();
      throw new Error(details || `Asset upload failed with ${response.status}`);
    }
    const result = await response.json();
    if (storageStatus2) {
      storageStatus2.textContent = "Asset uploaded to cloud storage.";
    }
    return result;
  }
  function setPreviewButton(activeId) {
    ["desktopButton", "mobileButton"].forEach((id) => {
      document.querySelector(`#${id}`).classList.toggle("active", id === activeId);
    });
  }
  function normalizeEditedValue(path, value) {
    if (!path.startsWith("theme.colors.") && !path.startsWith("brand.") && !path.endsWith(".settings.background") && !path.endsWith(".settings.text_color")) {
      return value;
    }
    return resolveColor(value, value);
  }

  // src/ai-builder/index.js
  function adminHeaders(extra = {}) {
    const token = localStorage.getItem("lumaAdminToken") || "";
    return {
      ...extra,
      ...token ? { "x-admin-token": token } : {}
    };
  }
  var GUIDED_QUESTIONS = {
    en: {
      websiteIntent: "Tell me what you want to build in one rich paragraph: what the business sells or does, who buys, the style/colors, location, and whether it should sell online, book appointments, collect leads, or present the company. I will choose the best structure and ask only for what is missing.",
      businessName: "First, what is the name of your business?",
      businessDescription: "Tell me what it sells or does in one message. I will use it as design strategy, not as literal page copy.",
      industry: "What industry or category best fits it?",
      location: "Where is it located or where does it serve customers?",
      servicesProducts: "I only need one clarification: what are the main product categories or services?",
      targetAudience: "Who is the main customer? You can also let AI decide.",
      preferredTone: "What style should it feel like: elegant, modern, friendly, premium? Or should AI decide?",
      preferredColors: "Any preferred colors, or do you want to upload a logo so I can extract a palette from it?",
      contactInfo: "What contact details should appear on the site, and what email should we use to save your draft/account? You can also continue with Google or Apple before generating.",
      salesMode: "Should the site support online sales, quote requests, in-person visits, or a mix?",
      hasLogoPhotos: "Do you have a logo to upload? If not, we can continue with your business name in text for now.",
      desiredDomain: "What domain would you like? You can write a name like lunastore.com or skip it for now.",
      review: "I have enough to create the first draft."
    },
    es: {
      websiteIntent: "Descr\xEDbeme en un p\xE1rrafo qu\xE9 quieres construir: qu\xE9 vende o hace el negocio, para qui\xE9n, estilo/colores, ubicaci\xF3n y si debe vender online, reservar citas, captar clientes o s\xF3lo presentar la empresa. Yo elegir\xE9 la mejor estructura y s\xF3lo preguntar\xE9 lo que falte.",
      businessName: "Primero, \xBFc\xF3mo se llama tu negocio?",
      businessDescription: "Dime qu\xE9 vende o qu\xE9 hace en un solo mensaje. Lo usar\xE9 como estrategia de dise\xF1o, no como texto literal para la p\xE1gina.",
      industry: "En que industria o categoria lo pondrias?",
      location: "Donde atiende o donde esta ubicado?",
      servicesProducts: "S\xF3lo necesito aclarar algo: \xBFcu\xE1les son las categor\xEDas principales de productos o servicios?",
      targetAudience: "A quien le vendes principalmente? Tambien puedes dejar que IA decida.",
      preferredTone: "Que estilo quieres: elegante, moderno, cercano, premium? O dejo que IA decida?",
      preferredColors: "\xBFTienes colores preferidos o quieres subir un logo para sacar una paleta desde ah\xED?",
      contactInfo: "Que contacto quieres mostrar en la pagina y que email usamos para guardar tu borrador/cuenta? Tambien puedes continuar con Google o Apple antes de generar.",
      salesMode: "\xBFQuieres ventas online, solicitudes de cotizaci\xF3n, visitas presenciales o una mezcla?",
      hasLogoPhotos: "\xBFTienes un logo para subir? Si no, seguimos con el nombre de tu negocio en texto por ahora.",
      desiredDomain: "Que dominio te gustaria? Puedes escribir algo como lunastore.com o saltarlo por ahora.",
      review: "Ya tengo suficiente para crear el primer borrador."
    },
    fr: {
      websiteIntent: "D\xE9crivez en un paragraphe ce que vous voulez cr\xE9er : ce que l'entreprise vend ou fait, pour qui, le style/couleurs, la localisation, et si le site doit vendre en ligne, r\xE9server, capter des leads ou pr\xE9senter l'entreprise. Je choisirai la meilleure structure et demanderai seulement ce qui manque.",
      businessName: "Quel est le nom de l'entreprise?",
      businessDescription: "Dites-moi ce qu'elle vend ou propose en un seul message. Je l'utiliserai comme strat\xE9gie de design, pas comme texte litt\xE9ral.",
      industry: "Dans quel secteur ou cat\xE9gorie la placeriez-vous?",
      location: "O\xF9 se trouve-t-elle ou o\xF9 sert-elle ses clients?",
      servicesProducts: "J'ai seulement besoin d'une pr\xE9cision : quelles sont les principales cat\xE9gories de produits ou services ?",
      targetAudience: "Qui est le client principal? Vous pouvez aussi laisser l'IA d\xE9cider.",
      preferredTone: "Quel style voulez-vous: \xE9l\xE9gant, moderne, chaleureux, premium? Ou l'IA d\xE9cide?",
      preferredColors: "Avez-vous des couleurs pr\xE9f\xE9r\xE9es, ou voulez-vous importer un logo pour que j'en extraie une palette?",
      contactInfo: "Quelles coordonn\xE9es afficher sur le site, et quel email utiliser pour sauvegarder le brouillon/compte? Vous pouvez aussi continuer avec Google ou Apple avant de g\xE9n\xE9rer.",
      salesMode: "Le site doit-il proposer la vente en ligne, les demandes de devis, les visites en personne, ou un m\xE9lange?",
      hasLogoPhotos: "Avez-vous un logo \xE0 importer ? Sinon, nous continuons avec le nom de votre entreprise en texte.",
      desiredDomain: "Quel domaine souhaitez-vous? Vous pouvez \xE9crire lunastore.com ou ignorer pour l'instant.",
      review: "J'ai assez d'informations pour cr\xE9er le premier brouillon."
    },
    pt: {
      websiteIntent: "Descreva em um par\xE1grafo o que voc\xEA quer criar: o que o neg\xF3cio vende ou faz, para quem, estilo/cores, localiza\xE7\xE3o, e se deve vender online, agendar, captar contatos ou apresentar a empresa. Eu escolho a melhor estrutura e pergunto apenas o que faltar.",
      businessName: "Qual \xE9 o nome do neg\xF3cio?",
      businessDescription: "Diga o que ele vende ou faz em uma mensagem. Vou usar isso como estrat\xE9gia de design, n\xE3o como texto literal da p\xE1gina.",
      industry: "Em qual setor ou categoria ele se encaixa?",
      location: "Onde fica ou onde atende clientes?",
      servicesProducts: "S\xF3 preciso esclarecer uma coisa: quais s\xE3o as principais categorias de produtos ou servi\xE7os?",
      targetAudience: "Quem \xE9 o cliente principal? Voc\xEA tamb\xE9m pode deixar a IA decidir.",
      preferredTone: "Qual estilo voc\xEA quer: elegante, moderno, pr\xF3ximo, premium? Ou a IA decide?",
      preferredColors: "Tem cores preferidas, ou quer enviar um logo para eu extrair uma paleta dele?",
      contactInfo: "Quais contatos devem aparecer no site, e qual email usamos para salvar seu rascunho/conta? Voc\xEA tamb\xE9m pode continuar com Google ou Apple antes de gerar.",
      salesMode: "O site deve aceitar vendas online, pedidos de or\xE7amento, visitas presenciais, ou uma mistura?",
      hasLogoPhotos: "Voc\xEA tem um logo para enviar? Caso n\xE3o, seguimos com o nome do neg\xF3cio em texto por enquanto.",
      desiredDomain: "Qual dom\xEDnio voc\xEA gostaria? Pode escrever lunastore.com ou pular por enquanto.",
      review: "J\xE1 tenho o suficiente para criar o primeiro rascunho."
    }
  };
  var GUIDED_STEPS = [
    "websiteIntent",
    "businessName",
    "businessDescription",
    "servicesProducts",
    "preferredColors",
    "contactInfo",
    "review"
  ];
  var REQUIRED_GUIDED_STEPS = [
    "websiteIntent",
    "businessName",
    "businessDescription"
  ];
  var SMART_GUIDED_STEP_PRIORITY = [
    "websiteIntent",
    "businessName",
    "businessDescription",
    "servicesProducts",
    "preferredColors",
    "contactInfo"
  ];
  var OPTIONAL_GUIDED_STEPS = /* @__PURE__ */ new Set([]);
  var guidedAskedSteps = /* @__PURE__ */ new Map();
  function bootLegacyBuilderPage() {
    document.body.classList.toggle("embedded-chat", isEmbeddedClientSetup);
    registerCriticalGuidedControls();
    safeBootStep("avatar", initBuilderAvatarAssistant);
    safeBootStep("language", initLanguageControls);
    safeBootStep("voice", initVoiceInput);
    safeBootStep("audio-toggle", updateAssistantAudioToggle);
    safeBootStep("assistant-state", () => setAssistantState("happy"));
    safeBootStep("client-auth-reset", captureClientAuthResetIntent);
    safeBootStep("auth-redirect", captureStudioAuthRedirect);
    safeBootStep("request-hydration", hydrateFromSelectedRequest);
    safeBootStep("guided-intake", initGuidedIntake);
    safeBootStep("guided-media-drop", initGuidedMediaDrop);
    safeBootStep("studio-lyra-panel", renderStudioLyraInsights);
    safeBootStep("client-session", initClientIntakeSessionGate);
    safeBootStep("client-account-control", renderClientAccountControl);
    safeBootStep("client-workspace-security", initClientWorkspaceSecurity);
    function safeBootStep(label, callback) {
      try {
        callback();
      } catch (error) {
        console.error(`LYRA boot step failed: ${label}`, error);
        if (guidedStatusText) {
          guidedStatusText.textContent = langText({
            en: "LYRA loaded with a recoverable issue. You can keep answering.",
            es: "LYRA carg\xF3 con un problema recuperable. Puedes seguir respondiendo.",
            fr: "LYRA a charg\xE9 avec un probl\xE8me r\xE9cup\xE9rable. Vous pouvez continuer.",
            pt: "A LYRA carregou com um problema recuper\xE1vel. Voc\xEA pode continuar."
          });
        }
      }
    }
    function registerCriticalGuidedControls() {
      document.addEventListener("click", (event) => {
        const target = event.target?.closest?.("#guidedSendButton, #guidedSkipButton, #switchManualFormButton, #backToChatButton, #guidedGenerateButton, [data-chat-generate]");
        if (!target) return;
        event.preventDefault();
        event.stopImmediatePropagation();
        if (target.id === "guidedSendButton") {
          handleGuidedSendAction(event);
        } else if (target.id === "guidedSkipButton") {
          skipGuidedQuestion();
        } else if (target.id === "switchManualFormButton") {
          switchToManualForm();
        } else if (target.id === "backToChatButton") {
          switchBackToChat();
        } else if (target.id === "guidedGenerateButton" || target.matches?.("[data-chat-generate]")) {
          handleGuidedGenerateButton(event);
        }
      }, true);
      document.addEventListener("keydown", (event) => {
        if (event.key !== "Enter" || event.target?.id !== "guidedReply") return;
        event.preventDefault();
        event.stopImmediatePropagation();
        handleGuidedSendAction(event);
      }, true);
    }
    form2.addEventListener("submit", async (event) => {
      event.preventDefault();
      await generateWebsite();
    });
    document.querySelector("#desktopButton").addEventListener("click", () => {
      previewFrame2.classList.remove("mobile");
      setPreviewButton("desktopButton");
    });
    document.querySelector("#mobileButton").addEventListener("click", () => {
      previewFrame2.classList.add("mobile");
      setPreviewButton("mobileButton");
    });
    document.querySelector("#copySchemaButton").addEventListener("click", async () => {
      if (!builderState.currentSchema) return;
      await navigator.clipboard.writeText(JSON.stringify(builderState.currentSchema, null, 2));
      storageStatus2.textContent = "JSON copied";
    });
    document.querySelector("#saveSchemaButton").addEventListener("click", (event) => requireStudioAccount(event, "save", saveCurrentSchema));
    document.querySelector("#publishButton").addEventListener("click", (event) => requireStudioAccount(event, "publish", publishCurrentSite));
    document.querySelector("#clientPreviewButton").addEventListener("click", () => {
      document.body.classList.add("client-preview-mode");
    });
    document.querySelector("#exitClientPreviewButton").addEventListener("click", () => {
      document.body.classList.remove("client-preview-mode", "draft-adjust-open");
    });
    submitDraftReviewButton?.addEventListener("click", (event) => requireStudioAccount(event, "review", submitGeneratedDraftForReview));
    adjustWithLumaButton?.addEventListener("click", adjustGeneratedDraftWithLuma);
    studioAdjustButton?.addEventListener("click", adjustGeneratedDraftWithLuma);
    startNewProjectButton?.addEventListener("click", startNewClientProject);
    startNewGeneratedProjectButton?.addEventListener("click", startNewClientProject);
    document.querySelectorAll("[data-studio-add-section]").forEach((button) => {
      button.addEventListener("click", () => addStudioSection(button.dataset.studioAddSection));
    });
    document.querySelectorAll("[data-studio-add-product]").forEach((button) => {
      button.addEventListener("click", addStudioCatalogItem);
    });
    studioSelectionToolbar?.querySelectorAll("[data-selection-action]").forEach((button) => {
      button.addEventListener("click", () => handleStudioSelectionAction(button.dataset.selectionAction));
    });
    studioAuthCloseButton?.addEventListener("click", closeStudioAuthGate);
    studioAuthDemoButton?.addEventListener("click", continueWithDemoSession);
    studioGoogleAuthButton?.addEventListener("click", () => continueWithStudioAuth("google"));
    studioAppleAuthButton?.addEventListener("click", () => continueWithStudioAuth("apple"));
    studioEmailAuthButton?.addEventListener("click", () => {
      if (studioEmailAuthForm) studioEmailAuthForm.hidden = !studioEmailAuthForm.hidden;
      studioAuthEmail?.focus();
    });
    studioEmailAuthForm?.addEventListener("submit", continueWithEmailAuth);
    quickModeButton2.addEventListener("click", () => setIntakeMode("quick"));
    guidedModeButton2.addEventListener("click", () => setIntakeMode("guided"));
    guidedCloseButton.addEventListener("click", () => {
      if (isPublicClientSetup) {
        if (document.body.classList.contains("draft-adjust-open")) {
          closeDraftAdjustmentChat();
          return;
        }
        if (builderState.currentSchema) {
          showGeneratedClientPreview();
          guidedStatusText.textContent = langText({
            en: "Your generated draft is still here.",
            es: "Tu borrador generado sigue aqu\xED.",
            fr: "Votre brouillon g\xE9n\xE9r\xE9 est toujours ici.",
            pt: "Seu rascunho gerado continua aqui."
          });
          return;
        }
        if (isEmbeddedClientSetup) {
          window.parent.postMessage({ type: "luma-close" }, "*");
          return;
        }
        window.location.href = `/start/?lang=${builderState.selectedLanguage}`;
        return;
      }
      setIntakeMode("quick");
    });
    switchManualFormButton?.addEventListener("click", switchToManualForm);
    backToChatButton?.addEventListener("click", switchBackToChat);
    assistantAudioToggle.addEventListener("click", toggleAssistantAudio);
    reviewDetailsButton.addEventListener("click", openReviewDetails);
    keepChattingButton.addEventListener("click", keepChatting);
    checkDomainButton?.addEventListener("click", checkDesiredDomainOptions);
    guidedSendButton?.addEventListener("pointerdown", handleGuidedSendAction);
    guidedSendButton?.addEventListener("click", handleGuidedSendAction);
    document.addEventListener("click", (event) => {
      if (event.target?.closest?.("#guidedSendButton")) {
        handleGuidedSendAction(event);
      }
    });
    document.querySelector("#guidedSkipButton").addEventListener("click", skipGuidedQuestion);
    window.handleGuidedGenerateButton = handleGuidedGenerateButton;
    guidedGenerateButton.addEventListener("pointerdown", handleGuidedGenerateButton);
    guidedGenerateButton.addEventListener("click", handleGuidedGenerateButton);
    document.addEventListener("click", (event) => {
      if (event.target?.closest?.("#guidedGenerateButton")) {
        handleGuidedGenerateButton(event);
      }
    }, true);
    guidedReply2.addEventListener("keydown", (event) => {
      if (event.key === "Enter") {
        event.preventDefault();
        handleGuidedSendAction(event);
      }
    });
    guidedReply2.addEventListener("input", updateAssetPromptVisibility);
    builderAvatarManager?.bindTyping(guidedReply2);
    document.querySelectorAll("[data-ai-decide]").forEach((button) => {
      button.addEventListener("click", () => letAiDecide(button.dataset.aiDecide));
    });
    document.querySelectorAll("[data-summary-field]").forEach((field) => {
      field.addEventListener("input", syncGuidedStateFromSummary);
    });
    quickChipRow.querySelectorAll("[data-chip]").forEach((button) => {
      button.addEventListener("click", () => insertQuickChip(button.dataset.chip));
    });
    guidedLogoUpload.addEventListener("change", handleGuidedLogoUpload);
    guidedPhotoUpload.addEventListener("change", handleGuidedPhotoUpload);
    chatLogoUploadButton?.addEventListener("click", () => guidedLogoUpload.click());
    chatPhotoUploadButton?.addEventListener("click", () => guidedPhotoUpload.click());
    summaryLogoUploadButton?.addEventListener("click", () => guidedLogoUpload.click());
    summaryPhotoUploadButton?.addEventListener("click", () => guidedPhotoUpload.click());
    editDetailsButton?.addEventListener("click", openReviewDetails);
    guidedMicButton.addEventListener("click", startVoiceInput);
    languageSelector.addEventListener("change", () => setSelectedLanguage(languageSelector.value));
    summaryLanguageSelector.addEventListener("change", () => setSelectedLanguage(summaryLanguageSelector.value));
    window.addEventListener("load", () => {
      const params = new URLSearchParams(window.location.search);
      if (isPublicClientSetup || params.get("guided") !== "0") {
        setIntakeMode("guided");
      }
      if (isPublicClientSetup && params.get("manual") === "1") {
        switchToManualForm();
      }
    });
    window.addEventListener("beforeunload", () => {
      if (isPublicClientSetup) {
        syncGuidedStateFromSummary();
        saveGuidedDraft();
      }
    });
  }
  if (isLegacyBuilderPage) {
    bootLegacyBuilderPage();
  }
  function initLanguageControls() {
    const params = new URLSearchParams(window.location.search);
    builderState.selectedLanguage = normalizeBrowserLanguage(
      params.get("lang") || (navigator.languages || [navigator.language || "en"])[0]
    );
    builderState.guidedState.selectedLanguage = builderState.selectedLanguage;
    languageSelector.value = builderState.selectedLanguage;
    summaryLanguageSelector.value = builderState.selectedLanguage;
    applyI18n();
  }
  function setSelectedLanguage(value) {
    const previousLanguage = builderState.selectedLanguage;
    builderState.selectedLanguage = normalizeBrowserLanguage(value);
    builderState.guidedState.selectedLanguage = builderState.selectedLanguage;
    languageSelector.value = builderState.selectedLanguage;
    summaryLanguageSelector.value = builderState.selectedLanguage;
    applyI18n();
    updateBuilderAvatarLabels();
    renderGuidedSummary();
    if (previousLanguage !== builderState.selectedLanguage) {
      resetAssistantConversation();
    }
  }
  function t(key) {
    return publicAssistantCopy(I18N[builderState.selectedLanguage]?.[key] || I18N.en[key] || key);
  }
  function langText(map, language = builderState.selectedLanguage) {
    return publicAssistantCopy(map[language] || map.en || "");
  }
  function guidedStage(step = builderState.guidedStep) {
    if (step === "websiteIntent") {
      return {
        index: 1,
        title: langText({ en: "Define the business goal", es: "Definir el objetivo", fr: "D\xE9finir l'objectif", pt: "Definir o objetivo" }),
        body: langText({
          en: "Start with the outcome. LYRA will decide whether this should be a store, catalog, booking flow, lead page, company site or marketplace.",
          es: "Empieza por el objetivo. LYRA decide si conviene tienda, catalogo, reservas, captacion, pagina de empresa o marketplace.",
          fr: "Commencez par l'objectif. LYRA d\xE9cide si le mieux est boutique, catalogue, r\xE9servation, leads, site d'entreprise ou marketplace.",
          pt: "Comece pelo objetivo. A LYRA decide se deve ser loja, cat\xE1logo, agendamento, capta\xE7\xE3o, site empresarial ou marketplace."
        }),
        examples: [
          langText({ en: "Sell products online", es: "Vender productos online", fr: "Vendre en ligne", pt: "Vender online" }),
          langText({ en: "Show a catalog and receive quotes", es: "Mostrar catalogo y recibir cotizaciones", fr: "Catalogue et devis", pt: "Mostrar catalogo e receber or\xE7amentos" }),
          langText({ en: "Explain my company and get leads", es: "Explicar mi empresa y captar clientes", fr: "Pr\xE9senter l'entreprise et capter des leads", pt: "Explicar a empresa e captar contatos" })
        ]
      };
    }
    if (["businessName", "businessDescription", "industry", "location", "servicesProducts"].includes(step)) {
      return {
        index: 2,
        title: langText({ en: "Understand the business", es: "Entender el negocio", fr: "Comprendre l'entreprise", pt: "Entender o neg\xF3cio" }),
        body: langText({
          en: "Give LYRA the raw business details. One rich paragraph is enough; it will extract what matters.",
          es: "Dale a LYRA los datos reales del negocio. Un p\xE1rrafo completo sirve; LYRA extrae lo importante.",
          fr: "Donnez les d\xE9tails bruts. Un paragraphe riche suffit; LYRA extrait l'essentiel.",
          pt: "D\xEA os dados reais do neg\xF3cio. Um par\xE1grafo completo basta; a LYRA extrai o essencial."
        }),
        examples: [
          langText({ en: "Name, what it sells, city", es: "Nombre, qu\xE9 vende, ciudad", fr: "Nom, offre, ville", pt: "Nome, o que vende, cidade" }),
          langText({ en: "Top products or services", es: "Productos o servicios principales", fr: "Produits ou services cl\xE9s", pt: "Produtos ou servi\xE7os principais" })
        ]
      };
    }
    if (["targetAudience", "preferredTone", "preferredColors", "salesMode", "hasLogoPhotos"].includes(step)) {
      return {
        index: 3,
        title: langText({ en: "Shape the brand", es: "Definir el estilo", fr: "D\xE9finir le style", pt: "Definir o estilo" }),
        body: langText({
          en: "Now LYRA chooses the visual personality, sales flow and assets. You can answer or let AI decide.",
          es: "Ahora LYRA define personalidad visual, forma de venta y recursos. Puedes responder o dejar que IA decida.",
          fr: "LYRA d\xE9finit la personnalit\xE9 visuelle, le flux de vente et les assets. Vous pouvez r\xE9pondre ou laisser l'IA d\xE9cider.",
          pt: "Agora a LYRA define visual, venda e assets. Voc\xEA pode responder ou deixar a IA decidir."
        }),
        examples: [
          langText({ en: "Premium, minimal, bold, futuristic", es: "Premium, minimal, llamativo, futurista", fr: "Premium, minimal, audacieux, futuriste", pt: "Premium, minimal, chamativo, futurista" }),
          langText({ en: "Upload logo/photos", es: "Subir logo/fotos", fr: "Importer logo/photos", pt: "Enviar logo/fotos" })
        ]
      };
    }
    if (["contactInfo", "desiredDomain"].includes(step)) {
      return {
        index: 4,
        title: langText({ en: "Prepare launch details", es: "Preparar datos de lanzamiento", fr: "Pr\xE9parer le lancement", pt: "Preparar lan\xE7amento" }),
        body: langText({
          en: "Contact and domain details help make the draft feel real and ready to publish.",
          es: "Contacto y dominio ayudan a que el borrador se sienta real y listo para publicar.",
          fr: "Contact et domaine rendent le brouillon r\xE9el et pr\xEAt \xE0 publier.",
          pt: "Contato e dom\xEDnio deixam o rascunho real e pronto para publicar."
        }),
        examples: [
          langText({ en: "WhatsApp, email, Instagram", es: "WhatsApp, email, Instagram", fr: "WhatsApp, email, Instagram", pt: "WhatsApp, email, Instagram" }),
          langText({ en: "Desired domain", es: "Dominio deseado", fr: "Domaine souhait\xE9", pt: "Dom\xEDnio desejado" })
        ]
      };
    }
    const plan = builderState.guidedState.sitePlan || (builderState.forcedTemplateSelection?.templateId ? buildSitePlan() : null);
    return {
      index: 5,
      title: langText({ en: "Review and generate", es: "Revisar y generar", fr: "V\xE9rifier et g\xE9n\xE9rer", pt: "Revisar e gerar" }),
      body: langText({
        en: "LYRA has enough to create the first editable website draft. Review or ask for one more change.",
        es: "LYRA ya tiene suficiente para crear el primer borrador editable. Revisa o pide un \xFAltimo ajuste.",
        fr: "LYRA a assez d'informations pour cr\xE9er le premier brouillon modifiable. V\xE9rifiez ou demandez un dernier ajustement.",
        pt: "A LYRA j\xE1 tem o suficiente para criar o primeiro rascunho edit\xE1vel. Revise ou pe\xE7a um \xFAltimo ajuste."
      }),
      examples: [
        plan ? langText({ en: "Approve site plan", es: "Aprobar plan del sitio", fr: "Approuver le plan", pt: "Aprovar plano" }) : langText({ en: "Generate draft", es: "Generar borrador", fr: "G\xE9n\xE9rer le brouillon", pt: "Gerar rascunho" }),
        langText({ en: "Ask for a change", es: "Pedir un cambio", fr: "Demander un changement", pt: "Pedir uma mudan\xE7a" })
      ]
    };
  }
  function renderLiveSitePreview() {
    const card = ensureLiveSitePreviewCard();
    if (!card) return;
    card.classList.remove("template-board-card-host", "template-board-loading-host", "selected-template-card-host", "live-render-card-host");
    if (builderState.isGeneratingWebsite) {
      card.classList.add("live-generation-card-host");
      card.innerHTML = `
      <div class="live-generation-state" role="status" aria-live="polite">
        <span class="live-generation-spinner" aria-hidden="true"></span>
        <div>
          <strong>${escapeHtml(langText({
        en: "LYRA is building your website...",
        es: "LYRA est\xE1 construyendo tu sitio...",
        fr: "LYRA construit votre site...",
        pt: "A LYRA est\xE1 criando seu site..."
      }))}</strong>
          <p>${escapeHtml(langText({
        en: "Your structure, content, catalog and visual system are being prepared.",
        es: "Estamos preparando la estructura, el contenido, el cat\xE1logo y el sistema visual.",
        fr: "La structure, le contenu, le catalogue et le syst\xE8me visuel sont en pr\xE9paration.",
        pt: "A estrutura, o conte\xFAdo, o cat\xE1logo e o sistema visual est\xE3o sendo preparados."
      }))}</p>
        </div>
      </div>
    `;
      return;
    }
    card.classList.remove("live-generation-card-host");
    syncTemplateSelectionFromGuidedContext();
    if (shouldShowCanvasTemplateCarousel()) {
      if (shouldRenderTemplateBoardSkeleton()) {
        renderCanvasTemplateSkeleton(card);
        return;
      }
      renderCanvasTemplateCarousel(card);
      return;
    }
    if (shouldShowSelectedTemplatePreview()) {
      renderSelectedTemplateCanvasPreview(card);
      return;
    }
    if (!hasEnoughContextForTemplatePreview()) {
      card.innerHTML = renderNeutralLiveWorkspace();
      return;
    }
    if (renderAiContractPreview(card)) {
      return;
    }
    const payload = livePreviewPayload();
    const selection = livePreviewTemplateSelection();
    let schema = buildInstantTemplateSchema(payload, selection);
    schema = prepareWebsiteConfig(schema, payload, selection);
    card.classList.add("live-render-card-host");
    card.innerHTML = `
    <div class="live-template-preview-viewport">
      <div class="live-template-preview-shell">
        ${renderWebsite2(schema, schema.pages?.[0]?.page_key || "home")}
      </div>
    </div>
  `;
    fitLiveTemplatePreviewToCard(card);
  }
  var LIVE_PREVIEW_DESIGN_WIDTH = 1280;
  var LIVE_PREVIEW_MIN_SCALE = 0.18;
  function fitLiveTemplatePreviewToCard(card) {
    const viewport = card.querySelector(".live-template-preview-viewport");
    const shell = card.querySelector(".live-template-preview-shell");
    if (!viewport || !shell) return;
    shell.style.transform = "none";
    shell.style.width = `${LIVE_PREVIEW_DESIGN_WIDTH}px`;
    const naturalHeight = shell.scrollHeight || shell.offsetHeight || 1;
    const cardRect = card.getBoundingClientRect();
    const availableWidth = cardRect.width || card.clientWidth || LIVE_PREVIEW_DESIGN_WIDTH;
    const availableHeight = cardRect.height || card.clientHeight || naturalHeight;
    const scale = Math.max(
      LIVE_PREVIEW_MIN_SCALE,
      Math.min(availableWidth / LIVE_PREVIEW_DESIGN_WIDTH, availableHeight / naturalHeight, 1)
    );
    shell.style.transformOrigin = "top left";
    shell.style.transform = `scale(${scale})`;
    viewport.style.height = `${Math.round(naturalHeight * scale)}px`;
  }
  var __livePreviewResizeTimer = null;
  if (typeof window !== "undefined") {
    window.addEventListener("resize", () => {
      if (__livePreviewResizeTimer) clearTimeout(__livePreviewResizeTimer);
      __livePreviewResizeTimer = setTimeout(() => {
        const card = document.querySelector(".live-site-preview-card.live-render-card-host");
        if (card) fitLiveTemplatePreviewToCard(card);
      }, 150);
    });
  }
  function shouldShowCanvasTemplateCarousel() {
    if (builderState.isGeneratingWebsite) return false;
    if (!isPublicClientSetup || builderState.currentSchema) return false;
    if (!hasEnoughContextForTemplatePreview()) return false;
    const pages = Array.isArray(builderState.guidedState.sitePlan?.pages) ? builderState.guidedState.sitePlan.pages : [];
    if (pages.some((page) => Array.isArray(page.sections) && page.sections.some((section) => section?.componentType))) return false;
    if (builderState.forcedTemplateSelection?.intent === "client_visual_template_choice") return false;
    return true;
  }
  function templateBoardLoadingKey() {
    const selection = livePreviewTemplateSelection();
    return [
      builderState.selectedLanguage,
      builderState.guidedStep,
      selection?.templateId || "",
      selection?.catalogType || "",
      builderState.guidedState.websiteIntent || "",
      builderState.guidedState.businessDescription || "",
      arrayValue2(builderState.guidedState.servicesProducts).join("|")
    ].join("::");
  }
  function shouldRenderTemplateBoardSkeleton() {
    const key = templateBoardLoadingKey();
    if (key !== builderState.templateBoardLoadingSignature) {
      builderState.templateBoardLoadingSignature = key;
      builderState.templateBoardLoading = true;
      if (builderState.templateBoardLoadingTimer) clearTimeout(builderState.templateBoardLoadingTimer);
      builderState.templateBoardLoadingTimer = setTimeout(() => {
        builderState.templateBoardLoading = false;
        if (shouldShowCanvasTemplateCarousel()) renderLiveSitePreview();
      }, 560);
      return true;
    }
    return builderState.templateBoardLoading;
  }
  function renderCanvasTemplateSkeleton(card) {
    card.classList.add("template-board-loading-host");
    const skeletonCards = Array.from({ length: 5 }).map((_, index) => `
    <article class="template-skeleton-card" aria-hidden="true" style="--skeleton-delay: ${index * 90}ms">
      <div class="template-skeleton-preview"></div>
      <div class="template-skeleton-body">
        <span class="template-skeleton-pill"></span>
        <strong></strong>
        <small></small>
        <small></small>
        <em></em>
      </div>
    </article>
  `).join("");
    card.innerHTML = `
    <section class="template-choice-panel template-board-panel template-skeleton-panel" aria-busy="true" aria-live="polite">
      <div class="template-choice-heading template-carousel-heading">
        <strong>${escapeHtml(langText({
      en: "Preparing premium template options",
      es: "Preparando opciones premium",
      fr: "Preparation des options premium",
      pt: "Preparando opcoes premium"
    }))}</strong>
        <span>${escapeHtml(langText({
      en: "LYRA is matching your business context to the strongest editable template bases.",
      es: "LYRA esta cruzando el contexto del negocio con las mejores bases editables.",
      fr: "LYRA rapproche le contexte business des meilleures bases modifiables.",
      pt: "A LYRA esta combinando o contexto do negocio com as melhores bases editaveis."
    }))}</span>
      </div>
      <div class="template-board-grid template-skeleton-grid">
        ${skeletonCards}
      </div>
    </section>
  `;
  }
  function renderCanvasTemplateCarousel(card) {
    card.classList.add("template-board-card-host");
    const selection = livePreviewTemplateSelection();
    const selectedId = selection?.templateId || "";
    const choices = canvasTemplateChoices(selectedId).slice(0, 5);
    if (!choices.length) {
      card.innerHTML = renderNeutralLiveWorkspace();
      return;
    }
    const shouldAnimateCards = !window.__lyraTemplateBoardAnimated;
    card.innerHTML = `
    <section class="template-choice-panel template-board-panel">
      <div class="template-choice-heading template-carousel-heading">
        <strong>${escapeHtml(langText({
      en: "Select the structure LYRA should use",
      es: "Selecciona la estructura que LYRA debe usar",
      fr: "Selectionnez la structure que LYRA doit utiliser",
      pt: "Selecione a estrutura que a LYRA deve usar"
    }))}</strong>
        <span>${escapeHtml(langText({
      en: "These are real template bases. LYRA will adapt copy, colors, products and flow after you choose one.",
      es: "Estas son bases reales. LYRA adaptara textos, colores, productos y flujo despues de elegir una.",
      fr: "Ce sont de vraies bases. LYRA adaptera textes, couleurs, produits et parcours apres le choix.",
      pt: "Estas sao bases reais. A LYRA adapta textos, cores, produtos e fluxo depois da escolha."
    }))}</span>
      </div>
      <div class="template-board-grid template-coverflow-track ${shouldAnimateCards ? "template-board-grid-enter" : ""}" aria-label="Template options">
        ${choices.map((choice, index) => `
          <article class="template-choice-card template-board-card template-coverflow-card template-carousel-card ${choice.templateId === selectedId ? "active-card recommended" : index === 0 ? "recommended" : ""}" style="--template-card-index: ${index};" data-template-choice="${escapeAttribute(choice.templateId)}" data-catalog-type="${escapeAttribute(choice.catalogType || "")}" tabindex="0">
            <div class="template-board-image">
              ${templateLivePreviewMarkup(choice)}
            </div>
            <div class="template-board-body">
              <div class="template-board-meta">
                <span class="template-status-pill">${escapeHtml(choice.templateId === selectedId || index === 0 ? langText({ en: "Recommended", es: "Recomendada", fr: "Recommandee", pt: "Recomendada" }) : langText({ en: "Alternative", es: "Alternativa", fr: "Alternative", pt: "Alternativa" }))}</span>
                <div class="template-board-badges" aria-label="${escapeAttribute(langText({ en: "Template categories", es: "Categorias del template", fr: "Categories du template", pt: "Categorias do template" }))}">
                  ${templateCardBadges(choice).map((badge, badgeIndex) => `<em data-badge-tone="${badgeIndex % 3}">${escapeHtml(badge)}</em>`).join("")}
                </div>
              </div>
              <strong>${escapeHtml(localizedTemplateName(choice))}</strong>
              <small>${escapeHtml(localizedTemplateDescription(choice))}</small>
              <button type="button" data-template-preview="${escapeAttribute(choice.templateId)}">${escapeHtml(langText({ en: "Preview", es: "Previsualizar", fr: "Previsualiser", pt: "Previsualizar" }))}</button>
            </div>
          </article>
        `).join("")}
      </div>
    </section>
  `;
    window.__lyraTemplateBoardAnimated = true;
    const panel = card.querySelector(".template-board-panel");
    initTemplateCarousel(card.querySelector(".template-board-grid"));
    panel?.addEventListener("click", (event) => {
      const button = event.target?.closest?.("[data-template-preview]");
      if (!button) return;
      const templateId = button.dataset.templatePreview || "";
      if (!templateId) return;
      event.preventDefault();
      event.stopPropagation();
      window.Lyra?.selectTemplate(templateId);
    });
  }
  function canvasTemplateChoices(selectedTemplateId = "") {
    const selected = templatePreviewMeta(selectedTemplateId);
    const selectedCatalog = selected?.catalogType || "";
    const selectedText = `${selectedTemplateId} ${selectedCatalog}`;
    const ordered = [];
    const usedImages = /* @__PURE__ */ new Set();
    const add = (templateId, options = {}) => {
      const choice = templatePreviewMeta(templateId);
      if (!choice || ordered.some((item) => item.templateId === choice.templateId)) return;
      if (usedImages.has(choice.image) && !options.allowDuplicateImage) return;
      usedImages.add(choice.image);
      ordered.push(choice);
    };
    add(selectedTemplateId);
    if (/legal|professional|consulting|tax|insurance|advisor/.test(selectedText)) {
      ["legal-professional-services-pro", "corporate-company-pro", "b2b-saas-enterprise-pro", "booking-appointment-pro", "medical-wellness-clinic-pro"].forEach(add);
    } else if (/clinic|medical|wellness|dental/.test(selectedText)) {
      ["medical-wellness-clinic-pro", "booking-appointment-pro", "local-services-pro-plus", "corporate-company-pro", "lead-funnel-pro"].forEach(add);
    } else if (/single_vendor_dense|retail|dense|mega/.test(selectedText)) {
      ["mega-retail-store", "mega-marketplace", "listing-marketplace-pro", "fashion-drop-pro", "digital-products-store"].forEach(add);
    } else if (/listing|classified|real_estate|real-estate/.test(selectedText)) {
      ["listing-marketplace-pro", "real-estate-listings-pro", "mega-marketplace", "mega-retail-store", "corporate-company-pro"].forEach(add);
    } else if (/restaurant|menu|food/.test(selectedText)) {
      ["restaurant-food-business", "booking-appointment-pro", "lead-funnel-pro", "home-services-premium", "local-services-pro-plus"].forEach(add);
    } else if (/booking|appointment/.test(selectedText)) {
      ["booking-appointment-pro", "local-services-pro-plus", "home-services-premium", "lead-funnel-pro", "corporate-company-pro"].forEach(add);
    } else if (/industrial|manufacturing|supplier/.test(selectedText)) {
      ["manufacturing-industrial-supplier-pro", "b2b-saas-enterprise-pro", "corporate-company-pro", "lead-funnel-pro", "mega-retail-store"].forEach(add);
    } else if (/b2b|saas|enterprise|solution/.test(selectedText)) {
      ["b2b-saas-enterprise-pro", "corporate-company-pro", "lead-funnel-pro", "manufacturing-industrial-supplier-pro", "premium-product-store"].forEach(add);
    } else if (/fashion|lookbook|collection/.test(selectedText)) {
      ["fashion-drop-pro", "luxury-high-ticket-pro", "premium-product-store", "mega-retail-store", "listing-marketplace-pro"].forEach(add);
    } else {
      rankedFallbackChoices(selectedTemplateId).forEach((choice) => add(choice.templateId));
    }
    TEMPLATE_PREVIEW_CHOICES.forEach((choice) => {
      if (ordered.length < 5) add(choice.templateId);
    });
    TEMPLATE_PREVIEW_CHOICES.forEach((choice) => {
      if (ordered.length < 5) add(choice.templateId, { allowDuplicateImage: true });
    });
    return ordered;
  }
  function shouldShowSelectedTemplatePreview() {
    return Boolean(
      isPublicClientSetup && !builderState.currentSchema && builderState.forcedTemplateSelection?.intent === "client_visual_template_choice" && templatePreviewMeta(builderState.forcedTemplateSelection.templateId)
    );
  }
  function renderSelectedTemplateCanvasPreview(card) {
    card.classList.add("selected-template-card-host");
    const choice = templatePreviewMeta(builderState.forcedTemplateSelection.templateId);
    card.innerHTML = `
    <section class="selected-template-preview canvas-fade-in">
      <div class="selected-template-preview-copy">
        <span>${escapeHtml(langText({ en: "Selected architecture", es: "Arquitectura seleccionada", fr: "Architecture selectionnee", pt: "Arquitetura selecionada" }))}</span>
        <h2>${escapeHtml(localizedTemplateName(choice))}</h2>
        <p>${escapeHtml(langText({
      en: "This is the real template base. LYRA will now adapt it to the business copy, colors, catalog and conversion flow.",
      es: "Esta es la base real del template. LYRA ahora la adaptara al negocio, textos, colores, catalogo y flujo de conversion.",
      fr: "C'est la vraie base du template. LYRA l'adaptera au business, textes, couleurs, catalogue et conversion.",
      pt: "Esta e a base real do template. A LYRA vai adapta-la ao negocio, textos, cores, catalogo e conversao."
    }))}</p>
      </div>
      <div class="selected-template-preview-frame">
        <img src="${escapeAttribute(choice.image)}" alt="${escapeAttribute(localizedTemplateName(choice))}">
      </div>
      <div class="selected-template-preview-actions">
        <button type="button" data-change-template>${escapeHtml(langText({ en: "Choose another base", es: "Elegir otra base", fr: "Choisir une autre base", pt: "Escolher outra base" }))}</button>
        <button type="button" data-chat-generate>${escapeHtml(langText({ en: "Generate editable site", es: "Generar sitio editable", fr: "Generer le site modifiable", pt: "Gerar site editavel" }))}</button>
      </div>
    </section>
  `;
    card.querySelector("[data-change-template]")?.addEventListener("click", () => {
      builderState.forcedTemplateSelection = {
        ...builderState.forcedTemplateSelection,
        intent: "ai_studio_plan"
      };
      renderLiveSitePreview();
    });
    card.querySelector("[data-chat-generate]")?.addEventListener("click", handleGuidedGenerateButton);
  }
  function hasEnoughContextForTemplatePreview() {
    const intent = normalizeTemplateIntentText(builderState.guidedState.websiteIntent);
    const description = normalizeTemplateIntentText(builderState.guidedState.businessDescription);
    const offerItems = meaningfulOfferItems(builderState.guidedState.servicesProducts);
    const services = offerItems.join(" ");
    const commerceSignal = normalizeTemplateIntentText(`${intent} ${description} ${services}`);
    const hasIntent = /tienda|store|shop|marketplace|catalogo|servicio|service|reserva|booking|restaurante|restaurant|pagina|website|landing|amazon|ebay|abogado|abogados|legal|lawyer|bufet|firma|consultoria|consulting|clinica|clinic|empresa|company/.test(commerceSignal);
    const hasConcreteOffer = offerItems.length >= 2 || description.length >= 45 || commerceSignal.split(/\s+/).length >= 16 || /abogado|abogados|legal|lawyer|bufet|firma|consultoria|consulting|clinica|clinic|servicio|service/.test(commerceSignal) || textSuggestsBroadMarketplace(commerceSignal) || textSuggestsFocusedProductLine(commerceSignal);
    return Boolean(hasIntent && hasConcreteOffer);
  }
  function meaningfulOfferItems(value) {
    return arrayValue2(value).map((item) => cleanExtractedPhrase(item, 70)).filter((item) => item && !isGenericCommerceIntent(item) && !isWeakOfferItem(item));
  }
  function isWeakOfferItem(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text) return true;
    if (/^(online|internet|web|ecommerce|e-commerce|venta|ventas|sell|selling|products?|productos?|services?|servicios?|catalogo|catalog|marketplace)$/i.test(text)) return true;
    return /^(aceptar|recibir|tomar|hacer|gestionar)?\s*(pedidos?|orders?|ordenes|online orders)$/i.test(text) || /^(vender|venta|ventas)\s*(online|en linea)?$/i.test(text);
  }
  function livePreviewTemplateSelection() {
    const profile = livePreviewTemplateProfile();
    const meta = templatePreviewMeta(profile.templateId) || templatePreviewMeta("mega-retail-store");
    const forcedMatchesProfile = builderState.forcedTemplateSelection?.templateId === profile.templateId;
    return {
      templateId: meta?.templateId || profile.templateId || "mega-retail-store",
      template: forcedMatchesProfile && builderState.forcedTemplateSelection?.template ? builderState.forcedTemplateSelection.template : { id: meta?.templateId || profile.templateId || "mega-retail-store", name: localizedTemplateName(meta), catalogModel: { catalogType: meta?.catalogType || profile.catalogType || "single_vendor_dense_catalog" } },
      intent: forcedMatchesProfile ? builderState.forcedTemplateSelection?.intent || "live_preview_template" : "live_preview_template",
      catalogType: meta?.catalogType || profile.catalogType || "single_vendor_dense_catalog",
      reason: "Live preview selected from guided context"
    };
  }
  function livePreviewTemplateProfile() {
    const templateId = inferLivePreviewTemplateId();
    const meta = templatePreviewMeta(templateId) || templatePreviewMeta("mega-retail-store");
    const catalogType = builderState.forcedTemplateSelection?.templateId === templateId ? builderState.forcedTemplateSelection?.catalogType || meta?.catalogType || "" : meta?.catalogType || "";
    const common = {
      templateId,
      label: localizedTemplateName(meta),
      image: meta?.image || "/templates-preview/screenshots/premium.png",
      itemLabels: [
        langText({ en: "Offer", es: "Oferta", fr: "Offre", pt: "Oferta" }),
        langText({ en: "Featured", es: "Destacado", fr: "Phare", pt: "Destaque" }),
        langText({ en: "CTA", es: "Accion", fr: "Action", pt: "Acao" })
      ],
      sectionKicker: langText({ en: "Page structure", es: "Estructura de pagina", fr: "Structure de page", pt: "Estrutura da pagina" }),
      sectionTitle: langText({ en: "Editable sections ready for this business", es: "Secciones editables listas para este negocio", fr: "Sections modifiables pour cette entreprise", pt: "Secoes editaveis para este negocio" })
    };
    if (/mega-retail-store/.test(templateId) || /single_vendor_dense|dense_retail|retail/.test(catalogType)) {
      return {
        ...common,
        kind: "retail",
        kicker: langText({ en: "Search-first retail store", es: "Tienda retail con busqueda", fr: "Boutique retail avec recherche", pt: "Loja retail com busca" }),
        cta: langText({ en: "Shop catalog", es: "Ver catalogo", fr: "Voir catalogue", pt: "Ver catalogo" }),
        sectionKicker: langText({ en: "Retail catalog structure", es: "Estructura de catalogo retail", fr: "Structure catalogue retail", pt: "Estrutura de catalogo retail" }),
        sectionTitle: langText({ en: "Categories, offers and owned product discovery", es: "Categorias, ofertas y productos propios", fr: "Categories, offres et produits propres", pt: "Categorias, ofertas e produtos proprios" }),
        itemLabels: [
          langText({ en: "Category", es: "Categoria", fr: "Categorie", pt: "Categoria" }),
          langText({ en: "Deal", es: "Oferta", fr: "Offre", pt: "Oferta" }),
          langText({ en: "Product", es: "Producto", fr: "Produit", pt: "Produto" })
        ]
      };
    }
    if (/mega-marketplace|marketplace-style/.test(templateId) || /dense_marketplace/.test(catalogType)) {
      return {
        ...common,
        kind: "marketplace",
        kicker: langText({ en: "Search-first catalog", es: "Catalogo con busqueda", fr: "Catalogue avec recherche", pt: "Catalogo com busca" }),
        cta: langText({ en: "Shop deals", es: "Ver ofertas", fr: "Voir offres", pt: "Ver ofertas" }),
        sectionKicker: langText({ en: "Catalog structure", es: "Estructura del catalogo", fr: "Structure catalogue", pt: "Estrutura do catalogo" }),
        sectionTitle: langText({ en: "Categories, offers and product discovery", es: "Categorias, ofertas y descubrimiento de productos", fr: "Categories, offres et decouverte produit", pt: "Categorias, ofertas e descoberta de produtos" }),
        itemLabels: [
          langText({ en: "Category", es: "Categoria", fr: "Categorie", pt: "Categoria" }),
          langText({ en: "Deal", es: "Oferta", fr: "Offre", pt: "Oferta" }),
          langText({ en: "Product", es: "Producto", fr: "Produit", pt: "Produto" })
        ]
      };
    }
    if (/listing|real-estate/.test(templateId) || /listing|classified|real_estate/.test(catalogType)) {
      return {
        ...common,
        kind: "listings",
        kicker: langText({ en: "Seller listings", es: "Listados de vendedores", fr: "Annonces vendeurs", pt: "Anuncios de vendedores" }),
        cta: langText({ en: "Compare listings", es: "Comparar listados", fr: "Comparer", pt: "Comparar" }),
        sectionKicker: langText({ en: "Listing flow", es: "Flujo de listados", fr: "Flux annonces", pt: "Fluxo de anuncios" }),
        sectionTitle: langText({ en: "Seller trust, filters and comparison", es: "Confianza, filtros y comparacion", fr: "Confiance, filtres et comparaison", pt: "Confianca, filtros e comparacao" }),
        itemLabels: [
          langText({ en: "Listing", es: "Listado", fr: "Annonce", pt: "Anuncio" }),
          langText({ en: "Condition", es: "Condicion", fr: "Etat", pt: "Condicao" }),
          langText({ en: "Seller", es: "Vendedor", fr: "Vendeur", pt: "Vendedor" })
        ]
      };
    }
    if (/fashion/.test(templateId) || /lookbook|collection/.test(catalogType)) {
      return {
        ...common,
        kind: "fashion",
        kicker: langText({ en: "Editorial collection", es: "Coleccion editorial", fr: "Collection editoriale", pt: "Colecao editorial" }),
        cta: langText({ en: "Explore drop", es: "Ver coleccion", fr: "Voir collection", pt: "Ver colecao" }),
        sectionKicker: langText({ en: "Collection story", es: "Historia de coleccion", fr: "Histoire collection", pt: "Historia da colecao" }),
        sectionTitle: langText({ en: "Drops, looks and visual product cards", es: "Drops, looks y productos visuales", fr: "Drops, looks et fiches visuelles", pt: "Drops, looks e cards visuais" }),
        itemLabels: [
          langText({ en: "Collection", es: "Coleccion", fr: "Collection", pt: "Colecao" }),
          langText({ en: "Look", es: "Look", fr: "Look", pt: "Look" }),
          langText({ en: "Drop", es: "Drop", fr: "Drop", pt: "Drop" })
        ]
      };
    }
    if (/restaurant/.test(templateId) || /restaurant|menu|food/.test(catalogType)) {
      return {
        ...common,
        kind: "restaurant",
        kicker: langText({ en: "Menu and ordering", es: "Menu y pedidos", fr: "Menu et commande", pt: "Menu e pedidos" }),
        cta: langText({ en: "View menu", es: "Ver menu", fr: "Voir menu", pt: "Ver menu" }),
        sectionKicker: langText({ en: "Menu system", es: "Sistema de menu", fr: "Systeme menu", pt: "Sistema de menu" }),
        sectionTitle: langText({ en: "Signature dishes, specials and ordering", es: "Platos, especiales y pedidos", fr: "Plats, specials et commande", pt: "Pratos, especiais e pedidos" }),
        itemLabels: [
          langText({ en: "Dish", es: "Plato", fr: "Plat", pt: "Prato" }),
          langText({ en: "Special", es: "Especial", fr: "Special", pt: "Especial" }),
          langText({ en: "Order", es: "Pedido", fr: "Commande", pt: "Pedido" })
        ]
      };
    }
    if (/booking|services|clinic|legal|corporate|lead-funnel|local/.test(templateId) || /service|booking|lead|company|legal|medical/.test(catalogType)) {
      return {
        ...common,
        kind: "service",
        kicker: langText({ en: "Trust and conversion", es: "Confianza y conversion", fr: "Confiance et conversion", pt: "Confianca e conversao" }),
        cta: langText({ en: "Request quote", es: "Solicitar cotizacion", fr: "Demander devis", pt: "Pedir orcamento" }),
        sectionKicker: langText({ en: "Service path", es: "Ruta del servicio", fr: "Parcours service", pt: "Caminho do servico" }),
        sectionTitle: langText({ en: "Proof, process and fast contact", es: "Prueba, proceso y contacto rapido", fr: "Preuves, processus et contact", pt: "Prova, processo e contato rapido" }),
        itemLabels: [
          langText({ en: "Service", es: "Servicio", fr: "Service", pt: "Servico" }),
          langText({ en: "Proof", es: "Prueba", fr: "Preuve", pt: "Prova" }),
          langText({ en: "Process", es: "Proceso", fr: "Processus", pt: "Processo" })
        ]
      };
    }
    return {
      ...common,
      kind: "premium",
      kicker: langText({ en: "Premium showcase", es: "Presentacion premium", fr: "Presentation premium", pt: "Apresentacao premium" }),
      cta: langText({ en: "Explore product", es: "Ver producto", fr: "Voir produit", pt: "Ver produto" }),
      sectionKicker: langText({ en: "Product story", es: "Historia del producto", fr: "Histoire produit", pt: "Historia do produto" }),
      sectionTitle: langText({ en: "Hero, story and feature highlights", es: "Hero, historia y detalles clave", fr: "Hero, histoire et points forts", pt: "Hero, historia e destaques" })
    };
  }
  function inferLivePreviewTemplateId() {
    const text = guidedTemplateContextText();
    const inferred = inferTemplateIdFromText(text);
    if (inferred) return inferred;
    const aiSelectedTemplateId = resolvedAiTemplateId();
    if (aiSelectedTemplateId) return aiSelectedTemplateId;
    if (builderState.forcedTemplateSelection?.templateId) return builderState.forcedTemplateSelection.templateId;
    return "mega-retail-store";
  }
  function resolvedAiTemplateId() {
    if (builderState.forcedTemplateSelection?.intent === "client_visual_template_choice" && isConcreteTemplateId(builderState.forcedTemplateSelection.templateId)) {
      return builderState.forcedTemplateSelection.templateId;
    }
    const candidates = [
      builderState.guidedState.designStrategy?.selectedTemplateId,
      builderState.guidedState.designStrategy?.diagnosis?.recommendedTemplateId,
      builderState.guidedState.sitePlan?.templateId,
      builderState.forcedTemplateSelection?.templateId
    ];
    return candidates.find((templateId) => isConcreteTemplateId(templateId)) || "";
  }
  function isConcreteTemplateId(templateId) {
    const value = String(templateId || "").trim();
    return Boolean(value && !/^default_|pending|unknown|collecting|provisional/i.test(value));
  }
  function guidedTemplateContextText(extra = "") {
    return [
      builderState.guidedState.websiteIntent,
      builderState.guidedState.businessName,
      builderState.guidedState.industry,
      builderState.guidedState.businessDescription,
      arrayValue2(builderState.guidedState.servicesProducts).join(" "),
      builderState.guidedState.targetAudience,
      builderState.guidedState.preferredTone,
      arrayValue2(builderState.guidedState.preferredColors).join(" "),
      builderState.guidedState.salesMode,
      extra
    ].join(" ").toLowerCase();
  }
  function normalizeTemplateIntentText(value) {
    return String(value || "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/\s+/g, " ").trim();
  }
  function textSuggestsBroadMarketplace(value) {
    const text = normalizeTemplateIntentText(value);
    const explicitBroad = /\b(amazon|tipo amazon|como amazon|mega tienda|mega store|mega marketplace|marketplace tipo amazon|marketplace estilo amazon)\b/.test(text);
    const explicitMarketplace = /\b(marketplace|market place|mercado online|multi vendedor|multi-vendedor|multi seller|multiseller)\b/.test(text) && !/\b(tipo ebay|como ebay|ebay|clasificados|classifieds|listados|listings|segunda mano|usado|used)\b/.test(text);
    const crossCategoryBroad = /(ropa|accesorios).*(carros|autos|juguetes|anime|gadgets)/.test(text) || /(carros|autos|juguetes|anime|gadgets).*(ropa|accesorios)/.test(text);
    const broadCatalog = /\b(de todo|todo tipo|variedad|variado|variados|productos variados|catalogo grande|catalogo variado|muchos productos|muchas categorias|multi categoria|cosas raras|cosas inusuales|inusual|unusual|nada comun|poco comun|dificil de encontrar|curiosidades|gadgets|anime|juguetes)\b/.test(text) || crossCategoryBroad;
    if (explicitBroad || explicitMarketplace) return true;
    if (textSuggestsFocusedCommerceStore(text) && !crossCategoryBroad) return false;
    return broadCatalog;
  }
  function textSuggestsMultiVendorMarketplace(value) {
    const text = normalizeTemplateIntentText(value);
    return /\b(multi vendedor|multi-vendedor|multi seller|multiseller|vendedores externos|external sellers|third party sellers|otros vendedores|que otros vendan|plataforma para vendedores|seller onboarding|vendor onboarding|vendor payout|payouts|comisiones a vendedores|marketplace multi vendor|marketplace multi-vendedor|varios vendedores independientes)\b/.test(text);
  }
  function textSuggestsMegaRetailStore(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text || textSuggestsMultiVendorMarketplace(text)) return false;
    const explicitRetail = /\b(tipo amazon|como amazon|amazon|mega tienda|mega store|tienda grande|catalogo grande|catalogo variado|productos variados|muchos productos|muchas categorias|de todo|todo tipo|inventario propio|mi tienda|mis productos|yo vendo|voy a vender|una sola tienda|un solo vendedor|single vendor|owned inventory)\b/.test(text);
    return explicitRetail || textSuggestsBroadMarketplace(text);
  }
  function textSuggestsJewelryAccessoryStore(value) {
    const text = normalizeTemplateIntentText(value);
    const automotiveAccessory = /\b(accesorios? (para|de) (carros|autos|automotriz|automotrices|camionetas|motos|4x4)|auto accessories|car accessories)\b/.test(text);
    if (automotiveAccessory) return false;
    return /\b(bisuteria|bijouterie|joyeria|jewelry|jewellery|accesorios hechos a mano|accesorios artesanales|collar|collares|necklace|necklaces|pulsera|pulseras|bracelet|bracelets|arete|aretes|zarcillo|zarcillos|earring|earrings|anillo|anillos|rings?|cadena|cadenas|dije|dijes|charm|charms|handmade accessories|handmade jewelry)\b/.test(text) || /\baccesorios?\b/.test(text) && /\b(moda|fashion|boutique|bisuteria|joyeria|collar|pulsera|arete|anillo)\b/.test(text);
  }
  function textSuggestsHandmadeCraftGoods(value) {
    const text = normalizeTemplateIntentText(value);
    return /\b(artesanal|artesanales|artesania|artesanía|hecho a mano|hechos a mano|handmade|jabon|jabones|soap|bath bomb|bath bombs|bomba de bano|bombas de bano|velas|candles|manualidades|crafts)\b/.test(text);
  }
  function textSuggestsProfessionalService(value) {
    const text = normalizeTemplateIntentText(value);
    return /\b(abogado|abogados|bufete|lawyer|law firm|legal|contador|accountant|tax|taxes|impuestos|consulting|consultoria|consultoría|seguros|insurance|asesor|advisor|financiero|compliance|firma profesional|inmigracion|inmigración|contratos|defensa civil)\b/.test(text);
  }
  function textSuggestsFocusedCommerceStore(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text) return false;
    return textSuggestsJewelryAccessoryStore(text) || /\b(ropa|fashion|moda|boutique|streetwear|zapato|sneaker|apparel|clothing|beauty|belleza|skincare|cosmeticos|cosméticos|velas|candles|decoracion|decoración|ceramica|cerámica|arte hecho a mano|manualidades|crafts|productos artesanales|artesania|artesanía|coleccion propia|coleccion de|colección de)\b/.test(text);
  }
  function textSuggestsBeautyCommerceStore(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text || textSuggestsBroadMarketplace(text) || textSuggestsMultiVendorMarketplace(text)) return false;
    const beautyProduct = /\b(jabones?|soap|velas?|candles?|bath|bano|baño|bath bombs?|bombas de bano|bombas de baño|body care|cuidado personal|skincare|cosmeticos|cosméticos|spa products|productos de spa)\b/.test(text);
    const commerceIntent = /\b(vender|venta|ventas|tienda|store|shop|catalogo|catálogo|productos|online|ecommerce|e-commerce|checkout|carrito)\b/.test(text);
    return beautyProduct && commerceIntent;
  }
  function textSuggestsFocusedProductLine(value) {
    const text = normalizeTemplateIntentText(value);
    const vehicleProductLine = /\b(pickup|camioneta|camionetas)\b/.test(text) && /\b(truck|camioneta|camionetas|4x4|off road|automotriz|automotive|vehiculo|autos?|carros?)\b/.test(text);
    return (/\b(linea de|linea para|product line|same niche|mismo nicho|mismo tipo|una categoria|varios modelos|modelos para|parachoques|bumper|4x4|off road)\b/.test(text) || vehicleProductLine) && !textSuggestsBroadMarketplace(text);
  }
  function textSuggestsSingleProductShowcase(value) {
    const text = normalizeTemplateIntentText(value);
    return /\b(un solo producto|solo un producto|producto unico|producto estrella|flagship|single product|one product|producto premium|showcase|presentacion premium|portafolio|portfolio)\b/.test(text) && !textSuggestsBroadMarketplace(text);
  }
  function textSuggestsPremiumProductPreference(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text || textSuggestsBroadMarketplace(text)) return false;
    return textSuggestsSingleProductShowcase(text) || textSuggestsFocusedProductLine(text) || /\b(linea de|coleccion de|producto especializado|producto de nicho|flagship|single product|one product|limited collection|curated collection)\b/.test(text);
  }
  function templateIntentScorecard(value, payload = {}) {
    const text = normalizeTemplateIntentText([
      value,
      payload.business_name,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience,
      payload.preferred_tone,
      arrayValue2(payload.preferred_colors).join(" "),
      payload.salesMode || builderState.guidedState.salesMode
    ].join(" "));
    const products = meaningfulOfferItems(payload.services_products || []);
    if (!text) return [];
    const scoreFor = (templateId, score, reason) => ({ templateId, score, reason });
    const scores = [];
    const has = (pattern) => pattern.test(text);
    if (textSuggestsMultiVendorMarketplace(text)) scores.push(scoreFor("mega-marketplace", 145, "explicit multi-vendor marketplace reference"));
    if (textSuggestsMegaRetailStore(text)) scores.push(scoreFor("mega-retail-store", 140, "large single-owner retail catalog"));
    if (textSuggestsProfessionalService(text)) scores.push(scoreFor("legal-professional-services-pro", 135, "professional services trust flow"));
    if (textSuggestsBeautyCommerceStore(text)) {
      scores.push(textSuggestsHandmadeCraftGoods(text) ? scoreFor("premium-product-store", 122, "handmade beauty/home-craft boutique (soap, candles, bath goods)") : scoreFor("mega-retail-store", 122, "beauty and personal-care retail catalog"));
    }
    if (textSuggestsBroadMarketplace(text) && !textSuggestsMultiVendorMarketplace(text)) scores.push(scoreFor("mega-retail-store", 105, "broad owned catalog"));
    if (textSuggestsBroadMarketplace(text) && textSuggestsMultiVendorMarketplace(text)) scores.push(scoreFor("mega-marketplace", 105, "broad multi-vendor catalog"));
    if (products.length >= 10 && !textSuggestsFocusedCommerceStore(text) && !textSuggestsProfessionalService(text) && !has(/\b(restaurante|restaurant|menu|comida|food|cafe|cafeteria|barber|barberia|salon|spa|clinica|clinic|servicio|service|contractor|curso|course)\b/)) {
      scores.push(scoreFor(textSuggestsMultiVendorMarketplace(text) ? "mega-marketplace" : "mega-retail-store", 55, "many independent product categories"));
    }
    if (has(/\b(tipo ebay|como ebay|clasificados|classifieds|listados|listings|vendedores|seller|usado|used|segunda mano)\b/)) scores.push(scoreFor("listing-marketplace-pro", 120, "seller/listing marketplace"));
    if (has(/\b(real estate|bienes raices|bienes raices|inmuebles|propiedades|casas|apartamentos|alquiler|renta|rentals|zillow|realtor|mls|autotrader|auto trader|vehiculos|vehiculos usados|carros usados)\b/)) scores.push(scoreFor("real-estate-listings-pro", 126, "searchable listing business"));
    if (has(/\b(restaurante|restaurant|food truck|cafeteria|cafeteria|catering|menu|menu|comida|pizza|tacos|bakery|panaderia|bar|delivery de comida|pickup de comida|ordenar comida|pedir comida|pedido de comida)\b/)) scores.push(scoreFor("restaurant-food-business", 125, "restaurant/menu flow"));
    if (has(/\b(barber|barberia|barberia|barbershop|salon|spa|citas|reservas|appointments|booking|agenda|agendar|calendar|calendario)\b/)) scores.push(scoreFor("booking-appointment-pro", 120, "appointment booking flow"));
    if (has(/\b(clinica|clinica|clinic|med spa|medical spa|spa medico|estetica|estetica|aesthetic|dental|dentist|doctor|wellness|salud|health|therapy|terapia|nutricion|laser|botox|facial|skincare|dermatology|fisio|physio|chiropractor|consulta medica)\b/)) scores.push(scoreFor("medical-wellness-clinic-pro", 118, "clinic/wellness trust flow"));
    if (has(/\b(abogado|lawyer|legal|law firm|contador|accountant|tax|taxes|impuestos|consulting|consultoria|seguros|insurance|asesor|advisor|financiero|compliance|firma profesional)\b/)) scores.push(scoreFor("legal-professional-services-pro", 115, "professional services trust flow"));
    if (has(/\b(b2b saas|saas|enterprise|software empresarial|automatizacion|automation|platform|plataforma|dashboard|crm|erp|integraciones|api|managed services|it services|business systems|request demo|demo|workflow)\b/)) scores.push(scoreFor("b2b-saas-enterprise-pro", 115, "B2B software/enterprise flow"));
    if (has(/\b(manufacturing|manufacturer|industrial|industrial supplier|fabrica|fabricante|manufactura|maquinaria|machinery|repuestos industriales|parts supplier|tools supplier|herramientas industriales|safety equipment|bulk order|b2b procurement|rfq|proveedor industrial|suministros industriales)\b/)) scores.push(scoreFor("manufacturing-industrial-supplier-pro", 114, "industrial supplier flow"));
    if (has(/\b(curso|cursos|course|courses|academy|academia|escuela online|bootcamp|training|formacion|clases|classes|lessons|masterclass|workshop|taller|coaching program|certificacion)\b/)) {
      scores.push(scoreFor("education-course-academy-pro", 112, "course/academy flow"));
    }
    if (has(/\b(digital|ebook|e-book|templates|plantillas|descarga|download|pdf|pack|membresia|membership|digital products|productos digitales)\b/)) scores.push(scoreFor("digital-products-store", 105, "digital product flow"));
    if (has(/\b(contratista|contractor|construccion|construction|limpieza|cleaning|plomeria|plumbing|electricista|electrician|hvac|aire acondicionado|mechanic|mecanico|landscaping|seguridad|security|servicio local|local service|reparacion|repair|presupuesto|cotizacion|quote|emergencia)\b/)) scores.push(scoreFor("home-services-premium", 108, "local service quote flow"));
    if (textSuggestsJewelryAccessoryStore(text) && !textSuggestsBroadMarketplace(text)) scores.push(scoreFor("fashion-drop-pro", 118, "focused jewelry/accessory boutique"));
    if (has(/\b(ropa|fashion|moda|boutique|streetwear|zapato|sneaker|apparel|clothing|drop|lookbook)\b/) && !textSuggestsBroadMarketplace(text)) scores.push(scoreFor("fashion-drop-pro", 104, "fashion/lookbook commerce"));
    if (has(/\b(lujo|luxury|high ticket|alta gama|exclusivo|joyeria|joyería|jewelry|jewellery|perfumes|fragancia|relojes|watches|cuero|arte|coleccionable|private appointment|cita privada|precio a consultar)\b/)) scores.push(scoreFor("luxury-high-ticket-pro", 110, "luxury/high-ticket showroom"));
    if (textSuggestsFocusedProductLine(text) || textSuggestsSingleProductShowcase(text) || textSuggestsPremiumProductPreference(text)) scores.push(scoreFor("premium-product-store", 108, "focused product line"));
    if (has(/\b(landing|landing page|funnel|embudo|captar clientes|captar leads|leads|campana|campaign|oferta|offer|book call|agenda una llamada|alta conversion)\b/)) scores.push(scoreFor("lead-funnel-pro", 95, "lead capture funnel"));
    if (has(/\b(empresa|company|corporate|corporativo|pagina web|website|agencia|agency|firma|business website|organizacion|organization)\b/)) scores.push(scoreFor("corporate-company-pro", 90, "company website"));
    return scores.filter((item) => item.score > 0 && templatePreviewMeta(item.templateId)).sort((a, b) => b.score - a.score);
  }
  function bestTemplateIdFromContext(value, payload = {}) {
    return templateIntentScorecard(value, payload)[0]?.templateId || "";
  }
  function isGenericCommerceIntent(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text || textSuggestsBroadMarketplace(text) || textSuggestsFocusedProductLine(text) || textSuggestsSingleProductShowcase(text)) return false;
    const generic = /^(sell products online|vender productos online|venta online|ventas online|online sales|online store|tienda online|tienda|store|shop|ecommerce|e-commerce|catalogo|catalog|marketplace|vender|comprar online)$/;
    return generic.test(text) || text.split(/\s+/).length <= 5 && /\b(vender|venta|online|store|shop|tienda|ecommerce|catalogo|catalog|marketplace)\b/.test(text);
  }
  function inferTemplateIdFromText(value) {
    const text = normalizeTemplateIntentText(value);
    if (!text) return "";
    const scoredTemplateId = bestTemplateIdFromContext(text);
    if (scoredTemplateId) return scoredTemplateId;
    if (textSuggestsMultiVendorMarketplace(text)) return "mega-marketplace";
    if (textSuggestsBeautyCommerceStore(text)) return textSuggestsHandmadeCraftGoods(text) ? "premium-product-store" : "mega-retail-store";
    if (textSuggestsMegaRetailStore(text) || textSuggestsBroadMarketplace(text)) return "mega-retail-store";
    if (textSuggestsFocusedProductLine(text)) return "premium-product-store";
    if (/tipo ebay|como ebay|clasificados|listados|vendedores|usado|seller|listing/.test(text)) return "listing-marketplace-pro";
    if (/restaurante|restaurant|menu|comida|food|cafe|cafeteria|delivery de comida|ordenar comida|pedir comida|pedido de comida/.test(text)) return "restaurant-food-business";
    if (/barber|barberia|salon|spa|cita|booking|reserva|appointment/.test(text)) return "booking-appointment-pro";
    if (/abogado|legal|lawyer|contador|tax|impuestos|consultoria|consulting|seguros|asesor/.test(text)) return "legal-professional-services-pro";
    if (/clinica|clinic|med spa|wellness|dental|doctor|estetica|salud|therapy|skincare/.test(text)) return "medical-wellness-clinic-pro";
    if (/saas|software|enterprise|automatizacion|platform|plataforma|dashboard|crm|erp|integraciones|api/.test(text)) return "b2b-saas-enterprise-pro";
    if (/industrial|manufactur|fabrica|maquinaria|repuestos|proveedor industrial|suministros/.test(text)) return "manufacturing-industrial-supplier-pro";
    if (/curso|course|academy|academia|bootcamp|training|clases|masterclass|workshop/.test(text)) {
      return "education-course-academy-pro";
    }
    if (/digital|ebook|templates|plantillas|descarga|download|membresia|membership/.test(text)) return "digital-products-store";
    if (/servicio|service|contractor|limpieza|roofing|repair|reparacion|cotizacion|quote/.test(text)) return "local-services-pro-plus";
    if (textSuggestsJewelryAccessoryStore(text) || /ropa|fashion|moda|boutique|streetwear|zapato|sneaker|accesorio/.test(text)) return "fashion-drop-pro";
    if (/lujo|luxury|alta gama|exclusivo|joyeria|relojes|arte|coleccionable/.test(text)) return "luxury-high-ticket-pro";
    if (textSuggestsPremiumProductPreference(text)) return "premium-product-store";
    if (/empresa|company|corporate|corporativo|pagina web|website|agencia|firma/.test(text)) return "corporate-company-pro";
    return "";
  }
  function syncTemplateSelectionFromGuidedContext(extra = "") {
    const text = guidedTemplateContextText(extra);
    const inferredTemplateId = inferTemplateIdFromText(text);
    if (!inferredTemplateId || builderState.forcedTemplateSelection?.templateId === inferredTemplateId) return;
    const shouldOverride = !builderState.forcedTemplateSelection?.templateId || textSuggestsBroadMarketplace(text) || textSuggestsFocusedProductLine(text) || textSuggestsSingleProductShowcase(text) || builderState.forcedTemplateSelection.intent === "default_minimal" || builderState.forcedTemplateSelection.intent === "provisional_needs_catalog_context" || builderState.forcedTemplateSelection.intent === "guided_context_template";
    if (!shouldOverride) return;
    const meta = templatePreviewMeta(inferredTemplateId);
    builderState.forcedTemplateSelection = {
      templateId: inferredTemplateId,
      template: builderState.forcedTemplateSelection?.templateId === inferredTemplateId ? builderState.forcedTemplateSelection.template : null,
      catalogType: meta?.catalogType || "",
      intent: "guided_context_template",
      reason: "Updated from the full guided intake context"
    };
    builderState.guidedState.sitePlan = null;
    builderState.guidedState.sitePlanApproved = false;
  }
  function publicProductFocus(items = [], fallback = "") {
    const cleanItems = arrayValue2(items).map((item) => String(item || "").trim()).filter(Boolean).slice(0, 2);
    if (cleanItems.length) {
      const joiner = builderState.selectedLanguage === "en" ? " and " : builderState.selectedLanguage === "fr" ? " et " : " y ";
      return cleanItems.join(joiner);
    }
    return fallback || langText({
      en: "the right products and services",
      es: "los productos y servicios correctos",
      fr: "les bons produits et services",
      pt: "os produtos e servicos certos"
    });
  }
  function applyI18n() {
    document.documentElement.lang = builderState.selectedLanguage;
    document.title = t("pageTitle");
    document.querySelectorAll("[data-i18n]").forEach((item) => {
      item.textContent = t(item.dataset.i18n);
    });
    document.querySelectorAll("[data-i18n-placeholder]").forEach((item) => {
      item.placeholder = t(item.dataset.i18nPlaceholder);
    });
    document.querySelectorAll("[data-i18n-label]").forEach((label) => {
      setLabelText(label, t(label.dataset.i18nLabel));
    });
    initVoiceInput();
    updateAssistantAudioToggle();
    updateBuilderAvatarLabels();
    renderGuidedCoachCard();
  }
  function updateBuilderAvatarLabels() {
    if (!builderState.builderAvatarAssistant) return;
    builderState.builderAvatarAssistant.labels = {
      idle: t("assistantSubtitle"),
      listening: langText({ en: "I'm listening to your idea.", es: "Estoy escuchando tu idea.", fr: "J'\xE9coute votre id\xE9e.", pt: "Estou ouvindo sua ideia." }),
      thinking: t("thinking"),
      speaking: langText({ en: "Guiding you step by step.", es: "Te guio paso a paso.", fr: "Je vous guide \xE9tape par \xE9tape.", pt: "Vou guiar voc\xEA passo a passo." }),
      happy: langText({ en: "Ready to help.", es: "Listo para ayudarte.", fr: "Pr\xEAte \xE0 vous aider.", pt: "Pronta para ajudar." }),
      confused: langText({ en: "I need a little more context.", es: "Necesito un poco mas de contexto.", fr: "J'ai besoin d'un peu plus de contexte.", pt: "Preciso de um pouco mais de contexto." }),
      success: langText({ en: "Your draft is ready.", es: "Tu borrador esta listo.", fr: "Votre brouillon est pr\xEAt.", pt: "Seu rascunho est\xE1 pronto." })
    };
    builderState.builderAvatarAssistant.setState(builderAvatarManager?.getState() || "idle");
  }
  function setLabelText(label, value) {
    const textNodes = Array.from(label.childNodes).filter((node) => node.nodeType === Node.TEXT_NODE);
    let target = textNodes.find((node) => node.textContent.trim().length > 0);
    if (!target) {
      target = document.createTextNode("");
      label.insertBefore(target, label.firstChild);
    }
    textNodes.forEach((node) => {
      if (node !== target && node.textContent.trim().length > 0) {
        node.textContent = "";
      }
    });
    target.textContent = `${value} `;
  }
  function setAssistantState(state) {
    builderState.assistantState = normalizeAssistantState(state);
    document.body.dataset.assistantState = builderState.assistantState;
    builderAvatarManager?.setState(avatarStateFromAssistantState(builderState.assistantState), { source: "guided-assistant" });
    document.querySelectorAll(".assistant-avatar").forEach((avatar) => {
      avatar.dataset.state = builderState.assistantState;
      avatar.src = ASSISTANT_AVATARS[builderState.assistantState] || ASSISTANT_AVATAR_FALLBACK;
      avatar.onerror = () => {
        avatar.onerror = null;
        avatar.src = ASSISTANT_AVATAR_FALLBACK;
      };
    });
  }
  function languageToSpeechLocale(language) {
    return {
      en: "en-US",
      es: "es-US",
      fr: "fr-FR",
      pt: "pt-BR"
    }[language] || "en-US";
  }
  function compactEmailLabel(email) {
    const text = String(email || "").trim();
    if (text.length <= 24) return text;
    const [name, domain] = text.split("@");
    if (!domain) return `${text.slice(0, 21)}...`;
    return `${name.slice(0, 10)}...@${domain}`;
  }
  function storedClientAccessToken() {
    return localStorage.getItem("lumaClientAccessToken") || sessionStorage.getItem("lumaClientAccessToken") || "";
  }
  function formatProjectUpdatedAt(value) {
    if (!value) return langText({ en: "No recent edits", es: "Sin ediciones recientes", fr: "Aucune \xE9dition r\xE9cente", pt: "Sem edi\xE7\xF5es recentes" });
    const date = new Date(value);
    if (Number.isNaN(date.getTime())) return String(value);
    try {
      return new Intl.DateTimeFormat(builderState.selectedLanguage || "en", {
        month: "short",
        day: "numeric",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit"
      }).format(date);
    } catch {
      return date.toLocaleString();
    }
  }
  function resetGuidedStateForNewAccount(options = {}) {
    const preserveAuth = Boolean(options.preserveAuth);
    builderState.guidedState = createEmptyGuidedState(builderState.selectedLanguage);
    builderState.currentSchema = null;
    builderState.currentRequestId = null;
    builderState.currentSiteId = null;
    builderState.currentBusinessId = null;
    builderState.currentGenerationId = null;
    builderState.currentCatalogItems = [];
    builderState.selectedPageKey = "home";
    builderState.selectedVariantId = "";
    builderState.forcedTemplateSelection = null;
    builderState.clientIntakeSession = null;
    builderState.restoredGuidedDraftInfo = null;
    builderState.restoredDraftNoticeShown = false;
    builderState.restoredDraftNoticeCard?.remove();
    builderState.restoredDraftNoticeCard = null;
    removeGuidedBuildStatusCard();
    builderState.guidedStep = "websiteIntent";
    builderState.lastAskedGuidedField = "";
    builderState.hasBackendIntakeSignal = false;
    builderState.backendReadyToGenerate = false;
    builderState.backendMissingFields = [];
    try {
      localStorage.removeItem(GUIDED_DRAFT_STORAGE_KEY);
      localStorage.removeItem(GENERATED_SITE_STORAGE_KEY);
      localStorage.removeItem(CLIENT_INTAKE_SESSION_STORAGE_KEY);
      localStorage.removeItem("lumaPendingGeneratedSite");
      if (!preserveAuth) {
        localStorage.removeItem("lumaPendingClientEmail");
        localStorage.removeItem("lumaClientAccessToken");
        localStorage.removeItem("lumaClientRefreshToken");
        sessionStorage.removeItem("lumaClientAccessToken");
        sessionStorage.removeItem("lumaClientRefreshToken");
      }
    } catch {
    }
    if (typeof siteTitle2 !== "undefined" && siteTitle2) siteTitle2.textContent = "";
    applyGuidedStateToForm();
    renderGuidedSummary();
    resetAssistantConversation();
  }
  function isNewProjectBriefMessage(message) {
    const text = String(message || "").trim();
    if (!isRichIntakeMessage(text)) return false;
    return /\b(quiero|necesito|vamos a|deseo|crear|hacer|build|create|make)\b[\s\S]{0,80}\b(p[aá]gina|website|site|tienda|store|marketplace|cat[aá]logo|catalog)\b/i.test(text);
  }
  function shouldResetRestoredWorkspaceForMessage(message) {
    if (!isPublicClientSetup || builderState.currentSchema) return false;
    const restored = Boolean(builderState.restoredGuidedDraftInfo || builderState.clientIntakeSession?.restored || readClientIntakeSession()?.restored);
    if (!restored || !isNewProjectBriefMessage(message)) return false;
    const incomingName = extractBusinessName(message).toLowerCase();
    const existingName = String(builderState.guidedState.businessName || "").trim().toLowerCase();
    if (incomingName && existingName && incomingName !== existingName) return true;
    return Boolean(builderState.guidedState.businessDescription || builderState.guidedState.websiteIntent || builderState.guidedState.selectedTemplateId || builderState.guidedState.catalogType);
  }
  function applyPromptFromQuery() {
    const params = new URLSearchParams(window.location.search);
    const prompt = params.get("prompt") || params.get("description") || "";
    if (!prompt.trim()) return;
    builderState.guidedState.websiteIntent = prompt.trim();
    builderState.guidedState.businessDescription = prompt.trim();
    builderState.guidedState.preferredTone = builderState.guidedState.preferredTone || extractStyleHint(prompt);
    builderState.guidedState.industry = builderState.guidedState.industry || inferIndustryFromPrompt(prompt);
    if (params.get("templateId")) {
      builderState.forcedTemplateSelection = {
        templateId: params.get("templateId"),
        catalogType: params.get("catalogType") || "",
        intent: params.get("intent") || "manual_template",
        reason: "Template selected from customer template gallery"
      };
    }
    builderState.guidedStep = "businessName";
    applyGuidedStateToForm();
  }
  function extractStyleHint(prompt) {
    const text = String(prompt || "").toLowerCase();
    const styles = ["cyberpunk", "futurista", "neon", "ne\xF3n", "premium", "minimal", "lujo", "elegante", "moderno", "shopify", "amazon", "ebay"];
    return styles.filter((style) => text.includes(style)).join(", ");
  }
  function inferIndustryFromPrompt(prompt) {
    const text = String(prompt || "").toLowerCase();
    if (/restaurante|restaurant|menu|menú|comida|food/.test(text)) return langText({ en: "Restaurant / food", es: "Restaurante / comida", fr: "Restaurant / alimentation", pt: "Restaurante / comida" });
    if (textSuggestsProfessionalService(text)) return langText({ en: "Professional services", es: "Servicios profesionales", fr: "Services professionnels", pt: "Servi\xE7os profissionais" });
    if (textSuggestsBroadMarketplace(text) || /tienda|store|shop|producto|productos|vender|ecommerce|amazon|ebay|cyberpunk|gamer/.test(text)) return langText({ en: "Online store / ecommerce", es: "Tienda online / ecommerce", fr: "Boutique en ligne / ecommerce", pt: "Loja online / ecommerce" });
    if (/barber|barberia|barbería|salon|spa|\b(cita|citas|reserva|reservas)\b/.test(text)) return langText({ en: "Appointment services", es: "Servicios con citas", fr: "Services avec rendez-vous", pt: "Servi\xE7os com agendamento" });
    if (/curso|course|ebook|digital|software|membres/.test(text)) return langText({ en: "Digital products", es: "Productos digitales", fr: "Produits num\xE9riques", pt: "Produtos digitais" });
    if (/contractor|construction|remodel|pintura|roofing|flooring/.test(text)) return langText({ en: "Contractor", es: "Construccion / contractor", fr: "Construction / entrepreneur", pt: "Constru\xE7\xE3o / empreiteiro" });
    return "";
  }
  function resetAssistantConversation() {
    guidedChat.innerHTML = "";
    builderState.guidedHistory = [];
    setAssistantState("happy");
    renderGuidedCoachCard();
    let askedPrompt = false;
    if (builderState.restoredGuidedDraftInfo) {
      renderRestoredDraftNotice({ force: true });
    }
    if (builderState.guidedStep === "websiteIntent") {
      appendChatMessage(
        "assistant",
        `${guidedQuestion(builderState.guidedStep)}

${langText({
          en: "You can answer naturally, for example: a large catalog store, a restaurant menu, a booking site for a barbershop, or a cyberpunk online store.",
          es: "Puedes responder natural, por ejemplo: una tienda con catalogo grande, un menu de restaurante, una barberia con citas, o una tienda cyberpunk.",
          fr: "Vous pouvez r\xE9pondre naturellement, par exemple : grand catalogue, menu de restaurant, site de reservation pour salon, ou boutique cyberpunk.",
          pt: "Voc\xEA pode responder naturalmente, por exemplo: loja com catalogo grande, menu de restaurante, site de reservas para barbearia ou loja cyberpunk."
        })}`,
        "speaking"
      );
      askedPrompt = true;
    } else if (builderState.forcedTemplateSelection?.templateId && builderState.guidedState.websiteIntent) {
      appendTemplateDetectionMessage({
        templateId: builderState.forcedTemplateSelection.templateId,
        template: builderState.forcedTemplateSelection.template || null,
        intent: builderState.forcedTemplateSelection.intent || "manual_template",
        catalogType: builderState.forcedTemplateSelection.catalogType || "",
        reason: builderState.forcedTemplateSelection.reason || "Template selected from landing page"
      });
    }
    if (!askedPrompt && builderState.guidedStep && builderState.guidedStep !== "review") {
      appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), "speaking");
    }
  }
  function switchToManualForm() {
    applyGuidedStateToForm();
    document.body.classList.add("manual-form-open");
    document.body.classList.remove("review-details-open", "final-review-mode");
    form2.classList.add("active");
    guidedPanel2.classList.remove("active");
    if ("speechSynthesis" in window) window.speechSynthesis.cancel();
    setAssistantState("neutral");
  }
  function saveGuidedDraft() {
    if (!isPublicClientSetup) return;
    try {
      localStorage.setItem(
        GUIDED_DRAFT_STORAGE_KEY,
        JSON.stringify({
          guidedState: guidedStateForApi(),
          guidedStep: builderState.guidedStep,
          selectedLanguage: builderState.selectedLanguage,
          completionPercent: guidedCompletionPercent(),
          missingSteps: missingGuidedSteps(),
          savedAt: (/* @__PURE__ */ new Date()).toISOString()
        })
      );
    } catch {
    }
    syncClientIntakeSession({ reason: "autosave" });
  }
  function restoreGuidedDraft() {
    if (!isPublicClientSetup) return;
    try {
      const raw = localStorage.getItem(GUIDED_DRAFT_STORAGE_KEY);
      if (!raw) return;
      const draft = JSON.parse(raw);
      if (draft.selectedLanguage) setSelectedLanguage(draft.selectedLanguage);
      if (draft.guidedState) {
        builderState.guidedState = {
          ...builderState.guidedState,
          ...draft.guidedState,
          servicesProducts: arrayValue2(draft.guidedState.servicesProducts),
          preferredColors: arrayValue2(draft.guidedState.preferredColors),
          photoUrls: arrayValue2(draft.guidedState.photoUrls),
          videoUrls: arrayValue2(draft.guidedState.videoUrls),
          logoPalette: arrayValue2(draft.guidedState.logoPalette),
          brand: draft.guidedState.brand || null,
          contactInfo: draft.guidedState.contactInfo || {}
        };
      }
      const missing = missingGuidedSteps();
      const savedStep = draft.guidedStep || builderState.guidedStep;
      builderState.guidedStep = savedStep === "review" && !builderState.hasBackendIntakeSignal && missing.length ? missing[0] : normalizeNextGuidedStep(savedStep);
      builderState.restoredGuidedDraftInfo = {
        savedAt: draft.savedAt || "",
        completionPercent: guidedCompletionPercent(),
        missing
      };
      applyGuidedStateToForm();
    } catch {
      localStorage.removeItem(GUIDED_DRAFT_STORAGE_KEY);
    }
  }
  function renderRestoredDraftNotice({ force = false } = {}) {
    if (!builderState.restoredGuidedDraftInfo || !guidedChatCard || !guidedChat) return;
    if (builderState.restoredDraftNoticeShown && !force) return;
    builderState.restoredDraftNoticeShown = true;
    const missingLabels = arrayValue2(builderState.restoredGuidedDraftInfo.missing).slice(0, 3).map((step) => t(step)).join(", ");
    const savedAt = formatDraftSavedAt(builderState.restoredGuidedDraftInfo.savedAt);
    if (!builderState.restoredDraftNoticeCard) {
      builderState.restoredDraftNoticeCard = document.createElement("section");
      builderState.restoredDraftNoticeCard.className = "draft-restore-card";
    }
    builderState.restoredDraftNoticeCard.innerHTML = `
    <div>
      <strong>${escapeHtml(langText({
      en: "Saved draft found",
      es: "Borrador guardado encontrado",
      fr: "Brouillon enregistre trouve",
      pt: "Rascunho salvo encontrado"
    }))}</strong>
      <span>${escapeHtml(langText({
      en: `This workspace is ${builderState.restoredGuidedDraftInfo.completionPercent}% complete${savedAt ? ` from ${savedAt}` : ""}${missingLabels ? `. Missing: ${missingLabels}.` : "."}`,
      es: `Este espacio va en ${builderState.restoredGuidedDraftInfo.completionPercent}%${savedAt ? ` desde ${savedAt}` : ""}${missingLabels ? `. Falta: ${missingLabels}.` : "."}`,
      fr: `Cet espace est complete a ${builderState.restoredGuidedDraftInfo.completionPercent}%${savedAt ? ` depuis ${savedAt}` : ""}${missingLabels ? `. Il manque: ${missingLabels}.` : "."}`,
      pt: `Este espaco esta ${builderState.restoredGuidedDraftInfo.completionPercent}% completo${savedAt ? ` desde ${savedAt}` : ""}${missingLabels ? `. Falta: ${missingLabels}.` : "."}`
    }))}</span>
    </div>
    <div class="draft-restore-actions">
      <button type="button" data-draft-continue>${escapeHtml(langText({ en: "Continue", es: "Continuar", fr: "Continuer", pt: "Continuar" }))}</button>
      <button type="button" data-draft-new>${escapeHtml(langText({ en: "Start new", es: "Nuevo", fr: "Nouveau", pt: "Novo" }))}</button>
    </div>
  `;
    builderState.restoredDraftNoticeCard.querySelector("[data-draft-continue]")?.addEventListener("click", () => {
      builderState.restoredDraftNoticeCard?.remove();
      builderState.restoredDraftNoticeCard = null;
    });
    builderState.restoredDraftNoticeCard.querySelector("[data-draft-new]")?.addEventListener("click", () => {
      builderState.restoredDraftNoticeCard?.remove();
      builderState.restoredDraftNoticeCard = null;
      startNewClientProject();
    });
    if (builderState.restoredDraftNoticeCard.parentElement !== guidedChatCard) {
      guidedChatCard.insertBefore(builderState.restoredDraftNoticeCard, guidedChat);
    }
  }
  function guidedBuildPhases() {
    return [
      {
        key: "save",
        label: langText({ en: "Saving your details", es: "Guardando tus datos", fr: "Enregistrement de vos infos", pt: "Salvando seus dados" }),
        body: langText({
          en: "Keeping your answers safe in your workspace.",
          es: "Guardamos tus respuestas de forma segura en tu espacio.",
          fr: "Vos reponses sont enregistrees en toute securite.",
          pt: "Guardamos suas respostas com seguranca no seu espaco."
        })
      },
      {
        key: "strategy",
        label: langText({ en: "Choosing your style", es: "Eligiendo tu estilo", fr: "Choix de votre style", pt: "Escolhendo seu estilo" }),
        body: langText({
          en: "Matching your business with the look and layout that fit it best.",
          es: "Buscando el diseno y la estructura que mejor le quedan a tu negocio.",
          fr: "Recherche du design et de la structure qui vous conviennent le mieux.",
          pt: "Buscando o design e a estrutura que mais combinam com seu negocio."
        })
      },
      {
        key: "generate",
        label: langText({ en: "Writing your content", es: "Escribiendo tu contenido", fr: "Redaction de votre contenu", pt: "Escrevendo seu conteudo" }),
        body: langText({
          en: "Writing your homepage, products and page text.",
          es: "Escribiendo tu pagina de inicio, productos y textos.",
          fr: "Redaction de votre page d'accueil, produits et textes.",
          pt: "Escrevendo sua pagina inicial, produtos e textos."
        })
      },
      {
        key: "render",
        label: langText({ en: "Building your preview", es: "Armando tu vista previa", fr: "Preparation de votre apercu", pt: "Montando sua previa" }),
        body: langText({
          en: "Putting together your editable preview so you can review it right away.",
          es: "Armando tu vista previa editable para que la revises enseguida.",
          fr: "Assemblage de votre apercu modifiable pour le revoir immediatement.",
          pt: "Montando sua previa editavel para voce revisar na hora."
        })
      }
    ];
  }
  function ensureGuidedBuildStatusCard() {
    if (!guidedChatCard || !guidedChat) return null;
    if (!builderState.guidedBuildStatusCard) {
      builderState.guidedBuildStatusCard = document.createElement("section");
      builderState.guidedBuildStatusCard.className = "guided-build-card";
      builderState.guidedBuildStatusCard.setAttribute("aria-live", "polite");
    }
    if (builderState.guidedBuildStatusCard.parentElement !== guidedChatCard) {
      guidedChatCard.appendChild(builderState.guidedBuildStatusCard);
    }
    return builderState.guidedBuildStatusCard;
  }
  function setGuidedBuildPhase(phase, detail = "") {
    if (!isPublicClientSetup) return;
    const card = ensureGuidedBuildStatusCard();
    if (!card) return;
    document.body.classList.add("lyra-build-mode");
    const phases = guidedBuildPhases();
    const activeIndex = phase === "ready" ? phases.length : Math.max(0, phases.findIndex((item) => item.key === phase));
    const activePhase = phases[Math.min(activeIndex, phases.length - 1)] || phases[0];
    const isError = phase === "error";
    const isReady = phase === "ready";
    card.classList.toggle("is-error", isError);
    card.classList.toggle("is-ready", isReady);
    card.innerHTML = `
    <div class="guided-build-head">
      <span>${escapeHtml(langText({ en: "Building your website", es: "Creando tu sitio", fr: "Creation de votre site", pt: "Criando seu site" }))}</span>
      <strong>${escapeHtml(isError ? langText({ en: "Generation stopped", es: "Generacion detenida", fr: "Generation arretee", pt: "Geracao interrompida" }) : isReady ? langText({ en: "Preview ready", es: "Preview listo", fr: "Apercu pret", pt: "Preview pronto" }) : activePhase.label)}</strong>
      <em>${escapeHtml(isReady ? "100%" : `${Math.min(95, Math.round((activeIndex + 1) / phases.length * 100))}%`)}</em>
    </div>
    <p>${escapeHtml(detail || (isError ? langText({ en: "Nothing was lost \u2014 your answers are saved. Let's try again.", es: "No se perdio nada, tus respuestas quedaron guardadas. Intentemos de nuevo.", fr: "Rien n'est perdu, vos reponses sont enregistrees. Reessayons.", pt: "Nada foi perdido, suas respostas ficaram salvas. Vamos tentar de novo." }) : isReady ? langText({ en: "The editable draft is ready to review.", es: "El borrador editable esta listo para revisar.", fr: "Le brouillon modifiable est pret a etre revise.", pt: "O rascunho editavel esta pronto para revisar." }) : activePhase.body))}</p>
    ${isError ? `<div class="guided-build-actions"><button type="button" data-guided-build-retry>${escapeHtml(langText({ en: "Try again", es: "Reintentar", fr: "Reessayer", pt: "Tentar novamente" }))}</button></div>` : `<div class="guided-build-steps">
      ${phases.map((item, index) => {
      const state = isReady || index < activeIndex ? "done" : index === activeIndex ? "active" : "pending";
      return `<span class="${state}">${escapeHtml(item.label)}</span>`;
    }).join("")}
    </div>`}
  `;
    card.querySelector("[data-guided-build-retry]")?.addEventListener("click", () => {
      window.handleGuidedGenerateButton?.();
    });
    requestAnimationFrame(() => {
      card.scrollIntoView({ block: "nearest", behavior: "smooth" });
    });
  }
  function removeGuidedBuildStatusCard() {
    document.body.classList.remove("lyra-build-mode");
    builderState.guidedBuildStatusCard?.remove();
    builderState.guidedBuildStatusCard = null;
  }
  function formatDraftSavedAt(value) {
    if (!value) return "";
    try {
      return new Intl.DateTimeFormat(builderState.selectedLanguage, {
        dateStyle: "short",
        timeStyle: "short"
      }).format(new Date(value));
    } catch {
      return "";
    }
  }
  function saveGeneratedSite(result) {
    if (!isPublicClientSetup) return;
    try {
      localStorage.setItem(
        GENERATED_SITE_STORAGE_KEY,
        JSON.stringify({
          result,
          selectedPageKey: builderState.selectedPageKey,
          selectedVariantId: builderState.selectedVariantId,
          savedAt: (/* @__PURE__ */ new Date()).toISOString()
        })
      );
    } catch {
    }
  }
  function restoreGeneratedSite() {
    if (!isPublicClientSetup) return;
    try {
      const raw = localStorage.getItem(GENERATED_SITE_STORAGE_KEY);
      if (!raw) return;
      const saved = JSON.parse(raw);
      const result = saved.result || {};
      if (!result.schema) return;
      builderState.currentSchema = prepareWebsiteConfig(result.schema, { brand: result.schema.brand || builderState.guidedState.brand || {} }, null);
      builderState.currentSiteId = result.generatedSiteId || result.projectId || result.site_id || null;
      builderState.currentBusinessId = result.business_id || null;
      builderState.currentGenerationId = result.generation_id || null;
      builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
      builderState.selectedPageKey = saved.selectedPageKey || builderState.currentSchema.pages?.[0]?.page_key || "home";
      builderState.selectedVariantId = saved.selectedVariantId || builderState.currentSchema.design_variants?.[0]?.id || "";
      siteTitle2.textContent = builderState.currentSchema.business?.name || "Generated site";
      storageStatus2.textContent = storageLabel(result.storage_status, result.used_dev_mock);
    } catch {
      localStorage.removeItem(GENERATED_SITE_STORAGE_KEY);
    }
  }
  function continuePendingStudioAction() {
    const action = localStorage.getItem("lumaPendingAuthAction") || "";
    localStorage.removeItem("lumaPendingAuthAction");
    if (action === "save") {
      saveCurrentSchema();
    } else if (action === "review") {
      submitGeneratedDraftForReview();
    } else if (action === "publish") {
      storageStatus2.textContent = langText({
        en: "Account connected. Review once more, then publish.",
        es: "Cuenta conectada. Revisa una vez m\xE1s y luego publica.",
        fr: "Compte connect\xE9. V\xE9rifiez encore une fois, puis publiez.",
        pt: "Conta conectada. Revise mais uma vez e publique."
      });
    }
  }
  async function handleGuidedSendAction(event) {
    event?.preventDefault?.();
    event?.stopPropagation?.();
    const now = Date.now();
    if (builderState.guidedSendLocked || now - builderState.guidedLastSendAt < 350) return;
    builderState.guidedLastSendAt = now;
    builderState.guidedSendLocked = true;
    try {
      await sendGuidedReply();
    } finally {
      builderState.guidedSendLocked = false;
    }
  }
  async function applyLumaAgentDecision(result = {}) {
    const selectedTemplateId = result.selectedTemplateId || result.selected_template_id || "";
    const localPlan = refreshAiStudioPlanFromContext();
    const effectiveTemplateId = shouldPreferLocalStudioTemplate(selectedTemplateId, localPlan) ? localPlan.recommendedTemplateId : selectedTemplateId;
    if (effectiveTemplateId && window.TemplateRouter?.getTemplateById) {
      let template = null;
      try {
        template = await window.TemplateRouter.getTemplateById(effectiveTemplateId);
      } catch (error) {
        console.warn("LYRA selected template could not be loaded.", error);
      }
      builderState.forcedTemplateSelection = {
        templateId: effectiveTemplateId,
        template,
        intent: shouldPreferLocalStudioTemplate(selectedTemplateId, localPlan) ? "ai_studio_plan_override" : result.intent || "luma_agent_template",
        catalogType: shouldPreferLocalStudioTemplate(selectedTemplateId, localPlan) ? localPlan.recommendedCatalogType : result.catalogType || result.catalog_type || template?.catalogModel?.catalogType || "",
        reason: shouldPreferLocalStudioTemplate(selectedTemplateId, localPlan) ? localPlan.reasoningSummary : result.selectedTemplateReason || result.selected_template_reason || "Selected by LYRA from the conversation"
      };
    } else if (!selectedTemplateId && result.intent === "collect_info") {
      builderState.forcedTemplateSelection = {
        templateId: localPlan.recommendedTemplateId || "",
        template: null,
        intent: localPlan.recommendedTemplateId ? "ai_studio_plan" : "luma_agent_collecting_context",
        catalogType: localPlan.recommendedCatalogType || "",
        reason: localPlan.recommendedTemplateId ? localPlan.reasoningSummary : "LYRA is still collecting enough context before choosing a structure"
      };
    }
    if (result.designStrategy) {
      builderState.guidedState.designStrategy = result.designStrategy;
    }
    if (result.sitePlan) {
      builderState.guidedState.sitePlan = {
        ...result.sitePlan,
        aiStudioPlan: localPlan
      };
      builderState.guidedState.sitePlanApproved = false;
    } else if (!selectedTemplateId) {
      builderState.guidedState.sitePlan = localPlan.recommendedTemplateId ? buildSitePlan(builderState.forcedTemplateSelection) : null;
      if (builderState.guidedState.sitePlan) builderState.guidedState.sitePlan.aiStudioPlan = localPlan;
      builderState.guidedState.sitePlanApproved = false;
    }
  }
  function shouldPreferLocalStudioTemplate(serverTemplateId, localPlan = {}) {
    if (!localPlan?.recommendedTemplateId) return false;
    if (!serverTemplateId) return true;
    if (serverTemplateId === localPlan.recommendedTemplateId) return false;
    const planSignal = `${localPlan.websiteType || ""} ${localPlan.recommendedCatalogType || ""} ${localPlan.reasoningSummary || ""}`;
    return /mega_marketplace|marketplace|broad catalog|muchos productos|de todo|varied|catalog|booking|restaurant|service|quote|premium_showcase/i.test(planSignal);
  }
  function rankedFallbackChoices(selectedTemplateId = "") {
    const selected = templatePreviewMeta(selectedTemplateId);
    const selectedCatalog = selected?.catalogType || "";
    const commerceHeavy = /marketplace|dense|listing|retail|single_vendor/.test(selectedCatalog);
    const listingHeavy = /real_estate|listing/.test(selectedCatalog);
    const restaurantHeavy = /restaurant|menu|food/.test(selectedCatalog);
    const digitalHeavy = /digital|pricing|software|course/.test(selectedCatalog);
    const serviceHeavy = /service|booking/.test(selectedCatalog);
    const ordered = commerceHeavy ? listingHeavy ? ["real-estate-listings-pro", "listing-marketplace-pro", "mega-marketplace", "mega-retail-store"] : ["mega-retail-store", "mega-marketplace", "listing-marketplace-pro", "fashion-drop-pro"] : restaurantHeavy ? ["restaurant-food-business", "lead-funnel-pro", "home-services-premium", "booking-appointment-pro"] : digitalHeavy ? ["digital-products-store", "lead-funnel-pro", "premium-product-store", "corporate-company-pro"] : serviceHeavy ? ["home-services-premium", "lead-funnel-pro", "local-services-pro-plus", "booking-appointment-pro"] : ["lead-funnel-pro", "corporate-company-pro", "premium-product-store", "fashion-drop-pro"];
    return ordered.map((templateId) => templatePreviewMeta(templateId)).filter(Boolean);
  }
  function templatePreviewMeta(templateId) {
    const id = normalizeTemplateId(templateId);
    return withTemplateExecutionStatus(TEMPLATE_PREVIEW_CHOICES.find((choice) => normalizeTemplateId(choice.templateId) === id) || null);
  }
  function localizedTemplateName(choice) {
    return choice?.names?.[builderState.selectedLanguage] || choice?.name || choice?.template?.clientSelectionCard?.title || choice?.template?.name || choice?.templateId || "";
  }
  function localizedTemplateDescription(choice) {
    return choice?.descriptions?.[builderState.selectedLanguage] || choice?.description || choice?.template?.clientSelectionCard?.difference || choice?.template?.visualDifference || "";
  }
  function localizedTemplateBadgeSet(map, language = builderState.selectedLanguage) {
    const value = map[language] || map.en || [];
    const badges = Array.isArray(value) ? value : [value];
    return badges.map((badge) => publicAssistantCopy(badge)).filter(Boolean);
  }
  function templateCardBadges(choice) {
    const catalogType = String(choice?.catalogType || "").toLowerCase();
    if (/dense_marketplace/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["E-commerce", "Deals", "Search-first"],
      es: ["E-commerce", "Ofertas", "Busqueda"],
      fr: ["E-commerce", "Offres", "Recherche"],
      pt: ["E-commerce", "Ofertas", "Busca"]
    });
    if (/listing/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Marketplace", "Listings", "Offers"],
      es: ["Marketplace", "Listados", "Ofertas"],
      fr: ["Marketplace", "Annonces", "Offres"],
      pt: ["Marketplace", "Anuncios", "Ofertas"]
    });
    if (/lookbook|collection/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Fashion", "Lookbook", "Drops"],
      es: ["Moda", "Lookbook", "Drops"],
      fr: ["Mode", "Lookbook", "Drops"],
      pt: ["Moda", "Lookbook", "Drops"]
    });
    if (/premium|luxury/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Premium", "Showcase", "Editorial"],
      es: ["Premium", "Showcase", "Editorial"],
      fr: ["Premium", "Showcase", "Editorial"],
      pt: ["Premium", "Showcase", "Editorial"]
    });
    if (/industrial|supplier/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["B2B", "RFQ", "Materials"],
      es: ["B2B", "Cotizacion", "Materiales"],
      fr: ["B2B", "Devis", "Materiaux"],
      pt: ["B2B", "Orcamento", "Materiais"]
    });
    if (/b2b|solution/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["B2B", "SaaS", "Systems"],
      es: ["B2B", "SaaS", "Sistemas"],
      fr: ["B2B", "SaaS", "Systemes"],
      pt: ["B2B", "SaaS", "Sistemas"]
    });
    if (/medical|wellness/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Clinic", "Booking", "Trust"],
      es: ["Clinica", "Reservas", "Confianza"],
      fr: ["Clinique", "Reservations", "Confiance"],
      pt: ["Clinica", "Agendas", "Confianca"]
    });
    if (/legal|professional|company/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Services", "Authority", "Leads"],
      es: ["Servicios", "Autoridad", "Leads"],
      fr: ["Services", "Autorite", "Leads"],
      pt: ["Servicos", "Autoridade", "Leads"]
    });
    if (/restaurant|menu/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Menu", "Orders", "Local"],
      es: ["Menu", "Pedidos", "Local"],
      fr: ["Menu", "Commandes", "Local"],
      pt: ["Menu", "Pedidos", "Local"]
    });
    if (/booking/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Booking", "Services", "Calendar"],
      es: ["Reservas", "Servicios", "Calendario"],
      fr: ["Reservations", "Services", "Calendrier"],
      pt: ["Agendas", "Servicos", "Calendario"]
    });
    if (/digital/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Digital", "Bundles", "Instant access"],
      es: ["Digital", "Bundles", "Acceso inmediato"],
      fr: ["Digital", "Bundles", "Acces immediat"],
      pt: ["Digital", "Bundles", "Acesso imediato"]
    });
    if (/service|quote/.test(catalogType)) return localizedTemplateBadgeSet({
      en: ["Services", "Quote", "Local"],
      es: ["Servicios", "Cotizacion", "Local"],
      fr: ["Services", "Devis", "Local"],
      pt: ["Servicos", "Orcamento", "Local"]
    });
    return localizedTemplateBadgeSet({
      en: ["Website", "Editable", "AI-ready"],
      es: ["Web", "Editable", "IA lista"],
      fr: ["Site", "Modifiable", "IA prete"],
      pt: ["Site", "Editavel", "IA pronta"]
    });
  }
  var TEMPLATE_PREVIEW_PALETTES = Object.freeze({
    "premium-product-store": { paper: "#f7f6ff", ink: "#10101a", accent: "#6d5dfc" },
    "luxury-high-ticket-pro": { paper: "#11100f", ink: "#f7efe2", accent: "#d6a84f" },
    "education-course-academy-pro": { paper: "#eef5ff", ink: "#101d33", accent: "#3275e7" },
    "medical-wellness-clinic-pro": { paper: "#e8f4f0", ink: "#102624", accent: "#006b63" },
    "legal-professional-services-pro": { paper: "#eef1f6", ink: "#131b2b", accent: "#1f3a63" },
    "b2b-saas-enterprise-pro": { paper: "#edf7ff", ink: "#101d33", accent: "#0f7cc7" },
    "manufacturing-industrial-supplier-pro": { paper: "#f4f1e9", ink: "#1d2528", accent: "#ef6b32" },
    "mega-retail-store": { paper: "#eef2ff", ink: "#101a33", accent: "#2563eb" },
    "mega-marketplace": { paper: "#f4eee7", ink: "#261c17", accent: "#d8643d" },
    "listing-marketplace-pro": { paper: "#f0f7f5", ink: "#12231f", accent: "#0f8c78" },
    "fashion-drop-pro": { paper: "#f4f1e9", ink: "#1d1616", accent: "#c9262f" },
    "corporate-company-pro": { paper: "#f2f6fb", ink: "#14213a", accent: "#335c81" },
    "lead-funnel-pro": { paper: "#eef8f0", ink: "#16231c", accent: "#1f9d55" },
    "restaurant-food-business": { paper: "#fff7e8", ink: "#2c2116", accent: "#ec8c37" },
    "digital-products-store": { paper: "#f5f0ff", ink: "#211a33", accent: "#8b5cf6" },
    "real-estate-listings-pro": { paper: "#eff6f7", ink: "#13252a", accent: "#147c8c" },
    "home-services-premium": { paper: "#f0f8f3", ink: "#16231c", accent: "#16803f" },
    "local-services-pro-plus": { paper: "#fff7ed", ink: "#2a1c12", accent: "#e46a2a" },
    "booking-appointment-pro": { paper: "#faf5fc", ink: "#2b2134", accent: "#a579db" }
  });
  function templateAccentPalette(catalogType, templateId = "") {
    const override = TEMPLATE_PREVIEW_PALETTES[normalizeTemplateId(templateId)];
    if (override) return override;
    const type = String(catalogType || "").toLowerCase();
    if (/premium|luxury/.test(type)) return { paper: "#f7f6ff", ink: "#10101a", accent: "#6d5dfc" };
    if (/dense_marketplace|listing/.test(type)) return { paper: "#f4eee7", ink: "#261c17", accent: "#d8643d" };
    if (/single_vendor_dense/.test(type)) return { paper: "#eef2ff", ink: "#101a33", accent: "#2563eb" };
    if (/lookbook|collection/.test(type)) return { paper: "#f4f1e9", ink: "#1d1616", accent: "#c9262f" };
    if (/education/.test(type)) return { paper: "#eef5ff", ink: "#101d33", accent: "#3275e7" };
    if (/medical|wellness/.test(type)) return { paper: "#e8f4f0", ink: "#102624", accent: "#006b63" };
    if (/legal|professional|company/.test(type)) return { paper: "#eef1f6", ink: "#131b2b", accent: "#1f3a63" };
    if (/industrial|supplier/.test(type)) return { paper: "#f4f1e9", ink: "#1d2528", accent: "#ef6b32" };
    if (/b2b|solution/.test(type)) return { paper: "#eef5ff", ink: "#101d33", accent: "#3275e7" };
    if (/restaurant|menu/.test(type)) return { paper: "#fff7e8", ink: "#2c2116", accent: "#ec8c37" };
    if (/booking/.test(type)) return { paper: "#faf5fc", ink: "#2b2134", accent: "#a579db" };
    if (/digital/.test(type)) return { paper: "#f5f0ff", ink: "#211a33", accent: "#8b5cf6" };
    if (/lead_funnel|service|quote/.test(type)) return { paper: "#eef8f0", ink: "#16231c", accent: "#1f9d55" };
    return { paper: "#f2f2ec", ink: "#111111", accent: "#101828" };
  }
  function templateLivePreviewMarkup(choice) {
    const palette = templateAccentPalette(choice?.catalogType, choice?.templateId);
    const brand = escapeHtml((builderState.guidedState.businessName || "").slice(0, 18) || langText({
      en: "YOUR BRAND",
      es: "TU MARCA",
      fr: "VOTRE MARQUE",
      pt: "SUA MARCA"
    }));
    return `
    <div class="template-live-preview" style="--tlp-paper:${palette.paper};--tlp-ink:${palette.ink};--tlp-accent:${palette.accent};">
      <div class="tlp-nav"><b>${brand}</b><i></i><i></i><em></em></div>
      <div class="tlp-hero">
        <span class="tlp-copy"><b></b><b></b><i></i></span>
        <span class="tlp-image"><i></i><i></i><i></i></span>
      </div>
      <div class="tlp-cards"><i></i><i></i><i></i></div>
    </div>
  `;
  }
  function buildAiStudioPlanFromGuidedState(extra = "") {
    const contextText = guidedTemplateContextText(extra);
    const payload = guidedStatePayloadForPlanning();
    const templateId = inferDesignerTemplateIdFromPayload(payload) || inferTemplateIdFromText(contextText) || builderState.forcedTemplateSelection?.templateId || "";
    const templateMeta = templatePreviewMeta(templateId);
    const catalogType = templateMeta?.catalogType || builderState.forcedTemplateSelection?.catalogType || "";
    const websiteType = inferWebsiteTypeFromContext(contextText, catalogType);
    const salesFlow = inferSalesFlowForPlan(contextText, websiteType);
    const requiredFeatures = inferRequiredFeaturesForPlan(contextText, websiteType, catalogType);
    const missingImportantFields = missingImportantFieldsForPlan();
    const reasoningSummary = explainTemplateDecision(templateId, websiteType, contextText);
    const pages = buildPlanPagesForContext(templateId, websiteType, catalogType);
    return {
      version: 2,
      decisionState: missingImportantFields.length ? "enough_to_plan_with_optional_gaps" : "ready_to_generate",
      websiteType,
      recommendedTemplateId: templateId,
      recommendedTemplateName: localizedTemplateName(templateMeta) || builderState.forcedTemplateSelection?.template?.name || templateId,
      recommendedCatalogType: catalogType,
      reasoningSummary,
      salesFlow,
      requiredFeatures,
      pages,
      missingImportantFields,
      copyPolicy: "Use intake as private strategy. Rewrite all visible copy as polished customer-facing content.",
      editabilityContract: [
        "Every visible text block must be editable.",
        "Every catalog item, category, price label and image must be editable.",
        "Colors, fonts, buttons, page order and section order must remain editable.",
        "Future chat changes must patch only the requested area unless the client asks for a full redesign."
      ]
    };
  }
  function guidedStatePayloadForPlanning() {
    return {
      business_name: builderState.guidedState.businessName,
      business_description: builderState.guidedState.businessDescription || builderState.guidedState.websiteIntent,
      industry: builderState.guidedState.industry,
      services_products: arrayValue2(builderState.guidedState.servicesProducts),
      target_audience: builderState.guidedState.targetAudience,
      preferred_tone: builderState.guidedState.preferredTone,
      preferred_colors: arrayValue2(builderState.guidedState.preferredColors),
      salesMode: builderState.guidedState.salesMode
    };
  }
  function refreshAiStudioPlanFromContext(extra = "") {
    const plan = buildAiStudioPlanFromGuidedState(extra);
    builderState.guidedState.aiStudioPlan = plan;
    builderState.guidedState.designStrategy = {
      ...builderState.guidedState.designStrategy || {},
      diagnosis: plan,
      selectedTemplateId: plan.recommendedTemplateId,
      selectedTemplateReason: plan.reasoningSummary,
      selectedCatalogType: plan.recommendedCatalogType,
      designerRole: "senior ecommerce strategist, UX architect and brand designer",
      templateUsePolicy: "Choose the closest proven template as architecture, then adapt copy, colors, sections, catalog and CTAs to the client's business."
    };
    if (plan.recommendedTemplateId && builderState.forcedTemplateSelection?.templateId !== plan.recommendedTemplateId) {
      builderState.forcedTemplateSelection = {
        templateId: plan.recommendedTemplateId,
        template: null,
        catalogType: plan.recommendedCatalogType,
        intent: "ai_studio_plan",
        reason: plan.reasoningSummary
      };
      builderState.guidedState.sitePlan = null;
      builderState.guidedState.sitePlanApproved = false;
    } else if (builderState.forcedTemplateSelection?.templateId) {
      builderState.forcedTemplateSelection = {
        ...builderState.forcedTemplateSelection,
        catalogType: builderState.forcedTemplateSelection.catalogType || plan.recommendedCatalogType,
        reason: builderState.forcedTemplateSelection.reason || plan.reasoningSummary
      };
    }
    return plan;
  }
  function inferWebsiteTypeFromContext(contextText, catalogType = "") {
    const text = normalizeTemplateIntentText(`${contextText} ${catalogType}`);
    if (/restaurant|restaurante|menu|comida|food|cafe|delivery/.test(text)) return "restaurant_menu";
    if (/booking|appointment|reserva|cita|barber|salon|spa/.test(text)) return "booking_site";
    if (textSuggestsMultiVendorMarketplace(text) || /dense_marketplace_catalog/.test(text)) return "mega_marketplace";
    if (textSuggestsMegaRetailStore(text) || /single_vendor_dense|dense_retail|retail|amazon|de todo|todo tipo|variad[oa]s?|muchos productos|catalogo grande|multi.?category|multi.?categoria/.test(text)) return "mega_retail_store";
    if (/listing|classified|real estate|inmueble|rental|alquiler|carros usados/.test(text)) return "listing_marketplace";
    if (/digital|download|curso|course|ebook|membership|membresia/.test(text)) return "digital_products";
    if (/quote|cotizacion|cotización|service|servicio|contractor|legal|clinic|industrial/.test(text)) return "service_or_quote_site";
    if (/premium_editorial|flagship|single product|producto unico|mismo nicho|linea de|showcase/.test(text)) return "premium_showcase_store";
    if (/store|shop|tienda|ecommerce|venta online|vender online|catalog/.test(text)) return "online_store";
    return "business_website";
  }
  function inferSalesFlowForPlan(contextText, websiteType) {
    const text = normalizeTemplateIntentText(contextText);
    if (/quote|cotizacion|cotización|presupuesto|consulta|inquiry/.test(text) || /service|quote/.test(websiteType)) return "quote_or_lead_request";
    if (/booking|appointment|reserva|cita/.test(text) || websiteType === "booking_site") return "appointment_booking";
    if (/restaurant|restaurante|menu|comida|food|cafe|cafeteria|delivery de comida/.test(text) || websiteType === "restaurant_menu") return "menu_order_or_contact";
    if (/marketplace|store|shop|tienda|vender|venta online|checkout|carrito/.test(text) || /store|marketplace|digital/.test(websiteType)) return "online_sales";
    return "lead_capture";
  }
  function inferRequiredFeaturesForPlan(contextText, websiteType, catalogType = "") {
    const text = normalizeTemplateIntentText(`${contextText} ${websiteType} ${catalogType}`);
    const features = /* @__PURE__ */ new Set(["editable_pages", "editable_sections", "editable_brand_system", "contact_capture"]);
    if (/marketplace|dense|catalog|store|shop|tienda|product|producto/.test(text)) {
      ["catalog_manager", "categories", "search", "filters", "product_cards"].forEach((feature) => features.add(feature));
    }
    if (/online_sales|checkout|carrito|cart|shop|store|tienda|marketplace|digital/.test(text)) {
      ["cart", "checkout_path", "customer_account", "featured_deals"].forEach((feature) => features.add(feature));
    }
    if (/marketplace|dense|retail|amazon|muchos productos|de todo|todo tipo/.test(text)) {
      ["deal_sections", "best_sellers", "newsletter_or_deal_subscription", "ratings_reviews", "wishlist_ready"].forEach((feature) => features.add(feature));
    }
    if (/booking|appointment|reserva|cita/.test(text)) features.add("booking_request_flow");
    if (/restaurant|menu|food|comida/.test(text)) features.add("menu_categories");
    if (/quote|cotizacion|cotización|service|servicio|industrial|legal|clinic/.test(text)) features.add("quote_request_flow");
    return [...features];
  }
  function missingImportantFieldsForPlan() {
    const missing = [];
    if (!builderState.guidedState.businessName) missing.push("businessName");
    if (!builderState.guidedState.businessDescription && !arrayValue2(builderState.guidedState.servicesProducts).length) missing.push("businessDescription");
    if (!builderState.guidedState.websiteIntent && !builderState.guidedState.salesMode) missing.push("websiteIntent");
    return missing;
  }
  function explainTemplateDecision(templateId, websiteType, contextText) {
    if (templateId === "mega-retail-store" || websiteType === "mega_retail_store") {
      return langText({
        en: "The offer reads as a broad catalog owned by one business, so the best base is a search-first retail store with categories, deals, filters, account and cart logic.",
        es: "La oferta se entiende como un catalogo amplio de un solo negocio, asi que la mejor base es una tienda retail con busqueda, categorias, ofertas, filtros, cuenta y carrito.",
        fr: "L'offre ressemble a un grand catalogue gere par une seule entreprise; la meilleure base est donc une boutique retail avec recherche, categories, offres, filtres, compte et panier.",
        pt: "A oferta parece um catalogo amplo de um unico negocio; a melhor base e uma loja retail com busca, categorias, ofertas, filtros, conta e carrinho."
      });
    }
    if (templateId === "mega-marketplace") {
      return langText({
        en: "The offer reads as a broad, varied catalog, so the best base is a search-first marketplace with categories, deals, filters, account and cart logic.",
        es: "La oferta se entiende como un catalogo amplio y variado, asi que la mejor base es un marketplace con busqueda, categorias, ofertas, filtros, cuenta y carrito.",
        fr: "L'offre ressemble a un catalogue large et varie; la meilleure base est donc un marketplace avec recherche, categories, offres, filtres, compte et panier.",
        pt: "A oferta parece um catalogo amplo e variado; a melhor base e um marketplace com busca, categorias, ofertas, filtros, conta e carrinho."
      });
    }
    if (normalizeTemplateId(templateId) === "premium-product-store") {
      return langText({
        en: "The offer is focused enough for a premium product presentation with stronger story, proof, feature sections and conversion.",
        es: "La oferta es suficientemente enfocada para una presentacion premium con historia, prueba, secciones de beneficios y conversion.",
        fr: "L'offre est assez focalisee pour une presentation premium avec histoire, preuves, benefices et conversion.",
        pt: "A oferta e focada o suficiente para uma apresentacao premium com historia, prova, beneficios e conversao."
      });
    }
    if (templateId === "restaurant-food-business") return langText({ en: "The business needs menu discovery and ordering/contact, so LYRA should use a restaurant menu architecture.", es: "El negocio necesita menu, descubrimiento y pedido/contacto, asi que LYRA debe usar arquitectura de restaurante.", fr: "Le business demande menu, decouverte et commande/contact.", pt: "O negocio precisa de menu, descoberta e pedido/contato." });
    if (templateId === "booking-appointment-pro") return langText({ en: "The core action is scheduling, so LYRA should make booking and services the main conversion path.", es: "La accion central es agendar, asi que LYRA debe poner reservas y servicios como conversion principal.", fr: "L'action principale est la reservation.", pt: "A acao principal e agendamento." });
    if (templateId) return langText({
      en: `LYRA selected ${localizedTemplateName(templatePreviewMeta(templateId)) || templateId} because it best matches ${websiteType.replace(/_/g, " ")}.`,
      es: `LYRA selecciono ${localizedTemplateName(templatePreviewMeta(templateId)) || templateId} porque encaja mejor con ${websiteType.replace(/_/g, " ")}.`,
      fr: `LYRA a choisi ${localizedTemplateName(templatePreviewMeta(templateId)) || templateId} car il correspond a ${websiteType.replace(/_/g, " ")}.`,
      pt: `A LYRA escolheu ${localizedTemplateName(templatePreviewMeta(templateId)) || templateId} porque combina melhor com ${websiteType.replace(/_/g, " ")}.`
    });
    return langText({
      en: "LYRA is still identifying the strongest template from the business context.",
      es: "LYRA todavia esta identificando la plantilla mas fuerte segun el contexto del negocio.",
      fr: "LYRA identifie encore le meilleur template selon le contexte.",
      pt: "A LYRA ainda esta identificando o melhor template pelo contexto."
    });
  }
  function buildPlanPagesForContext(templateId, websiteType, catalogType) {
    if (templateId === "mega-retail-store" || websiteType === "mega_retail_store") {
      return [
        { key: "home", title: "Home", purpose: "Search, categories, deals, best sellers and trust modules", sections: ["retail_header", "search_hero", "category_rail", "deal_row", "best_seller_grid", "subscribe"] },
        { key: "catalog", title: "Catalog", purpose: "Dense searchable owned product catalog", sections: ["filters", "sort", "product_grid", "ratings", "shipping_badges"] },
        { key: "deals", title: "Deals", purpose: "Promotions, drops and featured offers", sections: ["daily_deals", "featured_collections", "newsletter"] },
        { key: "account", title: "Account / Cart", purpose: "Customer account, cart, checkout path and saved items", sections: ["cart", "wishlist", "checkout", "order_contact"] }
      ];
    }
    if (templateId === "mega-marketplace" || websiteType === "mega_marketplace") {
      return [
        { key: "home", title: "Home", purpose: "Search, categories, deals, best sellers and trust modules", sections: ["marketplace_header", "search_hero", "category_rail", "deal_row", "best_seller_grid", "subscribe"] },
        { key: "catalog", title: "Catalog", purpose: "Dense searchable product catalog", sections: ["filters", "sort", "product_grid", "ratings", "shipping_badges"] },
        { key: "deals", title: "Deals", purpose: "Promotions, drops and featured offers", sections: ["daily_deals", "featured_collections", "newsletter"] },
        { key: "account", title: "Account / Cart", purpose: "Customer account, cart, checkout path and saved items", sections: ["cart", "wishlist", "checkout", "order_contact"] }
      ];
    }
    if (normalizeTemplateId(templateId) === "premium-product-store" || websiteType === "premium_showcase_store") {
      return [
        { key: "home", title: "Home", purpose: "Cinematic product story and premium CTA", sections: ["premium_hero", "feature_spotlight", "proof", "cta"] },
        { key: "catalog", title: "Collection", purpose: "Curated product line or models", sections: ["featured_products", "specs", "comparison"] },
        { key: "story", title: "Story", purpose: "Brand/product positioning and benefits", sections: ["editorial_story", "gallery", "trust"] },
        { key: "buy", title: "Buy / Contact", purpose: "Purchase, quote or lead action", sections: ["checkout_or_inquiry", "faq", "support"] }
      ];
    }
    return buildSitePlan({ templateId, catalogType, template: builderState.forcedTemplateSelection?.template || {} }).pages;
  }
  function buildSitePlan(selection = builderState.forcedTemplateSelection) {
    const template = selection?.template || {};
    const catalogType = selection?.catalogType || template.catalogModel?.catalogType || "";
    const isBooking = /booking|appointment/.test(catalogType);
    const isService = /service|quote|practice|project/.test(catalogType);
    const isListing = /listing/.test(catalogType);
    const isMarketplace = /marketplace|dense/.test(catalogType);
    const isDigital = /digital|pricing|ticket/.test(catalogType);
    const shopLabel = isBooking ? langText({ en: "Services", es: "Servicios", fr: "Services", pt: "Servi\xE7os" }) : isService ? langText({ en: "Services", es: "Servicios", fr: "Services", pt: "Servi\xE7os" }) : isListing ? langText({ en: "Listings", es: "Listados", fr: "Annonces", pt: "An\xFAncios" }) : isDigital ? langText({ en: "Offers", es: "Ofertas", fr: "Offres", pt: "Ofertas" }) : langText({ en: "Catalog", es: "Cat\xE1logo", fr: "Catalogue", pt: "Cat\xE1logo" });
    const actionLabel = isBooking ? langText({ en: "Booking", es: "Reservas", fr: "R\xE9servation", pt: "Agendamento" }) : isService ? langText({ en: "Quote", es: "Cotizaci\xF3n", fr: "Devis", pt: "Or\xE7amento" }) : langText({ en: "Checkout / Contact", es: "Checkout / Contacto", fr: "Checkout / Contact", pt: "Checkout / Contato" });
    const templatePages = Array.isArray(template.pages) && template.pages.length ? template.pages.slice(0, 4).map((page, index) => ({
      key: slugify2(page.name || page.page_key || `page-${index + 1}`),
      title: page.name || page.title || `Page ${index + 1}`,
      purpose: page.purpose || page.layout || "",
      sections: arrayValue2(page.usesSections || page.sections).slice(0, 6)
    })) : [];
    const basePages = templatePages.length ? templatePages : [
      {
        key: "home",
        title: langText({ en: "Home", es: "Inicio", fr: "Accueil", pt: "In\xEDcio" }),
        purpose: langText({ en: "Brand promise, hero, best offer, trust and primary CTA.", es: "Promesa de marca, hero, mejor oferta, confianza y CTA principal.", fr: "Promesse de marque, hero, offre principale, confiance et CTA.", pt: "Promessa da marca, hero, oferta principal, confian\xE7a e CTA." }),
        sections: ["Hero", "Featured", "Trust", "CTA"]
      },
      {
        key: "catalog",
        title: shopLabel,
        purpose: langText({ en: "Products/services with filters, cards and clear actions.", es: "Productos/servicios con filtros, tarjetas y acciones claras.", fr: "Produits/services avec filtres, cartes et actions claires.", pt: "Produtos/servi\xE7os com filtros, cards e a\xE7\xF5es claras." }),
        sections: ["Search", "Categories", "Grid", "Detail CTA"]
      },
      {
        key: "detail",
        title: isService ? langText({ en: "Service detail", es: "Detalle del servicio", fr: "D\xE9tail du service", pt: "Detalhe do servi\xE7o" }) : langText({ en: "Product detail", es: "Detalle de producto", fr: "D\xE9tail produit", pt: "Detalhe do produto" }),
        purpose: langText({ en: "Gallery, benefits, price/quote logic, proof and related items.", es: "Galer\xEDa, beneficios, l\xF3gica de precio/cotizaci\xF3n, prueba y relacionados.", fr: "Galerie, b\xE9n\xE9fices, prix/devis, preuve et \xE9l\xE9ments li\xE9s.", pt: "Galeria, benef\xEDcios, pre\xE7o/or\xE7amento, prova e relacionados." }),
        sections: ["Gallery", "Benefits", "Proof", "Related"]
      },
      {
        key: "contact",
        title: actionLabel,
        purpose: langText({ en: "Final conversion path with contact, booking, quote or checkout intent.", es: "Ruta final de conversi\xF3n con contacto, reserva, cotizaci\xF3n o compra.", fr: "Parcours final avec contact, r\xE9servation, devis ou achat.", pt: "Caminho final com contato, agendamento, or\xE7amento ou compra." }),
        sections: ["Contact", "Form", "Social", "Location"]
      }
    ];
    return {
      version: 1,
      approved: false,
      templateId: selection?.templateId || template.id || "",
      templateName: template.name || selection?.templateId || "",
      catalogType,
      strategy: template.visualDifference || template.catalogModel?.customerFeeling || "",
      pages: basePages,
      rules: [
        "Use this plan as the generation contract.",
        "Generate visible copy from client intent, not literal intake notes.",
        "Keep all pages, sections, products, colors and text editable.",
        "Do not replace this plan unless the client asks for a different structure."
      ]
    };
  }
  function ensureSitePlan() {
    const currentTemplateId = builderState.forcedTemplateSelection?.templateId || builderState.guidedState.sitePlan?.templateId || "";
    if (!builderState.guidedState.sitePlan || builderState.guidedState.sitePlan.templateId !== currentTemplateId) {
      builderState.guidedState.sitePlan = buildSitePlan();
      builderState.guidedState.sitePlanApproved = false;
    }
    return builderState.guidedState.sitePlan;
  }
  function renderSitePlanCard() {
    const plan = ensureSitePlan();
    const card = document.createElement("section");
    card.className = `site-plan-card ${builderState.guidedState.sitePlanApproved ? "approved" : ""}`;
    card.innerHTML = `
    <div class="site-plan-head">
      <div>
        <span>${escapeHtml(langText({ en: "Site plan", es: "Plan del sitio", fr: "Plan du site", pt: "Plano do site" }))}</span>
        <strong>${escapeHtml(plan.templateName || plan.templateId || langText({ en: "Selected structure", es: "Estructura seleccionada", fr: "Structure s\xE9lectionn\xE9e", pt: "Estrutura selecionada" }))}</strong>
      </div>
      <em>${escapeHtml(plan.catalogType || "")}</em>
    </div>
    ${plan.strategy ? `<p>${escapeHtml(plan.strategy)}</p>` : ""}
    <div class="site-plan-pages">
      ${plan.pages.map((page, index) => `
        <article>
          <b>${index + 1}</b>
          <div>
            <strong>${escapeHtml(page.title)}</strong>
            <span>${escapeHtml(page.purpose)}</span>
            <small>${escapeHtml(arrayValue2(page.sections).join(" \xB7 "))}</small>
          </div>
        </article>
      `).join("")}
    </div>
    <div class="site-plan-actions">
      <button type="button" data-site-plan-approve>${escapeHtml(builderState.guidedState.sitePlanApproved ? langText({ en: "Approved", es: "Aprobado", fr: "Approuv\xE9", pt: "Aprovado" }) : langText({ en: "Approve this structure", es: "Aprobar estructura", fr: "Approuver la structure", pt: "Aprovar estrutura" }))}</button>
      <button type="button" data-site-plan-adjust>${escapeHtml(langText({ en: "Ask LYRA to change it", es: "Pedir cambio a LYRA", fr: "Demander un changement", pt: "Pedir mudan\xE7a \xE0 LYRA" }))}</button>
    </div>
  `;
    card.querySelector("[data-site-plan-approve]")?.addEventListener("click", () => {
      builderState.guidedState.sitePlanApproved = true;
      builderState.guidedState.sitePlan = { ...builderState.guidedState.sitePlan, approved: true };
      appendChatMessage("user", langText({ en: "I approve this site structure.", es: "Apruebo esta estructura del sitio.", fr: "J'approuve cette structure.", pt: "Aprovo esta estrutura do site." }));
      appendChatMessage("assistant", langText({
        en: "Good. I will use this plan as the structure for the draft and keep it editable.",
        es: "Bien. Usar\xE9 este plan como estructura del borrador y lo mantendr\xE9 editable.",
        fr: "Tr\xE8s bien. J'utiliserai ce plan comme structure du brouillon et il restera modifiable.",
        pt: "Certo. Vou usar este plano como estrutura do rascunho e manter tudo edit\xE1vel."
      }), "success");
      renderGuidedSummary();
    });
    card.querySelector("[data-site-plan-adjust]")?.addEventListener("click", () => {
      builderState.guidedStep = "review";
      guidedReply2.value = langText({
        en: "Change the site plan: ",
        es: "Cambia el plan del sitio: ",
        fr: "Change le plan du site : ",
        pt: "Mude o plano do site: "
      });
      guidedReply2.focus();
    });
    return card;
  }
  function appendTemplateDetectionMessage(selection) {
    if (!document.body.classList.contains("lyra-debug-mode")) return;
    if (!selection?.templateId) {
      appendChatMessage("assistant", langText({
        en: "I understand the general goal. I will choose the visual structure after I know what you sell or offer.",
        es: "Entiendo el objetivo general. Elegire la estructura visual cuando sepa que vendes u ofreces.",
        fr: "Je comprends l'objectif general. Je choisirai la structure visuelle apres avoir compris l'offre.",
        pt: "Entendi o objetivo geral. Vou escolher a estrutura visual depois de entender o que voce vende ou oferece."
      }), "thinking");
      return;
    }
    const templateName = selection.template?.name || selection.templateId;
    const explanation = selection.template?.catalogModel?.customerFeeling || selection.template?.visualDifference || selection.reason;
    const lines = [
      t("intentDetected"),
      `${t("selectedTemplate")}: ${templateName}`,
      `${t("catalogType")}: ${selection.catalogType}`,
      explanation
    ];
    appendChatMessage("assistant", lines.join("\n"), "success");
  }
  function initTemplateCarousel(track) {
    if (!track || track.dataset.carouselReady === "1") return;
    track.dataset.carouselReady = "1";
    let rafId = 0;
    const schedule = () => {
      cancelAnimationFrame(rafId);
      updateTemplateCarouselActiveCard(track);
      rafId = requestAnimationFrame(() => updateTemplateCarouselActiveCard(track));
    };
    track.addEventListener("click", (event) => {
      if (event.target?.closest?.("[data-template-preview]")) return;
      const card = event.target?.closest?.(".template-carousel-card");
      if (!card) return;
      card.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "center" });
      schedule();
    });
    track.addEventListener("keydown", (event) => {
      if (!["Enter", " "].includes(event.key)) return;
      const card = event.target?.closest?.(".template-carousel-card");
      if (!card) return;
      event.preventDefault();
      card.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "center" });
      schedule();
    });
    track.addEventListener("scroll", schedule, { passive: true });
    window.addEventListener("resize", schedule, { passive: true });
    schedule();
  }
  function updateTemplateCarouselActiveCard(track) {
    if (!track) return;
    const cards = [...track.querySelectorAll(".template-carousel-card")];
    if (!cards.length) return;
    const trackRect = track.getBoundingClientRect();
    const center = trackRect.left + trackRect.width / 2;
    let active = cards[0];
    let smallestDistance = Infinity;
    cards.forEach((card) => {
      const rect = card.getBoundingClientRect();
      const cardCenter = rect.left + rect.width / 2;
      const distance = Math.abs(center - cardCenter);
      if (distance < smallestDistance) {
        active = card;
        smallestDistance = distance;
      }
    });
    cards.forEach((card) => {
      const rect = card.getBoundingClientRect();
      const cardCenter = rect.left + rect.width / 2;
      const cardWidth = Math.max(1, rect.width);
      const rawOffset = (cardCenter - center) / cardWidth;
      const offset = Math.max(-2.4, Math.min(2.4, rawOffset));
      const distance = Math.min(2.4, Math.abs(offset));
      const rotate = -offset * 17;
      const scale = Math.max(0.78, 1 - distance * 0.085);
      const depth = Math.max(-30, 26 - distance * 24);
      const shift = -offset * 14;
      const opacity = Math.max(0.62, 1 - distance * 0.16);
      const brightness = Math.max(0.86, 1 - distance * 0.055);
      const saturate = Math.max(0.78, 1 - distance * 0.07);
      card.style.setProperty("--coverflow-rotate", `${rotate.toFixed(2)}deg`);
      card.style.setProperty("--coverflow-scale", scale.toFixed(3));
      card.style.setProperty("--coverflow-depth", `${depth.toFixed(1)}px`);
      card.style.setProperty("--coverflow-shift", `${shift.toFixed(1)}px`);
      card.style.setProperty("--coverflow-opacity", opacity.toFixed(3));
      card.style.setProperty("--coverflow-brightness", brightness.toFixed(3));
      card.style.setProperty("--coverflow-saturate", saturate.toFixed(3));
      card.style.setProperty("--coverflow-z", String(Math.round(100 - distance * 18)));
      card.classList.toggle("active-card", card === active);
      card.setAttribute("aria-current", card === active ? "true" : "false");
    });
  }
  async function chooseTemplatePreview(choice, options = {}) {
    const template = window.TemplateRouter?.getTemplateById ? await window.TemplateRouter.getTemplateById(choice.templateId) : null;
    builderState.forcedTemplateSelection = {
      templateId: choice.templateId,
      template,
      catalogType: choice.catalogType,
      intent: "client_visual_template_choice",
      reason: "Client selected a visual template preview in guided setup",
      executionStatus: templateExecutionStatus(choice.templateId)
    };
    builderState.guidedState.websiteIntent = builderState.guidedState.websiteIntent || localizedTemplateName(choice);
    document.querySelectorAll(".template-choice-card.selected").forEach((card) => card.classList.remove("selected"));
    document.querySelector(`[data-template-choice="${cssEscape(choice.templateId)}"]`)?.classList.add("selected");
    if (options.hideCarousel) {
      document.querySelectorAll(".template-carousel-panel").forEach((panel) => {
        panel.classList.add("template-carousel-panel-hidden");
      });
    }
    if (options.fadeCanvas && liveSitePreviewMount) {
      liveSitePreviewMount.classList.remove("canvas-fade-in");
      void liveSitePreviewMount.offsetWidth;
      liveSitePreviewMount.classList.add("canvas-fade-in");
    }
    renderLiveSitePreview();
    appendChatMessage(
      "user",
      langText({
        en: `Use ${localizedTemplateName(choice)}`,
        es: `Usar ${localizedTemplateName(choice)}`,
        fr: `Utiliser ${localizedTemplateName(choice)}`,
        pt: `Usar ${localizedTemplateName(choice)}`
      })
    );
    appendTemplateDetectionMessage(builderState.forcedTemplateSelection);
    renderGuidedCoachCard();
    renderGuidedSummary();
    saveGuidedDraft();
  }
  window.Lyra = {
    ...window.Lyra || {},
    async selectTemplate(templateId) {
      const choice = templatePreviewMeta(templateId);
      if (!choice) return null;
      await chooseTemplatePreview(choice, { hideCarousel: true, fadeCanvas: true });
      return builderState.forcedTemplateSelection;
    }
  };
  function letAiDecide(field) {
    const decision = t("letAiDecide");
    if (field === "preferredColors") {
      builderState.guidedState.preferredColors = [decision];
    } else if (field === "sectionsPreference") {
      builderState.guidedState.sectionsPreference = decision;
    } else {
      builderState.guidedState[field] = decision;
    }
    builderState.guidedStep = field === "sectionsPreference" ? "review" : nextSmartGuidedStep(field);
    appendChatMessage("user", `${field}: ${decision}`);
    appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), builderState.guidedStep === "review" ? "success" : "speaking");
    renderGuidedSummary();
  }
  function insertQuickChip(value) {
    const translated = translateChip(value);
    if (builderState.guidedStep === "websiteIntent") {
      guidedReply2.value = translated;
      updateAssetPromptVisibility();
      handleGuidedSendAction();
      return;
    }
    if (value === "Yes, correct") {
      builderState.guidedStep = nextSmartGuidedStep(builderState.guidedStep);
      appendChatMessage("user", translated);
      appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), builderState.guidedStep === "review" ? "success" : "speaking");
      renderGuidedSummary();
      refreshQuickChips();
      return;
    }
    if (value === "Change style") {
      builderState.guidedStep = "preferredTone";
      guidedReply2.value = "";
      appendChatMessage("user", translated);
      appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), "speaking");
      renderGuidedSummary();
      refreshQuickChips();
      guidedReply2.focus();
      return;
    }
    if (value === "Upload logo") {
      guidedLogoUpload.click();
      return;
    }
    if (value === "Continue without logo") {
      builderState.guidedState.hasLogoPhotos = langText({
        en: "Continue without a logo",
        es: "Seguir sin logo",
        fr: "Continuer sans logo",
        pt: "Continuar sem logo"
      });
      builderState.guidedState.logoPreference = "text_only";
      builderState.guidedState.aiGeneratedLogoRequested = false;
      builderState.guidedStep = nextSmartGuidedStep("hasLogoPhotos");
      appendChatMessage("user", translated);
      appendChatMessage("assistant", guidedQuestion(builderState.guidedStep), builderState.guidedStep === "review" ? "success" : "speaking");
      renderGuidedSummary();
      refreshQuickChips();
      return;
    }
    if (value === "Review details") {
      openReviewDetails();
      return;
    }
    if (value === "Use my logo colors") {
      guidedReply2.value = translated;
      updateAssetPromptVisibility();
      guidedLogoUpload.click();
      guidedReply2.focus();
      return;
    }
    guidedReply2.value = guidedReply2.value ? `${guidedReply2.value}, ${translated}` : translated;
    updateAssetPromptVisibility();
    guidedReply2.focus();
  }
  function refreshQuickChips() {
    const chipsByStep = {
      websiteIntent: ["Sell online", "Show catalog", "Booking", "Request quotes"],
      preferredTone: ["Elegant", "Modern", "Warm", "Let AI decide"],
      preferredColors: ["Let AI choose", "Use my logo colors", "I have specific colors"],
      salesMode: ["Sell online", "Request quotes", "Calls/messages", "Not sure"],
      targetAudience: ["Local customers", "Families", "Professionals", "Let AI decide"],
      hasLogoPhotos: ["Upload logo", "Continue without logo"],
      review: []
    };
    const chips = (chipsByStep[builderState.guidedStep] || []).slice(0, 4);
    quickChipRow.innerHTML = chips.map((chip) => `<button data-chip="${escapeAttribute(chip)}" type="button">${escapeHtml(translateChip(chip))}</button>`).join("");
    quickChipRow.querySelectorAll("[data-chip]").forEach((button) => {
      button.addEventListener("click", () => insertQuickChip(button.dataset.chip));
    });
    updateAssetPromptVisibility();
  }
  function wantsAiGeneratedLogo(value, options = {}) {
    const text = String(value || "").toLowerCase();
    const logoContext = options.assumeLogoContext || /logo|brand mark|marca visual|identidad visual|brand identity/.test(text);
    const directCreateRequest = /no tengo logo|sin logo|crea(?:r)?(?:me)?(?: un)? logo|crear(?: un)? logo|generate(?: a)? logo|make(?: a)? logo|haz(?:me)?(?: un)? logo|diseñ(?:a|ar)(?: un)? logo|disena(?:r)?(?: un)? logo|gen[eé]rame(?: un)? logo/.test(text);
    const delegatedCreation = /(?:lyra|ia|ai|tu|t[uú]|you)\s+(?:decide|elige|choose|hazlo|create it)|(?:decide|elige|hazlo|crealo|créalo|generalo|gen[eé]ralo)\s+(?:tu|t[uú]|lyra|ia|ai|you)|sorpr[eé]ndeme|surprise me|you decide/.test(text);
    return directCreateRequest || logoContext && delegatedCreation;
  }
  function logoPreferenceFromText(value, options = {}) {
    return wantsAiGeneratedLogo(value, options) ? "generate_ai_logo" : "";
  }
  function contactInfoCompactLabel(value) {
    const keys = Object.keys(value || {}).filter((key) => value[key]);
    return keys.slice(0, 3).join(", ");
  }
  function translateChip(value) {
    const dictionary = {
      es: {
        Elegant: "Elegante",
        Modern: "Moderno",
        Premium: "Premium",
        Warm: "Calido",
        Bold: "Llamativo",
        Fun: "Divertido",
        Professional: "Profesional",
        "Let AI decide": "Que IA decida",
        "Let AI choose": "Que IA elija",
        "Online store": "Tienda online",
        "Show catalog": "Mostrar catalogo",
        "Business info site": "Pagina informativa",
        Marketplace: "Marketplace",
        Restaurant: "Restaurante",
        Services: "Servicios",
        Booking: "Reservas",
        "Digital products": "Productos digitales",
        "Use my logo colors": "Usar colores de mi logo",
        "I have specific colors": "Tengo colores especificos",
        "Sell online": "Vender online",
        "Request quotes": "Recibir cotizaciones",
        "Calls/messages": "Llamadas/mensajes",
        "All of the above": "Todo lo anterior",
        "Not sure": "No estoy seguro",
        "Local customers": "Clientes locales",
        Families: "Familias",
        Professionals: "Profesionales",
        Businesses: "Empresas",
        "Yes, correct": "S\xED, correcto",
        "Change style": "Cambiar estilo",
        "Upload logo": "Subir logo",
        "Continue without logo": "Seguir sin logo",
        "Review details": "Revisar detalles"
      },
      fr: {
        Elegant: "\xC9l\xE9gant",
        Modern: "Moderne",
        Premium: "Premium",
        Warm: "Chaleureux",
        Bold: "Audacieux",
        Fun: "Amusant",
        Professional: "Professionnel",
        "Let AI decide": "Laisser l'IA d\xE9cider",
        "Let AI choose": "Laisser l'IA choisir",
        "Show catalog": "Afficher un catalogue",
        "Business info site": "Site d'information",
        Marketplace: "Marketplace",
        Booking: "R\xE9servation",
        "Use my logo colors": "Utiliser les couleurs du logo",
        "I have specific colors": "J'ai des couleurs pr\xE9cises",
        "Sell online": "Vendre en ligne",
        "Request quotes": "Demander des devis",
        "Calls/messages": "Appels/messages",
        "All of the above": "Tout ce qui pr\xE9c\xE8de",
        "Not sure": "Je ne suis pas s\xFBr",
        "Local customers": "Clients locaux",
        Families: "Familles",
        Professionals: "Professionnels",
        Businesses: "Entreprises",
        "Yes, correct": "Oui, c'est correct",
        "Change style": "Changer le style",
        "Upload logo": "Importer un logo",
        "Continue without logo": "Continuer sans logo",
        "Review details": "V\xE9rifier les d\xE9tails"
      },
      pt: {
        Elegant: "Elegante",
        Modern: "Moderno",
        Premium: "Premium",
        Warm: "Acolhedor",
        Bold: "Marcante",
        Fun: "Divertido",
        Professional: "Profissional",
        "Let AI decide": "Deixar a IA decidir",
        "Let AI choose": "Deixar a IA escolher",
        "Show catalog": "Mostrar catalogo",
        "Business info site": "Site informativo",
        Marketplace: "Marketplace",
        Booking: "Agendamento",
        "Use my logo colors": "Usar as cores do logo",
        "I have specific colors": "Tenho cores especificas",
        "Sell online": "Vender online",
        "Request quotes": "Solicitar or\xE7amentos",
        "Calls/messages": "Liga\xE7\xF5es/mensagens",
        "All of the above": "Tudo isso",
        "Not sure": "N\xE3o tenho certeza",
        "Local customers": "Clientes locais",
        Families: "Fam\xEDlias",
        Professionals: "Profissionais",
        Businesses: "Empresas",
        "Yes, correct": "Sim, correto",
        "Change style": "Mudar estilo",
        "Upload logo": "Enviar logo",
        "Continue without logo": "Continuar sem logo",
        "Review details": "Revisar detalhes"
      }
    };
    return dictionary[builderState.selectedLanguage]?.[value] || value;
  }
  async function handleGuidedLogoUpload() {
    const file = guidedLogoUpload.files?.[0];
    if (!file) return;
    await processGuidedLogoFile(file);
  }
  async function processGuidedLogoFile(file) {
    if (!file || !file.type?.startsWith("image/")) return;
    const localLogoUrl = await fileToDataUrl(file);
    let storedLogoUrl = localLogoUrl;
    try {
      storedLogoUrl = await uploadAssetOrFallback(file, "logo", "Uploaded logo");
    } catch {
      storedLogoUrl = localLogoUrl;
    }
    builderState.guidedState.logoUrl = storedLogoUrl || localLogoUrl;
    builderState.guidedState.hasLogo = true;
    const brand = await analyzeLogoBrand(localLogoUrl, {
      logoUrl: builderState.guidedState.logoUrl,
      businessName: builderState.guidedState.businessName,
      industry: builderState.guidedState.industry,
      tone: builderState.guidedState.preferredTone
    });
    const palette = brand.extractedColors || [];
    builderState.guidedState.logoPalette = palette;
    builderState.guidedState.brand = brand;
    if (palette.length) {
      builderState.guidedState.preferredColors = palette;
    }
    builderState.guidedState.hasLogoPhotos = builderState.guidedState.hasPhotos ? "Logo and photos uploaded" : "Logo uploaded";
    applyBrandToCurrentSchema(brand);
    appendChatMessage(
      "assistant",
      palette.length ? langText({
        en: `Nice. I extracted this palette from the logo: ${palette.join(", ")}. I will use it as visual direction, not as page text.`,
        es: `Listo. Saqu\xE9 esta paleta del logo: ${palette.join(", ")}. La usar\xE9 como direcci\xF3n visual, no como texto de la p\xE1gina.`,
        fr: `Parfait. J'ai extrait cette palette du logo: ${palette.join(", ")}. Je l'utiliserai comme direction visuelle, pas comme texte du site.`,
        pt: `Pronto. Extra\xED esta paleta do logo: ${palette.join(", ")}. Vou us\xE1-la como dire\xE7\xE3o visual, n\xE3o como texto do site.`
      }) : langText({
        en: "Logo uploaded. I will use it as visual direction for the draft.",
        es: "Logo subido. Lo usar\xE9 como referencia visual para la primera versi\xF3n.",
        fr: "Logo import\xE9. Je l'utiliserai comme r\xE9f\xE9rence visuelle pour la premi\xE8re version.",
        pt: "Logo enviado. Vou us\xE1-lo como refer\xEAncia visual para o primeiro rascunho."
      }),
      "success"
    );
    renderBrandKit();
    renderGuidedSummary();
    refreshQuickChips();
    saveGuidedDraft();
  }
  async function handleGuidedPhotoUpload() {
    const files = Array.from(guidedPhotoUpload.files || []).filter((file) => file.size > 0);
    await processGuidedPhotoFiles(files.filter((file) => file.type?.startsWith("image/")));
    await processGuidedVideoFiles(files.filter((file) => file.type?.startsWith("video/")));
  }
  async function processGuidedPhotoFiles(files) {
    files = arrayValue2(files).filter((file) => file?.size > 0 && file.type?.startsWith("image/"));
    if (!files.length) return;
    const urls = [];
    for (const file of files) {
      urls.push(await fileToDataUrl(file));
    }
    builderState.guidedState.photoUrls = [...arrayValue2(builderState.guidedState.photoUrls), ...urls];
    builderState.guidedState.hasPhotos = true;
    builderState.guidedState.hasLogoPhotos = builderState.guidedState.hasLogo ? "Logo and photos uploaded" : "Photos uploaded";
    appendChatMessage(
      "assistant",
      langText({
        en: "Photos uploaded. I will use them to make the draft feel closer to the real business.",
        es: "Fotos subidas. Las usar\xE9 para que la primera versi\xF3n se sienta m\xE1s cercana al negocio real.",
        fr: "Photos import\xE9es. Je les utiliserai pour rapprocher le brouillon du vrai business.",
        pt: "Fotos enviadas. Vou us\xE1-las para deixar o rascunho mais pr\xF3ximo do neg\xF3cio real."
      }),
      "success"
    );
    renderGuidedSummary();
    saveGuidedDraft();
  }
  async function processGuidedVideoFiles(files) {
    files = arrayValue2(files).filter((file) => file?.size > 0 && file.type?.startsWith("video/"));
    if (!files.length) return;
    const urls = [];
    for (const file of files) {
      try {
        const uploaded = await uploadAssetFile(file, "video", `Uploaded video ${urls.length + 1}`);
        if (uploaded?.url) {
          urls.push(uploaded.url);
        }
      } catch (error) {
        console.warn("Video upload failed; keeping it as a session-only reference.", error);
      }
    }
    builderState.guidedState.videoUrls = [...arrayValue2(builderState.guidedState.videoUrls), ...urls];
    builderState.guidedState.hasPhotos = true;
    builderState.guidedState.hasLogoPhotos = builderState.guidedState.hasLogo ? "Logo, photos and videos uploaded" : "Photos/videos uploaded";
    appendChatMessage(
      "assistant",
      urls.length ? langText({
        en: "Video received and saved as a visual reference for the draft.",
        es: "Video recibido y guardado como referencia visual para el borrador.",
        fr: "Video recue et enregistree comme reference visuelle pour le brouillon.",
        pt: "Video recebido e salvo como referencia visual para o rascunho."
      }) : langText({
        en: "I received the video as a session reference. For publishing, upload a smaller file or use cloud storage later.",
        es: "Recib\xED el video como referencia de esta sesi\xF3n. Para publicar, luego usa un archivo m\xE1s peque\xF1o o almacenamiento en la nube.",
        fr: "J'ai recu la video comme reference de session. Pour publier, utilisez ensuite un fichier plus petit ou un stockage cloud.",
        pt: "Recebi o video como referencia desta sessao. Para publicar, depois use um arquivo menor ou armazenamento em nuvem."
      }),
      "success"
    );
    renderGuidedSummary();
    saveGuidedDraft();
  }
  function initGuidedMediaDrop() {
    if (!isPublicClientSetup || !guidedPanel2 || !guidedChatCard) return;
    const dropTargets = [guidedPanel2, guidedChatCard, guidedChat].filter(Boolean);
    let dragDepth = 0;
    const setActive = (active) => {
      guidedChatCard.dataset.dropLabel = langText({
        en: "Drop images or videos for LYRA",
        es: "Suelta im\xE1genes o videos para LYRA",
        fr: "D\xE9posez images ou vid\xE9os pour LYRA",
        pt: "Solte imagens ou videos para a LYRA"
      });
      guidedPanel2.classList.toggle("media-drop-active", active);
      setAssistantState(active ? "listening" : "neutral");
    };
    dropTargets.forEach((target) => {
      target.addEventListener("dragenter", (event) => {
        if (!event.dataTransfer?.types?.includes("Files")) return;
        event.preventDefault();
        event.stopPropagation();
        dragDepth += 1;
        setActive(true);
      });
      target.addEventListener("dragover", (event) => {
        if (!event.dataTransfer?.types?.includes("Files")) return;
        event.preventDefault();
        event.stopPropagation();
        event.dataTransfer.dropEffect = "copy";
        setActive(true);
      });
      target.addEventListener("dragleave", (event) => {
        event.stopPropagation();
        dragDepth = Math.max(0, dragDepth - 1);
        if (!dragDepth) setActive(false);
      });
      target.addEventListener("drop", async (event) => {
        event.preventDefault();
        event.stopPropagation();
        dragDepth = 0;
        setActive(false);
        const files = Array.from(event.dataTransfer?.files || []);
        await receiveGuidedDroppedFiles(files);
      });
    });
  }
  async function receiveGuidedDroppedFiles(files) {
    const supported = arrayValue2(files).filter((file) => file?.type?.startsWith("image/") || file?.type?.startsWith("video/"));
    if (!supported.length) {
      appendChatMessage("assistant", langText({
        en: "Drop images or videos here and I will use them as visual references.",
        es: "Arrastra im\xE1genes o videos aqu\xED y los usar\xE9 como referencias visuales.",
        fr: "Deposez ici des images ou videos et je les utiliserai comme references visuelles.",
        pt: "Arraste imagens ou videos aqui e eu usarei como referencias visuais."
      }), "alert");
      return;
    }
    const images = supported.filter((file) => file.type.startsWith("image/"));
    const videos = supported.filter((file) => file.type.startsWith("video/"));
    const logoFile = images.find((file) => /logo|brand|marca/i.test(file.name || "")) || (!builderState.guidedState.logoUrl && builderState.guidedStep === "preferredColors" && images.length === 1 ? images[0] : null);
    const photoFiles = images.filter((file) => file !== logoFile);
    if (logoFile) await processGuidedLogoFile(logoFile);
    if (photoFiles.length) await processGuidedPhotoFiles(photoFiles);
    if (videos.length) await processGuidedVideoFiles(videos);
    guidedStatusText.textContent = langText({
      en: "Media received. LYRA will use it as visual direction.",
      es: "Archivo recibido. LYRA lo usar\xE1 como direcci\xF3n visual.",
      fr: "Media recu. LYRA l'utilisera comme direction visuelle.",
      pt: "Arquivo recebido. A LYRA usara como direcao visual."
    });
  }
  async function extractPaletteFromDataUrl(dataUrl) {
    if (!dataUrl || !dataUrl.startsWith("data:image")) return [];
    const image = await loadImage(dataUrl);
    const canvas = document.createElement("canvas");
    const size = 96;
    canvas.width = size;
    canvas.height = size;
    const context = canvas.getContext("2d", { willReadFrequently: true });
    if (!context) return [];
    context.drawImage(image, 0, 0, size, size);
    const pixels = context.getImageData(0, 0, size, size).data;
    const buckets = /* @__PURE__ */ new Map();
    for (let i = 0; i < pixels.length; i += 16) {
      const r = pixels[i];
      const g = pixels[i + 1];
      const b = pixels[i + 2];
      const a = pixels[i + 3];
      if (a < 150) continue;
      const brightness = (r + g + b) / 3;
      if (brightness > 244 || brightness < 18) continue;
      const saturation = Math.max(r, g, b) - Math.min(r, g, b);
      if (saturation < 18 && brightness > 210) continue;
      const key = [r, g, b].map((channel) => Math.round(channel / 24) * 24).join(",");
      buckets.set(key, (buckets.get(key) || 0) + 1);
    }
    return Array.from(buckets.entries()).sort((a, b) => b[1] - a[1]).map(([key]) => key.split(",").map(Number)).reduce((palette, rgb) => {
      if (palette.length >= 5) return palette;
      const isDifferent = palette.every((hex) => colorDistance(rgb, hexToRgb(hex)) > 54);
      if (isDifferent) palette.push(rgbToHex(...rgb));
      return palette;
    }, []);
  }
  async function analyzeLogoBrand(dataUrl, context = {}) {
    const extractedColors = await extractPaletteFromDataUrl(dataUrl);
    return createBrandSystem({
      ...context,
      extractedColors,
      logoUrl: context.logoUrl || dataUrl || ""
    });
  }
  function createBrandSystem(input = {}) {
    const briefText = [
      input.businessName,
      input.industry,
      input.tone,
      arrayValue2(input.preferredColors).join(" ")
    ].join(" ");
    if (briefRequestsCyberpunk(briefText)) {
      return {
        ...DEFAULT_BRAND,
        logoUrl: input.logoUrl || "",
        primaryColor: "#00f5ff",
        secondaryColor: "#ff2bd6",
        accentColor: "#a855f7",
        backgroundColor: "#070714",
        surfaceColor: "#111126",
        textColor: "#f8fbff",
        mutedTextColor: "#a7b0ff",
        borderColor: "rgba(0, 245, 255, 0.24)",
        buttonColor: "#00f5ff",
        buttonTextColor: "#050814",
        styleDirection: "cyberpunk neon marketplace",
        fontPairing: { heading: "Space Grotesk", body: "Inter" },
        borderRadius: "10px",
        shadowStyle: "0 22px 70px rgba(0, 245, 255, 0.22)",
        extractedColors: [],
        paletteSource: "ai_style_keyword"
      };
    }
    const extractedColors = arrayValue2(input.extractedColors).map((color) => resolveColor(color, "")).filter(Boolean);
    const preferredColors = arrayValue2(input.preferredColors).map((color) => resolveColor(color, "")).filter(Boolean);
    const sourceColors = [...extractedColors, ...preferredColors].filter((color, index, list) => list.indexOf(color) === index);
    const primary = selectAccessibleBrandColor(sourceColors, DEFAULT_BRAND.primaryColor);
    const secondarySource = sourceColors.find((color) => colorDistance(hexToRgb(color), hexToRgb(primary)) > 72) || rotateHue(primary, 26);
    const accent = ensureContrastColor(adjustColor(secondarySource, { saturation: 1.16, lightness: -0.02 }), "#ffffff", 3) || primary;
    const background = mixColors(primary, "#ffffff", 0.06);
    const surface = "#ffffff";
    const text = contrastRatio("#101828", background) >= 7 ? "#101828" : "#0b1220";
    const muted = ensureContrastColor(mixColors(text, background, 0.42), background, 4.5) || "#475467";
    const secondary = mixColors(primary, "#ffffff", 0.84);
    const button = contrastRatio(primary, "#ffffff") >= 4.5 ? primary : darkenUntilContrast(primary, "#ffffff", 4.5);
    const businessText = `${input.businessName || ""} ${input.industry || ""} ${input.tone || ""}`.toLowerCase();
    const isPremium = /premium|luxury|lujo|elegant|elegante|boutique/.test(businessText);
    const isOperational = /service|servicio|repair|clean|limpieza|contractor|professional|legal|account/.test(businessText);
    const radius = isPremium ? "6px" : isOperational ? "8px" : "12px";
    return {
      ...DEFAULT_BRAND,
      logoUrl: input.logoUrl || "",
      primaryColor: primary,
      secondaryColor: secondary,
      accentColor: accent,
      backgroundColor: background,
      surfaceColor: surface,
      textColor: text,
      mutedTextColor: muted,
      borderColor: mixColors(text, background, 0.14),
      buttonColor: button,
      buttonTextColor: contrastColor(button),
      styleDirection: isPremium ? "refined premium" : isOperational ? "trustworthy conversion-focused" : "modern professional",
      fontPairing: isPremium ? { heading: "Playfair Display", body: "Inter" } : { heading: "Inter", body: "Inter" },
      borderRadius: radius,
      shadowStyle: `0 18px 48px ${hexToRgba(text, 0.1)}`,
      extractedColors,
      paletteSource: extractedColors.length ? "uploaded_logo" : "guided_preferences"
    };
  }
  function selectAccessibleBrandColor(colors, fallback) {
    const ranked = colors.map((color) => ({ color, score: brandColorScore(color) })).filter((item) => contrastRatio(item.color, "#ffffff") >= 3 || contrastRatio(item.color, "#101828") >= 3).sort((a, b) => b.score - a.score);
    return ranked[0]?.color || fallback;
  }
  function brandColorScore(color) {
    const [r, g, b] = hexToRgb(color);
    const max = Math.max(r, g, b);
    const min = Math.min(r, g, b);
    const saturation = max - min;
    const brightness = (r + g + b) / 3;
    const contrast = Math.max(contrastRatio(color, "#ffffff"), contrastRatio(color, "#101828"));
    return saturation * 1.2 + contrast * 20 - Math.abs(brightness - 118) * 0.35;
  }
  function ensureContrastColor(color, background, minRatio) {
    if (contrastRatio(color, background) >= minRatio) return color;
    const darker = darkenUntilContrast(color, background, minRatio);
    if (contrastRatio(darker, background) >= minRatio) return darker;
    const lighter = lightenUntilContrast(color, background, minRatio);
    return contrastRatio(lighter, background) >= minRatio ? lighter : color;
  }
  function darkenUntilContrast(color, background, minRatio) {
    let current = color;
    for (let i = 0; i < 18 && contrastRatio(current, background) < minRatio; i += 1) {
      current = mixColors(current, "#000000", 0.12);
    }
    return current;
  }
  function lightenUntilContrast(color, background, minRatio) {
    let current = color;
    for (let i = 0; i < 18 && contrastRatio(current, background) < minRatio; i += 1) {
      current = mixColors(current, "#ffffff", 0.12);
    }
    return current;
  }
  function contrastColor(color) {
    return contrastRatio(color, "#ffffff") >= contrastRatio(color, "#101828") ? "#ffffff" : "#101828";
  }
  function contrastRatio(a, b) {
    const l1 = relativeLuminance(hexToRgb(resolveColor(a, "#000000")));
    const l2 = relativeLuminance(hexToRgb(resolveColor(b, "#ffffff")));
    const light = Math.max(l1, l2);
    const dark = Math.min(l1, l2);
    return (light + 0.05) / (dark + 0.05);
  }
  function relativeLuminance(rgb) {
    const [r, g, b] = rgb.map((value) => {
      const channel = value / 255;
      return channel <= 0.03928 ? channel / 12.92 : Math.pow((channel + 0.055) / 1.055, 2.4);
    });
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  }
  function mixColors(a, b, amount = 0.5) {
    const first = hexToRgb(resolveColor(a, "#000000"));
    const second = hexToRgb(resolveColor(b, "#ffffff"));
    const mixed = first.map((channel, index) => Math.round(channel * (1 - amount) + second[index] * amount));
    return rgbToHex(mixed[0], mixed[1], mixed[2]);
  }
  function adjustColor(color, options = {}) {
    const [h, s, l] = rgbToHsl(...hexToRgb(resolveColor(color, DEFAULT_BRAND.primaryColor)));
    return hslToHex(
      h,
      Math.max(0, Math.min(1, s * (options.saturation || 1))),
      Math.max(0, Math.min(1, l + (options.lightness || 0)))
    );
  }
  function rotateHue(color, degrees) {
    const [h, s, l] = rgbToHsl(...hexToRgb(resolveColor(color, DEFAULT_BRAND.primaryColor)));
    return hslToHex((h + degrees + 360) % 360, s, l);
  }
  function rgbToHsl(r, g, b) {
    r /= 255;
    g /= 255;
    b /= 255;
    const max = Math.max(r, g, b);
    const min = Math.min(r, g, b);
    let h = 0;
    let s = 0;
    const l = (max + min) / 2;
    if (max !== min) {
      const d = max - min;
      s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
      h = max === r ? (g - b) / d + (g < b ? 6 : 0) : max === g ? (b - r) / d + 2 : (r - g) / d + 4;
      h *= 60;
    }
    return [h, s, l];
  }
  function hslToHex(h, s, l) {
    const c = (1 - Math.abs(2 * l - 1)) * s;
    const x = c * (1 - Math.abs(h / 60 % 2 - 1));
    const m = l - c / 2;
    const [r, g, b] = h < 60 ? [c, x, 0] : h < 120 ? [x, c, 0] : h < 180 ? [0, c, x] : h < 240 ? [0, x, c] : h < 300 ? [x, 0, c] : [c, 0, x];
    return rgbToHex(Math.round((r + m) * 255), Math.round((g + m) * 255), Math.round((b + m) * 255));
  }
  function hexToRgba(hex, alpha) {
    const [r, g, b] = hexToRgb(resolveColor(hex, "#101828"));
    return `rgba(${r}, ${g}, ${b}, ${alpha})`;
  }
  function loadImage(src) {
    return new Promise((resolve, reject) => {
      const image = new Image();
      image.onload = () => resolve(image);
      image.onerror = reject;
      image.src = src;
    });
  }
  function rgbToHex(r, g, b) {
    return `#${[r, g, b].map((value) => Math.max(0, Math.min(255, value)).toString(16).padStart(2, "0")).join("")}`;
  }
  function hexToRgb(hex) {
    const normalized = String(hex || "").replace("#", "");
    if (normalized.length !== 6) return [0, 0, 0];
    return [0, 2, 4].map((index) => parseInt(normalized.slice(index, index + 2), 16));
  }
  function colorDistance(a, b) {
    return Math.sqrt(
      Math.pow((a[0] || 0) - (b[0] || 0), 2) + Math.pow((a[1] || 0) - (b[1] || 0), 2) + Math.pow((a[2] || 0) - (b[2] || 0), 2)
    );
  }
  function applyBrandToCurrentSchema(brand) {
    if (!builderState.currentSchema || !brand) return;
    builderState.currentSchema = applyBrandSystemToSchema(
      builderState.currentSchema,
      brand,
      { forceBrandOverride: true }
    );
    builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
    renderEditor();
    renderPreview();
  }
  async function reviewAndGenerateFromGuided() {
    const ownsGenerationState = !builderState.isGeneratingWebsite;
    if (ownsGenerationState) builderState.isGeneratingWebsite = true;
    renderLiveSitePreview();
    syncGuidedStateFromSummary();
    normalizeGuidedStateBeforeGenerate();
    applyGuidedStateToForm();
    const previousText = guidedGenerateButton.textContent;
    setGuidedGenerateControlsBusy(true, langText({
      en: "Saving request...",
      es: "Guardando solicitud...",
      fr: "Enregistrement de la demande...",
      pt: "Salvando solicita\xE7\xE3o..."
    }));
    setGuidedBuildPhase("save");
    guidedStatusText.textContent = t("savingRequest");
    try {
      const saved = await saveGuidedClientRequest();
      if (saved.request_id) builderState.currentRequestId = saved.request_id;
      guidedStatusText.textContent = saved.storage_status === "stored" ? `Client request saved: ${saved.request_number || saved.request_id}` : t("requestNotSaved");
    } catch (error) {
      guidedStatusText.textContent = t("requestNotSaved");
    }
    setGuidedGenerateControlsBusy(true, t("generating"));
    setGuidedBuildPhase("strategy");
    guidedStatusText.textContent = t("generatingLong");
    const generated = await generateWebsite(guidedGenerateButton);
    if (generated && builderState.currentSchema) {
      setGuidedBuildPhase("ready");
      const successMessage = langText({
        en: "Draft generated. You can review and edit it now.",
        es: "Borrador generado. Ya puedes revisarlo y editarlo.",
        fr: "Brouillon g\xE9n\xE9r\xE9. Vous pouvez maintenant le r\xE9viser et le modifier.",
        pt: "Rascunho gerado. Agora voc\xEA pode revisar e editar."
      });
      guidedStatusText.textContent = successMessage;
      appendChatMessage("assistant", successMessage, "success");
      renderLiveSitePreview();
      showGeneratedClientPreview();
    } else if (generated === "needs_more_info") {
      const missingQuestion = builderState.pendingServerIntakeGate?.next_question || "";
      const missingNames = arrayValue2(builderState.pendingServerIntakeGate?.missing_fields).join(", ");
      guidedStatusText.textContent = missingQuestion || langText({
        en: `Before generating, LYRA still needs: ${missingNames || "one required detail"}.`,
        es: `Antes de generar, LYRA todav\xEDa necesita: ${missingNames || "un dato obligatorio"}.`,
        fr: `Avant de g\xE9n\xE9rer, LYRA a encore besoin de : ${missingNames || "une information obligatoire"}.`,
        pt: `Antes de gerar, a LYRA ainda precisa de: ${missingNames || "um dado obrigat\xF3rio"}.`
      });
    } else {
      setGuidedBuildPhase("error");
      guidedStatusText.textContent = langText({
        en: "LYRA could not create a draft yet. Try again or keep chatting with more details.",
        es: "LYRA no pudo crear el borrador todav\xEDa. Intenta otra vez o agrega m\xE1s detalles.",
        fr: "LYRA n'a pas encore pu cr\xE9er de brouillon. R\xE9essayez ou ajoutez plus de d\xE9tails.",
        pt: "A LYRA ainda n\xE3o conseguiu criar o rascunho. Tente novamente ou adicione mais detalhes."
      });
    }
    guidedGenerateButton.textContent = previousText || t("reviewGenerate");
    setGuidedGenerateControlsBusy(false);
    if (ownsGenerationState) {
      builderState.isGeneratingWebsite = false;
      renderLiveSitePreview();
    }
  }
  function setGuidedGenerateControlsBusy(isBusy, label = "") {
    const readyLabel = langText({
      en: "Generate website now",
      es: "Generar pagina ahora",
      fr: "Generer maintenant",
      pt: "Gerar site agora"
    });
    document.querySelectorAll("[data-chat-generate]").forEach((button) => {
      button.disabled = Boolean(isBusy);
      button.setAttribute("aria-busy", isBusy ? "true" : "false");
      button.textContent = isBusy ? label || t("generating") : readyLabel;
    });
    if (guidedGenerateButton) {
      guidedGenerateButton.disabled = Boolean(isBusy);
      guidedGenerateButton.setAttribute("aria-busy", isBusy ? "true" : "false");
      if (isBusy) guidedGenerateButton.textContent = label || t("generating");
    }
  }
  async function handleGuidedGenerateButton(event) {
    event?.preventDefault?.();
    event?.stopPropagation?.();
    if (builderState.isGeneratingWebsite) return;
    if (isPublicClientSetup) {
      const authAction = generationAuthAction({
        hasCredential: hasStudioAccountSession(),
        workspaceUnlocked: isClientWorkspaceUnlocked()
      });
      if (authAction === "prompt_auth") {
        promptAccountBeforeGenerate();
        return;
      }
      if (authAction === "unlock_and_continue") {
        markClientWorkspaceUnlocked();
        closeStudioAuthGate();
      }
    }
    syncGuidedStateFromSummary();
    normalizeGuidedStateBeforeGenerate();
    const requiredMissing = missingGuidedSteps();
    if (requiredMissing.length) {
      const nextMissing = requiredMissing[0];
      builderState.guidedStep = nextMissing;
      document.body.classList.remove("review-details-open", "final-review-mode");
      appendChatMessage("assistant", langText({
        en: `I still need one key detail before generating:

${guidedQuestion(nextMissing)}`,
        es: `Todav\xEDa necesito un dato clave antes de generar:

${guidedQuestion(nextMissing)}`,
        fr: `Il me manque encore un d\xE9tail cl\xE9 avant de g\xE9n\xE9rer :

${guidedQuestion(nextMissing)}`,
        pt: `Ainda preciso de um detalhe importante antes de gerar:

${guidedQuestion(nextMissing)}`
      }), "alert");
      guidedStatusText.textContent = langText({
        en: "Answer the missing detail, then generate.",
        es: "Responde ese dato y luego genera.",
        fr: "R\xE9pondez \xE0 ce d\xE9tail, puis g\xE9n\xE9rez.",
        pt: "Responda esse detalhe e depois gere."
      });
      renderGuidedSummary();
      refreshQuickChips();
      guidedReply2.focus();
      return;
    }
    builderState.guidedStep = "review";
    document.body.classList.remove("review-details-open");
    renderGuidedSummary();
    setGuidedGenerateControlsBusy(true, t("generating"));
    setGuidedBuildPhase("save");
    guidedStatusText.textContent = t("generatingLong");
    builderState.isGeneratingWebsite = true;
    renderLiveSitePreview();
    try {
      await reviewAndGenerateFromGuided();
    } catch (error) {
      console.error("Guided website generation failed", error);
      const message = `${t("generateError")}: ${shortError(error?.message || error)}`;
      guidedStatusText.textContent = message;
      appendChatMessage("assistant", langText({
        en: `${message}. I could not finish the draft from this click. Try again or add one more detail and I will retry.`,
        es: `${message}. No pude terminar el borrador con este click. Intenta otra vez o agrega un detalle m\xE1s y lo vuelvo a intentar.`,
        fr: `${message}. Je n'ai pas pu terminer le brouillon avec ce clic. R\xE9essayez ou ajoutez un d\xE9tail et je recommence.`,
        pt: `${message}. N\xE3o consegui terminar o rascunho com este clique. Tente novamente ou adicione mais um detalhe e eu tento de novo.`
      }), "alert");
    } finally {
      builderState.isGeneratingWebsite = false;
      renderLiveSitePreview();
      setGuidedGenerateControlsBusy(false);
      guidedGenerateButton.textContent = t("reviewGenerate");
    }
  }
  function promptAccountBeforeGenerate() {
    syncGuidedStateFromSummary();
    normalizeGuidedStateBeforeGenerate();
    applyGuidedStateToForm();
    persistPendingStudioAccountAction("generate");
    openStudioAuthGate("generate");
    const email = builderState.guidedState.contactInfo?.email || builderState.guidedState.contactInfo?.contact || "";
    if (studioAuthEmail && email) studioAuthEmail.value = email;
    appendChatMessage("assistant", langText({
      en: "Before I generate it, connect an account so your draft is saved and you can come back later. You can use Google, Apple, or email.",
      es: "Antes de generarla, conecta una cuenta para guardar tu borrador y poder volver luego. Puedes usar Google, Apple o email.",
      fr: "Avant de g\xE9n\xE9rer, connectez un compte pour sauvegarder le brouillon et revenir plus tard. Vous pouvez utiliser Google, Apple ou email.",
      pt: "Antes de gerar, conecte uma conta para salvar o rascunho e voltar depois. Voc\xEA pode usar Google, Apple ou email."
    }), "success");
    guidedStatusText.textContent = langText({
      en: "Connect an account to save and generate the draft.",
      es: "Conecta una cuenta para guardar y generar el borrador.",
      fr: "Connectez un compte pour sauvegarder et g\xE9n\xE9rer le brouillon.",
      pt: "Conecte uma conta para salvar e gerar o rascunho."
    });
  }
  function normalizeGuidedStateBeforeGenerate() {
    const services = meaningfulOfferItems(builderState.guidedState.servicesProducts);
    if (!services.length) {
      services.push(
        builderState.guidedState.industry || builderState.guidedState.businessDescription || langText({
          en: "AI-defined offer",
          es: "oferta definida por IA",
          fr: "offre definie par IA",
          pt: "oferta definida por IA"
        })
      );
    }
    builderState.guidedState.businessName = builderState.guidedState.businessName || t("newClientWebsite");
    builderState.guidedState.industry = builderState.guidedState.industry || t("generalBusiness");
    builderState.guidedState.businessDescription = builderState.guidedState.businessDescription || langText({
      en: `Professional website for ${builderState.guidedState.businessName} focused on ${services.join(", ") || builderState.guidedState.industry}.`,
      es: `Pagina profesional para ${builderState.guidedState.businessName} enfocada en ${services.join(", ") || builderState.guidedState.industry}.`,
      fr: `Site professionnel pour ${builderState.guidedState.businessName} ax\xE9 sur ${services.join(", ") || builderState.guidedState.industry}.`,
      pt: `Site profissional para ${builderState.guidedState.businessName} focado em ${services.join(", ") || builderState.guidedState.industry}.`
    });
    builderState.guidedState.servicesProducts = services;
    builderState.guidedState.targetAudience = builderState.guidedState.targetAudience || t("letAiDecide");
    builderState.guidedState.preferredTone = builderState.guidedState.preferredTone || t("letAiDecide");
    builderState.guidedState.preferredColors = arrayValue2(builderState.guidedState.preferredColors).length ? arrayValue2(builderState.guidedState.preferredColors) : arrayValue2(builderState.guidedState.logoPalette).length ? arrayValue2(builderState.guidedState.logoPalette) : [t("letAiDecide")];
    if (builderState.guidedState.aiGeneratedLogoRequested && !builderState.guidedState.logoPreference) {
      builderState.guidedState.logoPreference = "generate_ai_logo";
    }
    builderState.guidedState.contactInfo = builderState.guidedState.contactInfo || {};
    builderState.guidedState.salesMode = builderState.guidedState.salesMode || t("letAiDecide");
    if (!builderState.guidedState.industry || builderState.guidedState.industry === t("generalBusiness")) {
      builderState.guidedState.industry = inferCommerceIndustry(builderState.guidedState);
    }
    const aiStudioPlan = refreshAiStudioPlanFromContext();
    builderState.guidedState.sitePlan = builderState.guidedState.sitePlan || buildSitePlan(builderState.forcedTemplateSelection);
    if (builderState.guidedState.sitePlan) builderState.guidedState.sitePlan.aiStudioPlan = aiStudioPlan;
  }
  function inferCommerceIndustry(state) {
    const text = [
      state.businessDescription,
      state.salesMode,
      arrayValue2(state.servicesProducts).join(" ")
    ].join(" ").toLowerCase();
    if (/(tienda|store|shop|venta|vender|comprar|producto|catalogo|catálogo|online sales|ecommerce|boutique)/.test(text)) {
      return langText({
        en: "Online store / ecommerce",
        es: "Tienda online / ecommerce",
        fr: "Boutique en ligne / ecommerce",
        pt: "Loja online / ecommerce"
      });
    }
    return t("generalBusiness");
  }
  async function saveGuidedClientRequest() {
    if (builderState.clientIntakeSession?.clientEmail) {
      const session = await createOrResumeClientIntakeSession({
        email: builderState.clientIntakeSession.clientEmail,
        name: builderState.guidedState.contactInfo?.name || builderState.guidedState.businessName || "",
        reason: "final-save"
      });
      return {
        request_id: session.requestId,
        request_number: session.requestNumber,
        storage_status: session.storageStatus
      };
    }
    const response = await fetch(CLIENT_REQUESTS_URL, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify(guidedStateForApi())
    });
    if (!response.ok) {
      throw new Error(await readErrorMessage(response));
    }
    return response.json();
  }
  function importQuickFormToGuidedState() {
    const data = new FormData(form2);
    builderState.guidedState = {
      ...builderState.guidedState,
      businessName: data.get("business_name")?.toString().trim() || builderState.guidedState.businessName,
      businessDescription: data.get("business_description")?.toString().trim() || builderState.guidedState.businessDescription,
      industry: data.get("industry")?.toString().trim() || builderState.guidedState.industry,
      location: data.get("location")?.toString().trim() || builderState.guidedState.location,
      servicesProducts: splitCommaOrLines(data.get("services_products")?.toString() || "").length ? splitCommaOrLines(data.get("services_products")?.toString() || "") : builderState.guidedState.servicesProducts,
      targetAudience: data.get("target_audience")?.toString().trim() || builderState.guidedState.targetAudience,
      preferredTone: data.get("preferred_tone")?.toString().trim() || builderState.guidedState.preferredTone,
      preferredColors: splitCommaOrLines(data.get("preferred_colors")?.toString() || "").length ? splitCommaOrLines(data.get("preferred_colors")?.toString() || "") : builderState.guidedState.preferredColors,
      contactInfo: Object.keys(parseKeyValueLines(data.get("contact_info")?.toString() || "")).length ? parseKeyValueLines(data.get("contact_info")?.toString() || "") : builderState.guidedState.contactInfo,
      logoUrl: data.get("logo_url")?.toString().trim() || builderState.guidedState.logoUrl,
      photoUrls: splitLines(data.get("photo_urls")?.toString() || "").length ? splitLines(data.get("photo_urls")?.toString() || "") : builderState.guidedState.photoUrls,
      selectedLanguage: builderState.selectedLanguage,
      hasLogo: Boolean(data.get("logo_url")?.toString().trim() || builderState.guidedState.logoUrl),
      hasPhotos: Boolean(splitLines(data.get("photo_urls")?.toString() || "").length || builderState.guidedState.photoUrls.length),
      desiredDomain: data.get("desired_domain")?.toString().trim() || builderState.guidedState.desiredDomain
    };
  }
  function applyGuidedStateToForm() {
    setInputValue("business_name", builderState.guidedState.businessName);
    setInputValue("business_description", builderState.guidedState.businessDescription);
    setInputValue("industry", builderState.guidedState.industry);
    setInputValue("location", builderState.guidedState.location);
    setInputValue("services_products", arrayValue2(builderState.guidedState.servicesProducts).join("\n"));
    setInputValue("target_audience", builderState.guidedState.targetAudience);
    setInputValue("preferred_tone", builderState.guidedState.preferredTone);
    setInputValue("preferred_colors", arrayValue2(builderState.guidedState.preferredColors).join(", "));
    setInputValue("contact_info", contactInfoToLines(builderState.guidedState.contactInfo));
    setInputValue("desired_domain", builderState.guidedState.desiredDomain);
    setInputValue("logo_url", builderState.guidedState.logoUrl);
    setInputValue("logo_preference", builderState.guidedState.logoPreference || (builderState.guidedState.aiGeneratedLogoRequested ? "generate_ai_logo" : ""));
    setInputValue("photo_urls", arrayValue2(builderState.guidedState.photoUrls).join("\n"));
  }
  function renderGuidedSummary() {
    syncTemplateSelectionFromGuidedContext();
    builderState.guidedStep = normalizeGuidedStepForCurrentState(builderState.guidedStep);
    if (builderState.restoredGuidedDraftInfo && builderState.hasBackendIntakeSignal) {
      builderState.restoredGuidedDraftInfo.completionPercent = guidedCompletionPercent();
      builderState.restoredGuidedDraftInfo.missing = missingGuidedSteps();
      if (builderState.restoredDraftNoticeCard) renderRestoredDraftNotice({ force: true });
    }
    syncLyraExperienceMode();
    document.querySelectorAll("[data-summary-field]").forEach((field) => {
      const key = field.dataset.summaryField;
      const value = builderState.guidedState[key];
      if (key === "selectedLanguage") {
        field.value = builderState.selectedLanguage;
      } else if (Array.isArray(value)) {
        field.value = value.join("\n");
      } else if (key === "contactInfo") {
        field.value = contactInfoToLines(value);
      } else {
        field.value = value || "";
      }
    });
    const stepIndex = displayStepIndex(builderState.guidedStep);
    const isFinalReview = builderState.guidedStep === "review";
    if (isFinalReview && builderState.assistantState !== "success") setAssistantState("success");
    guidedStepLabel.textContent = isFinalReview ? t("reviewStep") : `${t("step")} ${stepIndex} ${t("of")} 7`;
    guidedProgressBar.style.width = `${Math.min(100, Math.round(stepIndex / 7 * 100))}%`;
    renderGuidedStepRail(stepIndex);
    document.body.classList.toggle("ready-chat-mode", isFinalReview);
    document.body.classList.remove("final-review-mode");
    guidedGenerateButton.textContent = isFinalReview ? t("generateMyWebsite") : t("reviewGenerate");
    currentInfoPreview.textContent = compactCollectedPreview();
    currentInfoMeta.textContent = conversationProgressLabel();
    renderGuidedCoachCard();
    renderLiveSitePreview();
    renderGuidedBriefReview();
    renderAssetPreviews();
    renderSelectedDomainState();
    updateAssetPromptVisibility();
    renderSitePlanInChatIfNeeded();
    saveGuidedDraft();
  }
  function syncLyraExperienceMode() {
    if (!isPublicClientSetup) return;
    const hasGeneratedResult = Boolean(
      builderState.currentSchema || document.body.classList.contains("generated-preview-open") || document.body.classList.contains("client-preview-mode")
    );
    document.body.classList.toggle("lyra-result-mode", hasGeneratedResult);
    document.body.classList.toggle("lyra-conversation-mode", !hasGeneratedResult);
  }
  function aiBuildFocusLine() {
    const name = builderState.guidedState.businessName || t("newClientWebsite");
    const products = arrayValue2(builderState.guidedState.servicesProducts).filter(Boolean).slice(0, 3);
    if (products.length) {
      return langText({
        en: `${name}: LYRA is shaping the site around ${products.join(", ")}.`,
        es: `${name}: LYRA esta armando el sitio alrededor de ${products.join(", ")}.`,
        fr: `${name} : LYRA structure le site autour de ${products.join(", ")}.`,
        pt: `${name}: a LYRA esta montando o site em torno de ${products.join(", ")}.`
      });
    }
    return langText({
      en: `${name}: LYRA will use the conversation as strategy, not as raw page copy.`,
      es: `${name}: LYRA usara la conversacion como estrategia, no como texto crudo en la pagina.`,
      fr: `${name} : LYRA utilisera la conversation comme strategie, pas comme texte brut.`,
      pt: `${name}: a LYRA usara a conversa como estrategia, nao como texto bruto.`
    });
  }
  function aiCatalogStrategyLine(plan) {
    const catalog = `${plan.catalogType || builderState.forcedTemplateSelection?.catalogType || ""}`.toLowerCase();
    if (/single_vendor_dense|dense_retail|retail/.test(catalog)) {
      return langText({
        en: "The products look varied but owned by one business, so LYRA is using a search-first retail store structure instead of a multi-vendor marketplace.",
        es: "Los productos se ven variados pero de un solo negocio, asi que LYRA usa una tienda retail con busqueda, no un marketplace multi-vendedor.",
        fr: "Les produits semblent varies mais geres par une seule entreprise; LYRA utilise donc une boutique retail avec recherche.",
        pt: "Os produtos parecem variados mas de um unico negocio, entao a LYRA usa uma loja retail com busca."
      });
    }
    if (/dense_marketplace|marketplace/.test(catalog)) {
      return langText({
        en: "The products look varied, so LYRA is using a search-first marketplace structure instead of a single-product landing.",
        es: "Los productos se ven variados, asi que LYRA usa una estructura tipo marketplace con busqueda, no una landing de producto unico.",
        fr: "Les produits semblent varies, donc LYRA utilise une structure marketplace avec recherche.",
        pt: "Os produtos parecem variados, entao a LYRA usa uma estrutura marketplace com busca."
      });
    }
    if (/premium_editorial/.test(catalog)) {
      return langText({
        en: "The offer looks focused, so LYRA is using a premium product-line presentation with strong story and conversion sections.",
        es: "La oferta se ve enfocada, asi que LYRA usa una presentacion premium de linea de producto con historia y conversion.",
        fr: "L'offre semble ciblee, donc LYRA utilise une presentation premium de gamme produit.",
        pt: "A oferta parece focada, entao a LYRA usa uma apresentacao premium de linha de produto."
      });
    }
    if (/service|booking|lead|legal|medical/.test(catalog)) {
      return langText({
        en: "The request is service-led, so LYRA is prioritizing trust, proof, process and a clear contact path.",
        es: "La solicitud es de servicios, asi que LYRA prioriza confianza, prueba, proceso y contacto claro.",
        fr: "La demande est orientee service, donc LYRA priorise confiance, preuves et contact clair.",
        pt: "O pedido e de servicos, entao a LYRA prioriza confianca, prova, processo e contato."
      });
    }
    return langText({
      en: "LYRA is matching the structure to the business type and will keep the generated site editable.",
      es: "LYRA esta ajustando la estructura al tipo de negocio y mantendra el sitio editable.",
      fr: "LYRA adapte la structure au type d'entreprise et gardera le site modifiable.",
      pt: "A LYRA esta ajustando a estrutura ao tipo de negocio e mantera o site editavel."
    });
  }
  function aiVisualStrategyLine(plan) {
    const colorSource = arrayValue2(builderState.guidedState.logoPalette).length ? langText({ en: "logo palette", es: "paleta del logo", fr: "palette du logo", pt: "paleta do logo" }) : arrayValue2(builderState.guidedState.preferredColors).length ? arrayValue2(builderState.guidedState.preferredColors).join(", ") : t("letAiDecide");
    return langText({
      en: `Design direction: ${builderState.guidedState.preferredTone || "AI-selected"} tone, ${colorSource} colors, customer-facing copy rewritten from the conversation.`,
      es: `Direccion visual: tono ${builderState.guidedState.preferredTone || "elegido por IA"}, colores ${colorSource}, copy comercial reescrito desde la conversacion.`,
      fr: `Direction visuelle : ton ${builderState.guidedState.preferredTone || "choisi par IA"}, couleurs ${colorSource}, copy reecrit depuis la conversation.`,
      pt: `Direcao visual: tom ${builderState.guidedState.preferredTone || "escolhido por IA"}, cores ${colorSource}, copy comercial reescrito da conversa.`
    });
  }
  function aiSourceSignalList() {
    const signals = [];
    if (builderState.guidedState.businessDescription) signals.push(langText({ en: "business description", es: "descripcion del negocio", fr: "description business", pt: "descricao do negocio" }));
    if (arrayValue2(builderState.guidedState.servicesProducts).length) signals.push(langText({ en: "products/services", es: "productos/servicios", fr: "produits/services", pt: "produtos/servicos" }));
    if (builderState.guidedState.salesMode) signals.push(langText({ en: "sales flow", es: "flujo de venta", fr: "flux de vente", pt: "fluxo de venda" }));
    if (builderState.guidedState.logoUrl || arrayValue2(builderState.guidedState.logoPalette).length) signals.push(langText({ en: "logo and palette", es: "logo y paleta", fr: "logo et palette", pt: "logo e paleta" }));
    if (builderState.guidedState.contactInfo && Object.keys(builderState.guidedState.contactInfo).length) signals.push(langText({ en: "contact routes", es: "vias de contacto", fr: "contacts", pt: "contatos" }));
    return signals.length ? signals : [langText({ en: "conversation context", es: "contexto de la conversacion", fr: "contexte conversation", pt: "contexto da conversa" })];
  }
  function humanizePlanFeature(feature) {
    const labels = {
      editable_pages: { en: "Editable pages", es: "Paginas editables", fr: "Pages modifiables", pt: "Paginas editaveis" },
      editable_sections: { en: "Editable sections", es: "Secciones editables", fr: "Sections modifiables", pt: "Secoes editaveis" },
      editable_brand_system: { en: "Editable brand", es: "Marca editable", fr: "Marque modifiable", pt: "Marca editavel" },
      contact_capture: { en: "Contact capture", es: "Captura contacto", fr: "Capture contact", pt: "Captura contato" },
      catalog_manager: { en: "Catalog manager", es: "Catalogo editable", fr: "Catalogue modifiable", pt: "Catalogo editavel" },
      categories: { en: "Categories", es: "Categorias", fr: "Categories", pt: "Categorias" },
      search: { en: "Search", es: "Busqueda", fr: "Recherche", pt: "Busca" },
      filters: { en: "Filters", es: "Filtros", fr: "Filtres", pt: "Filtros" },
      product_cards: { en: "Product cards", es: "Tarjetas producto", fr: "Fiches produit", pt: "Cards produto" },
      cart: { en: "Cart", es: "Carrito", fr: "Panier", pt: "Carrinho" },
      checkout_path: { en: "Checkout path", es: "Ruta de pago", fr: "Parcours paiement", pt: "Fluxo pagamento" },
      customer_account: { en: "Customer account", es: "Cuenta cliente", fr: "Compte client", pt: "Conta cliente" },
      featured_deals: { en: "Featured deals", es: "Ofertas destacadas", fr: "Offres en avant", pt: "Ofertas destaque" },
      deal_sections: { en: "Deal sections", es: "Secciones ofertas", fr: "Sections offres", pt: "Secoes ofertas" },
      best_sellers: { en: "Best sellers", es: "Mas vendidos", fr: "Meilleures ventes", pt: "Mais vendidos" },
      newsletter_or_deal_subscription: { en: "Deal subscription", es: "Suscripcion ofertas", fr: "Abonnement offres", pt: "Assinatura ofertas" },
      ratings_reviews: { en: "Ratings/reviews", es: "Ratings/rese\xF1as", fr: "Avis/notes", pt: "Avaliacoes" },
      wishlist_ready: { en: "Wishlist ready", es: "Favoritos listo", fr: "Favoris pr\xEAt", pt: "Favoritos pronto" },
      booking_request_flow: { en: "Booking flow", es: "Flujo reservas", fr: "Flux reservation", pt: "Fluxo agenda" },
      menu_categories: { en: "Menu categories", es: "Categorias menu", fr: "Categories menu", pt: "Categorias menu" },
      quote_request_flow: { en: "Quote flow", es: "Flujo cotizacion", fr: "Flux devis", pt: "Fluxo orcamento" }
    };
    return langText(labels[feature] || {
      en: String(feature || "").replace(/_/g, " "),
      es: String(feature || "").replace(/_/g, " "),
      fr: String(feature || "").replace(/_/g, " "),
      pt: String(feature || "").replace(/_/g, " ")
    });
  }
  function openReviewDetails() {
    syncGuidedStateFromSummary();
    document.body.classList.add("review-details-open");
    document.querySelectorAll(".summary-panel .mobile-review-details").forEach((details) => {
      details.open = true;
    });
    renderGuidedSummary();
  }
  function displayStepIndex(step) {
    if (["businessName", "businessDescription", "industry"].includes(step)) return 1;
    if (step === "servicesProducts") return 2;
    if (step === "location") return 3;
    if (step === "salesMode") return 4;
    if (["preferredTone", "targetAudience"].includes(step)) return 5;
    if (["preferredColors", "hasLogoPhotos"].includes(step)) return 6;
    return 7;
  }
  function missingGuidedSteps() {
    if (builderState.hasBackendIntakeSignal) {
      if (builderState.backendReadyToGenerate) return [];
      const backendMissing = arrayValue2(builderState.backendMissingFields).map((slot) => mapBackendSlotToGuidedField(slot)).filter(Boolean);
      if (backendMissing.length) return [...new Set(backendMissing)];
      const fallbackStep = builderState.lastAskedGuidedField || (builderState.guidedStep !== "review" ? builderState.guidedStep : "websiteIntent");
      return [fallbackStep];
    }
    const requiredMissing = REQUIRED_GUIDED_STEPS.filter((step) => !isGuidedStepAnswered(step));
    if (requiredMissing.length) return requiredMissing;
    return SMART_GUIDED_STEP_PRIORITY.filter((step) => {
      if (!OPTIONAL_GUIDED_STEPS.has(step)) return false;
      if (step === "desiredDomain") return false;
      if (isGuidedStepAnswered(step)) return false;
      return (guidedAskedSteps.get(step) || 0) < 1;
    });
  }
  function guidedCompletionPercent() {
    if (builderState.hasBackendIntakeSignal) {
      if (builderState.backendReadyToGenerate) return 100;
      const missingCount = Math.max(1, arrayValue2(builderState.backendMissingFields).length);
      return Math.max(10, Math.min(95, Math.round((7 - Math.min(6, missingCount)) / 7 * 100)));
    }
    const requiredCompleted = REQUIRED_GUIDED_STEPS.filter((step) => isGuidedStepAnswered(step)).length;
    const optionalSteps = SMART_GUIDED_STEP_PRIORITY.filter((step) => OPTIONAL_GUIDED_STEPS.has(step) && step !== "desiredDomain");
    const optionalCompleted = optionalSteps.filter((step) => isGuidedStepAnswered(step) || (guidedAskedSteps.get(step) || 0) > 0).length;
    const requiredScore = requiredCompleted / REQUIRED_GUIDED_STEPS.length * 82;
    const optionalScore = optionalSteps.length ? optionalCompleted / optionalSteps.length * 18 : 18;
    return Math.min(100, Math.round(requiredScore + optionalScore));
  }
  function conversationProgressLabel() {
    const missing = missingGuidedSteps();
    if (!missing.length) {
      return langText({
        en: `${guidedCompletionPercent()}% \xB7 ready to review`,
        es: `${guidedCompletionPercent()}% \xB7 listo para revisar`,
        fr: `${guidedCompletionPercent()}% \xB7 pr\xEAt \xE0 v\xE9rifier`,
        pt: `${guidedCompletionPercent()}% \xB7 pronto para revisar`
      });
    }
    return langText({
      en: `${guidedCompletionPercent()}% \xB7 missing ${missing.length}`,
      es: `${guidedCompletionPercent()}% \xB7 faltan ${missing.length}`,
      fr: `${guidedCompletionPercent()}% \xB7 ${missing.length} manquant(s)`,
      pt: `${guidedCompletionPercent()}% \xB7 faltam ${missing.length}`
    });
  }
  function syncGuidedStateFromSummary() {
    document.querySelectorAll("[data-summary-field]").forEach((field) => {
      const key = field.dataset.summaryField;
      if (key === "selectedLanguage") {
        setSelectedLanguage(field.value);
      } else if (["servicesProducts", "preferredColors", "photoUrls", "videoUrls"].includes(key)) {
        builderState.guidedState[key] = splitCommaOrLines(field.value);
      } else if (key === "contactInfo") {
        builderState.guidedState[key] = parseKeyValueLines(field.value);
      } else {
        builderState.guidedState[key] = field.value.trim();
      }
    });
  }
  function mergeGuidedUpdates(updates) {
    Object.entries(updates).forEach(([key, value]) => {
      if (!(key in builderState.guidedState)) return;
      if (key === "businessName" && isInvalidBusinessNameUpdate(value)) return;
      if (key === "fieldMeta") {
        builderState.guidedState.fieldMeta = { ...builderState.guidedState.fieldMeta || {}, ...value || {} };
      } else if (key === "contactInfo") {
        builderState.guidedState.contactInfo = { ...builderState.guidedState.contactInfo, ...value || {} };
      } else if (key === "servicesProducts") {
        const incoming = meaningfulOfferItems(value);
        const existing = meaningfulOfferItems(builderState.guidedState.servicesProducts);
        if (!incoming.length && existing.length) return;
        builderState.guidedState.servicesProducts = [.../* @__PURE__ */ new Set([...existing, ...incoming])];
      } else if (["servicesProducts", "preferredColors", "photoUrls", "videoUrls"].includes(key)) {
        builderState.guidedState[key] = arrayValue2(value);
      } else {
        builderState.guidedState[key] = value || builderState.guidedState[key];
      }
    });
  }
  function isInvalidBusinessNameUpdate(value) {
    const text = String(value || "").trim();
    return text.length > 70 || /(\n|productos?|servicios?|ubicaci[oó]n|contacto|telefono|tel[eé]fono|whatsapp|instagram|colores?|vende|ofrece)/i.test(text);
  }
  function isPlaceholderBusinessName(value) {
    return /^(your business|tu negocio|votre entreprise|seu negocio|seu negócio|new client website|nueva pagina|nueva página)$/i.test(String(value || "").trim());
  }
  function guidedStateForApi() {
    const logoUrl = isCloudSafeUrl(builderState.guidedState.logoUrl) ? builderState.guidedState.logoUrl : "";
    const photoUrls = arrayValue2(builderState.guidedState.photoUrls).filter(isCloudSafeUrl);
    const videoUrls = arrayValue2(builderState.guidedState.videoUrls).filter(isCloudSafeUrl);
    const aiStudioPlan = refreshAiStudioPlanFromContext();
    const sitePlan = builderState.guidedState.sitePlan || (builderState.forcedTemplateSelection?.templateId ? buildSitePlan() : null);
    if (sitePlan && aiStudioPlan) sitePlan.aiStudioPlan = aiStudioPlan;
    const logoPreference = builderState.guidedState.logoPreference || (builderState.guidedState.aiGeneratedLogoRequested ? "generate_ai_logo" : "");
    const fieldMeta = { ...builderState.guidedState.fieldMeta || {} };
    if (logoPreference) {
      fieldMeta.logo = fieldMeta.logo || { source: "explicit", confidence: 1 };
      fieldMeta.logoPreference = fieldMeta.logoPreference || { source: "explicit", confidence: 1 };
    }
    const brand = normalizeBrand(builderState.guidedState.brand || {
      logoUrl,
      extractedColors: arrayValue2(builderState.guidedState.logoPalette)
    });
    const colorProvenance = buildColorProvenance({
      preferredColors: builderState.guidedState.preferredColors,
      logoPalette: builderState.guidedState.logoPalette,
      localBrand: brand,
      preferredColorMeta: fieldMeta.preferredColors || {}
    });
    builderState.guidedState.colorProvenance = colorProvenance;
    const payload = {
      generatedSiteId: builderState.currentSiteId || builderState.clientIntakeSession?.generatedSiteId || builderState.clientIntakeSession?.projectId || builderState.guidedState.generatedSiteId || "",
      projectId: builderState.currentSiteId || builderState.clientIntakeSession?.projectId || builderState.clientIntakeSession?.generatedSiteId || builderState.guidedState.projectId || "",
      websiteIntent: builderState.guidedState.websiteIntent,
      businessName: builderState.guidedState.businessName,
      businessDescription: builderState.guidedState.businessDescription,
      industry: builderState.guidedState.industry,
      location: builderState.guidedState.location,
      servicesProducts: arrayValue2(builderState.guidedState.servicesProducts),
      targetAudience: builderState.guidedState.targetAudience,
      preferredTone: builderState.guidedState.preferredTone,
      preferredColors: arrayValue2(builderState.guidedState.preferredColors),
      contactInfo: builderState.guidedState.contactInfo || {},
      desiredDomain: builderState.guidedState.desiredDomain,
      logoUrl,
      photoUrls,
      videoUrls,
      logoPalette: arrayValue2(builderState.guidedState.logoPalette),
      colorProvenance,
      logoPreference,
      salesFlow: builderState.guidedState.salesFlow || "",
      fieldMeta,
      brand,
      designStrategy: {
        ...createDesignStrategy({
          business_name: builderState.guidedState.businessName,
          business_description: builderState.guidedState.businessDescription,
          website_intent: builderState.guidedState.websiteIntent,
          industry: builderState.guidedState.industry,
          target_audience: builderState.guidedState.targetAudience,
          preferred_tone: builderState.guidedState.preferredTone,
          salesMode: builderState.guidedState.salesFlow || builderState.guidedState.salesMode
        }),
        ...builderState.guidedState.designStrategy || {},
        aiStudioPlan
      },
      qualityRules: DESIGN_QUALITY_RULES,
      selectedLanguage: builderState.selectedLanguage,
      hasLogo: Boolean(builderState.guidedState.hasLogo || builderState.guidedState.logoUrl),
      hasPhotos: Boolean(builderState.guidedState.hasPhotos || arrayValue2(builderState.guidedState.photoUrls).length || arrayValue2(builderState.guidedState.videoUrls).length),
      salesMode: builderState.guidedState.salesFlow || builderState.guidedState.salesMode,
      hasLogoPhotos: builderState.guidedState.hasLogoPhotos,
      sectionsPreference: builderState.guidedState.sectionsPreference,
      aiStudioPlan,
      sitePlan,
      sitePlanApproved: Boolean(builderState.guidedState.sitePlanApproved),
      designVariantOffset: Math.max(0, Number(builderState.guidedState.designVariantOffset) || 0),
      publicCopyPolicy: {
        mode: "designer_rewrite",
        visibleCopyMustBeOriginal: true,
        intakeIsStrategyOnly: true,
        forbiddenLiteralSources: ["businessDescription", "chat answers", "client notes", "internal requirements"],
        instruction: "Treat all intake answers as private creative direction. Do not paste them into visible website copy. Rewrite them into concise, polished, customer-facing titles, slogans, paragraphs, CTAs, product descriptions, and section labels in builderState.selectedLanguage."
      },
      source: "ai_guided_setup",
      status: "ready_to_generate",
      brandContextNote: "Intake answers describe what the client wants and should guide design strategy only. Never copy internal context, raw client notes, requirements, or rough answers literally into visible website copy."
    };
    const revisionInstructions = buildRevisionInstructions();
    if (revisionInstructions) {
      payload.revisionMode = revisionInstructions.mode;
      payload.requestedAdjustments = revisionInstructions.requestedAdjustments;
      payload.revisionInstructions = {
        ...revisionInstructions,
        previousSchema: void 0
      };
    }
    return payload;
  }
  async function fetchWithTimeout(url, options = {}, timeoutMs = 18e3) {
    const controller = new AbortController();
    const timer = setTimeout(() => controller.abort(), timeoutMs);
    try {
      return await fetch(url, {
        ...options,
        signal: controller.signal
      });
    } catch (error) {
      if (error?.name === "AbortError") {
        throw new Error("Request timed out. Check your connection and try again.");
      }
      throw error;
    } finally {
      clearTimeout(timer);
    }
  }
  function isCloudSafeUrl(value) {
    const text = String(value || "");
    return Boolean(text) && !text.startsWith("data:") && !text.startsWith("blob:");
  }
  function setThinking(active) {
    guidedThinking.classList.toggle("active", active);
    if (active) {
      setAssistantState("thinking");
    } else if (builderState.assistantState === "thinking") {
      setAssistantState(builderState.guidedStep === "review" ? "success" : "neutral");
    }
  }
  function chooseNextQuestionText(serverQuestion, step) {
    if (step === "review") return guidedQuestion("review");
    const fallback = guidedQuestion(step);
    const candidate = String(serverQuestion || "").trim();
    if (!candidate) return fallback;
    if (questionTargetsAnsweredField(candidate)) return fallback;
    if (isDuplicateQuestion(candidate, fallback)) return fallback;
    return candidate;
  }
  function questionTargetsAnsweredField(question) {
    const text = questionSignature(question);
    const checks = [
      ["businessName", /\b(name|nombre|nom|nome|llama|called|business)\b/],
      ["businessDescription", /\b(sells|does|vende|hace|description|descripcion|descricao|propose)\b/],
      ["servicesProducts", /\b(products|services|productos|servicios|categories|categorias)\b/],
      ["preferredColors", /\b(colors|colores|couleurs|cores|palette|paleta)\b/],
      ["preferredTone", /\b(style|tone|estilo|tono|visual|premium|modern)\b/],
      ["salesMode", /\b(online|sales|sell|venta|vender|quote|cotizacion|devis)\b/],
      ["hasLogoPhotos", /\b(logo|photo|photos|foto|fotos|image|imagen)\b/],
      ["contactInfo", /\b(contact|email|phone|whatsapp|telefono|correo)\b/]
    ];
    return checks.some(([step, pattern]) => pattern.test(text) && isGuidedStepAnswered(step));
  }
  function isDuplicateQuestion(a, b) {
    const left = questionSignature(a);
    const right = questionSignature(b);
    return Boolean(left && right && (left === right || left.includes(right) || right.includes(left)));
  }
  function questionSignature(value) {
    return String(value || "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9]+/g, " ").replace(/\b(what|which|who|where|do|does|want|quieres|tienes|que|cual|como|donde|para|the|a|el|la|los|las|un|una|de|del)\b/g, "").replace(/\s+/g, " ").trim().slice(0, 120);
  }
  function completeGuidedBriefFromMessage(message, pendingUpdates = {}) {
    const text = String(message || "").trim();
    const updates = {};
    const merged = {
      ...builderState.guidedState,
      ...pendingUpdates,
      contactInfo: { ...builderState.guidedState.contactInfo || {}, ...pendingUpdates.contactInfo || {} }
    };
    const combinedText = normalizeTemplateIntentText([
      text,
      merged.websiteIntent,
      merged.businessDescription,
      arrayValue2(merged.servicesProducts).join(" ")
    ].join(" "));
    const explicitName = extractBusinessName(text);
    if (explicitName && (!merged.businessName || isPlaceholderBusinessName(merged.businessName))) {
      updates.businessName = explicitName;
    }
    if (!merged.websiteIntent && isRichIntakeMessage(text)) {
      updates.websiteIntent = extractWebsiteIntent(text) || text.slice(0, 180);
    }
    if (!merged.businessDescription && isRichIntakeMessage(text)) {
      updates.businessDescription = text;
    }
    const inferredServices = extractServicesProducts(text);
    if (!arrayValue2(merged.servicesProducts).length && inferredServices.length) {
      updates.servicesProducts = inferredServices;
    }
    const salesMode = extractSalesMode(combinedText);
    if (!merged.salesMode && salesMode) updates.salesMode = salesMode;
    if (!merged.preferredTone && briefRequestsCyberpunk(text)) {
      updates.preferredTone = langText({
        en: "Cyberpunk, neon, high-energy marketplace",
        es: "Cyberpunk, neon, marketplace energetico",
        fr: "Cyberpunk, neon, marketplace energique",
        pt: "Cyberpunk, neon, marketplace energetico"
      });
    }
    if (!arrayValue2(merged.preferredColors).length && briefRequestsCyberpunk(text)) {
      updates.preferredColors = ["cyberpunk", "neon cyan", "magenta", "deep black"];
    }
    if (wantsAiGeneratedLogo(text)) {
      updates.aiGeneratedLogoRequested = true;
      updates.logoPreference = "generate_ai_logo";
      updates.hasLogoPhotos = langText({
        en: "Client has no logo and wants LYRA to create a simple brand mark from the business name and style.",
        es: "El cliente no tiene logo y quiere que LYRA cree una marca simple con el nombre y el estilo.",
        fr: "Le client n'a pas de logo et veut que LYRA cr\xE9e une marque simple avec le nom et le style.",
        pt: "O cliente nao tem logo e quer que a LYRA crie uma marca simples com o nome e o estilo."
      });
    }
    return updates;
  }
  var BACKEND_SLOT_TO_GUIDED_FIELD = {
    business_name: "businessName",
    business_description: "businessDescription",
    services_products: "servicesProducts",
    niche: "industry",
    sales_flow: "salesMode",
    brand_style: "preferredColors",
    logo: "hasLogoPhotos",
    target_audience: "targetAudience",
    location: "location",
    contact_info: "contactInfo"
  };
  function mapBackendSlotToGuidedField(slot) {
    return BACKEND_SLOT_TO_GUIDED_FIELD[String(slot || "").trim()] || "";
  }
  function hasExistingGuidedValue(key) {
    const value = builderState.guidedState[key];
    if (Array.isArray(value)) return value.length > 0;
    if (value && typeof value === "object") return Object.keys(value).length > 0;
    return Boolean(String(value || "").trim());
  }
  function inferGuidedUpdates(step, message) {
    if (step === "servicesProducts") {
      if (hasExistingGuidedValue("servicesProducts")) return {};
      const extracted = extractServicesProducts(message);
      return extracted.length ? { servicesProducts: extracted } : {};
    }
    if (step === "preferredColors") {
      return hasExistingGuidedValue("preferredColors") ? {} : { preferredColors: splitCommaOrLines(message) };
    }
    if (step === "contactInfo") {
      return hasExistingGuidedValue("contactInfo") ? {} : { contactInfo: parseKeyValueLines(message.includes(":") ? message : `notes: ${message}`) };
    }
    if (step === "hasLogoPhotos" && wantsAiGeneratedLogo(message, { assumeLogoContext: true })) {
      return {
        hasLogoPhotos: message,
        aiGeneratedLogoRequested: true,
        logoPreference: "generate_ai_logo"
      };
    }
    if (step === "businessName" && isRichIntakeMessage(message) && !extractBusinessName(message)) {
      return {};
    }
    const keyByStep = {
      websiteIntent: "websiteIntent",
      businessName: "businessName",
      businessDescription: "businessDescription",
      industry: "industry",
      location: "location",
      targetAudience: "targetAudience",
      preferredTone: "preferredTone",
      salesMode: "salesMode",
      hasLogoPhotos: "hasLogoPhotos",
      desiredDomain: "desiredDomain"
    };
    const key = keyByStep[step];
    if (!key) return {};
    return hasExistingGuidedValue(key) ? {} : { [key]: message };
  }
  function inferGuidedUpdatesFromAnyMessage(message, attributionStep = "") {
    const text = String(message || "").trim();
    const lower = text.toLowerCase();
    const updates = {};
    if (!builderState.guidedState.websiteIntent && isRichIntakeMessage(text)) {
      updates.websiteIntent = extractWebsiteIntent(text);
    }
    if (!builderState.guidedState.businessDescription && text.length > 45) {
      updates.businessDescription = text;
    }
    const businessName = extractBusinessName(text);
    if (businessName && (!builderState.guidedState.businessName || isPlaceholderBusinessName(builderState.guidedState.businessName))) {
      updates.businessName = businessName;
    }
    const isSalesModeAnswer = attributionStep === "salesMode";
    const industry = isSalesModeAnswer ? "" : inferIndustryFromPrompt(text);
    if (industry && !builderState.guidedState.industry) updates.industry = industry;
    const location = extractLocation(text);
    if (location && !builderState.guidedState.location) updates.location = location;
    const services = isSalesModeAnswer ? [] : extractServicesProducts(text);
    if (services.length && !arrayValue2(builderState.guidedState.servicesProducts).length) {
      updates.servicesProducts = services;
    }
    const audience = extractTargetAudience(text);
    if (audience && !builderState.guidedState.targetAudience) updates.targetAudience = audience;
    const tone = extractToneFromText(text);
    if (tone && !builderState.guidedState.preferredTone) updates.preferredTone = tone;
    const colors = extractColorsFromText(text);
    if (colors.length && !arrayValue2(builderState.guidedState.preferredColors).length) {
      updates.preferredColors = colors;
    } else if (briefRequestsCyberpunk(text) && !arrayValue2(builderState.guidedState.preferredColors).length) {
      updates.preferredColors = ["cyberpunk", "neon cyan", "magenta"];
      if (!updates.preferredTone && !builderState.guidedState.preferredTone) updates.preferredTone = "Cyberpunk, neon, energetic, modern";
    }
    const contactInfo = extractContactInfo(text);
    if (Object.keys(contactInfo).length) {
      updates.contactInfo = contactInfo;
    }
    const salesMode = extractSalesMode(lower);
    if (salesMode && !builderState.guidedState.salesMode) updates.salesMode = salesMode;
    if (/logo|foto|fotos|imagen|imagenes|photo|photos|image|images/.test(lower) && !builderState.guidedState.hasLogoPhotos) {
      const wantsGeneratedLogo = wantsAiGeneratedLogo(text, { assumeLogoContext: true });
      if (wantsGeneratedLogo) updates.aiGeneratedLogoRequested = true;
      if (wantsGeneratedLogo) updates.logoPreference = "generate_ai_logo";
      updates.hasLogoPhotos = wantsGeneratedLogo ? langText({
        en: "Client has no logo and wants LYRA to create a simple brand mark from the business name and style.",
        es: "El cliente no tiene logo y quiere que LYRA cree una marca simple con el nombre y el estilo.",
        fr: "Le client n'a pas de logo et veut que LYRA cr\xE9e une marque simple avec le nom et le style.",
        pt: "O cliente nao tem logo e quer que a LYRA crie uma marca simples com o nome e o estilo."
      }) : langText({
        en: "Client mentioned logo/photos",
        es: "El cliente mencion\xF3 logo/fotos",
        fr: "Le client a mentionn\xE9 logo/photos",
        pt: "O cliente mencionou logo/fotos"
      });
    }
    return updates;
  }
  function extractWebsiteIntent(text) {
    if (/marketplace|amazon|ebay/i.test(text)) return "Marketplace / online store";
    if (/restaurante|restaurant|menu|menú/i.test(text)) return "Restaurant menu website";
    if (/\b(cita|citas|reserva|reservas|booking|appointment)\b/i.test(text)) return "Booking website";
    if (/tienda|store|shop|ecommerce|venta online|vender online/i.test(text)) return "Online store";
    if (/servicio|services|cotizacion|cotización|quote/i.test(text)) return "Service business website";
    return text.slice(0, 180);
  }
  function extractBusinessName(text) {
    const patterns = [
      /(?:nombre(?: del negocio)?|negocio|tienda|marca|empresa)\s*(?:es|se llama|:|-)\s*([^.,;\n]+)/i,
      /(?:con el nombre|con nombre|nombre)\s+([^.,;\n]+)/i,
      /(?:se llamar[aá]|se va a llamar|sera llamado|será llamado)\s+([^.,;\n]+)/i,
      /(?:business(?: name)?|store|brand|company)\s*(?:is|called|:|-)\s*([^.,;\n]+)/i,
      /(?:llamad[oa]|named)\s+([^.,;\n]+)/i,
      /(?:se llama|called)\s+([^.,;\n]+)/i
    ];
    for (const pattern of patterns) {
      const match = text.match(pattern);
      if (match?.[1]) {
        const name = match[1].split(/\s+(?:y\s+)?(?:voy\s+a\s+vender|vamos\s+a\s+vender|va\s+a\s+vender|quiero\s+vender|necesito\s+vender|vende|vendo|vendemos|vender|ofrece|ofrecemos|hace|tiene|con|para|debe|sera|será|ser|vamos|va|ubicad[ao]|en\s+usa|desde|despacho|env[ií]o|no\s+tengo|sin\s+logo|pero|quiero|necesito|necesitamos|menu|men[uú]|pedidos?|direcci[oó]n|colores?)\b/i)[0];
        return cleanExtractedPhrase(name, 56);
      }
    }
    return "";
  }
  function extractLocation(text) {
    const patterns = [
      /(?:ubicaci[oó]n|ubicado en|est[aá] en|atiende en|localidad|ciudad|zona)\s*(?:es|:|-)?\s*([^.,;\n]+)/i,
      /(?:en|desde)\s+([A-ZÁÉÍÓÚÑ][a-záéíóúñ]+(?:\s+[A-ZÁÉÍÓÚÑ][a-záéíóúñ]+){0,2})(?:\s|,|\.|;|$)/,
      /(?:location|located in|serves|city|area)\s*(?:is|:|-)?\s*([^.,;\n]+)/i,
      /(?:in|from)\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+){0,2})(?:\s|,|\.|;|$)/
    ];
    for (const pattern of patterns) {
      const match = text.match(pattern);
      if (match?.[1]) return cleanExtractedPhrase(match[1], 70);
    }
    return "";
  }
  function extractServicesProducts(text) {
    if (isGenericCommerceIntent(text)) return [];
    if (textSuggestsBroadMarketplace(text)) {
      const candidates = [
        ["ropa", "fashion"],
        ["accesorios", "accessories"],
        ["carros", "auto accessories"],
        ["autos", "auto accessories"],
        ["automotriz", "auto accessories"],
        ["regalos", "gifts"],
        ["hogar", "home goods"],
        ["juguetes", "toys"],
        ["anime", "anime collectibles"],
        ["gadgets", "gadgets"],
        ["cosas raras", "rare finds"],
        ["inusual", "unusual products"],
        ["unusual", "unusual products"]
      ];
      const normalized = normalizeTemplateIntentText(text);
      const inferred = candidates.filter(([needle]) => normalized.includes(needle)).map(([, label]) => label);
      return [...new Set(inferred)].slice(0, 8);
    }
    const productLine = text.match(/(?:l[ií]nea|linea|colecci[oó]n|collection)\s+(?:de|para)\s+([^.;,\n]+)/i)?.[1];
    if (productLine) {
      return splitOfferItems(productLine).slice(0, 8);
    }
    const patterns = [
      /(?:productos?|servicios?|vende\b|vender\b|vendo\b|vendemos\b|venta de|ofrece\b|ofrecemos\b|catalogo|cat[aá]logo)\s*(?:son|es|:|-)?\s*([^.;\n]+)/i,
      /(?:tienda|store|shop)(?:\s+online)?\s+(?:para|for)\s+([^.;\n]+)/i,
      /(?:tienda|negocio|marca|empresa)\s+de\s+([^.;\n]+)/i,
      /(?:pagina|p[aá]gina|web|site)\s+de\s+([^.;\n]+)/i,
      /(?:products?|services?|sells|offers|catalog)\s*(?:are|is|:|-)?\s*([^.;\n]+)/i,
      /(?:store|shop|business|brand|website)\s+(?:for|of)\s+([^.;\n]+)/i
    ];
    for (const pattern of patterns) {
      const match = text.match(pattern);
      if (match?.[1]) {
        const items = splitOfferItems(match[1]).filter((item) => item.length > 1 && meaningfulOfferItems([item]).length > 0).slice(0, 8);
        if (items.length) return items;
      }
    }
    return [];
  }
  function extractTargetAudience(text) {
    const patterns = [
      /(?:audiencia|cliente ideal|clientes?|publico|p[uú]blico|target)\s*(?:es|son|:|-)?\s*([^.;\n]+)/i,
      /(?:para|dirigido a|enfocado en)\s+([^.;\n]+)/i,
      /(?:for|target audience|ideal customer|customers)\s*(?:are|is|:|-)?\s*([^.;\n]+)/i,
      /(?:for|aimed at|focused on)\s+([^.;\n]+)/i
    ];
    for (const pattern of patterns) {
      const match = text.match(pattern);
      if (match?.[1]) return cleanExtractedPhrase(match[1], 90);
    }
    return "";
  }
  function extractToneFromText(text) {
    const styleHint = extractStyleHint(text);
    const styles = ["elegante", "moderno", "premium", "minimalista", "lujoso", "juvenil", "profesional", "futurista", "cyberpunk", "neon", "ne\xF3n", "oscuro", "dark", "friendly", "modern", "luxury", "minimal", "bold", "clean"];
    const found = styles.filter((style) => new RegExp(`\\b${escapeRegExp(style)}\\b`, "i").test(text));
    const normalized = [...splitCommaOrLines(styleHint), ...found].map((item) => cleanExtractedPhrase(item.replace(/neón/i, "neon"), 32).toLowerCase()).filter(Boolean);
    return [...new Set(normalized)].join(", ");
  }
  function extractColorsFromText(text) {
    const hexColors = text.match(/#[0-9a-f]{3,8}\b/gi) || [];
    const colorVocabulary = [
      "rojo",
      "azul",
      "verde",
      "negro",
      "blanco",
      "gris",
      "dorado",
      "amarillo",
      "naranja",
      "morado",
      "violeta",
      "rosa",
      "beige",
      "calido",
      "calidos",
      "c\xE1lido",
      "c\xE1lidos",
      "oscuro",
      "cyberpunk",
      "neon",
      "ne\xF3n",
      "cyan",
      "magenta",
      "red",
      "blue",
      "green",
      "black",
      "white",
      "gray",
      "grey",
      "gold",
      "yellow",
      "orange",
      "purple",
      "pink",
      "dark"
    ];
    const colorNames = colorVocabulary.filter((color) => new RegExp(`\\b${escapeRegExp(color)}\\b`, "i").test(text));
    const colorPhrase = cleanColorSegment(text.match(/(?:colores?|colors?|paleta|palette)\s*(?:son|es|:|-)?\s*([^.;\n]+)/i)?.[1] || "");
    const phraseTokens = splitCommaOrLines(colorPhrase).flatMap((item) => {
      const found = colorVocabulary.filter((color) => new RegExp(`\\b${escapeRegExp(color)}\\b`, "i").test(item));
      return found.length ? found : [item];
    });
    return [...new Set([...hexColors, ...phraseTokens, ...colorNames].map((item) => cleanExtractedPhrase(item.replace(/neón/i, "neon"), 32).toLowerCase()).filter(Boolean))].slice(0, 8);
  }
  function extractContactInfo(text) {
    const contact = {};
    const email = text.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i)?.[0];
    if (email) contact.email = email;
    const phone = text.match(/(?:\+?\d[\d\s().-]{7,}\d)/)?.[0];
    if (phone) contact.phone = phone.trim();
    const instagram = text.match(/(?:instagram|ig)\s*(?:es|:|-)?\s*(@?[a-z0-9._]+)/i)?.[1] || text.match(/@[a-z0-9._]{3,}/i)?.[0];
    if (instagram) contact.instagram = instagram.startsWith("@") ? instagram : `@${instagram}`;
    const whatsapp = text.match(/(?:whatsapp|wasap|wsp)\s*(?:es|:|-)?\s*([+\d][\d\s().-]{7,}\d)/i)?.[1];
    if (whatsapp) contact.whatsapp = whatsapp.trim();
    return contact;
  }
  function extractSalesMode(lower) {
    const modes = [];
    if (/online|ecommerce|e-commerce|env[ií]o|delivery|pago en linea|pago online|comprar/.test(lower)) modes.push(langText({ en: "online sales", es: "ventas online", fr: "vente en ligne", pt: "vendas online" }));
    if (/presencial|tienda fisica|tienda física|in person|local|visita/.test(lower)) modes.push(langText({ en: "in-person visits", es: "visitas presenciales", fr: "visites en personne", pt: "visitas presenciais" }));
    if (/cotizaci[oó]n|cotizar|quote|estimate|presupuesto/.test(lower)) modes.push(langText({ en: "quote requests", es: "solicitudes de cotizaci\xF3n", fr: "demandes de devis", pt: "pedidos de or\xE7amento" }));
    if (/\b(cita|citas|reserva|reservas|booking|appointment|agendar|agenda|consulta|consultas)\b/.test(lower)) modes.push(langText({ en: "appointments/bookings", es: "citas/reservas", fr: "rendez-vous/r\xE9servations", pt: "agendamentos/reservas" }));
    if (/captar clientes|captar leads|lead|leads|contact request|solicitudes de contacto/.test(lower)) modes.push(langText({ en: "lead capture", es: "captacion de clientes", fr: "capture de prospects", pt: "captacao de clientes" }));
    return [...new Set(modes)].join(", ");
  }
  function cleanOfferSegment(value) {
    return String(value || "").split(/\b(?:se\s+llam(?:a|ara|ará)|la\s+marca|el\s+negocio|no\s+tengo|sin\s+logo|colores?|colors?|paleta|palette|vendo\s+online|vender\s+online|venta\s+online|desde|ubicad[ao]|despacho|env[ií]o|contacto|whatsapp|direcci[oó]n|pedidos?|men[uú]\s+online|necesitamos?|quiero\s+estilo|quiero\s+que|quiero\s+captar|captar\s+clientes|agendar\s+consultas|agenda\s+consultas|debe\s+ser|should\s+be|located|shipping)\b/i)[0].replace(/\b(categor[ií]as|categorias)\s+desde\b/i, "").replace(/\b(de todo tipo|todo tipo|varios tipos|muchos tipos)\b/gi, "").replace(/\s+/g, " ").trim();
  }
  function splitOfferItems(value) {
    const segment = cleanOfferSegment(value);
    if (!segment) return [];
    const lower = normalizeTemplateIntentText(segment);
    const knownGroups = [
      ["entradas", /\bentradas?\b/],
      ["platos principales", /\bplatos?\s+principales\b/],
      ["bebidas", /\bbebidas?\b/],
      ["postres", /\bpostres?\b/],
      ["bisuteria hecha a mano", /\b(bisuteria|bijouterie|joyeria|jewelry|handmade jewelry|hecho a mano|artesanal)\b/],
      ["collares", /\b(collares?|necklaces?)\b/],
      ["pulseras", /\b(pulseras?|bracelets?)\b/],
      ["aretes", /\b(aretes?|zarcillos?|earrings?)\b/],
      ["ropa", /\bropa\b/],
      ["accesorios", /\baccesorios?\b/],
      ["auto accessories", /\b(automotriz|autos?|carros?|camionetas?|4x4)\b/],
      ["regalos", /\bregalos?\b/],
      ["hogar", /\bhogar\b/],
      ["juguetes", /\bjuguetes?\b/],
      ["anime collectibles", /\banime\b/],
      ["gadgets", /\bgadgets?\b/]
    ];
    const matched = knownGroups.filter(([, regex]) => regex.test(lower)).map(([label]) => label);
    if (matched.length >= 2) return [...new Set(matched)];
    return splitCommaOrLines(segment.replace(/\s+(?:y|and)\s+/gi, ", ")).map((item) => cleanExtractedPhrase(item, 64)).filter(Boolean);
  }
  function cleanColorSegment(value) {
    return String(value || "").split(/\b(?:vendo\s+online|vender\s+online|venta\s+online|desde|ubicad[ao]|despacho|env[ií]o|contacto|whatsapp|direcci[oó]n|pedidos?|men[uú]\s+online|no\s+tengo|sin\s+logo|pero|y\s+vendo)\b/i)[0].replace(/\s+/g, " ").trim();
  }
  function cleanExtractedPhrase(value, maxLength) {
    return String(value || "").replace(/^(que|de|para|with|for)\s+/i, "").replace(/\s+/g, " ").trim().slice(0, maxLength).trim();
  }
  function escapeRegExp(value) {
    return String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  }
  function contactInfoToLines(value) {
    return Object.entries(value || {}).filter(([, item]) => item).map(([key, item]) => `${key}: ${item}`).join("\n");
  }
  function arrayValue2(value) {
    if (Array.isArray(value)) return value.filter(Boolean);
    if (!value) return [];
    return splitCommaOrLines(String(value));
  }
  function shortError(message) {
    return String(message || "").slice(0, 180);
  }
  async function readErrorMessage(response) {
    const text = await response.text();
    try {
      const data = JSON.parse(text);
      return data.detail || data.message || text;
    } catch {
      return text;
    }
  }
  function handleServerNeedsMoreInfo(result = {}) {
    builderState.pendingServerIntakeGate = {
      missing_fields: arrayValue2(result.missing_fields),
      next_question: result.next_question || ""
    };
    const gate = ensureServerIntakeGate();
    const question = gate.querySelector(".server-intake-question");
    const field = gate.querySelector("[name='server_intake_reply']");
    if (question) question.textContent = builderState.pendingServerIntakeGate.next_question;
    gate.hidden = false;
    if (field) {
      field.value = "";
      field.focus();
    }
    const message = builderState.pendingServerIntakeGate.next_question || t("reviewGenerate");
    statusText.textContent = message;
    if (isPublicClientSetup) {
      guidedStatusText.textContent = message;
      appendChatMessage("assistant", message, "alert");
    }
    builderAvatarManager?.setState("speaking", { source: "server-needs-more-info" });
  }
  async function generateWebsite(triggerButton = document.querySelector("#generateButton")) {
    setStudioProgressPhase("understanding");
    const payload = await collectPayload();
    setStudioProgressPhase("brand");
    setGuidedBuildPhase("strategy");
    const templateSelection = await selectTemplateForPayload(payload);
    attachTemplateSelection(payload, templateSelection);
    enrichPayloadDesignStrategy(payload, templateSelection);
    setStudioProgressPhase("homepage");
    setGuidedBuildPhase("generate");
    const button = triggerButton;
    button.disabled = true;
    button.textContent = t("generating");
    statusText.textContent = t("generatingLong");
    if (isPublicClientSetup) guidedStatusText.textContent = t("generatingLong");
    builderAvatarManager?.setState("thinking", { source: "generate-website" });
    const GENERATION_ATTEMPTS = 2;
    let error = null;
    try {
      for (let attempt = 1; attempt <= GENERATION_ATTEMPTS; attempt += 1) {
        try {
          const response = await fetch(API_URL, {
            method: "POST",
            headers: clientAuthHeaders({ "content-type": "application/json" }),
            credentials: "include",
            body: JSON.stringify(payload)
          });
          if (!response.ok) {
            const responseError = await readErrorMessage(response);
            throw new Error(responseError || "Website generation failed.");
          }
          const result = await response.json();
          if (result.needs_more_info) {
            handleServerNeedsMoreInfo(result);
            return "needs_more_info";
          }
          setStudioProgressPhase("shop");
          setGuidedBuildPhase("render");
          const finalTemplateSelection = await resolveGeneratedTemplateSelection(result.schema, templateSelection);
          if (finalTemplateSelection) {
            result.schema = mergeTemplateSelectionIntoSchema(result.schema, finalTemplateSelection);
          }
          statusText.textContent = result.used_dev_mock ? "Development mock used because OPENAI_API_KEY is missing on the server." : t("generatedOpenAI");
          applyGenerationResult(result, payload, finalTemplateSelection);
          setStudioProgressPhase("mobile");
          await createDomainOrderIfNeeded(payload, result);
          setStudioProgressPhase("ready");
          setGuidedBuildPhase("ready");
          return true;
        } catch (attemptError) {
          error = attemptError;
          if (attempt < GENERATION_ATTEMPTS) {
            const retryMessage = langText({
              en: "That took longer than expected. Trying once more automatically...",
              es: "Eso tard\xF3 m\xE1s de lo esperado. Reintentando autom\xE1ticamente...",
              fr: "Cela a pris plus de temps que pr\xE9vu. Nouvelle tentative automatique...",
              pt: "Isso demorou mais do que o esperado. Tentando novamente automaticamente..."
            });
            statusText.textContent = retryMessage;
            if (isPublicClientSetup) guidedStatusText.textContent = retryMessage;
            await new Promise((resolve) => setTimeout(resolve, 1500));
          }
        }
      }
      {
        builderAvatarManager?.setState("confused", { source: "generate-error" });
        setStudioProgressPhase("homepage");
        setGuidedBuildPhase("error");
        if (isPublicClientSetup) {
          const message2 = langText({
            en: "The AI generator did not finish this request. I will not show a local fallback because it could be the wrong template. Please try again in a moment.",
            es: "El generador de IA no termin\xF3 esta solicitud. No voy a mostrar un borrador local porque podr\xEDa usar la plantilla equivocada. Intenta otra vez en un momento.",
            fr: "Le g\xE9n\xE9rateur IA n'a pas termin\xE9 cette demande. Je ne vais pas afficher de brouillon local car il pourrait utiliser le mauvais template. R\xE9essayez dans un instant.",
            pt: "O gerador de IA n\xE3o terminou esta solicita\xE7\xE3o. N\xE3o vou mostrar um rascunho local porque poderia usar o template errado. Tente novamente em instantes."
          });
          statusText.textContent = message2;
          guidedStatusText.textContent = message2;
          appendChatMessage("assistant", message2, "alert");
          return false;
        }
        const runtimeFallbackSelection = runtimeTemplateSelection(templateSelection);
        const fallbackPayload = runtimeFallbackSelection ? payload : stripReferenceTemplateRuntimePayload(payload);
        const fallbackResult = buildInstantTemplateResult(fallbackPayload, error, runtimeFallbackSelection);
        applyGenerationResult(fallbackResult, fallbackPayload, runtimeFallbackSelection);
        setStudioProgressPhase("ready");
        const message = isPublicClientSetup ? `${langText({
          en: runtimeFallbackSelection ? "I created a fast editable draft from the selected template while the AI service recovers." : "I created a safe editable draft while the AI service recovers.",
          es: runtimeFallbackSelection ? "Cre\xE9 un borrador editable r\xE1pido desde la plantilla elegida mientras se recupera el servicio de IA." : "Cre\xE9 un borrador editable seguro mientras se recupera el servicio de IA.",
          fr: runtimeFallbackSelection ? "J'ai cr\xE9\xE9 un brouillon modifiable rapide depuis le template choisi pendant que le service IA r\xE9cup\xE8re." : "J'ai cr\xE9\xE9 un brouillon modifiable s\xFBr pendant que le service IA r\xE9cup\xE8re.",
          pt: runtimeFallbackSelection ? "Criei um rascunho edit\xE1vel r\xE1pido a partir do template escolhido enquanto o servi\xE7o de IA se recupera." : "Criei um rascunho edit\xE1vel seguro enquanto o servi\xE7o de IA se recupera."
        })}` : `${t("generateError")}: ${shortError(error.message)}. Showing a fast editable draft instead.`;
        statusText.textContent = message;
        guidedStatusText.textContent = message;
        return true;
      }
    } finally {
      button.disabled = false;
      button.textContent = button.id === "guidedGenerateButton" ? t("reviewGenerate") : t("generateButton");
    }
  }
  function enrichPayloadDesignStrategy(payload, templateSelection) {
    const executionStatus = templateExecutionStatus(templateSelection?.templateId || "");
    payload.designStrategy = {
      ...payload.designStrategy || {},
      designerMode: true,
      templateUsePolicy: "Use the selected template as a reference architecture only. Adapt content, copy, palette, sections, product taxonomy, and CTAs to the business brief.",
      selectedTemplateReason: templateSelection?.reason || "",
      selectedTemplateId: templateSelection?.templateId || "",
      selectedCatalogType: templateSelection?.catalogType || "",
      templateExecutionPolicy: executionStatus,
      catalogComplexity: inferCatalogComplexity(payload),
      publicCopyPolicy: "Never paste intake answers verbatim into the website. Rewrite as polished customer-facing copy in the selected language."
    };
  }
  function inferCatalogComplexity(payload = {}) {
    const text = normalizeTemplateIntentText(`${payload.business_description || ""} ${arrayValue2(payload.services_products).join(" ")} ${payload.industry || ""}`);
    const products = meaningfulOfferItems(payload.services_products);
    if (textSuggestsBroadMarketplace(text) || products.length >= 10 && !textSuggestsFocusedCommerceStore(text)) return "broad_multi_category_catalog";
    if (textSuggestsFocusedProductLine(text) || products.length <= 2) return "focused_product_line";
    return "standard_catalog";
  }
  async function selectTemplateForPayload(payload) {
    if (!window.TemplateRouter?.selectTemplateFromPrompt) return null;
    const prompt = [
      payload.business_name,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience,
      payload.preferred_tone,
      arrayValue2(payload.preferred_colors).join(" "),
      payload.salesMode || builderState.guidedState.salesMode
    ].join(" ");
    const explicitForcedTemplate = builderState.forcedTemplateSelection?.templateId && builderState.forcedTemplateSelection?.intent === "client_visual_template_choice" ? builderState.forcedTemplateSelection : null;
    if (explicitForcedTemplate?.templateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(explicitForcedTemplate.templateId);
      if (template) {
        return {
          ...explicitForcedTemplate,
          template,
          catalogType: explicitForcedTemplate.catalogType || template.catalogModel?.catalogType || ""
        };
      }
    }
    const inferredTemplateId = inferDesignerTemplateIdFromPayload(payload) || inferTemplateIdFromText(prompt);
    if (inferredTemplateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(inferredTemplateId);
      if (template) {
        return {
          templateId: inferredTemplateId,
          template,
          intent: "guided_context_template",
          catalogType: template.catalogModel?.catalogType || templatePreviewMeta(inferredTemplateId)?.catalogType || "",
          reason: "Selected from the current business context"
        };
      }
    }
    const studioPlanTemplateId = builderState.guidedState.aiStudioPlan?.recommendedTemplateId || builderState.guidedState.designStrategy?.diagnosis?.recommendedTemplateId || "";
    if (studioPlanTemplateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(studioPlanTemplateId);
      if (template) {
        return {
          templateId: studioPlanTemplateId,
          template,
          intent: "ai_studio_plan",
          catalogType: builderState.guidedState.aiStudioPlan?.recommendedCatalogType || builderState.guidedState.designStrategy?.diagnosis?.recommendedCatalogType || template.catalogModel?.catalogType || templatePreviewMeta(studioPlanTemplateId)?.catalogType || "",
          reason: builderState.guidedState.aiStudioPlan?.reasoningSummary || builderState.guidedState.designStrategy?.diagnosis?.reasoningSummary || "Selected by LYRA's AI Studio plan"
        };
      }
    }
    const aiSelectedTemplateId = resolvedAiTemplateId();
    if (aiSelectedTemplateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(aiSelectedTemplateId);
      if (template) {
        return {
          templateId: aiSelectedTemplateId,
          template,
          intent: builderState.forcedTemplateSelection?.intent || builderState.guidedState.designStrategy?.diagnosis?.decisionState || "ai_selected_template",
          catalogType: builderState.forcedTemplateSelection?.catalogType || builderState.guidedState.sitePlan?.catalogType || builderState.guidedState.designStrategy?.selectedCatalogType || builderState.guidedState.designStrategy?.diagnosis?.recommendedCatalogType || template.catalogModel?.catalogType || templatePreviewMeta(aiSelectedTemplateId)?.catalogType || "",
          reason: builderState.forcedTemplateSelection?.reason || builderState.guidedState.designStrategy?.selectedTemplateReason || builderState.guidedState.designStrategy?.diagnosis?.reasoningSummary || "Selected by LYRA from the guided conversation"
        };
      }
    }
    if (builderState.forcedTemplateSelection?.templateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(builderState.forcedTemplateSelection.templateId);
      if (template) {
        return {
          ...builderState.forcedTemplateSelection,
          template,
          catalogType: builderState.forcedTemplateSelection.catalogType || template.catalogModel?.catalogType || ""
        };
      }
    }
    const fallbackTemplateId = inferDesignerTemplateIdFromPayload(payload);
    if (fallbackTemplateId && window.TemplateRouter.getTemplateById) {
      const template = await window.TemplateRouter.getTemplateById(fallbackTemplateId);
      if (template) {
        return {
          templateId: fallbackTemplateId,
          template,
          intent: "designer_mode_template_reference",
          catalogType: template.catalogModel?.catalogType || templatePreviewMeta(fallbackTemplateId)?.catalogType || "",
          reason: "Designer-mode fallback selected from catalog complexity and business type"
        };
      }
    }
    try {
      return await window.TemplateRouter.selectTemplateFromPrompt(prompt);
    } catch (error) {
      console.warn("Template selection failed", error);
      return null;
    }
  }
  function inferDesignerTemplateIdFromPayload(payload = {}) {
    const text = normalizeTemplateIntentText([
      payload.business_name,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience,
      payload.preferred_tone,
      arrayValue2(payload.preferred_colors).join(" "),
      payload.salesMode || builderState.guidedState.salesMode
    ].join(" "));
    const products = meaningfulOfferItems(payload.services_products);
    if (textSuggestsProfessionalService(text)) return "legal-professional-services-pro";
    if (textSuggestsMultiVendorMarketplace(text)) {
      return "mega-marketplace";
    }
    if (textSuggestsBeautyCommerceStore(text)) {
      return textSuggestsHandmadeCraftGoods(text) ? "premium-product-store" : "mega-retail-store";
    }
    if (textSuggestsMegaRetailStore(text) || textSuggestsBroadMarketplace(text) || products.length >= 10 && !textSuggestsFocusedCommerceStore(text) && !textSuggestsProfessionalService(text)) {
      return "mega-retail-store";
    }
    if (textSuggestsFocusedProductLine(text)) return "premium-product-store";
    const scoredTemplateId = bestTemplateIdFromContext(text, payload);
    if (scoredTemplateId) return scoredTemplateId;
    if (/restaurant|restaurante|menu|comida|food|cafe|cafeteria|delivery/.test(text)) return "restaurant-food-business";
    if (/barber|barberia|salon|spa|booking|reserva|cita|appointment/.test(text)) return "booking-appointment-pro";
    if (/legal|abogado|lawyer|contador|tax|impuestos|consultoria|consulting|seguros/.test(text)) return "legal-professional-services-pro";
    if (/clinic|clinica|wellness|dental|doctor|estetica|salud|therapy|skincare/.test(text)) return "medical-wellness-clinic-pro";
    if (/saas|software|enterprise|automatizacion|plataforma|dashboard|crm|erp|api/.test(text)) return "b2b-saas-enterprise-pro";
    if (/industrial|manufactur|fabrica|maquinaria|repuestos|herramientas|suministros/.test(text)) return "manufacturing-industrial-supplier-pro";
    if (/curso|course|academy|academia|bootcamp|training|clases|masterclass/.test(text)) {
      return "education-course-academy-pro";
    }
    if (/digital|ebook|templates|plantillas|descarga|download|membresia|membership/.test(text)) return "digital-products-store";
    if (textSuggestsJewelryAccessoryStore(text) || /ropa|fashion|moda|boutique|streetwear|zapato|sneaker|accesorio/.test(text)) return "fashion-drop-pro";
    if (textSuggestsSingleProductShowcase(text) || textSuggestsPremiumProductPreference(text)) {
      return "premium-product-store";
    }
    if (/servicio|service|contractor|limpieza|roofing|repair|reparacion|cotizacion|quote/.test(text)) return "local-services-pro-plus";
    if (/empresa|company|corporate|corporativo|pagina web|website|agencia|firma/.test(text)) return "corporate-company-pro";
    return "";
  }
  function attachTemplateSelection(payload, selection) {
    if (!selection) return payload;
    const normalizedTemplateId = normalizeTemplateId(selection.templateId);
    const executionStatus = templateExecutionStatus(normalizedTemplateId);
    payload.templateId = normalizedTemplateId;
    payload.templateIntent = selection.intent;
    payload.catalogType = selection.catalogType;
    payload.selectedTemplate = selection.template || {};
    payload.templateInstructions = buildTemplateInstructions(selection);
    payload.templateExecution = executionStatus;
    return payload;
  }
  function buildTemplateInstructions(selection) {
    const template = selection?.template || {};
    const normalizedTemplateId = normalizeTemplateId(selection?.templateId || template.id || "");
    const executionStatus = templateExecutionStatus(normalizedTemplateId);
    const sections = Array.isArray(template.sections) ? template.sections.map((section) => section.type).filter(Boolean).join(", ") : "";
    const pages = Array.isArray(template.pages) ? template.pages.map((page) => `${page.name || page.page_key || "Page"}: ${page.purpose || page.layout || ""}`).join(" | ") : "";
    const catalog = template.catalogModel || {};
    return {
      templateId: normalizedTemplateId,
      intent: selection?.intent || "",
      executionStatus,
      name: template.name || "",
      visualDifference: template.visualDifference || "",
      aiPrompt: template.aiPrompt || "",
      editableSlots: template.editableSlots || [],
      copyGenerationRules: [
        "If executionStatus.runtimeEnabled is false, treat the template as visual reference only. Do not assume the reference image is an executable page.",
        "If payload.sitePlan exists, treat it as the approved structure contract for pages, major sections, and conversion flow.",
        "Generate a strong brand headline, slogan, section titles, CTAs, product/service descriptions, trust copy, and footer copy from the business context.",
        "All generated visible copy must remain editable in the JSON under section.editable, catalog_items, theme, navigation, business, or global_components.",
        "Never paste businessDescription, chat answers, intake notes, internal requirements, or client wording verbatim into visible page copy.",
        "Use businessDescription and chat intake only as strategic context. Rewrite every visible headline, paragraph, CTA and section title as polished customer-facing marketing copy.",
        "If the client answer contains rough notes, product lists, spelling mistakes, internal instructions, or phrases like 'quiero', 'debe', 'necesito', convert them into professional copy or keep them out of the page.",
        "If the client gave only rough details, invent polished but safe placeholder copy that matches the selected language and mark it as editable.",
        "Keep the selected template's layout personality and catalog model. Do not flatten every template into the same hero/grid structure.",
        "Use website_config.brand and CSS brand tokens for all colors. Do not copy hardcoded template default colors when a logo or brand palette exists."
      ],
      catalogType: selection?.catalogType || catalog.catalogType || "",
      catalogCardStyle: catalog.productCardStyle || "",
      collectionLayout: catalog.collectionLayout || "",
      productDetailModel: catalog.productDetailModel || "",
      upsellModel: catalog.upsellModel || "",
      customerFeeling: catalog.customerFeeling || "",
      filters: catalog.filters || [],
      sectionOrder: sections,
      pages,
      instruction: "Use this template as the structural base. Replace placeholders with business-specific titles, slogans, section copy, products, services, colors, images, and CTAs. Intake text is design strategy only: do not copy it verbatim into visible website copy. Keep every generated element editable, and preserve the unique layout pattern instead of turning all templates into the same generic page."
    };
  }
  function buildRevisionInstructions() {
    const requestedAdjustments = arrayValue2(builderState.guidedState.requestedAdjustments);
    if (builderState.guidedState.revisionMode !== "targeted_edit" || !builderState.currentSchema || !requestedAdjustments.length) return null;
    return {
      mode: "targeted_edit",
      requestedAdjustments,
      preserveExistingDraft: true,
      preserveTemplate: true,
      preservePageOrder: true,
      preserveUnrequestedSections: true,
      preserveCatalogItems: true,
      preserveLanguage: true,
      selectedLanguage: builderState.selectedLanguage,
      previousSchema: builderState.currentSchema,
      instruction: "This is a revision of an already liked draft. Apply only the specific requested changes. Do not redesign, re-theme, reorder pages, replace unrelated copy, change catalog items, change business data, or switch templates unless the client explicitly asked for that exact change."
    };
  }
  async function requestLyraSchemaEdit(message, payload = {}, localContextUpdates = {}, templateSelection = null) {
    const response = await fetch(LYRA_EDIT_URL, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        currentSchema: builderState.currentSchema,
        instruction: message,
        selectedLanguage: builderState.selectedLanguage,
        userContext: {
          businessName: builderState.guidedState.businessName || payload.business_name,
          businessDescription: builderState.guidedState.businessDescription || payload.business_description,
          industry: builderState.guidedState.industry || payload.industry,
          servicesProducts: builderState.guidedState.servicesProducts || payload.services_products,
          preferredTone: builderState.guidedState.preferredTone || payload.preferred_tone,
          preferredColors: builderState.guidedState.preferredColors || payload.preferred_colors,
          selectedTemplateId: templateSelection?.templateId || builderState.guidedState.sitePlan?.templateId || builderState.currentSchema?.selected_template?.id,
          catalogType: templateSelection?.catalogType || builderState.guidedState.sitePlan?.catalogType || builderState.currentSchema?.catalog_model?.catalogType,
          salesFlow: builderState.guidedState.salesFlow || payload.sales_flow,
          ...localContextUpdates
        }
      })
    });
    if (!response.ok) {
      const error = await readErrorMessage(response);
      throw new Error(error || "Lyra edit failed.");
    }
    return response.json();
  }
  function explicitlyRequestsTemplateSwitch(text) {
    return /cambia(?:r)?(?: la)? plantilla|otra plantilla|plantilla diferente|switch template|change template|different template|nuevo layout|layout diferente|otra estructura|estructura diferente/.test(text);
  }
  function shouldRebuildDraftFromTemplate(message, payload = {}, templateSelection = null) {
    const text = normalizeTemplateIntentText([
      message,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.templateIntent,
      templateSelection?.templateId,
      templateSelection?.catalogType
    ].join(" "));
    const currentTemplateId = builderState.currentSchema?.selected_template?.id || builderState.currentSchema?.active_template?.id || builderState.currentSchema?.layout_mode?.template_id || "";
    const nextTemplateId = templateSelection?.templateId || inferDesignerTemplateIdFromPayload(payload) || inferTemplateIdFromText(text);
    return Boolean(
      /template|plantilla|estructura|layout|marketplace|catalogo|catálogo|tienda|store|shop|cyberpunk|neon|estilo|style|diseño|diseno/.test(text) || textSuggestsBroadMarketplace(text) || textSuggestsFocusedProductLine(text) || nextTemplateId && nextTemplateId !== currentTemplateId
    );
  }
  function applyTargetedSchemaPatch(schema, message, payload = {}, localContextUpdates = {}, templateSelection = null) {
    let nextSchema = structuredClone(schema);
    const text = normalizeTemplateIntentText(message);
    const revisionFlags = draftRevisionFlags(text);
    if (briefRequestsCyberpunk(message)) {
      nextSchema = applyCyberpunkVisualDirection(nextSchema);
    }
    if (localContextUpdates.businessName || payload.business_name) {
      nextSchema.business = { ...nextSchema.business || {}, name: payload.business_name || localContextUpdates.businessName || nextSchema.business?.name };
    }
    if (arrayValue2(localContextUpdates.servicesProducts).length) {
      nextSchema.catalog_items = mergeCatalogFromOfferItems(nextSchema.catalog_items, localContextUpdates.servicesProducts, payload);
    }
    if (/headline|titulo|título|slogan|hero|texto principal|copy|copia/.test(text)) {
      nextSchema = updatePrimaryHeroCopy(nextSchema, message, payload, templateSelection);
    }
    if (/bot[oó]n|button|cta|llamada|call to action/.test(text)) {
      nextSchema = updatePrimaryCtaCopy(nextSchema, message);
    }
    if (revisionFlags.visualSafety || revisionFlags.visualPolish || revisionFlags.copyRefresh) {
      nextSchema = applyReviewVisualPatch(nextSchema, message, payload, templateSelection, revisionFlags);
    }
    nextSchema = ensureSemanticSeedContent(nextSchema, payload, templateSelection);
    nextSchema.revision_history = [
      ...arrayValue2(nextSchema.revision_history).slice(-8),
      {
        requested_at: (/* @__PURE__ */ new Date()).toISOString(),
        request: cleanShortText(message, 260),
        applied_locally: true,
        flags: revisionFlags
      }
    ];
    return nextSchema;
  }
  function draftRevisionFlags(text) {
    return {
      visualSafety: /montad|encim|sobrepuest|solap|overlap|clip|cortad|descuadr|tapad|una sobre otra|no se ve|gigante|muy grande|demasiado grande|texto grande|se rompe/.test(text),
      visualPolish: /pro|premium|profesional|pulid|elegante|limpio|ordenad|mejor|mejora|bonito|feo|mediocre|visual|diseñ|disen|layout|estructura|espaci/.test(text),
      copyRefresh: /texto|copy|titulo|título|headline|slogan|frase|mensaje|redacci|copia|no pegues|notas/.test(text)
    };
  }
  function applyReviewVisualPatch(schema, message, payload = {}, templateSelection = null, flags = {}) {
    const nextSchema = structuredClone(schema);
    const text = normalizeTemplateIntentText(message);
    const selectedTemplateId = nextSchema.selected_template?.id || nextSchema.active_template?.id || templateSelection?.templateId || "";
    const selectedCatalogType = nextSchema.catalog_model?.catalogType || nextSchema.layout_mode?.catalog_type || templateSelection?.catalogType || "";
    nextSchema.layout_mode = {
      ...nextSchema.layout_mode || {},
      visual_safety: "no_overlap",
      revision_mode: "targeted_client_edit"
    };
    nextSchema.pages = arrayValue2(nextSchema.pages).map((page) => ({
      ...page,
      sections: arrayValue2(page.sections).map((section) => {
        const sectionType = section.type || "";
        const isHeroSection = /Hero/i.test(sectionType);
        const isVisualSection = /Hero|Signature|Feature|Story|ProductGrid|Collection|Provenance/i.test(sectionType);
        if (!isVisualSection) return section;
        const settings = {
          ...section.settings || {},
          mobile_stack: true,
          visual_safety: "no_overlap"
        };
        if (isHeroSection) {
          settings.heading_size = "safe";
          settings.spacing = "balanced";
          settings.container_width = settings.container_width || "wide";
        }
        const editable = { ...section.editable || {} };
        if (isHeroSection) {
          const currentHeadline = editable.headline || "";
          const shouldReplaceHeadline = flags.visualSafety || flags.copyRefresh || isUnsafeHeroHeadline(currentHeadline, selectedTemplateId, selectedCatalogType);
          if (shouldReplaceHeadline) {
            editable.headline = safeRevisionHeadline(nextSchema, payload, templateSelection);
          }
          if (editable.subtitle || flags.copyRefresh || flags.visualSafety) {
            editable.subtitle = safeRevisionSubtitle(nextSchema, payload, templateSelection);
          }
        }
        if (flags.visualSafety && editable.text) {
          editable.text = cleanShortText(editable.text, 220);
        }
        if (/color|paleta|cyberpunk|neon|neón|oscuro|dark|claro|minimal/.test(text) && briefRequestsCyberpunk(message)) {
          settings.background = settings.background || "#070714";
          settings.text_color = settings.text_color || "#f8fbff";
        }
        return { ...section, editable, settings };
      })
    }));
    return nextSchema;
  }
  function isUnsafeHeroHeadline(headline, templateId = "", catalogType = "") {
    const value = String(headline || "").trim();
    if (!value) return true;
    const normalized = normalizeTemplateIntentText(value);
    const isEditorialTemplate = /luxury|premium|fashion|editorial|high-ticket|product-store/.test(`${templateId} ${catalogType}`);
    if (/quiero|necesito|debe|puede|para venderlo|lo que quiero/.test(normalized)) return true;
    if (/\n/.test(value)) return true;
    if (isEditorialTemplate && value.length > 42) return true;
    return value.length > 70;
  }
  function safeRevisionHeadline(schema, payload = {}, templateSelection = null) {
    const language = payload.selectedLanguage || schema.business?.selectedLanguage || builderState.selectedLanguage || "en";
    const name = payload.business_name || schema.business?.name || builderState.guidedState.businessName || "Kreaton";
    const key = `${templateSelection?.templateId || schema.selected_template?.id || schema.active_template?.id || ""} ${templateSelection?.catalogType || schema.catalog_model?.catalogType || ""}`;
    if (/luxury|high-ticket/.test(key)) {
      return langTextFor(language, {
        en: `${name}: private collection`,
        es: `${name}: colecci\xF3n privada`,
        fr: `${name}: collection priv\xE9e`,
        pt: `${name}: cole\xE7\xE3o privada`
      });
    }
    if (/premium|product-store|editorial/.test(key)) {
      return langTextFor(language, {
        en: `${name}, designed to stand out`,
        es: `${name}, dise\xF1ado para destacar`,
        fr: `${name}, con\xE7u pour se distinguer`,
        pt: `${name}, criado para se destacar`
      });
    }
    if (/marketplace|retail|store|catalog/.test(key)) {
      return langTextFor(language, {
        en: `${name}: find it fast`,
        es: `${name}: encuentra todo r\xE1pido`,
        fr: `${name}: trouvez vite ce qu'il faut`,
        pt: `${name}: encontre tudo r\xE1pido`
      });
    }
    return langTextFor(language, {
      en: `${name} built with clarity`,
      es: `${name} con una presencia clara`,
      fr: `${name} avec une pr\xE9sence claire`,
      pt: `${name} com presen\xE7a clara`
    });
  }
  function safeRevisionSubtitle(schema, payload = {}, templateSelection = null) {
    const language = payload.selectedLanguage || schema.business?.selectedLanguage || builderState.selectedLanguage || "en";
    const description = payload.business_description || schema.business?.description || "";
    const key = `${templateSelection?.templateId || schema.selected_template?.id || schema.active_template?.id || ""} ${templateSelection?.catalogType || schema.catalog_model?.catalogType || ""}`;
    const context = cleanShortText(description, 130);
    if (/luxury|high-ticket/.test(key)) {
      return langTextFor(language, {
        en: "A curated buying experience with clear categories, private inquiry, and a polished path from discovery to request.",
        es: "Una experiencia de compra curada, con categor\xEDas claras, consulta privada y una ruta elegante desde descubrir hasta solicitar.",
        fr: "Une exp\xE9rience d'achat soign\xE9e, avec cat\xE9gories claires, demande priv\xE9e et parcours \xE9l\xE9gant.",
        pt: "Uma experi\xEAncia de compra curada, com categorias claras, consulta privada e caminho elegante at\xE9 o pedido."
      });
    }
    if (context) return context;
    return langTextFor(language, {
      en: "A focused, editable draft with cleaner spacing, stronger hierarchy, and a safer responsive layout.",
      es: "Un borrador editable con mejor espaciado, jerarqu\xEDa m\xE1s clara y layout responsive m\xE1s seguro.",
      fr: "Un brouillon modifiable avec meilleur espacement, hi\xE9rarchie claire et mise en page responsive s\xFBre.",
      pt: "Um rascunho edit\xE1vel com melhor espa\xE7amento, hierarquia clara e layout responsivo mais seguro."
    });
  }
  function langTextFor(language, options = {}) {
    return options[language] || options.en || Object.values(options).find(Boolean) || "";
  }
  function mergeCatalogFromOfferItems(existingItems = [], offerItems = [], payload = {}) {
    const labels = instantLocaleCopy(payload.selectedLanguage || builderState.selectedLanguage || "en");
    const categoryContext = [
      payload.business_description,
      payload.industry,
      payload.templateIntent,
      arrayValue2(payload.services_products).join(" "),
      arrayValue2(offerItems).join(" ")
    ].join(" ");
    const existing = arrayValue2(existingItems);
    const known = new Set(existing.map((item) => normalizeTemplateIntentText(item.name || item.title || item)));
    const additions = meaningfulOfferItems(offerItems).filter((item) => !known.has(normalizeTemplateIntentText(item))).map((name, index) => ({
      id: `chat_item_${Date.now()}_${index}`,
      sku: `CHAT-${existing.length + index + 1}`,
      name,
      description: labels.itemDescription(payload.business_name || builderState.guidedState.businessName || labels.newStore),
      category: marketplaceCategoryForIndex(existing.length + index, labels, categoryContext, payload.selectedLanguage || builderState.selectedLanguage || "en"),
      price_type: "fixed",
      price_amount: "",
      currency: "USD",
      price_label: labels.priceNotSet,
      button_label: labels.viewProduct,
      inventory_quantity: "",
      track_inventory: true,
      image_url: "",
      is_active: true,
      is_featured: existing.length + index < 3,
      sort_order: existing.length + index
    }));
    return [...existing, ...additions];
  }
  var SEMANTIC_SEED_PRODUCT_LIBRARY = {
    jewelry: [
      { name: { en: "Aurora Crystal Layered Necklace", es: "Collar Aurora de Cristal" }, category: { en: "Necklaces", es: "Collares" }, price: 42, keyword: "handmade-crystal-necklace", description: { en: "A luminous layered necklace designed for everyday shine and special looks. Lightweight, gift-ready, and easy to combine with minimalist or bold outfits.", es: "Collar en capas con brillo delicado para uso diario y looks especiales. Ligero, listo para regalo y facil de combinar con estilos minimalistas o llamativos." } },
      { name: { en: "Luna Pearl Charm Bracelet", es: "Pulsera Luna con Dijes Perla" }, category: { en: "Bracelets", es: "Pulseras" }, price: 34.5, keyword: "pearl-charm-bracelet", description: { en: "A polished bracelet with soft pearl accents and adjustable comfort. Built for customers who want a refined detail without feeling too formal.", es: "Pulsera pulida con detalles tipo perla y ajuste comodo. Pensada para clientes que quieren un detalle refinado sin sentirse demasiado formal." } },
      { name: { en: "Noir Statement Earrings", es: "Aretes Noir Statement" }, category: { en: "Earrings", es: "Aretes" }, price: 29.99, keyword: "statement-earrings", description: { en: "Bold earrings that frame the face with a clean boutique finish. Ideal for evening outfits, events, and high-impact product photography.", es: "Aretes llamativos que enmarcan el rostro con acabado boutique. Ideales para salidas, eventos y fotos de producto de alto impacto." } },
      { name: { en: "Sol Minimal Ring Set", es: "Set de Anillos Sol Minimal" }, category: { en: "Rings", es: "Anillos" }, price: 26, keyword: "minimal-ring-set", description: { en: "A stackable ring set with a clean metallic look. Customers can wear one piece alone or combine the full set for a curated finish.", es: "Set de anillos apilables con acabado metalico limpio. El cliente puede usar una pieza sola o combinar el set completo para un look curado." } },
      { name: { en: "Celeste Gift Jewelry Box", es: "Caja Regalo Celeste de Bisuteria" }, category: { en: "Gift sets", es: "Sets de regalo" }, price: 58, keyword: "jewelry-gift-box", description: { en: "A ready-to-gift accessory set with coordinated pieces and premium presentation. Perfect for birthdays, holidays, and quick online gifting.", es: "Set de accesorios listo para regalar con piezas coordinadas y presentacion premium. Perfecto para cumpleanos, temporadas y regalos online rapidos." } },
      { name: { en: "Muse Custom Initial Charm", es: "Charm Muse con Inicial Personalizada" }, category: { en: "Custom pieces", es: "Piezas personalizadas" }, price: 21.5, keyword: "custom-initial-charm", description: { en: "A personal charm made to add identity to chains, bracelets, or gift sets. Small, memorable, and ideal for repeat purchases.", es: "Charm personal para agregar identidad a cadenas, pulseras o sets de regalo. Pequeno, memorable e ideal para compras recurrentes." } }
    ],
    fashion: [
      { name: { en: "NeoFlex Street Jacket", es: "Chaqueta Street NeoFlex" }, category: { en: "Outerwear", es: "Abrigos" }, price: 79, keyword: "streetwear-jacket", description: { en: "A lightweight statement jacket with urban energy and everyday comfort. Designed as the hero piece for drops, bundles, and social-first product pages.", es: "Chaqueta ligera con energia urbana y comodidad diaria. Disenada como pieza protagonista para drops, combos y paginas enfocadas en redes." } },
      { name: { en: "Core Graphic Oversized Tee", es: "Camiseta Grafica Core Oversized" }, category: { en: "T-shirts", es: "Camisetas" }, price: 32, keyword: "oversized-graphic-tshirt", description: { en: "A soft oversized tee with a strong graphic presence. Easy to style, easy to photograph, and built for repeat seasonal drops.", es: "Camiseta oversized suave con presencia grafica fuerte. Facil de combinar, facil de fotografiar y lista para drops por temporada." } },
      { name: { en: "Pulse Utility Crossbody", es: "Bolso Cruzado Pulse Utility" }, category: { en: "Accessories", es: "Accesorios" }, price: 46.5, keyword: "utility-crossbody-bag", description: { en: "Compact storage with a clean fashion profile for daily carry. Fits essentials while keeping the outfit intentional and modern.", es: "Almacenamiento compacto con perfil moderno para uso diario. Guarda lo esencial y mantiene el outfit intencional y actual." } },
      { name: { en: "Volt Ribbed Crop Top", es: "Top Crop Acanalado Volt" }, category: { en: "Tops", es: "Tops" }, price: 27.99, keyword: "ribbed-crop-top", description: { en: "A fitted staple with strong color potential and a premium hand feel. Ideal for lookbook grids, bundles, and quick-size selection.", es: "Pieza ajustada basica con potencial de color y tacto premium. Ideal para lookbooks, combos y seleccion rapida de tallas." } },
      { name: { en: "Apex Cargo Denim", es: "Jean Cargo Apex" }, category: { en: "Denim", es: "Denim" }, price: 68, keyword: "cargo-denim-jeans", description: { en: "Structured cargo denim with utility details and a clean silhouette. A practical best-seller candidate for customers who want style and function.", es: "Denim cargo estructurado con detalles utilitarios y silueta limpia. Candidato a best seller para clientes que buscan estilo y funcion." } },
      { name: { en: "Orbit Sneaker Low", es: "Sneaker Orbit Low" }, category: { en: "Footwear", es: "Calzado" }, price: 89, keyword: "modern-sneakers", description: { en: "A versatile sneaker with a crisp profile for everyday movement. Built to anchor outfits across casual, streetwear, and travel looks.", es: "Sneaker versatil con perfil limpio para movimiento diario. Pensado para completar looks casuales, streetwear y de viaje." } }
    ],
    coffee: [
      { name: { en: "Single Origin Espresso Blend", es: "Blend Espresso Origen Unico" }, category: { en: "Coffee beans", es: "Cafe en grano" }, price: 18.5, keyword: "single-origin-coffee-beans", description: { en: "A balanced espresso blend with cocoa notes, smooth body, and a clean finish. Built for customers who want cafe-quality coffee at home.", es: "Blend espresso balanceado con notas de cacao, cuerpo suave y final limpio. Ideal para clientes que quieren cafe de calidad en casa." } },
      { name: { en: "Cold Brew Concentrate", es: "Concentrado Cold Brew" }, category: { en: "Ready to drink", es: "Listo para tomar" }, price: 14.99, keyword: "cold-brew-coffee", description: { en: "A smooth cold brew concentrate made for fast morning routines and premium iced drinks. Mix with water, milk, or signature syrups.", es: "Concentrado cold brew suave para rutinas rapidas y bebidas frias premium. Mezcla con agua, leche o siropes de autor." } },
      { name: { en: "Ceramic Pour-Over Kit", es: "Kit Pour Over Ceramico" }, category: { en: "Brewing gear", es: "Equipo de preparacion" }, price: 38, keyword: "ceramic-pour-over-coffee", description: { en: "A clean brewing kit for slow coffee rituals and giftable home setups. Helps customers control extraction, aroma, and presentation.", es: "Kit de preparacion limpio para rituales de cafe lento y regalos de casa. Ayuda a controlar extraccion, aroma y presentacion." } },
      { name: { en: "Vanilla Oat Latte Pack", es: "Pack Latte Avena Vainilla" }, category: { en: "Bundles", es: "Combos" }, price: 24, keyword: "vanilla-oat-latte", description: { en: "A ready-to-enjoy latte bundle with cozy vanilla notes and dairy-free flexibility. Designed for repeat orders and seasonal campaigns.", es: "Combo latte listo para disfrutar con notas de vainilla y opcion sin lacteos. Disenado para recompra y campanas de temporada." } },
      { name: { en: "House Roast Subscription", es: "Suscripcion House Roast" }, category: { en: "Subscriptions", es: "Suscripciones" }, price: 29, keyword: "coffee-subscription-box", description: { en: "Fresh roasted coffee delivered on a recurring schedule. A strong retention product for customers who never want to run out.", es: "Cafe recien tostado entregado de forma recurrente. Producto fuerte de retencion para clientes que no quieren quedarse sin cafe." } },
      { name: { en: "Signature Tasting Flight", es: "Flight de Cata Signature" }, category: { en: "Gift sets", es: "Sets de regalo" }, price: 36, keyword: "coffee-tasting-set", description: { en: "A curated tasting box with multiple roast profiles and clear flavor notes. Perfect for gifting, discovery, and premium product storytelling.", es: "Caja de cata curada con varios perfiles de tueste y notas claras. Perfecta para regalos, descubrimiento y narrativa premium." } }
    ],
    auto: [
      { name: { en: "TrailGuard 4x4 Front Bumper", es: "Parachoques Frontal TrailGuard 4x4" }, category: { en: "Bumpers", es: "Parachoques" }, price: 649, keyword: "off-road-front-bumper", description: { en: "Heavy-duty front protection built for 4x4 trucks, trail routes, and aggressive styling. Designed for buyers comparing durability, fitment, and finish.", es: "Proteccion frontal reforzada para camionetas 4x4, rutas off-road y estilo agresivo. Pensada para compradores que comparan resistencia, ajuste y acabado." } },
      { name: { en: "RaptorLine Skid Plate Kit", es: "Kit Protector RaptorLine Skid Plate" }, category: { en: "Protection", es: "Proteccion" }, price: 289, keyword: "truck-skid-plate", description: { en: "Underbody protection for rocky terrain and work-ready trucks. A practical upgrade for customers who want confidence beyond the pavement.", es: "Proteccion inferior para terrenos rocosos y camionetas de trabajo. Mejora practica para clientes que quieren confianza fuera del asfalto." } },
      { name: { en: "AeroMax LED Light Bar", es: "Barra LED AeroMax" }, category: { en: "Lighting", es: "Iluminacion" }, price: 159.99, keyword: "led-light-bar-truck", description: { en: "High-output lighting for night trails, job sites, and rugged builds. Adds visibility, presence, and a strong visual upgrade.", es: "Iluminacion potente para rutas nocturnas, trabajo y builds robustas. Agrega visibilidad, presencia y mejora visual fuerte." } },
      { name: { en: "Overland Recovery Strap Set", es: "Set de Eslingas Overland Recovery" }, category: { en: "Recovery", es: "Rescate" }, price: 74.5, keyword: "off-road-recovery-strap", description: { en: "Recovery straps and hardware packed for trail safety and quick response. A high-trust add-on for off-road shoppers.", es: "Eslingas y herrajes para seguridad de ruta y respuesta rapida. Complemento de alta confianza para compradores off-road." } },
      { name: { en: "RidgeFit Fender Flares", es: "Extensiones Fender RidgeFit" }, category: { en: "Exterior", es: "Exterior" }, price: 219, keyword: "truck-fender-flares", description: { en: "Bold fender coverage with a clean molded look for wider wheels and rugged stance. Built for model-based catalog filtering.", es: "Cobertura de guardafangos con acabado limpio para rines anchos y presencia robusta. Ideal para catalogo filtrado por modelo." } },
      { name: { en: "CabinVault Cargo Organizer", es: "Organizador de Cabina CabinVault" }, category: { en: "Interior", es: "Interior" }, price: 54.99, keyword: "car-cargo-organizer", description: { en: "A practical organizer for tools, cables, emergency kits, and travel gear. Helps increase average order value with simple utility.", es: "Organizador practico para herramientas, cables, kit de emergencia y viaje. Ayuda a subir el ticket promedio con utilidad simple." } }
    ],
    tech: [
      { name: { en: "HoloGrip MagSafe Phone Stand", es: "Base Magnetica HoloGrip para Telefono" }, category: { en: "Mobile accessories", es: "Accesorios moviles" }, price: 19.99, keyword: "magsafe-phone-stand", description: { en: "A compact magnetic stand for desk setups, video calls, and hands-free viewing. Small, useful, and easy to sell as an impulse add-on.", es: "Base magnetica compacta para escritorio, videollamadas y uso manos libres. Pequena, util y facil de vender como complemento." } },
      { name: { en: "PulseDrive USB-C Turbo Cable", es: "Cable USB-C Turbo PulseDrive" }, category: { en: "Charging", es: "Carga" }, price: 12.5, keyword: "usb-c-fast-charging-cable", description: { en: "Braided fast-charge cable built for daily use and clean merchandising. A reliable essential for tech shoppers and bundle offers.", es: "Cable trenzado de carga rapida para uso diario y merchandising limpio. Esencial confiable para compradores tech y combos." } },
      { name: { en: "Mini Nebula Projector", es: "Mini Proyector Nebula" }, category: { en: "Entertainment", es: "Entretenimiento" }, price: 129, keyword: "mini-portable-projector", description: { en: "A portable projector for rooms, trips, and quick entertainment setups. Strong visual product for marketplace hero placements.", es: "Proyector portatil para habitaciones, viajes y entretenimiento rapido. Producto visual fuerte para destacados de marketplace." } },
      { name: { en: "Retro Reactor Keycap Set", es: "Set de Keycaps Retro Reactor" }, category: { en: "Gaming", es: "Gaming" }, price: 49, keyword: "mechanical-keyboard-keycaps", description: { en: "A limited-run mechanical keyboard keycap set with bright desk appeal. Built for gamers, creators, and customization shoppers.", es: "Set limitado de keycaps para teclado mecanico con alto impacto visual. Pensado para gamers, creadores y compradores de personalizacion." } },
      { name: { en: "CyberLamp Ambient Desk Light", es: "Lampara de Escritorio CyberLamp" }, category: { en: "Desk setup", es: "Setup de escritorio" }, price: 59, keyword: "rgb-desk-lamp", description: { en: "Customizable ambient lighting for desks, rooms, and streaming spaces. Adds mood, color, and a strong upsell for tech carts.", es: "Luz ambiental personalizable para escritorios, habitaciones y streaming. Agrega atmosfera, color y mejora el valor del carrito." } },
      { name: { en: "GlowPatch Sticker Pack", es: "Pack de Stickers GlowPatch" }, category: { en: "Collectibles", es: "Coleccionables" }, price: 9.99, keyword: "holographic-sticker-pack", description: { en: "A weatherproof glow-style sticker pack for laptops, bottles, cars, and gifts. Affordable, collectible, and ideal for checkout add-ons.", es: "Pack de stickers estilo glow resistente para laptops, botellas, carros y regalos. Accesible, coleccionable e ideal como add-on." } }
    ],
    beauty: [
      { name: { en: "GlowReset Vitamin C Serum", es: "Serum Vitamina C GlowReset" }, category: { en: "Skincare", es: "Cuidado facial" }, price: 28, keyword: "vitamin-c-serum", description: { en: "A brightening serum positioned for daily routines and visible glow. Strong for educational product pages and subscription bundles.", es: "Serum iluminador para rutinas diarias y brillo visible. Ideal para paginas educativas de producto y combos de suscripcion." } },
      { name: { en: "HydraCloud Moisture Cream", es: "Crema Hidratante HydraCloud" }, category: { en: "Moisturizers", es: "Hidratantes" }, price: 32.5, keyword: "moisturizing-face-cream", description: { en: "A rich but lightweight moisturizer for soft skin and simple routine building. Easy to pair with cleansers, serums, and bundles.", es: "Hidratante rica pero ligera para piel suave y rutina simple. Facil de combinar con limpiadores, serums y kits." } },
      { name: { en: "SilkFinish Lip Oil", es: "Lip Oil SilkFinish" }, category: { en: "Lips", es: "Labios" }, price: 16.99, keyword: "lip-oil-beauty", description: { en: "A glossy lip oil with a soft finish and giftable look. Designed for quick purchases, social content, and shade collections.", es: "Aceite labial con brillo suave y apariencia lista para regalo. Disenado para compras rapidas, contenido social y colecciones de tonos." } },
      { name: { en: "CleanSkin Travel Kit", es: "Kit de Viaje CleanSkin" }, category: { en: "Kits", es: "Kits" }, price: 44, keyword: "skincare-travel-kit", description: { en: "A travel-ready routine kit with essentials packed for convenience. A strong starter product for new customers.", es: "Kit de rutina listo para viaje con esenciales practicos. Producto ideal para iniciar a nuevos clientes." } },
      { name: { en: "Rose Quartz Face Roller", es: "Rodillo Facial Rose Quartz" }, category: { en: "Tools", es: "Herramientas" }, price: 22, keyword: "rose-quartz-face-roller", description: { en: "A soothing beauty tool for self-care rituals and premium product photography. Pairs naturally with serums and creams.", es: "Herramienta de belleza calmante para rituales de cuidado y fotografia premium. Combina naturalmente con serums y cremas." } },
      { name: { en: "NightRepair Mask", es: "Mascarilla NightRepair" }, category: { en: "Masks", es: "Mascarillas" }, price: 25, keyword: "night-face-mask-skincare", description: { en: "A replenishing night mask designed for soft, rested-looking skin. Good for routine bundles, promotions, and repeat purchases.", es: "Mascarilla nocturna reparadora para piel suave y descansada. Buena para combos de rutina, promociones y recompra." } }
    ],
    home: [
      { name: { en: "Nordic Arc Table Lamp", es: "Lampara de Mesa Nordic Arc" }, category: { en: "Lighting", es: "Iluminacion" }, price: 68, keyword: "nordic-table-lamp", description: { en: "A sculptural lamp with warm light and a clean silhouette. Strong visual anchor for home decor collections and lifestyle shots.", es: "Lampara escultorica con luz calida y silueta limpia. Ancla visual fuerte para colecciones de hogar y fotos lifestyle." } },
      { name: { en: "LinenSoft Throw Blanket", es: "Manta LinenSoft" }, category: { en: "Textiles", es: "Textiles" }, price: 39.99, keyword: "linen-throw-blanket", description: { en: "A soft throw blanket for sofas, bedrooms, and giftable home styling. Simple, tactile, and easy to bundle with cushions.", es: "Manta suave para sofas, habitaciones y regalos de hogar. Simple, tactil y facil de combinar con cojines." } },
      { name: { en: "CasaMist Ceramic Diffuser", es: "Difusor Ceramico CasaMist" }, category: { en: "Wellness", es: "Bienestar" }, price: 45, keyword: "ceramic-aroma-diffuser", description: { en: "A quiet ceramic diffuser for calm rooms and daily rituals. Adds lifestyle value and pairs well with oils and gift sets.", es: "Difusor ceramico silencioso para espacios tranquilos y rituales diarios. Agrega valor lifestyle y combina con aceites y regalos." } },
      { name: { en: "OakGrid Desk Organizer", es: "Organizador de Escritorio OakGrid" }, category: { en: "Organization", es: "Organizacion" }, price: 31.5, keyword: "wood-desk-organizer", description: { en: "A clean organizer for workspaces, shelves, and entry tables. Useful, photogenic, and built for productivity-focused shoppers.", es: "Organizador limpio para escritorios, repisas y entradas. Util, fotogenico y pensado para compradores enfocados en productividad." } },
      { name: { en: "MarbleDot Coaster Set", es: "Set de Posavasos MarbleDot" }, category: { en: "Dining", es: "Mesa" }, price: 18, keyword: "marble-coaster-set", description: { en: "A refined coaster set for coffee tables and dining spaces. Small enough for add-ons, polished enough for gift positioning.", es: "Set de posavasos refinado para mesas de cafe y comedor. Pequeno para add-ons y pulido para posicionarlo como regalo." } },
      { name: { en: "UrbanLeaf Planter Duo", es: "Duo de Macetas UrbanLeaf" }, category: { en: "Decor", es: "Decoracion" }, price: 36, keyword: "modern-indoor-planter", description: { en: "A modern planter pair for shelves, desks, and indoor greenery. Helps the store tell a warmer home lifestyle story.", es: "Par de macetas modernas para repisas, escritorios y plantas interiores. Ayuda a contar una historia de hogar mas calida." } }
    ],
    restaurant: [
      { name: { en: "Smoked Brisket Tacos", es: "Tacos de Brisket Ahumado" }, category: { en: "Mains", es: "Principales" }, price: 16, keyword: "brisket-tacos", description: { en: "Slow-smoked brisket served in warm tortillas with fresh toppings and house sauce. A strong signature item for pickup, delivery, and specials.", es: "Brisket ahumado lentamente en tortillas calientes con toppings frescos y salsa de la casa. Plato fuerte para pickup, delivery y especiales." } },
      { name: { en: "Citrus Avocado Salad", es: "Ensalada Citrus Avocado" }, category: { en: "Starters", es: "Entradas" }, price: 12.5, keyword: "avocado-citrus-salad", description: { en: "Fresh greens, avocado, citrus, and crunchy texture for a bright starter. Designed to balance heavier menu items and attract lighter orders.", es: "Verdes frescos, aguacate, citricos y textura crujiente para una entrada brillante. Equilibra platos fuertes y atrae pedidos ligeros." } },
      { name: { en: "Truffle Mushroom Flatbread", es: "Flatbread de Hongos Trufados" }, category: { en: "Specials", es: "Especiales" }, price: 18, keyword: "mushroom-flatbread", description: { en: "Crisp flatbread with mushrooms, melted cheese, and truffle aroma. Premium enough for specials, approachable enough for repeat orders.", es: "Flatbread crujiente con hongos, queso fundido y aroma de trufa. Premium para especiales y accesible para pedidos recurrentes." } },
      { name: { en: "House Lime Refresher", es: "Refrescante de Lima de la Casa" }, category: { en: "Drinks", es: "Bebidas" }, price: 6.5, keyword: "lime-mocktail", description: { en: "A bright house drink with lime, herbs, and a clean finish. Easy to promote with combos, lunch offers, and online ordering.", es: "Bebida de la casa con lima, hierbas y final limpio. Facil de promocionar con combos, almuerzos y pedidos online." } },
      { name: { en: "Chef's Dessert Jar", es: "Postre del Chef en Jar" }, category: { en: "Desserts", es: "Postres" }, price: 8, keyword: "dessert-jar", description: { en: "A layered dessert jar built for delivery stability and strong photos. Sweet, portable, and ideal for upsells.", es: "Postre en capas pensado para delivery y fotos atractivas. Dulce, portable e ideal para agregar al pedido." } },
      { name: { en: "Family Combo Box", es: "Combo Familiar Box" }, category: { en: "Combos", es: "Combos" }, price: 42, keyword: "family-meal-box", description: { en: "A shareable meal box with mains, sides, and drinks for simple group ordering. Built to raise average ticket value.", es: "Caja para compartir con principales, acompanantes y bebidas para pedidos grupales. Disenada para subir el ticket promedio." } }
    ],
    default: [
      { name: { en: "Studio Carry Tote", es: "Bolso Tote Studio" }, category: { en: "Accessories", es: "Accesorios" }, price: 39, keyword: "minimal-canvas-tote-bag", description: { en: "A clean everyday tote with sturdy handles and a polished retail look. Useful as a real starter product for lifestyle, gift, or boutique stores.", es: "Bolso tote limpio para uso diario, con asas resistentes y apariencia retail pulida. Sirve como producto inicial real para tiendas lifestyle, regalos o boutique." } },
      { name: { en: "Ceramic Desk Tray", es: "Bandeja Ceramica de Escritorio" }, category: { en: "Home office", es: "Oficina en casa" }, price: 28, keyword: "ceramic-desk-tray", description: { en: "A refined tray for keys, jewelry, stationery, or daily essentials. Small, photogenic, and easy to position as an add-on purchase.", es: "Bandeja refinada para llaves, bisuteria, papeleria o esenciales diarios. Pequena, fotogenica y facil de vender como complemento." } },
      { name: { en: "Soft Linen Pouch", es: "Pouch de Lino Soft" }, category: { en: "Storage", es: "Organizacion" }, price: 24, keyword: "linen-zip-pouch", description: { en: "A soft zip pouch for travel, cosmetics, accessories, or small tools. Practical enough for repeat use and clean enough for premium presentation.", es: "Pouch suave con cierre para viaje, cosmeticos, accesorios o herramientas pequenas. Practico para uso recurrente y limpio para presentacion premium." } },
      { name: { en: "Matte Gift Card Set", es: "Set de Tarjetas Regalo Matte" }, category: { en: "Gift details", es: "Detalles de regalo" }, price: 18, keyword: "minimal-gift-card-set", description: { en: "A tasteful card set for wrapping, thank-you notes, and curated orders. Adds a personal touch without turning the catalog into fake bundles.", es: "Set de tarjetas sobrias para envoltorios, notas de agradecimiento y pedidos curados. Agrega detalle personal sin convertir el catalogo en combos falsos." } },
      { name: { en: "Modern Display Stand", es: "Base de Exhibicion Moderna" }, category: { en: "Display", es: "Exhibicion" }, price: 34, keyword: "minimal-product-display-stand", description: { en: "A compact display stand for showcasing small products, samples, or featured pieces. Helps a new store look organized and ready to sell.", es: "Base compacta para exhibir productos pequenos, muestras o piezas destacadas. Ayuda a que una tienda nueva se vea ordenada y lista para vender." } },
      { name: { en: "Signature Care Kit", es: "Kit de Cuidado Signature" }, category: { en: "Care", es: "Cuidado" }, price: 22, keyword: "product-care-kit", description: { en: "A simple care kit for keeping products clean, stored, and ready to use. Works as a realistic accessory item across many store types.", es: "Kit simple para mantener productos limpios, guardados y listos para usar. Funciona como accesorio realista para varios tipos de tienda." } }
    ]
  };
  function ensureSemanticSeedContent(schema, payload = {}, templateSelection = null) {
    if (!schema) return schema;
    const nextSchema = structuredClone(schema);
    const language = nextSchema.business?.selectedLanguage || payload.selectedLanguage || builderState.selectedLanguage || "en";
    const contextText = semanticSeedContextText(nextSchema, payload, templateSelection);
    const templateText = `${templateSelection?.templateId || ""} ${templateSelection?.catalogType || ""} ${nextSchema.selected_template?.id || ""} ${nextSchema.layout_mode?.template_id || ""} ${nextSchema.catalog_model?.catalogType || ""}`;
    const templateCommerce = /marketplace|retail|online_store|product|fashion|luxury|restaurant|menu|digital|ecommerce|dense_marketplace|dense_retail|single_vendor/i.test(`${templateText} ${nextSchema.site_type || ""}`);
    const serviceOnlyTemplate = /legal|professional_services|clinic|medical|home_services|service_area|booking|b2b|industrial|company_services|lead_funnel/i.test(templateText) && !/restaurant|menu|digital|product|fashion|marketplace|retail|luxury/i.test(templateText);
    const clientCommerce = /tienda|store|shop|marketplace|catalogo|producto|productos|vender|comprar|restaurant|menu|online/i.test(contextText);
    const shouldSeedCatalog = templateCommerce || clientCommerce && !serviceOnlyTemplate;
    if (!shouldSeedCatalog) return nextSchema;
    const profileKey = inferSemanticSeedProfile(contextText, templateText);
    const copyKit = semanticSeedCopyKit(profileKey, nextSchema.business?.name || payload.business_name || builderState.guidedState.businessName || "Kreaton Store", language, templateText);
    const seedItems = profileKey === "default" ? buildContextDerivedSeedProducts(contextText, language) || buildSemanticSeedProducts(profileKey, language) : buildSemanticSeedProducts(profileKey, language);
    const existing = arrayValue2(nextSchema.catalog_items || nextSchema.products_services);
    const catalogSource = catalogSourceFromSchema(nextSchema);
    const mergedCatalog = mergeSemanticSeedCatalog(existing, seedItems, language, contextText, {
      catalogSource
    });
    nextSchema.catalog_items = mergedCatalog;
    if (Array.isArray(nextSchema.products_services)) {
      nextSchema.products_services = mergedCatalog;
    }
    nextSchema.business = {
      ...nextSchema.business || {},
      industry: isWeakSeedCopy(nextSchema.business?.industry, payload) ? copyKit.industry : nextSchema.business?.industry,
      description: isWeakSeedCopy(nextSchema.business?.description, payload) ? copyKit.businessDescription : nextSchema.business?.description
    };
    nextSchema.pages = fillSemanticSeedPageCopy(nextSchema.pages, copyKit, payload);
    nextSchema.seed_content_rules = {
      ...nextSchema.seed_content_rules || {},
      niche: profileKey,
      catalog_count: mergedCatalog.length,
      no_lorem: true,
      no_generic_products: true,
      unsplash_featured_urls: true,
      catalog_source: catalogSource || "unknown"
    };
    return nextSchema;
  }
  function semanticSeedContextText(schema = {}, payload = {}, templateSelection = null) {
    return normalizeTemplateIntentText([
      payload.business_name,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience,
      payload.preferred_tone,
      arrayValue2(payload.preferred_colors).join(" "),
      schema.business?.name,
      schema.business?.description,
      schema.business?.industry,
      arrayValue2(schema.catalog_items || schema.products_services).map((item) => item.name || item.title || item).join(" ")
    ].join(" "));
  }
  function inferSemanticSeedProfile(contextText = "", templateText = "") {
    const text = normalizeTemplateIntentText(contextText);
    if (textSuggestsBroadMarketplace(text) || textSuggestsMegaRetailStore(text)) return "marketplace";
    if (/restaurant|restaurante|menu|food|comida|cafe|coffee|bakery|bar|pizza|taco/.test(text)) return /coffee|cafe/.test(text) ? "coffee" : "restaurant";
    if (textSuggestsJewelryAccessoryStore(text)) return "jewelry";
    if (/parachoques|bumper|4x4|off road|off-road|auto parts|repuestos|automotriz|camioneta|truck|motos?|car accessories/.test(text)) return "auto";
    if (/ropa|fashion|moda|streetwear|sneaker|zapato|camiseta|clothing|apparel|boutique/.test(text)) return "fashion";
    if (/beauty|belleza|skincare|cosmet|maquillaje|spa|bath|bano|baño|jabon|jabón|soap|vela|velas|candle|personal care|cuidado personal/.test(text)) return "beauty";
    if (/decor|hogar|home|furniture|muebles|interior|lampara|casa/.test(text)) return "home";
    if (/tech|tecnologia|gadget|electron|gaming|usb|phone|laptop|anime|juguete|toy|curioso|raro|inusual|cyberpunk/.test(text)) return "tech";
    if (/luxury|lujo|premium|exclusive|exclusivo|alta gama|private|privado/.test(text)) return "default";
    return "default";
  }
  function buildSemanticSeedProducts(profileKey = "default", language = builderState.selectedLanguage) {
    const products = profileKey === "marketplace" ? [
      SEMANTIC_SEED_PRODUCT_LIBRARY.tech[0],
      SEMANTIC_SEED_PRODUCT_LIBRARY.fashion[0],
      SEMANTIC_SEED_PRODUCT_LIBRARY.auto[5],
      SEMANTIC_SEED_PRODUCT_LIBRARY.tech[2],
      SEMANTIC_SEED_PRODUCT_LIBRARY.home[0],
      SEMANTIC_SEED_PRODUCT_LIBRARY.tech[5]
    ] : SEMANTIC_SEED_PRODUCT_LIBRARY[profileKey] || SEMANTIC_SEED_PRODUCT_LIBRARY.default;
    return products.slice(0, 6).map((product, index) => {
      const name = localizedSeedValue(product.name, language);
      const category = localizedSeedValue(product.category, language);
      const description = localizedSeedValue(product.description, language);
      return {
        id: `prod_${String(index + 1).padStart(3, "0")}`,
        sku: `${profileKey.slice(0, 3).toUpperCase()}-${String(index + 1).padStart(3, "0")}`,
        name,
        description,
        category,
        price: Number(product.price),
        price_type: "fixed",
        price_value: Number(product.price),
        price_amount: Number(product.price),
        currency: "USD",
        price_label: `USD ${Number(product.price).toFixed(2)}`,
        rating: Number((4.5 + index % 4 * 0.1).toFixed(1)),
        review_count: 36 + index * 29,
        badge: index === 0 ? "Best Seller" : index === 1 ? "New" : index === 2 ? "Fast Ship" : "Featured",
        deal_label: index === 0 ? "Best Seller" : index % 2 === 0 ? "Featured" : "",
        shipping_label: index % 2 === 0 ? "Fast ship" : "Ready to ship",
        button_label: language === "es" ? "Agregar al carrito" : language === "fr" ? "Ajouter" : language === "pt" ? "Adicionar" : "Add to cart",
        inventory_quantity: 18 + index * 7,
        track_inventory: true,
        imageSearchQuery: product.keyword,
        image_url: unsplashSeedUrl(product.keyword),
        is_active: true,
        is_featured: index < 4,
        sort_order: index
      };
    });
  }
  function buildContextDerivedSeedProducts(contextText = "", language = builderState.selectedLanguage) {
    const offerItems = meaningfulOfferItems(builderState.guidedState.servicesProducts);
    if (!offerItems.length) return null;
    const addLabel = language === "es" ? "Agregar al carrito" : language === "fr" ? "Ajouter" : language === "pt" ? "Adicionar" : "Add to cart";
    const businessName = builderState.guidedState.businessName || "";
    return offerItems.slice(0, 6).map((rawName, index) => {
      const name = cleanShortText(rawName, 90) || rawName;
      return {
        id: `prod_${String(index + 1).padStart(3, "0")}`,
        sku: `CTX-${String(index + 1).padStart(3, "0")}`,
        name,
        description: langText({
          en: `A featured offer from ${businessName || "this business"}. Edit this with the real details.`,
          es: `Una oferta destacada de ${businessName || "este negocio"}. Edita esto con los detalles reales.`,
          fr: `Une offre en vedette de ${businessName || "cette entreprise"}. Modifiez avec les vrais details.`,
          pt: `Uma oferta em destaque de ${businessName || "este negocio"}. Edite com os detalhes reais.`
        }, language),
        category: langText({ en: "Featured", es: "Destacado", fr: "En vedette", pt: "Destaque" }, language),
        price: 29,
        price_type: "fixed",
        price_value: 29,
        price_amount: 29,
        currency: "USD",
        price_label: "USD 29.00",
        rating: Number((4.5 + index % 4 * 0.1).toFixed(1)),
        review_count: 36 + index * 29,
        badge: index === 0 ? "Best Seller" : index === 1 ? "New" : index === 2 ? "Fast Ship" : "Featured",
        deal_label: index === 0 ? "Best Seller" : "",
        shipping_label: index % 2 === 0 ? "Fast ship" : "Ready to ship",
        button_label: addLabel,
        inventory_quantity: 18 + index * 7,
        track_inventory: true,
        imageSearchQuery: name,
        image_url: unsplashSeedUrl(name),
        is_active: true,
        is_featured: index < 4,
        sort_order: index
      };
    });
  }
  function localizedSeedValue(value, language = builderState.selectedLanguage) {
    if (!value || typeof value !== "object") return String(value || "");
    return value[language] || value.es || value.en || Object.values(value).find(Boolean) || "";
  }
  function unsplashSeedUrl(keyword = "") {
    const clean = String(keyword || "premium-product").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "premium-product";
    return stableCatalogImageUrl(clean);
  }
  function mergeSemanticSeedCatalog(existingItems = [], seedItems = [], language = builderState.selectedLanguage, contextText = "", options = {}) {
    const existing = arrayValue2(existingItems);
    const preserveAiGeneratedIdentity = options.catalogSource === "ai_generated";
    const genericCount = existing.filter((item) => isGenericSeedProduct(item, contextText)).length;
    const needsReplacement = !preserveAiGeneratedIdentity && (existing.length < 4 || genericCount >= Math.max(1, Math.ceil(existing.length / 2)));
    const base = preserveAiGeneratedIdentity ? existing : needsReplacement ? seedItems : existing;
    const merged = base.slice(0, 6).map((item, index) => {
      const seed = seedItems[index % seedItems.length];
      const source = typeof item === "string" ? { name: item } : { ...item };
      const sourceIsGeneric = isGenericSeedProduct(source, contextText);
      const useSeedIdentity = !preserveAiGeneratedIdentity && (needsReplacement || sourceIsGeneric);
      const price = Number(source.price_amount ?? source.price_value ?? source.price ?? seed.price);
      return {
        ...source,
        id: source.id || seed.id || `prod_${String(index + 1).padStart(3, "0")}`,
        sku: source.sku || seed.sku || `SKU-${String(index + 1).padStart(3, "0")}`,
        name: useSeedIdentity ? seed.name : cleanShortText(source.name || source.title || (preserveAiGeneratedIdentity ? "" : seed.name), 90),
        description: useSeedIdentity || !preserveAiGeneratedIdentity && isWeakSeedCopy(source.description, {}) ? seed.description : source.description,
        category: useSeedIdentity || !preserveAiGeneratedIdentity && (!source.category || isGenericText(source.category)) ? seed.category : source.category,
        price: Number.isFinite(price) && price > 0 ? price : seed.price,
        price_type: source.price_type && source.price_type !== "quote_only" ? source.price_type : "fixed",
        price_value: Number.isFinite(price) && price > 0 ? price : seed.price,
        price_amount: Number.isFinite(price) && price > 0 ? price : seed.price,
        currency: source.currency || "USD",
        price_label: source.price_label && !/price editable|precio editable|price to be set|consultar/i.test(source.price_label) ? source.price_label : `USD ${(Number.isFinite(price) && price > 0 ? price : seed.price).toFixed(2)}`,
        rating: Number(source.rating || seed.rating || 4.7),
        review_count: source.review_count || seed.review_count,
        badge: source.badge || seed.badge,
        deal_label: source.deal_label || seed.deal_label || "",
        shipping_label: source.shipping_label || seed.shipping_label,
        button_label: source.button_label || seed.button_label,
        inventory_quantity: source.inventory_quantity ?? seed.inventory_quantity,
        track_inventory: source.track_inventory ?? seed.track_inventory,
        imageSearchQuery: source.imageSearchQuery || source.image_search_query || seed.imageSearchQuery,
        image_url: preserveAiGeneratedIdentity ? source.image_url || source.imageUrl || seed.image_url : useSeedIdentity || shouldReplaceCatalogSeedImage(source, seed, contextText) ? seed.image_url : source.image_url || source.imageUrl || seed.image_url,
        is_active: source.is_active !== false,
        is_featured: source.is_featured ?? index < 4,
        sort_order: Number(source.sort_order ?? index)
      };
    });
    while (!preserveAiGeneratedIdentity && merged.length < 4 && seedItems[merged.length]) {
      merged.push({ ...seedItems[merged.length], sort_order: merged.length });
    }
    return merged.slice(0, 6);
  }
  function shouldReplaceCatalogSeedImage(source = {}, seed = {}, contextText = "") {
    const raw = String(source.image_url || source.imageUrl || "").trim();
    if (!raw) return true;
    if (/featured\/600x600|\/source\/|photo-1523275335684-37898b6baf30/i.test(raw)) return true;
    if (isGenericSeedProduct(source, contextText)) return true;
    const query = normalizeTemplateIntentText(source.imageSearchQuery || source.image_search_query || source.category || source.name || "");
    return !query && Boolean(seed.image_url);
  }
  function isGenericSeedProduct(item = {}, contextText = "") {
    const source = typeof item === "string" ? { name: item } : item || {};
    const name = normalizeTemplateIntentText(source.name || source.title || source);
    const description = normalizeTemplateIntentText(source.description || source.text || "");
    const price = source.price_amount ?? source.price_value ?? source.price;
    const image = source.image_url || source.imageUrl || "";
    const nameIsRawContext = name && contextText && contextText.includes(name) && name.split(/\s+/).length > 7;
    return !name || /^(item|product|producto|service|servicio|featured item|new arrival|limited find|customer favorite|signature item|featured offer|popular choice|main offer)(\s+\d+)?$/.test(name) || /^(signature starter pack|pack inicial signature|customer favorite bundle|bundle favorito del cliente|premium upgrade|upgrade premium|limited edition drop|drop de edicion limitada|everyday essential|esencial de uso diario|gift ready selection|seleccion lista para regalo)$/.test(name) || /price to be set|precio editable|editable product|lorem|placeholder/.test(description) || nameIsRawContext || !description || price === "" || price === null || price === void 0;
  }
  function isWeakSeedCopy(value, payload = {}) {
    const text = String(value || "").trim();
    if (!text) return true;
    const normalized = normalizeTemplateIntentText(text);
    const raw = normalizeTemplateIntentText([
      payload.business_description,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience
    ].join(" "));
    return isGenericText(text) || /lorem|placeholder|your business|your site|producto 1|product 1|price to be set|editable product/i.test(text) || text.length > 260 || raw && normalized.length > 60 && raw.includes(normalized.slice(0, 60));
  }
  function semanticSeedCopyKit(profileKey = "default", businessName = "Kreaton Store", language = builderState.selectedLanguage, templateText = "") {
    const name = cleanShortText(businessName, 48);
    const ecommerce = /marketplace|retail|store|shop|catalog|product|fashion|luxury/i.test(templateText);
    const kits = {
      es: {
        jewelry: { industry: "Bisuteria y accesorios", headline: `${name}: piezas hechas para destacar`, subtitle: "Accesorios curados con brillo, detalle y una ruta simple para comprar o pedir una pieza especial.", productTitle: "Piezas listas para vender", productText: "Un catalogo inicial con nombres, precios, fotos y descripciones persuasivas para editar.", primary: "Ver coleccion", secondary: "Pedir asesoria" },
        fashion: { industry: "Moda y accesorios", headline: `${name}: una tienda visual para comprar con confianza`, subtitle: "Colecciones claras, piezas destacadas y una experiencia lista para convertir visitas en compras.", productTitle: "Drop destacado", productText: "Productos iniciales creados para verse reales desde el primer preview.", primary: "Comprar ahora", secondary: "Ver categorias" },
        coffee: { industry: "Cafe y bebidas", headline: `${name}: cafe con identidad propia`, subtitle: "Productos de cafe con narrativa clara, combos vendibles y una experiencia pensada para recompra.", productTitle: "Favoritos de la casa", productText: "Cafe, kits y bundles listos para editar y publicar.", primary: "Comprar cafe", secondary: "Ver combos" },
        auto: { industry: "Repuestos y accesorios automotrices", headline: `${name}: equipamiento listo para rutas exigentes`, subtitle: "Catalogo de accesorios, proteccion y piezas utiles con precios claros, filtros y camino rapido al carrito.", productTitle: "Piezas destacadas", productText: "Productos semilla con categoria, precio, imagen y copy comercial.", primary: "Ver catalogo", secondary: "Consultar compatibilidad" },
        tech: { industry: "Tecnologia y gadgets", headline: `${name}: hallazgos tech que se compran rapido`, subtitle: "Gadgets, accesorios y productos curiosos organizados para descubrir, comparar y agregar al carrito.", productTitle: "Deals destacados", productText: "Productos iniciales listos para un marketplace visual y editable.", primary: "Explorar deals", secondary: "Ver categorias" },
        beauty: { industry: "Belleza y cuidado personal", headline: `${name}: belleza clara, deseable y facil de comprar`, subtitle: "Rutinas, kits y productos destacados con copy orientado a beneficios y recompra.", productTitle: "Rutina destacada", productText: "Catalogo semilla con productos reales, precios e imagenes.", primary: "Comprar rutina", secondary: "Ver productos" },
        home: { industry: "Hogar y decoracion", headline: `${name}: piezas para elevar cada espacio`, subtitle: "Decoracion, organizacion y accesorios de hogar presentados con claridad comercial y visuales limpios.", productTitle: "Seleccion para el hogar", productText: "Productos iniciales listos para editar y convertir en tienda.", primary: "Ver seleccion", secondary: "Explorar ambientes" },
        marketplace: { industry: "Tienda multi-categoria", headline: `${name}: hallazgos raros, utiles y listos para descubrir`, subtitle: "Una experiencia tipo gran tienda con categorias variadas, ofertas visibles y productos listos para agregar al carrito.", productTitle: "Deals y hallazgos destacados", productText: "Un catalogo mixto con productos reales para mostrar variedad desde el primer preview.", primary: "Explorar deals", secondary: "Ver categorias" },
        restaurant: { industry: "Restaurante y menu", headline: `${name}: menu listo para pedir`, subtitle: "Platos destacados, combos y bebidas con precios claros y una experiencia simple para ordenar.", productTitle: "Especiales de la casa", productText: "Items de menu creados para publicar, ajustar y vender.", primary: "Ver menu", secondary: "Ordenar ahora" },
        default: { industry: ecommerce ? "Comercio online" : "Negocio profesional", headline: `${name}: una experiencia lista para vender`, subtitle: "Catalogo inicial con productos especificos, precios realistas y mensajes pulidos para editar antes de publicar.", productTitle: "Productos destacados", productText: "Seleccion semilla creada por Lyra segun el nicho detectado.", primary: "Ver catalogo", secondary: "Contactar" }
      },
      en: {
        jewelry: { industry: "Jewelry and accessories", headline: `${name}: pieces made to stand out`, subtitle: "Curated accessories with polish, detail, and a simple path to shop or request a special piece.", productTitle: "Ready-to-sell pieces", productText: "An initial catalog with names, prices, images, and persuasive editable descriptions.", primary: "View collection", secondary: "Ask for guidance" },
        fashion: { industry: "Fashion and accessories", headline: `${name}: a visual store built for confident shopping`, subtitle: "Clear collections, highlighted pieces, and a buying path designed to turn visits into orders.", productTitle: "Featured drop", productText: "Seed products created to feel real from the first preview.", primary: "Shop now", secondary: "View categories" },
        coffee: { industry: "Coffee and drinks", headline: `${name}: coffee with a clear identity`, subtitle: "Coffee products with strong storytelling, sellable bundles, and a repeat-purchase experience.", productTitle: "House favorites", productText: "Coffee, kits, and bundles ready to edit and publish.", primary: "Shop coffee", secondary: "View bundles" },
        auto: { industry: "Automotive parts and accessories", headline: `${name}: equipment for serious routes`, subtitle: "Accessories, protection, and useful parts with clear prices, filters, and a fast path to cart.", productTitle: "Featured parts", productText: "Seed products with category, price, image, and commercial copy.", primary: "View catalog", secondary: "Check fitment" },
        tech: { industry: "Technology and gadgets", headline: `${name}: tech finds made for fast discovery`, subtitle: "Gadgets, accessories, and curious products organized to discover, compare, and add to cart.", productTitle: "Featured deals", productText: "Initial products ready for a visual editable marketplace.", primary: "Explore deals", secondary: "View categories" },
        beauty: { industry: "Beauty and personal care", headline: `${name}: beauty that is clear and easy to shop`, subtitle: "Routines, kits, and hero products with benefit-driven copy and repeat purchase potential.", productTitle: "Featured routine", productText: "Seed catalog with real products, prices, and images.", primary: "Shop routine", secondary: "View products" },
        home: { industry: "Home and decor", headline: `${name}: pieces that elevate every space`, subtitle: "Decor, organization, and home accessories presented with clear commerce and clean visuals.", productTitle: "Home selection", productText: "Initial products ready to edit into a working store.", primary: "View selection", secondary: "Explore rooms" },
        marketplace: { industry: "Multi-category retail", headline: `${name}: uncommon finds ready to discover`, subtitle: "A big-store experience with varied categories, visible deals, and products ready to add to cart.", productTitle: "Deals and featured finds", productText: "A mixed seed catalog with real products to show variety from the first preview.", primary: "Explore deals", secondary: "View categories" },
        restaurant: { industry: "Restaurant and menu", headline: `${name}: a menu ready to order`, subtitle: "Signature dishes, combos, and drinks with clear prices and a simple ordering experience.", productTitle: "House specials", productText: "Menu items created to publish, adjust, and sell.", primary: "View menu", secondary: "Order now" },
        default: { industry: ecommerce ? "Online commerce" : "Professional business", headline: `${name}: an experience ready to sell`, subtitle: "An initial catalog with specific products, realistic prices, and polished messages ready to edit before publishing.", productTitle: "Featured products", productText: "Seed selection created by Lyra from the detected niche.", primary: "View catalog", secondary: "Contact" }
      }
    };
    const set = kits[language] || kits.en;
    return set[profileKey] || set.default;
  }
  function fillSemanticSeedPageCopy(pages = [], copyKit = {}, payload = {}) {
    return arrayValue2(pages).map((page) => ({
      ...page,
      sections: arrayValue2(page.sections).map((section) => {
        const editable = { ...section.editable || {} };
        if (/Hero|MarketplaceHero|PremiumHero|FashionHero|RestaurantHero/i.test(section.type || "")) {
          if (isWeakSeedCopy(editable.headline, payload)) editable.headline = copyKit.headline;
          if (isWeakSeedCopy(editable.subtitle || editable.text, payload)) editable.subtitle = copyKit.subtitle;
          if (!editable.primary_button || isWeakCta(editable.primary_button)) editable.primary_button = copyKit.primary;
          if (!editable.secondary_button || isWeakCta(editable.secondary_button)) editable.secondary_button = copyKit.secondary;
        }
        if (/ProductGrid|ServiceList|DealRow|CategoryRail/i.test(section.type || "")) {
          if (isWeakSeedCopy(editable.title || editable.headline, payload)) editable.title = copyKit.productTitle;
          if (isWeakSeedCopy(editable.text || editable.subtitle, payload)) editable.text = copyKit.productText;
        }
        return { ...section, editable };
      })
    }));
  }
  function updatePrimaryHeroCopy(schema, message, payload = {}, templateSelection = null) {
    const copy = instantLocaleCopy(payload.selectedLanguage || builderState.selectedLanguage || "en");
    const description = professionalPublicDescription({
      payload,
      template: templateSelection?.template || schema.selected_template || {},
      templateId: templateSelection?.templateId || schema.selected_template?.id || schema.layout_mode?.template_id || "",
      catalogType: templateSelection?.catalogType || schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || "",
      copy,
      name: payload.business_name || schema.business?.name || copy.newStore,
      products: payload.services_products,
      language: payload.selectedLanguage || builderState.selectedLanguage || "en"
    });
    const page = arrayValue2(schema.pages).find((item) => item.page_key === "home") || schema.pages?.[0];
    const hero = page?.sections?.find((section) => /Hero/i.test(section.type));
    if (hero) {
      hero.editable = {
        ...hero.editable || {},
        headline: hero.editable?.headline || copy.marketplaceHeadline?.(payload.business_name || schema.business?.name || copy.newStore) || schema.business?.name,
        subtitle: description
      };
    }
    return schema;
  }
  function updatePrimaryCtaCopy(schema, message) {
    const match = String(message || "").match(/(?:bot[oó]n|button|cta|dice|decir|texto)\s*(?:principal)?\s*(?:a|por|:)?\s*["“]?([^"”]{3,40})["”]?/i);
    const label = cleanExtractedPhrase(match?.[1] || "", 40);
    if (!label) return schema;
    arrayValue2(schema.pages).forEach((page) => {
      arrayValue2(page.sections).forEach((section) => {
        if (/Hero|ProductGrid|MarketplaceHero/i.test(section.type)) {
          section.editable = { ...section.editable || {}, primary_button: label };
        }
      });
    });
    return schema;
  }
  function draftAdjustmentReply(rebuiltFromTemplate, templateSelection = null) {
    const templateName = templateSelection?.template?.name || localizedTemplateName(templatePreviewMeta(templateSelection?.templateId || "")) || "";
    if (rebuiltFromTemplate) {
      return langText({
        en: `Done. I rebuilt the draft using the best matching structure${templateName ? `: ${templateName}` : ""}, then kept it editable.`,
        es: `Listo. Reorganic\xE9 el borrador usando la estructura que mejor encaja${templateName ? `: ${templateName}` : ""}, y sigue editable.`,
        fr: `C'est fait. J'ai reconstruit le brouillon avec la structure la plus adapt\xE9e${templateName ? `: ${templateName}` : ""}, en gardant tout modifiable.`,
        pt: `Pronto. Reorganizei o rascunho usando a estrutura que melhor encaixa${templateName ? `: ${templateName}` : ""}, mantendo tudo edit\xE1vel.`
      });
    }
    return langText({
      en: "Done. I applied that change to the draft on the left. Tell me the next specific adjustment.",
      es: "Listo. Apliqu\xE9 ese cambio al borrador de la izquierda. Dime el siguiente ajuste espec\xEDfico.",
      fr: "C'est fait. J'ai appliqu\xE9 ce changement au brouillon \xE0 gauche. Dites-moi le prochain ajustement pr\xE9cis.",
      pt: "Pronto. Apliquei essa mudan\xE7a ao rascunho \xE0 esquerda. Diga o pr\xF3ximo ajuste espec\xEDfico."
    });
  }
  function mergeTemplateSelectionIntoSchema(schema, selection) {
    if (!schema || !selection) return schema;
    const backendTemplateId = schemaTemplateId(schema);
    const selectedTemplateId = normalizeTemplateId(selection.templateId);
    const isExplicitClientChoice = selection.intent === "client_visual_template_choice";
    if (backendTemplateId && backendTemplateId !== selectedTemplateId && !isExplicitClientChoice) {
      return schema;
    }
    const templateInstructions = buildTemplateInstructions(selection);
    const executionStatus = templateExecutionStatus(selection.templateId);
    schema.selected_template = {
      id: selection.templateId,
      name: selection.template?.name || selection.templateId,
      category: selection.template?.category || "",
      intent: selection.intent,
      reason: selection.reason,
      executionStatus,
      visualDifference: selection.template?.visualDifference || "",
      clientSelectionCard: selection.template?.clientSelectionCard || {},
      sections: selection.template?.sections || [],
      pages: selection.template?.pages || [],
      instructions: templateInstructions
    };
    schema.active_template = {
      id: selection.templateId,
      name: selection.template?.name || selection.templateId,
      category: selection.template?.category || ""
    };
    schema.catalog_model = selection.template?.catalogModel || { catalogType: selection.catalogType };
    schema.layout_mode = {
      ...schema.layout_mode || {},
      template_id: selection.templateId,
      catalog_type: selection.catalogType,
      intent: selection.intent,
      execution_status: executionStatus.status,
      runtime_enabled: executionStatus.runtimeEnabled
    };
    return schema;
  }
  function schemaTemplateId(schema = {}) {
    return normalizeTemplateId(
      schema.selected_template?.id || schema.active_template?.id || schema.layout_mode?.template_id || ""
    );
  }
  async function resolveGeneratedTemplateSelection(schema = {}, clientSelection = null) {
    if (clientSelection?.intent === "client_visual_template_choice") return clientSelection;
    const backendTemplateId = schemaTemplateId(schema);
    if (!isConcreteTemplateId(backendTemplateId)) return clientSelection;
    let template = null;
    if (window.TemplateRouter?.getTemplateById) {
      try {
        template = await window.TemplateRouter.getTemplateById(backendTemplateId);
      } catch (error) {
        console.warn("Could not load the backend-selected template", error);
      }
    }
    const catalogType = schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || template?.catalogModel?.catalogType || clientSelection?.catalogType || "";
    return {
      ...clientSelection || {},
      templateId: backendTemplateId,
      template: template || {
        id: backendTemplateId,
        name: schema.selected_template?.name || schema.active_template?.name || backendTemplateId,
        category: schema.selected_template?.category || "",
        catalogModel: { catalogType }
      },
      catalogType,
      intent: "backend_schema_template",
      reason: schema.selected_template?.reason || "Selected by LYRA's backend plan"
    };
  }
  function applyGenerationResult(result, payload = {}, templateSelection = null) {
    syncCatalogSourceMetadata(result);
    try {
      result.schema = prepareWebsiteConfig(result.schema, payload, templateSelection);
    } catch (renderError) {
      console.error("Could not prepare the generated template; showing the emergency editable draft.", renderError);
      result.schema = buildEmergencyEditableSchema(payload, renderError);
      result.storage_status = "instant_template_fallback";
      result.error = String(renderError?.message || renderError || "");
    }
    builderState.currentSchema = result.schema;
    builderState.currentSiteId = result.generatedSiteId || result.projectId || result.site_id || null;
    builderState.currentBusinessId = result.business_id || null;
    builderState.currentGenerationId = result.generation_id || null;
    builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
    builderState.selectedPageKey = builderState.currentSchema.pages[0]?.page_key || "home";
    builderState.selectedVariantId = builderState.currentSchema.design_variants?.[0]?.id || "";
    saveGeneratedSite(result);
    if (builderState.currentSiteId) {
      builderState.guidedState.generatedSiteId = builderState.currentSiteId;
      builderState.guidedState.projectId = builderState.currentSiteId;
      builderState.clientIntakeSession = {
        ...builderState.clientIntakeSession || {},
        generatedSiteId: builderState.currentSiteId,
        projectId: builderState.currentSiteId,
        clientEmail: builderState.clientIntakeSession?.clientEmail || builderState.guidedState.contactInfo?.email || localStorage.getItem("lumaPendingClientEmail") || "",
        draft: guidedSessionDraftForApi()
      };
      writeClientIntakeSession(builderState.clientIntakeSession);
    }
    siteTitle2.textContent = builderState.currentSchema.business?.name || "Generated site";
    storageStatus2.textContent = storageLabel(result.storage_status, result.used_dev_mock);
    renderEditor();
    renderPreview();
    if (!isPublicClientSetup) {
      document.body.classList.remove("guided-modal-open");
      guidedPanel2.classList.remove("active");
    }
    showGeneratedClientPreview();
    syncLyraExperienceMode();
    builderState.guidedState.revisionMode = "";
    builderState.guidedState.requestedAdjustments = [];
    builderAvatarManager?.setState("success", { source: "preview-generated" });
  }
  function syncCatalogSourceMetadata(result = {}) {
    if (!result?.schema || !result.catalog_source) return result;
    result.schema.generation_metadata = {
      ...result.schema.generation_metadata || {},
      catalog_source: result.catalog_source
    };
    return result;
  }
  function catalogSourceFromSchema(schema = {}) {
    return String(
      schema?.generation_metadata?.catalog_source || schema?.generation_metadata?.catalogSource || schema?.catalog_source || schema?.catalogSource || ""
    );
  }
  function prepareWebsiteConfig(schema, payload = {}, templateSelection = null) {
    if (!schema) return schema;
    const brand = normalizeBrand(payload.brand || builderState.guidedState.brand || schema.brand || {
      logoUrl: payload.assets?.find((asset) => asset.asset_type === "logo")?.url || schema.global_components?.logo_url || "",
      preferredColors: payload.preferred_colors
    });
    let nextSchema = applyBrandSystemToSchema(structuredClone(schema), brand);
    nextSchema = applyDesignIntelligence(nextSchema, payload, templateSelection);
    nextSchema = ensureSemanticSeedContent(nextSchema, payload, templateSelection);
    nextSchema.quality_rules = DESIGN_QUALITY_RULES;
    nextSchema.design_score = scoreWebsiteConfig(nextSchema);
    nextSchema.designScore = nextSchema.design_score.total;
    for (let attempt = 0; attempt < 3 && nextSchema.design_score.total < 85; attempt += 1) {
      nextSchema = improveWebsiteConfig(nextSchema);
      nextSchema = applyDesignIntelligence(nextSchema, payload, templateSelection, { reviewOnly: true });
      nextSchema = ensureSemanticSeedContent(nextSchema, payload, templateSelection);
      nextSchema.design_score = scoreWebsiteConfig(nextSchema);
      nextSchema.designScore = nextSchema.design_score.total;
    }
    return nextSchema;
  }
  function applyBrandSystemToSchema(schema, brandInput, options = {}) {
    const localBrand = normalizeBrand(brandInput);
    const brand = normalizeBrand(
      options.forceBrandOverride ? localBrand : applyAuthoritativeThemeToBrand(schema, localBrand)
    );
    if (options.forceBrandOverride && schema.generation_metadata?.theme_source === "backend_generated") {
      schema.generation_metadata = {
        ...schema.generation_metadata,
        theme_source: "explicit_user_override"
      };
    }
    schema.brand = brand;
    schema.global_components = {
      ...schema.global_components || {},
      logo_url: brand.logoUrl || schema.global_components?.logo_url || "",
      favicon_url: brand.logoUrl || schema.global_components?.favicon_url || ""
    };
    schema.theme = {
      ...schema.theme || {},
      colors: brandToThemeColors(brand),
      fonts: brand.fontPairing || DEFAULT_BRAND.fontPairing,
      radius: Number.parseInt(brand.borderRadius, 10) || 10,
      shadow: brand.shadowStyle,
      buttons: {
        ...schema.theme?.buttons || {},
        background: brand.buttonColor,
        text: brand.buttonTextColor,
        radius: brand.borderRadius
      }
    };
    schema.design_variants = arrayValue2(schema.design_variants).map((variant) => ({
      ...variant,
      theme: {
        ...variant.theme || {},
        colors: brandToThemeColors(brand),
        fonts: brand.fontPairing || DEFAULT_BRAND.fontPairing,
        radius: Number.parseInt(brand.borderRadius, 10) || 10,
        shadow: brand.shadowStyle,
        buttons: {
          ...variant.theme?.buttons || {},
          background: brand.buttonColor,
          text: brand.buttonTextColor,
          radius: brand.borderRadius
        }
      }
    }));
    if (!schema.design_variants.length) {
      schema.design_variants = [{
        id: "brand-system",
        name: "Brand system",
        description: brand.styleDirection,
        theme: schema.theme,
        layout_mode_id: schema.layout_mode?.id || "brand_system"
      }];
    }
    return schema;
  }
  function normalizeBrand(brandInput = {}) {
    const input = brandInput || {};
    const brand = { ...DEFAULT_BRAND, ...input };
    const colors = input.colors || {};
    brand.logoUrl = brand.logoUrl || input.logo_url || input.logo || "";
    brand.primaryColor = resolveColor(brand.primaryColor || colors.primary, DEFAULT_BRAND.primaryColor);
    brand.secondaryColor = resolveColor(brand.secondaryColor || colors.secondary, DEFAULT_BRAND.secondaryColor);
    brand.accentColor = resolveColor(brand.accentColor || colors.accent, brand.primaryColor);
    brand.backgroundColor = resolveColor(brand.backgroundColor || colors.background, DEFAULT_BRAND.backgroundColor);
    brand.surfaceColor = resolveColor(brand.surfaceColor || colors.surface, DEFAULT_BRAND.surfaceColor);
    brand.textColor = resolveColor(brand.textColor || colors.text, DEFAULT_BRAND.textColor);
    brand.successColor = resolveColor(brand.successColor || colors.success, "#15803D");
    brand.warningColor = resolveColor(brand.warningColor || colors.warning, "#B45309");
    brand.errorColor = resolveColor(brand.errorColor || colors.error, "#B91C1C");
    brand.infoColor = resolveColor(brand.infoColor || colors.info, "#1D4ED8");
    brand.mutedTextColor = resolveColor(brand.mutedTextColor || colors.muted, DEFAULT_BRAND.mutedTextColor);
    brand.borderColor = resolveColor(brand.borderColor || colors.border, mixColors(brand.textColor, brand.backgroundColor, 0.14));
    brand.buttonColor = resolveColor(brand.buttonColor || colors.button || brand.primaryColor, brand.primaryColor);
    brand.buttonTextColor = resolveColor(brand.buttonTextColor || colors.buttonText || contrastColor(brand.buttonColor), contrastColor(brand.buttonColor));
    brand.fontPairing = typeof brand.fontPairing === "string" ? fontPairingFromString(brand.fontPairing) : brand.fontPairing || DEFAULT_BRAND.fontPairing;
    brand.borderRadius = String(brand.borderRadius || DEFAULT_BRAND.borderRadius);
    brand.shadowStyle = brand.shadowStyle || DEFAULT_BRAND.shadowStyle;
    return brand;
  }
  function brandToThemeColors(brand) {
    return {
      background: brand.backgroundColor,
      surface: brand.surfaceColor,
      primary: brand.primaryColor,
      secondary: brand.secondaryColor,
      accent: brand.accentColor,
      text: brand.textColor,
      success: brand.successColor,
      warning: brand.warningColor,
      error: brand.errorColor,
      info: brand.infoColor,
      muted: brand.mutedTextColor,
      border: brand.borderColor,
      button: brand.buttonColor,
      buttonText: brand.buttonTextColor
    };
  }
  function brandPalette(brand) {
    return [
      brand.primaryColor,
      brand.secondaryColor,
      brand.accentColor,
      brand.backgroundColor,
      brand.surfaceColor,
      brand.textColor,
      brand.mutedTextColor,
      brand.borderColor
    ].filter(Boolean);
  }
  function fontPairingFromString(value) {
    const [heading, body] = String(value || "").split(/[+/|,]/).map((part) => part.trim()).filter(Boolean);
    return { heading: heading || "Inter", body: body || heading || "Inter" };
  }
  function fontPairingLabel(pairing = {}) {
    return `${pairing.heading || "Inter"} + ${pairing.body || "Inter"}`;
  }
  function createDesignStrategy(payload = {}, templateSelection = null, schema = {}) {
    const businessContext = analyzeBusinessContext(payload, schema);
    const designDirection = chooseDesignDirection(businessContext, templateSelection, schema);
    const layoutStrategy = createLayoutStrategy(businessContext, designDirection, schema);
    const template = templateSelection?.template || payload.selectedTemplate || schema.selected_template || {};
    const goalText = payload.salesMode || payload.templateIntent || payload.desiredDomain || "";
    const isCommerce = /store|shop|ecommerce|tienda|catalog|marketplace|producto|vender/i.test(`${template.id || ""} ${template.category || ""} ${goalText}`);
    const isLeadGen = /quote|contact|booking|service|cita|consulta|lead/i.test(`${template.id || ""} ${template.category || ""} ${goalText}`);
    return {
      businessGoal: businessContext.mainConversionGoal || (isCommerce ? "Sell or present offers clearly" : isLeadGen ? "Generate qualified inquiries" : "Build trust and guide visitors to contact"),
      targetAudience: businessContext.audience,
      visualDirection: designDirection.replace(/_/g, " "),
      conversionGoal: layoutStrategy.heroPurpose,
      sectionPriority: layoutStrategy.sectionOrder,
      layoutStyle: layoutStrategy.layoutStyle || template.name || schema.layout_mode?.template_id || "brand-led responsive layout",
      brandPersonality: businessContext.brandStyle || schema.brand?.styleDirection || payload.preferred_tone || "professional and confident",
      contentTone: payload.preferred_tone || schema.business?.tone || "clear, specific, customer-focused",
      businessContext,
      designDirection,
      layoutStrategy
    };
  }
  function applyDesignIntelligence(schema, payload = {}, templateSelection = null, options = {}) {
    const businessContext = analyzeBusinessContext(payload, schema);
    const designDirection = chooseDesignDirection(businessContext, templateSelection, schema);
    const layoutStrategy = createLayoutStrategy(businessContext, designDirection, schema);
    const strategy = {
      ...createDesignStrategy(payload, templateSelection, schema),
      businessContext,
      designDirection,
      layoutStrategy
    };
    let nextSchema = {
      ...schema,
      design_strategy: strategy,
      design_intelligence: {
        businessContext,
        designDirection,
        layoutStrategy,
        strictRules: DESIGN_QUALITY_RULES
      }
    };
    nextSchema = ensurePurposefulSections(nextSchema, strategy, options);
    nextSchema = normalizeCatalogCommerceModel(nextSchema, strategy);
    nextSchema = enforceSelectedTemplateArchitecture(nextSchema, payload, templateSelection);
    nextSchema = orderSectionsByStrategy(nextSchema, layoutStrategy);
    nextSchema = applyProfessionalLayoutSettings(nextSchema, strategy);
    nextSchema = reviewWebsiteConfigBeforeRender(nextSchema, strategy);
    return nextSchema;
  }
  function enforceSelectedTemplateArchitecture(schema, payload = {}, templateSelection = null) {
    const templateId = `${templateSelection?.templateId || payload.templateId || schema.selected_template?.id || schema.layout_mode?.template_id || ""}`;
    const catalogType = `${templateSelection?.catalogType || payload.catalogType || schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || ""}`;
    const brief = [
      payload.business_name,
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" "),
      payload.target_audience,
      payload.preferred_tone,
      arrayValue2(payload.preferred_colors).join(" ")
    ].join(" ");
    let nextSchema = lockSchemaToExecutableTemplate(schema, payload, templateSelection, { templateId, catalogType, brief });
    if (/mega-retail-store/i.test(templateId) || /single_vendor_dense_catalog|dense_retail_catalog/i.test(catalogType) || textSuggestsMegaRetailStore(brief)) {
      const copy = instantLocaleCopy(payload.selectedLanguage || builderState.selectedLanguage || "en");
      const name = payload.business_name || schema.business?.name || copy.newStore;
      const description = professionalPublicDescription({
        payload,
        template: templateSelection?.template || schema.selected_template || { id: "mega-retail-store", category: "ecommerce" },
        templateId: "mega-retail-store",
        catalogType: "single_vendor_dense_catalog",
        copy,
        name,
        products: arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : arrayValue2(schema.products_services).map((item) => item.name),
        language: payload.selectedLanguage || builderState.selectedLanguage || "en"
      });
      const sourcePagesByKey = new Map(arrayValue2(schema.pages).map((page) => [page.page_key || page.pageKey || page.pageId, page]));
      const retailPages = buildRetailInstantPages(copy, name, description, payload).map((page) => mergeLockedTemplatePage(page, sourcePagesByKey.get(page.page_key)));
      const retailPageKeys = new Set(retailPages.map((page) => page.page_key));
      const existingPages = arrayValue2(schema.pages).filter((page) => page.page_key && !retailPageKeys.has(page.page_key));
      nextSchema = {
        ...nextSchema,
        site_type: "online_store",
        selected_template: {
          ...nextSchema.selected_template || {},
          id: "mega-retail-store",
          name: nextSchema.selected_template?.name || "Mega Retail Store",
          intent: "mega_retail_store"
        },
        catalog_model: {
          ...nextSchema.catalog_model || {},
          catalogType: "single_vendor_dense_catalog"
        },
        layout_mode: {
          ...nextSchema.layout_mode || {},
          template_id: "mega-retail-store",
          catalog_type: "single_vendor_dense_catalog",
          intent: "mega_retail_store",
          navigation: { ...nextSchema.layout_mode?.navigation || {}, show_cart: true, show_header: true, sticky_header: true },
          checkout: { ...nextSchema.layout_mode?.checkout || {}, mode: "single_store_checkout", primary_action: copy.shopNow }
        },
        navigation: navigationWithCustomPages([
          { label: copy.home, page_key: "home" },
          { label: copy.deals, page_key: "deals" },
          { label: copy.categories, page_key: "categories" },
          { label: copy.catalog, page_key: "catalog" },
          { label: copy.support, page_key: "support" }
        ], existingPages, schema.navigation),
        pages: [...retailPages, ...existingPages]
      };
    } else if (/mega-marketplace/i.test(templateId) || /dense_marketplace_catalog/i.test(catalogType) || textSuggestsMultiVendorMarketplace(brief)) {
      const copy = instantLocaleCopy(payload.selectedLanguage || builderState.selectedLanguage || "en");
      const name = payload.business_name || schema.business?.name || copy.newStore;
      const description = professionalPublicDescription({
        payload,
        template: templateSelection?.template || schema.selected_template || { id: "mega-marketplace", category: "marketplace" },
        templateId: "mega-marketplace",
        catalogType: "dense_marketplace_catalog",
        copy,
        name,
        products: arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : arrayValue2(schema.products_services).map((item) => item.name),
        language: payload.selectedLanguage || builderState.selectedLanguage || "en"
      });
      const sourcePagesByKey = new Map(arrayValue2(schema.pages).map((page) => [page.page_key || page.pageKey || page.pageId, page]));
      const marketplacePages = buildMarketplaceInstantPages(copy, name, description, payload).map((page) => mergeLockedTemplatePage(page, sourcePagesByKey.get(page.page_key)));
      const marketplacePageKeys = new Set(marketplacePages.map((page) => page.page_key));
      const existingPages = arrayValue2(schema.pages).filter((page) => page.page_key && !marketplacePageKeys.has(page.page_key));
      nextSchema = {
        ...nextSchema,
        site_type: "online_store",
        selected_template: {
          ...nextSchema.selected_template || {},
          id: "mega-marketplace",
          name: nextSchema.selected_template?.name || "Mega Marketplace",
          intent: "amazon_marketplace"
        },
        catalog_model: {
          ...nextSchema.catalog_model || {},
          catalogType: "dense_marketplace_catalog"
        },
        layout_mode: {
          ...nextSchema.layout_mode || {},
          template_id: "mega-marketplace",
          catalog_type: "dense_marketplace_catalog",
          intent: "amazon_marketplace",
          navigation: { ...nextSchema.layout_mode?.navigation || {}, show_cart: true, show_header: true, sticky_header: true },
          checkout: { ...nextSchema.layout_mode?.checkout || {}, mode: "cart_setup_required", primary_action: copy.shopNow }
        },
        navigation: navigationWithCustomPages([
          { label: copy.home, page_key: "home" },
          { label: copy.deals, page_key: "deals" },
          { label: copy.categories, page_key: "categories" },
          { label: copy.catalog, page_key: "catalog" },
          { label: copy.support, page_key: "support" }
        ], existingPages, schema.navigation),
        pages: [...marketplacePages, ...existingPages]
      };
    }
    if (briefRequestsCyberpunk(brief)) {
      nextSchema = applyCyberpunkVisualDirection(nextSchema);
    }
    return nextSchema;
  }
  function navigationWithCustomPages(baseNavigation = [], customPages = [], sourceNavigation = []) {
    const baseKeys = new Set(baseNavigation.map((item) => item.page_key));
    const sourceByKey = new Map(arrayValue2(sourceNavigation).map((item) => [item.page_key, item]));
    const customNavigation = arrayValue2(customPages).filter((page) => page.page_key && !baseKeys.has(page.page_key)).map((page) => sourceByKey.get(page.page_key) || {
      label: page.title || page.page_key,
      page_key: page.page_key
    });
    if (!customNavigation.length) return baseNavigation;
    const supportIndex = baseNavigation.findIndex((item) => /support|contact/i.test(item.page_key || ""));
    const nextNavigation = [...baseNavigation];
    nextNavigation.splice(supportIndex >= 0 ? supportIndex : nextNavigation.length, 0, ...customNavigation);
    return nextNavigation;
  }
  function lockSchemaToExecutableTemplate(schema, payload = {}, templateSelection = null, context = {}) {
    const template = templateSelection?.template || payload.selectedTemplate || schema.selected_template || {};
    const templateId = context.templateId || templateSelection?.templateId || payload.templateId || schema.selected_template?.id || schema.layout_mode?.template_id || template.id || "";
    const catalogType = context.catalogType || templateSelection?.catalogType || payload.catalogType || schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || template.catalogModel?.catalogType || "";
    if (!templateId && !catalogType) return schema;
    const copy = instantLocaleCopy(payload.selectedLanguage || schema.business?.selectedLanguage || builderState.selectedLanguage || "en");
    const name = payload.business_name || schema.business?.name || copy.newStore;
    const products = arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : arrayValue2(schema.catalog_items || schema.products_services).map((item) => item.name || item.title || item).filter(Boolean);
    const description = professionalPublicDescription({
      payload: {
        ...payload,
        business_name: name,
        services_products: products,
        business_description: payload.business_description || schema.business?.description || "",
        preferred_tone: payload.preferred_tone || schema.business?.tone || ""
      },
      template,
      templateId,
      catalogType,
      copy,
      name,
      products,
      language: payload.selectedLanguage || schema.business?.selectedLanguage || builderState.selectedLanguage || "en"
    });
    const lockedPages = executablePagesForTemplate(templateId, catalogType, copy, name, description, payload);
    if (!lockedPages.length) return schema;
    const existingByKey = new Map(arrayValue2(schema.pages).map((page) => [page.page_key || page.pageKey || page.pageId, page]));
    const mergedPages = lockedPages.map((page) => mergeLockedTemplatePage(page, existingByKey.get(page.page_key)));
    const customPages = arrayValue2(schema.pages).filter((page) => page.page_key && !mergedPages.some((locked) => locked.page_key === page.page_key));
    return {
      ...schema,
      pages: [...mergedPages, ...customPages.map((page, index) => ({ ...page, order: mergedPages.length + index + 1 }))],
      selected_template: {
        ...schema.selected_template || {},
        ...template || {},
        id: normalizeTemplatePresetId(templateId, catalogType),
        name: template.name || schema.selected_template?.name || localizedTemplateName(templatePreviewMeta(templateId)) || templateId,
        architecture_locked: true,
        editable_content_only: true
      },
      active_template: {
        ...schema.active_template || {},
        id: normalizeTemplatePresetId(templateId, catalogType),
        architecture_locked: true
      },
      layout_mode: {
        ...schema.layout_mode || {},
        template_id: normalizeTemplatePresetId(templateId, catalogType),
        catalog_type: catalogType || schema.layout_mode?.catalog_type || schema.catalog_model?.catalogType || "",
        architecture_locked: true,
        navigation: {
          ...schema.layout_mode?.navigation || {},
          show_header: true,
          sticky_header: true,
          show_cart: /marketplace|store|shop|ecommerce|catalog|retail|product/i.test(`${templateId} ${catalogType}`)
        }
      },
      catalog_model: {
        ...schema.catalog_model || {},
        ...template.catalogModel || {},
        catalogType: catalogType || template.catalogModel?.catalogType || schema.catalog_model?.catalogType || ""
      },
      template_lock: {
        enabled: true,
        template_id: normalizeTemplatePresetId(templateId, catalogType),
        rule: "AI may edit copy, products, media, colors, CTAs, and ordering only inside the selected executable template. It must not invent a new layout."
      }
    };
  }
  function normalizeTemplatePresetId(templateId = "", catalogType = "") {
    templateId = normalizeTemplateId(templateId);
    if (/mega-retail-store/i.test(templateId) || /single_vendor_dense_catalog|dense_retail_catalog/i.test(catalogType)) return "mega-retail-store";
    if (/mega-marketplace|marketplace-style/i.test(templateId) || /dense_marketplace_catalog/i.test(catalogType)) return "mega-marketplace";
    if (/premium-product-store/i.test(templateId) || /premium_editorial_catalog/i.test(catalogType)) return "premium-product-store";
    return templateId || "";
  }
  function mergeLockedTemplatePage(lockedPage, existingPage = null) {
    if (!existingPage) return lockedPage;
    const optionalBackendTypes = /* @__PURE__ */ new Set(["QuoteRequestForm", "CapabilitiesEquipment", "PortfolioGallery", "VideoShowcase", "CourseOffering"]);
    const normalizedExistingSections = arrayValue2(existingPage.sections).map((section, index) => {
      const type = section.type || section.component || "";
      const nestedEditable = section.editable || {};
      const copy = nestedEditable.copy || {};
      const media = nestedEditable.media || {};
      const dataBinding = nestedEditable.dataBinding || section.dataBinding || {};
      return {
        ...section,
        id: section.id || section.sectionId || `${slugify2(type || "section")}-${index + 1}`,
        type,
        order: section.order || index + 1,
        editable: { ...copy, ...media, ...dataBinding, ...nestedEditable },
        settings: { ...section.settings || {}, layout: section.settings?.layout || section.variant || "default" }
      };
    });
    const existingSections = new Map(normalizedExistingSections.map((section) => [section.id || section.type, section]));
    const mergedLockedSections = arrayValue2(lockedPage.sections).map((lockedSection) => {
      const existing = existingSections.get(lockedSection.id) || existingSections.get(lockedSection.type);
      if (!existing) return lockedSection;
      return {
        ...lockedSection,
        editable: {
          ...lockedSection.editable || {},
          ...existing.editable || {}
        },
        settings: {
          ...lockedSection.settings || {},
          ...existing.settings || {},
          layout: lockedSection.settings?.layout || existing.settings?.layout
        }
      };
    });
    const optionalSections = normalizedExistingSections.filter((section) => optionalBackendTypes.has(section.type) && !mergedLockedSections.some((locked) => locked.id === section.id || locked.type === section.type));
    return {
      ...lockedPage,
      title: existingPage.title || lockedPage.title,
      slug: existingPage.slug || lockedPage.slug,
      sections: [...mergedLockedSections, ...optionalSections].map((section, index) => ({ ...section, order: index + 1 }))
    };
  }
  function executablePagesForTemplate(templateId = "", catalogType = "", copy, name, description, payload = {}) {
    const key = `${templateId} ${catalogType}`.toLowerCase();
    if (/mega-retail-store|single_vendor_dense_catalog|dense_retail_catalog/.test(key)) return buildRetailInstantPages(copy, name, description, payload);
    if (/mega-marketplace|marketplace-style|dense_marketplace_catalog/.test(key)) return buildMarketplaceInstantPages(copy, name, description, payload);
    if (/premium-product-store|apple-premium-product|premium_editorial_catalog/.test(key)) return buildPremiumProductInstantPages(copy, name, description, payload);
    if (/luxury-high-ticket-pro|luxury_high_ticket_catalog/.test(key)) return buildLuxuryHighTicketInstantPages(copy, name, description, payload);
    if (/education-course-academy-pro|education_course_catalog/.test(key)) return buildEducationAcademyInstantPages(copy, name, description, payload);
    if (/medical-wellness-clinic-pro|medical_wellness_service_catalog/.test(key)) return buildMedicalWellnessInstantPages(copy, name, description, payload);
    if (/legal-professional-services-pro|legal_professional_services_catalog/.test(key)) return buildLegalProfessionalInstantPages(copy, name, description, payload);
    if (/b2b-saas-enterprise-pro|b2b_solution_catalog/.test(key)) return buildB2BEnterpriseInstantPages(copy, name, description, payload);
    if (/manufacturing-industrial-supplier-pro|industrial_supplier_catalog/.test(key)) return buildManufacturingIndustrialInstantPages(copy, name, description, payload);
    if (/fashion-drop-pro|bold-fashion-store|lookbook_collection_catalog/.test(key)) return buildFashionDropInstantPages(copy, name, description, payload);
    if (/corporate-company-pro|company_services_catalog/.test(key)) return buildCorporateCompanyInstantPages(copy, name, description, payload);
    if (/home-services-premium|local-services-pro-plus|home_services_quote_catalog|service_area_catalog/.test(key)) return buildHomeServicesPremiumInstantPages(copy, name, description, payload);
    if (/booking-appointment-pro|booking_menu_catalog/.test(key)) return buildBookingAppointmentInstantPages(copy, name, description, payload);
    if (/restaurant-food-business|restaurant_menu_catalog|menu_catalog/.test(key)) return buildRestaurantMenuInstantPages(copy, name, description, payload);
    if (/digital-products-store|digital_offer_catalog/.test(key)) return buildDigitalProductsInstantPages(copy, name, description, payload);
    if (/real-estate-listings-pro|real_estate_listing_catalog/.test(key)) return buildRealEstateListingsInstantPages(copy, name, description, payload);
    if (/lead-funnel-pro|lead_funnel_offer_catalog/.test(key)) return buildLeadFunnelInstantPages(copy, name, description, payload);
    return [];
  }
  function runtimeCompositionIndex(name, payload = {}, recipeCount = 3) {
    return pickVariantSeed(
      name || payload.business_name || payload.businessName || "",
      payload.industry || payload.niche || payload.business?.industry || "",
      payload.designVariantOffset ?? builderState.guidedState.designVariantOffset ?? 0,
      recipeCount
    );
  }
  function resequenceSections(sections = []) {
    return sections.map((section, index) => ({ ...section, order: index + 1 }));
  }
  function briefRequestsCyberpunk(value = "") {
    return /cyberpunk|neon|neón|futurista|future|gaming|gamer|super cool|sci.?fi|techno/i.test(String(value || ""));
  }
  function applyCyberpunkVisualDirection(schema) {
    const colors = {
      background: "#070714",
      surface: "#111126",
      primary: "#00f5ff",
      secondary: "#ff2bd6",
      text: "#f8fbff",
      muted: "#a7b0ff"
    };
    const brand = normalizeBrand({
      ...schema.brand || {},
      colors,
      primaryColor: colors.primary,
      secondaryColor: colors.secondary,
      accentColor: colors.secondary,
      backgroundColor: colors.background,
      surfaceColor: colors.surface,
      textColor: colors.text,
      mutedTextColor: colors.muted,
      buttonColor: colors.primary,
      buttonTextColor: "#050814",
      styleDirection: "cyberpunk neon marketplace",
      fontPairing: { heading: "Space Grotesk", body: "Inter" },
      borderRadius: "10px",
      shadowStyle: "0 22px 70px rgba(0, 245, 255, 0.22)"
    });
    return {
      ...schema,
      brand,
      theme: {
        ...schema.theme || {},
        colors,
        fonts: brand.fontPairing,
        radius: 10,
        shadow: brand.shadowStyle,
        buttons: {
          ...schema.theme?.buttons || {},
          background: colors.primary,
          text: "#050814",
          radius: "10px"
        }
      },
      design_variants: arrayValue2(schema.design_variants).map((variant, index) => ({
        ...variant,
        name: index === 0 ? "Cyberpunk marketplace" : variant.name,
        background_style: index === 0 ? "dark neon commerce grid" : variant.background_style,
        theme: {
          ...variant.theme || {},
          colors,
          fonts: brand.fontPairing
        }
      }))
    };
  }
  function normalizeCatalogCommerceModel(schema, strategy = {}) {
    const direction = strategy.designDirection || schema.design_intelligence?.designDirection || "";
    const checkoutMode = schema.layout_mode?.checkout?.mode || "";
    const businessGoal = `${strategy.businessContext?.mainConversionGoal || strategy.businessGoal || ""}`;
    const isShop = /ecommerce|marketplace|shop|store|catalog|offer|choose/i.test(`${direction} ${checkoutMode} ${businessGoal}`);
    if (!isShop) return schema;
    const askPricePattern = /ask|consult|consultar|cotizar|quote|precio\?|preguntar/i;
    return {
      ...schema,
      catalog_items: arrayValue2(schema.catalog_items || schema.products_services).map((item, index) => {
        const nextItem = typeof item === "string" ? { name: item } : { ...item };
        nextItem.id = nextItem.id || `item_${index + 1}`;
        nextItem.sku = nextItem.sku || `SKU-${index + 1}`;
        nextItem.price_type = nextItem.price_type === "quote_only" ? "fixed" : nextItem.price_type || "fixed";
        nextItem.currency = nextItem.currency || "USD";
        nextItem.price_value = nextItem.price_value ?? nextItem.price_amount ?? "";
        nextItem.price_amount = nextItem.price_amount ?? nextItem.price_value ?? "";
        if (!nextItem.price_label || askPricePattern.test(nextItem.price_label)) {
          nextItem.price_label = nextItem.price_value ? `${nextItem.currency} ${nextItem.price_value}` : "Precio editable";
        }
        if (!nextItem.button_label || askPricePattern.test(nextItem.button_label)) {
          nextItem.button_label = "Ver producto";
        }
        nextItem.inventory_quantity = nextItem.inventory_quantity ?? nextItem.stock ?? "";
        nextItem.track_inventory = nextItem.track_inventory ?? true;
        nextItem.is_active = nextItem.is_active !== false;
        nextItem.is_featured = nextItem.is_featured ?? index < 3;
        nextItem.sort_order = Number(nextItem.sort_order ?? index);
        return nextItem;
      }),
      layout_mode: {
        ...schema.layout_mode || {},
        checkout: {
          ...schema.layout_mode?.checkout || {},
          mode: checkoutMode && checkoutMode !== "quote_or_cart" ? checkoutMode : "cart_setup_required",
          primary_action: schema.layout_mode?.checkout?.primary_action || "Comprar ahora"
        }
      }
    };
  }
  function analyzeBusinessContext(payload = {}, schema = {}) {
    const business = schema.business || {};
    const contact = schema.contact || {};
    const catalog = arrayValue2(schema.catalog_items || schema.products_services);
    const products = arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : catalog.map((item) => item.name).filter(Boolean);
    const assets = arrayValue2(payload.assets);
    const text = [
      payload.business_name,
      payload.business_description,
      payload.industry,
      payload.preferred_tone,
      payload.salesMode,
      payload.templateIntent,
      business.name,
      business.description,
      business.industry,
      business.tone,
      products.join(" ")
    ].filter(Boolean).join(" ").toLowerCase();
    const industry = payload.industry || business.industry || inferIndustryFromText(text);
    const isCommerce = /shop|store|tienda|ecommerce|catalog|producto|collection|marketplace|menu|order/.test(text);
    const isBooking = /booking|book|appointment|cita|reservation|reserva|consulta|schedule/.test(text);
    const isQuote = /quote|estimate|cotiz|proposal|consulta|service|servicio/.test(text);
    const priceLevel = /luxury|premium|boutique|exclusive|high.?end|lujo|gourmet|signature/.test(text) ? "premium" : /budget|cheap|discount|deal|econ/.test(text) ? "value" : "standard";
    const trustFactors = [];
    if (business.location || contact.address) trustFactors.push("local presence");
    if (contact.phone || contact.email || contact.whatsapp) trustFactors.push("direct contact");
    if (assets.some((asset) => asset.asset_type === "logo") || schema.brand?.logoUrl) trustFactors.push("brand identity");
    if (assets.some((asset) => asset.asset_type === "photo") || catalog.some((item) => item.image_url)) trustFactors.push("real visuals");
    if (catalog.length > 1 || products.length > 1) trustFactors.push("clear offer range");
    return {
      industry,
      audience: payload.target_audience || business.target_audience || inferAudience(industry, text),
      offer: payload.business_description || business.description || products.slice(0, 3).join(", ") || "a clear professional offer",
      trustFactors: trustFactors.length ? trustFactors : ["clear offer", "consistent brand", "easy contact"],
      mainConversionGoal: isCommerce ? "Guide visitors to choose an offer" : isBooking ? "Drive bookings or appointments" : isQuote ? "Generate qualified inquiries" : "Build trust and drive contact",
      productsOrServices: products.length ? products : catalog.map((item) => item.name).filter(Boolean),
      priceLevel,
      brandStyle: payload.preferred_tone || business.tone || schema.brand?.styleDirection || "professional"
    };
  }
  function inferIndustryFromText(text) {
    if (/restaurant|food|menu|cafe|bar|pizza|taco|bakery|comida|restaurante/.test(text)) return "Restaurant";
    if (/software|ai|saas|digital|course|ebook|template|tech|app/.test(text)) return "Digital products";
    if (/real estate|property|broker|inmueble/.test(text)) return "Real estate";
    if (/law|legal|account|finance|insurance|consult/.test(text)) return "Professional services";
    if (/salon|spa|beauty|wellness|fitness|health/.test(text)) return "Wellness";
    if (/shop|store|collection|product|ecommerce|marketplace/.test(text)) return "Commerce";
    return "Local business";
  }
  function inferAudience(industry, text) {
    if (/b2b|enterprise|business|company|empresa/.test(text)) return "decision-makers comparing professional options";
    if (/family|kids|children|local|neighborhood|vecino/.test(text)) return "local customers looking for a trusted choice";
    if (/premium|luxury|boutique|exclusive/.test(text)) return "quality-conscious customers who value trust and polish";
    if (/digital|course|ebook|template|software|ai/.test(`${industry} ${text}`.toLowerCase())) return "online buyers looking for practical, credible value";
    return "customers who need a clear, trustworthy solution";
  }
  function chooseDesignDirection(context = {}, templateSelection = null, schema = {}) {
    const template = templateSelection?.template || schema.selected_template || {};
    const catalogType = `${templateSelection?.catalogType || schema.catalog_model?.catalogType || schema.layout_mode?.catalog_type || ""}`.toLowerCase();
    const text = `${context.industry || ""} ${context.offer || ""} ${context.brandStyle || ""} ${template.id || ""} ${template.category || ""} ${catalogType}`.toLowerCase();
    if (/marketplace|classified|listing|directory|dense/.test(text)) return "marketplace";
    if (/restaurant|food|menu|cafe|bar|bakery|order|comida/.test(text)) return "restaurant";
    if (context.priceLevel === "premium" && /shop|store|collection|product|commerce|ecommerce/.test(text)) return "ecommerce_premium";
    if (context.priceLevel === "premium") return "luxury";
    if (/software|saas|ai|tech|digital|app|course|ebook|template/.test(text)) return "modern_tech";
    if (/law|finance|account|insurance|consult|corporate|enterprise/.test(text)) return "corporate";
    if (/salon|spa|health|fitness|repair|agency|service|professional/.test(text)) return "service_professional";
    if (/kids|toy|party|creative|playful|fun|colorful/.test(text)) return "playful";
    if (/minimal|simple|clean|studio|portfolio/.test(text)) return "minimalist";
    return "local_business";
  }
  function createLayoutStrategy(context = {}, designDirection = "local_business", schema = {}) {
    const scales = {
      luxury: { sectionPadding: "spacious", containerWidth: "narrow", cardGap: "comfortable", typographyScale: "editorial", cardDensity: "airy", maxHomeSections: 5, heroLayout: "editorial_showcase" },
      modern_tech: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "strong", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "split_showcase" },
      playful: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "friendly", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "split_showcase" },
      minimalist: { sectionPadding: "spacious", containerWidth: "narrow", cardGap: "relaxed", typographyScale: "quiet", cardDensity: "airy", maxHomeSections: 5, heroLayout: "minimal_statement" },
      local_business: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "clear", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "trust_first" },
      corporate: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "structured", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "authority_split" },
      ecommerce_premium: { sectionPadding: "spacious", containerWidth: "wide", cardGap: "relaxed", typographyScale: "editorial", cardDensity: "airy", maxHomeSections: 5, heroLayout: "collection_showcase" },
      marketplace: { sectionPadding: "compact", containerWidth: "wide", cardGap: "tight", typographyScale: "utility", cardDensity: "compact", maxHomeSections: 5, heroLayout: "search_led" },
      restaurant: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "warm", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "menu_story" },
      service_professional: { sectionPadding: "balanced", containerWidth: "standard", cardGap: "comfortable", typographyScale: "clear", cardDensity: "comfortable", maxHomeSections: 6, heroLayout: "problem_solution" }
    };
    const scale = scales[designDirection] || scales.local_business;
    const isCommerce = /offer|choose|catalog|product|shop|store/i.test(context.mainConversionGoal || "");
    const isBooking = /booking|appointment|book|reservation/i.test(context.mainConversionGoal || "");
    return {
      heroPurpose: isCommerce ? "Present the main offer and move visitors into the catalog" : isBooking ? "Make booking feel clear and low-friction" : "Build immediate trust and invite the next contact action",
      sectionOrder: isCommerce ? ["Hero", "ProductGrid", "ServiceList", "Testimonials", "About", "Gallery", "Contact"] : ["Hero", "FeatureBand", "ServiceList", "ProductGrid", "Testimonials", "About", "Gallery", "Contact"],
      ctaPlacement: ["hero primary action", "offer cards", "final contact section"],
      trustSignals: context.trustFactors || [],
      productServicePresentation: isCommerce ? "comparison-ready catalog cards with clear labels" : "service cards focused on outcomes and next steps",
      testimonialPlacement: "after offer clarity and before final contact",
      contactBookingFlow: isBooking ? "booking-first CTA with contact fallback" : "direct contact CTA with phone/email support",
      layoutStyle: `${designDirection.replace(/_/g, " ")} ${scale.typographyScale}`,
      spacingScale: scale,
      mobileBreakpoints: { stackAt: 760, compactNavAt: 680, cardColumnsAt: 820 }
    };
  }
  function orderSectionsByStrategy(schema, layoutStrategy = {}) {
    const order = layoutStrategy.sectionOrder || ["Hero", "ProductGrid", "ServiceList", "FeatureBand", "Testimonials", "About", "Gallery", "Contact"];
    return {
      ...schema,
      pages: arrayValue2(schema.pages).map((page) => ({
        ...page,
        sections: arrayValue2(page.sections).sort((a, b) => (order.indexOf(a.type) === -1 ? 99 : order.indexOf(a.type)) - (order.indexOf(b.type) === -1 ? 99 : order.indexOf(b.type)))
      }))
    };
  }
  function applyProfessionalLayoutSettings(schema, strategy) {
    const scale = strategy.layoutStrategy?.spacingScale || {};
    const direction = strategy.designDirection || "local_business";
    return {
      ...schema,
      pages: arrayValue2(schema.pages).map((page) => ({
        ...page,
        sections: arrayValue2(page.sections).map((section, index) => addSectionPurpose({
          ...section,
          editable: improveSectionCopy(section, schema, strategy),
          settings: normalizeSectionSettings(section, index, scale, direction)
        }, index, strategy))
      }))
    };
  }
  function professionalHeadline(schema, context = {}) {
    const name = schema.business?.name || "Your brand";
    const offer = cleanShortText(context.offer || schema.business?.description || context.industry || "a better customer experience", 58);
    if (offer && !new RegExp(`^${escapeRegExp(name)}$`, "i").test(offer)) return `${name}: ${offer}`;
    return `${name} built for ${context.audience || "modern customers"}`;
  }
  function professionalSubtitle(schema, context = {}) {
    const trust = arrayValue2(context.trustFactors).slice(0, 2).join(" and ");
    const audience = context.audience || "customers";
    return `A focused ${context.industry || "business"} experience for ${audience}${trust ? `, backed by ${trust}` : ""}.`;
  }
  function primaryCtaForStrategy(strategy = {}) {
    const goal = `${strategy.businessContext?.mainConversionGoal || strategy.conversionGoal || ""}`.toLowerCase();
    if (/booking|appointment|reservation|book/.test(goal)) return "Book now";
    if (/quote|inquir|contact|proposal/.test(goal)) return "Request a quote";
    if (/offer|catalog|product|shop|choose/.test(goal)) return "View offerings";
    return "Get started";
  }
  function isWeakHeadline(value, businessName = "") {
    const text = String(value || "").trim();
    if (!text) return true;
    if (businessName && text.toLowerCase() === String(businessName).trim().toLowerCase()) return true;
    return text.length < 16 || isGenericText(text);
  }
  function isWeakCta(value) {
    return !value || /click here|learn more|submit|button|cta/i.test(String(value));
  }
  function isGenericText(value) {
    return /lorem|placeholder|generic|products and services|featured product|main service|special offer|your business|welcome to/i.test(String(value || ""));
  }
  function cleanShortText(value, maxLength = 60) {
    const text = String(value || "").replace(/\s+/g, " ").trim();
    if (text.length <= maxLength) return text;
    return `${text.slice(0, maxLength).replace(/\s+\S*$/, "")}`;
  }
  function normalizeGenericText(value) {
    return String(value || "").trim().toLowerCase().replace(/\s+/g, " ");
  }
  function scoreWebsiteConfig(schema) {
    const brand = normalizeBrand(schema.brand || {});
    const pages = arrayValue2(schema.pages);
    const sections = pages.flatMap((page) => arrayValue2(page.sections));
    const hero = sections.find((section) => section.type === "Hero");
    const hasLogo = Boolean(brand.logoUrl || schema.global_components?.logo_url);
    const hasBrandColors = Boolean(schema.brand?.primaryColor && schema.theme?.colors?.primary);
    const hasPurposes = sections.length && sections.every((section) => section.reason && section.conversion_purpose && section.priority);
    const repeatedTypes = sections.length - new Set(sections.map((section) => `${section.type}:${section.editable?.title || section.editable?.headline || ""}`)).size;
    const contrastOk = contrastRatio(brand.textColor, brand.backgroundColor) >= 4.5 && contrastRatio(brand.buttonColor, brand.buttonTextColor) >= 4.5;
    const designDirection = schema.design_intelligence?.designDirection || schema.design_strategy?.designDirection;
    const layoutStrategy = schema.design_intelligence?.layoutStrategy || schema.design_strategy?.layoutStrategy;
    const genericTextCount = sections.filter((section) => {
      const editable = section.editable || {};
      return [editable.headline, editable.title, editable.subtitle, editable.text].some(isGenericText);
    }).length;
    const weakCtaCount = sections.filter((section) => section.type === "Hero" && isWeakCta(section.editable?.primary_button)).length;
    const hasLayoutSettings = sections.every((section) => section.settings?.spacing && section.settings?.container_width && section.settings?.card_density);
    const purposefulSectionCount = sections.filter((section) => section.type !== "Footer").length;
    const clutterPenalty = Math.max(0, purposefulSectionCount - 7) * 8;
    const metrics = {
      brandConsistency: Math.min(100, 56 + (hasLogo ? 18 : 0) + (hasBrandColors ? 26 : 0)),
      visualHierarchy: hero?.editable?.headline && hero?.editable?.primary_button ? 92 : 68,
      contentClarity: Math.max(55, (sections.filter((section) => section.editable?.text || section.editable?.subtitle || section.editable?.headline).length >= Math.min(3, sections.length) ? 90 : 70) - genericTextCount * 12),
      conversionStrength: Math.max(55, (hero?.editable?.primary_button && sections.some((section) => /Contact|ProductGrid|ServiceList/.test(section.type)) ? 92 : 72) - weakCtaCount * 18),
      spacing: hasLayoutSettings ? 94 : sections.every((section) => section.settings?.spacing || section.type === "Hero") ? 86 : 74,
      accessibility: contrastOk ? 94 : 62,
      templateFit: schema.selected_template?.id || schema.active_template?.id || designDirection ? 90 : 78,
      mobileUsability: pages.length && sections.length < 10 && sections.every((section) => section.settings?.mobile_stack !== false) ? 92 : 76,
      sectionPurpose: hasPurposes ? 95 : 65,
      repetition: Math.max(58, 95 - repeatedTypes * 12),
      designDirection: designDirection ? 94 : 60,
      layoutStrategy: layoutStrategy?.sectionOrder?.length ? 94 : 64,
      clutterControl: Math.max(58, 95 - clutterPenalty)
    };
    const total = Math.round(Object.values(metrics).reduce((sum, value) => sum + value, 0) / Object.keys(metrics).length);
    return { total, metrics };
  }
  function improveWebsiteConfig(schema) {
    const improved = structuredClone(schema);
    const brand = normalizeBrand(improved.brand || {});
    improved.theme = improved.theme || {};
    improved.theme.colors = brandToThemeColors(brand);
    const strategy = improved.design_strategy || createDesignStrategy({}, null, improved);
    improved.pages = arrayValue2(improved.pages).map((page) => ({
      ...page,
      sections: arrayValue2(page.sections).filter((section, index, list) => index === list.findIndex((item) => `${item.type}:${item.editable?.title || item.editable?.headline || ""}` === `${section.type}:${section.editable?.title || section.editable?.headline || ""}`)).map((section, index) => ({
        ...section,
        editable: improveSectionCopy(section, improved, strategy),
        settings: {
          ...section.settings || {},
          spacing: section.settings?.spacing || (index === 0 ? "spacious" : "balanced"),
          heading_size: section.settings?.heading_size || (index === 0 ? "large" : "medium"),
          container_width: section.settings?.container_width || "standard",
          card_density: section.settings?.card_density || "comfortable",
          card_gap: section.settings?.card_gap || "comfortable",
          mobile_stack: true
        }
      }))
    }));
    improved.design_review = {
      ...improved.design_review || {},
      autoImproved: true,
      designScoreTarget: 85
    };
    return improved;
  }
  function buildInstantTemplateResult(payload, error, templateSelection) {
    let schema;
    try {
      schema = buildInstantTemplateSchema(payload, templateSelection);
    } catch (templateError) {
      console.warn("Selected instant template failed; using emergency editable draft.", templateError);
      schema = buildEmergencyEditableSchema(payload, templateError);
    }
    return {
      business_id: null,
      site_id: null,
      generation_id: null,
      storage_status: "instant_template_fallback",
      schema,
      used_dev_mock: false,
      error: String(error?.message || error || "")
    };
  }
  function buildEmergencyEditableSchema(payload = {}, error = null) {
    const language = payload.selectedLanguage || builderState.selectedLanguage || "en";
    const copy = instantLocaleCopy(language);
    const name = payload.business_name || copy.newStore || "New website";
    const products = sanitizePublicProductList(
      arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : copy.defaultProducts,
      payload,
      copy,
      language,
      "emergency_editable"
    );
    const brand = normalizeBrand(payload.brand || createBrandSystem({
      extractedColors: payload.logoPalette,
      preferredColors: payload.preferred_colors,
      businessName: name,
      industry: payload.industry,
      tone: payload.preferred_tone
    }));
    const description = professionalPublicDescription({
      payload,
      template: { id: "emergency-editable", category: "business" },
      templateId: "emergency-editable",
      catalogType: "editorial_minimal_grid",
      copy,
      name,
      products,
      language
    });
    return {
      schema_version: "1.0",
      site_type: "business_website",
      business: {
        name,
        description,
        industry: payload.industry || copy.generalBusiness,
        location: payload.location || "",
        target_audience: payload.target_audience || "",
        tone: payload.preferred_tone || copy.defaultTone,
        selectedLanguage: language
      },
      theme: {
        colors: brandToThemeColors(brand),
        fonts: brand.fontPairing,
        buttons: {
          primary_label: copy.request || copy.shopNow,
          secondary_label: copy.contactRequest,
          background: brand.buttonColor,
          text: brand.buttonTextColor,
          radius: brand.borderRadius
        },
        radius: Number.parseInt(brand.borderRadius, 10) || 10,
        shadow: brand.shadowStyle
      },
      brand,
      selected_template: {
        id: "emergency-editable",
        name: "Emergency editable draft",
        architecture_locked: false
      },
      layout_mode: {
        id: "emergency_editable",
        template_id: "emergency-editable",
        catalog_type: "editorial_minimal_grid",
        navigation: { show_header: true, sticky_header: true, show_cart: false }
      },
      navigation: [
        { label: copy.home, page_key: "home" },
        { label: copy.services || copy.catalog, page_key: "catalog" },
        { label: copy.contactRequest, page_key: "contact" }
      ],
      products_services: products.map((item, index) => ({
        id: `emergency_${index + 1}`,
        name: item,
        description: copy.itemDescription(name),
        price_type: "quote_only",
        price_label: copy.askPrice || "",
        button_label: copy.request || copy.contactRequest,
        is_active: true,
        is_featured: index < 3,
        sort_order: index + 1
      })),
      pages: buildDefaultInstantPages(copy, name, description, payload),
      fallback_error: String(error?.message || error || "")
    };
  }
  function professionalPublicDescription({ payload = {}, template = {}, templateId = "", catalogType = "", copy = {}, name = "", products = [], language = builderState.selectedLanguage }) {
    const templateText = `${catalogType} ${templateId} ${template.id || ""} ${template.category || ""}`.toLowerCase();
    if (window.__lyraDebugCopy) {
      console.warn("[LYRA-DEBUG professionalPublicDescription]", { templateId, catalogType, templateObjId: template.id || "", templateText });
    }
    const focus = productFocusForLanguage(products, payload.industry, language);
    const descriptions = {
      en: {
        marketplace: `A search-first shopping experience for ${focus}, with clear categories, featured finds, and a fast path to buy or ask.`,
        fashion: `A visual storefront for ${focus}, built around collections, product storytelling, and a clean path to shop.`,
        restaurant: "A warm menu experience with signature items, specials, hours, location, and a simple ordering path.",
        service: `A trust-first service website for ${focus}, with proof, process, contact options, and quote-ready sections.`,
        premium: `A polished brand experience for ${focus}, with refined messaging, strong visuals, and editable story sections.`,
        company: `A professional business website for ${focus}, built around clarity, trust, services, and direct contact.`
      },
      es: {
        marketplace: `Una experiencia de compra con busqueda clara para ${focus}, con categorias ordenadas, destacados y una ruta rapida para comprar o preguntar.`,
        fashion: `Una tienda visual para ${focus}, pensada alrededor de colecciones, narrativa de producto y una ruta clara para comprar.`,
        restaurant: "Una experiencia calida de menu con platos destacados, especiales, horarios, ubicacion y pedidos simples.",
        service: `Una pagina enfocada en confianza para ${focus}, con pruebas, proceso, contacto y secciones listas para cotizar.`,
        premium: `Una experiencia de marca pulida para ${focus}, con mensaje refinado, visuales fuertes y secciones editables.`,
        company: `Una pagina profesional para ${focus}, enfocada en claridad, confianza, servicios y contacto directo.`
      },
      fr: {
        marketplace: `Une experience d'achat claire pour ${focus}, avec categories, selections et parcours rapide vers l'achat ou la demande.`,
        fashion: `Une boutique visuelle pour ${focus}, construite autour des collections, du storytelling produit et d'un achat clair.`,
        restaurant: "Une experience menu chaleureuse avec plats signature, offres, horaires, adresse et commande simple.",
        service: `Un site de service axe confiance pour ${focus}, avec preuves, processus, contact et demande de devis.`,
        premium: `Une experience de marque soignee pour ${focus}, avec message raffine, visuels forts et sections modifiables.`,
        company: `Un site professionnel pour ${focus}, centre sur la clarte, la confiance, les services et le contact direct.`
      },
      pt: {
        marketplace: `Uma experiencia de compra clara para ${focus}, com categorias, destaques e caminho rapido para comprar ou perguntar.`,
        fashion: `Uma loja visual para ${focus}, feita em torno de colecoes, narrativa de produto e compra clara.`,
        restaurant: "Uma experiencia acolhedora de menu com destaques, especiais, horarios, localizacao e pedido simples.",
        service: `Um site focado em confianca para ${focus}, com provas, processo, contato e secoes prontas para orcamento.`,
        premium: `Uma experiencia de marca polida para ${focus}, com mensagem refinada, visual forte e secoes editaveis.`,
        company: `Um site profissional para ${focus}, focado em clareza, confianca, servicos e contato direto.`
      }
    };
    const lang = descriptions[language] ? language : "en";
    const set = descriptions[lang];
    if (/fashion|lookbook|collection/.test(templateText)) return set.fashion;
    if (/restaurant|menu|food/.test(templateText)) return set.restaurant;
    if (/service|booking|clinic|legal|professional|home_services|quote/.test(templateText)) return set.service;
    if (/company|corporate|b2b|industrial|enterprise/.test(templateText)) return set.company;
    if (/marketplace|dense|listing|store|shop|catalog/.test(templateText)) return set.marketplace;
    return set.premium || copy.defaultDescription || name;
  }
  function productFocusForLanguage(products = [], fallback = "", language = builderState.selectedLanguage) {
    const clean = arrayValue2(products).map((item) => String(item || "").trim()).filter(Boolean).slice(0, 2);
    if (clean.length) {
      const joiner = language === "en" ? " and " : language === "fr" ? " et " : " y ";
      return clean.join(joiner);
    }
    const fallbacks = {
      en: "the right products and services",
      es: "los productos y servicios correctos",
      fr: "les bons produits et services",
      pt: "os produtos e servicos certos"
    };
    return fallback || fallbacks[language] || fallbacks.en;
  }
  function sanitizePublicProductList(products = [], payload = {}, copy = {}, language = builderState.selectedLanguage, templateHint = "") {
    const sourceText = `${payload.business_description || ""} ${payload.industry || ""} ${arrayValue2(products).join(" ")} ${templateHint || ""}`;
    const sourceSuggestsBroadMarketplace = textSuggestsBroadMarketplace(sourceText);
    const usesMarketplaceTemplate = /mega-marketplace|marketplace-style|dense_marketplace_catalog/i.test(templateHint);
    const usesMegaRetailTemplate = /mega-retail-store|single_vendor_dense_catalog|dense_retail_catalog/i.test(templateHint);
    const cleaned = arrayValue2(products).map((item) => cleanPublicItemLabel(item)).filter(Boolean).slice(0, 8);
    if (sourceSuggestsBroadMarketplace || usesMarketplaceTemplate || usesMegaRetailTemplate) {
      const inferred2 = inferredPublicCatalogLabels({ text: sourceText, language });
      return [
        .../* @__PURE__ */ new Set([
          ...cleaned.filter((item) => item.split(/\s+/).length <= 4),
          ...inferred2,
          ...marketplaceSeedCatalogLabels(sourceText, language)
        ])
      ].slice(0, 12);
    }
    if (cleaned.length >= 2 && !sourceSuggestsBroadMarketplace) return cleaned;
    const inferred = inferredPublicCatalogLabels({
      text: sourceText,
      language
    });
    if (inferred.length) {
      return sourceSuggestsBroadMarketplace ? [.../* @__PURE__ */ new Set([...inferred, ...cleaned.filter((item) => item.split(/\s+/).length <= 3)])].slice(0, 6) : inferred;
    }
    return cleaned.length ? cleaned : arrayValue2(copy.defaultProducts).slice(0, 4);
  }
  function marketplaceSeedCatalogLabels(text = "", language = builderState.selectedLanguage) {
    const lower = String(text || "").toLowerCase();
    const sets = {
      es: {
        variety: ["Gadget destacado", "Accesorio automotriz", "Coleccionable anime", "Regalo curioso", "Organizador para auto", "Accesorio para el hogar", "Producto viral", "Set de novedades", "Herramienta util", "Decoracion tematica", "Combo sorpresa", "Oferta limitada"],
        jewelry: ["Collar artesanal", "Pulsera ajustable", "Aretes llamativos", "Set de bisuteria", "Anillo decorativo", "Charm personalizado", "Accesorio para regalo", "Coleccion de temporada", "Pieza minimalista", "Pulsera de cuentas", "Aretes de fiesta", "Pack especial"],
        fashion: ["Chaqueta destacada", "Camiseta grafica", "Accesorio urbano", "Bolso compacto", "Gorra de temporada", "Set streetwear", "Pieza limitada", "Look completo", "Sneaker destacado", "Oferta de moda", "Drop nuevo", "Combo de accesorios"],
        default: ["Producto destacado", "Oferta especial", "Nuevo lanzamiento", "Best seller", "Pack recomendado", "Accesorio esencial", "Producto premium", "Combo ahorro", "Seleccion popular", "Edicion limitada", "Hallazgo especial", "Categoria estrella"]
      },
      en: {
        variety: ["Featured gadget", "Auto accessory", "Anime collectible", "Curious gift", "Car organizer", "Home accessory", "Viral product", "New finds set", "Useful tool", "Theme decor", "Surprise bundle", "Limited offer"],
        jewelry: ["Handmade necklace", "Adjustable bracelet", "Statement earrings", "Jewelry set", "Decorative ring", "Custom charm", "Gift accessory", "Seasonal collection", "Minimal piece", "Beaded bracelet", "Party earrings", "Special pack"],
        fashion: ["Featured jacket", "Graphic tee", "Urban accessory", "Compact bag", "Seasonal cap", "Streetwear set", "Limited piece", "Complete look", "Featured sneaker", "Fashion deal", "New drop", "Accessory bundle"],
        default: ["Featured product", "Special offer", "New arrival", "Best seller", "Recommended pack", "Essential accessory", "Premium item", "Savings bundle", "Popular pick", "Limited edition", "Special find", "Category star"]
      },
      fr: {
        variety: ["Gadget vedette", "Accessoire auto", "Objet anime", "Cadeau original", "Organiseur voiture", "Accessoire maison", "Produit viral", "Nouveautes", "Outil utile", "Decoration theme", "Pack surprise", "Offre limitee"],
        jewelry: ["Collier artisanal", "Bracelet reglable", "Boucles statement", "Set bijoux", "Bague decorative", "Charm personnalise", "Accessoire cadeau", "Collection saison", "Piece minimaliste", "Bracelet perles", "Boucles fete", "Pack special"],
        fashion: ["Veste vedette", "T-shirt graphique", "Accessoire urbain", "Sac compact", "Casquette saison", "Set streetwear", "Piece limitee", "Look complet", "Sneaker vedette", "Offre mode", "Nouveau drop", "Pack accessoires"],
        default: ["Produit vedette", "Offre speciale", "Nouveaute", "Best seller", "Pack recommande", "Accessoire essentiel", "Produit premium", "Pack economie", "Choix populaire", "Edition limitee", "Trouvaille speciale", "Categorie phare"]
      },
      pt: {
        variety: ["Gadget destaque", "Acessorio automotivo", "Colecionavel anime", "Presente curioso", "Organizador de carro", "Acessorio para casa", "Produto viral", "Set de novidades", "Ferramenta util", "Decoracao tematica", "Combo surpresa", "Oferta limitada"],
        jewelry: ["Colar artesanal", "Pulseira ajustavel", "Brincos marcantes", "Kit bijuteria", "Anel decorativo", "Charm personalizado", "Acessorio presente", "Colecao sazonal", "Peca minimalista", "Pulseira de contas", "Brincos festa", "Pack especial"],
        fashion: ["Jaqueta destaque", "Camiseta grafica", "Acessorio urbano", "Bolsa compacta", "Bone sazonal", "Set streetwear", "Peca limitada", "Look completo", "Sneaker destaque", "Oferta moda", "Novo drop", "Combo acessorios"],
        default: ["Produto destaque", "Oferta especial", "Lancamento", "Mais vendido", "Pack recomendado", "Acessorio essencial", "Produto premium", "Combo economia", "Escolha popular", "Edicao limitada", "Achado especial", "Categoria estrela"]
      }
    };
    const set = sets[language] || sets.en;
    if (textSuggestsJewelryAccessoryStore(lower)) return set.jewelry;
    if (/ropa|moda|fashion|camisa|zapato|sneaker|clothing|apparel|streetwear/.test(lower)) return set.fashion;
    if (/carro|auto|automotriz|camioneta|anime|gadget|juguete|regalo|raro|curioso|hogar|home|toy|gift|collectible/.test(lower)) return set.variety;
    return set.default;
  }
  function marketplacePriceForIndex(index) {
    const prices = [19.99, 24.5, 34.99, 49, 12.5, 79, 9.99, 59, 129, 17.99, 89, 39.99];
    return prices[index % prices.length];
  }
  function cleanPublicItemLabel(value) {
    const text = String(value || "").replace(/\s+/g, " ").trim();
    if (!text) return "";
    if (text.length > 48) return "";
    if (/[.!?¿]/.test(text)) return "";
    if (textSuggestsBroadMarketplace(text) && text.split(/\s+/).length > 3) return "";
    if (/\b(quiero|necesito|debe|deberia|página|pagina|website|marketplace|cliente|contacto|ubicaci[oó]n|telefono|tel[eé]fono|whatsapp|productos?|servicios?|vendo|vende|ofrece|online|etc)\b/i.test(text) && text.split(/\s+/).length > 3) {
      return "";
    }
    return text;
  }
  function inferredPublicCatalogLabels({ text = "", language = builderState.selectedLanguage } = {}) {
    const lower = String(text || "").toLowerCase();
    const labels = {
      es: {
        jewelry: ["Collares artesanales", "Pulseras", "Aretes y zarcillos"],
        fashion: ["Ropa seleccionada", "Accesorios", "Novedades"],
        marketplace: ["Hallazgos especiales", "Gadgets utiles", "Coleccionables"],
        restaurant: ["Platos destacados", "Especiales", "Bebidas"],
        service: ["Servicio principal", "Paquetes", "Consulta"],
        default: ["Oferta destacada", "Categorias clave", "Novedades"]
      },
      en: {
        jewelry: ["Handmade necklaces", "Bracelets", "Earrings"],
        fashion: ["Selected apparel", "Accessories", "New arrivals"],
        marketplace: ["Special finds", "Useful gadgets", "Collectibles"],
        restaurant: ["Signature dishes", "Specials", "Drinks"],
        service: ["Main service", "Packages", "Consultation"],
        default: ["Featured offer", "Key categories", "New arrivals"]
      },
      fr: {
        jewelry: ["Colliers artisanaux", "Bracelets", "Boucles d'oreilles"],
        fashion: ["Vetements selectionnes", "Accessoires", "Nouveautes"],
        marketplace: ["Trouvailles speciales", "Gadgets utiles", "Objets de collection"],
        restaurant: ["Plats signature", "Specials", "Boissons"],
        service: ["Service principal", "Forfaits", "Consultation"],
        default: ["Offre phare", "Categories cles", "Nouveautes"]
      },
      pt: {
        jewelry: ["Colares artesanais", "Pulseiras", "Brincos"],
        fashion: ["Roupas selecionadas", "Acessorios", "Novidades"],
        marketplace: ["Achados especiais", "Gadgets uteis", "Colecionaveis"],
        restaurant: ["Pratos destaque", "Especiais", "Bebidas"],
        service: ["Servico principal", "Pacotes", "Consulta"],
        default: ["Oferta destaque", "Categorias chave", "Novidades"]
      }
    };
    const set = labels[language] || labels.en;
    if (textSuggestsJewelryAccessoryStore(lower)) return set.jewelry;
    if (/ropa|moda|fashion|camisa|zapato|sneaker|clothing|apparel/.test(lower)) return set.fashion;
    if (/comida|restaurante|menu|food|restaurant|cafe/.test(lower)) return set.restaurant;
    if (/servicio|service|consulta|booking|cita|quote|cotiz/.test(lower)) return set.service;
    if (/carro|anime|gadget|juguete|curioso|raro|marketplace|store|shop|tienda|producto/.test(lower)) return set.marketplace;
    return set.default;
  }
  function textSuggestsCourseOffering(value = "") {
    return /\b(curso|cursos|course|courses|academy|academia|training|formaci[oó]n|taller|workshop|clase|classes)\b/i.test(String(value || ""));
  }
  function instantCoursePageForPayload(payload = {}, language = builderState.selectedLanguage, catalogItems = []) {
    const sourceText = [
      payload.business_description,
      payload.industry,
      arrayValue2(payload.services_products).join(" ")
    ].filter(Boolean).join(" ");
    if (!textSuggestsCourseOffering(sourceText)) return null;
    const labels = {
      es: { page: "Cursos", fallback: "Curso practico", audience: "Para personas que quieren aprender con una guia clara", includes: ["Contenido paso a paso", "Ejemplos practicos", "Orientacion para aplicar lo aprendido"], cta: "Inscribirme" },
      en: { page: "Courses", fallback: "Practical course", audience: "For people who want to learn with clear guidance", includes: ["Step-by-step content", "Practical examples", "Guidance to apply what you learn"], cta: "Enroll" },
      fr: { page: "Cours", fallback: "Cours pratique", audience: "Pour apprendre avec un accompagnement clair", includes: ["Contenu pas a pas", "Exemples pratiques", "Conseils d'application"], cta: "M'inscrire" },
      pt: { page: "Cursos", fallback: "Curso pratico", audience: "Para quem quer aprender com orientacao clara", includes: ["Conteudo passo a passo", "Exemplos praticos", "Orientacao para aplicar o aprendizado"], cta: "Inscrever-me" }
    };
    const copy = labels[language] || labels.en;
    const namedCourses = arrayValue2(payload.services_products).map((item) => String(item || "").trim()).filter((item) => textSuggestsCourseOffering(item)).slice(0, 4);
    const courses = namedCourses.length ? namedCourses : [copy.fallback];
    const directPurchase = /online_sales|sell online|venta online|ecommerce/i.test(`${payload.sales_flow || ""} ${payload.salesMode || ""}`);
    return {
      page_key: "courses",
      title: copy.page,
      slug: "/courses",
      order: 90,
      sections: courses.map((title, index) => ({
        id: `course-offering-${index + 1}`,
        type: "CourseOffering",
        order: index + 1,
        editable: {
          itemId: catalogItems.find((item) => String(item.name || "").trim() === title)?.id || `course-${index + 1}`,
          title,
          description: sourceText,
          audience: copy.audience,
          includes: copy.includes,
          videoUrl: "",
          ctaLabel: copy.cta,
          ctaMode: directPurchase ? "purchase" : "inquiry",
          priceLabel: ""
        },
        settings: { container_width: "wide" }
      }))
    };
  }
  function buildInstantTemplateSchema(payload, templateSelection) {
    const language = payload.selectedLanguage || builderState.selectedLanguage || "en";
    const template = templateSelection?.template || payload.selectedTemplate || {};
    const templateId = templateSelection?.templateId || payload.templateId || template.id || "";
    const catalogType = templateSelection?.catalogType || template.catalogModel?.catalogType || payload.catalogType || "editorial_minimal_grid";
    const templateInstructions = templateSelection ? buildTemplateInstructions(templateSelection) : payload.templateInstructions || {};
    const copy = instantLocaleCopy(language);
    const name = payload.business_name || copy.newStore;
    const products = sanitizePublicProductList(
      arrayValue2(payload.services_products).length ? arrayValue2(payload.services_products) : copy.defaultProducts,
      payload,
      copy,
      language,
      `${catalogType} ${template.id || ""}`
    );
    const description = professionalPublicDescription({ payload, template, templateId, catalogType, copy, name, products, language });
    const salesText = `${payload.salesMode || ""} ${payload.sales_mode || ""} ${payload.templateIntent || ""} ${template.category || ""} ${template.id || ""}`.toLowerCase();
    const isMegaRetailTemplate = catalogType === "single_vendor_dense_catalog" || catalogType === "dense_retail_catalog" || /mega-retail-store/i.test(template.id || "");
    const isMarketplaceTemplate = catalogType === "dense_marketplace_catalog" || /mega-marketplace|marketplace-style/i.test(template.id || "");
    const isOnlineShop = /sell online|online sales|shop|store|tienda|ecommerce|cart|checkout|vender|comprar|marketplace/.test(salesText) || isMarketplaceTemplate || isMegaRetailTemplate;
    const categoryContext = [
      payload.business_description,
      payload.industry,
      payload.templateIntent,
      arrayValue2(payload.services_products).join(" "),
      products.join(" "),
      template.id,
      catalogType
    ].join(" ");
    const brand = normalizeBrand(payload.brand || createBrandSystem({
      logoUrl: payload.assets?.find((asset) => asset.asset_type === "logo")?.url || "",
      extractedColors: payload.logoPalette,
      preferredColors: payload.preferred_colors,
      businessName: name,
      industry: payload.industry,
      tone: payload.preferred_tone
    }));
    const colors = brandToThemeColors(brand);
    const catalogItems = products.map((item, index) => ({
      id: `instant_${index + 1}`,
      sku: `SKU-${index + 1}`,
      name: item,
      description: copy.itemDescription(name),
      category: marketplaceCategoryForIndex(index, copy, categoryContext, language),
      rating: (4.3 + index % 5 * 0.12).toFixed(1),
      review_count: 42 + index * 31,
      shipping_label: index % 2 === 0 ? copy.fastDelivery : copy.freeShipping,
      deal_label: index % 3 === 0 ? copy.todayDeal : "",
      price_type: isOnlineShop ? "fixed" : "quote_only",
      price_amount: isMarketplaceTemplate || isMegaRetailTemplate ? marketplacePriceForIndex(index) : "",
      currency: "USD",
      price_label: isMarketplaceTemplate || isMegaRetailTemplate ? `USD ${marketplacePriceForIndex(index).toFixed(2)}` : isOnlineShop ? copy.priceNotSet : copy.askPrice,
      button_label: isOnlineShop ? copy.viewProduct : copy.request,
      inventory_quantity: isMarketplaceTemplate || isMegaRetailTemplate ? 24 + index * 3 : "",
      track_inventory: isOnlineShop,
      image_url: "",
      is_active: true,
      is_featured: index < 3,
      offer_type: textSuggestsCourseOffering(item) ? "course" : "product",
      display_in_catalog: !textSuggestsCourseOffering(item),
      sort_order: index
    }));
    const isPremiumTemplate = catalogType === "premium_editorial_catalog" || /premium-product-store|apple-premium-product/i.test(template.id || "");
    const isFashionTemplate = catalogType === "lookbook_collection_catalog" || /fashion-drop-pro/i.test(template.id || "");
    const isCorporateTemplate = catalogType === "company_services_catalog" || /corporate-company-pro/i.test(template.id || "");
    const isLeadFunnelTemplate = catalogType === "lead_funnel_offer_catalog" || /lead-funnel-pro/i.test(template.id || "");
    const isHomeServicesTemplate = catalogType === "home_services_quote_catalog" || catalogType === "service_area_catalog" || /home-services-premium|local-services-pro-plus/i.test(template.id || "");
    const isBookingTemplate = catalogType === "booking_menu_catalog" || /booking-appointment-pro/i.test(template.id || "");
    const isRestaurantTemplate = catalogType === "restaurant_menu_catalog" || catalogType === "menu_catalog" || /restaurant-food-business/i.test(template.id || "");
    const isDigitalTemplate = catalogType === "digital_offer_catalog" || /digital-products-store/i.test(template.id || "");
    const isRealEstateListingTemplate = catalogType === "real_estate_listing_catalog" || /real-estate-listings-pro/i.test(template.id || "");
    const isLuxuryHighTicketTemplate = catalogType === "luxury_high_ticket_catalog" || /luxury-high-ticket-pro/i.test(template.id || "");
    const isEducationTemplate = catalogType === "education_course_catalog" || /education-course-academy-pro/i.test(template.id || "");
    const isClinicTemplate = catalogType === "medical_wellness_service_catalog" || /medical-wellness-clinic-pro/i.test(template.id || "");
    const isProfessionalTemplate = catalogType === "legal_professional_services_catalog" || /legal-professional-services-pro/i.test(template.id || "");
    const isB2BTemplate = catalogType === "b2b_solution_catalog" || /b2b-saas-enterprise-pro/i.test(template.id || "");
    const isManufacturingTemplate = catalogType === "industrial_supplier_catalog" || /manufacturing-industrial-supplier-pro/i.test(template.id || "");
    const isBusinessWebsite = isCorporateTemplate || isLeadFunnelTemplate || isHomeServicesTemplate || isBookingTemplate || isRestaurantTemplate || isRealEstateListingTemplate || isLuxuryHighTicketTemplate || isEducationTemplate || isClinicTemplate || isProfessionalTemplate || isB2BTemplate || isManufacturingTemplate;
    const primaryCta = isManufacturingTemplate ? copy.requestQuote : isB2BTemplate ? copy.requestDemo : isProfessionalTemplate ? copy.scheduleConsultation : isClinicTemplate ? copy.bookConsultation : isEducationTemplate ? copy.enrollNow : isLuxuryHighTicketTemplate ? copy.requestPrivateViewing : isRealEstateListingTemplate ? copy.searchListings : isDigitalTemplate ? copy.getAccess : isRestaurantTemplate ? copy.orderNow : isBookingTemplate ? copy.bookNow : isHomeServicesTemplate ? copy.freeQuote : isCorporateTemplate ? copy.requestConsultation : isLeadFunnelTemplate ? copy.claimOffer : copy.shopNow;
    const secondaryCta = isManufacturingTemplate ? copy.viewSpecs : isB2BTemplate ? copy.viewSolutions : isProfessionalTemplate ? copy.viewServices : isClinicTemplate ? copy.viewTreatments : isEducationTemplate ? copy.viewCurriculum : isLuxuryHighTicketTemplate ? copy.viewCollection : isRealEstateListingTemplate ? copy.viewListings : isDigitalTemplate ? copy.viewProducts : isRestaurantTemplate ? copy.viewMenu : isBookingTemplate ? copy.viewServices : isHomeServicesTemplate ? copy.callNow : isLeadFunnelTemplate ? copy.seeProof : copy.viewCatalog;
    if (isLeadFunnelTemplate || isHomeServicesTemplate || isBookingTemplate || isRestaurantTemplate) {
      catalogItems.forEach((item) => {
        item.price_type = "quote_only";
        item.category = isRestaurantTemplate ? restaurantCategoryForIndex(item.sort_order || 0, copy) : item.category;
        item.price_label = isRestaurantTemplate ? copy.menuPrice : isBookingTemplate ? copy.fromQuote : copy.askPrice;
        item.button_label = isRestaurantTemplate ? copy.orderNow : isBookingTemplate ? copy.bookNow : isHomeServicesTemplate ? copy.freeQuote : copy.claimOffer;
        item.track_inventory = false;
      });
    }
    if (isDigitalTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "fixed";
        item.category = digitalCategoryForIndex(index, copy);
        item.price_label = item.price_label && item.price_label !== copy.priceNotSet ? item.price_label : copy.digitalPrice;
        item.button_label = copy.getAccess;
        item.track_inventory = false;
        item.shipping_label = copy.instantAccess;
      });
    }
    if (isRealEstateListingTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = listingCategoryForIndex(index, copy);
        item.price_label = item.price_label && item.price_label !== copy.priceNotSet ? item.price_label : copy.listingPrice;
        item.button_label = copy.inquireNow;
        item.shipping_label = listingLocationForIndex(index, copy);
        item.deal_label = index === 0 ? copy.featuredListing : index % 2 ? copy.newListing : copy.availableNow;
        item.track_inventory = false;
      });
    }
    if (isFashionTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = isOnlineShop ? "fixed" : "quote_only";
        item.category = fashionCategoryForIndex(index, copy, `${payload.business_description || ""} ${payload.industry || ""} ${arrayValue2(payload.services_products).join(" ")}`);
        item.price_label = item.price_label && item.price_label !== copy.priceNotSet ? item.price_label : copy.priceNotSet;
        item.button_label = isOnlineShop ? copy.viewProduct : copy.privateInquiry || copy.request;
        item.shipping_label = index % 2 ? copy.limitedSelection : copy.newDrop;
        item.deal_label = index === 0 ? copy.signaturePiece || copy.newDrop : index % 2 ? copy.limitedSelection : "";
        item.track_inventory = isOnlineShop;
      });
    }
    if (isLuxuryHighTicketTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = luxuryCategoryForIndex(index, copy);
        item.price_label = copy.priceOnRequest;
        item.button_label = copy.requestPrivateViewing;
        item.shipping_label = copy.privateService;
        item.deal_label = index === 0 ? copy.signaturePiece : index % 2 ? copy.authenticated : copy.limitedPiece;
        item.track_inventory = false;
      });
    }
    if (isEducationTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "fixed";
        item.category = educationCategoryForIndex(index, copy);
        item.price_label = item.price_label && item.price_label !== copy.priceNotSet ? item.price_label : copy.coursePrice;
        item.button_label = copy.enrollNow;
        item.shipping_label = educationDurationForIndex(index, copy);
        item.deal_label = index === 0 ? copy.featuredProgram : index % 2 ? copy.beginnerFriendly : copy.certificateReady;
        item.track_inventory = false;
      });
    }
    if (isClinicTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = clinicCategoryForIndex(index, copy);
        item.price_label = copy.consultationBased;
        item.button_label = copy.bookConsultation;
        item.shipping_label = clinicDurationForIndex(index, copy);
        item.deal_label = index === 0 ? copy.popularTreatment : index % 2 ? copy.specialistLed : copy.personalizedPlan;
        item.track_inventory = false;
      });
    }
    if (isProfessionalTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = professionalCategoryForIndex(index, copy);
        item.price_label = copy.consultationBased;
        item.button_label = copy.scheduleConsultation;
        item.shipping_label = professionalEngagementForIndex(index, copy);
        item.deal_label = index === 0 ? copy.confidential : index % 2 ? copy.seniorAdvisor : copy.caseReview;
        item.track_inventory = false;
      });
    }
    if (isB2BTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = enterpriseCategoryForIndex(index, copy);
        item.price_label = copy.customPlan;
        item.button_label = copy.requestDemo;
        item.shipping_label = enterpriseTimelineForIndex(index, copy);
        item.deal_label = index === 0 ? copy.enterpriseReady : index % 2 ? copy.integrationReady : copy.roiFocused;
        item.track_inventory = false;
      });
    }
    if (isManufacturingTemplate) {
      catalogItems.forEach((item, index) => {
        item.price_type = "quote_only";
        item.category = industrialCategoryForIndex(index, copy);
        item.price_label = copy.quoteRequired;
        item.button_label = copy.requestQuote;
        item.shipping_label = industrialLeadTimeForIndex(index, copy);
        item.deal_label = index === 0 ? copy.certified : index % 2 ? copy.bulkReady : copy.specReady;
        item.track_inventory = false;
        item.specs = {
          sku: `IND-${String(index + 1).padStart(3, "0")}`,
          moq: index % 2 ? "100+" : "25+",
          material: copy.industrialMaterials?.[index % (copy.industrialMaterials?.length || 1)] || copy.specReady,
          lead_time: item.shipping_label
        };
      });
    }
    const instantPages = isMegaRetailTemplate ? buildRetailInstantPages(copy, name, description, payload) : isMarketplaceTemplate ? buildMarketplaceInstantPages(copy, name, description, payload) : isPremiumTemplate ? buildPremiumProductInstantPages(copy, name, description, payload) : isLuxuryHighTicketTemplate ? buildLuxuryHighTicketInstantPages(copy, name, description, payload) : isEducationTemplate ? buildEducationAcademyInstantPages(copy, name, description, payload) : isClinicTemplate ? buildMedicalWellnessInstantPages(copy, name, description, payload) : isProfessionalTemplate ? buildLegalProfessionalInstantPages(copy, name, description, payload) : isB2BTemplate ? buildB2BEnterpriseInstantPages(copy, name, description, payload) : isManufacturingTemplate ? buildManufacturingIndustrialInstantPages(copy, name, description, payload) : isFashionTemplate ? buildFashionDropInstantPages(copy, name, description, payload) : isCorporateTemplate ? buildCorporateCompanyInstantPages(copy, name, description, payload) : isHomeServicesTemplate ? buildHomeServicesPremiumInstantPages(copy, name, description, payload) : isBookingTemplate ? buildBookingAppointmentInstantPages(copy, name, description, payload) : isRestaurantTemplate ? buildRestaurantMenuInstantPages(copy, name, description, payload) : isDigitalTemplate ? buildDigitalProductsInstantPages(copy, name, description, payload) : isRealEstateListingTemplate ? buildRealEstateListingsInstantPages(copy, name, description, payload) : isLeadFunnelTemplate ? buildLeadFunnelInstantPages(copy, name, description, payload) : buildDefaultInstantPages(copy, name, description, payload);
    const schema = {
      schema_version: "1.0",
      site_type: isBusinessWebsite ? "business_website" : "online_store",
      business: {
        name,
        description,
        industry: payload.industry || copy.onlineStore,
        location: payload.location || "",
        target_audience: payload.target_audience || "",
        tone: payload.preferred_tone || copy.defaultTone,
        selectedLanguage: language
      },
      theme: {
        colors,
        fonts: brand.fontPairing,
        buttons: {
          primary_label: primaryCta,
          secondary_label: secondaryCta,
          background: brand.buttonColor,
          text: brand.buttonTextColor,
          radius: brand.borderRadius
        },
        radius: Number.parseInt(brand.borderRadius, 10) || 10,
        shadow: brand.shadowStyle
      },
      brand,
      design_strategy: createDesignStrategy(payload, templateSelection, { brand }),
      layout_mode: {
        id: template.id || "instant_storefront",
        template_id: template.id || "",
        catalog_type: catalogType,
        intent: templateSelection?.intent || payload.templateIntent || "",
        navigation: { show_cart: !isBusinessWebsite, show_header: true, sticky_header: true },
        checkout: { mode: isBusinessWebsite ? "lead_capture" : isOnlineShop ? "cart_setup_required" : "quote_or_cart", primary_action: primaryCta }
      },
      integrations: { contact: { whatsapp_enabled: true, email_enabled: true }, analytics: { enabled: false, provider: "" }, payments: { enabled: false, mode: "setup_required" } },
      custom_logic: { enabled: false, risk_level: "restricted", automations: "" },
      site_plan: payload.sitePlan || buildSitePlan(templateSelection),
      navigation: isMegaRetailTemplate ? [
        { label: copy.home, page_key: "home" },
        { label: copy.deals, page_key: "catalog" },
        { label: copy.categories, page_key: "catalog" },
        { label: copy.support, page_key: "contact" }
      ] : isMarketplaceTemplate ? [
        { label: copy.home, page_key: "home" },
        { label: copy.deals, page_key: "catalog" },
        { label: copy.categories, page_key: "catalog" },
        { label: copy.support, page_key: "contact" }
      ] : isPremiumTemplate ? [
        { label: copy.overview, page_key: "home" },
        { label: copy.products, page_key: "catalog" },
        { label: copy.story, page_key: "about" },
        { label: copy.contact, page_key: "contact" }
      ] : isLuxuryHighTicketTemplate ? [
        { label: copy.collection, page_key: "home" },
        { label: copy.products, page_key: "catalog" },
        { label: copy.provenance, page_key: "about" },
        { label: copy.privateInquiry, page_key: "contact" }
      ] : isClinicTemplate ? [
        { label: copy.treatments, page_key: "home" },
        { label: copy.services, page_key: "catalog" },
        { label: copy.results, page_key: "about" },
        { label: copy.bookConsultation, page_key: "contact" }
      ] : isProfessionalTemplate ? [
        { label: copy.services, page_key: "home" },
        { label: copy.process, page_key: "about" },
        { label: copy.proof, page_key: "about" },
        { label: copy.scheduleConsultation, page_key: "contact" }
      ] : isManufacturingTemplate ? [
        { label: copy.products, page_key: "home" },
        { label: copy.capabilities, page_key: "about" },
        { label: copy.certifications, page_key: "about" },
        { label: copy.requestQuote, page_key: "contact" }
      ] : isB2BTemplate ? [
        { label: copy.solutions, page_key: "home" },
        { label: copy.useCases, page_key: "about" },
        { label: copy.integrations, page_key: "about" },
        { label: copy.requestDemo, page_key: "contact" }
      ] : isFashionTemplate ? [
        { label: copy.newDrop, page_key: "home" },
        { label: copy.collections, page_key: "catalog" },
        { label: copy.lookbook, page_key: "about" },
        { label: copy.contact, page_key: "contact" }
      ] : isCorporateTemplate ? [
        { label: copy.company, page_key: "home" },
        { label: copy.services, page_key: "catalog" },
        { label: copy.process, page_key: "about" },
        { label: copy.contact, page_key: "contact" }
      ] : isLeadFunnelTemplate ? [
        { label: copy.offer, page_key: "home" },
        { label: copy.benefits, page_key: "catalog" },
        { label: copy.proof, page_key: "about" },
        { label: copy.contact, page_key: "contact" }
      ] : isRestaurantTemplate ? [
        { label: copy.menu, page_key: "home" },
        { label: copy.specials, page_key: "catalog" },
        { label: copy.hoursLocation, page_key: "about" },
        { label: copy.orderNow, page_key: "contact" }
      ] : isDigitalTemplate ? [
        { label: copy.offer, page_key: "home" },
        { label: copy.products, page_key: "catalog" },
        { label: copy.modules, page_key: "about" },
        { label: copy.getAccess, page_key: "contact" }
      ] : isRealEstateListingTemplate ? [
        { label: copy.search, page_key: "home" },
        { label: copy.listings, page_key: "catalog" },
        { label: copy.areas, page_key: "about" },
        { label: copy.inquireNow, page_key: "contact" }
      ] : isHomeServicesTemplate ? [
        { label: copy.services, page_key: "home" },
        { label: copy.serviceAreas, page_key: "catalog" },
        { label: copy.workProof, page_key: "about" },
        { label: copy.freeQuote, page_key: "contact" }
      ] : isBookingTemplate ? [
        { label: copy.services, page_key: "home" },
        { label: copy.availability, page_key: "catalog" },
        { label: copy.team, page_key: "about" },
        { label: copy.bookNow, page_key: "contact" }
      ] : [
        { label: copy.home, page_key: "home" },
        { label: copy.shop, page_key: "catalog" },
        { label: copy.about, page_key: "about" },
        { label: copy.contact, page_key: "contact" }
      ],
      pages: instantPages,
      global_components: {
        logo_url: brand.logoUrl || payload.assets?.find((asset) => asset.asset_type === "logo")?.url || "",
        favicon_url: brand.logoUrl || payload.assets?.find((asset) => asset.asset_type === "logo")?.url || "",
        footer_text: copy.footerText(name)
      },
      selected_template: {
        id: template.id || "",
        name: template.name || "",
        category: template.category || "",
        intent: templateSelection?.intent || "",
        reason: templateSelection?.reason || "",
        visualDifference: template.visualDifference || "",
        clientSelectionCard: template.clientSelectionCard || {},
        sections: template.sections || [],
        pages: template.pages || [],
        instructions: templateInstructions
      },
      catalog_model: template.catalogModel || { catalogType },
      design_variants: [
        {
          id: "instant-modern",
          name: template.name || copy.modernCommercial,
          description: template.visualDifference || copy.fastBase,
          theme: { colors, fonts: brand.fontPairing, buttons: { primary_label: primaryCta, secondary_label: secondaryCta, background: brand.buttonColor, text: brand.buttonTextColor, radius: brand.borderRadius }, radius: Number.parseInt(brand.borderRadius, 10) || 10, shadow: brand.shadowStyle },
          layout_mode_id: template.id || "instant_storefront",
          hero_layout: isPremiumTemplate ? "premium_center_stage" : isLuxuryHighTicketTemplate ? "private_luxury_showroom" : isClinicTemplate ? "clinic_care_stage" : isProfessionalTemplate ? "professional_authority" : isB2BTemplate ? "enterprise_dashboard" : isFashionTemplate ? "fashion_editorial_drop" : isCorporateTemplate ? "corporate_editorial" : isLeadFunnelTemplate ? "conversion_funnel" : isHomeServicesTemplate ? "local_service_quote" : isBookingTemplate ? "appointment_booking" : isRestaurantTemplate ? "restaurant_menu_story" : isDigitalTemplate ? "digital_product_launch" : "split_showcase",
          product_layout: catalogType
        }
      ],
      products_services: catalogItems,
      catalog_items: catalogItems,
      contact: payload.contact_info || {},
      editable_fields: ["headline", "subtitle", "title", "text", "primary_button", "secondary_button", "image_url", "images"]
    };
    const coursePage = instantCoursePageForPayload(payload, language, catalogItems);
    if (coursePage && !schema.pages.some((page) => page.page_key === "courses")) {
      const contactIndex = schema.pages.findIndex((page) => page.page_key === "contact");
      schema.pages.splice(contactIndex >= 0 ? contactIndex : schema.pages.length, 0, coursePage);
      const contactNavIndex = schema.navigation.findIndex((item) => item.page_key === "contact");
      schema.navigation.splice(contactNavIndex >= 0 ? contactNavIndex : schema.navigation.length, 0, {
        label: coursePage.title,
        page_key: coursePage.page_key
      });
    }
    return schema;
  }
  function buildDefaultInstantPages(copy, name, description, payload = {}) {
    return [
      {
        page_key: "home",
        title: copy.home,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "hero",
            type: "Hero",
            order: 1,
            editable: {
              headline: name,
              subtitle: description,
              primary_button: copy.viewShop,
              secondary_button: copy.contactVerb,
              image_url: payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "",
              images: []
            },
            settings: { layout: "split_showcase" }
          },
          {
            id: "featured",
            type: "ProductGrid",
            order: 2,
            editable: {
              title: copy.featuredProducts,
              text: copy.featuredText,
              images: []
            },
            settings: { layout: "featured", columns: 3 }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.shop,
        slug: copy.shopSlug,
        order: 2,
        sections: [
          {
            id: "catalog_grid",
            type: "ProductGrid",
            order: 1,
            editable: {
              title: copy.catalog,
              text: copy.catalogText,
              images: []
            },
            settings: { layout: "grid", columns: 3 }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.about,
        slug: copy.aboutSlug,
        order: 3,
        sections: [{ id: "about", type: "About", order: 1, editable: { title: copy.aboutBrand, text: description }, settings: { layout: "feature" } }]
      },
      {
        page_key: "contact",
        title: copy.contact,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "contact", type: "Contact", order: 1, editable: { title: copy.letsTalk, text: copy.contactText }, settings: { layout: "simple" } }]
      }
    ];
  }
  function buildPremiumProductInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    const composition = runtimeCompositionIndex(name, payload);
    const recipes = [
      { hero: "split_showcase", order: ["premium_hero", "premium_story", "premium_feature", "premium_gallery", "premium_specs"] },
      { hero: "centered_bold", order: ["premium_hero", "premium_gallery", "premium_feature", "premium_story", "premium_specs"] },
      { hero: "asymmetric_grid", order: ["premium_hero", "premium_specs", "premium_story", "premium_gallery", "premium_feature"] }
    ];
    const recipe = recipes[composition];
    const pages = [
      {
        page_key: "home",
        title: copy.overview,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "premium_hero",
            type: "PremiumHero",
            order: 1,
            editable: {
              headline: copy.premiumHeadline(name),
              subtitle: copy.premiumSubheadline(description),
              primary_button: copy.premiumPrimary,
              secondary_button: copy.premiumSecondary,
              image_url: heroImage,
              images: []
            },
            variant: recipe.hero,
            settings: { layout: recipe.hero, spacing: "cinematic", container_width: "wide" }
          },
          {
            id: "premium_story",
            type: "ProductStory",
            order: 2,
            editable: {
              title: copy.premiumStoryTitle,
              text: copy.premiumStoryText,
              image_url: heroImage,
              images: []
            },
            variant: composition === 2 ? "image_left" : "feature_band",
            settings: { layout: "editorial_split", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "premium_feature",
            type: "FeatureShowcase",
            order: 3,
            editable: {
              title: copy.premiumFeatureTitle,
              text: copy.premiumFeatureText,
              images: []
            },
            variant: composition === 1 ? "card_grid" : "feature_band",
            settings: { layout: "feature_focus", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "premium_gallery",
            type: "PortfolioGallery",
            order: 4,
            editable: {
              title: copy.premiumGalleryTitle,
              text: copy.premiumGalleryText,
              images: []
            },
            settings: { layout: "premium_cards", columns: 3, spacing: "balanced", container_width: "wide" }
          },
          {
            id: "premium_specs",
            type: "SpecStrip",
            order: 5,
            editable: {
              title: copy.premiumSpecsTitle,
              text: copy.premiumSpecsText,
              items: copy.premiumSpecItems
            },
            settings: { layout: "quiet_specs", spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.products,
        slug: copy.shopSlug,
        order: 2,
        sections: [
          {
            id: "premium_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.premiumGalleryTitle, text: copy.premiumGalleryText, images: [] },
            settings: { layout: "premium_editorial", columns: 3, spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.story,
        slug: copy.aboutSlug,
        order: 3,
        sections: [{ id: "about", type: "ProductStory", order: 1, editable: { title: copy.aboutBrand, text: description, image_url: heroImage }, settings: { layout: "editorial_split", container_width: "wide" } }]
      },
      {
        page_key: "contact",
        title: copy.contact,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "contact", type: "Contact", order: 1, editable: { title: copy.letsTalk, text: copy.contactText }, settings: { layout: "simple", container_width: "wide" } }]
      }
    ];
    const home = pages.find((page) => page.page_key === "home");
    const byId = new Map(home.sections.map((section) => [section.id, section]));
    home.sections = resequenceSections(recipe.order.map((id) => byId.get(id)).filter(Boolean));
    return pages;
  }
  function buildLuxuryHighTicketInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.collection,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "luxury_hero",
            type: "LuxuryHero",
            order: 1,
            editable: {
              headline: copy.luxuryHeadline(name),
              subtitle: copy.luxurySubheadline(description),
              primary_button: copy.requestPrivateViewing,
              secondary_button: copy.viewCollection,
              image_url: heroImage,
              badge: copy.limitedSelection,
              images: []
            },
            settings: { layout: "private_showroom", spacing: "cinematic", container_width: "wide" }
          },
          {
            id: "luxury_signature",
            type: "LuxurySignature",
            order: 2,
            editable: {
              title: copy.signaturePiece,
              text: copy.luxurySignatureText,
              image_url: heroImage,
              items: copy.luxuryProofItems,
              images: []
            },
            settings: { layout: "signature_piece", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "luxury_collection",
            type: "LuxuryCollection",
            order: 3,
            editable: {
              title: copy.luxuryCollectionTitle,
              text: copy.luxuryCollectionText,
              images: []
            },
            settings: { layout: "editorial_collection", columns: 3, spacing: "spacious", container_width: "wide" }
          },
          {
            id: "luxury_provenance",
            type: "LuxuryProvenance",
            order: 4,
            editable: {
              title: copy.luxuryProvenanceTitle,
              text: copy.luxuryProvenanceText,
              items: copy.luxuryProofItems,
              images: []
            },
            settings: { layout: "provenance_grid", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "luxury_private_service",
            type: "LuxuryPrivateService",
            order: 5,
            editable: {
              title: copy.luxuryPrivateTitle,
              text: copy.luxuryPrivateText,
              primary_button: copy.requestPrivateViewing,
              images: []
            },
            settings: { layout: "concierge_cta", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.products,
        slug: copy.shopSlug,
        order: 2,
        sections: [
          {
            id: "luxury_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.luxuryCollectionTitle, text: copy.luxuryCollectionText, images: [] },
            settings: { layout: "luxury_high_ticket", columns: 3, spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.provenance,
        slug: copy.aboutSlug,
        order: 3,
        sections: [
          {
            id: "provenance",
            type: "LuxuryProvenance",
            order: 1,
            editable: { title: copy.luxuryProvenanceTitle, text: copy.luxuryProvenanceText, items: copy.luxuryProofItems, images: [] },
            settings: { layout: "provenance_grid", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "contact",
        title: copy.privateInquiry,
        slug: copy.contactSlug,
        order: 4,
        sections: [
          {
            id: "private_inquiry",
            type: "LuxuryContact",
            order: 1,
            editable: { title: copy.luxuryContactTitle, text: copy.luxuryContactText, primary_button: copy.requestPrivateViewing, images: [] },
            settings: { layout: "private_inquiry", spacing: "spacious", container_width: "wide" }
          }
        ]
      }
    ];
  }
  function buildFashionDropInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.newDrop,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "fashion_hero",
            type: "FashionHero",
            order: 1,
            editable: {
              headline: copy.fashionHeadline(name),
              subtitle: copy.fashionSubheadline(description),
              primary_button: copy.shopTheDrop,
              secondary_button: copy.viewLookbook,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "editorial_drop", spacing: "cinematic", container_width: "wide" }
          },
          {
            id: "fashion_collections",
            type: "FashionCollectionRail",
            order: 2,
            editable: { title: copy.collections, text: copy.collectionsText },
            settings: { layout: "collection_tiles", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "fashion_drop_story",
            type: "FashionDropStory",
            order: 3,
            editable: { title: copy.dropStoryTitle, text: copy.dropStoryText, image_url: heroImage },
            settings: { layout: "split_editorial", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "fashion_lookbook",
            type: "FashionLookbook",
            order: 4,
            editable: { title: copy.lookbook, text: copy.lookbookText },
            settings: { layout: "horizontal_lookbook", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "fashion_products",
            type: "ProductGrid",
            order: 5,
            editable: { title: copy.newArrivals, text: copy.newArrivalsText, images: [] },
            settings: { layout: "fashion_drop_grid", columns: 3, spacing: "spacious", container_width: "wide" }
          },
          {
            id: "fashion_fit",
            type: "FashionFitGuide",
            order: 6,
            editable: { title: copy.fitGuideTitle, text: copy.fitGuideText, items: copy.fitGuideItems },
            settings: { layout: "fit_guide", spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.collections,
        slug: copy.shopSlug,
        order: 2,
        sections: [
          {
            id: "fashion_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.collections, text: copy.newArrivalsText, images: [] },
            settings: { layout: "fashion_drop_grid", columns: 3, spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.lookbook,
        slug: copy.aboutSlug,
        order: 3,
        sections: [{ id: "lookbook", type: "FashionLookbook", order: 1, editable: { title: copy.lookbook, text: copy.lookbookText }, settings: { layout: "horizontal_lookbook", container_width: "wide" } }]
      },
      {
        page_key: "contact",
        title: copy.contact,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "contact", type: "Contact", order: 1, editable: { title: copy.letsTalk, text: copy.contactText }, settings: { layout: "simple", container_width: "wide" } }]
      }
    ];
  }
  function buildCorporateCompanyInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.company,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "corporate_hero",
            type: "CorporateHero",
            order: 1,
            editable: {
              headline: copy.corporateHeadline(name),
              subtitle: copy.corporateSubheadline(description),
              primary_button: copy.requestConsultation,
              secondary_button: copy.viewServices,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "corporate_editorial" }
          },
          {
            id: "corporate_services",
            type: "CorporateServices",
            order: 2,
            editable: { title: copy.corporateServicesTitle, text: copy.corporateServicesText, images: [] },
            settings: { layout: "capabilities", columns: 3 }
          },
          {
            id: "corporate_process",
            type: "CorporateProcess",
            order: 3,
            editable: { title: copy.corporateProcessTitle, text: copy.corporateProcessText, items: copy.corporateProcessItems },
            settings: { layout: "numbered_steps" }
          },
          {
            id: "corporate_proof",
            type: "CorporateProof",
            order: 4,
            editable: { title: copy.corporateProofTitle, text: copy.corporateProofText, items: copy.corporateProofItems },
            settings: { layout: "proof_panel" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.services,
        slug: copy.servicesSlug,
        order: 2,
        sections: [
          {
            id: "services_grid",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.corporateServicesTitle, text: copy.corporateServicesText, images: [] },
            settings: { layout: "company_services", columns: 3 }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.process,
        slug: copy.processSlug,
        order: 3,
        sections: [
          { id: "process", type: "CorporateProcess", order: 1, editable: { title: copy.corporateProcessTitle, text: copy.corporateProcessText, items: copy.corporateProcessItems }, settings: { layout: "numbered_steps" } },
          { id: "proof", type: "CorporateProof", order: 2, editable: { title: copy.corporateProofTitle, text: copy.corporateProofText, items: copy.corporateProofItems }, settings: { layout: "proof_panel" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.contact,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "contact", type: "Contact", order: 1, editable: { title: copy.letsTalk, text: copy.corporateContactText }, settings: { layout: "simple" } }]
      }
    ];
  }
  function buildRestaurantMenuInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.menu,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "restaurant_hero",
            type: "RestaurantHero",
            order: 1,
            editable: {
              headline: copy.restaurantHeadline(name),
              subtitle: copy.restaurantSubheadline(description),
              primary_button: copy.orderNow,
              secondary_button: copy.viewMenu,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "restaurant_menu_story", spacing: "cinematic", container_width: "wide" }
          },
          {
            id: "restaurant_categories",
            type: "RestaurantCategoryRail",
            order: 2,
            editable: { title: copy.restaurantCategoriesTitle, text: copy.restaurantCategoriesText, items: copy.restaurantCategories },
            settings: { layout: "menu_category_rail", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "restaurant_signature",
            type: "RestaurantSignatureMenu",
            order: 3,
            editable: { title: copy.restaurantSignatureTitle, text: copy.restaurantSignatureText, images: [] },
            settings: { layout: "signature_menu", columns: 3, spacing: "spacious", container_width: "wide" }
          },
          {
            id: "restaurant_specials",
            type: "RestaurantSpecials",
            order: 4,
            editable: { title: copy.restaurantSpecialsTitle, text: copy.restaurantSpecialsText, items: copy.restaurantSpecialItems },
            settings: { layout: "specials_strip", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "restaurant_info",
            type: "RestaurantInfo",
            order: 5,
            editable: { title: copy.restaurantInfoTitle, text: copy.restaurantInfoText, items: copy.restaurantInfoItems },
            settings: { layout: "hours_location", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "restaurant_order",
            type: "RestaurantOrderPanel",
            order: 6,
            editable: { title: copy.restaurantOrderTitle, text: copy.restaurantOrderText },
            settings: { layout: "order_panel", spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.specials,
        slug: copy.menuSlug,
        order: 2,
        sections: [
          {
            id: "restaurant_menu_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.restaurantSignatureTitle, text: copy.restaurantSignatureText, images: [] },
            settings: { layout: "restaurant_menu", columns: 3, spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.hoursLocation,
        slug: copy.hoursSlug,
        order: 3,
        sections: [
          { id: "restaurant_info_page", type: "RestaurantInfo", order: 1, editable: { title: copy.restaurantInfoTitle, text: copy.restaurantInfoText, items: copy.restaurantInfoItems }, settings: { layout: "hours_location", container_width: "wide" } },
          { id: "restaurant_specials_page", type: "RestaurantSpecials", order: 2, editable: { title: copy.restaurantSpecialsTitle, text: copy.restaurantSpecialsText, items: copy.restaurantSpecialItems }, settings: { layout: "specials_strip", container_width: "wide" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.orderNow,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "restaurant_order_page", type: "RestaurantOrderPanel", order: 1, editable: { title: copy.restaurantOrderTitle, text: copy.restaurantOrderText }, settings: { layout: "order_panel", container_width: "wide" } }]
      }
    ];
  }
  function buildEducationAcademyInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.academy,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "academy_hero",
            type: "AcademyHero",
            order: 1,
            editable: {
              headline: copy.educationHeadline(name),
              subtitle: copy.educationSubheadline(description),
              primary_button: copy.enrollNow,
              secondary_button: copy.viewCurriculum,
              image_url: heroImage,
              badge: copy.courseAcademy,
              images: []
            },
            settings: { layout: "academy_platform", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "academy_path",
            type: "AcademyLearningPath",
            order: 2,
            editable: {
              title: copy.learningPathTitle,
              text: copy.learningPathText,
              items: copy.learningPathItems,
              images: []
            },
            settings: { layout: "module_roadmap", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "academy_programs",
            type: "AcademyPrograms",
            order: 3,
            editable: {
              title: copy.programsTitle,
              text: copy.programsText,
              images: []
            },
            settings: { layout: "program_cards", columns: 3, spacing: "balanced", container_width: "wide" }
          },
          {
            id: "academy_outcomes",
            type: "AcademyOutcomes",
            order: 4,
            editable: {
              title: copy.outcomesTitle,
              text: copy.outcomesText,
              items: copy.learningOutcomeItems,
              images: []
            },
            settings: { layout: "outcome_grid", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "academy_instructor",
            type: "AcademyInstructor",
            order: 5,
            editable: {
              title: copy.instructorTitle,
              text: copy.instructorText,
              items: copy.instructorTrustItems,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "instructor_trust", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "academy_enroll",
            type: "AcademyEnroll",
            order: 6,
            editable: {
              title: copy.enrollmentTitle,
              text: copy.enrollmentText,
              primary_button: copy.enrollNow,
              images: []
            },
            settings: { layout: "enrollment_cta", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "programs",
        title: copy.programs,
        slug: copy.programsSlug,
        order: 2,
        sections: [
          {
            id: "program_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.programsTitle, text: copy.programsText, images: [] },
            settings: { layout: "education_course", columns: 3, spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "curriculum",
        title: copy.curriculum,
        slug: copy.curriculumSlug,
        order: 3,
        sections: [
          {
            id: "curriculum_path",
            type: "AcademyLearningPath",
            order: 1,
            editable: { title: copy.learningPathTitle, text: copy.learningPathText, items: copy.learningPathItems, images: [] },
            settings: { layout: "module_roadmap", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "enroll",
        title: copy.enroll,
        slug: copy.enrollSlug,
        order: 4,
        sections: [
          {
            id: "enroll_contact",
            type: "AcademyEnroll",
            order: 1,
            editable: { title: copy.enrollmentTitle, text: copy.enrollmentText, primary_button: copy.enrollNow, images: [] },
            settings: { layout: "enrollment_cta", spacing: "spacious", container_width: "wide" }
          }
        ]
      }
    ];
  }
  function buildMedicalWellnessInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.treatments,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "clinic_hero",
            type: "ClinicHero",
            order: 1,
            editable: {
              headline: copy.clinicHeadline(name),
              subtitle: copy.clinicSubheadline(description),
              primary_button: copy.bookConsultation,
              secondary_button: copy.viewTreatments,
              image_url: heroImage,
              badge: copy.clinicCare,
              images: []
            },
            settings: { layout: "clinic_care_stage", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "clinic_services",
            type: "ClinicServices",
            order: 2,
            editable: { title: copy.treatmentsTitle, text: copy.treatmentsText, images: [] },
            settings: { layout: "treatment_cards", columns: 3, spacing: "balanced", container_width: "wide" }
          },
          {
            id: "clinic_path",
            type: "ClinicTreatmentPath",
            order: 3,
            editable: { title: copy.carePathTitle, text: copy.carePathText, items: copy.carePathItems, images: [] },
            settings: { layout: "care_path", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "clinic_trust",
            type: "ClinicTrust",
            order: 4,
            editable: { title: copy.clinicTrustTitle, text: copy.clinicTrustText, items: copy.clinicTrustItems, images: [] },
            settings: { layout: "trust_credentials", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "clinic_results",
            type: "ClinicResults",
            order: 5,
            editable: { title: copy.resultsTitle, text: copy.resultsText, items: copy.resultItems, images: [] },
            settings: { layout: "results_proof", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "clinic_team",
            type: "ClinicTeam",
            order: 6,
            editable: { title: copy.clinicTeamTitle, text: copy.clinicTeamText, items: copy.clinicTeamItems, image_url: heroImage, images: [] },
            settings: { layout: "specialist_team", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "clinic_booking",
            type: "ClinicBooking",
            order: 7,
            editable: { title: copy.clinicBookingTitle, text: copy.clinicBookingText, primary_button: copy.bookConsultation, images: [] },
            settings: { layout: "booking_cta", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.services,
        slug: copy.servicesSlug,
        order: 2,
        sections: [
          {
            id: "clinic_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.treatmentsTitle, text: copy.treatmentsText, images: [] },
            settings: { layout: "medical_wellness", columns: 3, spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.results,
        slug: copy.resultsSlug,
        order: 3,
        sections: [
          { id: "results", type: "ClinicResults", order: 1, editable: { title: copy.resultsTitle, text: copy.resultsText, items: copy.resultItems, images: [] }, settings: { layout: "results_proof", container_width: "wide" } },
          { id: "team", type: "ClinicTeam", order: 2, editable: { title: copy.clinicTeamTitle, text: copy.clinicTeamText, items: copy.clinicTeamItems, image_url: heroImage, images: [] }, settings: { layout: "specialist_team", container_width: "wide" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.bookConsultation,
        slug: copy.contactSlug,
        order: 4,
        sections: [
          { id: "booking", type: "ClinicBooking", order: 1, editable: { title: copy.clinicBookingTitle, text: copy.clinicBookingText, primary_button: copy.bookConsultation, images: [] }, settings: { layout: "booking_cta", container_width: "wide" } }
        ]
      }
    ];
  }
  function buildLegalProfessionalInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.services,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "professional_hero",
            type: "ProfessionalHero",
            order: 1,
            editable: {
              headline: copy.professionalHeadline(name),
              subtitle: copy.professionalSubheadline(description),
              primary_button: copy.scheduleConsultation,
              secondary_button: copy.viewServices,
              image_url: heroImage,
              badge: copy.professionalFirm,
              images: []
            },
            settings: { layout: "professional_authority", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "professional_services",
            type: "ProfessionalPracticeAreas",
            order: 2,
            editable: { title: copy.practiceAreasTitle, text: copy.practiceAreasText, images: [] },
            settings: { layout: "practice_area_cards", columns: 3, spacing: "balanced", container_width: "wide" }
          },
          {
            id: "professional_process",
            type: "ProfessionalProcess",
            order: 3,
            editable: { title: copy.professionalProcessTitle, text: copy.professionalProcessText, items: copy.professionalProcessItems, images: [] },
            settings: { layout: "advisory_process", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "professional_proof",
            type: "ProfessionalProof",
            order: 4,
            editable: { title: copy.professionalProofTitle, text: copy.professionalProofText, items: copy.professionalProofItems, images: [] },
            settings: { layout: "trust_proof", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "professional_team",
            type: "ProfessionalTeam",
            order: 5,
            editable: { title: copy.professionalTeamTitle, text: copy.professionalTeamText, items: copy.professionalTeamItems, image_url: heroImage, images: [] },
            settings: { layout: "advisor_team", spacing: "spacious", container_width: "wide" }
          },
          {
            id: "professional_faq",
            type: "ProfessionalFAQ",
            order: 6,
            editable: { title: copy.professionalFaqTitle, items: copy.professionalFaqItems, images: [] },
            settings: { layout: "firm_faq", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "professional_consultation",
            type: "ProfessionalConsultation",
            order: 7,
            editable: { title: copy.professionalConsultationTitle, text: copy.professionalConsultationText, primary_button: copy.scheduleConsultation, images: [] },
            settings: { layout: "consultation_cta", spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.services,
        slug: copy.servicesSlug,
        order: 2,
        sections: [
          {
            id: "professional_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.practiceAreasTitle, text: copy.practiceAreasText, images: [] },
            settings: { layout: "legal_professional", columns: 3, spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.process,
        slug: copy.processSlug,
        order: 3,
        sections: [
          { id: "process", type: "ProfessionalProcess", order: 1, editable: { title: copy.professionalProcessTitle, text: copy.professionalProcessText, items: copy.professionalProcessItems, images: [] }, settings: { layout: "advisory_process", container_width: "wide" } },
          { id: "proof", type: "ProfessionalProof", order: 2, editable: { title: copy.professionalProofTitle, text: copy.professionalProofText, items: copy.professionalProofItems, images: [] }, settings: { layout: "trust_proof", container_width: "wide" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.scheduleConsultation,
        slug: copy.contactSlug,
        order: 4,
        sections: [
          { id: "consultation", type: "ProfessionalConsultation", order: 1, editable: { title: copy.professionalConsultationTitle, text: copy.professionalConsultationText, primary_button: copy.scheduleConsultation, images: [] }, settings: { layout: "consultation_cta", container_width: "wide" } }
        ]
      }
    ];
  }
  function buildB2BEnterpriseInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.solutions,
        slug: "/",
        order: 1,
        sections: [
          { id: "enterprise_hero", type: "EnterpriseHero", order: 1, editable: { headline: copy.enterpriseHeadline(name), subtitle: copy.enterpriseSubheadline(description), primary_button: copy.requestDemo, secondary_button: copy.viewSolutions, image_url: heroImage, badge: copy.enterprisePlatform, images: [] }, settings: { layout: "enterprise_dashboard", spacing: "spacious", container_width: "wide" } },
          { id: "enterprise_solutions", type: "EnterpriseSolutions", order: 2, editable: { title: copy.enterpriseSolutionsTitle, text: copy.enterpriseSolutionsText, images: [] }, settings: { layout: "solution_cards", columns: 3, spacing: "balanced", container_width: "wide" } },
          { id: "enterprise_use_cases", type: "EnterpriseUseCases", order: 3, editable: { title: copy.enterpriseUseCasesTitle, text: copy.enterpriseUseCasesText, items: copy.enterpriseUseCaseItems, images: [] }, settings: { layout: "use_case_matrix", spacing: "balanced", container_width: "wide" } },
          { id: "enterprise_integrations", type: "EnterpriseIntegrations", order: 4, editable: { title: copy.enterpriseIntegrationsTitle, text: copy.enterpriseIntegrationsText, items: copy.enterpriseIntegrationItems, images: [] }, settings: { layout: "integration_map", spacing: "balanced", container_width: "wide" } },
          { id: "enterprise_proof", type: "EnterpriseProof", order: 5, editable: { title: copy.enterpriseProofTitle, text: copy.enterpriseProofText, items: copy.enterpriseProofItems, images: [] }, settings: { layout: "roi_proof", spacing: "balanced", container_width: "wide" } },
          { id: "enterprise_pricing", type: "EnterprisePricing", order: 6, editable: { title: copy.enterprisePricingTitle, text: copy.enterprisePricingText, images: [] }, settings: { layout: "pricing_packages", spacing: "balanced", container_width: "wide" } },
          { id: "enterprise_demo", type: "EnterpriseDemo", order: 7, editable: { title: copy.enterpriseDemoTitle, text: copy.enterpriseDemoText, primary_button: copy.requestDemo, images: [] }, settings: { layout: "demo_cta", spacing: "spacious", container_width: "wide" } }
        ]
      },
      { page_key: "catalog", title: copy.solutions, slug: copy.solutionsSlug, order: 2, sections: [{ id: "enterprise_catalog", type: "ProductGrid", order: 1, editable: { title: copy.enterpriseSolutionsTitle, text: copy.enterpriseSolutionsText, images: [] }, settings: { layout: "b2b_solution", columns: 3, spacing: "balanced", container_width: "wide" } }] },
      { page_key: "about", title: copy.useCases, slug: copy.useCasesSlug, order: 3, sections: [
        { id: "use_cases", type: "EnterpriseUseCases", order: 1, editable: { title: copy.enterpriseUseCasesTitle, text: copy.enterpriseUseCasesText, items: copy.enterpriseUseCaseItems, images: [] }, settings: { layout: "use_case_matrix", container_width: "wide" } },
        { id: "integrations", type: "EnterpriseIntegrations", order: 2, editable: { title: copy.enterpriseIntegrationsTitle, text: copy.enterpriseIntegrationsText, items: copy.enterpriseIntegrationItems, images: [] }, settings: { layout: "integration_map", container_width: "wide" } }
      ] },
      { page_key: "contact", title: copy.requestDemo, slug: copy.contactSlug, order: 4, sections: [{ id: "demo", type: "EnterpriseDemo", order: 1, editable: { title: copy.enterpriseDemoTitle, text: copy.enterpriseDemoText, primary_button: copy.requestDemo, images: [] }, settings: { layout: "demo_cta", container_width: "wide" } }] }
    ];
  }
  function buildManufacturingIndustrialInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.industrialSupplier,
        slug: "/",
        order: 1,
        sections: [
          { id: "industrial_hero", type: "IndustrialHero", order: 1, editable: { headline: copy.industrialHeadline(name), subtitle: copy.industrialSubheadline(description), primary_button: copy.requestQuote, secondary_button: copy.viewSpecs, image_url: heroImage, badge: copy.industrialSupplier, images: [] }, settings: { layout: "industrial_rfq_dashboard", spacing: "spacious", container_width: "wide" } },
          { id: "industrial_catalog", type: "IndustrialSpecCatalog", order: 2, editable: { title: copy.industrialCatalogTitle, text: copy.industrialCatalogText, images: [] }, settings: { layout: "spec_cards", columns: 3, spacing: "compact", container_width: "wide" } },
          { id: "industrial_capabilities", type: "CapabilitiesEquipment", order: 3, editable: { title: copy.industrialCapabilitiesTitle, text: copy.industrialCapabilitiesText, items: copy.industrialCapabilityItems, images: [] }, settings: { layout: "capability_matrix", spacing: "balanced", container_width: "wide" } },
          { id: "industrial_certifications", type: "IndustrialCertifications", order: 4, editable: { title: copy.industrialCertificationsTitle, text: copy.industrialCertificationsText, items: copy.industrialCertificationItems, images: [] }, settings: { layout: "certification_grid", spacing: "balanced", container_width: "wide" } },
          { id: "industrial_supply", type: "IndustrialSupplyChain", order: 5, editable: { title: copy.industrialSupplyTitle, text: copy.industrialSupplyText, items: copy.industrialSupplyItems, images: [] }, settings: { layout: "supply_timeline", spacing: "balanced", container_width: "wide" } },
          { id: "industrial_quote", type: "QuoteRequestForm", order: 6, editable: { title: copy.industrialQuoteTitle, text: copy.industrialQuoteText, primary_button: copy.requestQuote, images: [] }, settings: { layout: "rfq_panel", spacing: "spacious", container_width: "wide" } }
        ]
      },
      { page_key: "catalog", title: copy.products, slug: copy.shopSlug, order: 2, sections: [{ id: "industrial_products", type: "ProductGrid", order: 1, editable: { title: copy.industrialCatalogTitle, text: copy.industrialCatalogText, images: [] }, settings: { layout: "industrial_specs", columns: 3, spacing: "compact", container_width: "wide" } }] },
      { page_key: "about", title: copy.capabilities, slug: "/capabilities", order: 3, sections: [
        { id: "capabilities", type: "CapabilitiesEquipment", order: 1, editable: { title: copy.industrialCapabilitiesTitle, text: copy.industrialCapabilitiesText, items: copy.industrialCapabilityItems, images: [] }, settings: { layout: "capability_matrix", container_width: "wide" } },
        { id: "certifications", type: "IndustrialCertifications", order: 2, editable: { title: copy.industrialCertificationsTitle, text: copy.industrialCertificationsText, items: copy.industrialCertificationItems, images: [] }, settings: { layout: "certification_grid", container_width: "wide" } },
        { id: "supply", type: "IndustrialSupplyChain", order: 3, editable: { title: copy.industrialSupplyTitle, text: copy.industrialSupplyText, items: copy.industrialSupplyItems, images: [] }, settings: { layout: "supply_timeline", container_width: "wide" } }
      ] },
      { page_key: "contact", title: copy.requestQuote, slug: copy.contactSlug, order: 4, sections: [{ id: "quote", type: "QuoteRequestForm", order: 1, editable: { title: copy.industrialQuoteTitle, text: copy.industrialQuoteText, primary_button: copy.requestQuote, images: [] }, settings: { layout: "rfq_panel", container_width: "wide" } }] }
    ];
  }
  function buildDigitalProductsInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.offer,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "digital_hero",
            type: "DigitalHero",
            order: 1,
            editable: {
              headline: copy.digitalHeadline(name),
              subtitle: copy.digitalSubheadline(description),
              primary_button: copy.getAccess,
              secondary_button: copy.viewProducts,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "digital_product_launch", spacing: "cinematic", container_width: "wide" }
          },
          {
            id: "digital_bundle",
            type: "DigitalBundle",
            order: 2,
            editable: { title: copy.digitalBundleTitle, text: copy.digitalBundleText, images: [] },
            settings: { layout: "bundle_cards", columns: 3, spacing: "spacious", container_width: "wide" }
          },
          {
            id: "digital_access",
            type: "DigitalAccessPanel",
            order: 3,
            editable: { title: copy.digitalAccessTitle, text: copy.digitalAccessText },
            settings: { layout: "access_panel", spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.products,
        slug: copy.shopSlug,
        order: 2,
        sections: [
          {
            id: "digital_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.digitalBundleTitle, text: copy.digitalBundleText, images: [] },
            settings: { layout: "digital_offer_cards", columns: 3, spacing: "spacious", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.modules,
        slug: copy.modulesSlug,
        order: 3,
        sections: [
          { id: "digital_modules_page", type: "DigitalModules", order: 1, editable: { title: copy.digitalModulesTitle, text: copy.digitalModulesText, items: copy.digitalModuleItems }, settings: { layout: "module_grid", container_width: "wide" } },
          { id: "digital_proof_page", type: "DigitalProof", order: 2, editable: { title: copy.digitalProofTitle, text: copy.digitalProofText, items: copy.digitalProofItems }, settings: { layout: "proof_panel", container_width: "wide" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.getAccess,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "digital_access_page", type: "DigitalAccessPanel", order: 1, editable: { title: copy.digitalAccessTitle, text: copy.digitalAccessText }, settings: { layout: "access_panel", container_width: "wide" } }]
      }
    ];
  }
  function buildRealEstateListingsInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.search,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "listing_hero",
            type: "ListingHero",
            order: 1,
            editable: {
              headline: copy.listingHeadline(name),
              subtitle: description || copy.listingSubheadline(),
              primary_button: copy.searchListings,
              secondary_button: copy.viewListings,
              search_placeholder: copy.listingSearchPlaceholder,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "search_map", spacing: "compact", container_width: "wide" }
          },
          {
            id: "listing_filters",
            type: "ListingFilters",
            order: 2,
            editable: { title: copy.listingFiltersTitle, text: copy.listingFiltersText, items: copy.listingCategories },
            settings: { layout: "filter_bar", spacing: "compact", container_width: "wide" }
          },
          {
            id: "featured_listings",
            type: "ListingFeatured",
            order: 3,
            editable: { title: copy.featuredListingsTitle, text: copy.featuredListingsText, images: [] },
            settings: { layout: "listing_cards", columns: 3, spacing: "balanced", container_width: "wide" }
          },
          {
            id: "area_map",
            type: "ListingAreaPanel",
            order: 4,
            editable: { title: copy.listingAreaTitle, text: copy.listingAreaText, items: copy.listingLocations },
            settings: { layout: "map_panel", spacing: "balanced", container_width: "wide" }
          },
          {
            id: "listing_trust",
            type: "ListingTrust",
            order: 5,
            editable: { title: copy.listingTrustTitle, text: copy.listingTrustText, items: copy.listingTrustItems },
            settings: { layout: "trust_row", spacing: "compact", container_width: "wide" }
          },
          {
            id: "listing_contact",
            type: "ListingContact",
            order: 6,
            editable: { title: copy.listingContactTitle, text: copy.listingContactText, images: [] },
            settings: { layout: "inquiry_panel", spacing: "balanced", container_width: "wide" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.listings,
        slug: copy.listingsSlug,
        order: 2,
        sections: [
          {
            id: "listings_catalog",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.featuredListingsTitle, text: copy.featuredListingsText, images: [] },
            settings: { layout: "listing_catalog", columns: 3, spacing: "compact", container_width: "wide", card_density: "comfortable", card_gap: "comfortable" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.areas,
        slug: copy.areasSlug,
        order: 3,
        sections: [
          { id: "areas", type: "ListingAreaPanel", order: 1, editable: { title: copy.listingAreaTitle, text: copy.listingAreaText, items: copy.listingLocations }, settings: { layout: "map_panel", container_width: "wide" } },
          { id: "trust", type: "ListingTrust", order: 2, editable: { title: copy.listingTrustTitle, text: copy.listingTrustText, items: copy.listingTrustItems }, settings: { layout: "trust_row", container_width: "wide" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.inquireNow,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "contact", type: "ListingContact", order: 1, editable: { title: copy.listingContactTitle, text: copy.listingContactText }, settings: { layout: "inquiry_panel", container_width: "wide" } }]
      }
    ];
  }
  function normalizeHomeServiceInstantCopy(input = {}, language = builderState.selectedLanguage) {
    const base = instantLocaleCopy(language);
    const source = input && typeof input === "object" ? input : {};
    const merged = { ...base, ...source };
    const textValue = (key) => {
      const value = merged[key];
      return typeof value === "string" && value.trim() ? value : base[key] || "";
    };
    const listValue = (key) => {
      const values = arrayValue2(merged[key]).filter(Boolean);
      return values.length ? values : arrayValue2(base[key]);
    };
    return {
      ...merged,
      services: textValue("services") || base.services || "Services",
      servicesSlug: textValue("servicesSlug") || "/services",
      serviceAreas: textValue("serviceAreas") || base.serviceAreas || "Service areas",
      workProof: textValue("workProof") || base.workProof || "Work proof",
      workSlug: textValue("workSlug") || "/work",
      contactSlug: textValue("contactSlug") || "/contact",
      freeQuote: textValue("freeQuote") || textValue("request") || "Request quote",
      callNow: textValue("callNow") || "Call now",
      askPrice: textValue("askPrice") || "Ask for price",
      homeServiceHeadline: typeof merged.homeServiceHeadline === "function" ? merged.homeServiceHeadline : typeof base.homeServiceHeadline === "function" ? base.homeServiceHeadline : (businessName) => `${businessName} handles the job right the first time`,
      homeServiceSubheadline: typeof merged.homeServiceSubheadline === "function" ? merged.homeServiceSubheadline : typeof base.homeServiceSubheadline === "function" ? base.homeServiceSubheadline : (value) => value || "Trusted service with clear communication, proof, and an easy quote path.",
      homeServiceCategoriesTitle: textValue("homeServiceCategoriesTitle"),
      homeServiceCategoriesText: textValue("homeServiceCategoriesText"),
      homeServiceAreasTitle: textValue("homeServiceAreasTitle"),
      homeServiceAreasText: textValue("homeServiceAreasText"),
      serviceAreaItems: listValue("serviceAreaItems"),
      beforeAfterTitle: textValue("beforeAfterTitle"),
      beforeAfterText: textValue("beforeAfterText"),
      homeServiceTrustTitle: textValue("homeServiceTrustTitle"),
      homeServiceTrustText: textValue("homeServiceTrustText"),
      homeServiceTrustItems: listValue("homeServiceTrustItems"),
      homeServiceQuoteTitle: textValue("homeServiceQuoteTitle"),
      homeServiceQuoteText: textValue("homeServiceQuoteText")
    };
  }
  function buildHomeServicesPremiumInstantPages(copy, name, description, payload = {}) {
    copy = normalizeHomeServiceInstantCopy(copy, payload.selectedLanguage || builderState.selectedLanguage || "en");
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    const homeHeadline = typeof copy.homeServiceHeadline === "function" ? copy.homeServiceHeadline(name) : String(copy.homeServiceHeadline || `${name} handles the job right the first time`);
    const homeSubheadline = typeof copy.homeServiceSubheadline === "function" ? copy.homeServiceSubheadline(description) : String(copy.homeServiceSubheadline || description || "Trusted local service with clear communication, proof, and an easy quote path.");
    return [
      {
        page_key: "home",
        title: copy.services,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "home_service_hero",
            type: "HomeServiceHero",
            order: 1,
            editable: {
              headline: homeHeadline,
              subtitle: homeSubheadline,
              primary_button: copy.freeQuote,
              secondary_button: copy.callNow,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "phone_first_quote" }
          },
          {
            id: "home_service_categories",
            type: "HomeServiceCategories",
            order: 2,
            editable: { title: copy.homeServiceCategoriesTitle, text: copy.homeServiceCategoriesText, images: [] },
            settings: { layout: "service_tiles", columns: 3 }
          },
          {
            id: "home_service_areas",
            type: "HomeServiceAreas",
            order: 3,
            editable: { title: copy.homeServiceAreasTitle, text: copy.homeServiceAreasText, items: copy.serviceAreaItems, images: [] },
            settings: { layout: "area_map_panel" }
          },
          {
            id: "home_service_gallery",
            type: "PortfolioGallery",
            order: 4,
            editable: { title: copy.beforeAfterTitle, text: copy.beforeAfterText, images: [] },
            settings: { layout: "before_after" }
          },
          {
            id: "home_service_trust",
            type: "HomeServiceTrust",
            order: 5,
            editable: { title: copy.homeServiceTrustTitle, text: copy.homeServiceTrustText, items: copy.homeServiceTrustItems, images: [] },
            settings: { layout: "review_panel" }
          },
          {
            id: "home_service_quote",
            type: "HomeServiceQuote",
            order: 6,
            editable: { title: copy.homeServiceQuoteTitle, text: copy.homeServiceQuoteText, images: [] },
            settings: { layout: "quote_panel" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.serviceAreas,
        slug: copy.servicesSlug,
        order: 2,
        sections: [
          { id: "services_grid", type: "ProductGrid", order: 1, editable: { title: copy.homeServiceCategoriesTitle, text: copy.homeServiceCategoriesText, images: [] }, settings: { layout: "home_services_quote", columns: 3 } },
          { id: "areas", type: "HomeServiceAreas", order: 2, editable: { title: copy.homeServiceAreasTitle, text: copy.homeServiceAreasText, items: copy.serviceAreaItems, images: [] }, settings: { layout: "area_map_panel" } }
        ]
      },
      {
        page_key: "about",
        title: copy.workProof,
        slug: copy.workSlug,
        order: 3,
        sections: [
          { id: "work", type: "PortfolioGallery", order: 1, editable: { title: copy.beforeAfterTitle, text: copy.beforeAfterText, images: [] }, settings: { layout: "before_after" } },
          { id: "trust", type: "HomeServiceTrust", order: 2, editable: { title: copy.homeServiceTrustTitle, text: copy.homeServiceTrustText, items: copy.homeServiceTrustItems, images: [] }, settings: { layout: "review_panel" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.freeQuote,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "quote", type: "HomeServiceQuote", order: 1, editable: { title: copy.homeServiceQuoteTitle, text: copy.homeServiceQuoteText, images: [] }, settings: { layout: "quote_panel" } }]
      }
    ];
  }
  function buildBookingAppointmentInstantPages(copy, name, description, payload = {}) {
    copy = normalizeBookingInstantCopy(copy, payload.selectedLanguage || builderState.selectedLanguage || "en");
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.bookNow,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "booking_hero",
            type: "BookingHero",
            order: 1,
            editable: {
              headline: copy.bookingHeadline(name),
              subtitle: copy.bookingSubheadline(description),
              primary_button: copy.bookNow,
              secondary_button: copy.viewServices,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "appointment_stage" }
          },
          {
            id: "booking_services",
            type: "BookingServices",
            order: 2,
            editable: { title: copy.bookingServicesTitle, text: copy.bookingServicesText, images: [] },
            settings: { layout: "service_menu", columns: 3 }
          },
          {
            id: "booking_availability",
            type: "BookingAvailability",
            order: 3,
            editable: { title: copy.availabilityTitle, text: copy.availabilityText, items: copy.availabilityItems, images: [] },
            settings: { layout: "schedule_cards" }
          },
          {
            id: "booking_team",
            type: "BookingTeam",
            order: 4,
            editable: { title: copy.bookingTeamTitle, text: copy.bookingTeamText, items: copy.bookingTeamItems, images: [] },
            settings: { layout: "staff_process" }
          },
          {
            id: "booking_contact",
            type: "BookingContact",
            order: 5,
            editable: { title: copy.bookingContactTitle, text: copy.bookingContactText, images: [] },
            settings: { layout: "booking_panel" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.availability,
        slug: copy.servicesSlug,
        order: 2,
        sections: [
          { id: "services_grid", type: "ProductGrid", order: 1, editable: { title: copy.bookingServicesTitle, text: copy.bookingServicesText, images: [] }, settings: { layout: "booking_menu", columns: 3 } },
          { id: "availability", type: "BookingAvailability", order: 2, editable: { title: copy.availabilityTitle, text: copy.availabilityText, items: copy.availabilityItems, images: [] }, settings: { layout: "schedule_cards" } }
        ]
      },
      {
        page_key: "about",
        title: copy.team,
        slug: copy.teamSlug,
        order: 3,
        sections: [
          { id: "team", type: "BookingTeam", order: 1, editable: { title: copy.bookingTeamTitle, text: copy.bookingTeamText, items: copy.bookingTeamItems, images: [] }, settings: { layout: "staff_process" } }
        ]
      },
      {
        page_key: "contact",
        title: copy.bookNow,
        slug: copy.contactSlug,
        order: 4,
        sections: [{ id: "booking_contact", type: "BookingContact", order: 1, editable: { title: copy.bookingContactTitle, text: copy.bookingContactText, images: [] }, settings: { layout: "booking_panel" } }]
      }
    ];
  }
  function normalizeBookingInstantCopy(input = {}, language = builderState.selectedLanguage) {
    const source = input && typeof input === "object" ? input : {};
    const locale = catalogLocaleLabels({ business: { selectedLanguage: language || "en" } });
    const functionValue = (key, fallback) => typeof source[key] === "function" ? source[key] : typeof locale[key] === "function" ? locale[key] : fallback;
    const textValue = (key, fallback = "") => typeof source[key] === "string" && source[key].trim() ? source[key] : typeof locale[key] === "string" && locale[key].trim() ? locale[key] : fallback;
    const listValue = (key, fallback = []) => {
      const values = arrayValue2(source[key]).filter(Boolean);
      if (values.length) return values;
      const localized = arrayValue2(locale[key]).filter(Boolean);
      return localized.length ? localized : fallback;
    };
    return {
      ...source,
      fromQuote: textValue("fromQuote", source.askPrice || "Price on request"),
      bookingHeadline: functionValue("bookingHeadline", (businessName) => `Book ${businessName} with a clear next step`),
      bookingSubheadline: functionValue("bookingSubheadline", (value) => value || "Choose a service, preferred time, and contact method in one simple booking flow."),
      bookingServicesTitle: textValue("bookingServicesTitle", "Choose the right appointment"),
      bookingServicesText: textValue("bookingServicesText", "Show duration, service details, and what the client should expect before booking."),
      availabilityTitle: textValue("availabilityTitle", "Availability that feels simple"),
      availabilityText: textValue("availabilityText", "Make open windows, preparation notes, and confirmation expectations clear."),
      availabilityItems: listValue("availabilityItems", ["Today / tomorrow windows", "Morning appointments", "Afternoon appointments"]),
      bookingTeamTitle: textValue("bookingTeamTitle", "A smoother visit from start to finish"),
      bookingTeamText: textValue("bookingTeamText", "Explain the staff, process, and preparation so clients know what happens next."),
      bookingTeamItems: listValue("bookingTeamItems", ["Pick a service", "Choose a preferred time", "Receive confirmation"]),
      bookingContactTitle: textValue("bookingContactTitle", "Reserve the next available time"),
      bookingContactText: textValue("bookingContactText", "Send the preferred service, day, and contact method to request confirmation.")
    };
  }
  function buildLeadFunnelInstantPages(copy, name, description, payload = {}) {
    const heroImage = payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "";
    return [
      {
        page_key: "home",
        title: copy.offer,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "funnel_hero",
            type: "FunnelHero",
            order: 1,
            editable: {
              headline: copy.funnelHeadline(name),
              subtitle: copy.funnelSubheadline(description),
              primary_button: copy.claimOffer,
              secondary_button: copy.seeProof,
              image_url: heroImage,
              images: []
            },
            settings: { layout: "conversion_funnel" }
          },
          {
            id: "funnel_benefits",
            type: "FunnelBenefits",
            order: 2,
            editable: {
              title: copy.funnelBenefitsTitle,
              text: copy.funnelBenefitsText,
              items: copy.funnelBenefitsItems,
              images: []
            },
            settings: { layout: "benefit_stack", columns: 3 }
          },
          {
            id: "funnel_offer",
            type: "FunnelOffer",
            order: 3,
            editable: {
              title: copy.funnelOfferTitle,
              text: copy.funnelOfferText,
              guarantee: copy.funnelGuarantee,
              images: []
            },
            settings: { layout: "offer_stack" }
          },
          {
            id: "funnel_proof",
            type: "FunnelProof",
            order: 4,
            editable: {
              title: copy.funnelProofTitle,
              text: copy.funnelProofText,
              items: copy.funnelProofItems,
              images: []
            },
            settings: { layout: "proof_wall" }
          },
          {
            id: "funnel_faq",
            type: "FunnelFAQ",
            order: 5,
            editable: {
              title: copy.funnelFaqTitle,
              items: copy.funnelFaqItems,
              images: []
            },
            settings: { layout: "objection_handling" }
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.benefits,
        slug: copy.offerSlug,
        order: 2,
        sections: [
          {
            id: "offer_stack",
            type: "ProductGrid",
            order: 1,
            editable: { title: copy.funnelOfferTitle, text: copy.funnelOfferText, images: [] },
            settings: { layout: "lead_offer_stack", columns: 3 }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.proof,
        slug: copy.proofSlug,
        order: 3,
        sections: [
          {
            id: "proof",
            type: "FunnelProof",
            order: 1,
            editable: { title: copy.funnelProofTitle, text: copy.funnelProofText, items: copy.funnelProofItems, images: [] },
            settings: { layout: "proof_wall" }
          },
          {
            id: "faq",
            type: "FunnelFAQ",
            order: 2,
            editable: { title: copy.funnelFaqTitle, items: copy.funnelFaqItems, images: [] },
            settings: { layout: "objection_handling" }
          }
        ]
      },
      {
        page_key: "contact",
        title: copy.contact,
        slug: copy.contactSlug,
        order: 4,
        sections: [
          {
            id: "contact",
            type: "Contact",
            order: 1,
            editable: { title: copy.letsTalk, text: copy.funnelContactText, images: [] },
            settings: { layout: "lead_capture" }
          }
        ]
      }
    ];
  }
  function retailCopyForInstant(copy, language = builderState.selectedLanguage) {
    const localized = {
      en: {
        headline: (name) => `${name} store`,
        dealText: "A high-density retail draft with categories, offers, ratings and checkout-ready product cards.",
        trustText: "Clear delivery, secure checkout, inventory validation, support and simple returns.",
        catalogText: "Products ready to organize, edit, price, activate and publish."
      },
      es: {
        headline: (name) => `${name} tienda online`,
        dealText: "Borrador retail de alta densidad con categorias, ofertas, calificaciones y productos listos para checkout.",
        trustText: "Entrega clara, checkout seguro, validacion de inventario, soporte y devoluciones simples.",
        catalogText: "Productos listos para organizar, editar, colocar precio, activar y publicar."
      },
      fr: {
        headline: (name) => `Boutique ${name}`,
        dealText: "Brouillon retail dense avec categories, offres, notes et fiches produits pretes pour le checkout.",
        trustText: "Livraison claire, paiement securise, validation d'inventaire, support et retours simples.",
        catalogText: "Produits prets a organiser, modifier, tarifer, activer et publier."
      },
      pt: {
        headline: (name) => `${name} loja online`,
        dealText: "Rascunho retail de alta densidade com categorias, ofertas, avaliacoes e produtos prontos para checkout.",
        trustText: "Entrega clara, checkout seguro, validacao de inventario, suporte e devolucoes simples.",
        catalogText: "Produtos prontos para organizar, editar, precificar, ativar e publicar."
      }
    };
    const text = localized[language] || localized.en;
    return {
      ...copy,
      marketplaceHeadline: text.headline,
      dealText: text.dealText,
      dealsText: text.dealText,
      trustText: text.trustText,
      catalogText: text.catalogText
    };
  }
  function buildRetailInstantPages(copy, name, description, payload = {}) {
    const language = payload.selectedLanguage || builderState.selectedLanguage || "en";
    return buildMarketplaceInstantPages(retailCopyForInstant(copy, language), name, description, payload).map((page) => ({
      ...page,
      sections: arrayValue2(page.sections).map((section) => ({
        ...section,
        id: String(section.id || "").replace(/marketplace/g, "retail"),
        settings: {
          ...section.settings || {},
          layout: String(section.settings?.layout || "").replace(/marketplace/g, "retail"),
          ownership_model: "single_owner"
        }
      }))
    }));
  }
  function buildMarketplaceInstantPages(copy, name, description, payload = {}) {
    const productGridSettings = { layout: "marketplace_grid", columns: 4, spacing: "balanced", container_width: "wide", card_density: "compact", card_gap: "tight" };
    const composition = runtimeCompositionIndex(name, payload);
    const recipes = [
      { hero: "split_showcase", homeOrder: ["marketplace_hero", "category_rail", "deal_row", "marketplace_catalog", "trust_strip"], feature: "image_right" },
      { hero: "centered_bold", homeOrder: ["marketplace_hero", "deal_row", "marketplace_catalog", "category_rail", "trust_strip", "marketplace_testimonials"], feature: "card_grid" },
      { hero: "asymmetric_grid", homeOrder: ["marketplace_hero", "category_rail", "marketplace_catalog", "marketplace_gallery", "deal_row", "trust_strip"], feature: "image_left" }
    ];
    const recipe = recipes[composition];
    const pages = [
      {
        page_key: "home",
        title: copy.home,
        slug: "/",
        order: 1,
        sections: [
          {
            id: "marketplace_hero",
            type: "MarketplaceHero",
            order: 1,
            editable: {
              headline: copy.marketplaceHeadline(name),
              subtitle: description,
              primary_button: copy.shopDeals,
              secondary_button: copy.viewCategories,
              search_placeholder: copy.searchPlaceholder,
              deal_badge: copy.todayDeal,
              deal_title: copy.dealTitle,
              deal_text: copy.dealText,
              image_url: payload.assets?.find((asset) => asset.asset_type === "photo")?.url || "",
              images: []
            },
            variant: recipe.hero,
            settings: { layout: recipe.hero, spacing: "compact", container_width: "wide" }
          },
          {
            id: "category_rail",
            type: "CategoryRail",
            order: 2,
            editable: { title: copy.shopByCategory, text: copy.categoryRailText },
            settings: { layout: "category_tiles", spacing: "compact", container_width: "wide" }
          },
          {
            id: "deal_row",
            type: "DealRow",
            order: 3,
            editable: { title: copy.todayDeals, text: copy.dealsText },
            settings: { layout: "deal_row", spacing: "compact", container_width: "wide" }
          },
          {
            id: "marketplace_catalog",
            type: "ProductGrid",
            order: 4,
            editable: { title: copy.bestSellers, text: copy.catalogText, images: [] },
            settings: productGridSettings
          },
          {
            id: "trust_strip",
            type: "TrustStrip",
            order: 5,
            editable: { title: copy.whyBuyHere, text: copy.trustText },
            settings: { layout: "marketplace_trust", spacing: "compact", container_width: "wide" }
          },
          ...composition === 1 ? [{
            id: "marketplace_testimonials",
            type: "Testimonials",
            editable: { title: copy.whyBuyHere, text: copy.trustText, images: [] },
            variant: recipe.feature,
            settings: { layout: "feature", spacing: "balanced", container_width: "wide" }
          }] : [],
          ...composition === 2 ? [{
            id: "marketplace_gallery",
            type: "Gallery",
            editable: { title: copy.featuredProducts, text: copy.catalogText, images: [] },
            variant: recipe.feature,
            settings: { layout: "gallery", columns: 3, spacing: "balanced", container_width: "wide" }
          }] : []
        ]
      },
      {
        page_key: "deals",
        title: copy.deals,
        slug: "/deals",
        order: 2,
        sections: [
          {
            id: "deals_hero",
            type: "MarketplaceHero",
            order: 1,
            editable: {
              headline: copy.todayDeals,
              subtitle: copy.dealsText,
              primary_button: copy.shopDeals,
              secondary_button: copy.viewCategories,
              search_placeholder: copy.searchPlaceholder,
              deal_badge: copy.todayDeal,
              deal_title: copy.dealTitle,
              deal_text: copy.dealText,
              images: []
            },
            settings: { layout: "marketplace_search", spacing: "compact", container_width: "wide" }
          },
          {
            id: "deals_row",
            type: "DealRow",
            order: 2,
            editable: { title: copy.todayDeals, text: copy.dealsText },
            settings: { layout: "deal_row", spacing: "compact", container_width: "wide" }
          },
          {
            id: "deals_grid",
            type: "ProductGrid",
            order: 3,
            editable: { title: copy.bestSellers, text: copy.catalogText, images: [] },
            settings: productGridSettings
          }
        ]
      },
      {
        page_key: "categories",
        title: copy.categories,
        slug: "/categories",
        order: 3,
        sections: [
          {
            id: "categories_rail",
            type: "CategoryRail",
            order: 1,
            editable: { title: copy.shopByCategory, text: copy.categoryRailText },
            settings: { layout: "category_tiles", spacing: "compact", container_width: "wide" }
          },
          {
            id: "categories_grid",
            type: "ProductGrid",
            order: 2,
            editable: { title: copy.catalog, text: copy.catalogText, images: [] },
            settings: productGridSettings
          }
        ]
      },
      {
        page_key: "catalog",
        title: copy.shop,
        slug: copy.shopSlug,
        order: 4,
        sections: [
          {
            id: "catalog_search",
            type: "MarketplaceHero",
            order: 1,
            editable: {
              headline: copy.catalog,
              subtitle: copy.catalogText,
              primary_button: copy.shopDeals,
              secondary_button: copy.viewCategories,
              search_placeholder: copy.searchPlaceholder,
              deal_badge: copy.bestSellers,
              deal_title: copy.todayDeals,
              deal_text: copy.dealsText,
              images: []
            },
            settings: { layout: "marketplace_search", spacing: "compact", container_width: "wide" }
          },
          {
            id: "catalog_grid",
            type: "ProductGrid",
            order: 2,
            editable: { title: copy.catalog, text: copy.catalogText, images: [] },
            settings: { ...productGridSettings, spacing: "compact" }
          }
        ]
      },
      {
        page_key: "about",
        title: copy.about,
        slug: copy.aboutSlug,
        order: 5,
        sections: [{ id: "about", type: "About", order: 1, editable: { title: copy.aboutBrand, text: description }, settings: { layout: "feature", container_width: "wide" } }]
      },
      {
        page_key: "support",
        title: copy.support,
        slug: "/support",
        order: 6,
        sections: [
          {
            id: "support_trust",
            type: "TrustStrip",
            order: 1,
            editable: { title: copy.whyBuyHere, text: copy.trustText },
            settings: { layout: "marketplace_trust", spacing: "compact", container_width: "wide" }
          },
          {
            id: "contact",
            type: "Contact",
            order: 2,
            editable: { title: copy.letsTalk, text: copy.contactText },
            settings: { layout: "simple", container_width: "wide" }
          }
        ]
      }
    ];
    const home = pages.find((page) => page.page_key === "home");
    const byId = new Map(home.sections.map((section) => [section.id, section]));
    home.sections = resequenceSections(recipe.homeOrder.map((id) => byId.get(id)).filter(Boolean));
    for (const page of pages) {
      for (const section of page.sections || []) {
        if (section.type === "MarketplaceHero" && !section.variant) section.variant = recipe.hero;
        if (["About", "Testimonials"].includes(section.type) && !section.variant) section.variant = recipe.feature;
      }
    }
    return pages;
  }
  function marketplaceCategoryForIndex(index, copy, contextText = "", language = builderState.selectedLanguage) {
    const normalized = normalizeTemplateIntentText(contextText);
    const localized = {
      en: {
        jewelry: ["Necklaces", "Bracelets", "Earrings", "Custom pieces", "Gift sets", "New arrivals"],
        fashion: ["New arrivals", "Accessories", "Outfits", "Statement pieces", "Limited drop", "Best sellers"],
        variety: ["Featured", "Auto accessories", "Collectibles", "Home finds", "Gifts", "Deals"]
      },
      es: {
        jewelry: ["Collares", "Pulseras", "Aretes", "Piezas personalizadas", "Sets de regalo", "Novedades"],
        fashion: ["Novedades", "Accesorios", "Looks", "Piezas destacadas", "Drop limitado", "Mas vendidos"],
        variety: ["Destacados", "Accesorios auto", "Coleccionables", "Hogar", "Regalos", "Ofertas"]
      },
      fr: {
        jewelry: ["Colliers", "Bracelets", "Boucles", "Pieces personnalisees", "Sets cadeaux", "Nouveautes"],
        fashion: ["Nouveautes", "Accessoires", "Looks", "Pieces fortes", "Drop limite", "Meilleures ventes"],
        variety: ["Selections", "Accessoires auto", "Objets de collection", "Maison", "Cadeaux", "Offres"]
      },
      pt: {
        jewelry: ["Colares", "Pulseiras", "Brincos", "Pecas personalizadas", "Kits presente", "Novidades"],
        fashion: ["Novidades", "Acessorios", "Looks", "Pecas destaque", "Drop limitado", "Mais vendidos"],
        variety: ["Destaques", "Acessorios auto", "Colecionaveis", "Casa", "Presentes", "Ofertas"]
      }
    };
    const set = localized[language] || localized.en;
    let categories = copy.marketplaceCategories || ["Featured", "Deals", "New", "Popular"];
    if (textSuggestsJewelryAccessoryStore(normalized)) {
      categories = set.jewelry;
    } else if (/ropa|moda|fashion|camisa|zapato|sneaker|clothing|apparel|streetwear|lookbook/.test(normalized)) {
      categories = set.fashion;
    } else if (/carro|auto|automotriz|anime|gadget|juguete|regalo|raro|curioso|hogar|home|gift|collectible|variedad|varied|variado/.test(normalized)) {
      categories = set.variety;
    }
    return categories[index % categories.length];
  }
  function fashionCategoryForIndex(index, copy, contextText = "") {
    if (textSuggestsJewelryAccessoryStore(contextText)) {
      const jewelryCategories = copy.jewelryCategories || ["Necklaces", "Bracelets", "Earrings", "Rings", "Sets", "Custom pieces"];
      return jewelryCategories[index % jewelryCategories.length];
    }
    const categories = copy.fashionCollections || ["New arrivals", "Essentials", "Statement pieces", "Accessories", "Limited drop", "Best sellers"];
    return categories[index % categories.length];
  }
  function restaurantCategoryForIndex(index, copy) {
    const categories = copy.restaurantCategories || ["Starters", "Mains", "Drinks", "Desserts"];
    return categories[index % categories.length];
  }
  function digitalCategoryForIndex(index, copy) {
    const categories = copy.digitalCategories || ["Course", "Template", "Toolkit", "Membership"];
    return categories[index % categories.length];
  }
  function listingCategoryForIndex(index, copy) {
    const categories = copy.listingCategories || ["Homes", "Rentals", "Commercial", "Featured"];
    return categories[index % categories.length];
  }
  function luxuryCategoryForIndex(index, copy) {
    const categories = copy.luxuryCategories || ["Watches", "Jewelry", "Art", "Collectibles", "Private collection", "Limited pieces"];
    return categories[index % categories.length];
  }
  function educationCategoryForIndex(index, copy) {
    const categories = copy.educationCategories || ["Foundation", "Advanced", "Workshop", "Coaching", "Certification", "Membership"];
    return categories[index % categories.length];
  }
  function educationDurationForIndex(index, copy) {
    const durations = copy.educationDurations || ["4 weeks", "6 modules", "Live cohort", "Self-paced"];
    return durations[index % durations.length];
  }
  function clinicCategoryForIndex(index, copy) {
    const categories = copy.clinicCategories || ["Aesthetic care", "Wellness", "Dental", "Therapy", "Nutrition", "Consultation"];
    return categories[index % categories.length];
  }
  function clinicDurationForIndex(index, copy) {
    const durations = copy.clinicDurations || ["30 min consult", "45 min session", "Personal plan", "Follow-up ready"];
    return durations[index % durations.length];
  }
  function professionalCategoryForIndex(index, copy) {
    const categories = copy.professionalCategories || ["Legal", "Tax", "Accounting", "Insurance", "Consulting", "Compliance"];
    return categories[index % categories.length];
  }
  function professionalEngagementForIndex(index, copy) {
    const engagements = copy.professionalEngagements || ["Initial review", "Document review", "Strategy call", "Ongoing advisory"];
    return engagements[index % engagements.length];
  }
  function enterpriseCategoryForIndex(index, copy) {
    const categories = copy.enterpriseCategories || ["Automation", "Analytics", "CRM", "Operations", "Integrations", "Security"];
    return categories[index % categories.length];
  }
  function enterpriseTimelineForIndex(index, copy) {
    const timelines = copy.enterpriseTimelines || ["Demo first", "2 week setup", "API-ready", "Managed rollout"];
    return timelines[index % timelines.length];
  }
  function industrialCategoryForIndex(index, copy) {
    const categories = copy.industrialCategories || ["Machinery", "Parts", "Tools", "Safety", "Materials", "Maintenance"];
    return categories[index % categories.length];
  }
  function industrialLeadTimeForIndex(index, copy) {
    const timelines = copy.industrialLeadTimes || ["Quote first", "Bulk order", "2-4 week lead", "Custom spec"];
    return timelines[index % timelines.length];
  }
  function listingLocationForIndex(index, copy) {
    const locations = copy.listingLocations || ["Central area", "North side", "West district", "Near downtown"];
    return locations[index % locations.length];
  }
  function marketplaceCategories(schema) {
    const items = marketplaceItems(schema);
    const fromItems = [...new Set(items.map((item) => item.category).filter(Boolean))];
    if (fromItems.length > 0) return fromItems;
    const copy = instantLocaleCopy(schema?.business?.selectedLanguage || builderState.selectedLanguage || "en");
    return [.../* @__PURE__ */ new Set([...fromItems, ...copy.marketplaceCategories || []])];
  }
  function instantLocaleCopy(language) {
    const copies = {
      en: {
        newStore: "New store",
        defaultDescription: "A brand ready to sell online.",
        defaultProducts: ["Featured product", "Main service", "Special offer"],
        itemDescription: (name) => `A featured option from ${name}, ready to present with details, benefits, and a clear call to action.`,
        askPrice: "Ask for price",
        priceNotSet: "Price editable",
        request: "Request",
        viewProduct: "View product",
        onlineStore: "Online store",
        defaultTone: "Professional and friendly",
        shopNow: "Shop now",
        viewCatalog: "View catalog",
        requestOrder: "Request order",
        menu: "Menu",
        specials: "Specials",
        hoursLocation: "Hours & location",
        orderNow: "Order now",
        viewMenu: "View menu",
        menuSlug: "/menu",
        hoursSlug: "/hours-location",
        menuPrice: "Menu price",
        restaurantHeadline: (name) => `${name} menu, made easy to order`,
        restaurantSubheadline: (description) => description || "A warm restaurant site with signature dishes, menu categories, specials, hours, location and a simple ordering path.",
        restaurantCategoriesTitle: "Explore the menu by category",
        restaurantCategoriesText: "Give customers fast paths to starters, mains, drinks, desserts, specials and favorites.",
        restaurantCategories: ["Starters", "Mains", "Drinks", "Desserts", "Specials", "Chef picks"],
        restaurantSignatureTitle: "Signature dishes ready to order",
        restaurantSignatureText: "Highlight the dishes, combos and customer favorites that should sell first.",
        restaurantSpecialsTitle: "Specials and combos",
        restaurantSpecialsText: "Use this section for lunch specials, family combos, happy hour, delivery offers or chef recommendations.",
        restaurantSpecialItems: ["Chef recommendation", "Family combo", "Pickup special", "Delivery-ready"],
        restaurantInfoTitle: "Hours, location and service options",
        restaurantInfoText: "Show open hours, pickup, delivery, dine-in, service area and how customers should place an order.",
        restaurantInfoItems: ["Open hours", "Pickup", "Delivery", "Dine-in", "Catering", "WhatsApp orders"],
        restaurantOrderTitle: "Place an order or ask a question",
        restaurantOrderText: "Send the preferred dishes, pickup or delivery option, and contact method. The business can confirm by phone, WhatsApp or email.",
        clinicCare: "Personalized care",
        treatments: "Treatments",
        results: "Results",
        bookConsultation: "Book consultation",
        viewTreatments: "View treatments",
        consultationBased: "Consultation-based",
        popularTreatment: "Popular treatment",
        specialistLed: "Specialist-led",
        personalizedPlan: "Personalized plan",
        clinicHeadline: (name) => `${name} care designed around your next step`,
        clinicSubheadline: (description) => description || "A calm, professional clinic website built around trust, treatments, results, specialists, and easy booking.",
        treatmentsTitle: "Treatments and services",
        treatmentsText: "Present the services customers can request, compare, and book with clear next steps.",
        carePathTitle: "A clear path from first question to care",
        carePathText: "Show how the clinic evaluates needs, recommends the right treatment, and follows up with confidence.",
        carePathItems: ["Initial consultation", "Personalized plan", "Treatment session", "Progress check", "Aftercare guidance", "Next visit"],
        clinicTrustTitle: "Trust signals patients look for",
        clinicTrustText: "Use credentials, safety notes, specialist experience, transparent expectations and response times.",
        clinicTrustItems: ["Specialist guidance", "Safety-first process", "Clear expectations", "Private consultation", "Aftercare notes", "Fast response"],
        resultsTitle: "Results with realistic expectations",
        resultsText: "Use this area for before/after proof, transformation notes, case examples or expected outcomes.",
        resultItems: ["Visible progress", "Before/after proof", "Personal plan", "Follow-up support", "Clear preparation", "Aftercare included"],
        clinicTeamTitle: "Specialists who guide the process",
        clinicTeamText: "Introduce the provider, clinical standards, certifications or care philosophy behind the services.",
        clinicTeamItems: ["Credentialed team", "Consultation-first", "Patient education", "Care follow-up"],
        clinicBookingTitle: "Book a consultation or ask a question",
        clinicBookingText: "Send the treatment, concern, preferred schedule and contact method. The clinic can confirm next steps.",
        clinicCategories: ["Aesthetic care", "Wellness", "Dental", "Therapy", "Nutrition", "Consultation"],
        clinicDurations: ["30 min consult", "45 min session", "Personal plan", "Follow-up ready"],
        professionalFirm: "Professional firm",
        scheduleConsultation: "Schedule consultation",
        confidential: "Confidential",
        seniorAdvisor: "Senior advisor",
        caseReview: "Case review",
        businessReady: "Business-ready",
        professionalHeadline: (name) => `${name} guidance for decisions that matter`,
        professionalSubheadline: (description) => description || "A premium professional services website built around trust, clear practice areas, process, proof, and consultation requests.",
        practiceAreasTitle: "Practice areas and services",
        practiceAreasText: "Show the services clients can review, compare and request with a clear consultation path.",
        professionalProcessTitle: "A clear advisory process",
        professionalProcessText: "Help clients understand how the firm reviews the situation, recommends next steps and follows through.",
        professionalProcessItems: ["Initial review", "Document check", "Strategy call", "Action plan", "Follow-up", "Ongoing advisory"],
        professionalProofTitle: "Proof clients need before they contact you",
        professionalProofText: "Use credentials, confidentiality, response expectations and professional standards.",
        professionalProofItems: ["Confidential process", "Senior review", "Clear next steps", "Business-ready advice", "Document support", "Responsive contact"],
        professionalTeamTitle: "Advisors who handle the details",
        professionalTeamText: "Introduce the attorney, accountant, consultant or advisor standards behind the service.",
        professionalTeamItems: ["Experienced advisors", "Private consultation", "Clear documentation", "Follow-through"],
        professionalFaqTitle: "Questions before the consultation",
        professionalFaqItems: ["What should I prepare?", "How does the first consultation work?", "Can you review documents?", "Can this become ongoing advisory?"],
        professionalConsultationTitle: "Request a consultation or document review",
        professionalConsultationText: "Send the service needed, urgency, preferred schedule and contact method. The firm can confirm the next step.",
        professionalCategories: ["Legal", "Tax", "Accounting", "Insurance", "Consulting", "Compliance"],
        professionalEngagements: ["Initial review", "Document review", "Strategy call", "Ongoing advisory"],
        enterprisePlatform: "Enterprise platform",
        requestDemo: "Request demo",
        viewSolutions: "View solutions",
        solutions: "Solutions",
        useCases: "Use cases",
        integrations: "Integrations",
        customPlan: "Custom plan",
        enterpriseReady: "Enterprise-ready",
        integrationReady: "Integration-ready",
        roiFocused: "ROI-focused",
        enterpriseHeadline: (name) => `${name} systems that help teams move faster`,
        enterpriseSubheadline: (description) => description || "A premium B2B website for software, automation, integrations, dashboards and enterprise services.",
        enterpriseSolutionsTitle: "Solutions built for operational teams",
        enterpriseSolutionsText: "Present products, services or packages as clear business solutions with demo-first next steps.",
        enterpriseUseCasesTitle: "Use cases by workflow",
        enterpriseUseCasesText: "Show how the solution fits sales, operations, support, finance, leadership or field teams.",
        enterpriseUseCaseItems: ["Automate manual work", "Centralize reporting", "Connect business tools", "Improve team visibility", "Reduce operational delays", "Scale service delivery"],
        enterpriseIntegrationsTitle: "Integrations and rollout",
        enterpriseIntegrationsText: "Make technical buyers comfortable with APIs, CRM, ERP, payments, analytics and support workflows.",
        enterpriseIntegrationItems: ["CRM", "ERP", "Payments", "Analytics", "Support desk", "Custom API"],
        enterpriseProofTitle: "Business proof before the demo",
        enterpriseProofText: "Use security, implementation, ROI, response time and support signals.",
        enterpriseProofItems: ["Secure workflows", "Implementation support", "Clear ROI path", "Admin controls", "API-ready", "Team onboarding"],
        enterprisePricingTitle: "Packages that can start simple and scale",
        enterprisePricingText: "Use this area for starter, growth and enterprise packages without exposing internal costs.",
        enterpriseDemoTitle: "Book a demo or request a solution review",
        enterpriseDemoText: "Send team size, current tools, workflow problem and preferred contact method. The team can qualify the lead.",
        enterpriseCategories: ["Automation", "Analytics", "CRM", "Operations", "Integrations", "Security"],
        enterpriseTimelines: ["Demo first", "2 week setup", "API-ready", "Managed rollout"],
        industrialSupplier: "Industrial supplier",
        requestQuote: "Request quote",
        viewSpecs: "View specs",
        capabilities: "Capabilities",
        certifications: "Certifications",
        supplyChain: "Supply chain",
        quoteRequired: "Quote required",
        certified: "Certified",
        bulkReady: "Bulk-ready",
        specReady: "Spec-ready",
        moqReady: "MOQ-ready",
        industrialHeadline: (name) => `${name} supply built for serious operations`,
        industrialSubheadline: (description) => description || "A technical industrial supplier website with RFQ flow, specs, MOQ, lead times, certifications and supply-chain confidence.",
        industrialCatalogTitle: "Technical products and industrial supply",
        industrialCatalogText: "Present parts, equipment and materials with specs buyers can verify before requesting a quote.",
        industrialCapabilitiesTitle: "Capabilities buyers need to verify",
        industrialCapabilitiesText: "Show custom specs, bulk ordering, sourcing, quality control and support for repeat procurement.",
        industrialCapabilityItems: ["Custom specs", "Bulk ordering", "Quality control", "Fast sourcing", "Replacement parts", "B2B support"],
        industrialCertificationsTitle: "Certifications, quality and documentation",
        industrialCertificationsText: "Give procurement teams confidence with documentation, safety data, warranty and compliance notes.",
        industrialCertificationItems: ["ISO-ready docs", "Material specs", "Warranty notes", "Safety data", "Compliance support", "Batch tracking"],
        industrialSupplyTitle: "Supply chain and fulfillment",
        industrialSupplyText: "Make lead times, MOQ planning, freight support and repeat orders easy to understand.",
        industrialSupplyItems: ["Lead times", "MOQ planning", "Freight support", "Warehouse-ready", "Vendor sourcing", "Repeat orders"],
        industrialQuoteTitle: "Request a quote with specs",
        industrialQuoteText: "Send product, quantities, material, deadline and delivery needs. The supplier can respond with availability and pricing.",
        industrialCategories: ["Machinery", "Parts", "Tools", "Safety", "Materials", "Maintenance"],
        industrialLeadTimes: ["Quote first", "Bulk order", "2-4 week lead", "Custom spec"],
        industrialMaterials: ["Steel", "Aluminum", "Composite", "Safety-rated"],
        solutionsSlug: "/solutions",
        useCasesSlug: "/use-cases",
        home: "Home",
        overview: "Overview",
        products: "Products",
        story: "Story",
        shop: "Shop",
        about: "About",
        contact: "Contact",
        viewShop: "View shop",
        contactVerb: "Contact",
        featuredProducts: "Featured products",
        featuredText: "An initial selection to show the business offer.",
        catalog: "Catalog",
        catalogText: "Products and services ready to edit, activate, and publish.",
        aboutBrand: "About the brand",
        letsTalk: "Let us talk",
        contactText: "Contact the business to buy, quote, or request information.",
        footerText: (name) => `${name} - Editable draft website.`,
        modernCommercial: "Modern commercial",
        fastBase: "Fast, clean, editable base to validate the store.",
        shopSlug: "/shop",
        aboutSlug: "/about",
        contactSlug: "/contact",
        deals: "Deals",
        categories: "Categories",
        support: "Support",
        fastDelivery: "Fast delivery",
        freeShipping: "Free shipping",
        todayDeal: "Today deal",
        marketplaceHeadline: (name) => `${name} marketplace`,
        shopDeals: "Shop deals",
        viewCategories: "View categories",
        searchPlaceholder: "Search products, brands, or categories",
        dealTitle: "Top picks ready to sell",
        dealText: "A marketplace-style draft with categories, deals, ratings, and checkout-ready product cards.",
        shopByCategory: "Shop by category",
        categoryRailText: "Quick paths for customers to compare products faster.",
        todayDeals: "Today's deals",
        dealsText: "Limited rows, featured offers, and fast-discovery product cards.",
        bestSellers: "Best sellers",
        whyBuyHere: "Why customers buy here",
        trustText: "Clear delivery, secure checkout, support, and simple returns.",
        marketplaceCategories: ["Electronics", "Home", "Fashion", "Beauty", "Sports", "Deals"],
        listings: "Listings",
        areas: "Areas",
        listingsSlug: "/listings",
        areasSlug: "/areas",
        searchListings: "Search listings",
        viewListings: "View listings",
        inquireNow: "Inquire now",
        featuredListing: "Featured listing",
        newListing: "New listing",
        availableNow: "Available now",
        listingPrice: "Price on request",
        listingHeadline: (name) => `Find the right listing with ${name}`,
        listingSubheadline: () => "Search premium listings by location, price, category and specs with a clear inquiry path.",
        listingSearchPlaceholder: "Search by location, type, price or keyword",
        listingFiltersTitle: "Search with the right filters",
        listingFiltersText: "Help customers narrow options by category, location, price and availability before they inquire.",
        featuredListingsTitle: "Featured listings",
        featuredListingsText: "A polished selection of active listings with price, location, specs and inquiry CTAs.",
        listingAreaTitle: "Explore the best areas",
        listingAreaText: "Use area cards, neighborhood notes or service zones to make discovery feel local and clear.",
        listingTrustTitle: "Confidence before the inquiry",
        listingTrustText: "Show verified details, clear contact paths and updated availability before the customer reaches out.",
        listingContactTitle: "Ask about a listing",
        listingContactText: "Send the preferred listing, budget, location and contact method. The business can respond with availability or next steps.",
        listingCategories: ["Homes", "Rentals", "Commercial", "Land", "Cars", "Featured"],
        listingLocations: ["Downtown", "North area", "West district", "Near schools", "Waterfront", "Business zone"],
        listingTrustItems: ["Verified details", "Updated availability", "Clear pricing", "Local support", "Fast response", "Easy comparison"],
        company: "Company",
        services: "Services",
        process: "Process",
        servicesSlug: "/services",
        processSlug: "/company",
        requestConsultation: "Request consultation",
        viewServices: "View services",
        corporateHeadline: (name) => `${name} builds work clients can trust`,
        corporateSubheadline: (description) => description || "A professional company website focused on clarity, services, proof, and direct inquiries.",
        corporateServicesTitle: "Services and capabilities",
        corporateServicesText: "A clear overview of what the company does, who it helps, and the outcomes clients can expect.",
        corporateProcessTitle: "A simple process from first conversation to delivery",
        corporateProcessText: "Show how the company evaluates needs, recommends the right path, and delivers with accountability.",
        corporateProcessItems: ["Understand the goal", "Plan the right solution", "Deliver with clear communication", "Support the next step"],
        corporateProofTitle: "Built on trust and measurable work",
        corporateProofText: "Use this section for credibility, experience, certifications, client types, or operating standards.",
        corporateProofItems: ["Reliable delivery", "Clear communication", "Professional standards"],
        corporateContactText: "Send a message to discuss services, availability, pricing, or a custom project.",
        offer: "Offer",
        benefits: "Benefits",
        proof: "Proof",
        faq: "FAQ",
        offerSlug: "/offer",
        proofSlug: "/proof",
        claimOffer: "Start now",
        seeProof: "See proof",
        funnelHeadline: (name) => `${name} turns interest into real customers`,
        funnelSubheadline: (description) => description || "A focused landing page built around one clear offer, proof, and a direct next step.",
        funnelBenefitsTitle: "Why this offer works",
        funnelBenefitsText: "Show the most important outcomes clearly so visitors understand the value before they contact you.",
        funnelBenefitsItems: ["Clear promise", "Simple next step", "Built for qualified leads", "Easy to edit", "Proof-first structure", "Fast launch"],
        funnelOfferTitle: "Everything needed to take the next step",
        funnelOfferText: "Present the offer, package, consultation, or service in a focused stack that keeps attention on conversion.",
        funnelGuarantee: "Clear expectations before the customer commits.",
        funnelProofTitle: "Proof that removes doubt",
        funnelProofText: "Use results, testimonials, standards, or before-and-after outcomes to build confidence.",
        funnelProofItems: ["Specific customer outcome", "Simple process", "Fast response", "Clear pricing conversation"],
        funnelFaqTitle: "Questions before starting",
        funnelFaqItems: [
          { question: "What happens after I send a request?", answer: "The business follows up with the next step, quote, booking, or consultation." },
          { question: "Can the offer be customized?", answer: "Yes. The offer, copy, sections, and contact path are editable." },
          { question: "Is this a store?", answer: "No. This version is focused on leads, applications, bookings, or quote requests." }
        ],
        funnelContactText: "Send a request and the business can follow up with the next step, quote, or booking details.",
        premiumHeadline: (name) => `Meet ${name}`,
        premiumSubheadline: (description) => description || "A refined product experience built to feel simple, confident, and memorable.",
        premiumPrimary: "Explore products",
        premiumSecondary: "See the story",
        premiumStoryTitle: "Designed around the details",
        premiumStoryText: "A focused product story with clean copy, quiet confidence, and enough space for every feature to feel intentional.",
        premiumFeatureTitle: "One flagship experience",
        premiumFeatureText: "Use this section for the signature benefit, craft story, performance promise, or strongest reason to buy.",
        premiumGalleryTitle: "Curated essentials",
        premiumGalleryText: "A smaller catalog presented with more care, stronger imagery, and clearer product value.",
        premiumSpecsTitle: "Everything important, easy to compare",
        premiumSpecsText: "Highlight materials, warranty, delivery, support, personalization, or service quality without clutter.",
        premiumSpecItems: ["Refined presentation", "Editable product story", "Premium support", "Ready to publish"],
        collection: "Collection",
        provenance: "Provenance",
        privateInquiry: "Private inquiry",
        viewCollection: "View collection",
        requestPrivateViewing: "Request private viewing",
        priceOnRequest: "Price on request",
        limitedPiece: "Limited piece",
        authenticated: "Authenticated",
        privateService: "Private service",
        signaturePiece: "Signature piece",
        luxuryHeadline: (name) => `${name}: private pieces for selected clients`,
        luxurySubheadline: (description) => description || "A private showroom for high-ticket pieces with provenance, editorial presentation, and appointment-led inquiries.",
        luxurySignatureText: "Lead with one remarkable piece, the story behind it, and why it deserves private attention.",
        luxuryCollectionTitle: "Curated private collection",
        luxuryCollectionText: "Present limited pieces with stronger imagery, provenance cues, and a clear private inquiry path.",
        luxuryProvenanceTitle: "Provenance, authenticity and service",
        luxuryProvenanceText: "Luxury customers need confidence before they inquire. Use this section for sourcing, certification, materials, care and private support.",
        luxuryPrivateTitle: "A private appointment, handled with care",
        luxuryPrivateText: "Invite serious buyers to request availability, preferred piece, budget range, and contact method before a private follow-up.",
        luxuryContactTitle: "Request a private consultation",
        luxuryContactText: "Send the piece, occasion, preferred timing and contact method. A client advisor can respond with availability and next steps.",
        luxuryProofItems: ["Verified provenance", "Limited availability", "Private advisor", "Insured handling", "Secure inquiry", "Concierge follow-up"],
        luxuryCategories: ["Watches", "Jewelry", "Art", "Collectibles", "Private collection", "Limited pieces"],
        academy: "Academy",
        programs: "Programs",
        curriculum: "Curriculum",
        enroll: "Enroll",
        programsSlug: "/programs",
        curriculumSlug: "/curriculum",
        enrollSlug: "/enroll",
        courseAcademy: "Course academy",
        enrollNow: "Enroll now",
        viewCurriculum: "View curriculum",
        coursePrice: "Enrollment price",
        featuredProgram: "Featured program",
        beginnerFriendly: "Beginner friendly",
        certificateReady: "Certificate-ready",
        educationHeadline: (name) => `${name}: learn with a clear path`,
        educationSubheadline: (description) => description || "A premium academy website with courses, modules, outcomes, instructor trust and an enrollment path.",
        learningPathTitle: "A clear learning path from first lesson to result",
        learningPathText: "Show the roadmap, modules and milestones so students understand exactly how progress happens.",
        learningPathItems: ["Start with the foundation", "Practice with guided modules", "Apply the method", "Get feedback or support", "Complete the outcome", "Take the next step"],
        programsTitle: "Programs built around real outcomes",
        programsText: "Present courses, coaching packages, bootcamps or memberships with duration, level and enrollment CTAs.",
        outcomesTitle: "What students should be able to do",
        outcomesText: "Turn the course promise into concrete results, skills and confidence markers.",
        learningOutcomeItems: ["Clear skill progression", "Practical assignments", "Editable curriculum", "Student-ready proof", "Support notes", "Launch-ready offer"],
        instructorTitle: "Expert guidance, structured for action",
        instructorText: "Use this space for instructor credibility, teaching method, student support, certifications or community value.",
        instructorTrustItems: ["Expert-led lessons", "Structured modules", "Support path", "Student outcomes", "Flexible access", "Editable offer"],
        enrollmentTitle: "Ready to start learning?",
        enrollmentText: "Invite students to enroll, request access, ask about cohorts, or choose the right program.",
        educationCategories: ["Foundation", "Advanced", "Workshop", "Coaching", "Certification", "Membership"],
        educationDurations: ["4 weeks", "6 modules", "Live cohort", "Self-paced"],
        modules: "Modules",
        modulesSlug: "/modules",
        viewProducts: "View products",
        digitalPrice: "Price editable",
        digitalHeadline: (name) => `${name} digital products, ready for instant access`,
        digitalSubheadline: (description) => description || "A polished digital product store for courses, templates, software, memberships and downloadable packs.",
        digitalBundleTitle: "Digital offers built to sell",
        digitalBundleText: "Present products as clear bundles with instant access, deliverables, bonuses and support notes.",
        digitalModulesTitle: "What customers get inside",
        digitalModulesText: "Show modules, downloads, lessons, templates, licenses and support in a clean structure.",
        digitalModuleItems: ["Core training", "Downloadable resources", "Templates and tools", "Bonus material", "Access instructions", "Support notes"],
        digitalProofTitle: "Trust before checkout",
        digitalProofText: "Use proof, outcomes, guarantees or creator credibility to make the purchase feel safe.",
        digitalProofItems: ["Instant access", "Editable modules", "Clear license", "Support-ready", "Bundle value", "Simple checkout"],
        digitalAccessTitle: "Get access and start immediately",
        digitalAccessText: "After purchase or request, customers know exactly what they receive, how access works and where to get support.",
        digitalCategories: ["Course", "Template", "Toolkit", "Membership", "Software", "Bundle"],
        newDrop: "New drop",
        collections: "Collections",
        lookbook: "Lookbook",
        fashionHeadline: (name) => `${name} new drop`,
        fashionSubheadline: (description) => description || "A visual clothing store built around collections, styling, and quick shopping.",
        shopTheDrop: "Shop the drop",
        viewLookbook: "View lookbook",
        collectionsText: "Organize products by drop, fit, color, season, or collection.",
        dropStoryTitle: "Built for the way it is worn",
        dropStoryText: "Use this section to explain the mood, materials, fit, and visual story behind the collection.",
        lookbookText: "Show outfits, lifestyle moments, campaign photos, or product combinations that help customers imagine the look.",
        newArrivals: "New arrivals",
        newArrivalsText: "A shopping section for the current drop, best sellers, and pieces ready to buy.",
        fitGuideTitle: "Fit, sizes and styling",
        fitGuideText: "Give customers the confidence to choose the right size, material, fit, and complete look.",
        fitGuideItems: ["Size guide", "Fit notes", "Materials", "Complete the look"],
        jewelryCategories: ["Necklaces", "Bracelets", "Earrings", "Rings", "Gift sets", "Custom pieces"]
      },
      es: {
        newStore: "Nueva tienda",
        defaultDescription: "Una marca preparada para vender en linea.",
        defaultProducts: ["Producto destacado", "Servicio principal", "Oferta especial"],
        itemDescription: (name) => `Una opcion destacada de ${name}, lista para presentar al cliente con detalles, beneficios y llamada a la accion.`,
        askPrice: "Consultar precio",
        priceNotSet: "Precio editable",
        request: "Solicitar",
        viewProduct: "Ver producto",
        onlineStore: "Tienda online",
        defaultTone: "Profesional y cercano",
        shopNow: "Comprar ahora",
        viewCatalog: "Ver catalogo",
        requestOrder: "Solicitar pedido",
        menu: "Menu",
        specials: "Especiales",
        hoursLocation: "Horarios y ubicacion",
        orderNow: "Ordenar ahora",
        viewMenu: "Ver menu",
        menuSlug: "/menu",
        hoursSlug: "/horarios-ubicacion",
        menuPrice: "Precio del menu",
        restaurantHeadline: (name) => `${name}: menu claro y facil de ordenar`,
        restaurantSubheadline: (description) => description || "Pagina calida para restaurante con platos destacados, categorias de menu, especiales, horarios, ubicacion y pedido simple.",
        restaurantCategoriesTitle: "Explora el menu por categoria",
        restaurantCategoriesText: "Crea rutas rapidas hacia entradas, platos fuertes, bebidas, postres, especiales y favoritos.",
        restaurantCategories: ["Entradas", "Platos fuertes", "Bebidas", "Postres", "Especiales", "Favoritos"],
        restaurantSignatureTitle: "Platos destacados listos para pedir",
        restaurantSignatureText: "Muestra los platos, combos y favoritos que deberian venderse primero.",
        restaurantSpecialsTitle: "Especiales y combos",
        restaurantSpecialsText: "Usa esta seccion para almuerzos, combos familiares, happy hour, delivery o recomendaciones del chef.",
        restaurantSpecialItems: ["Recomendacion del chef", "Combo familiar", "Especial pickup", "Listo para delivery"],
        restaurantInfoTitle: "Horarios, ubicacion y opciones de servicio",
        restaurantInfoText: "Muestra horarios, pickup, delivery, consumo en local, zona de servicio y como debe ordenar el cliente.",
        restaurantInfoItems: ["Horarios", "Pickup", "Delivery", "En el local", "Catering", "Pedidos por WhatsApp"],
        restaurantOrderTitle: "Haz un pedido o pregunta",
        restaurantOrderText: "Envia platos preferidos, opcion de pickup o delivery y metodo de contacto. El negocio puede confirmar por telefono, WhatsApp o email.",
        clinicCare: "Cuidado personalizado",
        treatments: "Tratamientos",
        results: "Resultados",
        bookConsultation: "Agendar consulta",
        viewTreatments: "Ver tratamientos",
        consultationBased: "Segun consulta",
        popularTreatment: "Tratamiento popular",
        specialistLed: "Guiado por especialista",
        personalizedPlan: "Plan personalizado",
        clinicHeadline: (name) => `${name} cuida cada paso de tu tratamiento`,
        clinicSubheadline: (description) => description || "Una pagina profesional para clinicas y wellness enfocada en confianza, tratamientos, resultados, especialistas y reservas.",
        treatmentsTitle: "Tratamientos y servicios",
        treatmentsText: "Presenta los servicios que el cliente puede revisar, comparar y reservar con pasos claros.",
        carePathTitle: "Un camino claro desde la primera pregunta hasta la atencion",
        carePathText: "Muestra como la clinica evalua necesidades, recomienda el tratamiento correcto y da seguimiento.",
        carePathItems: ["Consulta inicial", "Plan personalizado", "Sesion de tratamiento", "Revision de progreso", "Guia de cuidado", "Proxima visita"],
        clinicTrustTitle: "Senales de confianza que el paciente busca",
        clinicTrustText: "Usa credenciales, seguridad, experiencia, expectativas claras y tiempos de respuesta.",
        clinicTrustItems: ["Guia especializada", "Proceso seguro", "Expectativas claras", "Consulta privada", "Cuidado posterior", "Respuesta rapida"],
        resultsTitle: "Resultados con expectativas reales",
        resultsText: "Usa esta seccion para antes/despues, notas de transformacion, casos o resultados esperados.",
        resultItems: ["Progreso visible", "Prueba antes/despues", "Plan personal", "Seguimiento", "Preparacion clara", "Cuidado posterior"],
        clinicTeamTitle: "Especialistas que guian el proceso",
        clinicTeamText: "Presenta al proveedor, estandares clinicos, certificaciones o filosofia de atencion.",
        clinicTeamItems: ["Equipo certificado", "Consulta primero", "Educacion al paciente", "Seguimiento"],
        clinicBookingTitle: "Agenda una consulta o haz una pregunta",
        clinicBookingText: "Envia el tratamiento, inquietud, horario preferido y metodo de contacto. La clinica confirma el siguiente paso.",
        clinicCategories: ["Estetica", "Wellness", "Dental", "Terapia", "Nutricion", "Consulta"],
        clinicDurations: ["Consulta 30 min", "Sesion 45 min", "Plan personal", "Seguimiento"],
        professionalFirm: "Firma profesional",
        scheduleConsultation: "Agendar consulta",
        confidential: "Confidencial",
        seniorAdvisor: "Asesor senior",
        caseReview: "Revision de caso",
        businessReady: "Listo para empresa",
        professionalHeadline: (name) => `${name} asesoria para decisiones importantes`,
        professionalSubheadline: (description) => description || "Una pagina profesional premium enfocada en confianza, areas de practica, proceso, pruebas y solicitudes de consulta.",
        practiceAreasTitle: "Areas de practica y servicios",
        practiceAreasText: "Muestra los servicios que el cliente puede revisar, comparar y solicitar con una ruta clara de consulta.",
        professionalProcessTitle: "Un proceso de asesoria claro",
        professionalProcessText: "Ayuda al cliente a entender como se revisa su situacion, se recomiendan pasos y se da seguimiento.",
        professionalProcessItems: ["Revision inicial", "Chequeo de documentos", "Llamada estrategica", "Plan de accion", "Seguimiento", "Asesoria continua"],
        professionalProofTitle: "Confianza antes del contacto",
        professionalProofText: "Usa credenciales, confidencialidad, expectativas de respuesta y estandares profesionales.",
        professionalProofItems: ["Proceso confidencial", "Revision senior", "Pasos claros", "Asesoria empresarial", "Soporte documental", "Contacto rapido"],
        professionalTeamTitle: "Asesores que manejan los detalles",
        professionalTeamText: "Presenta al abogado, contador, consultor o asesor y los estandares detras del servicio.",
        professionalTeamItems: ["Asesores expertos", "Consulta privada", "Documentacion clara", "Seguimiento"],
        professionalFaqTitle: "Preguntas antes de la consulta",
        professionalFaqItems: ["Que debo preparar?", "Como funciona la primera consulta?", "Pueden revisar documentos?", "Puede ser asesoria continua?"],
        professionalConsultationTitle: "Solicita una consulta o revision de documentos",
        professionalConsultationText: "Envia el servicio requerido, urgencia, horario preferido y metodo de contacto. La firma confirma el siguiente paso.",
        professionalCategories: ["Legal", "Impuestos", "Contabilidad", "Seguros", "Consultoria", "Compliance"],
        professionalEngagements: ["Revision inicial", "Revision documental", "Llamada estrategica", "Asesoria continua"],
        enterprisePlatform: "Plataforma empresarial",
        requestDemo: "Solicitar demo",
        viewSolutions: "Ver soluciones",
        solutions: "Soluciones",
        useCases: "Casos de uso",
        integrations: "Integraciones",
        customPlan: "Plan personalizado",
        enterpriseReady: "Listo para empresa",
        integrationReady: "Listo para integrar",
        roiFocused: "Enfocado en ROI",
        enterpriseHeadline: (name) => `${name} sistemas para que los equipos avancen mas rapido`,
        enterpriseSubheadline: (description) => description || "Una pagina B2B premium para software, automatizacion, integraciones, dashboards y servicios empresariales.",
        enterpriseSolutionsTitle: "Soluciones para equipos operativos",
        enterpriseSolutionsText: "Presenta productos, servicios o paquetes como soluciones de negocio con siguiente paso hacia demo.",
        enterpriseUseCasesTitle: "Casos de uso por flujo de trabajo",
        enterpriseUseCasesText: "Muestra como la solucion encaja en ventas, operaciones, soporte, finanzas, direccion o equipos de campo.",
        enterpriseUseCaseItems: ["Automatizar trabajo manual", "Centralizar reportes", "Conectar herramientas", "Mejorar visibilidad", "Reducir demoras", "Escalar operaciones"],
        enterpriseIntegrationsTitle: "Integraciones e implementacion",
        enterpriseIntegrationsText: "Da confianza tecnica con APIs, CRM, ERP, pagos, analitica y flujos de soporte.",
        enterpriseIntegrationItems: ["CRM", "ERP", "Pagos", "Analitica", "Soporte", "API personalizada"],
        enterpriseProofTitle: "Pruebas antes de la demo",
        enterpriseProofText: "Usa seguridad, implementacion, ROI, respuesta y soporte como senales de confianza.",
        enterpriseProofItems: ["Flujos seguros", "Soporte de implementacion", "Ruta clara de ROI", "Controles admin", "API lista", "Onboarding"],
        enterprisePricingTitle: "Paquetes para empezar simple y escalar",
        enterprisePricingText: "Usa esta area para paquetes starter, growth y enterprise sin mostrar costos internos.",
        enterpriseDemoTitle: "Agenda una demo o revision de solucion",
        enterpriseDemoText: "Envia tamano del equipo, herramientas actuales, problema del flujo y metodo de contacto.",
        enterpriseCategories: ["Automatizacion", "Analitica", "CRM", "Operaciones", "Integraciones", "Seguridad"],
        enterpriseTimelines: ["Demo primero", "Setup 2 semanas", "API lista", "Implementacion guiada"],
        industrialSupplier: "Proveedor industrial",
        requestQuote: "Solicitar cotizacion",
        viewSpecs: "Ver especificaciones",
        capabilities: "Capacidades",
        certifications: "Certificaciones",
        supplyChain: "Cadena de suministro",
        quoteRequired: "Cotizacion requerida",
        certified: "Certificado",
        bulkReady: "Listo por volumen",
        specReady: "Specs listas",
        moqReady: "MOQ listo",
        industrialHeadline: (name) => `${name} suministro para operaciones serias`,
        industrialSubheadline: (description) => description || "Pagina tecnica de proveedor industrial con cotizaciones, especificaciones, MOQ, tiempos de entrega, certificaciones y confianza B2B.",
        industrialCatalogTitle: "Productos tecnicos y suministro industrial",
        industrialCatalogText: "Presenta repuestos, equipos y materiales con datos que compras puede verificar antes de cotizar.",
        industrialCapabilitiesTitle: "Capacidades que compras necesita validar",
        industrialCapabilitiesText: "Muestra specs personalizadas, compras por volumen, sourcing, control de calidad y soporte recurrente.",
        industrialCapabilityItems: ["Specs personalizadas", "Ordenes por volumen", "Control de calidad", "Sourcing rapido", "Repuestos", "Soporte B2B"],
        industrialCertificationsTitle: "Certificaciones, calidad y documentacion",
        industrialCertificationsText: "Da confianza con documentacion, datos de seguridad, garantia y cumplimiento.",
        industrialCertificationItems: ["Docs ISO", "Specs de material", "Garantia", "Datos de seguridad", "Soporte compliance", "Trazabilidad"],
        industrialSupplyTitle: "Suministro y fulfillment",
        industrialSupplyText: "Explica tiempos, MOQ, flete y ordenes recurrentes de forma clara.",
        industrialSupplyItems: ["Tiempos de entrega", "Plan MOQ", "Soporte de flete", "Listo para almacen", "Sourcing", "Ordenes repetidas"],
        industrialQuoteTitle: "Solicita una cotizacion con especificaciones",
        industrialQuoteText: "Envia producto, cantidades, material, fecha limite y entrega. El proveedor responde con disponibilidad y precio.",
        industrialCategories: ["Maquinaria", "Repuestos", "Herramientas", "Seguridad", "Materiales", "Mantenimiento"],
        industrialLeadTimes: ["Cotizar primero", "Orden por volumen", "2-4 semanas", "Spec personalizada"],
        industrialMaterials: ["Acero", "Aluminio", "Compuesto", "Certificado"],
        solutionsSlug: "/soluciones",
        useCasesSlug: "/casos-de-uso",
        home: "Inicio",
        overview: "Vista general",
        products: "Productos",
        story: "Historia",
        shop: "Tienda",
        about: "Nosotros",
        contact: "Contacto",
        viewShop: "Ver tienda",
        contactVerb: "Contactar",
        featuredProducts: "Productos destacados",
        featuredText: "Una seleccion inicial para mostrar la oferta del negocio.",
        catalog: "Catalogo",
        catalogText: "Productos y servicios listos para editar, activar y publicar.",
        aboutBrand: "Sobre la marca",
        letsTalk: "Hablemos",
        contactText: "Contacta al negocio para comprar, cotizar o solicitar informacion.",
        footerText: (name) => `${name} - Pagina generada como borrador editable.`,
        modernCommercial: "Moderno comercial",
        fastBase: "Base rapida, limpia y editable para validar la tienda.",
        shopSlug: "/tienda",
        aboutSlug: "/nosotros",
        contactSlug: "/contacto",
        deals: "Ofertas",
        categories: "Categorias",
        support: "Soporte",
        fastDelivery: "Entrega rapida",
        freeShipping: "Envio gratis",
        todayDeal: "Oferta del dia",
        marketplaceHeadline: (name) => `${name} marketplace`,
        shopDeals: "Ver ofertas",
        viewCategories: "Ver categorias",
        searchPlaceholder: "Buscar productos, marcas o categorias",
        dealTitle: "Productos listos para vender",
        dealText: "Borrador tipo marketplace con categorias, ofertas, calificaciones y productos listos para checkout.",
        shopByCategory: "Compra por categoria",
        categoryRailText: "Rutas rapidas para que el cliente compare productos sin perder tiempo.",
        todayDeals: "Ofertas de hoy",
        dealsText: "Filas de ofertas, productos destacados y descubrimiento rapido.",
        bestSellers: "Mas vendidos",
        whyBuyHere: "Por que comprar aqui",
        trustText: "Entrega clara, checkout seguro, soporte y devoluciones simples.",
        marketplaceCategories: ["Electronica", "Hogar", "Moda", "Belleza", "Deportes", "Ofertas"],
        listings: "Listings",
        areas: "Zonas",
        listingsSlug: "/listings",
        areasSlug: "/zonas",
        searchListings: "Buscar listings",
        viewListings: "Ver listings",
        inquireNow: "Consultar ahora",
        featuredListing: "Listing destacado",
        newListing: "Nuevo listing",
        availableNow: "Disponible",
        listingPrice: "Precio a consultar",
        listingHeadline: (name) => `Encuentra el listing ideal con ${name}`,
        listingSubheadline: () => "Busca propiedades, alquileres, autos o clasificados por ubicacion, precio, categoria y detalles.",
        listingSearchPlaceholder: "Buscar por ubicacion, tipo, precio o palabra clave",
        listingFiltersTitle: "Busca con filtros claros",
        listingFiltersText: "Ayuda al cliente a comparar por categoria, zona, precio y disponibilidad antes de consultar.",
        featuredListingsTitle: "Listings destacados",
        featuredListingsText: "Una seleccion pulida de listings activos con precio, ubicacion, detalles y CTA de consulta.",
        listingAreaTitle: "Explora las mejores zonas",
        listingAreaText: "Usa tarjetas de zona, notas de ubicacion o areas de servicio para que la busqueda sea clara.",
        listingTrustTitle: "Confianza antes de consultar",
        listingTrustText: "Muestra detalles verificados, contacto claro y disponibilidad actualizada.",
        listingContactTitle: "Pregunta por un listing",
        listingContactText: "Envia el listing, presupuesto, ubicacion y metodo de contacto. El negocio puede responder con disponibilidad o siguiente paso.",
        listingCategories: ["Casas", "Alquileres", "Comercial", "Terrenos", "Autos", "Destacados"],
        listingLocations: ["Centro", "Zona norte", "Distrito oeste", "Cerca de escuelas", "Frente al agua", "Zona comercial"],
        listingTrustItems: ["Detalles verificados", "Disponibilidad actualizada", "Precios claros", "Soporte local", "Respuesta rapida", "Comparacion simple"],
        company: "Empresa",
        services: "Servicios",
        process: "Proceso",
        servicesSlug: "/servicios",
        processSlug: "/empresa",
        requestConsultation: "Solicitar consulta",
        viewServices: "Ver servicios",
        corporateHeadline: (name) => `${name} presenta una empresa clara y confiable`,
        corporateSubheadline: (description) => description || "Una pagina profesional enfocada en claridad, servicios, confianza y contacto directo.",
        corporateServicesTitle: "Servicios y capacidades",
        corporateServicesText: "Una vista clara de lo que hace la empresa, a quien ayuda y que resultados puede entregar.",
        corporateProcessTitle: "Un proceso simple desde la primera conversacion hasta la entrega",
        corporateProcessText: "Muestra como la empresa entiende la necesidad, recomienda el camino correcto y entrega con responsabilidad.",
        corporateProcessItems: ["Entender el objetivo", "Planificar la solucion correcta", "Entregar con comunicacion clara", "Acompanar el proximo paso"],
        corporateProofTitle: "Construido sobre confianza y trabajo medible",
        corporateProofText: "Usa esta seccion para credibilidad, experiencia, certificaciones, tipos de clientes o estandares de trabajo.",
        corporateProofItems: ["Entrega confiable", "Comunicacion clara", "Estandares profesionales"],
        corporateContactText: "Envia un mensaje para hablar de servicios, disponibilidad, precios o un proyecto personalizado.",
        offer: "Oferta",
        benefits: "Beneficios",
        proof: "Prueba",
        faq: "Preguntas",
        offerSlug: "/oferta",
        proofSlug: "/prueba",
        claimOffer: "Empezar ahora",
        seeProof: "Ver prueba",
        funnelHeadline: (name) => `${name} convierte interes en clientes reales`,
        funnelSubheadline: (description) => description || "Una landing enfocada en una oferta clara, prueba y un siguiente paso directo.",
        funnelBenefitsTitle: "Por que esta oferta funciona",
        funnelBenefitsText: "Muestra los resultados principales para que el visitante entienda el valor antes de contactar.",
        funnelBenefitsItems: ["Promesa clara", "Siguiente paso simple", "Pensado para leads calificados", "Facil de editar", "Estructura con prueba", "Lanzamiento rapido"],
        funnelOfferTitle: "Todo lo necesario para dar el siguiente paso",
        funnelOfferText: "Presenta la oferta, paquete, consulta o servicio en una estructura enfocada en conversion.",
        funnelGuarantee: "Expectativas claras antes de que el cliente se comprometa.",
        funnelProofTitle: "Prueba que elimina dudas",
        funnelProofText: "Usa resultados, testimonios, estandares o antes/despues para crear confianza.",
        funnelProofItems: ["Resultado especifico para el cliente", "Proceso simple", "Respuesta rapida", "Conversacion clara sobre precios"],
        funnelFaqTitle: "Preguntas antes de empezar",
        funnelFaqItems: [
          { question: "Que pasa despues de enviar la solicitud?", answer: "El negocio responde con el proximo paso, cotizacion, cita o consulta." },
          { question: "La oferta se puede personalizar?", answer: "Si. La oferta, textos, secciones y ruta de contacto son editables." },
          { question: "Esto es una tienda?", answer: "No. Esta version esta enfocada en leads, solicitudes, reservas o cotizaciones." }
        ],
        funnelContactText: "Envia una solicitud y el negocio puede responder con el siguiente paso, cotizacion o detalles de reserva.",
        premiumHeadline: (name) => `Conoce ${name}`,
        premiumSubheadline: (description) => description || "Una experiencia de producto refinada, simple, segura y memorable.",
        premiumPrimary: "Explorar productos",
        premiumSecondary: "Ver historia",
        premiumStoryTitle: "Dise\xF1ado alrededor de los detalles",
        premiumStoryText: "Una historia de producto enfocada, con textos limpios, confianza visual y espacio para que cada beneficio se entienda.",
        premiumFeatureTitle: "Una experiencia protagonista",
        premiumFeatureText: "Usa esta secci\xF3n para el beneficio principal, la historia de fabricaci\xF3n, la promesa de rendimiento o la raz\xF3n m\xE1s fuerte para comprar.",
        premiumGalleryTitle: "Selecci\xF3n curada",
        premiumGalleryText: "Un cat\xE1logo peque\xF1o presentado con m\xE1s cuidado, mejor jerarqu\xEDa visual y valor claro.",
        premiumSpecsTitle: "Lo importante, f\xE1cil de comparar",
        premiumSpecsText: "Destaca materiales, garant\xEDa, entrega, soporte, personalizaci\xF3n o calidad del servicio sin llenar la p\xE1gina de ruido.",
        premiumSpecItems: ["Presentaci\xF3n refinada", "Historia editable", "Soporte premium", "Listo para publicar"],
        collection: "Coleccion",
        provenance: "Provenance",
        privateInquiry: "Consulta privada",
        viewCollection: "Ver coleccion",
        requestPrivateViewing: "Solicitar cita privada",
        priceOnRequest: "Precio bajo consulta",
        limitedPiece: "Pieza limitada",
        authenticated: "Autenticada",
        privateService: "Servicio privado",
        signaturePiece: "Pieza protagonista",
        luxuryHeadline: (name) => `${name}: piezas exclusivas para clientes privados`,
        luxurySubheadline: (description) => description || "Un showroom privado para piezas de alto valor con provenance, presentacion editorial y consultas por cita.",
        luxurySignatureText: "Presenta una pieza protagonista, su historia y la razon por la que merece atencion privada.",
        luxuryCollectionTitle: "Coleccion privada curada",
        luxuryCollectionText: "Muestra piezas limitadas con mejor imagen, senales de autenticidad y ruta clara hacia consulta privada.",
        luxuryProvenanceTitle: "Provenance, autenticidad y servicio",
        luxuryProvenanceText: "El cliente de lujo necesita confianza antes de consultar. Usa esta seccion para origen, certificacion, materiales, cuidado y soporte privado.",
        luxuryPrivateTitle: "Una cita privada, manejada con cuidado",
        luxuryPrivateText: "Invita a compradores serios a solicitar disponibilidad, pieza preferida, rango de presupuesto y metodo de contacto.",
        luxuryContactTitle: "Solicita una consulta privada",
        luxuryContactText: "Envia la pieza, ocasion, horario preferido y metodo de contacto. Un asesor puede responder con disponibilidad y proximos pasos.",
        luxuryProofItems: ["Provenance verificada", "Disponibilidad limitada", "Asesor privado", "Manejo asegurado", "Consulta segura", "Seguimiento concierge"],
        luxuryCategories: ["Relojes", "Joyeria", "Arte", "Coleccionables", "Coleccion privada", "Piezas limitadas"],
        academy: "Academia",
        programs: "Programas",
        curriculum: "Curriculum",
        enroll: "Inscripcion",
        programsSlug: "/programas",
        curriculumSlug: "/curriculum",
        enrollSlug: "/inscripcion",
        courseAcademy: "Academia de cursos",
        enrollNow: "Inscribirme",
        viewCurriculum: "Ver curriculum",
        coursePrice: "Precio de inscripcion",
        featuredProgram: "Programa destacado",
        beginnerFriendly: "Ideal para empezar",
        certificateReady: "Con certificado",
        educationHeadline: (name) => `${name}: aprende con un camino claro`,
        educationSubheadline: (description) => description || "Una pagina de academia premium con cursos, modulos, resultados, confianza del instructor y ruta de inscripcion.",
        learningPathTitle: "Un camino claro desde la primera clase hasta el resultado",
        learningPathText: "Muestra el recorrido, modulos e hitos para que el estudiante entienda como va a avanzar.",
        learningPathItems: ["Empieza por la base", "Practica con modulos guiados", "Aplica el metodo", "Recibe soporte", "Completa el resultado", "Da el siguiente paso"],
        programsTitle: "Programas creados alrededor de resultados reales",
        programsText: "Presenta cursos, coaching, bootcamps o membresias con duracion, nivel y CTA de inscripcion.",
        outcomesTitle: "Lo que el estudiante debe lograr",
        outcomesText: "Convierte la promesa del curso en resultados, habilidades y senales claras de avance.",
        learningOutcomeItems: ["Progreso claro", "Practicas accionables", "Curriculum editable", "Prueba para estudiantes", "Notas de soporte", "Oferta lista para lanzar"],
        instructorTitle: "Guia experta, estructurada para actuar",
        instructorText: "Usa este espacio para credibilidad del instructor, metodo de ensenanza, soporte, certificaciones o valor de comunidad.",
        instructorTrustItems: ["Clases guiadas por expertos", "Modulos estructurados", "Ruta de soporte", "Resultados del estudiante", "Acceso flexible", "Oferta editable"],
        enrollmentTitle: "Listo para empezar a aprender?",
        enrollmentText: "Invita al estudiante a inscribirse, pedir acceso, consultar cohortes o elegir el programa correcto.",
        educationCategories: ["Base", "Avanzado", "Workshop", "Coaching", "Certificacion", "Membresia"],
        educationDurations: ["4 semanas", "6 modulos", "Cohorte en vivo", "A tu ritmo"],
        modules: "Modulos",
        modulesSlug: "/modulos",
        viewProducts: "Ver productos",
        digitalPrice: "Precio editable",
        digitalHeadline: (name) => `${name}: productos digitales con acceso inmediato`,
        digitalSubheadline: (description) => description || "Tienda pulida para cursos, plantillas, software, membresias y packs descargables.",
        digitalBundleTitle: "Ofertas digitales listas para vender",
        digitalBundleText: "Presenta productos como bundles claros con acceso inmediato, entregables, bonos y soporte.",
        digitalModulesTitle: "Que recibe el cliente",
        digitalModulesText: "Muestra modulos, descargas, lecciones, plantillas, licencias y soporte de forma clara.",
        digitalModuleItems: ["Entrenamiento principal", "Recursos descargables", "Plantillas y herramientas", "Material bonus", "Instrucciones de acceso", "Notas de soporte"],
        digitalProofTitle: "Confianza antes del checkout",
        digitalProofText: "Usa prueba social, resultados, garantia o autoridad del creador para que la compra se sienta segura.",
        digitalProofItems: ["Acceso inmediato", "Modulos editables", "Licencia clara", "Soporte listo", "Valor del bundle", "Checkout simple"],
        digitalAccessTitle: "Obt\xE9n acceso y empieza de inmediato",
        digitalAccessText: "Despues de comprar o solicitar, el cliente sabe exactamente que recibe, como entra y donde pide soporte.",
        digitalCategories: ["Curso", "Plantilla", "Toolkit", "Membresia", "Software", "Bundle"],
        newDrop: "Nuevo drop",
        collections: "Colecciones",
        lookbook: "Lookbook",
        fashionHeadline: (name) => `Nuevo drop de ${name}`,
        fashionSubheadline: (description) => description || "Una tienda visual de ropa pensada para colecciones, estilo y compra r\xE1pida.",
        shopTheDrop: "Comprar el drop",
        viewLookbook: "Ver lookbook",
        collectionsText: "Organiza productos por drop, fit, color, temporada o colecci\xF3n.",
        dropStoryTitle: "Dise\xF1ado para como se usa",
        dropStoryText: "Usa esta secci\xF3n para explicar el mood, materiales, fit y la historia visual de la colecci\xF3n.",
        lookbookText: "Muestra outfits, momentos lifestyle, fotos de campa\xF1a o combinaciones para que el cliente imagine el look.",
        newArrivals: "Nuevas piezas",
        newArrivalsText: "Una secci\xF3n de compra para el drop actual, m\xE1s vendidos y piezas listas para vender.",
        fitGuideTitle: "Fit, tallas y estilo",
        fitGuideText: "Dale confianza al cliente para escoger talla, material, fit y completar el look.",
        fitGuideItems: ["Gu\xEDa de tallas", "Notas de fit", "Materiales", "Completa el look"],
        jewelryCategories: ["Collares", "Pulseras", "Aretes y zarcillos", "Anillos", "Sets de regalo", "Piezas personalizadas"]
      },
      fr: {
        newStore: "Nouvelle boutique",
        defaultDescription: "Une marque pr\xEAte \xE0 vendre en ligne.",
        defaultProducts: ["Produit phare", "Service principal", "Offre sp\xE9ciale"],
        itemDescription: (name) => `Une option phare de ${name}, pr\xEAte \xE0 \xEAtre pr\xE9sent\xE9e avec d\xE9tails, b\xE9n\xE9fices et appel \xE0 l'action.`,
        askPrice: "Demander le prix",
        priceNotSet: "Prix editable",
        request: "Demander",
        viewProduct: "Voir le produit",
        onlineStore: "Boutique en ligne",
        defaultTone: "Professionnel et chaleureux",
        shopNow: "Acheter maintenant",
        viewCatalog: "Voir le catalogue",
        requestOrder: "Demander une commande",
        menu: "Menu",
        specials: "Specialites",
        hoursLocation: "Horaires et adresse",
        orderNow: "Commander",
        viewMenu: "Voir le menu",
        menuSlug: "/menu",
        hoursSlug: "/horaires-adresse",
        menuPrice: "Prix du menu",
        restaurantHeadline: (name) => `${name}: un menu simple a commander`,
        restaurantSubheadline: (description) => description || "Un site restaurant chaleureux avec plats signature, categories de menu, offres, horaires, adresse et commande simple.",
        restaurantCategoriesTitle: "Explorer le menu par categorie",
        restaurantCategoriesText: "Creez des chemins rapides vers entrees, plats, boissons, desserts, offres et favoris.",
        restaurantCategories: ["Entrees", "Plats", "Boissons", "Desserts", "Specialites", "Choix du chef"],
        restaurantSignatureTitle: "Plats signature prets a commander",
        restaurantSignatureText: "Mettez en avant les plats, menus et favoris qui doivent vendre en premier.",
        restaurantSpecialsTitle: "Offres et menus",
        restaurantSpecialsText: "Utilisez cette section pour formules dejeuner, menus famille, happy hour, livraison ou recommandations du chef.",
        restaurantSpecialItems: ["Recommandation du chef", "Menu famille", "Offre pickup", "Pret pour livraison"],
        restaurantInfoTitle: "Horaires, adresse et options de service",
        restaurantInfoText: "Affichez horaires, retrait, livraison, sur place, zone desservie et comment commander.",
        restaurantInfoItems: ["Horaires", "Retrait", "Livraison", "Sur place", "Traiteur", "Commandes WhatsApp"],
        restaurantOrderTitle: "Commander ou poser une question",
        restaurantOrderText: "Envoyez les plats souhaites, retrait ou livraison et le contact prefere. L'entreprise peut confirmer par telephone, WhatsApp ou email.",
        clinicCare: "Soin personnalise",
        treatments: "Soins",
        results: "Resultats",
        bookConsultation: "Reserver une consultation",
        viewTreatments: "Voir les soins",
        consultationBased: "Selon consultation",
        popularTreatment: "Soin populaire",
        specialistLed: "Guide par specialiste",
        personalizedPlan: "Plan personnalise",
        clinicHeadline: (name) => `${name} accompagne chaque etape de votre soin`,
        clinicSubheadline: (description) => description || "Un site professionnel de clinique ou wellness axe sur la confiance, les soins, les resultats, les specialistes et la reservation.",
        treatmentsTitle: "Soins et services",
        treatmentsText: "Presentez les services que le client peut consulter, comparer et reserver clairement.",
        carePathTitle: "Un parcours clair de la premiere question au soin",
        carePathText: "Montrez comment la clinique evalue les besoins, recommande le bon soin et assure le suivi.",
        carePathItems: ["Consultation initiale", "Plan personnalise", "Session de soin", "Suivi des progres", "Conseils apres-soin", "Prochaine visite"],
        clinicTrustTitle: "Signaux de confiance attendus",
        clinicTrustText: "Ajoutez diplomes, securite, experience, attentes claires et delais de reponse.",
        clinicTrustItems: ["Guidage specialise", "Processus securise", "Attentes claires", "Consultation privee", "Apres-soin", "Reponse rapide"],
        resultsTitle: "Resultats avec attentes realistes",
        resultsText: "Utilisez cette section pour avant/apres, cas, notes de transformation ou resultats attendus.",
        resultItems: ["Progres visible", "Preuve avant/apres", "Plan personnel", "Suivi", "Preparation claire", "Apres-soin inclus"],
        clinicTeamTitle: "Specialistes qui guident le processus",
        clinicTeamText: "Presentez le praticien, les standards, certifications ou philosophie de soin.",
        clinicTeamItems: ["Equipe certifiee", "Consultation d'abord", "Education patient", "Suivi"],
        clinicBookingTitle: "Reserver ou poser une question",
        clinicBookingText: "Envoyez le soin, le besoin, l'horaire prefere et le contact. La clinique confirme la suite.",
        clinicCategories: ["Esthetique", "Wellness", "Dentaire", "Therapie", "Nutrition", "Consultation"],
        clinicDurations: ["Consultation 30 min", "Session 45 min", "Plan personnel", "Suivi pret"],
        professionalFirm: "Cabinet professionnel",
        scheduleConsultation: "Reserver une consultation",
        confidential: "Confidentiel",
        seniorAdvisor: "Conseiller senior",
        caseReview: "Analyse du dossier",
        businessReady: "Pret pour entreprise",
        professionalHeadline: (name) => `${name} vous guide dans les decisions importantes`,
        professionalSubheadline: (description) => description || "Un site professionnel premium axe sur la confiance, les services, le processus, les preuves et les demandes de consultation.",
        practiceAreasTitle: "Services et domaines d'expertise",
        practiceAreasText: "Presentez les services que les clients peuvent comprendre, comparer et demander clairement.",
        professionalProcessTitle: "Un processus de conseil clair",
        professionalProcessText: "Expliquez comment le cabinet analyse la situation, recommande les prochaines etapes et assure le suivi.",
        professionalProcessItems: ["Analyse initiale", "Verification documents", "Appel strategie", "Plan d'action", "Suivi", "Conseil continu"],
        professionalProofTitle: "Preuves avant la prise de contact",
        professionalProofText: "Ajoutez credentials, confidentialite, attentes de reponse et standards professionnels.",
        professionalProofItems: ["Processus confidentiel", "Analyse senior", "Etapes claires", "Conseil entreprise", "Support documents", "Contact rapide"],
        professionalTeamTitle: "Des conseillers qui gerent les details",
        professionalTeamText: "Presentez avocat, comptable, consultant ou conseiller et les standards du service.",
        professionalTeamItems: ["Conseillers experimentes", "Consultation privee", "Documentation claire", "Suivi"],
        professionalFaqTitle: "Questions avant la consultation",
        professionalFaqItems: ["Que dois-je preparer?", "Comment se passe la premiere consultation?", "Pouvez-vous analyser des documents?", "Peut-on faire un accompagnement continu?"],
        professionalConsultationTitle: "Demander une consultation ou analyse de documents",
        professionalConsultationText: "Envoyez le service requis, l'urgence, le moment prefere et le contact. Le cabinet confirme la suite.",
        professionalCategories: ["Juridique", "Fiscalite", "Comptabilite", "Assurance", "Conseil", "Conformite"],
        professionalEngagements: ["Analyse initiale", "Analyse documents", "Appel strategie", "Conseil continu"],
        enterprisePlatform: "Plateforme entreprise",
        requestDemo: "Demander une demo",
        viewSolutions: "Voir les solutions",
        solutions: "Solutions",
        useCases: "Cas d'usage",
        integrations: "Integrations",
        customPlan: "Plan personnalise",
        enterpriseReady: "Pret entreprise",
        integrationReady: "Pret integration",
        roiFocused: "Oriente ROI",
        enterpriseHeadline: (name) => `${name} aide les equipes a avancer plus vite`,
        enterpriseSubheadline: (description) => description || "Un site B2B premium pour logiciel, automatisation, integrations, dashboards et services entreprise.",
        enterpriseSolutionsTitle: "Solutions pour equipes operationnelles",
        enterpriseSolutionsText: "Presentez produits, services ou packages comme solutions business avec demande de demo.",
        enterpriseUseCasesTitle: "Cas d'usage par workflow",
        enterpriseUseCasesText: "Montrez comment la solution s'adapte aux ventes, operations, support, finance ou direction.",
        enterpriseUseCaseItems: ["Automatiser le travail manuel", "Centraliser les rapports", "Connecter les outils", "Ameliorer la visibilite", "Reduire les delais", "Scaler les operations"],
        enterpriseIntegrationsTitle: "Integrations et deploiement",
        enterpriseIntegrationsText: "Rassurez les acheteurs techniques avec APIs, CRM, ERP, paiements, analytics et support.",
        enterpriseIntegrationItems: ["CRM", "ERP", "Paiements", "Analytics", "Support", "API custom"],
        enterpriseProofTitle: "Preuves avant la demo",
        enterpriseProofText: "Utilisez securite, implementation, ROI, reponse et support comme preuves.",
        enterpriseProofItems: ["Workflows securises", "Support deploiement", "ROI clair", "Controles admin", "API prete", "Onboarding equipe"],
        enterprisePricingTitle: "Packages simples a scaler",
        enterprisePricingText: "Starter, growth et enterprise sans exposer les couts internes.",
        enterpriseDemoTitle: "Reserver une demo ou analyse solution",
        enterpriseDemoText: "Envoyez taille d'equipe, outils actuels, probleme workflow et contact.",
        enterpriseCategories: ["Automatisation", "Analytics", "CRM", "Operations", "Integrations", "Securite"],
        enterpriseTimelines: ["Demo d'abord", "Setup 2 semaines", "API prete", "Deploiement guide"],
        industrialSupplier: "Fournisseur industriel",
        requestQuote: "Demander un devis",
        viewSpecs: "Voir les specs",
        capabilities: "Capacites",
        certifications: "Certifications",
        supplyChain: "Supply chain",
        quoteRequired: "Devis requis",
        certified: "Certifie",
        bulkReady: "Pret volume",
        specReady: "Specs pretes",
        moqReady: "MOQ pret",
        industrialHeadline: (name) => `${name} fournitures pour operations serieuses`,
        industrialSubheadline: (description) => description || "Site fournisseur industriel technique avec devis, specs, MOQ, delais, certifications et confiance B2B.",
        industrialCatalogTitle: "Produits techniques et fournitures industrielles",
        industrialCatalogText: "Presentez pieces, equipements et materiaux avec specs verifiables avant devis.",
        industrialCapabilitiesTitle: "Capacites a verifier avant achat",
        industrialCapabilitiesText: "Montrez specs custom, commandes volume, sourcing, qualite et support recurrent.",
        industrialCapabilityItems: ["Specs custom", "Commandes volume", "Controle qualite", "Sourcing rapide", "Pieces rechange", "Support B2B"],
        industrialCertificationsTitle: "Certifications, qualite et documentation",
        industrialCertificationsText: "Rassurez avec docs, donnees securite, garantie et conformite.",
        industrialCertificationItems: ["Docs ISO", "Specs materiaux", "Garantie", "Donnees securite", "Support conformite", "Tra\xE7abilite"],
        industrialSupplyTitle: "Supply chain et fulfillment",
        industrialSupplyText: "Clarifiez delais, MOQ, fret et commandes recurrentes.",
        industrialSupplyItems: ["Delais", "Plan MOQ", "Support fret", "Pret entrepot", "Sourcing vendeur", "Commandes repetees"],
        industrialQuoteTitle: "Demander un devis avec specs",
        industrialQuoteText: "Envoyez produit, quantites, materiau, delai et livraison. Le fournisseur repond avec disponibilite et prix.",
        industrialCategories: ["Machines", "Pieces", "Outils", "Securite", "Materiaux", "Maintenance"],
        industrialLeadTimes: ["Devis d'abord", "Commande volume", "2-4 semaines", "Spec custom"],
        industrialMaterials: ["Acier", "Aluminium", "Composite", "Certifie"],
        solutionsSlug: "/solutions",
        useCasesSlug: "/cas-usage",
        home: "Accueil",
        overview: "Aper\xE7u",
        products: "Produits",
        story: "Histoire",
        shop: "Boutique",
        about: "\xC0 propos",
        contact: "Contact",
        viewShop: "Voir la boutique",
        contactVerb: "Contacter",
        featuredProducts: "Produits phares",
        featuredText: "Une premi\xE8re s\xE9lection pour pr\xE9senter l'offre de l'entreprise.",
        catalog: "Catalogue",
        catalogText: "Produits et services pr\xEAts \xE0 modifier, activer et publier.",
        aboutBrand: "\xC0 propos de la marque",
        letsTalk: "Parlons-en",
        contactText: "Contactez l'entreprise pour acheter, demander un devis ou obtenir des informations.",
        footerText: (name) => `${name} - Brouillon de site modifiable.`,
        modernCommercial: "Commercial moderne",
        fastBase: "Base rapide, claire et modifiable pour valider la boutique.",
        shopSlug: "/boutique",
        aboutSlug: "/a-propos",
        contactSlug: "/contact",
        deals: "Offres",
        categories: "Categories",
        support: "Support",
        fastDelivery: "Livraison rapide",
        freeShipping: "Livraison gratuite",
        todayDeal: "Offre du jour",
        marketplaceHeadline: (name) => `Marketplace ${name}`,
        shopDeals: "Voir les offres",
        viewCategories: "Voir les categories",
        searchPlaceholder: "Rechercher produits, marques ou categories",
        dealTitle: "Selections pretes a vendre",
        dealText: "Brouillon marketplace avec categories, offres, notes et fiches produits pretes pour le checkout.",
        shopByCategory: "Acheter par categorie",
        categoryRailText: "Chemins rapides pour comparer les produits plus vite.",
        todayDeals: "Offres du jour",
        dealsText: "Rangees d'offres, produits mis en avant et decouverte rapide.",
        bestSellers: "Meilleures ventes",
        whyBuyHere: "Pourquoi acheter ici",
        trustText: "Livraison claire, paiement securise, support et retours simples.",
        marketplaceCategories: ["Electronique", "Maison", "Mode", "Beaute", "Sport", "Offres"],
        listings: "Annonces",
        areas: "Zones",
        listingsSlug: "/annonces",
        areasSlug: "/zones",
        searchListings: "Rechercher",
        viewListings: "Voir les annonces",
        inquireNow: "Demander",
        featuredListing: "Annonce en avant",
        newListing: "Nouvelle annonce",
        availableNow: "Disponible",
        listingPrice: "Prix sur demande",
        listingHeadline: (name) => `Trouvez la bonne annonce avec ${name}`,
        listingSubheadline: () => "Recherchez par localisation, prix, categorie et details avec un chemin de demande clair.",
        listingSearchPlaceholder: "Rechercher par lieu, type, prix ou mot-cle",
        listingFiltersTitle: "Rechercher avec les bons filtres",
        listingFiltersText: "Aidez les clients a comparer par categorie, zone, prix et disponibilite.",
        featuredListingsTitle: "Annonces en avant",
        featuredListingsText: "Une selection claire d'annonces actives avec prix, localisation, details et CTA.",
        listingAreaTitle: "Explorer les meilleures zones",
        listingAreaText: "Utilisez des cartes de zones et notes locales pour clarifier la recherche.",
        listingTrustTitle: "Confiance avant la demande",
        listingTrustText: "Affichez details verifies, contact clair et disponibilite mise a jour.",
        listingContactTitle: "Demander une annonce",
        listingContactText: "Envoyez l'annonce, le budget, la localisation et le contact prefere.",
        listingCategories: ["Maisons", "Locations", "Commercial", "Terrain", "Voitures", "En avant"],
        listingLocations: ["Centre", "Zone nord", "Quartier ouest", "Pres des ecoles", "Bord de l'eau", "Zone business"],
        listingTrustItems: ["Details verifies", "Disponibilite a jour", "Prix clairs", "Support local", "Reponse rapide", "Comparaison simple"],
        company: "Entreprise",
        services: "Services",
        process: "Processus",
        servicesSlug: "/services",
        processSlug: "/entreprise",
        requestConsultation: "Demander une consultation",
        viewServices: "Voir les services",
        corporateHeadline: (name) => `${name} presente une entreprise claire et fiable`,
        corporateSubheadline: (description) => description || "Un site professionnel centre sur la clarte, les services, la preuve et le contact direct.",
        corporateServicesTitle: "Services et capacites",
        corporateServicesText: "Un apercu clair de ce que fait l'entreprise, des clients aides et des resultats attendus.",
        corporateProcessTitle: "Un processus simple du premier echange a la livraison",
        corporateProcessText: "Montrez comment l'entreprise comprend le besoin, recommande la bonne approche et livre avec responsabilite.",
        corporateProcessItems: ["Comprendre l'objectif", "Planifier la bonne solution", "Livrer avec une communication claire", "Accompagner la suite"],
        corporateProofTitle: "Construit sur la confiance et un travail mesurable",
        corporateProofText: "Utilisez cette section pour la credibilite, l'experience, les certifications, les types de clients ou les standards.",
        corporateProofItems: ["Livraison fiable", "Communication claire", "Standards professionnels"],
        corporateContactText: "Envoyez un message pour discuter des services, disponibilites, prix ou d'un projet sur mesure.",
        offer: "Offre",
        benefits: "Benefices",
        proof: "Preuves",
        faq: "FAQ",
        offerSlug: "/offre",
        proofSlug: "/preuves",
        claimOffer: "Commencer",
        seeProof: "Voir les preuves",
        funnelHeadline: (name) => `${name} transforme l'interet en vrais clients`,
        funnelSubheadline: (description) => description || "Une landing centree sur une offre claire, des preuves et une prochaine etape directe.",
        funnelBenefitsTitle: "Pourquoi cette offre fonctionne",
        funnelBenefitsText: "Montrez les resultats importants pour que le visiteur comprenne la valeur avant de contacter.",
        funnelBenefitsItems: ["Promesse claire", "Prochaine etape simple", "Leads qualifies", "Facile a modifier", "Structure orientee preuves", "Lancement rapide"],
        funnelOfferTitle: "Tout pour passer a l'etape suivante",
        funnelOfferText: "Presentez l'offre, le forfait, la consultation ou le service dans une structure orientee conversion.",
        funnelGuarantee: "Des attentes claires avant l'engagement du client.",
        funnelProofTitle: "Des preuves qui reduisent le doute",
        funnelProofText: "Utilisez resultats, temoignages, standards ou avant/apres pour creer la confiance.",
        funnelProofItems: ["Resultat client specifique", "Processus simple", "Reponse rapide", "Discussion claire sur les prix"],
        funnelFaqTitle: "Questions avant de commencer",
        funnelFaqItems: [
          { question: "Que se passe-t-il apres la demande ?", answer: "L'entreprise repond avec la prochaine etape, le devis, le rendez-vous ou la consultation." },
          { question: "L'offre peut-elle etre personnalisee ?", answer: "Oui. L'offre, les textes, sections et contact sont modifiables." },
          { question: "Est-ce une boutique ?", answer: "Non. Cette version vise les leads, demandes, reservations ou devis." }
        ],
        funnelContactText: "Envoyez une demande et l'entreprise pourra repondre avec la prochaine etape, un devis ou une reservation.",
        premiumHeadline: (name) => `D\xE9couvrez ${name}`,
        premiumSubheadline: (description) => description || "Une exp\xE9rience produit raffin\xE9e, simple, confiante et m\xE9morable.",
        premiumPrimary: "Explorer les produits",
        premiumSecondary: "Voir l'histoire",
        premiumStoryTitle: "Con\xE7u autour des d\xE9tails",
        premiumStoryText: "Une histoire produit cibl\xE9e, avec un texte clair, une confiance discr\xE8te et assez d'espace pour chaque b\xE9n\xE9fice.",
        premiumFeatureTitle: "Une exp\xE9rience phare",
        premiumFeatureText: "Utilisez cette section pour le b\xE9n\xE9fice signature, l'histoire de fabrication, la promesse de performance ou la meilleure raison d'acheter.",
        premiumGalleryTitle: "S\xE9lection soign\xE9e",
        premiumGalleryText: "Un petit catalogue pr\xE9sent\xE9 avec plus de soin, une meilleure hi\xE9rarchie visuelle et une valeur claire.",
        premiumSpecsTitle: "L'essentiel, facile \xE0 comparer",
        premiumSpecsText: "Mettez en avant mat\xE9riaux, garantie, livraison, support, personnalisation ou qualit\xE9 de service sans surcharge.",
        premiumSpecItems: ["Pr\xE9sentation raffin\xE9e", "Histoire modifiable", "Support premium", "Pr\xEAt \xE0 publier"],
        academy: "Academie",
        programs: "Programmes",
        curriculum: "Programme",
        enroll: "Inscription",
        programsSlug: "/programmes",
        curriculumSlug: "/programme",
        enrollSlug: "/inscription",
        courseAcademy: "Academie de cours",
        enrollNow: "S'inscrire",
        viewCurriculum: "Voir le programme",
        coursePrice: "Prix d'inscription",
        featuredProgram: "Programme phare",
        beginnerFriendly: "Pour debuter",
        certificateReady: "Certificat pret",
        educationHeadline: (name) => `${name}: apprendre avec un parcours clair`,
        educationSubheadline: (description) => description || "Un site d'academie premium avec cours, modules, resultats, credibilite de l'instructeur et inscription.",
        learningPathTitle: "Un parcours clair de la premiere lecon au resultat",
        learningPathText: "Montrez les modules, jalons et etapes pour que l'etudiant comprenne sa progression.",
        learningPathItems: ["Bases", "Pratique guidee", "Application", "Support", "Resultat", "Prochaine etape"],
        programsTitle: "Programmes construits autour de resultats reels",
        programsText: "Presentez cours, coaching, bootcamps ou abonnements avec duree, niveau et CTA.",
        outcomesTitle: "Ce que l'etudiant doit savoir faire",
        outcomesText: "Transformez la promesse du cours en resultats, competences et signaux de progression.",
        learningOutcomeItems: ["Progression claire", "Exercices pratiques", "Programme modifiable", "Preuve etudiante", "Notes support", "Offre prete"],
        instructorTitle: "Guidage expert, structure pour agir",
        instructorText: "Utilisez cet espace pour la credibilite, la methode, le support ou la communaute.",
        instructorTrustItems: ["Cours experts", "Modules structures", "Parcours support", "Resultats etudiants", "Acces flexible", "Offre modifiable"],
        enrollmentTitle: "Pret a commencer?",
        enrollmentText: "Invitez l'etudiant a s'inscrire, demander l'acces, poser une question ou choisir le bon programme.",
        educationCategories: ["Bases", "Avance", "Workshop", "Coaching", "Certification", "Abonnement"],
        educationDurations: ["4 semaines", "6 modules", "Cohorte live", "A son rythme"],
        modules: "Modules",
        modulesSlug: "/modules",
        viewProducts: "Voir les produits",
        digitalPrice: "Prix modifiable",
        digitalHeadline: (name) => `${name}: produits digitaux avec acces immediat`,
        digitalSubheadline: (description) => description || "Boutique soignee pour cours, templates, logiciel, abonnements et packs telechargeables.",
        digitalBundleTitle: "Offres digitales pretes a vendre",
        digitalBundleText: "Presentez les produits en bundles clairs avec acces immediat, livrables, bonus et support.",
        digitalModulesTitle: "Ce que le client recoit",
        digitalModulesText: "Affichez modules, telechargements, lecons, templates, licences et support dans une structure claire.",
        digitalModuleItems: ["Formation principale", "Ressources telechargeables", "Templates et outils", "Bonus", "Instructions d'acces", "Notes de support"],
        digitalProofTitle: "Confiance avant le paiement",
        digitalProofText: "Utilisez preuves, resultats, garantie ou credibilite du createur pour rassurer l'achat.",
        digitalProofItems: ["Acces immediat", "Modules modifiables", "Licence claire", "Support pret", "Valeur du bundle", "Paiement simple"],
        digitalAccessTitle: "Obtenir l'acces et commencer tout de suite",
        digitalAccessText: "Apres achat ou demande, le client sait exactement ce qu'il recoit, comment acceder et ou demander support.",
        digitalCategories: ["Cours", "Template", "Toolkit", "Abonnement", "Logiciel", "Bundle"],
        newDrop: "Nouveau drop",
        collections: "Collections",
        lookbook: "Lookbook",
        fashionHeadline: (name) => `Nouveau drop ${name}`,
        fashionSubheadline: (description) => description || "Une boutique mode visuelle construite autour des collections, du style et de l'achat rapide.",
        shopTheDrop: "Acheter le drop",
        viewLookbook: "Voir le lookbook",
        collectionsText: "Organisez les produits par drop, coupe, couleur, saison ou collection.",
        dropStoryTitle: "Pens\xE9 pour \xEAtre port\xE9",
        dropStoryText: "Utilisez cette section pour expliquer l'ambiance, les mati\xE8res, la coupe et l'histoire visuelle de la collection.",
        lookbookText: "Montrez des tenues, moments lifestyle, photos de campagne ou combinaisons de produits.",
        newArrivals: "Nouveaut\xE9s",
        newArrivalsText: "Une section shopping pour le drop actuel, les meilleures ventes et les pi\xE8ces pr\xEAtes \xE0 acheter.",
        fitGuideTitle: "Coupe, tailles et style",
        fitGuideText: "Aidez les clients \xE0 choisir la bonne taille, mati\xE8re, coupe et tenue compl\xE8te.",
        fitGuideItems: ["Guide tailles", "Notes coupe", "Mati\xE8res", "Composer le look"],
        jewelryCategories: ["Colliers", "Bracelets", "Boucles d'oreilles", "Bagues", "Coffrets cadeau", "Pieces personnalisees"]
      },
      pt: {
        newStore: "Nova loja",
        defaultDescription: "Uma marca pronta para vender online.",
        defaultProducts: ["Produto em destaque", "Servi\xE7o principal", "Oferta especial"],
        itemDescription: (name) => `Uma op\xE7\xE3o em destaque de ${name}, pronta para apresentar detalhes, benef\xEDcios e chamada para a\xE7\xE3o.`,
        askPrice: "Consultar pre\xE7o",
        priceNotSet: "Pre\xE7o edit\xE1vel",
        request: "Solicitar",
        viewProduct: "Ver produto",
        onlineStore: "Loja online",
        defaultTone: "Profissional e pr\xF3ximo",
        shopNow: "Comprar agora",
        viewCatalog: "Ver cat\xE1logo",
        requestOrder: "Solicitar pedido",
        menu: "Menu",
        specials: "Especiais",
        hoursLocation: "Horarios e localizacao",
        orderNow: "Pedir agora",
        viewMenu: "Ver menu",
        menuSlug: "/menu",
        hoursSlug: "/horarios-localizacao",
        menuPrice: "Preco do menu",
        restaurantHeadline: (name) => `${name}: menu claro e facil de pedir`,
        restaurantSubheadline: (description) => description || "Site acolhedor para restaurante com pratos destaque, categorias, especiais, horarios, localizacao e pedido simples.",
        restaurantCategoriesTitle: "Explore o menu por categoria",
        restaurantCategoriesText: "Crie caminhos rapidos para entradas, pratos principais, bebidas, sobremesas, especiais e favoritos.",
        restaurantCategories: ["Entradas", "Pratos principais", "Bebidas", "Sobremesas", "Especiais", "Chef indica"],
        restaurantSignatureTitle: "Pratos destaque prontos para pedir",
        restaurantSignatureText: "Mostre pratos, combos e favoritos que devem vender primeiro.",
        restaurantSpecialsTitle: "Especiais e combos",
        restaurantSpecialsText: "Use esta secao para almocos, combos familiares, happy hour, delivery ou indicacoes do chef.",
        restaurantSpecialItems: ["Indicacao do chef", "Combo familia", "Especial pickup", "Pronto para delivery"],
        restaurantInfoTitle: "Horarios, localizacao e servico",
        restaurantInfoText: "Mostre horarios, retirada, delivery, consumo no local, area atendida e como o cliente deve pedir.",
        restaurantInfoItems: ["Horarios", "Pickup", "Delivery", "No local", "Catering", "Pedidos por WhatsApp"],
        restaurantOrderTitle: "Fazer pedido ou perguntar",
        restaurantOrderText: "Envie os pratos desejados, opcao de pickup ou delivery e metodo de contato. O negocio pode confirmar por telefone, WhatsApp ou email.",
        clinicCare: "Cuidado personalizado",
        treatments: "Tratamentos",
        results: "Resultados",
        bookConsultation: "Agendar consulta",
        viewTreatments: "Ver tratamentos",
        consultationBased: "Sob consulta",
        popularTreatment: "Tratamento popular",
        specialistLed: "Com especialista",
        personalizedPlan: "Plano personalizado",
        clinicHeadline: (name) => `${name} cuida de cada etapa do seu tratamento`,
        clinicSubheadline: (description) => description || "Um site profissional de clinica ou wellness focado em confianca, tratamentos, resultados, especialistas e agendamento.",
        treatmentsTitle: "Tratamentos e servicos",
        treatmentsText: "Apresente os servicos que o cliente pode revisar, comparar e agendar com passos claros.",
        carePathTitle: "Um caminho claro da primeira pergunta ao cuidado",
        carePathText: "Mostre como a clinica avalia necessidades, recomenda o tratamento certo e acompanha o cliente.",
        carePathItems: ["Consulta inicial", "Plano personalizado", "Sessao de tratamento", "Revisao de progresso", "Orientacao pos-cuidado", "Proxima visita"],
        clinicTrustTitle: "Sinais de confianca para o paciente",
        clinicTrustText: "Use credenciais, seguranca, experiencia, expectativas claras e tempos de resposta.",
        clinicTrustItems: ["Guia especializada", "Processo seguro", "Expectativas claras", "Consulta privada", "Pos-cuidado", "Resposta rapida"],
        resultsTitle: "Resultados com expectativas reais",
        resultsText: "Use esta secao para antes/depois, notas de transformacao, casos ou resultados esperados.",
        resultItems: ["Progresso visivel", "Prova antes/depois", "Plano pessoal", "Acompanhamento", "Preparacao clara", "Pos-cuidado incluso"],
        clinicTeamTitle: "Especialistas que guiam o processo",
        clinicTeamText: "Apresente o profissional, padroes clinicos, certificacoes ou filosofia de cuidado.",
        clinicTeamItems: ["Equipe certificada", "Consulta primeiro", "Educacao do paciente", "Acompanhamento"],
        clinicBookingTitle: "Agende uma consulta ou pergunte",
        clinicBookingText: "Envie o tratamento, duvida, horario preferido e metodo de contato. A clinica confirma o proximo passo.",
        clinicCategories: ["Estetica", "Wellness", "Dental", "Terapia", "Nutricao", "Consulta"],
        clinicDurations: ["Consulta 30 min", "Sessao 45 min", "Plano pessoal", "Acompanhamento"],
        professionalFirm: "Firma profissional",
        scheduleConsultation: "Agendar consulta",
        confidential: "Confidencial",
        seniorAdvisor: "Consultor senior",
        caseReview: "Revisao do caso",
        businessReady: "Pronto para empresa",
        professionalHeadline: (name) => `${name} orienta decisoes importantes`,
        professionalSubheadline: (description) => description || "Um site profissional premium focado em confianca, areas de servico, processo, provas e pedidos de consulta.",
        practiceAreasTitle: "Areas de atuacao e servicos",
        practiceAreasText: "Mostre os servicos que o cliente pode revisar, comparar e solicitar com uma rota clara de consulta.",
        professionalProcessTitle: "Um processo de consultoria claro",
        professionalProcessText: "Ajude o cliente a entender como a firma revisa a situacao, recomenda proximos passos e acompanha.",
        professionalProcessItems: ["Revisao inicial", "Analise documental", "Chamada estrategica", "Plano de acao", "Acompanhamento", "Consultoria continua"],
        professionalProofTitle: "Confianca antes do contato",
        professionalProofText: "Use credenciais, confidencialidade, expectativas de resposta e padroes profissionais.",
        professionalProofItems: ["Processo confidencial", "Revisao senior", "Passos claros", "Consultoria empresarial", "Suporte documental", "Contato rapido"],
        professionalTeamTitle: "Consultores que cuidam dos detalhes",
        professionalTeamText: "Apresente advogado, contador, consultor ou assessor e os padroes do servico.",
        professionalTeamItems: ["Consultores experientes", "Consulta privada", "Documentacao clara", "Acompanhamento"],
        professionalFaqTitle: "Perguntas antes da consulta",
        professionalFaqItems: ["O que devo preparar?", "Como funciona a primeira consulta?", "Podem revisar documentos?", "Pode virar consultoria continua?"],
        professionalConsultationTitle: "Solicite uma consulta ou revisao documental",
        professionalConsultationText: "Envie o servico requerido, urgencia, horario preferido e metodo de contato. A firma confirma o proximo passo.",
        professionalCategories: ["Juridico", "Impostos", "Contabilidade", "Seguros", "Consultoria", "Compliance"],
        professionalEngagements: ["Revisao inicial", "Revisao documental", "Chamada estrategica", "Consultoria continua"],
        enterprisePlatform: "Plataforma empresarial",
        requestDemo: "Solicitar demo",
        viewSolutions: "Ver solucoes",
        solutions: "Solucoes",
        useCases: "Casos de uso",
        integrations: "Integracoes",
        customPlan: "Plano personalizado",
        enterpriseReady: "Pronto para empresa",
        integrationReady: "Pronto para integrar",
        roiFocused: "Focado em ROI",
        enterpriseHeadline: (name) => `${name} sistemas para equipes avancarem mais rapido`,
        enterpriseSubheadline: (description) => description || "Um site B2B premium para software, automacao, integracoes, dashboards e servicos empresariais.",
        enterpriseSolutionsTitle: "Solucoes para equipes operacionais",
        enterpriseSolutionsText: "Apresente produtos, servicos ou pacotes como solucoes de negocio com proximo passo para demo.",
        enterpriseUseCasesTitle: "Casos de uso por workflow",
        enterpriseUseCasesText: "Mostre como a solucao se encaixa em vendas, operacoes, suporte, financeiro ou lideranca.",
        enterpriseUseCaseItems: ["Automatizar trabalho manual", "Centralizar relatorios", "Conectar ferramentas", "Melhorar visibilidade", "Reduzir atrasos", "Escalar operacoes"],
        enterpriseIntegrationsTitle: "Integracoes e implementacao",
        enterpriseIntegrationsText: "De confianca tecnica com APIs, CRM, ERP, pagamentos, analytics e suporte.",
        enterpriseIntegrationItems: ["CRM", "ERP", "Pagamentos", "Analytics", "Suporte", "API custom"],
        enterpriseProofTitle: "Provas antes da demo",
        enterpriseProofText: "Use seguranca, implementacao, ROI, resposta e suporte como sinais de confianca.",
        enterpriseProofItems: ["Workflows seguros", "Suporte de implementacao", "ROI claro", "Controles admin", "API pronta", "Onboarding"],
        enterprisePricingTitle: "Pacotes para comecar simples e escalar",
        enterprisePricingText: "Use starter, growth e enterprise sem expor custos internos.",
        enterpriseDemoTitle: "Agende uma demo ou revisao da solucao",
        enterpriseDemoText: "Envie tamanho da equipe, ferramentas atuais, problema do fluxo e contato.",
        enterpriseCategories: ["Automacao", "Analytics", "CRM", "Operacoes", "Integracoes", "Seguranca"],
        enterpriseTimelines: ["Demo primeiro", "Setup 2 semanas", "API pronta", "Implementacao guiada"],
        industrialSupplier: "Fornecedor industrial",
        requestQuote: "Solicitar cotacao",
        viewSpecs: "Ver especificacoes",
        capabilities: "Capacidades",
        certifications: "Certificacoes",
        supplyChain: "Cadeia de suprimentos",
        quoteRequired: "Cotacao requerida",
        certified: "Certificado",
        bulkReady: "Pronto para volume",
        specReady: "Specs prontas",
        moqReady: "MOQ pronto",
        industrialHeadline: (name) => `${name} suprimentos para operacoes serias`,
        industrialSubheadline: (description) => description || "Site tecnico de fornecedor industrial com cotacoes, especificacoes, MOQ, prazos, certificacoes e confianca B2B.",
        industrialCatalogTitle: "Produtos tecnicos e suprimento industrial",
        industrialCatalogText: "Apresente pecas, equipamentos e materiais com dados que compras pode verificar antes de cotar.",
        industrialCapabilitiesTitle: "Capacidades que compras precisa validar",
        industrialCapabilitiesText: "Mostre specs customizadas, compras em volume, sourcing, controle de qualidade e suporte recorrente.",
        industrialCapabilityItems: ["Specs customizadas", "Pedidos em volume", "Controle de qualidade", "Sourcing rapido", "Pecas reposicao", "Suporte B2B"],
        industrialCertificationsTitle: "Certificacoes, qualidade e documentacao",
        industrialCertificationsText: "De confianca com documentacao, dados de seguranca, garantia e compliance.",
        industrialCertificationItems: ["Docs ISO", "Specs de material", "Garantia", "Dados seguranca", "Suporte compliance", "Rastreabilidade"],
        industrialSupplyTitle: "Suprimentos e fulfillment",
        industrialSupplyText: "Explique prazos, MOQ, frete e pedidos recorrentes com clareza.",
        industrialSupplyItems: ["Prazos", "Planejamento MOQ", "Suporte frete", "Pronto para estoque", "Sourcing", "Pedidos repetidos"],
        industrialQuoteTitle: "Solicite cotacao com especificacoes",
        industrialQuoteText: "Envie produto, quantidades, material, prazo e entrega. O fornecedor responde com disponibilidade e preco.",
        industrialCategories: ["Maquinas", "Pecas", "Ferramentas", "Seguranca", "Materiais", "Manutencao"],
        industrialLeadTimes: ["Cotar primeiro", "Pedido volume", "2-4 semanas", "Spec custom"],
        industrialMaterials: ["Aco", "Aluminio", "Composto", "Certificado"],
        solutionsSlug: "/solucoes",
        useCasesSlug: "/casos-de-uso",
        home: "In\xEDcio",
        overview: "Vis\xE3o geral",
        products: "Produtos",
        story: "Hist\xF3ria",
        shop: "Loja",
        about: "Sobre",
        contact: "Contato",
        viewShop: "Ver loja",
        contactVerb: "Contato",
        featuredProducts: "Produtos em destaque",
        featuredText: "Uma sele\xE7\xE3o inicial para mostrar a oferta do neg\xF3cio.",
        catalog: "Cat\xE1logo",
        catalogText: "Produtos e servi\xE7os prontos para editar, ativar e publicar.",
        aboutBrand: "Sobre a marca",
        letsTalk: "Vamos conversar",
        contactText: "Entre em contato para comprar, pedir or\xE7amento ou solicitar informa\xE7\xF5es.",
        footerText: (name) => `${name} - Rascunho de site edit\xE1vel.`,
        modernCommercial: "Comercial moderno",
        fastBase: "Base r\xE1pida, limpa e edit\xE1vel para validar a loja.",
        shopSlug: "/loja",
        aboutSlug: "/sobre",
        contactSlug: "/contato",
        deals: "Ofertas",
        categories: "Categorias",
        support: "Suporte",
        fastDelivery: "Entrega rapida",
        freeShipping: "Frete gratis",
        todayDeal: "Oferta do dia",
        marketplaceHeadline: (name) => `${name} marketplace`,
        shopDeals: "Ver ofertas",
        viewCategories: "Ver categorias",
        searchPlaceholder: "Buscar produtos, marcas ou categorias",
        dealTitle: "Produtos prontos para vender",
        dealText: "Rascunho tipo marketplace com categorias, ofertas, avaliacoes e cards prontos para checkout.",
        shopByCategory: "Comprar por categoria",
        categoryRailText: "Caminhos rapidos para o cliente comparar produtos.",
        todayDeals: "Ofertas de hoje",
        dealsText: "Linhas de ofertas, produtos em destaque e descoberta rapida.",
        bestSellers: "Mais vendidos",
        whyBuyHere: "Por que comprar aqui",
        trustText: "Entrega clara, checkout seguro, suporte e devolucoes simples.",
        marketplaceCategories: ["Eletronicos", "Casa", "Moda", "Beleza", "Esportes", "Ofertas"],
        listings: "Anuncios",
        areas: "Areas",
        listingsSlug: "/anuncios",
        areasSlug: "/areas",
        searchListings: "Buscar anuncios",
        viewListings: "Ver anuncios",
        inquireNow: "Consultar agora",
        featuredListing: "Anuncio destaque",
        newListing: "Novo anuncio",
        availableNow: "Disponivel",
        listingPrice: "Preco sob consulta",
        listingHeadline: (name) => `Encontre o anuncio ideal com ${name}`,
        listingSubheadline: () => "Busque imoveis, alugueis, carros ou classificados por localizacao, preco, categoria e detalhes.",
        listingSearchPlaceholder: "Buscar por localizacao, tipo, preco ou palavra-chave",
        listingFiltersTitle: "Busca com filtros claros",
        listingFiltersText: "Ajude o cliente a comparar por categoria, area, preco e disponibilidade.",
        featuredListingsTitle: "Anuncios em destaque",
        featuredListingsText: "Uma selecao de anuncios ativos com preco, localizacao, detalhes e CTA de consulta.",
        listingAreaTitle: "Explore as melhores areas",
        listingAreaText: "Use cards de area e notas locais para deixar a busca clara.",
        listingTrustTitle: "Confianca antes da consulta",
        listingTrustText: "Mostre detalhes verificados, contato claro e disponibilidade atualizada.",
        listingContactTitle: "Perguntar sobre um anuncio",
        listingContactText: "Envie o anuncio, orcamento, localizacao e metodo de contato preferido.",
        listingCategories: ["Casas", "Alugueis", "Comercial", "Terrenos", "Carros", "Destaques"],
        listingLocations: ["Centro", "Zona norte", "Distrito oeste", "Perto de escolas", "Frente a agua", "Zona comercial"],
        listingTrustItems: ["Detalhes verificados", "Disponibilidade atualizada", "Precos claros", "Suporte local", "Resposta rapida", "Comparacao simples"],
        company: "Empresa",
        services: "Servicos",
        process: "Processo",
        servicesSlug: "/servicos",
        processSlug: "/empresa",
        requestConsultation: "Solicitar consulta",
        viewServices: "Ver servicos",
        corporateHeadline: (name) => `${name} apresenta uma empresa clara e confiavel`,
        corporateSubheadline: (description) => description || "Um site profissional focado em clareza, servicos, prova e contato direto.",
        corporateServicesTitle: "Servicos e capacidades",
        corporateServicesText: "Uma visao clara do que a empresa faz, quem ajuda e quais resultados pode entregar.",
        corporateProcessTitle: "Um processo simples da primeira conversa ate a entrega",
        corporateProcessText: "Mostre como a empresa entende a necessidade, recomenda o caminho certo e entrega com responsabilidade.",
        corporateProcessItems: ["Entender o objetivo", "Planejar a solucao certa", "Entregar com comunicacao clara", "Apoiar o proximo passo"],
        corporateProofTitle: "Construido com confianca e trabalho mensuravel",
        corporateProofText: "Use esta secao para credibilidade, experiencia, certificacoes, tipos de clientes ou padroes de trabalho.",
        corporateProofItems: ["Entrega confiavel", "Comunicacao clara", "Padroes profissionais"],
        corporateContactText: "Envie uma mensagem para falar sobre servicos, disponibilidade, precos ou um projeto personalizado.",
        offer: "Oferta",
        benefits: "Beneficios",
        proof: "Prova",
        faq: "FAQ",
        offerSlug: "/oferta",
        proofSlug: "/prova",
        claimOffer: "Comecar agora",
        seeProof: "Ver prova",
        funnelHeadline: (name) => `${name} transforma interesse em clientes reais`,
        funnelSubheadline: (description) => description || "Uma landing focada em uma oferta clara, prova e proximo passo direto.",
        funnelBenefitsTitle: "Por que esta oferta funciona",
        funnelBenefitsText: "Mostre os resultados principais para que o visitante entenda o valor antes de entrar em contato.",
        funnelBenefitsItems: ["Promessa clara", "Proximo passo simples", "Feito para leads qualificados", "Facil de editar", "Estrutura com prova", "Lancamento rapido"],
        funnelOfferTitle: "Tudo para dar o proximo passo",
        funnelOfferText: "Apresente a oferta, pacote, consulta ou servico em uma estrutura focada em conversao.",
        funnelGuarantee: "Expectativas claras antes do cliente se comprometer.",
        funnelProofTitle: "Prova que reduz duvidas",
        funnelProofText: "Use resultados, depoimentos, padroes ou antes/depois para criar confianca.",
        funnelProofItems: ["Resultado especifico para o cliente", "Processo simples", "Resposta rapida", "Conversa clara sobre preco"],
        funnelFaqTitle: "Perguntas antes de comecar",
        funnelFaqItems: [
          { question: "O que acontece depois de enviar a solicitacao?", answer: "O negocio responde com o proximo passo, orcamento, agendamento ou consulta." },
          { question: "A oferta pode ser personalizada?", answer: "Sim. A oferta, textos, secoes e caminho de contato sao editaveis." },
          { question: "Isso e uma loja?", answer: "Nao. Esta versao foca em leads, solicitacoes, reservas ou orcamentos." }
        ],
        funnelContactText: "Envie uma solicitacao e o negocio pode responder com o proximo passo, orcamento ou detalhes de agendamento.",
        premiumHeadline: (name) => `Conhe\xE7a ${name}`,
        premiumSubheadline: (description) => description || "Uma experi\xEAncia de produto refinada, simples, confiante e memor\xE1vel.",
        premiumPrimary: "Explorar produtos",
        premiumSecondary: "Ver hist\xF3ria",
        premiumStoryTitle: "Criado em torno dos detalhes",
        premiumStoryText: "Uma hist\xF3ria de produto focada, com texto limpo, confian\xE7a visual e espa\xE7o para cada benef\xEDcio aparecer.",
        premiumFeatureTitle: "Uma experi\xEAncia principal",
        premiumFeatureText: "Use esta se\xE7\xE3o para o benef\xEDcio principal, hist\xF3ria de fabrica\xE7\xE3o, promessa de desempenho ou motivo mais forte para comprar.",
        premiumGalleryTitle: "Sele\xE7\xE3o curada",
        premiumGalleryText: "Um cat\xE1logo menor apresentado com mais cuidado, melhor hierarquia visual e valor claro.",
        premiumSpecsTitle: "O importante, f\xE1cil de comparar",
        premiumSpecsText: "Destaque materiais, garantia, entrega, suporte, personaliza\xE7\xE3o ou qualidade do servi\xE7o sem poluir a p\xE1gina.",
        premiumSpecItems: ["Apresenta\xE7\xE3o refinada", "Hist\xF3ria edit\xE1vel", "Suporte premium", "Pronto para publicar"],
        academy: "Academia",
        programs: "Programas",
        curriculum: "Curriculo",
        enroll: "Inscricao",
        programsSlug: "/programas",
        curriculumSlug: "/curriculo",
        enrollSlug: "/inscricao",
        courseAcademy: "Academia de cursos",
        enrollNow: "Inscrever-se",
        viewCurriculum: "Ver curriculo",
        coursePrice: "Preco de inscricao",
        featuredProgram: "Programa destaque",
        beginnerFriendly: "Para iniciantes",
        certificateReady: "Com certificado",
        educationHeadline: (name) => `${name}: aprenda com um caminho claro`,
        educationSubheadline: (description) => description || "Um site premium de academia com cursos, modulos, resultados, confianca do instrutor e inscricao.",
        learningPathTitle: "Um caminho claro da primeira aula ao resultado",
        learningPathText: "Mostre roteiro, modulos e marcos para que o aluno entenda como vai evoluir.",
        learningPathItems: ["Base", "Pratica guiada", "Aplicacao", "Suporte", "Resultado", "Proximo passo"],
        programsTitle: "Programas criados para resultados reais",
        programsText: "Apresente cursos, coaching, bootcamps ou assinaturas com duracao, nivel e CTA.",
        outcomesTitle: "O que o aluno deve conseguir fazer",
        outcomesText: "Transforme a promessa do curso em resultados, habilidades e sinais claros de progresso.",
        learningOutcomeItems: ["Progressao clara", "Atividades praticas", "Curriculo editavel", "Prova para alunos", "Notas de suporte", "Oferta pronta"],
        instructorTitle: "Guia especialista, estruturada para agir",
        instructorText: "Use este espaco para credibilidade, metodo, suporte, certificacoes ou comunidade.",
        instructorTrustItems: ["Aulas com especialista", "Modulos estruturados", "Caminho de suporte", "Resultados dos alunos", "Acesso flexivel", "Oferta editavel"],
        enrollmentTitle: "Pronto para comecar?",
        enrollmentText: "Convide o aluno a se inscrever, pedir acesso, consultar turmas ou escolher o programa certo.",
        educationCategories: ["Base", "Avancado", "Workshop", "Coaching", "Certificacao", "Assinatura"],
        educationDurations: ["4 semanas", "6 modulos", "Turma ao vivo", "No seu ritmo"],
        modules: "Modulos",
        modulesSlug: "/modulos",
        viewProducts: "Ver produtos",
        digitalPrice: "Preco editavel",
        digitalHeadline: (name) => `${name}: produtos digitais com acesso imediato`,
        digitalSubheadline: (description) => description || "Loja polida para cursos, templates, software, assinaturas e packs baixaveis.",
        digitalBundleTitle: "Ofertas digitais prontas para vender",
        digitalBundleText: "Apresente produtos como bundles claros com acesso imediato, entregaveis, bonus e suporte.",
        digitalModulesTitle: "O que o cliente recebe",
        digitalModulesText: "Mostre modulos, downloads, aulas, templates, licencas e suporte em uma estrutura clara.",
        digitalModuleItems: ["Treinamento principal", "Recursos baixaveis", "Templates e ferramentas", "Material bonus", "Instrucoes de acesso", "Notas de suporte"],
        digitalProofTitle: "Confianca antes do checkout",
        digitalProofText: "Use provas, resultados, garantia ou autoridade do criador para tornar a compra segura.",
        digitalProofItems: ["Acesso imediato", "Modulos editaveis", "Licenca clara", "Suporte pronto", "Valor do bundle", "Checkout simples"],
        digitalAccessTitle: "Obtenha acesso e comece agora",
        digitalAccessText: "Depois da compra ou solicitacao, o cliente sabe exatamente o que recebe, como acessar e onde pedir suporte.",
        digitalCategories: ["Curso", "Template", "Toolkit", "Assinatura", "Software", "Bundle"],
        newDrop: "Novo drop",
        collections: "Cole\xE7\xF5es",
        lookbook: "Lookbook",
        fashionHeadline: (name) => `Novo drop ${name}`,
        fashionSubheadline: (description) => description || "Uma loja visual de roupas feita para cole\xE7\xF5es, estilo e compra r\xE1pida.",
        shopTheDrop: "Comprar o drop",
        viewLookbook: "Ver lookbook",
        collectionsText: "Organize produtos por drop, caimento, cor, temporada ou cole\xE7\xE3o.",
        dropStoryTitle: "Criado para a forma de usar",
        dropStoryText: "Use esta se\xE7\xE3o para explicar o mood, materiais, caimento e hist\xF3ria visual da cole\xE7\xE3o.",
        lookbookText: "Mostre looks, momentos lifestyle, fotos de campanha ou combina\xE7\xF5es de produtos.",
        newArrivals: "Novidades",
        newArrivalsText: "Uma se\xE7\xE3o de compra para o drop atual, mais vendidos e pe\xE7as prontas para vender.",
        fitGuideTitle: "Caimento, tamanhos e estilo",
        fitGuideText: "D\xEA confian\xE7a ao cliente para escolher tamanho, material, caimento e completar o look.",
        fitGuideItems: ["Guia de tamanhos", "Notas de caimento", "Materiais", "Complete o look"],
        jewelryCategories: ["Colares", "Pulseiras", "Brincos", "Aneis", "Kits presente", "Pecas personalizadas"]
      }
    };
    return { ...copies.en, ...copies[language] || {} };
  }
  function showGeneratedClientPreview() {
    if (!isPublicClientSetup) return;
    if (isEmbeddedClientSetup) {
      window.parent.postMessage({ type: "luma-generated-preview" }, "*");
    }
    document.body.classList.add("generated-preview-open", "client-preview-mode");
    document.body.classList.remove("review-details-open", "final-review-mode", "manual-form-open", "draft-adjust-open");
    syncLyraExperienceMode();
    guidedPanel2.classList.remove("active");
    storageStatus2.textContent = builderState.currentSiteId ? t("generatedOpenAI") : t("generatedOpenAI");
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  async function submitGeneratedDraftForReview() {
    if (!builderState.currentSchema) {
      storageStatus2.textContent = langText({ en: "Generate a draft first.", es: "Primero genera un borrador.", fr: "G\xE9n\xE9rez d'abord un brouillon.", pt: "Gere primeiro um rascunho." });
      return;
    }
    if (!builderState.currentSiteId) {
      storageStatus2.textContent = langText({
        en: "The fast draft is visible here, but it does not have a site_id for review yet.",
        es: "El borrador rapido se ve aqui, pero no tiene site_id para revisi\xF3n todav\xEDa.",
        fr: "Le brouillon rapide est visible ici, mais il n'a pas encore de site_id pour r\xE9vision.",
        pt: "O rascunho r\xE1pido est\xE1 vis\xEDvel aqui, mas ainda n\xE3o tem site_id para revis\xE3o."
      });
      return;
    }
    const contact = builderState.guidedState.contactInfo || {};
    const customerName = builderState.guidedState.businessName || builderState.currentSchema.business?.name || "Client";
    const message = langText({
      en: `Client submitted draft for review.
Business: ${customerName}
Industry: ${builderState.guidedState.industry || ""}
Desired domain: ${builderState.guidedState.desiredDomain || ""}
Site ID: ${builderState.currentSiteId}`,
      es: `Cliente envi\xF3 borrador para revisi\xF3n.
Negocio: ${customerName}
Industria: ${builderState.guidedState.industry || ""}
Dominio deseado: ${builderState.guidedState.desiredDomain || ""}
Site ID: ${builderState.currentSiteId}`,
      fr: `Client a envoy\xE9 le brouillon pour r\xE9vision.
Entreprise: ${customerName}
Secteur: ${builderState.guidedState.industry || ""}
Domaine souhait\xE9: ${builderState.guidedState.desiredDomain || ""}
Site ID: ${builderState.currentSiteId}`,
      pt: `Cliente enviou o rascunho para revis\xE3o.
Neg\xF3cio: ${customerName}
Setor: ${builderState.guidedState.industry || ""}
Dom\xEDnio desejado: ${builderState.guidedState.desiredDomain || ""}
Site ID: ${builderState.currentSiteId}`
    });
    submitDraftReviewButton.disabled = true;
    submitDraftReviewButton.textContent = langText({ en: "Sending...", es: "Enviando...", fr: "Envoi...", pt: "Enviando..." });
    try {
      const response = await fetch(`${API_BASE_URL}/public/leads`, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({
          siteId: builderState.currentSiteId,
          businessId: builderState.currentBusinessId,
          customerName,
          email: contact.email || contact.correo || "",
          phone: contact.phone || contact.whatsapp || "",
          message,
          source: "client_generated_draft_review"
        })
      });
      if (!response.ok) throw new Error(await readErrorMessage(response));
      storageStatus2.textContent = langText({
        en: "Sent for review. Your request is now visible in admin.",
        es: "Enviado para revisi\xF3n. Tu solicitud ya aparece en el admin.",
        fr: "Envoy\xE9 pour r\xE9vision. Votre demande appara\xEEt maintenant dans l'admin.",
        pt: "Enviado para revis\xE3o. Sua solicita\xE7\xE3o j\xE1 aparece no admin."
      });
      if (isEmbeddedClientSetup) {
        window.parent.postMessage({ type: "luma-draft-submitted", siteId: builderState.currentSiteId }, "*");
      }
    } catch (error) {
      storageStatus2.textContent = `${langText({ en: "Could not send", es: "No se pudo enviar", fr: "Impossible d'envoyer", pt: "N\xE3o foi poss\xEDvel enviar" })}: ${shortError(error.message)}`;
    } finally {
      submitDraftReviewButton.disabled = false;
      submitDraftReviewButton.textContent = langText({ en: "Send for review", es: "Enviar para revisi\xF3n", fr: "Envoyer pour r\xE9vision", pt: "Enviar para revis\xE3o" });
    }
  }
  function persistPendingStudioAccountAction(action) {
    try {
      localStorage.setItem("lumaPendingAuthAction", action);
      if (builderState.currentSchema) {
        localStorage.setItem("lumaPendingGeneratedSite", JSON.stringify({
          schema: builderState.currentSchema,
          siteId: builderState.currentSiteId,
          businessId: builderState.currentBusinessId,
          selectedPageKey: builderState.selectedPageKey,
          savedAt: (/* @__PURE__ */ new Date()).toISOString()
        }));
      }
    } catch {
    }
  }
  function isValidWorkspaceEmail(email) {
    return /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(String(email || "").trim());
  }
  function adjustGeneratedDraftWithLuma() {
    document.body.classList.add("generated-preview-open", "client-preview-mode", "draft-adjust-open");
    document.body.classList.remove("review-details-open", "final-review-mode", "manual-form-open");
    syncLyraExperienceMode();
    guidedPanel2.classList.add("active");
    document.body.classList.remove("guided-modal-open");
    builderState.guidedStep = "review";
    const message = langText({
      en: "Of course. Tell me what you want to change in the draft: colors, sections, copy, products, style, or any detail. Then I can generate a new version with those adjustments.",
      es: "Claro. Dime qu\xE9 quieres cambiar del borrador: colores, secciones, textos, productos, estilo o cualquier detalle. Luego genero una nueva versi\xF3n con esos ajustes.",
      fr: "Bien s\xFBr. Dites-moi ce que vous voulez changer dans le brouillon : couleurs, sections, textes, produits, style ou tout autre d\xE9tail. Ensuite je g\xE9n\xE8re une nouvelle version.",
      pt: "Claro. Diga o que deseja mudar no rascunho: cores, se\xE7\xF5es, textos, produtos, estilo ou qualquer detalhe. Depois gero uma nova vers\xE3o."
    });
    appendChatMessage("assistant", message, "thinking");
    guidedStatusText.textContent = langText({
      en: "Describe the adjustments you want.",
      es: "Describe los ajustes que quieres hacer.",
      fr: "D\xE9crivez les ajustements souhait\xE9s.",
      pt: "Descreva os ajustes que deseja."
    });
    guidedReply2.focus();
    if (isEmbeddedClientSetup) {
      window.parent.postMessage({ type: "luma-adjusting-draft" }, "*");
    }
  }
  function salesFlowFromIntakeFollowupAnswer(value = "") {
    const text = normalizeTemplateIntentText(value);
    if (!text) return "";
    if (/\b(online_sales|online sales|vender online|venta online|sell online|ecommerce|e-commerce|tienda online|shop online)\b/.test(text)) return "online_sales";
    if (/\b(booking|bookings|book appointment|appointment|appointments|reserva|reservas|cita|citas|agendar|agenda)\b/.test(text)) return "booking";
    if (/\b(quote_request|quote|quotes|cotizacion|cotización|cotizaciones|presupuesto|presupuestos)\b/.test(text)) return "quote_request";
    if (/\b(lead_capture|lead|leads|captar clientes|captar prospectos|contactos)\b/.test(text)) return "lead_capture";
    if (/\b(informational|informativo|presentar informacion|presentar información|solo informacion|solo información)\b/.test(text)) return "informational";
    return "";
  }
  async function collectPayload() {
    if (isPublicClientSetup) syncTemplateSelectionFromGuidedContext();
    const aiStudioPlan = isPublicClientSetup ? refreshAiStudioPlanFromContext() : builderState.guidedState.aiStudioPlan;
    const data = new FormData(form2);
    const intakeFollowupAnswer = data.get("server_intake_reply")?.toString().trim() || "";
    const intakeFollowupField = builderState.pendingServerIntakeGate?.missing_fields?.[0] || "";
    const baseIndustryValue = data.get("industry")?.toString().trim() || builderState.guidedState.industry || "";
    const followupSalesFlowValue = intakeFollowupField === "sales_flow" || intakeFollowupField === "niche" ? salesFlowFromIntakeFollowupAnswer(intakeFollowupAnswer) : "";
    const industryValue = intakeFollowupField === "niche" && intakeFollowupAnswer ? [baseIndustryValue, intakeFollowupAnswer].filter(Boolean).join(". ") : baseIndustryValue;
    const preferredToneValue = data.get("preferred_tone")?.toString().trim() || (intakeFollowupField === "brand_style" ? intakeFollowupAnswer : "");
    const formPreferredColorsValue = splitCommaOrLines(data.get("preferred_colors")?.toString() || "");
    const rawLogoPreferenceValue = data.get("logo_preference")?.toString().trim() || builderState.guidedState.logoPreference || (intakeFollowupField === "logo" ? intakeFollowupAnswer : "");
    const logoPreferenceValue = logoPreferenceFromText(rawLogoPreferenceValue, { assumeLogoContext: true }) || rawLogoPreferenceValue;
    if (logoPreferenceValue) {
      builderState.guidedState.logoPreference = logoPreferenceValue;
      if (logoPreferenceValue === "generate_ai_logo") builderState.guidedState.aiGeneratedLogoRequested = true;
    }
    const fieldMeta = { ...builderState.guidedState.fieldMeta || {} };
    if (intakeFollowupField === "niche" && intakeFollowupAnswer) {
      builderState.guidedState.industry = industryValue;
      fieldMeta.industry = { source: "explicit_user_choice", confidence: 1 };
      fieldMeta.niche = { source: "explicit_user_choice", confidence: 1 };
    }
    if (followupSalesFlowValue) {
      builderState.guidedState.salesFlow = followupSalesFlowValue;
      builderState.guidedState.salesMode = followupSalesFlowValue;
      fieldMeta.salesFlow = { source: "explicit_user_choice", confidence: 1 };
      fieldMeta.sales_flow = { source: "explicit_user_choice", confidence: 1 };
    }
    if (intakeFollowupField === "brand_style" && preferredToneValue) {
      builderState.guidedState.preferredTone = preferredToneValue;
      fieldMeta.preferredTone = { source: "explicit_user_choice", confidence: 1 };
      fieldMeta.brand_style = { source: "explicit_user_choice", confidence: 1 };
    }
    if (logoPreferenceValue) {
      fieldMeta.logo = fieldMeta.logo || { source: "explicit", confidence: 1 };
      fieldMeta.logoPreference = fieldMeta.logoPreference || { source: "explicit", confidence: 1 };
    }
    builderState.guidedState.fieldMeta = fieldMeta;
    const validatedGuidedPayload = isPublicClientSetup ? guidedStateForApi() : null;
    const generationBusinessName = validatedGuidedPayload?.businessName || data.get("business_name")?.toString().trim();
    const generationBusinessDescription = validatedGuidedPayload?.businessDescription || data.get("business_description")?.toString().trim();
    const generationIndustry = validatedGuidedPayload?.industry || industryValue;
    const generationLocation = validatedGuidedPayload?.location || data.get("location")?.toString().trim();
    const generationServicesProducts = validatedGuidedPayload ? arrayValue2(validatedGuidedPayload.servicesProducts) : splitCommaOrLines(data.get("services_products")?.toString() || "");
    const generationTargetAudience = validatedGuidedPayload?.targetAudience || data.get("target_audience")?.toString().trim();
    const generationPreferredTone = validatedGuidedPayload?.preferredTone || preferredToneValue;
    const resolvedSalesFlow = validatedGuidedPayload ? validatedGuidedPayload.salesFlow || validatedGuidedPayload.salesMode || "" : followupSalesFlowValue || builderState.guidedState.salesFlow || builderState.guidedState.salesMode || data.get("sales_flow")?.toString().trim() || "";
    const preferredColorsValue = validatedGuidedPayload ? arrayValue2(validatedGuidedPayload.preferredColors) : formPreferredColorsValue;
    const contactInfo = validatedGuidedPayload ? validatedGuidedPayload.contactInfo || {} : parseKeyValueLines(data.get("contact_info")?.toString() || "");
    const logoUrl = validatedGuidedPayload ? validatedGuidedPayload.logoUrl || "" : data.get("logo_url")?.toString().trim();
    const photoUrls = validatedGuidedPayload ? arrayValue2(validatedGuidedPayload.photoUrls) : splitLines(data.get("photo_urls")?.toString() || "");
    const assets = [];
    if (logoUrl) {
      assets.push({ asset_type: "logo", label: "Logo", url: logoUrl });
    }
    photoUrls.forEach((url, index) => {
      assets.push({ asset_type: "photo", label: `Photo ${index + 1}`, url });
    });
    arrayValue2(builderState.guidedState.videoUrls).filter(isCloudSafeUrl).forEach((url, index) => {
      assets.push({ asset_type: "video", label: `Video ${index + 1}`, url });
    });
    const logoFile = data.get("logo_file");
    if (logoFile instanceof File && logoFile.size > 0) {
      assets.push({ asset_type: "logo", label: "Uploaded logo", url: await uploadAssetOrFallback(logoFile, "logo", "Uploaded logo") });
    }
    const photoFiles = data.getAll("photo_files").filter((file) => file instanceof File && file.size > 0);
    for (const [index, file] of photoFiles.entries()) {
      const assetType = file.type?.startsWith("video/") ? "video" : "photo";
      assets.push({ asset_type: assetType, label: `Uploaded ${assetType} ${index + 1}`, url: await uploadAssetOrFallback(file, assetType, `Uploaded ${assetType} ${index + 1}`) });
    }
    const sitePlan = builderState.guidedState.sitePlan || (builderState.forcedTemplateSelection?.templateId ? buildSitePlan(builderState.forcedTemplateSelection) : null);
    if (sitePlan && aiStudioPlan) sitePlan.aiStudioPlan = aiStudioPlan;
    const payloadBrand = normalizeBrand(builderState.guidedState.brand || {
      logoUrl: assets.find((asset) => asset.asset_type === "logo")?.url || "",
      extractedColors: arrayValue2(builderState.guidedState.logoPalette),
      preferredColors: preferredColorsValue,
      industry: generationIndustry,
      tone: generationPreferredTone
    });
    const colorProvenance = validatedGuidedPayload?.colorProvenance || buildColorProvenance({
      preferredColors: preferredColorsValue,
      logoPalette: builderState.guidedState.logoPalette,
      localBrand: payloadBrand,
      preferredColorMeta: fieldMeta.preferredColors || {},
      structuredFormInput: !validatedGuidedPayload && preferredColorsValue.length > 0
    });
    const payload = {
      generatedSiteId: builderState.currentSiteId || builderState.clientIntakeSession?.generatedSiteId || builderState.clientIntakeSession?.projectId || builderState.guidedState.generatedSiteId || "",
      projectId: builderState.currentSiteId || builderState.clientIntakeSession?.projectId || builderState.clientIntakeSession?.generatedSiteId || builderState.guidedState.projectId || "",
      business_name: generationBusinessName,
      business_description: generationBusinessDescription,
      industry: generationIndustry,
      location: generationLocation,
      services_products: generationServicesProducts,
      target_audience: generationTargetAudience,
      preferred_tone: generationPreferredTone,
      preferred_colors: preferredColorsValue.length ? preferredColorsValue : arrayValue2(builderState.guidedState.logoPalette),
      brandStyle: generationPreferredTone,
      contact_info: contactInfo,
      logoPreference: logoPreferenceValue,
      fieldMeta: validatedGuidedPayload?.fieldMeta || fieldMeta,
      intakeFollowupAnswer,
      salesFlow: resolvedSalesFlow,
      desiredDomain: data.get("desired_domain")?.toString().trim() || builderState.guidedState.desiredDomain || "",
      selectedLanguage: builderState.selectedLanguage,
      request_id: builderState.currentRequestId,
      catalog_items: catalogItemsFromForm(),
      assets,
      logoPalette: arrayValue2(builderState.guidedState.logoPalette),
      colorProvenance,
      brand: payloadBrand,
      designStrategy: {
        ...createDesignStrategy({
          business_name: generationBusinessName,
          business_description: generationBusinessDescription,
          industry: generationIndustry,
          target_audience: generationTargetAudience,
          preferred_tone: generationPreferredTone,
          salesMode: resolvedSalesFlow
        }),
        aiStudioPlan,
        selectedTemplateId: aiStudioPlan?.recommendedTemplateId || "",
        selectedTemplateReason: aiStudioPlan?.reasoningSummary || "",
        selectedCatalogType: aiStudioPlan?.recommendedCatalogType || ""
      },
      aiStudioPlan,
      qualityRules: DESIGN_QUALITY_RULES,
      requestedAdjustments: arrayValue2(builderState.guidedState.requestedAdjustments),
      sitePlan,
      sitePlanApproved: Boolean(builderState.guidedState.sitePlanApproved),
      designVariantOffset: Math.max(0, Number(builderState.guidedState.designVariantOffset) || 0),
      brandContextNote: "Intake answers are client intent and design strategy context. Use them to create polished website copy, but do not copy internal planning answers literally unless they are natural public-facing text."
    };
    const revisionInstructions = buildRevisionInstructions();
    if (revisionInstructions) {
      payload.revisionMode = revisionInstructions.mode;
      payload.requestedAdjustments = revisionInstructions.requestedAdjustments;
      payload.revisionInstructions = revisionInstructions;
      payload.previousSchema = builderState.currentSchema;
    }
    return payload;
  }
  async function saveCurrentSchema() {
    if (!builderState.currentSchema) {
      storageStatus2.textContent = "Generate a site before saving.";
      return;
    }
    if (!builderState.currentSiteId) {
      storageStatus2.textContent = "Cannot save: database did not return a site id.";
      return;
    }
    const response = await fetch(`${API_BASE_URL}/sites/${builderState.currentSiteId}/schema`, {
      method: "PUT",
      headers: adminHeaders({ "content-type": "application/json" }),
      body: JSON.stringify({ schema: builderState.currentSchema, catalog_items: catalogItemsForApi() })
    });
    if (response.status === 401) {
      storageStatus2.textContent = "Admin token required to save.";
      return;
    }
    const result = await response.json();
    storageStatus2.textContent = result.storage_status === "stored" ? "Saved to database" : result.storage_status;
  }
  async function publishCurrentSite() {
    if (!builderState.currentSchema) {
      storageStatus2.textContent = "Generate a site before publishing.";
      return;
    }
    if (!builderState.currentSiteId) {
      storageStatus2.textContent = "Cannot publish: database did not return a site id.";
      return;
    }
    const response = await fetch(`${API_BASE_URL}/sites/${builderState.currentSiteId}/publish`, {
      method: "POST",
      headers: adminHeaders({ "content-type": "application/json" }),
      body: JSON.stringify({ schema: builderState.currentSchema, catalog_items: catalogItemsForApi() })
    });
    if (response.status === 401) {
      storageStatus2.textContent = "Admin token required to publish.";
      return;
    }
    const result = await response.json();
    storageStatus2.innerHTML = result.public_url ? `Published \xB7 <a href="${escapeAttribute(result.public_url)}" target="_blank" rel="noreferrer">Open public URL</a>` : result.storage_status;
  }
  function storageLabel(status, usedDevMock) {
    const mockLabel = usedDevMock ? " \xB7 development mock" : "";
    if (status === "stored") return `Saved draft to database${mockLabel}`;
    if (status === "supabase_not_configured") return `Generated, database not configured${mockLabel}`;
    if (status === "instant_template_fallback") return "Fast editable draft shown while AI generation is unavailable";
    return `${status}${mockLabel}`;
  }
  function hydrateFromSelectedRequest() {
    const raw = sessionStorage.getItem("selectedClientRequest");
    if (!raw) return;
    try {
      const request = JSON.parse(raw);
      builderState.currentRequestId = request.id;
      if (request.selectedLanguage || request.selected_language) {
        setSelectedLanguage(request.selectedLanguage || request.selected_language);
      }
      setInputValue("business_name", request.client);
      setInputValue("industry", request.industry);
      setInputValue("location", request.location || "");
      setInputValue("business_description", request.description || "");
      setInputValue("services_products", (request.products || []).join("\n"));
      setInputValue("target_audience", request.budget || "");
      setInputValue("preferred_tone", request.tone || "");
      setInputValue("preferred_colors", (request.colors || []).join(", "));
      setInputValue(
        "contact_info",
        [
          `email: ${request.contact || ""}`,
          `phone: ${request.phone || ""}`,
          `instagram: ${request.social?.instagram || ""}`
        ].join("\n")
      );
      statusText.textContent = `Loaded request ${request.id}.`;
    } catch {
      statusText.textContent = "Could not load selected request.";
    }
  }
  function setInputValue(name, value) {
    const field = form2.elements.namedItem(name);
    if (!field) return;
    field.value = value;
  }
  function catalogItemsFromForm() {
    return splitCommaOrLines(new FormData(form2).get("services_products")?.toString() || "").map((name, index) => ({
      id: `seed_${index + 1}`,
      name,
      description: "",
      category: "",
      variants: "",
      price_type: "quote_only",
      price_value: null,
      price_label: "Ask for pricing",
      image_url: "",
      button_label: "Request info",
      is_active: true,
      is_featured: index < 3,
      sort_order: index + 1
    }));
  }
  function catalogItemsFromSchema(schema) {
    return (schema.products_services || []).map((item, index) => ({
      id: item.id || `catalog_${index + 1}`,
      name: item.name || "Catalog item",
      description: item.description || "",
      category: item.category || "",
      variants: item.variants || "",
      price_type: item.price_type || "quote_only",
      price_value: item.price_value ?? null,
      price_label: item.price_label || "",
      image_url: item.image_url || "",
      button_label: item.button_label || "Request info",
      is_active: item.is_active !== false,
      is_featured: item.is_featured ?? index < 3,
      sort_order: item.sort_order || index + 1
    }));
  }
  function catalogItemsForApi() {
    return builderState.currentCatalogItems.map((item, index) => ({
      ...item,
      price_value: item.price_value === "" || item.price_value === void 0 ? null : Number(item.price_value),
      sort_order: item.sort_order || index + 1,
      is_active: item.is_active !== false,
      is_featured: Boolean(item.is_featured)
    }));
  }
  function renderEditor() {
    if (!builderState.currentSchema) return;
    editorMount.classList.toggle("advanced-inspector-open", builderState.advancedInspectorOpen);
    const pageOptions = builderState.currentSchema.pages.map(
      (page) => `<option value="${escapeAttribute(page.page_key)}" ${page.page_key === builderState.selectedPageKey ? "selected" : ""}>${escapeHtml(page.title)}</option>`
    ).join("");
    editorMount.innerHTML = `
    ${studioInspector()}
    <div class="editor-group">
      <h3>Design options</h3>
      <div class="variant-grid">${designVariantCards()}</div>
    </div>
    ${brandKitEditor(builderState.currentSchema.brand, builderState.currentSchema.design_score)}
    <div class="editor-group">
      <label>Editing page<select id="pageSelector">${pageOptions}</select></label>
      <div class="row-actions">
        <button class="small-button" data-move-page="up">Move page up</button>
        <button class="small-button" data-move-page="down">Move page down</button>
      </div>
    </div>
    <div class="editor-group">
      <h3>Brand</h3>
      ${inputField("Business name", "business.name", builderState.currentSchema.business.name)}
      ${textareaField("Description", "business.description", builderState.currentSchema.business.description)}
      ${inputField("Logo URL", "global_components.logo_url", builderState.currentSchema.global_components.logo_url || "")}
      ${inputField("Brand primary", "brand.primaryColor", builderState.currentSchema.brand?.primaryColor || "")}
      ${inputField("Brand accent", "brand.accentColor", builderState.currentSchema.brand?.accentColor || "")}
      ${inputField("Button color", "brand.buttonColor", builderState.currentSchema.brand?.buttonColor || "")}
    </div>
    <div class="editor-group">
      <h3>Colors</h3>
      ${inputField("Background", "theme.colors.background", builderState.currentSchema.theme.colors.background)}
      ${inputField("Primary", "theme.colors.primary", builderState.currentSchema.theme.colors.primary)}
      ${inputField("Secondary", "theme.colors.secondary", builderState.currentSchema.theme.colors.secondary)}
      ${inputField("Text", "theme.colors.text", builderState.currentSchema.theme.colors.text)}
    </div>
    <div class="editor-group">
      <h3>Contact</h3>
      ${Object.entries(builderState.currentSchema.contact || {}).map(([key, value]) => inputField(key, `contact.${key}`, value)).join("")}
    </div>
    <div class="editor-group">
      <h3>Catalog Manager</h3>
      <p class="mini-note">The public site renders products from catalog_items, not hardcoded page JSON.</p>
      ${builderState.currentCatalogItems.map(catalogItemEditor).join("")}
      <button class="small-button" id="addCatalogItemButton" type="button">Add catalog item</button>
    </div>
    <div class="editor-group">
      <h3>Sections</h3>
      ${selectedPage().sections.map(sectionEditor).join("")}
      <button class="small-button" id="addSectionButton" type="button">Add section</button>
    </div>
  `;
    editorMount.querySelector("#toggleAdvancedInspector")?.addEventListener("click", () => {
      builderState.advancedInspectorOpen = !builderState.advancedInspectorOpen;
      renderEditor();
    });
    editorMount.querySelectorAll("[data-studio-add-product]").forEach((button) => {
      button.addEventListener("click", addStudioCatalogItem);
    });
    editorMount.querySelector("#pageSelector").addEventListener("change", (event) => {
      builderState.selectedPageKey = event.target.value;
      renderEditor();
      renderPreview();
    });
    editorMount.querySelectorAll("[data-variant-id]").forEach((button) => {
      button.addEventListener("click", () => {
        builderState.selectedVariantId = button.dataset.variantId;
        renderEditor();
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-path]").forEach((input) => {
      input.addEventListener("input", () => {
        setPath(builderState.currentSchema, input.dataset.path, normalizeEditedValue(input.dataset.path, input.value));
        if (input.dataset.path.startsWith("brand.") || input.dataset.path === "global_components.logo_url") {
          builderState.currentSchema.brand = normalizeBrand({
            ...builderState.currentSchema.brand || {},
            logoUrl: builderState.currentSchema.global_components?.logo_url || builderState.currentSchema.brand?.logoUrl || ""
          });
          builderState.currentSchema = applyBrandSystemToSchema(
            builderState.currentSchema,
            builderState.currentSchema.brand,
            { forceBrandOverride: true }
          );
        }
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-catalog-path]").forEach((input) => {
      input.addEventListener("input", () => {
        const value = input.type === "checkbox" ? input.checked : input.value;
        setPath({ catalog_items: builderState.currentCatalogItems }, input.dataset.catalogPath, value);
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-catalog-image-upload]").forEach((input) => {
      input.addEventListener("change", async () => {
        const file = input.files?.[0];
        if (!file) return;
        const imageUrl = await uploadAssetOrFallback(file, "catalog", "Catalog item image");
        setPath({ catalog_items: builderState.currentCatalogItems }, input.dataset.catalogImageUpload, imageUrl);
        renderEditor();
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-remove-catalog-item]").forEach((button) => {
      button.addEventListener("click", () => {
        builderState.currentCatalogItems = builderState.currentCatalogItems.filter((item) => item.id !== button.dataset.removeCatalogItem);
        resequenceCatalog();
        renderEditor();
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-move-catalog-item]").forEach((button) => {
      button.addEventListener("click", () => {
        moveCatalogItem(button.dataset.moveCatalogItem, button.dataset.direction);
        renderEditor();
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-section-path]").forEach((input) => {
      input.addEventListener("input", () => {
        setPath(builderState.currentSchema, input.dataset.sectionPath, normalizeEditedValue(input.dataset.sectionPath, input.value));
        renderPreview();
      });
    });
    editorMount.querySelectorAll("[data-remove-section]").forEach((button) => {
      button.addEventListener("click", () => {
        const page = selectedPage();
        page.sections = page.sections.filter((section) => section.id !== button.dataset.removeSection);
        resequence(page.sections);
        renderEditor();
        renderPreview();
      });
    });
    editorMount.querySelector("#addSectionButton").addEventListener("click", () => {
      const page = selectedPage();
      page.sections.push(createSectionByType("FeatureBand", page.sections.length + 1));
      renderEditor();
      renderPreview();
    });
    editorMount.querySelector("#addCatalogItemButton").addEventListener("click", () => {
      builderState.currentCatalogItems.push({
        id: `catalog_${Date.now()}`,
        name: "New item",
        description: "Edit this product or service.",
        category: "",
        variants: "",
        price_type: "fixed",
        price_value: "",
        price_label: "Precio editable",
        image_url: "",
        button_label: "Ver producto",
        inventory_quantity: "",
        track_inventory: true,
        is_active: true,
        is_featured: false,
        sort_order: builderState.currentCatalogItems.length + 1
      });
      renderEditor();
      renderPreview();
    });
    editorMount.querySelectorAll("[data-move-page]").forEach((button) => {
      button.addEventListener("click", () => {
        movePage(button.dataset.movePage);
        renderEditor();
        renderPreview();
      });
    });
  }
  function renderPreview() {
    if (!builderState.currentSchema) return;
    applyGeneratedFavicon(builderState.currentSchema);
    previewFrame2.innerHTML = renderWebsite2(schemaForPreview(), builderState.selectedPageKey);
    renderStudioProgress();
    previewFrame2.querySelectorAll("[data-page-link]").forEach((link) => {
      link.addEventListener("click", (event) => {
        event.preventDefault();
        builderState.selectedPageKey = link.dataset.pageLink;
        renderEditor();
        renderPreview();
      });
    });
    previewFrame2.querySelectorAll("[data-studio-section]").forEach((sectionElement) => {
      sectionElement.addEventListener("click", (event) => {
        if (event.target.closest("a, button, input, select, textarea, form")) return;
        event.preventDefault();
        event.stopPropagation();
        selectStudioSection(sectionElement.dataset.studioSection);
      });
    });
    bindCatalogSearchInteractions(previewFrame2);
  }
  function bindCatalogSearchInteractions(root) {
    const applyFilter = (value = "") => {
      const query = normalizeTemplateIntentText(value);
      const items = [...root.querySelectorAll("[data-catalog-item]")];
      items.forEach((item) => {
        const searchable = normalizeTemplateIntentText(item.dataset.catalogSearch || "");
        const category = normalizeTemplateIntentText(item.dataset.catalogItemCategory || "");
        item.hidden = Boolean(query) && !searchable.includes(query) && category !== query;
      });
      root.querySelectorAll("[data-catalog-group]").forEach((group) => {
        group.hidden = Boolean(query) && !group.querySelector("[data-catalog-item]:not([hidden])");
      });
    };
    root.querySelectorAll("[data-catalog-search-form]").forEach((form3) => {
      form3.addEventListener("submit", (event) => {
        event.preventDefault();
        applyFilter(new FormData(form3).get("catalog-search") || "");
      });
    });
    root.querySelectorAll("[data-catalog-category]").forEach((button) => {
      button.addEventListener("click", () => applyFilter(button.dataset.catalogCategory || ""));
    });
  }
  function renderSchemaPreviewInto(schema, containerElement, payload = {}, templateSelection = null) {
    if (!schema || !containerElement) return null;
    const preparedSchema = prepareWebsiteConfig(schema, payload, templateSelection);
    const defaultPageKey = preparedSchema.pages?.find((page) => page.page_key === "home")?.page_key || preparedSchema.pages?.[0]?.page_key || "home";
    const hadClientPreviewMode = document.body.classList.contains("client-preview-mode");
    const previousSchema = builderState.currentSchema;
    const renderPage = (pageKey = defaultPageKey) => {
      builderState.currentSchema = preparedSchema;
      document.body.classList.add("client-preview-mode");
      try {
        containerElement.innerHTML = renderWebsite2(preparedSchema, pageKey);
      } finally {
        builderState.currentSchema = previousSchema;
        document.body.classList.toggle("client-preview-mode", hadClientPreviewMode);
      }
      containerElement.querySelectorAll("[data-page-link]").forEach((link) => {
        link.addEventListener("click", (event) => {
          event.preventDefault();
          renderPage(link.dataset.pageLink || defaultPageKey);
        });
      });
      bindCatalogSearchInteractions(containerElement);
    };
    renderPage(defaultPageKey);
    return preparedSchema;
  }
  window.KreatonBuilderRenderer = {
    ...window.KreatonBuilderRenderer || {},
    prepareWebsiteConfig,
    renderSchemaPreviewInto
  };
  function pageQuickInspector() {
    return `<div class="studio-inspector-grid">
    ${inputField("Business name", "business.name", builderState.currentSchema.business?.name || "")}
    ${inputField("Logo URL", "global_components.logo_url", builderState.currentSchema.global_components?.logo_url || "")}
    ${inputField("Brand primary", "brand.primaryColor", builderState.currentSchema.brand?.primaryColor || "")}
    ${inputField("Button color", "brand.buttonColor", builderState.currentSchema.brand?.buttonColor || "")}
  </div>`;
  }
  function addStudioCatalogItem() {
    if (!builderState.currentSchema) return;
    builderState.currentSchema.catalog_items = arrayValue2(builderState.currentSchema.catalog_items);
    builderState.currentSchema.catalog_items.push({
      id: `item_${Date.now()}`,
      sku: `SKU-${builderState.currentSchema.catalog_items.length + 1}`,
      name: "Nuevo producto",
      description: "Edita la descripci\xF3n del producto.",
      category: "",
      variants: "",
      price_type: "fixed",
      price_value: "",
      price_amount: "",
      currency: "USD",
      price_label: "Precio editable",
      button_label: "Ver producto",
      inventory_quantity: "",
      track_inventory: true,
      image_url: "",
      is_active: true,
      is_featured: builderState.currentSchema.catalog_items.length < 3,
      sort_order: builderState.currentSchema.catalog_items.length
    });
    builderState.currentSchema = prepareWebsiteConfig(builderState.currentSchema, { brand: builderState.currentSchema.brand || builderState.guidedState.brand || {} }, null);
    builderState.currentCatalogItems = catalogItemsFromSchema(builderState.currentSchema);
    renderEditor();
    renderPreview();
  }
  function cssEscape(value) {
    if (window.CSS?.escape) return CSS.escape(String(value || ""));
    return String(value || "").replace(/["\\]/g, "\\$&");
  }
  function applyGeneratedFavicon(schema) {
    const favicon = schema?.global_components?.favicon_url || schema?.brand?.logoUrl || "";
    if (!favicon) return;
    let link = document.querySelector("link[data-generated-favicon]");
    if (!link) {
      link = document.createElement("link");
      link.rel = "icon";
      link.dataset.generatedFavicon = "true";
      document.head.appendChild(link);
    }
    link.href = favicon;
  }
  function designVariantCards() {
    const variants = builderState.currentSchema.design_variants || [];
    if (!variants.length) {
      return `<p class="empty">Generate again to receive 3 AI design options.</p>`;
    }
    return variants.map(
      (variant, index) => {
        const preset = TEMPLATE_PRESETS[index % TEMPLATE_PRESETS.length];
        return `<button class="variant-card ${variant.id === builderState.selectedVariantId ? "active" : ""}" data-variant-id="${escapeAttribute(variant.id)}" type="button">
        <span class="variant-swatch" style="background:${resolveColor(variant.theme?.colors?.primary, "#008060")}"></span>
        <strong>${escapeHtml(variant.name)}</strong>
        <small>${escapeHtml(preset.name)} \xB7 ${escapeHtml(variant.description)}</small>
      </button>`;
      }
    ).join("");
  }
  function schemaForPreview() {
    const variant = selectedVariant();
    if (!variant) return ensureStorefrontExperience(structuredClone(builderState.currentSchema));
    const preset = selectedTemplatePreset();
    const schema = structuredClone(builderState.currentSchema);
    schema.theme = variant.theme || schema.theme;
    if (Array.isArray(builderState.currentCatalogItems) && builderState.currentCatalogItems.length) {
      schema.catalog_items = builderState.currentCatalogItems;
    }
    schema.layout_mode = { ...schema.layout_mode, id: variant.layout_mode_id || schema.layout_mode?.id };
    schema.active_design_variant = variant;
    if (!schema.active_template?.id && !schema.selected_template?.id) {
      schema.active_template = preset;
    }
    schema.pages = schema.pages.map((page) => ({
      ...page,
      sections: page.sections.map((section) => {
        if (section.type === "Hero") {
          return { ...section, settings: { ...section.settings, layout: preset.heroLayout || variant.hero_layout || section.settings?.layout || "image_right" } };
        }
        if (["ProductGrid", "ServiceList"].includes(section.type)) {
          return {
            ...section,
            settings: {
              ...section.settings,
              layout: preset.productLayout || variant.product_layout || section.settings?.layout || "grid",
              spacing: section.settings?.spacing || preset.density
            }
          };
        }
        return section;
      })
    }));
    return ensureStorefrontExperience(schema);
  }
  function ensureStorefrontExperience(schema) {
    if (!schema) return schema;
    schema.pages = Array.isArray(schema.pages) ? schema.pages : [];
    schema.navigation = Array.isArray(schema.navigation) ? schema.navigation : [];
    schema.catalog_items = normalizePreviewCatalogItems(schema);
    const catalogItems = schema.catalog_items.filter((item) => item.is_active !== false);
    if (!catalogItems.length) return schema;
    const language = schema.business?.selectedLanguage || builderState.selectedLanguage || "en";
    const labels = {
      en: { nav: "Shop", title: "Shop", slug: "/shop", featured: "Featured products", subtitle: "Explore the products and options available from this business." },
      es: { nav: "Tienda", title: "Tienda", slug: "/tienda", featured: "Productos destacados", subtitle: "Explora los productos y opciones disponibles para comprar o solicitar." },
      fr: { nav: "Boutique", title: "Boutique", slug: "/boutique", featured: "Produits phares", subtitle: "D\xE9couvrez les produits et options disponibles." },
      pt: { nav: "Loja", title: "Loja", slug: "/loja", featured: "Produtos em destaque", subtitle: "Explore os produtos e op\xE7\xF5es dispon\xEDveis." }
    }[language] || { nav: "Shop", title: "Shop", slug: "/shop", featured: "Featured products", subtitle: "Explore the products and options available from this business." };
    let catalogPage = schema.pages.find(
      (page) => /catalog|shop|store|tienda|boutique|loja|products|productos|produits|produtos|services|servicios/i.test(
        `${page.page_key || ""} ${page.title || ""} ${page.slug || ""}`
      )
    );
    if (!catalogPage) {
      catalogPage = {
        page_key: "catalog",
        title: labels.title,
        slug: labels.slug,
        order: schema.pages.length + 1,
        sections: []
      };
      schema.pages.push(catalogPage);
    }
    catalogPage.sections = Array.isArray(catalogPage.sections) ? catalogPage.sections : [];
    const hasCatalogSection = catalogPage.sections.some((section) => ["ProductGrid", "ServiceList"].includes(section.type));
    if (!hasCatalogSection) {
      catalogPage.sections.unshift({
        id: "catalog_auto",
        type: "ProductGrid",
        order: 1,
        editable: {
          title: labels.title,
          headline: labels.title,
          subtitle: labels.subtitle,
          text: labels.subtitle,
          primary_button: "",
          secondary_button: "",
          image_url: "",
          images: []
        },
        settings: { layout: "grid", source: "catalog", columns: 3 }
      });
    }
    const homePage = schema.pages.find((page) => /home|inicio/i.test(`${page.page_key || ""} ${page.title || ""}`)) || schema.pages[0];
    if (homePage) {
      homePage.sections = Array.isArray(homePage.sections) ? homePage.sections : [];
      const homeHasCatalog = homePage.sections.some((section) => ["ProductGrid", "ServiceList"].includes(section.type));
      if (!homeHasCatalog) {
        homePage.sections.push({
          id: "featured_catalog_auto",
          type: "ProductGrid",
          order: homePage.sections.length + 1,
          editable: {
            title: labels.featured,
            headline: labels.featured,
            subtitle: "",
            text: "",
            primary_button: "",
            secondary_button: "",
            image_url: "",
            images: []
          },
          settings: { layout: "featured", source: "catalog", columns: 3 }
        });
      }
    }
    if (!schema.navigation.some((item) => item.page_key === catalogPage.page_key)) {
      const navItem = { label: labels.nav, page_key: catalogPage.page_key };
      schema.navigation.length ? schema.navigation.splice(1, 0, navItem) : schema.navigation.push(navItem);
    }
    schema.pages = schema.pages.map((page, index) => ({ ...page, order: Number(page.order || index + 1) })).sort((a, b) => a.order - b.order);
    return schema;
  }
  function normalizePreviewCatalogItems(schema) {
    const labels = catalogLocaleLabels(schema);
    const existing = Array.isArray(schema.catalog_items) && schema.catalog_items.length ? schema.catalog_items : Array.isArray(schema.products_services) ? schema.products_services : [];
    return existing.map((item, index) => {
      if (typeof item === "string") {
        return {
          id: `item_${index + 1}`,
          sku: `SKU-${index + 1}`,
          name: item,
          description: "",
          category: "",
          variants: "",
          price_type: "fixed",
          price_value: "",
          price_amount: "",
          currency: "USD",
          price_label: "",
          button_label: labels.view,
          inventory_quantity: "",
          track_inventory: false,
          image_url: "",
          is_active: true,
          is_featured: index < 3,
          sort_order: index
        };
      }
      return {
        id: item.id || `item_${index + 1}`,
        sku: item.sku || `SKU-${index + 1}`,
        name: item.name || item.title || `Item ${index + 1}`,
        description: item.description || item.text || "",
        category: item.category || "",
        variants: item.variants || "",
        price_type: item.price_type || "fixed",
        price_value: item.price_value ?? item.price_amount ?? item.priceAmount ?? "",
        price_amount: item.price_amount ?? item.price_value ?? item.priceAmount ?? "",
        currency: item.currency || "USD",
        price_label: item.price_label || item.price || item.priceLabel || "",
        button_label: item.button_label || item.cta || labels.view,
        inventory_quantity: item.inventory_quantity ?? item.stock ?? "",
        track_inventory: Boolean(item.track_inventory || item.inventory_quantity || item.stock),
        image_url: item.image_url || item.imageUrl || "",
        is_active: item.is_active !== false,
        is_featured: item.is_featured ?? index < 3,
        sort_order: Number(item.sort_order ?? index)
      };
    });
  }
  function selectedVariant() {
    const variants = builderState.currentSchema?.design_variants || [];
    return variants.find((variant) => variant.id === builderState.selectedVariantId) || variants[0] || null;
  }
  function selectedTemplatePreset() {
    const variants = builderState.currentSchema?.design_variants || [];
    const index = Math.max(0, variants.findIndex((variant) => variant.id === builderState.selectedVariantId));
    return TEMPLATE_PRESETS[index % TEMPLATE_PRESETS.length];
  }
  function catalogItemEditor(item, index) {
    const basePath = `catalog_items.${index}`;
    return `<div class="editor-group catalog-item-editor">
    <h4>${index + 1}. ${escapeHtml(item.name || "Catalog item")}</h4>
    <div class="control-grid">
      ${inputField("SKU", `${basePath}.sku`, item.sku || "", "data-catalog-path")}
      ${inputField("Name", `${basePath}.name`, item.name, "data-catalog-path")}
      ${inputField("Category", `${basePath}.category`, item.category || "", "data-catalog-path")}
      ${inputField("Variants", `${basePath}.variants`, item.variants || "", "data-catalog-path")}
      ${selectGenericField("Price type", `${basePath}.price_type`, item.price_type || "quote_only", ["fixed", "starting_at", "quote_only"], "data-catalog-path")}
      ${inputField("Price value", `${basePath}.price_value`, item.price_value ?? "", "data-catalog-path")}
      ${inputField("Currency", `${basePath}.currency`, item.currency || "USD", "data-catalog-path")}
      ${inputField("Price label", `${basePath}.price_label`, item.price_label, "data-catalog-path")}
      ${inputField("Inventory", `${basePath}.inventory_quantity`, item.inventory_quantity ?? "", "data-catalog-path")}
      ${inputField("Image URL", `${basePath}.image_url`, item.image_url, "data-catalog-path")}
      <label>Upload image<input data-catalog-image-upload="${escapeAttribute(`${basePath}.image_url`)}" type="file" accept="image/*"></label>
      ${checkboxField("Track stock", `${basePath}.track_inventory`, Boolean(item.track_inventory), "data-catalog-path")}
      ${checkboxField("Active", `${basePath}.is_active`, item.is_active !== false, "data-catalog-path")}
      ${checkboxField("Featured", `${basePath}.is_featured`, Boolean(item.is_featured), "data-catalog-path")}
    </div>
    ${textareaField("Description", `${basePath}.description`, item.description, "data-catalog-path")}
    ${inputField("Button", `${basePath}.button_label`, item.button_label, "data-catalog-path")}
    <div class="row-actions">
      <button class="small-button" data-move-catalog-item="${escapeAttribute(item.id)}" data-direction="up" type="button">Move up</button>
      <button class="small-button" data-move-catalog-item="${escapeAttribute(item.id)}" data-direction="down" type="button">Move down</button>
      <button class="small-button" data-remove-catalog-item="${escapeAttribute(item.id)}" type="button">Delete</button>
    </div>
  </div>`;
  }
  function inputField(label, path, value, attr = "data-path") {
    return `<label>${escapeHtml(label)}<input ${attr}="${escapeAttribute(path)}" value="${escapeAttribute(value || "")}"></label>`;
  }
  function textareaField(label, path, value, attr = "data-path") {
    return `<label>${escapeHtml(label)}<textarea rows="3" ${attr}="${escapeAttribute(path)}">${escapeHtml(value || "")}</textarea></label>`;
  }
  function selectField(label, path, value, options) {
    return `<label>${escapeHtml(label)}<select data-section-path="${escapeAttribute(path)}">${options.map((option) => `<option value="${escapeAttribute(option)}" ${option === value ? "selected" : ""}>${escapeHtml(option)}</option>`).join("")}</select></label>`;
  }
  function selectGenericField(label, path, value, options, attr) {
    return `<label>${escapeHtml(label)}<select ${attr}="${escapeAttribute(path)}">${options.map((option) => `<option value="${escapeAttribute(option)}" ${option === value ? "selected" : ""}>${escapeHtml(option)}</option>`).join("")}</select></label>`;
  }
  function checkboxField(label, path, checked, attr) {
    return `<label class="checkbox-row"><input ${attr}="${escapeAttribute(path)}" type="checkbox" ${checked ? "checked" : ""}>${escapeHtml(label)}</label>`;
  }
  function selectedPage() {
    return builderState.currentSchema.pages.find((page) => page.page_key === builderState.selectedPageKey) || builderState.currentSchema.pages[0];
  }
  function pageIndex() {
    return builderState.currentSchema.pages.findIndex((page) => page.page_key === builderState.selectedPageKey);
  }
  function movePage(direction) {
    const index = pageIndex();
    const next = direction === "up" ? index - 1 : index + 1;
    if (next < 0 || next >= builderState.currentSchema.pages.length) return;
    const pages = builderState.currentSchema.pages;
    [pages[index], pages[next]] = [pages[next], pages[index]];
    resequence(pages);
  }
  function resequence(items) {
    items.forEach((item, index) => {
      item.order = index + 1;
    });
  }
  function moveCatalogItem(id, direction) {
    const index = builderState.currentCatalogItems.findIndex((item) => item.id === id);
    const next = direction === "up" ? index - 1 : index + 1;
    if (index < 0 || next < 0 || next >= builderState.currentCatalogItems.length) return;
    [builderState.currentCatalogItems[index], builderState.currentCatalogItems[next]] = [builderState.currentCatalogItems[next], builderState.currentCatalogItems[index]];
    resequenceCatalog();
  }
  function resequenceCatalog() {
    builderState.currentCatalogItems.forEach((item, index) => {
      item.sort_order = index + 1;
    });
  }
  function setPath(target, path, value) {
    const parts = path.split(".");
    let cursor = target;
    parts.slice(0, -1).forEach((part) => {
      const key = Number.isInteger(Number(part)) ? Number(part) : part;
      if (cursor[key] === void 0) {
        cursor[key] = {};
      }
      cursor = cursor[key];
    });
    cursor[parts.at(-1)] = value;
  }
  function parseKeyValueLines(value) {
    return Object.fromEntries(
      splitLines(value).map((line) => {
        const [key, ...rest] = line.split(":");
        return [key.trim(), rest.join(":").trim()];
      })
    );
  }
  function splitCommaOrLines(value) {
    return value.split(/[\n,]/).map((item) => item.trim()).filter(Boolean);
  }
  function splitLines(value) {
    return value.split("\n").map((line) => line.trim()).filter(Boolean);
  }
  async function fileToOptimizedDataUrl(file, assetType = "photo") {
    if (!file?.type?.startsWith("image/") || file.type === "image/svg+xml" || file.size < 28e4) {
      return fileToDataUrl(file);
    }
    const source = await fileToDataUrl(file);
    const image = await loadImage(source);
    const maxSide = assetType === "logo" ? 900 : assetType === "catalog" ? 1200 : 1600;
    const scale = Math.min(1, maxSide / Math.max(image.naturalWidth || image.width, image.naturalHeight || image.height));
    const canvas = document.createElement("canvas");
    canvas.width = Math.max(1, Math.round((image.naturalWidth || image.width) * scale));
    canvas.height = Math.max(1, Math.round((image.naturalHeight || image.height) * scale));
    const context = canvas.getContext("2d", { alpha: assetType === "logo" });
    context.drawImage(image, 0, 0, canvas.width, canvas.height);
    return canvas.toDataURL(assetType === "logo" ? "image/png" : "image/jpeg", assetType === "logo" ? 0.92 : 0.82);
  }
  function fileToDataUrl(file) {
    return new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.addEventListener("load", () => resolve(reader.result));
      reader.addEventListener("error", () => reject(reader.error));
      reader.readAsDataURL(file);
    });
  }
  function slugify2(value) {
    return String(value || "default").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
  }
})();
